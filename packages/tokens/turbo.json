{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["tokens/**", "src/**", "tsdown.config.ts", "tsconfig*.json"],
      "outputs": ["dist/**", "src/generated/**"]
    }
  }
}
