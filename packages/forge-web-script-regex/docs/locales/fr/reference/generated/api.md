# Référence API @mission-platform/forge-web-script-regex

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/forge-web-script-regex/docs/reference/generated/api.md: [packages/forge-web-script-regex/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/forge-web-script-regex`.

## `src/bytecode`

### CompiléRegex

**Genre :** interface

```typescript
export interface CompiledRegex
```

Représentation déterministe intégrée par un backend Forge.

### FORGE_REGEX_BYTECODE_VERSION

**Genre :** constante

```typescript
export const FORGE_REGEX_BYTECODE_VERSION;
```

Version stable du contrat de bytecode Forge regex partagé.

### INSTR_WIDTH

**Genre :** constante

```typescript
export const INSTR_WIDTH;
```

Largeur, en opérandes entiers de 32 bits, de chaque instruction.

### Opération

**Genre :** constante

```typescript
export const Op;
```

Opcodes consommés par le backend Forge et reflétés par l'oracle de référence.

### Opcode

**Genre :** type

```typescript
export type Opcode = (typeof Op)[keyof typeof Op];
```

Aucune description fournie.

## `src/compiler`

### compileRegex

**Genre :** fonction

```typescript
function compileRegex(pattern: string): CompiledRegex;
```

Compilez le sous-ensemble d’expressions régulières déterministes pris en charge dans le bytecode Forge.

#### Paramètres

| Nom    | Tapez  | Descriptif |
| ------ | ------ | ---------- |
| modèle | chaîne |            |

### Erreur de syntaxe Regex

**Genre :** classe

```typescript
export class RegexSyntaxError extends Error
```

Erreur de syntaxe appartenant au compilateur avec des messages déterministes relatifs à la source.

## `src/reference-vm`

### captureFin

**Genre :** fonction

```typescript
function captureEnd(captures: Captures | null, group: number): number;
```

Lisez une fin de capture sans exposer la disposition des emplacements de bytecode aux appelants.

#### Paramètres

| Nom     | Tapez           | Descriptif |
| ------- | --------------- | ---------- |
| capture | Captures \| nul |            |
| groupe  | numéro          |            |

### Captures

**Genre :** type

```typescript
export type Captures = number[];
```

Les emplacements de capture sont `[start0, end0, start1, end1, ...]` ; `-1` signifie non défini.

### captureDémarrer

**Genre :** fonction

```typescript
function captureStart(captures: Captures | null, group: number): number;
```

Lisez un démarrage de capture sans exposer la disposition des emplacements de bytecode aux appelants.

#### Paramètres

| Nom     | Tapez           | Descriptif |
| ------- | --------------- | ---------- |
| capture | Captures \| nul |            |
| groupe  | numéro          |            |

### match complet

**Genre :** fonction

```typescript
function fullMatch(re: CompiledRegex, input: string): Captures | null;
```

Correspondance de chaîne entière, ancrée à la position zéro.

#### Paramètres

| Nom    | Tapez        | Descriptif |
| ------ | ------------ | ---------- |
| re     | CompiléRegex |            |
| entrée | chaîne       |            |

### préfixeMatch

**Genre :** fonction

```typescript
function prefixMatch(re: CompiledRegex, input: string): Captures | null;
```

Correspondance de préfixe, ancrée à la position zéro mais pas à la fin.

#### Paramètres

| Nom    | Tapez        | Descriptif |
| ------ | ------------ | ---------- |
| re     | CompiléRegex |            |
| entrée | chaîne       |            |

### recherche

**Genre :** fonction

```typescript
function search(re: CompiledRegex, input: string, start = 0): Captures | null;
```

Correspondance la plus à gauche à ou après `start`.

#### Paramètres

| Nom      | Tapez        | Descriptif |
| -------- | ------------ | ---------- |
| re       | CompiléRegex |            |
| entrée   | chaîne       |            |
| démarrer |              |            |

### test

**Genre :** fonction

```typescript
function test(re: CompiledRegex, input: string): boolean;
```

Correspondance booléenne de chaîne entière.

#### Paramètres

| Nom    | Tapez        | Descriptif |
| ------ | ------------ | ---------- |
| re     | CompiléRegex |            |
| entrée | chaîne       |            |
