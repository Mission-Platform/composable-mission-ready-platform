# הפניה ל-@mission-platform/forge-web-script-regex API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/forge-web-script-regex/docs/reference/generated/api.md: [packages/forge-web-script-regex/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/forge-web-script-regex`.

## `src/bytecode`

### CompiledRegex

**סוג:** ממשק

```typescript
export interface CompiledRegex
```

ייצוג דטרמיניסטי מוטבע על ידי Forge backend.

### FORGE_REGEX_BYTECODE_VERSION

**סוג:** קבוע

```typescript
export const FORGE_REGEX_BYTECODE_VERSION
```

גרסה יציבה של חוזה קוד בתים משותף של Forge regex.

### INSTR_WIDTH

**סוג:** קבוע

```typescript
export const INSTR_WIDTH
```

רוחב, באופרנדים שלמים של 32 סיביות, של כל הוראה.

### אופ

**סוג:** קבוע

```typescript
export const Op
```

קודים הנצרכים על ידי Forge backend ומשתקפים על ידי האורקל הייחוס.

### Opcode

**סוג:** סוג

```typescript
export type Opcode = (typeof Op)[keyof typeof Op];
```

לא סופק תיאור.

## `src/compiler`

### compileRegex

פונקציה **סוג:**

```typescript
function compileRegex(pattern: string): CompiledRegex
```

קומפל את תת-ערכת ה-Regex הדטרמיניסטית הנתמכת לתוך Forge bytecode.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| דפוס | מחרוזת |  |

### RegexSyntaxError

**סוג:** כיתה

```typescript
export class RegexSyntaxError extends Error
```

שגיאת תחביר בבעלות מהדר עם הודעות דטרמיניסטיות הקשורות למקור.

## `src/reference-vm`

### captureEnd

פונקציה **סוג:**

```typescript
function captureEnd(captures: Captures | null, group: number): number
```

קרא סוף לכידה מבלי לחשוף את פריסת חריץ ה-bytecode למתקשרים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| לוכדת | לוכד \| null |  |
| קבוצה | מספר |  |

### לוכדים

**סוג:** סוג

```typescript
export type Captures = number[];
```

חריצי לכידה הם `[start0, end0, start1, end1, ...]`; `-1` פירושו לא מוגדר.

### captureStart

פונקציה **סוג:**

```typescript
function captureStart(captures: Captures | null, group: number): number
```

קרא התחלת לכידה מבלי לחשוף את פריסת חריץ ה-bytecode למתקשרים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| לוכדת | לוכד \| null |  |
| קבוצה | מספר |  |

### fullMatch

פונקציה **סוג:**

```typescript
function fullMatch(re: CompiledRegex, input: string): Captures | null
```

משחק מיתרים שלמים, מעוגן בעמדה אפס.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מחדש | CompiledRegex |  |
| קלט | מחרוזת |  |

### prefixMatch

פונקציה **סוג:**

```typescript
function prefixMatch(re: CompiledRegex, input: string): Captures | null
```

התאמת קידומת, מעוגנת בעמדה אפס אבל לא בסוף.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מחדש | CompiledRegex |  |
| קלט | מחרוזת |  |

### לְחַפֵּשׂ

פונקציה **סוג:**

```typescript
function search(re: CompiledRegex, input: string, start = 0): Captures | null
```

ההתאמה השמאלית ביותר ב-`start` או אחריו.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מחדש | CompiledRegex |  |
| קלט | מחרוזת |  |
| להתחיל |  |  |

### מִבְחָן

פונקציה **סוג:**

```typescript
function test(re: CompiledRegex, input: string): boolean
```

התאמה בוליאנית של מיתרים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מחדש | CompiledRegex |  |
| קלט | מחרוזת |  |
