# @mission-platform/forge-web-script-regex API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/forge-web-script-regex/docs/reference/generated/api.md: [packages/forge-web-script-regex/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-web-script-regex`.

## `src/bytecode`

### CompiledRegex

**Art:** Schnittstelle

```typescript
export interface CompiledRegex
```

Deterministische Darstellung, eingebettet in ein Forge-Backend.

### FORGE_REGEX_BYTECODE_VERSION

**Art:** konstant

```typescript
export const FORGE_REGEX_BYTECODE_VERSION;
```

Stabile Version des gemeinsam genutzten Forge-Regex-Bytecode-Vertrags.

### INSTR_WIDTH

**Art:** konstant

```typescript
export const INSTR_WIDTH;
```

Breite jeder Anweisung in 32-Bit-Ganzzahloperanden.

### Op

**Art:** konstant

```typescript
export const Op;
```

Opcodes, die vom Forge-Backend verbraucht und vom Referenzorakel gespiegelt werden.

### Opcode

**Art:** Typ

```typescript
export type Opcode = (typeof Op)[keyof typeof Op];
```

Keine Beschreibung angegeben.

## `src/compiler`

### kompilierenRegex

**Art:** Funktion

```typescript
function compileRegex(pattern: string): CompiledRegex;
```

Kompilieren Sie die unterstützte deterministische Regex-Teilmenge in Forge-Bytecode.

#### Parameter

| Name   | Geben Sie    | ein Beschreibung |
| ------ | ------------ | ---------------- |
| Muster | Zeichenfolge |                  |

### RegexSyntaxError

**Art:** Klasse

```typescript
export class RegexSyntaxError extends Error
```

Compilerbedingter Syntaxfehler mit deterministischen, quellenbezogenen Meldungen.

## `src/reference-vm`

### CaptureEnd

**Art:** Funktion

```typescript
function captureEnd(captures: Captures | null, group: number): number;
```

Lesen Sie ein Capture-Ende, ohne das Bytecode-Slot-Layout den Aufrufern zugänglich zu machen.

#### Parameter

| Name    | Geben Sie       | ein Beschreibung |
| ------- | --------------- | ---------------- |
| erfasst | Erfasst \| null |                  |
| Gruppe  | Zahl            |                  |

### Aufnahmen

**Art:** Typ

```typescript
export type Captures = number[];
```

Capture-Slots sind `[start0, end0, start1, end1, ...]`; `-1` bedeutet nicht gesetzt.

### CaptureStart

**Art:** Funktion

```typescript
function captureStart(captures: Captures | null, group: number): number;
```

Lesen Sie einen Capture-Start, ohne das Bytecode-Slot-Layout den Aufrufern zugänglich zu machen.

#### Parameter

| Name    | Geben Sie       | ein Beschreibung |
| ------- | --------------- | ---------------- |
| erfasst | Erfasst \| null |                  |
| Gruppe  | Zahl            |                  |

### fullMatch

**Art:** Funktion

```typescript
function fullMatch(re: CompiledRegex, input: string): Captures | null;
```

Ganzer String-Match, verankert an Position Null.

#### Parameter

| Name    | Geben Sie     | ein Beschreibung |
| ------- | ------------- | ---------------- |
| re      | CompiledRegex |                  |
| Eingabe | Zeichenfolge  |                  |

### PräfixMatch

**Art:** Funktion

```typescript
function prefixMatch(re: CompiledRegex, input: string): Captures | null;
```

Präfixübereinstimmung, verankert an Position Null, aber nicht am Ende.

#### Parameter

| Name    | Geben Sie     | ein Beschreibung |
| ------- | ------------- | ---------------- |
| re      | CompiledRegex |                  |
| Eingabe | Zeichenfolge  |                  |

### suchen

**Art:** Funktion

```typescript
function search(re: CompiledRegex, input: string, start = 0): Captures | null;
```

Übereinstimmung ganz links bei oder nach `start`.

#### Parameter

| Name    | Geben Sie     | ein Beschreibung |
| ------- | ------------- | ---------------- |
| re      | CompiledRegex |                  |
| Eingabe | Zeichenfolge  |                  |
| Start   |               |                  |

### prüfen

**Art:** Funktion

```typescript
function test(re: CompiledRegex, input: string): boolean;
```

Boolesche Übereinstimmung mit der gesamten Zeichenfolge.

#### Parameter

| Name    | Geben Sie     | ein Beschreibung |
| ------- | ------------- | ---------------- |
| re      | CompiledRegex |                  |
| Eingabe | Zeichenfolge  |                  |
