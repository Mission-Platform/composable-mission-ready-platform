# Riferimento API @mission-platform/matrix-code

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/matrix-code/docs/reference/generated/api.md: [packages/matrix-code/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/matrix-code`.

## `src/components/molecules/forge-matrix-code/forge-matrix-code`

### ForgeMatrixCode

**Tipo:** funzione

```typescript
function ForgeMatrixCode(properties: Readonly<MatrixCodeProperties>): MpElement
```

`ForgeMatrixCode`: esegue il rendering di un codice a barre a matrice 2D scansionabile (Data Matrix ECC
200), scritto una volta nel dialetto JSX neutro e compilato direttamente in React
o Vue da `@mission-platform/vite-plugin-forge`.

Il payload (`value`) è codificato interamente sul client dal file
Codificatore {@link encodeMatrix} da `@mission-platform/matrix-code` — a
codificatore privo di dipendenze scritto in Rust e compilato in WebAssembly (automatic
dimensionamento dei simboli e correzione degli errori di Reed-Solomon) - e disegnato in modo nitido e
SVG indipendente dalla risoluzione. Viene emesso un singolo `<path>` per tutti i moduli scuri,
quindi il markup rimane compatto anche per codici di grandi dimensioni. Per una scansione affidabile di
i colori scuri/chiari sono predefiniti su solid nero su bianco anziché sui token del tema,
ma entrambi sono sovrascrivibili tramite `color` / `background`.

I moduli dati possono essere disegnati come quadrati, quadrati arrotondati o punti
(`moduleShape`); il riempimento scuro accetta un `gradient` lineare o radiale a due stop;
e un `logo` centrale opzionale è sovrapposto su una piastra di supporto (mantienilo piccolo in modo
il simbolo viene comunque scansionato, poiché Data Matrix non dispone di correzione degli errori regolabile
livello).

Una barra degli strumenti di azione opzionale (abilitata tramite `showActions` — `true` per ogni
o un oggetto {@link MatrixCodeActions} per selezionarli singolarmente) consente
gli utenti **salvano il codice come immagine PNG**, **copiano l'immagine negli appunti**,
e **copia il valore codificato negli appunti**. Il PNG è rasterizzato sul file
client disegnando l'SVG renderizzato su una tela.

Il rendering `computed` è neutro {@link useMemo}; il caso `error` diventa
la prop di callback `onError` (richiamata quando la codifica fallisce). Possiede il suo stile
attraverso il modulo CSS co-locato `forge-matrix-code.module.scss`.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| proprietà | Sola lettura<MatrixCodeProperties> |  |

### MatrixCodeActions

**Tipo:** interfaccia

```typescript
export interface MatrixCodeActions
```

Quali pulsanti di azione mostra la barra degli strumenti. `true` li abilita tutti.

### MatrixCodeProperties

**Tipo:** interfaccia

```typescript
export interface MatrixCodeProperties
```

Nessuna descrizione fornita.

### MatrixGradiente

**Tipo:** interfaccia

```typescript
export interface MatrixGradient
```

Una sfumatura di colore di due stop applicata ai moduli scuri.

### MatrixLogo

**Tipo:** interfaccia

```typescript
export interface MatrixLogo
```

Un logo centrato sovrapposto al codice (mantienilo piccolo in modo che il simbolo venga comunque scansionato).

### MatrixModuleShape

**Genere:** tipo

```typescript
export type MatrixModuleShape = 'square' | 'rounded' | 'dot';
```

Forma utilizzata per disegnare ciascun modulo del codice.

## `src/decoder/index`

### decodificaMatrix

**Tipo:** funzione

```typescript
function decodeMatrix(matrix: MatrixCode): string | null
```

Decodifica nuovamente un simbolo di matrice 2D nel suo carico utile tramite il decodificatore FWS
artefatto. Restituisce `null` quando il simbolo non è valido, non è supportato o lo è troppo
danneggiato da recuperare.

La correzione degli errori Reed-Solomon ripara un simbolo con un numero limitato di
moduli capovolti, quindi un {@link MatrixCode} leggermente danneggiato continua a decodificare.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| matrice | CodiceMatrice |  |

### decodeMatrixAsync

**Tipo:** funzione

```typescript
function decodeMatrixAsync(matrix: MatrixCode): Promise<string | null>
```

Decodifica un simbolo di matrice 2D in modo asincrono tramite l'artefatto del decodificatore FWS.
Vedi {@link decodeMatrix}.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| matrice | CodiceMatrice |  |

### decodeMatrixAsyncWithErasures

**Tipo:** funzione

```typescript
function decodeMatrixAsyncWithErasures(matrix: MatrixCode, erasures?: ArrayLike<number>): Promise<string | null>
```

Controparte asincrona di {@link decodeMatrixWithErasures}.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| matrice | CodiceMatrice |  |
| cancellature | ArrayLike<number> |  |

### decodeMatrixWithErasures

**Tipo:** funzione

```typescript
function decodeMatrixWithErasures(matrix: MatrixCode, erasures: ArrayLike<number>): string | null
```

Decodifica una matrice contrassegnando i moduli a bassa confidenza come cancellazioni di Reed-Solomon.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| matrice | CodiceMatrice |  |
| cancellature | ArrayLike<number> |  |

## `src/encoder/index`

### codificaMatrix

**Tipo:** funzione

```typescript
function encodeMatrix(symbology: MatrixSymbology, data: string): MatrixCode
```

Codificare `data` in un codice a barre a matrice 2D del dato `symbology`, caricando il
Forgia artefatti codificatori in modo sincrono al primo utilizzo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| simbologia | MatriceSimbologia |  |
| dati | stringa |  |

#### Contrarre

- **@throws:** se il payload non è valido per la simbologia (vuoto o
troppo grande per i simboli supportati).

### codificaMatrixAsync

**Tipo:** funzione

```typescript
function encodeMatrixAsync(symbology: MatrixSymbology, data: string): Promise<MatrixCode>
```

Codificare `data` in un codice a barre a matrice 2D del dato `symbology`, caricando il
Forgia l'artefatto del codificatore in modo asincrono al primo utilizzo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| simbologia | MatriceSimbologia |  |
| dati | stringa |  |

#### Contrarre

- **@throws:** se il payload non è valido per la simbologia.

### MatrixCodice

**Tipo:** interfaccia

```typescript
export interface MatrixCode
```

Un codice a barre a matrice compressa: dimensioni più un elenco di bit del modulo nella riga principale
(`0` = chiaro, `1` = scuro).

### Simbologia della matrice

**Genere:** tipo

```typescript
export type MatrixSymbology = | 'datamatrix' | 'gs1datamatrix' | 'datamatrixrectangular' | 'aztec';
```

Le simbologie di matrice 2D supportate da questo codificatore. Passato come primo argomento
a {@link encodeMatrix}/{@link encodeMatrixAsync}.
