# @mission-platform/matrix-code API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/matrix-code/docs/reference/generated/api.md: [packages/matrix-code/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/matrix-code` 中的公共源声明生成。

## `src/components/molecules/forge-matrix-code/forge-matrix-code`

### 伪造矩阵代码

**种类：**功能

```typescript
function ForgeMatrixCode(properties: Readonly<MatrixCodeProperties>): MpElement
```

`ForgeMatrixCode` — 呈现可扫描的 2D 矩阵条形码（数据矩阵 ECC
200)，用中立的 JSX 方言编写一次并直接编译为 React
或 Vue by `@mission-platform/vite-plugin-forge`。

有效负载 (`value`) 完全由客户端在客户端上编码
来自 `@mission-platform/matrix-code` 的 {@linkencodeMatrix} 编码器 — a
用 Rust 编写并编译为 WebAssembly 的无依赖编码器（自动
符号大小调整和 Reed-Solomon 纠错） - 并绘制为清晰、
与分辨率无关的 SVG。所有暗模块都会发出一个 `<path>`，
因此，即使对于大型代码，标记也保持紧凑。为了可靠地扫描
深色/浅色默认为 solid 白底黑字而不是主题标记，
但两者都可以通过 `color` / `background` 覆盖。

数据模块可以绘制为正方形、圆角正方形或点
（`moduleShape`）；深色填充接受两级线性或径向 `gradient`；
可选的中心 `logo` 覆盖在背板上（保持较小，以便
由于 Data Matrix 没有可调整的纠错功能，符号仍然可以扫描
水平）。

可选操作工具栏（通过 `showActions` — `true` 启用，适用于每个
按钮，或一个 {@link MatrixCodeActions} 对象来单独选择它们）让
用户 **将代码保存为 PNG 图像**，**将图像复制到剪贴板**，
并**将编码值复制到剪贴板**。 PNG 被光栅化在
客户端将渲染的 SVG 绘制到画布上。

`computed` 渲染是中性的 {@link useMemo}； `error` 案例变为
`onError` 回调属性（编码失败时调用）。它有它的风格
通过位于同一位置的 CSS 模块 `forge-matrix-code.module.scss`。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|属性 |只读<MatrixCodeProperties> |  |

### 矩阵代码动作

**种类：**接口

```typescript
export interface MatrixCodeActions
```

工具栏显示哪些操作按钮。 `true` 启用所有这些功能。

### 矩阵代码属性

**种类：**接口

```typescript
export interface MatrixCodeProperties
```

没有提供描述。

### 矩阵梯度

**种类：**接口

```typescript
export interface MatrixGradient
```

应用于深色模块的两级颜色渐变。

### 矩阵标志

**种类：**接口

```typescript
export interface MatrixLogo
```

覆盖在代码上的居中徽标（保持较小，以便符号仍然可以扫描）。

### 矩阵模块形状

**种类：**类型

```typescript
export type MatrixModuleShape = 'square' | 'rounded' | 'dot';
```

用于绘制代码各模块的形状。

## `src/decoder/index`

### 解码矩阵

**种类：**功能

```typescript
function decodeMatrix(matrix: MatrixCode): string | null
```

通过 FWS 解码器将 2D 矩阵符号解码回其有效负载
神器。当符号格式错误、不受支持等时返回 `null`
损坏才能恢复。

里德-所罗门纠错修复了有限数量的符号
翻转模块，因此轻微损坏的 {@link MatrixCode} 仍然可以解码。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|矩阵|矩阵代码 |  |

### 解码矩阵异步

**种类：**功能

```typescript
function decodeMatrixAsync(matrix: MatrixCode): Promise<string | null>
```

通过 FWS 解码器工件异步解码 2D 矩阵符号。
请参阅{@link解码矩阵}。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|矩阵|矩阵代码 |  |

### 解码MatrixAsyncWithErasures

**种类：**功能

```typescript
function decodeMatrixAsyncWithErasures(matrix: MatrixCode, erasures?: ArrayLike<number>): Promise<string | null>
```

{@link debugMatrixWithErasures} 的异步对应项。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|矩阵|矩阵代码 |  |
|删除| ArrayLike<number> |  |

### 带擦除的解码矩阵

**种类：**功能

```typescript
function decodeMatrixWithErasures(matrix: MatrixCode, erasures: ArrayLike<number>): string | null
```

解码矩阵，同时将低置信度模块标记为 Reed-Solomon 擦除。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|矩阵|矩阵代码 |  |
|删除| ArrayLike<number> |  |

## `src/encoder/index`

### 编码矩阵

**种类：**功能

```typescript
function encodeMatrix(symbology: MatrixSymbology, data: string): MatrixCode
```

将 `data` 编码为给定 `symbology` 的二维矩阵条形码，加载
首次使用时同步锻造编码器工件。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|符号学|矩阵符号 |  |
|数据|字符串|  |

#### 合同

- **@throws:** 如果有效负载对于符号系统无效（空，或
对于支持的符号来说太大）。

### 编码矩阵异步

**种类：**功能

```typescript
function encodeMatrixAsync(symbology: MatrixSymbology, data: string): Promise<MatrixCode>
```

将 `data` 编码为给定 `symbology` 的二维矩阵条形码，加载
首次使用时异步伪造编码器工件。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|符号学|矩阵符号 |  |
|数据|字符串|  |

#### 合同

- **@throws:** 如果有效负载对于符号系统无效。

### 矩阵码

**种类：**接口

```typescript
export interface MatrixCode
```

打包矩阵条形码：尺寸加上模块位的行主列表
（`0` = 浅色，`1` = 深色）。

### 矩阵符号学

**种类：**类型

```typescript
export type MatrixSymbology = | 'datamatrix' | 'gs1datamatrix' | 'datamatrixrectangular' | 'aztec';
```

该编码器支持的 2D 矩阵符号体系。作为第一个参数传递
到{@linkencodeMatrix}/{@linkencodeMatrixAsync}。
