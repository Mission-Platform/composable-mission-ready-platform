# @mission-platform/matrix-code API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> packages/matrix-code/docs/reference/generated/api.md: [packages/matrix-code/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/matrix-code`.

## `src/components/molecules/forge-matrix-code/forge-matrix-code`

### ForgeMatrixCode

**Soort:** functie

```typescript
function ForgeMatrixCode(properties: Readonly<MatrixCodeProperties>): MpElement;
```

`ForgeMatrixCode` — geeft een scanbare 2D-matrixbarcode weer (Data Matrix ECC
200), ooit geschreven in het neutrale JSX-dialect en rechtstreeks gecompileerd naar React
of Vue door `@mission-platform/vite-plugin-forge`.

De payload (`value`) wordt volledig op de client gecodeerd door de
{@link encodeMatrix}-encoder van `@mission-platform/matrix-code` — a
afhankelijkheidsvrije encoder geschreven in Rust en gecompileerd naar WebAssembly (automatisch
symboolgrootte en Reed-Solomon-foutcorrectie) — en getekend als een scherpe,
resolutie-onafhankelijke SVG. Voor alle donkere modules wordt een enkele `<path>` uitgezonden,
zodat de opmaak compact blijft, zelfs voor grote codes. Voor betrouwbaar scannen van de
donkere/lichte kleuren zijn standaard solid zwart op wit in plaats van thematokens,
maar beide kunnen worden overschreven via `color` / `background`.

Gegevensmodules kunnen worden getekend als vierkanten, afgeronde vierkanten of punten
(`moduleShape`); de donkere vulling accepteert een twee-stops lineair of radiaal `gradient`;
en een optioneel midden `logo` wordt op een achterplaat gelegd (houd deze klein zodat
het symbool scant nog steeds, aangezien Data Matrix geen instelbare foutcorrectie heeft
niveau).

Een optionele actiewerkbalk (ingeschakeld via `showActions` — `true` voor elke
button, of een {@link MatrixCodeActions}-object om ze afzonderlijk te kiezen) let
gebruikers **sla de code op als een PNG-afbeelding**, **kopieer de afbeelding naar het klembord**,
en **kopieer de gecodeerde waarde naar het klembord**. De PNG wordt gerasterd op de
client door de gerenderde SVG op een canvas te tekenen.

De `computed`-weergave is de neutrale {@link useMemo}; de zaak `error` wordt
de `onError` callback prop (aangeroepen wanneer codering mislukt). Het is eigenaar van zijn stijl
via de naast elkaar geplaatste CSS-module `forge-matrix-code.module.scss`.

#### Parameters

| Naam          | Typ                                | Beschrijving |
| ------------- | ---------------------------------- | ------------ |
| eigenschappen | Alleen-lezen<MatrixCodeProperties> |              |

### MatrixCodeActions

**Soort:** interface

```typescript
export interface MatrixCodeActions
```

Welke actieknoppen de werkbalk toont. `true` maakt ze allemaal mogelijk.

### MatrixCodeEigenschappen

**Soort:** interface

```typescript
export interface MatrixCodeProperties
```

Geen beschrijving opgegeven.

### MatrixGradiënt

**Soort:** interface

```typescript
export interface MatrixGradient
```

Een kleurverloop van twee stops toegepast op de donkere modules.

### MatrixLogo

**Soort:** interface

```typescript
export interface MatrixLogo
```

Een gecentreerd logo over de code heen (houd het klein zodat het symbool nog steeds scant).

### MatrixModuleVorm

**Soort:** type

```typescript
export type MatrixModuleShape = 'square' | 'rounded' | 'dot';
```

Vorm die wordt gebruikt om elke module van de code te tekenen.

## `src/decoder/index`

### decodeMatrix

**Soort:** functie

```typescript
function decodeMatrix(matrix: MatrixCode): string | null;
```

Decodeer een 2D-matrixsymbool terug naar zijn payload via de FWS-decoder
artefact. Retourneert `null` wanneer het symbool een verkeerde vorm heeft, niet wordt ondersteund of ook
beschadigd om te herstellen.

Reed-Solomon-foutcorrectie repareert een symbool met een beperkt aantal
modules omgedraaid, zodat een licht beschadigde {@link MatrixCode} nog steeds decodeert.

#### Parameters

| Naam   | Typ        | Beschrijving |
| ------ | ---------- | ------------ |
| matrix | MatrixCode |              |

### decodeMatrixAsync

**Soort:** functie

```typescript
function decodeMatrixAsync(matrix: MatrixCode): Promise<string | null>;
```

Decodeer een 2D-matrixsymbool asynchroon via het FWS-decoderartefact.
Zie {@link decodeMatrix}.

#### Parameters

| Naam   | Typ        | Beschrijving |
| ------ | ---------- | ------------ |
| matrix | MatrixCode |              |

### decodeMatrixAsyncWithErasures

**Soort:** functie

```typescript
function decodeMatrixAsyncWithErasures(matrix: MatrixCode, erasures?: ArrayLike<number>): Promise<string | null>;
```

Asynchrone tegenhanger van {@link decodeMatrixWithErasures}.

#### Parameters

| Naam   | Typ               | Beschrijving |
| ------ | ----------------- | ------------ |
| matrix | MatrixCode        |              |
| wist   | ArrayLike<number> |              |

### decodeMatrixWithErasures

**Soort:** functie

```typescript
function decodeMatrixWithErasures(matrix: MatrixCode, erasures: ArrayLike<number>): string | null;
```

Decodeer een matrix terwijl u modules met weinig vertrouwen markeert terwijl Reed-Solomon wist.

#### Parameters

| Naam   | Typ               | Beschrijving |
| ------ | ----------------- | ------------ |
| matrix | MatrixCode        |              |
| wist   | ArrayLike<number> |              |

## `src/encoder/index`

### codeMatrix

**Soort:** functie

```typescript
function encodeMatrix(symbology: MatrixSymbology, data: string): MatrixCode;
```

Codeer `data` in een 2D-matrixbarcode van de gegeven `symbology`, waarbij de
Smeed het encoderartefact synchroon bij het eerste gebruik.

#### Parameters

| Naam      | Typ              | Beschrijving |
| --------- | ---------------- | ------------ |
| symboliek | MatrixSymbologie |              |
| gegevens  | tekenreeks       |              |

#### Contract

- **@throws:** als de payload ongeldig is voor de symbologie (leeg of
  te groot voor de ondersteunde symbolen).

### encodeMatrixAsync

**Soort:** functie

```typescript
function encodeMatrixAsync(symbology: MatrixSymbology, data: string): Promise<MatrixCode>;
```

Codeer `data` in een 2D-matrixbarcode van de gegeven `symbology`, waarbij de
Smeed het encoderartefact asynchroon bij het eerste gebruik.

#### Parameters

| Naam      | Typ              | Beschrijving |
| --------- | ---------------- | ------------ |
| symboliek | MatrixSymbologie |              |
| gegevens  | tekenreeks       |              |

#### Contract

- **@throws:** als de payload ongeldig is voor de symbologie.

### MatrixCode

**Soort:** interface

```typescript
export interface MatrixCode
```

Een verpakte matrixbarcode: afmetingen plus een rij-hoofdlijst met modulebits
(`0` = licht, `1` = donker).

### MatrixSymbologie

**Soort:** type

```typescript
export type MatrixSymbology = 'datamatrix' | 'gs1datamatrix' | 'datamatrixrectangular' | 'aztec';
```

De 2D-matrixsymbolieken die deze encoder ondersteunt. Doorgegeven als het eerste argument
naar {@link encodeMatrix} / {@link encodeMatrixAsync}.
