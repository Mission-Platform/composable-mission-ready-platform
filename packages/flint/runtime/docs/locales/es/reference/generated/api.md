# Referencia de API @mission-platform/flint-runtime

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/flint/runtime/docs/reference/generated/api.md: [packages/flint/runtime/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/flint-runtime`.

## `src/abi`

### afirmarValidFlintAbiManifest

**Tipo:** función

```typescript
function assertValidFlintAbiManifest(manifest: FlintAbiManifest): void;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo             | Descripción |
| ---------- | ---------------- | ----------- |
| manifiesto | FlintAbiManifest |             |

### igualFunción

**Tipo:** función

```typescript
function equalFunction(left: FlintAbiFunction, right: FlintAbiFunction): boolean;
```

No se proporciona descripción.

#### Parámetros

| Nombre    | Tipo             | Descripción |
| --------- | ---------------- | ----------- |
| izquierda | Función FlintAbi |             |
| derecha   | Función FlintAbi |             |

### FlintAbiValidationResultado

**Tipo:** interfaz

```typescript
export interface FlintAbiValidationResult
```

No se proporciona descripción.

### validarFlintAbiManifest

**Tipo:** función

```typescript
function validateFlintAbiManifest(manifest: FlintAbiManifest): FlintAbiValidationResult;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo             | Descripción |
| ---------- | ---------------- | ----------- |
| manifiesto | FlintAbiManifest |             |

## `src/async`

### crearFlintAsyncRuntime

**Tipo:** función

```typescript
function createFlintAsyncRuntime(options: FlintAsyncRuntimeOptions = {}): FlintAsyncRuntime;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                     | Descripción |
| -------- | ------------------------ | ----------- |
| opciones | FlintAsyncRuntimeOptions |             |

### FORGE_WEB_SCRIPT_ASYNC_CAPABILITIES

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_ASYNC_CAPABILITIES;
```

No se proporciona descripción.

### Capacidad FlintAsync

**Tipo:** tipo

```typescript
export type FlintAsyncCapability =
  (typeof FORGE_WEB_SCRIPT_ASYNC_CAPABILITIES)[keyof typeof FORGE_WEB_SCRIPT_ASYNC_CAPABILITIES];
```

No se proporciona descripción.

### Ejecución FlintAsync

**Tipo:** interfaz

```typescript
export interface FlintAsyncExecution
```

No se proporciona descripción.

### FlintAsyncExecutionResult

**Tipo:** tipo

```typescript
export type FlintAsyncExecutionResult = FlintAsyncExecution | FlintAsyncFailure;
```

No se proporciona descripción.

### Fallo de FlintAsync

**Tipo:** interfaz

```typescript
export interface FlintAsyncFailure
```

No se proporciona descripción.

### FlintAsyncFailureCode

**Tipo:** tipo

```typescript
export type FlintAsyncFailureCode =
  'capability-denied' | 'host-error' | 'invalid-message' | 'queue-limit' | 'task-error';
```

No se proporciona descripción.

### FlintAsyncHostAdapter

**Tipo:** interfaz

```typescript
export interface FlintAsyncHostAdapter
```

No se proporciona descripción.

### FlintAsyncRuntime

**Tipo:** interfaz

```typescript
export interface FlintAsyncRuntime
```

No se proporciona descripción.

### FlintAsyncRuntimeOptions

**Tipo:** interfaz

```typescript
export interface FlintAsyncRuntimeOptions
```

No se proporciona descripción.

### FlintAsyncScheduledTask

**Tipo:** interfaz

```typescript
export interface FlintAsyncScheduledTask
```

No se proporciona descripción.

### FlintAsyncScheduleResult

**Tipo:** tipo

```typescript
export type FlintAsyncScheduleResult = FlintAsyncScheduledTask | FlintAsyncFailure;
```

No se proporciona descripción.

### FlintAsyncTask

**Tipo:** interfaz

```typescript
export interface FlintAsyncTask
```

No se proporciona descripción.

### FlintAsyncTaskHandler

**Tipo:** tipo

```typescript
export type FlintAsyncTaskHandler = (payload: Uint8Array) => Uint8Array;
```

No se proporciona descripción.

### FlintAsyncTaskKind

**Tipo:** tipo

```typescript
export type FlintAsyncTaskKind = 'microtask' | 'worker';
```

No se proporciona descripción.

### FlintAsyncWorkerMensaje

**Tipo:** interfaz

```typescript
export interface FlintAsyncWorkerMessage
```

No se proporciona descripción.

## `src/collections`

### crearFlintArray

**Tipo:** función

```typescript
function createFlintArray(
  values: readonly TValue[] = [],
  ownership: FlintCollectionOwnership = 'owned',
): FlintArray<TValue>;
```

Cree una matriz fija copiando los valores proporcionados.

#### Parámetros

| Nombre    | Tipo                          | Descripción |
| --------- | ----------------------------- | ----------- |
| valores   | Valor de TV de sólo lectura[] |             |
| propiedad | Propiedad de FlintCollection  |             |

### crearFlintIterator

**Tipo:** función

```typescript
function createFlintIterator(values: Iterable<TValue>, descriptor: FlintIteratorDescriptor): FlintIterator<TValue>;
```

Adapte un iterable a un iterador diferido con el descriptor ABI proporcionado.

#### Parámetros

| Nombre     | Tipo                    | Descripción |
| ---------- | ----------------------- | ----------- |
| valores    | Iterable<TValue>        |             |
| descriptor | FlintIteratorDescriptor |             |

### crearFlintIteratorDescriptor

**Tipo:** función

```typescript
function createFlintIteratorDescriptor(
  id: string,
  elementType = 'unknown',
  capability: FlintIteratorCapability = 'linear',
  ownership: FlintCollectionOwnership = 'owned',
): FlintIteratorDescriptor;
```

Crear metadatos de límites de descriptores; Las personas que llaman heredadas utilizan de forma predeterminada el acceso lineal.

#### Parámetros

| Nombre           | Tipo                         | Descripción |
| ---------------- | ---------------------------- | ----------- |
| identificación   | cadena                       |             |
| tipo de elemento |                              |             |
| capacidad        | Capacidad de FlintIterator   |             |
| propiedad        | Propiedad de FlintCollection |             |

### crearFlintMap

**Tipo:** función

```typescript
function createFlintMap(
  entries: readonly FlintMapEntry<TKey, TValue>[] = [],
  strategy: FlintHashStrategy<TKey> = flintDefaultHashStrategy<TKey>(),
): FlintMap<TKey, TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                                       | Descripción |
| ---------- | ------------------------------------------ | ----------- |
| entradas   | sólo lectura FlintMapEntry<TKey, TValue>[] |             |
| estrategia | FlintHashStrategy<TKey>                    |             |

### crearFlintSet

**Tipo:** función

```typescript
function createFlintSet(
  values: readonly TValue[] = [],
  strategy: FlintHashStrategy<TValue> = flintDefaultHashStrategy<TValue>(),
): FlintSet<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                          | Descripción |
| ---------- | ----------------------------- | ----------- |
| valores    | Valor de TV de sólo lectura[] |             |
| estrategia | FlintHashStrategy<TValue>     |             |

### crearFlintVector

**Tipo:** función

```typescript
function createFlintVector(
  values: readonly TValue[] = [],
  ownership: FlintCollectionOwnership = 'owned',
): FlintVector<TValue>;
```

Cree un vector propio copiando los valores proporcionados.

#### Parámetros

| Nombre    | Tipo                          | Descripción |
| --------- | ----------------------------- | ----------- |
| valores   | Valor de TV de sólo lectura[] |             |
| propiedad | Propiedad de FlintCollection  |             |

### FlintArray

**Tipo:** interfaz

```typescript
export interface FlintArray<TValue>
```

Colección contigua de tamaño fijo; las actualizaciones devuelven un valor de matriz copiado.

### flintArrayGet

**Tipo:** función

```typescript
function flintArrayGet(array: FlintArray<TValue>, index: number): FlintOption<TValue>;
```

Leer un elemento de matriz; los índices no válidos devuelven Option.none.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| matriz | FlintArray<TValue> |             |
| índice | número             |             |

### flintArrayLength

**Tipo:** función

```typescript
function flintArrayLength(array: FlintArray<TValue>): number;
```

Devuelve la longitud de una matriz sin consumir ni copiar sus elementos.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| matriz | FlintArray<TValue> |             |

### flintArraySet

**Tipo:** función

```typescript
function flintArraySet(array: FlintArray<TValue>, index: number, value: TValue): FlintArray<TValue>;
```

Reemplazar un elemento de matriz; los índices no válidos arrojan RangeError.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| matriz | FlintArray<TValue> |             |
| índice | número             |             |
| valor  | Valor de TV        |             |

### flintArrayTrySet

**Tipo:** función

```typescript
function flintArrayTrySet(
  array: FlintArray<TValue>,
  index: number,
  value: TValue,
): FlintResultValue<FlintArray<TValue>>;
```

Reemplazar un elemento de matriz sin tirarlo; los índices no válidos devuelven un resultado de error.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| matriz | FlintArray<TValue> |             |
| índice | número             |             |
| valor  | Valor de TV        |             |

### Propiedad de la colección Flint

**Tipo:** tipo

```typescript
export type FlintCollectionOwnership = 'owned' | 'borrowed' | 'shared';
```

No se proporciona descripción.

### flintDefaultHashStrategy

**Tipo:** función

```typescript
function flintDefaultHashStrategy(): FlintHashStrategy<TValue>;
```

No se proporciona descripción.

### flintError

**Tipo:** constante

```typescript
export const flintError;
```

Construya un valor de resultado de error sin arrojarlo.

### FlintHashEstrategia

**Tipo:** interfaz

```typescript
export interface FlintHashStrategy<TValue>
```

No se proporciona descripción.

### FlintIterable

**Tipo:** tipo

```typescript
export type FlintIterable<TValue> = FlintIterator<TValue> | FlintArray<TValue> | FlintVector<TValue> | Iterable<TValue>;
```

Cualquier fuente aceptada por operaciones de iterador en serie y en paralelo.

### FlintIterador

**Tipo:** interfaz

```typescript
export interface FlintIterator<TValue>
```

Vista de iterador diferido con capacidad de descriptor e indexación directa opcional.

### flintIteratorAgregar

**Tipo:** función

```typescript
function flintIteratorAdd(source: FlintIterator<TValue>, value: TValue): FlintIterator<TValue>;
```

Perezosamente agrega un valor a un iterador.

#### Parámetros

| Nombre | Tipo                  | Descripción |
| ------ | --------------------- | ----------- |
| fuente | FlintIterator<TValue> |             |
| valor  | Valor de TV           |             |

### forjarWebScriptIteratorAt

**Tipo:** función

```typescript
function flintIteratorAt(iterator: FlintIterator<TValue>, index: number): FlintOption<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| iterador | FlintIterator<TValue> |             |
| índice   | número                |             |

### Capacidad de FlintIterator

**Tipo:** tipo

```typescript
export type FlintIteratorCapability = 'linear' | 'random-access';
```

Consumo lineal o acceso aleatorio sin consumo soportado por un iterador.

### flintIteratorCollect

**Tipo:** función

```typescript
function flintIteratorCollect(iterator: FlintIterator<TValue>): FlintVector<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| iterador | FlintIterator<TValue> |             |

### flintIteratorConcat

**Tipo:** función

```typescript
function flintIteratorConcat(first: FlintIterator<TValue>, second: FlintIterable<TValue>): FlintIterator<TValue>;
```

Concatena perezosamente dos fuentes sin cambiar el orden de ninguna de ellas.

#### Parámetros

| Nombre  | Tipo                  | Descripción |
| ------- | --------------------- | ----------- |
| primero | FlintIterator<TValue> |             |
| segundo | FlintIterable<TValue> |             |

### FlintIteratorDescriptor

**Tipo:** interfaz

```typescript
export interface FlintIteratorDescriptor
```

Metadatos ABI que describen la representación, propiedad y capacidad de acceso de los elementos.

### flintIteratorFilter

**Tipo:** función

```typescript
function flintIteratorFilter(
  source: FlintIterator<TValue>,
  predicate: (value: TValue) => boolean,
): FlintIterator<TValue>;
```

Filtrar valores de forma perezosa; el filtrado siempre degrada la capacidad al acceso lineal.

#### Parámetros

| Nombre    | Tipo                        | Descripción |
| --------- | --------------------------- | ----------- |
| fuente    | FlintIterator<TValue>       |             |
| predicado | (valor: TValue) => booleano |             |

### flintIteratorFirst

**Tipo:** función

```typescript
function flintIteratorFirst(iterator: FlintIterator<TValue>): FlintOption<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| iterador | FlintIterator<TValue> |             |

### flintIteratorAplanar

**Tipo:** función

```typescript
function flintIteratorFlatten(source: FlintIterator<FlintIterable<TValue>>): FlintIterator<TValue>;
```

Aplana perezosamente las fuentes anidadas en el orden de las fuentes externas e internas.

#### Parámetros

| Nombre | Tipo                                 | Descripción |
| ------ | ------------------------------------ | ----------- |
| fuente | FlintIterator<FlintIterable<TValue>> |             |

### flintIteratorFold

**Tipo:** función

```typescript
function flintIteratorFold(
  iterator: FlintIterator<TValue>,
  initial: TResult,
  reducer: (accumulator: TResult, value: TValue, index: number) => TResult,
): TResult;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                                                            | Descripción |
| -------- | --------------------------------------------------------------- | ----------- |
| iterador | FlintIterator<TValue>                                           |             |
| inicial  | TResultado                                                      |             |
| reductor | (acumulador: TResult, valor: TValue, índice: número) => TResult |             |

### flintIteratorFromArray

**Tipo:** función

```typescript
function flintIteratorFromArray(
  array: FlintArray<TValue>,
  descriptor: FlintIteratorDescriptor = iteratorDescriptor('array', 'unknown', 'random-access', array.ownership),
): FlintIterator<TValue>;
```

Cree un iterador de acceso aleatorio sobre una matriz fija sin cambiar su propiedad.

#### Parámetros

| Nombre     | Tipo                    | Descripción |
| ---------- | ----------------------- | ----------- |
| matriz     | FlintArray<TValue>      |             |
| descriptor | FlintIteratorDescriptor |             |

### forjarWebScriptIteratorFromIterable

**Tipo:** función

```typescript
function flintIteratorFromIterable(
  values: Iterable<TValue>,
  elementType = 'unknown',
  id = 'iterable',
): FlintIterator<TValue>;
```

Adapte un iterable arbitrario a un iterador lineal diferido de Forge Web Script.

#### Parámetros

| Nombre           | Tipo             | Descripción |
| ---------------- | ---------------- | ----------- |
| valores          | Iterable<TValue> |             |
| tipo de elemento |                  |             |
| identificación   |                  |             |

### forjarWebScriptIteratorFromVector

**Tipo:** función

```typescript
function flintIteratorFromVector(
  vector: FlintVector<TValue>,
  descriptor: FlintIteratorDescriptor = iteratorDescriptor('vector', 'unknown', 'random-access', vector.ownership),
): FlintIterator<TValue>;
```

Cree un iterador de acceso aleatorio sobre un vector sin cambiar su propiedad.

#### Parámetros

| Nombre     | Tipo                    | Descripción |
| ---------- | ----------------------- | ----------- |
| vector     | FlintVector<TValue>     |             |
| descriptor | FlintIteratorDescriptor |             |

### flintIteratorLast

**Tipo:** función

```typescript
function flintIteratorLast(iterator: FlintIterator<TValue>): FlintOption<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| iterador | FlintIterator<TValue> |             |

### flintIteratorMap

**Tipo:** función

```typescript
function flintIteratorMap(
  source: FlintIterator<TValue>,
  map: (value: TValue) => TResult,
  elementType = source.descriptor.elementType,
): FlintIterator<TResult>;
```

Asigna valores de forma perezosa y conserva el acceso aleatorio solo cuando la fuente lo admite.

#### Parámetros

| Nombre           | Tipo                       | Descripción |
| ---------------- | -------------------------- | ----------- |
| fuente           | FlintIterator<TValue>      |             |
| mapa             | (valor: TValue) => TResult |             |
| tipo de elemento |                            |             |

### FlintIteratorResultado

**Tipo:** interfaz

```typescript
export interface FlintIteratorResult<TValue>
```

Un resultado de extracción de iterador empaquetado; hecho sigue siendo cierto después del agotamiento.

### flintIteratorTake

**Tipo:** función

```typescript
function flintIteratorTake(source: FlintIterator<TValue>, count: number): FlintIterator<TValue>;
```

Limite perezosamente una fuente a como máximo valores de recuento.

#### Parámetros

| Nombre | Tipo                  | Descripción |
| ------ | --------------------- | ----------- |
| fuente | FlintIterator<TValue> |             |
| contar | número                |             |

### forjarWebScriptIteratorToArray

**Tipo:** función

```typescript
function flintIteratorToArray(
  iterator: FlintIterator<TValue>,
  ownership: FlintCollectionOwnership = 'owned',
): FlintArray<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre    | Tipo                         | Descripción |
| --------- | ---------------------------- | ----------- |
| iterador  | FlintIterator<TValue>        |             |
| propiedad | Propiedad de FlintCollection |             |

### FlintMapa

**Tipo:** interfaz

```typescript
export interface FlintMap<TKey, TValue>
```

No se proporciona descripción.

### flintMapEliminar

**Tipo:** función

```typescript
function flintMapDelete(map: FlintMap<TKey, TValue>, key: TKey): FlintMap<TKey, TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                   | Descripción |
| ------ | ---------------------- | ----------- |
| mapa   | FlintMap<TKey, TValue> |             |
| clave  | TClave                 |             |

### flintMapEntries

**Tipo:** función

```typescript
function flintMapEntries(map: FlintMap<TKey, TValue>): FlintVector<FlintMapEntry<TKey, TValue>>;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                   | Descripción |
| ------ | ---------------------- | ----------- |
| mapa   | FlintMap<TKey, TValue> |             |

### Entrada de mapa de Flint

**Tipo:** interfaz

```typescript
export interface FlintMapEntry<TKey, TValue>
```

No se proporciona descripción.

### flintMapGet

**Tipo:** función

```typescript
function flintMapGet(map: FlintMap<TKey, TValue>, key: TKey): FlintOption<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                   | Descripción |
| ------ | ---------------------- | ----------- |
| mapa   | FlintMap<TKey, TValue> |             |
| clave  | TClave                 |             |

### forjarWebScriptMapSet

**Tipo:** función

```typescript
function flintMapSet(map: FlintMap<TKey, TValue>, key: TKey, value: TValue): FlintMap<TKey, TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                   | Descripción |
| ------ | ---------------------- | ----------- |
| mapa   | FlintMap<TKey, TValue> |             |
| clave  | TClave                 |             |
| valor  | Valor de TV            |             |

### flintNinguno

**Tipo:** constante

```typescript
export const flintNone;
```

Construya un valor de opción vacío.

### flintOk

**Tipo:** constante

```typescript
export const flintOk;
```

Construya un valor de resultado exitoso.

### Opción Flint

**Tipo:** interfaz

```typescript
export interface FlintOption<TValue>
```

Valor similar a una opción utilizado por lecturas seguras y terminales de iterador vacíos.

### FlintResultValue

**Tipo:** interfaz

```typescript
export interface FlintResultValue<TValue, TError = string>
```

Valor similar a un resultado utilizado por ayudantes de mutación de colección sin captura.

### FlintSet

**Tipo:** interfaz

```typescript
export interface FlintSet<TValue>
```

No se proporciona descripción.

### flintSetAgregar

**Tipo:** función

```typescript
function flintSetAdd(set: FlintSet<TValue>, value: TValue): FlintSet<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo             | Descripción |
| -------- | ---------------- | ----------- |
| conjunto | FlintSet<TValue> |             |
| valor    | Valor de TV      |             |

### flintSetDelete

**Tipo:** función

```typescript
function flintSetDelete(set: FlintSet<TValue>, value: TValue): FlintSet<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo             | Descripción |
| -------- | ---------------- | ----------- |
| conjunto | FlintSet<TValue> |             |
| valor    | Valor de TV      |             |

### flintSetHas

**Tipo:** función

```typescript
function flintSetHas(set: FlintSet<TValue>, value: TValue): boolean;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo             | Descripción |
| -------- | ---------------- | ----------- |
| conjunto | FlintSet<TValue> |             |
| valor    | Valor de TV      |             |

### forjarWebScriptSetValues

**Tipo:** función

```typescript
function flintSetValues(set: FlintSet<TValue>): FlintVector<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo             | Descripción |
| -------- | ---------------- | ----------- |
| conjunto | FlintSet<TValue> |             |

### forjarWebScriptAlgunos

**Tipo:** constante

```typescript
export const flintSome;
```

Construya un valor de opción actual.

### ForjaWebScriptVector

**Tipo:** interfaz

```typescript
export interface FlintVector<TValue>
```

Colección contigua cultivable. Los vectores creados en tiempo de ejecución llevan un identificador de propiedad
contrato mientras que la propiedad prestada/compartida se conserva mediante valores derivados.

### forjarWebScriptVectorAñadir

**Tipo:** constante

```typescript
export const flintVectorAdd;
```

Alias ​​para inserción de vectores, que coincide con el vocabulario del iterador stdlib `add`.

### forjarWebScriptVectorFilter

**Tipo:** función

```typescript
function flintVectorFilter(
  vector: FlintVector<TValue>,
  predicate: (value: TValue, index: number) => boolean,
): FlintVector<TValue>;
```

Filtre con entusiasmo un vector en un nuevo vector preservando el orden de origen.

#### Parámetros

| Nombre    | Tipo                                        | Descripción |
| --------- | ------------------------------------------- | ----------- |
| vector    | FlintVector<TValue>                         |             |
| predicado | (valor: TValue, índice: número) => booleano |             |

### forjarWebScriptVectorFold

**Tipo:** función

```typescript
function flintVectorFold(
  vector: FlintVector<TValue>,
  initial: TResult,
  reducer: (accumulator: TResult, value: TValue, index: number) => TResult,
): TResult;
```

Doble los valores vectoriales de izquierda a derecha, incluido el acumulador inicial suministrado.

#### Parámetros

| Nombre   | Tipo                                                            | Descripción |
| -------- | --------------------------------------------------------------- | ----------- |
| vector   | FlintVector<TValue>                                             |             |
| inicial  | TResultado                                                      |             |
| reductor | (acumulador: TResult, valor: TValue, índice: número) => TResult |             |

### forjarWebScriptVectorGet

**Tipo:** función

```typescript
function flintVectorGet(vector: FlintVector<TValue>, index: number): FlintOption<TValue>;
```

Leer un elemento vectorial; los índices no válidos devuelven Option.none.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |
| índice | número              |             |

### forjarWebScriptVectorLength

**Tipo:** función

```typescript
function flintVectorLength(vector: FlintVector<TValue>): number;
```

Devuelve el número de elementos vectoriales inicializados.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |

### forjarWebScriptVectorMap

**Tipo:** función

```typescript
function flintVectorMap(
  vector: FlintVector<TValue>,
  transform: (value: TValue, index: number) => TResult,
): FlintVector<TResult>;
```

Asigne con entusiasmo un vector a un nuevo vector en el orden del índice de origen.

#### Parámetros

| Nombre      | Tipo                                       | Descripción |
| ----------- | ------------------------------------------ | ----------- |
| vector      | FlintVector<TValue>                        |             |
| transformar | (valor: TValue, índice: número) => TResult |             |

### forjarWebScriptVectorPop

**Tipo:** función

```typescript
function flintVectorPop(vector: FlintVector<TValue>): {
  readonly vector: FlintVector<TValue>;
  readonly value: FlintOption<TValue>;
};
```

Elimine el valor final del vector y devuelva el vector actualizado más el resultado de la Opción.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |

### forjarWebScriptVectorPush

**Tipo:** función

```typescript
function flintVectorPush(vector: FlintVector<TValue>, value: TValue): FlintVector<TValue>;
```

Agregue un valor y aumente la capacidad según sea necesario, preservando la propiedad.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |
| valor  | Valor de TV         |             |

### forjarWebScriptVectorSet

**Tipo:** función

```typescript
function flintVectorSet(vector: FlintVector<TValue>, index: number, value: TValue): FlintVector<TValue>;
```

Reemplazar un elemento vectorial; los índices no válidos arrojan RangeError.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |
| índice | número              |             |
| valor  | Valor de TV         |             |

### forjarWebScriptVectorTrySet

**Tipo:** función

```typescript
function flintVectorTrySet(
  vector: FlintVector<TValue>,
  index: number,
  value: TValue,
): FlintResultValue<FlintVector<TValue>>;
```

Reemplazar un elemento vectorial sin tirarlo; los índices no válidos devuelven un resultado de error.

#### Parámetros

| Nombre | Tipo                | Descripción |
| ------ | ------------------- | ----------- |
| vector | FlintVector<TValue> |             |
| índice | número              |             |
| valor  | Valor de TV         |             |

## `src/ecs`

### agregarFlintEcsComponent

**Tipo:** función

```typescript
function addFlintEcsComponent(
  world: FlintEcsWorld<TValue>,
  entity: FlintEcsEntity,
  component: string,
  value: TValue,
): FlintEcsResult<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                  | Descripción |
| ---------- | --------------------- | ----------- |
| mundo      | FlintEcsWorld<TValue> |             |
| entidad    | FlintEcsEntidad       |             |
| componente | cadena                |             |
| valor      | Valor de TV           |             |

### crearFlintEcsScheduler

**Tipo:** función

```typescript
function createFlintEcsScheduler(
  systems: readonly FlintEcsSystem<TValue>[],
  signals: readonly FlintEcsSignal[] = [],
  subscriptions: readonly FlintEcsSubscription[] = [],
  maxSteps = 1000,
): FlintEcsScheduler<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre        | Tipo                                  | Descripción |
| ------------- | ------------------------------------- | ----------- |
| sistemas      | sólo lectura FlintEcsSystem<TValue>[] |             |
| señales       | sólo lectura FlintEcsSignal[]         |             |
| suscripciones | solo lectura FlintEcsSubscription[]   |             |
| maxPasos      |                                       |             |

### crearFlintEcsWorld

**Tipo:** función

```typescript
function createFlintEcsWorld(): FlintEcsWorld<TValue>;
```

No se proporciona descripción.

### despawnFlintEcsEntity

**Tipo:** función

```typescript
function despawnFlintEcsEntity(world: FlintEcsWorld<TValue>, entity: FlintEcsEntity): FlintEcsResult<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre  | Tipo                  | Descripción |
| ------- | --------------------- | ----------- |
| mundo   | FlintEcsWorld<TValue> |             |
| entidad | FlintEcsEntidad       |             |

### FlintEcsComponentStore

**Tipo:** interfaz

```typescript
export interface FlintEcsComponentStore<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsEntidad

**Tipo:** interfaz

```typescript
export interface FlintEcsEntity
```

No se proporciona descripción.

### FlintEcsEntityResult

**Tipo:** interfaz

```typescript
export interface FlintEcsEntityResult<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsQuery

**Tipo:** interfaz

```typescript
export interface FlintEcsQuery
```

No se proporciona descripción.

### Resultado de FlintEcs

**Tipo:** tipo

```typescript
export type FlintEcsResult<TValue = Uint8Array> = |
```

No se proporciona descripción.

### FlintEcsScheduler

**Tipo:** interfaz

```typescript
export interface FlintEcsScheduler<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsScheduleResultado

**Tipo:** interfaz

```typescript
export interface FlintEcsScheduleResult<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsSignal

**Tipo:** interfaz

```typescript
export interface FlintEcsSignal
```

No se proporciona descripción.

### FlintEcsSuscripción

**Tipo:** interfaz

```typescript
export interface FlintEcsSubscription
```

No se proporciona descripción.

### Sistema FlintEcs

**Tipo:** interfaz

```typescript
export interface FlintEcsSystem<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsTransición

**Tipo:** interfaz

```typescript
export interface FlintEcsTransition<TValue = Uint8Array>
```

No se proporciona descripción.

### FlintEcsMundo

**Tipo:** interfaz

```typescript
export interface FlintEcsWorld<TValue = Uint8Array>
```

No se proporciona descripción.

### getFlintEcsComponente

**Tipo:** función

```typescript
function getFlintEcsComponent(
  world: FlintEcsWorld<TValue>,
  entity: FlintEcsEntity,
  component: string,
): TValue | undefined;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                  | Descripción |
| ---------- | --------------------- | ----------- |
| mundo      | FlintEcsWorld<TValue> |             |
| entidad    | FlintEcsEntidad       |             |
| componente | cadena                |             |

### isFlintEcsEntityAlive

**Tipo:** función

```typescript
function isFlintEcsEntityAlive(world: FlintEcsWorld<TValue>, entity: FlintEcsEntity): boolean;
```

No se proporciona descripción.

#### Parámetros

| Nombre  | Tipo                  | Descripción |
| ------- | --------------------- | ----------- |
| mundo   | FlintEcsWorld<TValue> |             |
| entidad | FlintEcsEntidad       |             |

### queryFlintEcsEntities

**Tipo:** función

```typescript
function queryFlintEcsEntities(world: FlintEcsWorld<TValue>, query: FlintEcsQuery): readonly FlintEcsEntity[];
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| mundo    | FlintEcsWorld<TValue> |             |
| consulta | FlintEcsQuery         |             |

### eliminarFlintEcsComponent

**Tipo:** función

```typescript
function removeFlintEcsComponent(
  world: FlintEcsWorld<TValue>,
  entity: FlintEcsEntity,
  component: string,
): FlintEcsResult<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                  | Descripción |
| ---------- | --------------------- | ----------- |
| mundo      | FlintEcsWorld<TValue> |             |
| entidad    | FlintEcsEntidad       |             |
| componente | cadena                |             |

### ejecutarFlintEcsScheduler

**Tipo:** función

```typescript
function runFlintEcsScheduler(
  world: FlintEcsWorld<TValue>,
  scheduler: FlintEcsScheduler<TValue>,
): FlintEcsScheduleResult<TValue> | Extract<FlintEcsResult<TValue>, { readonly ok: false }>;
```

No se proporciona descripción.

#### Parámetros

| Nombre       | Tipo                      | Descripción |
| ------------ | ------------------------- | ----------- |
| mundo        | FlintEcsWorld<TValue>     |             |
| planificador | FlintEcsScheduler<TValue> |             |

### setFlintEcsComponent

**Tipo:** función

```typescript
function setFlintEcsComponent(
  world: FlintEcsWorld<TValue>,
  entity: FlintEcsEntity,
  component: string,
  value: TValue,
): FlintEcsResult<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                  | Descripción |
| ---------- | --------------------- | ----------- |
| mundo      | FlintEcsWorld<TValue> |             |
| entidad    | FlintEcsEntidad       |             |
| componente | cadena                |             |
| valor      | Valor de TV           |             |

### spawnFlintEcsEntidad

**Tipo:** función

```typescript
function spawnFlintEcsEntity(world: FlintEcsWorld<TValue>): FlintEcsEntityResult<TValue>;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                  | Descripción |
| ------ | --------------------- | ----------- |
| mundo  | FlintEcsWorld<TValue> |             |

### validarFlintEcsSignals

**Tipo:** función

```typescript
function validateFlintEcsSignals(
  signals: readonly FlintEcsSignal[],
): { readonly valid: true } | { readonly valid: false; readonly cycle: readonly string[] };
```

No se proporciona descripción.

#### Parámetros

| Nombre  | Tipo                          | Descripción |
| ------- | ----------------------------- | ----------- |
| señales | sólo lectura FlintEcsSignal[] |             |

## `src/host`

### crearDefaultFlintCapabilities

**Tipo:** función

```typescript
function createDefaultFlintCapabilities(options: FlintDefaultHostOptions = {}): FlintCapabilityRegistry;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                    | Descripción |
| -------- | ----------------------- | ----------- |
| opciones | FlintDefaultHostOptions |             |

### crearFlintHost

**Tipo:** función

```typescript
function createFlintHost(
  manifest: FlintAbiManifest,
  registry: FlintCapabilityRegistry,
  options: FlintHostOptions = {},
): FlintHost;
```

No se proporciona descripción.

#### Parámetros

| Nombre     | Tipo                             | Descripción |
| ---------- | -------------------------------- | ----------- |
| manifiesto | FlintAbiManifest                 |             |
| registro   | Registro de capacidades de Flint |             |
| opciones   | Opciones de host de Flint        |             |

### Implementación de la capacidad de Flint

**Tipo:** interfaz

```typescript
export interface FlintCapabilityImplementation
```

No se proporciona descripción.

### Registro de capacidades de Flint

**Tipo:** tipo

```typescript
export type FlintCapabilityRegistry = Readonly<Record<string, FlintCapabilityImplementation>>;
```

No se proporciona descripción.

### FlintDefaultHostOptions

**Tipo:** interfaz

```typescript
export interface FlintDefaultHostOptions
```

No se proporciona descripción.

### FlintHost

**Tipo:** interfaz

```typescript
export interface FlintHost
```

No se proporciona descripción.

### FlintHostCall

**Tipo:** tipo

```typescript
export type FlintHostCall = (arguments_: readonly unknown[]) => unknown | Promise<unknown>;
```

No se proporciona descripción.

### Opciones de host de Flint

**Tipo:** interfaz

```typescript
export interface FlintHostOptions
```

No se proporciona descripción.

## `src/iterator`

### crearFlintWasmIterator

**Tipo:** función

```typescript
function createFlintWasmIterator(
  handle: number,
  next: (handle: number) => FlintPackedIteratorResult,
  options: FlintWasmIteratorOptions<TValue>,
): FlintWasmIterator<TValue>;
```

Adapta el identificador i32 del backend y el siguiente protocolo empaquetado i64 a la iteración JS.

#### Parámetros

| Nombre    | Tipo                                                 | Descripción |
| --------- | ---------------------------------------------------- | ----------- |
| mango     | número                                               |             |
| siguiente | (identificador: número) => FlintPackedIteratorResult |             |
| opciones  | FlintWasmIteratorOptions<TValue>                     |             |

### crearFlintWasmIteratorFactory

**Tipo:** función

```typescript
function createFlintWasmIteratorFactory(
  factory: (...arguments_: TArguments) => number,
  next: (handle: number) => FlintPackedIteratorResult,
  options: FlintWasmIteratorOptions<TValue>,
): (...arguments_: TArguments) => FlintWasmIterator<TValue>;
```

Crea una fábrica de iteradores orientada a JS a partir de una fábrica de backend y su exportación `.next`.

#### Parámetros

| Nombre    | Tipo                                                 | Descripción |
| --------- | ---------------------------------------------------- | ----------- |
| fábrica   | (...argumentos_: TArgumentos) => número              |             |
| siguiente | (identificador: número) => FlintPackedIteratorResult |             |
| opciones  | FlintWasmIteratorOptions<TValue>                     |             |

### FlintPackedIteratorResultado

**Tipo:** tipo

```typescript
export type FlintPackedIteratorResult = number | bigint;
```

No se proporciona descripción.

### FlintWasmIterator

**Tipo:** interfaz

```typescript
export interface FlintWasmIterator<TValue> extends Iterator<TValue>, Iterable<TValue>
```

No se proporciona descripción.

### Opciones de FlintWasmIterator

**Tipo:** interfaz

```typescript
export interface FlintWasmIteratorOptions<TValue>
```

No se proporciona descripción.

## `src/logging`

### crearFlintLogger

**Tipo:** función

```typescript
function createFlintLogger(options: FlintLoggerOptions = {}): FlintLogger;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                    | Descripción |
| -------- | ----------------------- | ----------- |
| opciones | Opciones de FlintLogger |             |

### FlintLogEvento

**Tipo:** interfaz

```typescript
export interface FlintLogEvent
```

No se proporciona descripción.

### FlintLogger

**Tipo:** interfaz

```typescript
export interface FlintLogger
```

No se proporciona descripción.

### Opciones de FlintLogger

**Tipo:** interfaz

```typescript
export interface FlintLoggerOptions
```

No se proporciona descripción.

### Nivel de registro de Flint

**Tipo:** tipo

```typescript
export type FlintLogLevel = 'debug' | 'info' | 'warn' | 'error';
```

No se proporciona descripción.

### FlintLogSink

**Tipo:** tipo

```typescript
export type FlintLogSink = (event: FlintLogEvent) => void;
```

No se proporciona descripción.

## `src/memory`

### crearFlintMemoria

**Tipo:** función

```typescript
function createFlintMemory(options?: FlintMemoryOptions): FlintMemory;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                         | Descripción |
| -------- | ---------------------------- | ----------- |
| opciones | Opciones de memoria de Flint |             |

### FORGE_WEB_SCRIPT_MEMORY_CAPABILITIES

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_MEMORY_CAPABILITIES;
```

No se proporciona descripción.

### FlintMemoria

**Tipo:** clase

```typescript
export class FlintMemory
```

No se proporciona descripción.

### Dirección de memoria de Flint

**Tipo:** tipo

```typescript
export type FlintMemoryAddress = number | bigint;
```

No se proporciona descripción.

### Opciones de memoria de Flint

**Tipo:** interfaz

```typescript
export interface FlintMemoryOptions
```

No se proporciona descripción.

## `src/parallel`

### FORGE_WEB_SCRIPT_PARALLEL_CAPABILITIES

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_PARALLEL_CAPABILITIES;
```

Nombres de capacidades utilizados para autorizar la ejecución de trabajadores, subprocesos, atómicas y de memoria compartida.

### FORGE_WEB_SCRIPT_PARALLEL_OPERATION_DESCRIPTORS

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_PARALLEL_OPERATION_DESCRIPTORS: Readonly<
  Record<FlintParallelOperation, FlintParallelOperationDescriptor>
>;
```

Descriptores estables para cada operación paralela con capacidad de respaldo en serie.

### forjarWebScriptIteratorParAt

**Tipo:** función

```typescript
function flintIteratorParAt(
  source: FlintIterable<TValue>,
  index: number,
  options: FlintParallelOptions = {},
): Promise<FlintOption<TValue>>;
```

Devuelve el resultado ordenado basado en cero u Option.none cuando el índice no es válido.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| fuente   | FlintIterable<TValue>       |             |
| índice   | número                      |             |
| opciones | Opciones paralelas de Flint |             |

### forjarWebScriptIteratorParCollect

**Tipo:** función

```typescript
function flintIteratorParCollect(
  source: FlintIterable<TValue>,
  options: FlintParallelOptions = {},
): Promise<FlintVector<TValue>>;
```

Materializar resultados paralelos ordenados en un vector propio.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| fuente   | FlintIterable<TValue>       |             |
| opciones | Opciones paralelas de Flint |             |

### flintIteratorParFilter

**Tipo:** función

```typescript
function flintIteratorParFilter(
  source: FlintIterable<TValue>,
  predicate: (value: TValue, index: number) => boolean | PromiseLike<boolean>,
  options: FlintParallelOptions = {},
): Promise<FlintIterator<TValue>>;
```

Evalúe un predicado en paralelo y conserve los valores aceptados en el orden de origen.
Esta es la API canónica `par_filter`.

#### Parámetros

| Nombre    | Tipo                                                                | Descripción |
| --------- | ------------------------------------------------------------------- | ----------- |
| fuente    | FlintIterable<TValue>                                               |             |
| predicado | (valor: TValue, índice: número) => booleano \| PromesaComo<boolean> |             |
| opciones  | Opciones paralelas de Flint                                         |             |

### flintIteratorParFirst

**Tipo:** función

```typescript
function flintIteratorParFirst(
  source: FlintIterable<TValue>,
  options: FlintParallelOptions = {},
): Promise<FlintOption<TValue>>;
```

Devuelve el primer resultado ordenado o un valor Option.none para una fuente vacía.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| fuente   | FlintIterable<TValue>       |             |
| opciones | Opciones paralelas de Flint |             |

### forjarWebScriptIteratorParFlatten

**Tipo:** función

```typescript
function flintIteratorParFlatten(
  source: FlintIterable<FlintIterable<TValue>>,
  options: FlintParallelOptions = {},
): Promise<FlintIterator<TValue>>;
```

Aplane las fuentes anidadas en paralelo preservando el orden exterior e interior.

#### Parámetros

| Nombre   | Tipo                                 | Descripción |
| -------- | ------------------------------------ | ----------- |
| fuente   | FlintIterable<FlintIterable<TValue>> |             |
| opciones | Opciones paralelas de Flint          |             |

### forjarWebScriptIteratorParFold

**Tipo:** función

```typescript
function flintIteratorParFold(
  source: FlintIterable<TValue>,
  initial: TResult,
  reducer: (accumulator: TResult, value: TValue, index: number) => TResult,
  options: FlintParallelOptions = {},
): Promise<TResult>;
```

Doble los resultados ordenados de izquierda a derecha, incluido el acumulador inicial suministrado.

#### Parámetros

| Nombre   | Tipo                                                            | Descripción |
| -------- | --------------------------------------------------------------- | ----------- |
| fuente   | FlintIterable<TValue>                                           |             |
| inicial  | TResultado                                                      |             |
| reductor | (acumulador: TResult, valor: TValue, índice: número) => TResult |             |
| opciones | Opciones paralelas de Flint                                     |             |

### flintIteratorParLast

**Tipo:** función

```typescript
function flintIteratorParLast(
  source: FlintIterable<TValue>,
  options: FlintParallelOptions = {},
): Promise<FlintOption<TValue>>;
```

Devuelve el último resultado ordenado o un valor Option.none para una fuente vacía.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| fuente   | FlintIterable<TValue>       |             |
| opciones | Opciones paralelas de Flint |             |

### flintIteratorParMap

**Tipo:** función

```typescript
function flintIteratorParMap(
  source: FlintIterable<TValue>,
  map: (value: TValue, index: number) => TResult | PromiseLike<TResult>,
  options: FlintParallelOptions = {},
): Promise<FlintIterator<TResult>>;
```

Aplique una devolución de llamada en paralelo y devuelva un iterador ordenado compatible con diferidos.
Los errores de devolución de llamada rechazan la operación y no quedan ocultos por el respaldo en serie.

#### Parámetros

| Nombre   | Tipo                                                               | Descripción |
| -------- | ------------------------------------------------------------------ | ----------- |
| fuente   | FlintIterable<TValue>                                              |             |
| mapa     | (valor: TValue, índice: número) => TResult \| PromesaComo<TResult> |             |
| opciones | Opciones paralelas de Flint                                        |             |

### forjarWebScriptIteratorParToArray

**Tipo:** función

```typescript
function flintIteratorParToArray(
  source: FlintIterable<TValue>,
  options: FlintParallelOptions = {},
): Promise<FlintArray<TValue>>;
```

Materialice resultados paralelos ordenados en una matriz fija propia.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| fuente   | FlintIterable<TValue>       |             |
| opciones | Opciones paralelas de Flint |             |

### flintParallelDescriptor

**Tipo:** función

```typescript
function flintParallelDescriptor(
  operation: FlintParallelOperation,
  options: FlintParallelOptions = {},
): FlintParallelOperationDescriptor & { readonly selectedStrategy: FlintParallelStrategy };
```

Describe una operación y adjunta la estrategia seleccionada entre las opciones suministradas.

#### Parámetros

| Nombre    | Tipo                        | Descripción |
| --------- | --------------------------- | ----------- |
| operación | Operación paralela de Flint |             |
| opciones  | Opciones paralelas de Flint |             |

### FlintParallelExecutor

**Tipo:** interfaz

```typescript
export interface FlintParallelExecutor
```

Límite mínimo del programador utilizado por los trabajadores host o un ejecutor de subprocesos Wasm compilado.

### Operación paralela de Flint

**Tipo:** tipo

```typescript
export type FlintParallelOperation =
  | 'par_map'
  | 'par_filter'
  | 'par_flatten'
  | 'par_collect'
  | 'par_to_array'
  | 'par_fold'
  | 'par_first'
  | 'par_last'
  | 'par_at';
```

Nombres de operaciones paralelas canónicas expuestas por el tiempo de ejecución; `par_filter` es intencional.

### FlintParallelOperationDescriptor

**Tipo:** interfaz

```typescript
export interface FlintParallelOperationDescriptor
```

Metadatos en tiempo de ejecución para una operación paralela. `ordered` siempre es cierto: el
El índice de origen, en lugar del tiempo de finalización del trabajador, define el orden de salida.

### Opciones paralelas de Flint

**Tipo:** interfaz

```typescript
export interface FlintParallelOptions
```

Solicitud de estrategia y entradas de capacidad. `auto` prefiere los hilos Wasm permitidos,
luego trabajadores anfitriones y finalmente ejecución en serie; no se infiere ninguna capacidad insegura.

### FlintPlan paralelo

**Tipo:** interfaz

```typescript
export interface FlintParallelPlan
```

Estrategia seleccionada y motivo explícito de su selección.

### FlintEstrategia paralela

**Tipo:** tipo

```typescript
export type FlintParallelStrategy = 'serial' | 'host-workers' | 'wasm-threads';
```

Estrategia de ejecución seleccionada para una operación paralela ordenada.

### seleccioneFlintParallelStrategy

**Tipo:** función

```typescript
function selectFlintParallelStrategy(options: FlintParallelOptions = {}): FlintParallelPlan;
```

Seleccione una estrategia permitida sin cambiar el contrato de resultado de la operación.

#### Parámetros

| Nombre   | Tipo                        | Descripción |
| -------- | --------------------------- | ----------- |
| opciones | Opciones paralelas de Flint |             |

## `src/self-hosted`

### FlintSelfHostedRunOptions

**Tipo:** interfaz

```typescript
export interface FlintSelfHostedRunOptions
```

No se proporciona descripción.

### FlintSelfHostedVmRun

**Tipo:** interfaz

```typescript
export interface FlintSelfHostedVmRun
```

No se proporciona descripción.

### ejecutarFlintSelfHostedCompiler

**Tipo:** función

```typescript
function runFlintSelfHostedCompiler(
  input: FlintCompileInput,
  mode: FlintVmExecutionMode,
  options: FlintSelfHostedRunOptions = {},
): FlintSelfHostedVmRun;
```

Ejecute el programa de arranque del compilador autohospedado limitado.

Ejecuta las etapas FWS lex y parser en el modo VM solicitado y
compara cada huella digital derivada de VM con la referencia de semilla independiente.
La emisión completa de Wasm permanece respaldada por semillas y se expone a través de `artifact` solo cuando
cada etapa reporta paridad.

#### Parámetros

| Nombre   | Tipo                                          | Descripción |
| -------- | --------------------------------------------- | ----------- |
| entrada  | FlintCompileInput                             |             |
| modo     | Modo de ejecución FlintVm                     |             |
| opciones | Opciones de ejecución autohospedadas de Flint |             |

### ejecutarFlintSelfHostedLexStage

**Tipo:** función

```typescript
function runFlintSelfHostedLexStage(
  input: Pick<FlintCompileInput, 'source' | 'fileName' | 'compilerVersion' | 'requestedCapabilities'>,
  mode: FlintVmExecutionMode,
  options: FlintSelfHostedRunOptions = {},
): FlintSelfHostedStageReport;
```

Ejecute solo la etapa lex limitada creada por FWS para los consumidores del compilador.

#### Parámetros

| Nombre   | Tipo                                                                                                   | Descripción |
| -------- | ------------------------------------------------------------------------------------------------------ | ----------- |
| entrada  | Seleccionar<FlintCompileInput, 'source' \| 'fileName' \| 'compilerVersion' \| 'requestedCapabilities'> |             |
| modo     | Modo de ejecución FlintVm                                                                              |             |
| opciones | Opciones de ejecución autohospedadas de Flint                                                          |             |

## `src/threading`

### canUseFlintWasmThreads

**Tipo:** función

```typescript
function canUseFlintWasmThreads(
  targetFeatures: FlintWasmThreadTargetFeatures | undefined,
  capabilities: readonly string[] | undefined,
): boolean;
```

La ejecución de subprocesos es voluntaria tanto en los límites de compilación como de tiempo de ejecución. en
En particular, una lista de capacidades no declaradas nunca habilita esta ruta.

#### Parámetros

| Nombre                  | Tipo                                        | Descripción |
| ----------------------- | ------------------------------------------- | ----------- |
| objetivoCaracterísticas | FlintWasmThreadTargetFeatures \| indefinido |             |
| capacidades             | cadena de solo lectura[] \| indefinido      |             |

### crearFlintAtomicI32

**Tipo:** función

```typescript
function createFlintAtomicI32(length = 1, options: FlintAtomicI32Options = {}): FlintAtomicI32;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| longitud |                       |             |
| opciones | FlintAtomicI32Options |             |

### crearFlintWasmThreadScheduler

**Tipo:** función

```typescript
function createFlintWasmThreadScheduler(options: FlintWasmThreadSchedulerOptions): FlintWorkerScheduler;
```

Envuelve un puente de subprocesos WASM compilado con contabilidad explícita de memoria compartida.
El tiempo de ejecución no fabrica un subproceso: sin `executor`, llamantes
debe utilizar el respaldo en serie seleccionado por el contrato paralelo.

#### Parámetros

| Nombre   | Tipo                            | Descripción |
| -------- | ------------------------------- | ----------- |
| opciones | FlintWasmThreadSchedulerOptions |             |

### crearFlintWorkerRuntime

**Tipo:** función

```typescript
function createFlintWorkerRuntime(
  createWorker: () => FlintWorkerPort,
  onMessage: (message: unknown) => void,
  onError: (error: unknown) => void = () => {},
  options: FlintWorkerRuntimeOptions = {},
): FlintWorkerRuntime;
```

No se proporciona descripción.

#### Parámetros

| Nombre          | Tipo                                           | Descripción |
| --------------- | ---------------------------------------------- | ----------- |
| crearTrabajador | () => FlintWorkerPort                          |             |
| en mensaje      | (mensaje: desconocido) => nulo                 |             |
| enError         | (error: desconocido) => nulo                   |             |
| opciones        | Opciones de tiempo de ejecución de FlintWorker |             |

### crearFlintWorkerScheduler

**Tipo:** función

```typescript
function createFlintWorkerScheduler(options: FlintWorkerSchedulerOptions = {}): FlintWorkerScheduler;
```

Crea el límite de programación del lado del host utilizado por las operaciones de iterador paralelo.

El trabajo todavía está representado por una devolución de llamada local porque las devoluciones de llamada JS arbitrarias no pueden
ser transferido a un Trabajador. Una integración de host puede proporcionar tiempos de ejecución de trabajadores para
ciclo de vida/limpieza y utilizar el protocolo indexado en su límite; este planificador
proporciona el mismo contrato asíncrono limitado para hosts sin soporte de trabajadores.

#### Parámetros

| Nombre   | Tipo                             | Descripción |
| -------- | -------------------------------- | ----------- |
| opciones | Opciones de FlintWorkerScheduler |             |

### FORGE_WEB_SCRIPT_THREADING_CAPABILITIES

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_THREADING_CAPABILITIES;
```

No se proporciona descripción.

### FlintAtomicI32

**Tipo:** interfaz

```typescript
export interface FlintAtomicI32
```

No se proporciona descripción.

### Opciones de FlintAtomicI32

**Tipo:** interfaz

```typescript
export interface FlintAtomicI32Options
```

No se proporciona descripción.

### FlintWasmThreadSchedulerOptions

**Tipo:** interfaz

```typescript
export interface FlintWasmThreadSchedulerOptions extends FlintWasmThreadTargetFeatures
```

No se proporciona descripción.

### FlintWasmThreadTargetCaracterísticas

**Tipo:** interfaz

```typescript
export interface FlintWasmThreadTargetFeatures
```

No se proporciona descripción.

### FlintWorkerPuerto

**Tipo:** interfaz

```typescript
export interface FlintWorkerPort
```

No se proporciona descripción.

### Tiempo de ejecución de FlintWorker

**Tipo:** interfaz

```typescript
export interface FlintWorkerRuntime
```

No se proporciona descripción.

### Opciones de tiempo de ejecución de FlintWorker

**Tipo:** interfaz

```typescript
export interface FlintWorkerRuntimeOptions
```

No se proporciona descripción.

### FlintWorkerScheduler

**Tipo:** interfaz

```typescript
export interface FlintWorkerScheduler
```

Un programador asíncrono limitado utilizado por operaciones de trabajo host orientadas al tiempo de ejecución.

### Opciones de FlintWorkerScheduler

**Tipo:** interfaz

```typescript
export interface FlintWorkerSchedulerOptions
```

No se proporciona descripción.

## `src/trace`

### crearFlintTraceRecorder

**Tipo:** función

```typescript
function createFlintTraceRecorder(options: FlintTraceOptions, functionName: string): FlintTraceRecorder;
```

No se proporciona descripción.

#### Parámetros

| Nombre            | Tipo                   | Descripción |
| ----------------- | ---------------------- | ----------- |
| opciones          | Opciones de FlintTrace |             |
| nombre de función | cadena                 |             |

### FlintTraceCaptureModo

**Tipo:** tipo

```typescript
export type FlintTraceCaptureMode = 'summary' | 'events' | 'snapshot';
```

No se proporciona descripción.

### FlintTraceContadores

**Tipo:** interfaz

```typescript
export interface FlintTraceCounters
```

No se proporciona descripción.

### FlintTraceEvento

**Tipo:** interfaz

```typescript
export interface FlintTraceEvent
```

No se proporciona descripción.

### FlintTraceEventType

**Tipo:** tipo

```typescript
export type FlintTraceEventType =
  'instruction' | 'call' | 'capability' | 'memory' | 'range-check' | 'trap' | 'resource';
```

No se proporciona descripción.

### Límites de seguimiento de Flint

**Tipo:** interfaz

```typescript
export interface FlintTraceLimits
```

No se proporciona descripción.

### Opciones de seguimiento de Flint

**Tipo:** interfaz

```typescript
export interface FlintTraceOptions extends FlintTraceLimits
```

No se proporciona descripción.

### FlintTraceRecorder

**Tipo:** interfaz

```typescript
export interface FlintTraceRecorder
```

No se proporciona descripción.

### Informe de seguimiento de Flint

**Tipo:** interfaz

```typescript
export interface FlintTraceReport
```

No se proporciona descripción.

### FlintTraceSourceUbicación

**Tipo:** interfaz

```typescript
export interface FlintTraceSourceLocation extends FlintVmDebugSpan
```

No se proporciona descripción.

### resumirFlintVmValue

**Tipo:** función

```typescript
function summarizeFlintVmValue(value: FlintVmValue, redact?: (value: FlintVmValue) => string): string;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                            | Descripción |
| -------- | ------------------------------- | ----------- |
| valor    | FlintVmValue                    |             |
| redactar | (valor: FlintVmValue) => cadena |             |

## `src/traps`

### adjuntarFlintTrace

**Tipo:** función

```typescript
function attachFlintTrace(error: unknown, trace: FlintTraceReport): void;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo                            | Descripción |
| ------ | ------------------------------- | ----------- |
| error  | desconocido                     |             |
| rastro | Informe de seguimiento de Flint |             |

### Trampa Flint

**Tipo:** clase

```typescript
export class FlintTrap extends Error
```

No se proporciona descripción.

### FlintTrapCode

**Tipo:** tipo

```typescript
export type FlintTrapCode =
  | 'CapabilityDenied'
  | 'HostError'
  | 'InvalidAbi'
  | 'InvalidOwnership'
  | 'MemoryExhausted'
  | 'MemoryOutOfBounds'
  | 'GuestTrap';
```

No se proporciona descripción.

### Opciones de FlintTrap

**Tipo:** interfaz

```typescript
export interface FlintTrapOptions extends ErrorOptions
```

No se proporciona descripción.

### toFlintHostError

**Tipo:** función

```typescript
function toFlintHostError(error: unknown, capability: string, logger?: FlintLogger): FlintTrap;
```

No se proporciona descripción.

#### Parámetros

| Nombre      | Tipo        | Descripción |
| ----------- | ----------- | ----------- |
| error       | desconocido |             |
| capacidad   | cadena      |             |
| registrador | FlintLogger |             |

## `src/vm-executor`

### crearFlintVmAotArtifact

**Tipo:** función

```typescript
function createFlintVmAotArtifact(module: FlintVmModule, compilerVersion: string): FlintVmAotArtifact;
```

No se proporciona descripción.

#### Parámetros

| Nombre                 | Tipo           | Descripción |
| ---------------------- | -------------- | ----------- |
| módulo                 | Módulo FlintVm |             |
| versión del compilador | cadena         |             |

### crearFlintVmExecutor

**Tipo:** función

```typescript
function createFlintVmExecutor(executorOptions: FlintVmExecutorOptions = {}): FlintVmExecutor;
```

No se proporciona descripción.

#### Parámetros

| Nombre               | Tipo                        | Descripción |
| -------------------- | --------------------------- | ----------- |
| Opciones de ejecutor | Opciones de FlintVmExecutor |             |

### ejecutarFlintVm

**Tipo:** función

```typescript
function executeFlintVm(
  module: FlintVmModule,
  functionName: string,
  arguments_: readonly FlintVmValue[],
  options: FlintVmExecutionOptions,
): FlintVmExecutionResult;
```

No se proporciona descripción.

#### Parámetros

| Nombre            | Tipo                             | Descripción |
| ----------------- | -------------------------------- | ----------- |
| módulo            | Módulo FlintVm                   |             |
| nombre de función | cadena                           |             |
| argumentos_       | sólo lectura FlintVmValue[]      |             |
| opciones          | Opciones de ejecución de FlintVm |             |

### ejecutarFlintVmAotArtifact

**Tipo:** función

```typescript
function executeFlintVmAotArtifact(
  artifact: FlintVmAotArtifact,
  functionName: string,
  arguments_: readonly FlintVmValue[],
  options: Omit<FlintVmExecutionOptions, 'mode'> = {},
): FlintVmExecutionResult;
```

No se proporciona descripción.

#### Parámetros

| Nombre            | Tipo                                    | Descripción |
| ----------------- | --------------------------------------- | ----------- |
| artefacto         | FlintVmAotArtifact                      |             |
| nombre de función | cadena                                  |             |
| argumentos_       | sólo lectura FlintVmValue[]             |             |
| opciones          | Omitir<FlintVmExecutionOptions, 'mode'> |             |

### Opciones de FlintVmExecutor

**Tipo:** interfaz

```typescript
export interface FlintVmExecutorOptions
```

No se proporciona descripción.

### ejecutarFlintVmBootstrap

**Tipo:** función

```typescript
function runFlintVmBootstrap(
  module: FlintVmModule,
  functionName: string,
  arguments_: readonly FlintVmValue[],
  mode: FlintVmExecutionMode = 'interpret',
  options: Omit<FlintVmExecutionOptions, 'mode'> = {},
): FlintVmExecutionResult;
```

No se proporciona descripción.

#### Parámetros

| Nombre            | Tipo                                    | Descripción |
| ----------------- | --------------------------------------- | ----------- |
| módulo            | Módulo FlintVm                          |             |
| nombre de función | cadena                                  |             |
| argumentos_       | sólo lectura FlintVmValue[]             |             |
| modo              | Modo de ejecución FlintVm               |             |
| opciones          | Omitir<FlintVmExecutionOptions, 'mode'> |             |

### validarFlintVmModule

**Tipo:** función

```typescript
function validateFlintVmModule(module: FlintVmModule): void;
```

No se proporciona descripción.

#### Parámetros

| Nombre | Tipo           | Descripción |
| ------ | -------------- | ----------- |
| módulo | Módulo FlintVm |             |

## `src/vm-wasm`

### compilarFlintVmWasm

**Tipo:** función

```typescript
function compileFlintVmWasm(module: FlintVmModule, options: FlintVmWasmCompileOptions = {}): FlintVmWasmArtifact;
```

No se proporciona descripción.

#### Parámetros

| Nombre   | Tipo                      | Descripción |
| -------- | ------------------------- | ----------- |
| módulo   | Módulo FlintVm            |             |
| opciones | FlintVmWasmCompileOptions |             |

### FlintVmWasmCompileOptions

**Tipo:** interfaz

```typescript
export interface FlintVmWasmCompileOptions
```

No se proporciona descripción.

### prepararFlintVm

**Tipo:** constante

```typescript
export const prepareFlintVm;
```

No se proporciona descripción.

### prepararFlintVmWasm

**Tipo:** función

```typescript
function prepareFlintVmWasm(
  moduleOrArtifact: FlintVmModule | FlintVmWasmArtifact,
  options: FlintVmPreparedExecutorOptions = {},
): FlintVmPreparedExecutor;
```

No se proporciona descripción.

#### Parámetros

| Nombre           | Tipo                                 | Descripción |
| ---------------- | ------------------------------------ | ----------- |
| móduloOrArtifact | FlintVmModule \| FlintVmWasmArtifact |             |
| opciones         | FlintVmPreparedExecutorOptions       |             |

## `src/vm`

### FORGE_WEB_SCRIPT_VM_WASM_ABI_VERSION

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_VM_WASM_ABI_VERSION;
```

No se proporciona descripción.

### FORGE_WEB_SCRIPT_VM_WASM_LOWERING_VERSION

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_VM_WASM_LOWERING_VERSION;
```

No se proporciona descripción.

### FlintVmAotArtifact

**Tipo:** interfaz

```typescript
export interface FlintVmAotArtifact
```

No se proporciona descripción.

### FlintVmCapabilityImportar

**Tipo:** interfaz

```typescript
export interface FlintVmCapabilityImport
```

No se proporciona descripción.

### FlintVmDebugSpan

**Tipo:** interfaz

```typescript
export interface FlintVmDebugSpan
```

No se proporciona descripción.

### Modo de ejecución FlintVm

**Tipo:** tipo

```typescript
export type FlintVmExecutionMode = 'interpret' | 'jit' | 'aot';
```

No se proporciona descripción.

### Opciones de ejecución de FlintVm

**Tipo:** interfaz

```typescript
export interface FlintVmExecutionOptions
```

No se proporciona descripción.

### Resultado de ejecución de FlintVm

**Tipo:** interfaz

```typescript
export interface FlintVmExecutionResult
```

No se proporciona descripción.

### FlintVmExecutor

**Tipo:** interfaz

```typescript
export interface FlintVmExecutor
```

No se proporciona descripción.

### Función FlintVm

**Tipo:** interfaz

```typescript
export interface FlintVmFunction
```

No se proporciona descripción.

### FlintVmInstrucción

**Tipo:** tipo

```typescript
export type FlintVmInstruction = |
```

No se proporciona descripción.

### FlintVmJitCache

**Tipo:** interfaz

```typescript
export interface FlintVmJitCache
```

No se proporciona descripción.

### Entrada FlintVmJit

**Tipo:** interfaz

```typescript
export interface FlintVmJitEntry
```

No se proporciona descripción.

### Módulo FlintVm

**Tipo:** interfaz

```typescript
export interface FlintVmModule
```

No se proporciona descripción.

### FlintVmPreparedExecutor

**Tipo:** interfaz

```typescript
export interface FlintVmPreparedExecutor
```

No se proporciona descripción.

### Opciones de FlintVmPreparedExecutor

**Tipo:** interfaz

```typescript
export interface FlintVmPreparedExecutorOptions
```

No se proporciona descripción.

### FlintVmValue

**Tipo:** tipo

```typescript
export type FlintVmValue = |
```

No se proporciona descripción.

### FlintVmWasmArtifact

**Tipo:** interfaz

```typescript
export interface FlintVmWasmArtifact
```

No se proporciona descripción.
