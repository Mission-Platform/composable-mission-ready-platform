# @mission-platform/flint-lsp API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/flint-lsp/docs/reference/generated/api.md: [packages/flint-lsp/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/flint-lsp` 中的公共源声明生成。

## `src/server`

### 创建FlintLspServer

**种类：**功能

```typescript
function createFlintLspServer(options: FlintLspServerOptions = {}): FlintLspServer;
```

没有提供描述。

#### 参数

| 名称 | 类型                  | 描述                  |
| ---- | --------------------- | --------------------- |
| 选项 | FlintLspServerOptions | FlintLspServerOptions | FlintLspServerOptions |

### FlintLsp文档

**种类：**接口

```typescript
export interface FlintLspDocument
```

没有提供描述。

### FlintLspLogEvent

**种类：**接口

```typescript
export interface FlintLspLogEvent
```

没有提供描述。

### FlintLspProgressEvent

**种类：**接口

```typescript
export interface FlintLspProgressEvent
```

没有提供描述。

### FlintLsp服务器

**种类：**接口

```typescript
export interface FlintLspServer
```

没有提供描述。

### FlintLspServerOptions

**种类：**接口

```typescript
export interface FlintLspServerOptions
```

没有提供描述。

### 注册FlintLsp

**种类：**功能

```typescript
function registerFlintLsp(connection: Connection, options: FlintLspServerOptions = {}): FlintLspServer;
```

没有提供描述。

#### 参数

| 名称 | 类型                  | 描述                  |
| ---- | --------------------- | --------------------- |
| 连接 | 连接                  |                       |
| 选项 | FlintLspServerOptions | FlintLspServerOptions | FlintLspServerOptions |

## `src/workspace`

### 创建FlintNodeWorkspaceHost

**种类：**功能

```typescript
function createFlintNodeWorkspaceHost(options: FlintNodeWorkspaceOptions): RootBoundedFlintWorkspaceHost;
```

没有提供描述。

#### 参数

| 名称 | 类型                      | 描述                      |
| ---- | ------------------------- | ------------------------- |
| 选项 | FlintNodeWorkspaceOptions | FlintNodeWorkspaceOptions | FlintNodeWorkspaceOptions |

### FlintNode文件系统

**种类：**接口

```typescript
export interface FlintNodeFileSystem
```

没有提供描述。

### FlintNode工作空间选项

**种类：**接口

```typescript
export interface FlintNodeWorkspaceOptions extends FlintWorkspaceOptions
```

没有提供描述。

### RootBoundedFlintWorkspaceHost

**种类：**类

```typescript
export class RootBoundedFlintWorkspaceHost implements FlintWorkspaceHost
```

没有提供描述。
