# Riferimento API @mission-platform/flint-vitest

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/flint-vitest/docs/reference/generated/api.md: [packages/flint-vitest/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/flint-vitest`.

## `src/diagnostics`

### assertFlintDiagnostic

**Tipo:** funzione

```typescript
function assertFlintDiagnostic(
  diagnostics: readonly FlintDiagnostic[],
  expectation: FlintDiagnosticExpectation,
): FlintDiagnostic;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                       | Descrizione |
| ----------- | ------------------------------ | ----------- |
| diagnostica | sola lettura FlintDiagnostic[] |             |
| aspettativa | FlintDiagnosticExpectation     |             |

### assertFlintNoDiagnostics

**Tipo:** funzione

```typescript
function assertFlintNoDiagnostics(diagnostics: readonly FlintDiagnostic[]): void;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                       | Descrizione |
| ----------- | ------------------------------ | ----------- |
| diagnostica | sola lettura FlintDiagnostic[] |             |

### findFlintDiagnostic

**Tipo:** funzione

```typescript
function findFlintDiagnostic(
  diagnostics: readonly FlintDiagnostic[],
  expectation: FlintDiagnosticExpectation,
): FlintDiagnostic | undefined;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                       | Descrizione |
| ----------- | ------------------------------ | ----------- |
| diagnostica | sola lettura FlintDiagnostic[] |             |
| aspettativa | FlintDiagnosticExpectation     |             |

### FlintDiagnosticExpectation

**Tipo:** interfaccia

```typescript
export interface FlintDiagnosticExpectation
```

Nessuna descrizione fornita.

### formatFlintDiagnostic

**Tipo:** funzione

```typescript
function formatFlintDiagnostic(diagnostic: FlintDiagnostic): string;
```

Formatta una diagnostica senza eliminare i metadati di posizione strutturati.

#### Parametri

| Nome        | Digitare         | Descrizione |
| ----------- | ---------------- | ----------- |
| diagnostico | FlintDiagnostica |             |

### formatFlintDiagnostics

**Tipo:** funzione

```typescript
function formatFlintDiagnostics(diagnostics: readonly FlintDiagnostic[]): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                       | Descrizione |
| ----------- | ------------------------------ | ----------- |
| diagnostica | sola lettura FlintDiagnostic[] |             |

## `src/harness`

### createFlintTestHarness

**Tipo:** funzione

```typescript
function createFlintTestHarness(options: FlintTestHarnessOptions = {}): FlintTestHarness;
```

Crea un cablaggio di un dispositivo condiviso con compilatore, funzionalità e impostazioni di destinazione opzionali.

#### Parametri

| Nome    | Digitare                | Descrizione |
| ------- | ----------------------- | ----------- |
| opzioni | FlintTestHarnessOptions |             |

### FlintCapabilityFunction

**Genere:** tipo

```typescript
export type FlintCapabilityFunction = (...arguments_: readonly unknown[]) => unknown;
```

Un callback host fornito per un'importazione di funzionalità dichiarata dal manifesto.

### FlintCapabilityImports

**Genere:** tipo

```typescript
export type FlintCapabilityImports = Readonly<Record<string, Readonly<Record<string, FlintCapabilityFunction>>>>;
```

Funzioni host caratterizzate da capacità e alias manifest; le importazioni non dichiarate vengono respinte.

### FlintCompilationResult

**Tipo:** interfaccia

```typescript
export interface FlintCompilationResult extends FlintCompiledModule
```

Output di compilazione più diagnostica, adatto per dispositivi validi e diagnostici.

### flintFixtureName

**Tipo:** funzione

```typescript
function flintFixtureName(fileName: string, fixtureRoot: string): string;
```

Converte il percorso di un'apparecchiatura in un nome di test/modulo stabile. I percorsi relativi sono
radicato in fixtureRoot e i separatori sono normalizzati in trattini.

#### Parametri

| Nome            | Digitare | Descrizione |
| --------------- | -------- | ----------- |
| nomefile        | stringa  |             |
| apparecchioRoot | stringa  |             |

### FlintInspectionResult

**Tipo:** interfaccia

```typescript
export interface FlintInspectionResult
```

Risultato dell'ispezione degli artefatti restituito senza istanziare Wasm.

### FlintLoadedExports

**Genere:** tipo

```typescript
export type FlintLoadedExports<TExports extends object> = TExports;
```

Visualizzazione a livello di tipo delle esportazioni convalidate dal cablaggio rispetto all'istanza Wasm.

### FlintTestHarness

**Gentile:** lezione

```typescript
export class FlintTestHarness
```

Compila e carica dispositivi FWS condivisi attraverso lo stesso confine Wasm utilizzato da
consumatori. Il cablaggio espone anche WAT, manifest, grafico e self-hosted
dati di ispezione per i test di conformità.

### Errore FlintTestHarnessDisposed

**Gentile:** lezione

```typescript
export class FlintTestHarnessDisposedError extends FlintTestHarnessError
```

Sollevato quando un'imbracatura smaltita viene riutilizzata.

### Errore FlintTestHarness

**Gentile:** lezione

```typescript
export class FlintTestHarnessError extends Error
```

Errore digitato di base per errori di risoluzione delle apparecchiature, compilazione, importazione e caricamento.

### FlintTestHarnessOptions

**Tipo:** interfaccia

```typescript
export interface FlintTestHarnessOptions extends FlintPluginOptions
```

Opzioni del compilatore e della root del dispositivo condivise dagli helper del cablaggio di conformità.

## `src/vitest`

### defineFlintVitestConfig

**Tipo:** funzione

```typescript
function defineFlintVitestConfig(options: FlintVitestConfigOptions = {}): ViteUserConfig;
```

Componi la configurazione standard di Mission Platform Vitest con Forge Web Script.
Le sostituzioni dei consumatori vengono unite anziché sostituite, inclusi i relativi plug-in;
le query relative alle apparecchiature FWS generate come `?flint-wat` rimangono disponibili.

#### Parametri

| Nome    | Digitare                 | Descrizione |
| ------- | ------------------------ | ----------- |
| opzioni | FlintVitestConfigOptions |             |

### FlintVitestConfigOptions

**Tipo:** interfaccia

```typescript
export interface FlintVitestConfigOptions extends Omit<VitestConfigOptions, 'overrides'>
```

Nessuna descrizione fornita.

### flintVitestPlugin

**Tipo:** funzione

```typescript
function flintVitestPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installa il plug-in Forge Web Script di produzione in una configurazione Vite/Vitest.

#### Parametri

| Nome    | Digitare           | Descrizione |
| ------- | ------------------ | ----------- |
| opzioni | FlintPluginOptions |             |
