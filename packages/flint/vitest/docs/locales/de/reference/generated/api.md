# @mission-platform/flint-vitest API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/flint-vitest/docs/reference/generated/api.md: [packages/flint-vitest/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/flint-vitest`.

## `src/diagnostics`

### behauptenFlintDiagnostic

**Art:** Funktion

```typescript
function assertFlintDiagnostic(
  diagnostics: readonly FlintDiagnostic[],
  expectation: FlintDiagnosticExpectation,
): FlintDiagnostic;
```

Keine Beschreibung angegeben.

#### Parameter

| Name      | Geben Sie                          | ein Beschreibung |
| --------- | ---------------------------------- | ---------------- |
| Diagnose  | schreibgeschützt FlintDiagnostic[] |                  |
| Erwartung | FlintDiagnosticExpectation         |                  |

### affirmFlintNoDiagnostics

**Art:** Funktion

```typescript
function assertFlintNoDiagnostics(diagnostics: readonly FlintDiagnostic[]): void;
```

Keine Beschreibung angegeben.

#### Parameter

| Name     | Geben Sie                          | ein Beschreibung |
| -------- | ---------------------------------- | ---------------- |
| Diagnose | schreibgeschützt FlintDiagnostic[] |                  |

### findFlintDiagnostic

**Art:** Funktion

```typescript
function findFlintDiagnostic(
  diagnostics: readonly FlintDiagnostic[],
  expectation: FlintDiagnosticExpectation,
): FlintDiagnostic | undefined;
```

Keine Beschreibung angegeben.

#### Parameter

| Name      | Geben Sie                          | ein Beschreibung |
| --------- | ---------------------------------- | ---------------- |
| Diagnose  | schreibgeschützt FlintDiagnostic[] |                  |
| Erwartung | FlintDiagnosticExpectation         |                  |

### FlintDiagnosticExpectation

**Art:** Schnittstelle

```typescript
export interface FlintDiagnosticExpectation
```

Keine Beschreibung angegeben.

### formatFlintDiagnostic

**Art:** Funktion

```typescript
function formatFlintDiagnostic(diagnostic: FlintDiagnostic): string;
```

Formatieren Sie eine Diagnose, ohne ihre strukturierten Standortmetadaten zu verwerfen.

#### Parameter

| Name     | Geben Sie       | ein Beschreibung |
| -------- | --------------- | ---------------- |
| Diagnose | FlintDiagnostic |                  |

### formatFlintDiagnostics

**Art:** Funktion

```typescript
function formatFlintDiagnostics(diagnostics: readonly FlintDiagnostic[]): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name     | Geben Sie                          | ein Beschreibung |
| -------- | ---------------------------------- | ---------------- |
| Diagnose | schreibgeschützt FlintDiagnostic[] |                  |

## `src/harness`

### createFlintTestHarness

**Art:** Funktion

```typescript
function createFlintTestHarness(options: FlintTestHarnessOptions = {}): FlintTestHarness;
```

Erstellen Sie einen gemeinsamen Geräte-Kabelbaum mit optionalen Compiler-, Funktions- und Zieleinstellungen.

#### Parameter

| Name     | Geben Sie               | ein Beschreibung |
| -------- | ----------------------- | ---------------- |
| Optionen | FlintTestHarnessOptions |                  |

### FlintCapabilityFunction

**Art:** Typ

```typescript
export type FlintCapabilityFunction = (...arguments_: readonly unknown[]) => unknown;
```

Ein Host-Rückruf, der für einen im Manifest deklarierten Funktionsimport bereitgestellt wird.

### FlintCapabilityImports

**Art:** Typ

```typescript
export type FlintCapabilityImports = Readonly<Record<string, Readonly<Record<string, FlintCapabilityFunction>>>>;
```

Host-Funktionen, kodiert nach Fähigkeit und Manifest-Alias; Nicht angemeldete Einfuhren werden abgelehnt.

### FlintCompilationResult

**Art:** Schnittstelle

```typescript
export interface FlintCompilationResult extends FlintCompiledModule
```

Kompilierungsausgabe plus Diagnose, geeignet für gültige und diagnostische Vorrichtungen.

### flintFixtureName

**Art:** Funktion

```typescript
function flintFixtureName(fileName: string, fixtureRoot: string): string;
```

Konvertieren Sie einen Fixture-Pfad in einen stabilen Test-/Modulnamen. Relative Pfade sind
wurzelt bei FixtureRoot und Trennzeichen werden auf Bindestriche normalisiert.

#### Parameter

| Name        | Geben Sie    | ein Beschreibung |
| ----------- | ------------ | ---------------- |
| Dateiname   | Zeichenfolge |                  |
| FixtureRoot | Zeichenfolge |                  |

### FlintInspectionResult

**Art:** Schnittstelle

```typescript
export interface FlintInspectionResult
```

Das Ergebnis der Artefaktprüfung wurde ohne Instanziierung von Wasm zurückgegeben.

### FlintLoadedExports

**Art:** Typ

```typescript
export type FlintLoadedExports<TExports extends object> = TExports;
```

Ansicht der vom Harness anhand der Wasm-Instanz validierten Exporte auf Typebene.

### FlintTestHarness

**Art:** Klasse

```typescript
export class FlintTestHarness
```

Kompilieren und laden Sie gemeinsam genutzte FWS-Geräte über dieselbe Wasm-Grenze, die von verwendet wird
Verbraucher. Der Kabelbaum stellt außerdem WAT, Manifest, Diagramm und selbstgehostet bereit
Inspektionsdaten für Konformitätstests.

### FlintTestHarnessDisposedError

**Art:** Klasse

```typescript
export class FlintTestHarnessDisposedError extends FlintTestHarnessError
```

Wird angehoben, wenn ein entsorgter Gurt erneut verwendet wird.

### FlintTestHarnessError

**Art:** Klasse

```typescript
export class FlintTestHarnessError extends Error
```

Basistypfehler für Geräteauflösungs-, Kompilierungs-, Import- und Ladefehler.

### FlintTestHarnessOptions

**Art:** Schnittstelle

```typescript
export interface FlintTestHarnessOptions extends FlintPluginOptions
```

Compiler- und Fixture-Root-Optionen, die von den Conformance Harness-Helfern gemeinsam genutzt werden.

## `src/vitest`

### defineFlintVitestConfig

**Art:** Funktion

```typescript
function defineFlintVitestConfig(options: FlintVitestConfigOptions = {}): ViteUserConfig;
```

Erstellen Sie die Standardkonfiguration der Mission Platform Vitest mit Forge Web Script.
Verbraucherüberschreibungen werden zusammengeführt statt ersetzt, einschließlich ihrer Plugins;
generierte FWS-Geräteabfragen wie `?flint-wat` bleiben verfügbar.

#### Parameter

| Name     | Geben Sie                | ein Beschreibung |
| -------- | ------------------------ | ---------------- |
| Optionen | FlintVitestConfigOptions |                  |

### FlintVitestConfigOptions

**Art:** Schnittstelle

```typescript
export interface FlintVitestConfigOptions extends Omit<VitestConfigOptions, 'overrides'>
```

Keine Beschreibung angegeben.

### flintVitestPlugin

**Art:** Funktion

```typescript
function flintVitestPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installieren Sie das Produktions-Forge-Webskript-Plugin in einer Vite/Vitest-Konfiguration.

#### Parameter

| Name     | Geben Sie          | ein Beschreibung |
| -------- | ------------------ | ---------------- |
| Optionen | FlintPluginOptions |                  |
