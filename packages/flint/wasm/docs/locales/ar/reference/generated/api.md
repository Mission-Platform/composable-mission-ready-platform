# مرجع @mission-platform/flint-wasm API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/flint/wasm/docs/reference/generated/api.md: [packages/flint/wasm/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/flint-wasm`.

## `src/collection-runtime`

### buildFlintWasmCollectionRuntimeBodies

**النوع:** الوظيفة

```typescript
function buildFlintWasmCollectionRuntimeBodies(): readonly FlintWasmRuntimeBody[];
```

لم يتم تقديم أي وصف.

### buildFlintWasmCollectionRuntimeWasmBodies

**النوع:** الوظيفة

```typescript
function buildFlintWasmCollectionRuntimeWasmBodies(allocatorIndex: number): readonly number[][];
```

تستخدم أجسام وقت التشغيل مقابض i32. المصفوفات هي [الطول، العناصر...] والمتجهات
هي [مؤشر البيانات، الطول، السعة]، مع فتحات عناصر عددية بأربعة بايت.
مكررات المصفوفة هي [مقبض المصفوفة، الفهرس التالي]. تستخدم نتائج التكرار
معبأة i64 `(done << 32) | i32 value` ABI.

#### حدود

| الاسم        | اكتب | الوصف |
| ------------ | ---- | ----- |
| مؤشر التخصيص | رقم  |       |

### FlintWasmRuntimeBody

**النوع:** الواجهة

```typescript
export interface FlintWasmRuntimeBody
```

لم يتم تقديم أي وصف.

## `src/contracts`

### ميزات FlintTarget

**النوع:** الواجهة

```typescript
export interface FlintTargetFeatures
```

لم يتم تقديم أي وصف.

### FlintWasmArtifactMetadata

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactMetadata
```

لم يتم تقديم أي وصف.

### FlintWasmAsyncCapability

**النوع:** النوع

```typescript
export type FlintWasmAsyncCapability = 'scheduler.microtask' | 'scheduler.worker';
```

لم يتم تقديم أي وصف.

### FlintWasmAsyncContract

**النوع:** الواجهة

```typescript
export interface FlintWasmAsyncContract
```

لم يتم تقديم أي وصف.

### FlintWasmBackendInput

**النوع:** الواجهة

```typescript
export interface FlintWasmBackendInput
```

لم يتم تقديم أي وصف.

### FlintWasmBackendResult

**النوع:** الواجهة

```typescript
export interface FlintWasmBackendResult
```

لم يتم تقديم أي وصف.

### FlintWasmBinaryOperator

**النوع:** النوع

```typescript
export type FlintWasmBinaryOperator = '!=' | '%' | '&&' | '*' | '+' | '-' | '/' | '<' | '<=' | '==' | '>' | '>=' | '||';
```

لم يتم تقديم أي وصف.

### FlintWasmCapabilityImport

**النوع:** الواجهة

```typescript
export interface FlintWasmCapabilityImport
```

لم يتم تقديم أي وصف.

### FlintWasmCollectionLayout

**النوع:** الواجهة

```typescript
export interface FlintWasmCollectionLayout
```

لم يتم تقديم أي وصف.

### FlintWasmCompilerHints

**النوع:** الواجهة

```typescript
export interface FlintWasmCompilerHints
```

لم يتم تقديم أي وصف.

### FlintWasmDiagnostic

**النوع:** الواجهة

```typescript
export interface FlintWasmDiagnostic
```

لم يتم تقديم أي وصف.

### FlintWasmEnumDeclaration

**النوع:** الواجهة

```typescript
export interface FlintWasmEnumDeclaration
```

لم يتم تقديم أي وصف.

### FlintWasmExpression

**النوع:** النوع

```typescript
export type FlintWasmExpression = |
```

لم يتم تقديم أي وصف.

### متطلبات ميزة FlintWasm

**النوع:** الواجهة

```typescript
export interface FlintWasmFeatureRequirements
```

لم يتم تقديم أي وصف.

### FlintWasmFunction

**النوع:** الواجهة

```typescript
export interface FlintWasmFunction
```

لم يتم تقديم أي وصف.

### FlintWasmGenericSpecialization

**النوع:** الواجهة

```typescript
export interface FlintWasmGenericSpecialization
```

لم يتم تقديم أي وصف.

### FlintWasmIteratorBoundaryDescriptor

**النوع:** الواجهة

```typescript
export interface FlintWasmIteratorBoundaryDescriptor
```

لم يتم تقديم أي وصف.

### FlintWasmIteratorCapability

**النوع:** النوع

```typescript
export type FlintWasmIteratorCapability = 'linear' | 'random-access';
```

لم يتم تقديم أي وصف.

### FlintWasmIteratorCapabilityDescriptor

**النوع:** الواجهة

```typescript
export interface FlintWasmIteratorCapabilityDescriptor
```

لم يتم تقديم أي وصف.

### FlintWasmIteratorExport

**النوع:** الواجهة

```typescript
export interface FlintWasmIteratorExport
```

لم يتم تقديم أي وصف.

### FlintWasmLogger

**النوع:** الواجهة

```typescript
export interface FlintWasmLogger
```

لم يتم تقديم أي وصف.

### FlintWasmModule

**النوع:** الواجهة

```typescript
export interface FlintWasmModule
```

لم يتم تقديم أي وصف.

### FlintWasmParameter

**النوع:** الواجهة

```typescript
export interface FlintWasmParameter
```

لم يتم تقديم أي وصف.

### FlintWasmPrimitiveType

**النوع:** النوع

```typescript
export type FlintWasmPrimitiveType =
  'bool' | 'bytes' | 'f32' | 'f64' | 'i32' | 'i64' | 'string' | 'u32' | 'u64' | 'unit';
```

لم يتم تقديم أي وصف.

### FlintWasmSourceImport

**النوع:** الواجهة

```typescript
export interface FlintWasmSourceImport
```

لم يتم تقديم أي وصف.

### FlintWasmSourceSpan

**النوع:** الواجهة

```typescript
export interface FlintWasmSourceSpan
```

لم يتم تقديم أي وصف.

### FlintWasmStandardLibraryOperation

**النوع:** النوع

```typescript
export type FlintWasmStandardLibraryOperation =
  | 'full-match'
  | 'prefix-match'
  | 'search'
  | 'full-capture-start'
  | 'full-capture-end'
  | 'prefix-capture-start'
  | 'prefix-capture-end'
  | 'search-capture-start'
  | 'search-capture-end'
  | 'string-concat'
  | 'string-length'
  | 'string-byte-at'
  | 'string-starts-with'
  | 'string-slice'
  | 'string-to-i32'
  | 'bytes-length'
  | 'bytes-length-u32'
  | 'bytes-byte-at'
  | 'bytes-byte-at-u32'
  | 'bytes-slice'
  | 'memory-alloc'
  | 'memory-dealloc'
  | 'memory-realloc'
  | 'memory-load-u32'
  | 'memory-store-u32'
  | 'memory-load-f64'
  | 'memory-store-f64'
  | 'f64-from-u32'
  | 'vector-new'
  | 'vector-push'
  | 'vector-get'
  | 'vector-set'
  | 'vector-length'
  | 'vector-pop'
  | 'array-new'
  | 'array-get'
  | 'array-set'
  | 'array-length'
  | 'array-iter'
  | 'iterator-next'
  | 'set-has'
  | 'map-get'
  | 'ecs-query'
  | 'ecs-transition'
  | 'signal-schedule'
  | 'async-schedule-microtask'
  | 'async-worker-post'
  | 'async-worker-receive';
```

لم يتم تقديم أي وصف.

### FlintWasmStatement

**النوع:** النوع

```typescript
export type FlintWasmStatement = |
```

لم يتم تقديم أي وصف.

### FlintWasmTypeName

**النوع:** الواجهة

```typescript
export interface FlintWasmTypeName
```

لم يتم تقديم أي وصف.

## `src/emitter`

### compileFlintWasm

**النوع:** الوظيفة

```typescript
function compileFlintWasm(input: FlintWasmBackendInput, fileName = '<input>'): FlintWasmBackendResult;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم     | اكتب                  | الوصف |
| --------- | --------------------- | ----- |
| الإدخال   | FlintWasmBackendInput |       |
| اسم الملف |                       |       |

## `src/verifier`

### FlintWasmArtifactManifest

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactManifest
```

يقوم المدقق باستهلاك البيان الهيكلي عمدًا لتجنب دورة الحزمة.

### FlintWasmArtifactVerificationDiagnostic

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactVerificationDiagnostic
```

لم يتم تقديم أي وصف.

### FlintWasmArtifactVerificationInput

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactVerificationInput
```

لم يتم تقديم أي وصف.

### FlintWasmArtifactVerificationPolicy

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactVerificationPolicy
```

لم يتم تقديم أي وصف.

### FlintWasmArtifactVerificationResult

**النوع:** الواجهة

```typescript
export interface FlintWasmArtifactVerificationResult
```

لم يتم تقديم أي وصف.

### FlintWasmArtifactVerificationSeverity

**النوع:** النوع

```typescript
export type FlintWasmArtifactVerificationSeverity = 'error' | 'warning' | 'info';
```

لم يتم تقديم أي وصف.

### FlintWasmManifestFunction

**النوع:** الواجهة

```typescript
export interface FlintWasmManifestFunction
```

لم يتم تقديم أي وصف.

### FlintWasmManifestImport

**النوع:** الواجهة

```typescript
export interface FlintWasmManifestImport
```

لم يتم تقديم أي وصف.

### FlintWasmManifestParameter

**النوع:** الواجهة

```typescript
export interface FlintWasmManifestParameter
```

لم يتم تقديم أي وصف.

### FlintWasmMemoryLayout

**النوع:** الواجهة

```typescript
export interface FlintWasmMemoryLayout
```

لم يتم تقديم أي وصف.

### VerifyFlintWasmArtifact

**النوع:** الوظيفة

```typescript
function verifyFlintWasmArtifact(input: FlintWasmArtifactVerificationInput): FlintWasmArtifactVerificationResult;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم   | اكتب                               | الوصف |
| ------- | ---------------------------------- | ----- |
| الإدخال | FlintWasmArtifactVerificationInput |       |

## `src/wat`

### FlintWasmWatMetadata

**النوع:** الواجهة

```typescript
export interface FlintWasmWatMetadata
```

لم يتم تقديم أي وصف.

### renderFlintWasmWat

**النوع:** الوظيفة

```typescript
function renderFlintWasmWat(module: FlintWasmModule, metadata: FlintWasmWatMetadata = {}): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم            | اكتب                 | الوصف |
| ---------------- | -------------------- | ----- |
| الوحدة النمطية   | FlintWasmModule      |       |
| البيانات الوصفية | FlintWasmWatMetadata |       |
