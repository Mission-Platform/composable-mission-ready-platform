# Riferimento API @mission-platform/flint-cli

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/flint-cli/docs/reference/generated/api.md: [packages/flint-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/flint-cli`.

## `src/args`

### FLINT_CLI_USAGE

**Genere:** costante

```typescript
export const FLINT_CLI_USAGE;
```

Nessuna descrizione fornita.

### FlintCliCommand

**Genere:** tipo

```typescript
export type FlintCliCommand = 'check' | 'compile' | 'trace';
```

Nessuna descrizione fornita.

### FlintCliOptions

**Tipo:** interfaccia

```typescript
export interface FlintCliOptions
```

Nessuna descrizione fornita.

### FlintCliTraceOptions

**Tipo:** interfaccia

```typescript
export interface FlintCliTraceOptions
```

Nessuna descrizione fornita.

### Errore FlintCliUsage

**Gentile:** lezione

```typescript
export class FlintCliUsageError extends Error
```

Nessuna descrizione fornita.

### parseFlintCliArgs

**Tipo:** funzione

```typescript
function parseFlintCliArgs(argv: readonly string[], cwd = process.cwd()): FlintCliOptions;
```

Nessuna descrizione fornita.

#### Parametri

| Nome | Digitare                  | Descrizione |
| ---- | ------------------------- | ----------- |
| argv | stringa di sola lettura[] |             |
| cwd  |                           |             |

## `src/main`

### FLINT_CLI_COMPILATION_EXIT_CODE

**Genere:** costante

```typescript
export const FLINT_CLI_COMPILATION_EXIT_CODE;
```

Nessuna descrizione fornita.

### FLINT_CLI_USAGE_EXIT_CODE

**Genere:** costante

```typescript
export const FLINT_CLI_USAGE_EXIT_CODE;
```

Nessuna descrizione fornita.

### FlintCliIo

**Tipo:** interfaccia

```typescript
export interface FlintCliIo
```

Nessuna descrizione fornita.

### eseguireFlintCli

**Tipo:** funzione

```typescript
function runFlintCli(
  argv: readonly string[] = process.argv.slice(2),
  io: FlintCliIo = defaultIo,
  cwd = process.cwd(),
): Promise<number>;
```

Nessuna descrizione fornita.

#### Parametri

| Nome | Digitare                  | Descrizione |
| ---- | ------------------------- | ----------- |
| argv | stringa di sola lettura[] |             |
| io   | FlintCliIo                |             |
| cwd  |                           |             |

## `src/output`

### artifactFilesFor

**Tipo:** funzione

```typescript
function artifactFilesFor(artifact: FlintArtifact): FlintCliArtifactFiles;
```

Nessuna descrizione fornita.

#### Parametri

| Nome      | Digitare      | Descrizione |
| --------- | ------------- | ----------- |
| artefatto | FlintArtifact |             |

### flintArtifactBaseName

**Tipo:** funzione

```typescript
function flintArtifactBaseName(entryFileName: string): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome         | Digitare | Descrizione |
| ------------ | -------- | ----------- |
| voceNomeFile | stringa  |             |

### FlintCliArtifactFiles

**Tipo:** interfaccia

```typescript
export interface FlintCliArtifactFiles
```

Nessuna descrizione fornita.

### formatFlintDiagnostics

**Tipo:** funzione

```typescript
function formatFlintDiagnostics(
  diagnostics: readonly {
    readonly code: string;
    readonly severity: string;
    readonly phase: string;
    readonly message: string;
    readonly fileName: string;
    readonly span: {
      readonly line: number;
      readonly column: number;
      readonly endLine: number;
      readonly endColumn: number;
    };
    readonly hint?: string;
  }[],
): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                                                                                                                                                                                                                                                                                                                                                                                | Descrizione |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| diagnostica | sola lettura {codice sola lettura: stringa; gravità di sola lettura: stringa; fase di sola lettura: stringa; messaggio di sola lettura: stringa; nomefile di sola lettura: stringa; span di sola lettura: { riga di sola lettura: numero; colonna di sola lettura: numero; sola lettura endLine: numero; sola lettura endColumn: numero; }; suggerimento di sola lettura?: stringa; }[] |             |

### outputDirectoryFor

**Tipo:** funzione

```typescript
function outputDirectoryFor(
  entryFileName: string,
  outputDirectory = path.join(path.dirname(entryFileName), 'dist'),
): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome            | Digitare | Descrizione |
| --------------- | -------- | ----------- |
| voceNomeFile    | stringa  |             |
| outputDirectory |          |             |

### writeFlintArtifacts

**Tipo:** funzione

```typescript
function writeFlintArtifacts(
  outputDirectory: string,
  entryFileName: string,
  artifact: FlintArtifact,
): Promise<readonly string[]>;
```

Scrivi il set completo di artefatti tramite una directory temporanea e rinomina ciascun file nella sua posizione.

#### Parametri

| Nome            | Digitare      | Descrizione |
| --------------- | ------------- | ----------- |
| outputDirectory | stringa       |             |
| voceNomeFile    | stringa       |             |
| artefatto       | FlintArtifact |             |
