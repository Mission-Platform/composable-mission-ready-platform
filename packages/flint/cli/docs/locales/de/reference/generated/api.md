# @mission-platform/flint-cli API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/flint-cli/docs/reference/generated/api.md: [packages/flint-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/flint-cli`.

## `src/args`

### FLINT_CLI_USAGE

**Art:** konstant

```typescript
export const FLINT_CLI_USAGE;
```

Keine Beschreibung angegeben.

### FlintCliCommand

**Art:** Typ

```typescript
export type FlintCliCommand = 'check' | 'compile' | 'trace';
```

Keine Beschreibung angegeben.

### FlintCliOptions

**Art:** Schnittstelle

```typescript
export interface FlintCliOptions
```

Keine Beschreibung angegeben.

### FlintCliTraceOptions

**Art:** Schnittstelle

```typescript
export interface FlintCliTraceOptions
```

Keine Beschreibung angegeben.

### FlintCliUsageError

**Art:** Klasse

```typescript
export class FlintCliUsageError extends Error
```

Keine Beschreibung angegeben.

### parseFlintCliArgs

**Art:** Funktion

```typescript
function parseFlintCliArgs(argv: readonly string[], cwd = process.cwd()): FlintCliOptions;
```

Keine Beschreibung angegeben.

#### Parameter

| Name | Geben Sie                   | ein Beschreibung |
| ---- | --------------------------- | ---------------- |
| argv | schreibgeschützter String[] |                  |
| cwd  |                             |                  |

## `src/main`

### FLINT_CLI_COMPILATION_EXIT_CODE

**Art:** konstant

```typescript
export const FLINT_CLI_COMPILATION_EXIT_CODE;
```

Keine Beschreibung angegeben.

### FLINT_CLI_USAGE_EXIT_CODE

**Art:** konstant

```typescript
export const FLINT_CLI_USAGE_EXIT_CODE;
```

Keine Beschreibung angegeben.

### FlintCliIo

**Art:** Schnittstelle

```typescript
export interface FlintCliIo
```

Keine Beschreibung angegeben.

### runFlintCli

**Art:** Funktion

```typescript
function runFlintCli(
  argv: readonly string[] = process.argv.slice(2),
  io: FlintCliIo = defaultIo,
  cwd = process.cwd(),
): Promise<number>;
```

Keine Beschreibung angegeben.

#### Parameter

| Name | Geben Sie                   | ein Beschreibung |
| ---- | --------------------------- | ---------------- |
| argv | schreibgeschützter String[] |                  |
| io   | FlintCliIo                  |                  |
| cwd  |                             |                  |

## `src/output`

### artefaktFilesFor

**Art:** Funktion

```typescript
function artifactFilesFor(artifact: FlintArtifact): FlintCliArtifactFiles;
```

Keine Beschreibung angegeben.

#### Parameter

| Name     | Geben Sie     | ein Beschreibung |
| -------- | ------------- | ---------------- |
| Artefakt | FlintArtifact |                  |

### flintArtifactBaseName

**Art:** Funktion

```typescript
function flintArtifactBaseName(entryFileName: string): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name             | Geben Sie    | ein Beschreibung |
| ---------------- | ------------ | ---------------- |
| EintragDateiname | Zeichenfolge |                  |

### FlintCliArtifactFiles

**Art:** Schnittstelle

```typescript
export interface FlintCliArtifactFiles
```

Keine Beschreibung angegeben.

### formatFlintDiagnostics

**Art:** Funktion

```typescript
function formatFlintDiagnostics(
  diagnostics: readonly {
    readonly code: string;
    readonly severity: string;
    readonly phase: string;
    readonly message: string;
    readonly fileName: string;
    readonly span: {
      readonly line: number;
      readonly column: number;
      readonly endLine: number;
      readonly endColumn: number;
    };
    readonly hint?: string;
  }[],
): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name     | Geben Sie                                                                                                                                                                                                                                                                                                                                                                                   | ein Beschreibung |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| Diagnose | readonly { schreibgeschützter Code: string; schreibgeschützter Schweregrad: Zeichenfolge; schreibgeschützte Phase: string; schreibgeschützte Nachricht: string; schreibgeschützter Dateiname: string; readonly span: { readonly line: number; schreibgeschützte Spalte: Zahl; schreibgeschützt endLine: Zahl; schreibgeschützt endColumn: Zahl; }; schreibgeschützter Hinweis?: string; }[] |                  |

### AusgabeverzeichnisFür

**Art:** Funktion

```typescript
function outputDirectoryFor(
  entryFileName: string,
  outputDirectory = path.join(path.dirname(entryFileName), 'dist'),
): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name               | Geben Sie    | ein Beschreibung |
| ------------------ | ------------ | ---------------- |
| EintragDateiname   | Zeichenfolge |                  |
| Ausgabeverzeichnis |              |                  |

### writeFlintArtifacts

**Art:** Funktion

```typescript
function writeFlintArtifacts(
  outputDirectory: string,
  entryFileName: string,
  artifact: FlintArtifact,
): Promise<readonly string[]>;
```

Schreiben Sie den gesamten Artefaktsatz über ein temporäres Verzeichnis und benennen Sie jede Datei an der richtigen Stelle um.

#### Parameter

| Name               | Geben Sie     | ein Beschreibung |
| ------------------ | ------------- | ---------------- |
| Ausgabeverzeichnis | Zeichenfolge  |                  |
| EintragDateiname   | Zeichenfolge  |                  |
| Artefakt           | FlintArtifact |                  |
