# Référence API @mission-platform/flint-cli

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/flint-cli/docs/reference/generated/api.md: [packages/flint-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/flint-cli`.

## `src/args`

### FLINT_CLI_USAGE

**Genre :** constante

```typescript
export const FLINT_CLI_USAGE;
```

Aucune description fournie.

### FlintCliCommand

**Genre :** type

```typescript
export type FlintCliCommand = 'check' | 'compile' | 'trace';
```

Aucune description fournie.

### FlintCliOptions

**Genre :** interface

```typescript
export interface FlintCliOptions
```

Aucune description fournie.

### FlintCliTraceOptions

**Genre :** interface

```typescript
export interface FlintCliTraceOptions
```

Aucune description fournie.

### FlintCliUsageError

**Genre :** classe

```typescript
export class FlintCliUsageError extends Error
```

Aucune description fournie.

### parseFlintCliArgs

**Genre :** fonction

```typescript
function parseFlintCliArgs(argv: readonly string[], cwd = process.cwd()): FlintCliOptions;
```

Aucune description fournie.

#### Paramètres

| Nom  | Tapez                     | Descriptif |
| ---- | ------------------------- | ---------- |
| argv | chaîne en lecture seule[] |            |
| cwd  |                           |            |

## `src/main`

### FLINT_CLI_COMPILATION_EXIT_CODE

**Genre :** constante

```typescript
export const FLINT_CLI_COMPILATION_EXIT_CODE;
```

Aucune description fournie.

### FLINT_CLI_USAGE_EXIT_CODE

**Genre :** constante

```typescript
export const FLINT_CLI_USAGE_EXIT_CODE;
```

Aucune description fournie.

### FlintCliIo

**Genre :** interface

```typescript
export interface FlintCliIo
```

Aucune description fournie.

### runFlintCli

**Genre :** fonction

```typescript
function runFlintCli(
  argv: readonly string[] = process.argv.slice(2),
  io: FlintCliIo = defaultIo,
  cwd = process.cwd(),
): Promise<number>;
```

Aucune description fournie.

#### Paramètres

| Nom  | Tapez                     | Descriptif |
| ---- | ------------------------- | ---------- |
| argv | chaîne en lecture seule[] |            |
| io   | FlintCliIo                |            |
| cwd  |                           |            |

## `src/output`

### artefactFilesFor

**Genre :** fonction

```typescript
function artifactFilesFor(artifact: FlintArtifact): FlintCliArtifactFiles;
```

Aucune description fournie.

#### Paramètres

| Nom      | Tapez         | Descriptif |
| -------- | ------------- | ---------- |
| artefact | FlintArtefact |            |

### flintArtifactBaseName

**Genre :** fonction

```typescript
function flintArtifactBaseName(entryFileName: string): string;
```

Aucune description fournie.

#### Paramètres

| Nom              | Tapez  | Descriptif |
| ---------------- | ------ | ---------- |
| NomFichierEntrée | chaîne |            |

### FlintCliArtifactFiles

**Genre :** interface

```typescript
export interface FlintCliArtifactFiles
```

Aucune description fournie.

### formatFlintDiagnostics

**Genre :** fonction

```typescript
function formatFlintDiagnostics(
  diagnostics: readonly {
    readonly code: string;
    readonly severity: string;
    readonly phase: string;
    readonly message: string;
    readonly fileName: string;
    readonly span: {
      readonly line: number;
      readonly column: number;
      readonly endLine: number;
      readonly endColumn: number;
    };
    readonly hint?: string;
  }[],
): string;
```

Aucune description fournie.

#### Paramètres

| Nom        | Tapez                                                                                                                                                                                                                                                                                                                                                                                                                     | Descriptif |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| diagnostic | lecture seule { code en lecture seule : chaîne ; gravité en lecture seule : chaîne ; phase en lecture seule : chaîne ; message en lecture seule : chaîne ; nom de fichier en lecture seule : chaîne ; étendue en lecture seule : { ligne en lecture seule : nombre ; colonne en lecture seule : numéro ; lecture seule endLine : numéro ; lecture seule endColumn : nombre ; } ; indice en lecture seule ? : chaîne ; }[] |            |

### Répertoire de sortiePour

**Genre :** fonction

```typescript
function outputDirectoryFor(
  entryFileName: string,
  outputDirectory = path.join(path.dirname(entryFileName), 'dist'),
): string;
```

Aucune description fournie.

#### Paramètres

| Nom                  | Tapez  | Descriptif |
| -------------------- | ------ | ---------- |
| NomFichierEntrée     | chaîne |            |
| Répertoire de sortie |        |            |

### writeFlintArtifacts

**Genre :** fonction

```typescript
function writeFlintArtifacts(
  outputDirectory: string,
  entryFileName: string,
  artifact: FlintArtifact,
): Promise<readonly string[]>;
```

Écrivez l'ensemble complet d'artefacts dans un répertoire temporaire et renommez chaque fichier en place.

#### Paramètres

| Nom                  | Tapez         | Descriptif |
| -------------------- | ------------- | ---------- |
| Répertoire de sortie | chaîne        |            |
| NomFichierEntrée     | chaîne        |            |
| artefact             | FlintArtefact |            |
