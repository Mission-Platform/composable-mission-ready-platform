# مرجع @mission-platform/icons API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/icons/docs/reference/generated/api.md: [packages/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/icons`.

## `src/catalog`

### ICON_CATALOG

**النوع:** ثابت

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[]
```

الكتالوج الذي تمت مراجعته لمجموعة الأيقونات الموجودة.

### ICON_GAP_REVIEW

**النوع:** ثابت

```typescript
export const ICON_GAP_REVIEW
```

إضافات عالية القيمة سيتم تنفيذها بعد إنشاء تصنيف المصدر.

### IconCatalogEntry

**النوع:** الواجهة

```typescript
export interface IconCatalogEntry
```

تعيين فئة ثابتة لكل مصدر رمز في هذه الحزمة.

### validateIconCatalog

**النوع:** الوظيفة

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void
```

تحقق من صحة الكتالوج الثابت قبل استخدامه لإنشاء بيانات تعريف الحزمة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| كتالوج | للقراءة فقط IconCatalogEntry[] |  |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### ForgeIconBell

**النوع:** الوظيفة

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement
```

`ForgeIconBell` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-bell.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconBellProperties> |  |

### IconBellProperties

**النوع:** الواجهة

```typescript
export interface IconBellProperties
```

لم يتم تقديم أي وصف.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### com.ForgeIconChat

**النوع:** الوظيفة

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement
```

`ForgeIconChat` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-chat.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconChatProperties> |  |

### IconChatProperties

**النوع:** الواجهة

```typescript
export interface IconChatProperties
```

لم يتم تقديم أي وصف.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### ForgeIconMail

**النوع:** الوظيفة

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement
```

`ForgeIconMail` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-mail.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMailProperties> |  |

### IconMailProperties

**النوع:** الواجهة

```typescript
export interface IconMailProperties
```

لم يتم تقديم أي وصف.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### ForgeIconPhone

**النوع:** الوظيفة

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement
```

`ForgeIconPhone` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-phone.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPhoneProperties> |  |

### IconPhoneProperties

**النوع:** الواجهة

```typescript
export interface IconPhoneProperties
```

لم يتم تقديم أي وصف.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### ForgeIconSend

**النوع:** الوظيفة

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement
```

`ForgeIconSend` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-send.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSendProperties> |  |

### IconSendProperties

**النوع:** الواجهة

```typescript
export interface IconSendProperties
```

لم يتم تقديم أي وصف.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### ForgeIconShare

**النوع:** الوظيفة

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement
```

`ForgeIconShare` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-share.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconShareProperties> |  |

### IconShareProperties

**النوع:** الواجهة

```typescript
export interface IconShareProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### ForgeIconCopy

**النوع:** الوظيفة

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement
```

`ForgeIconCopy` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-copy.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCopyProperties> |  |

### IconCopyProperties

**النوع:** الواجهة

```typescript
export interface IconCopyProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### ForgeIconEdit

**النوع:** الوظيفة

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement
```

`ForgeIconEdit` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-edit.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconEditProperties> |  |

### IconEditProperties

**النوع:** الواجهة

```typescript
export interface IconEditProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### ForgeIconEyeOff

**النوع:** الوظيفة

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement
```

`ForgeIconEyeOff` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-eye-off.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconEyeOffProperties> |  |

### IconEyeOffProperties

**النوع:** الواجهة

```typescript
export interface IconEyeOffProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### ForgeIconEye

**النوع:** الوظيفة

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement
```

`ForgeIconEye` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-eye.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconEyeProperties> |  |

### IconEyeProperties

**النوع:** الواجهة

```typescript
export interface IconEyeProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### ForgeIconRedo

**النوع:** الوظيفة

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement
```

يعيد تطبيق آخر تعديل للمحتوى تم التراجع عنه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconRedoProperties> |  |

### IconRedoProperties

**النوع:** الواجهة

```typescript
export interface IconRedoProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### ForgeIconTrash

**النوع:** الوظيفة

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement
```

`ForgeIconTrash` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-trash.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTrashProperties> |  |

### IconTrashProperties

**النوع:** الواجهة

```typescript
export interface IconTrashProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### ForgeIconUndo

**النوع:** الوظيفة

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement
```

يعكس أحدث تعديل للمحتوى.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconUndoProperties> |  |

### IconUndoProperties

**النوع:** الواجهة

```typescript
export interface IconUndoProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIconDownload

**النوع:** الوظيفة

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement
```

`ForgeIconDownload` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-download.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDownloadProperties> |  |

### أيقونة تحميل الخصائص

**النوع:** الواجهة

```typescript
export interface IconDownloadProperties
```

لم يتم تقديم أي وصف.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconUpload

**النوع:** الوظيفة

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement
```

`ForgeIconUpload` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-upload.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconUploadProperties> |  |

### IconUploadProperties

**النوع:** الواجهة

```typescript
export interface IconUploadProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### ForgeIconFilter

**النوع:** الوظيفة

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement
```

`ForgeIconFilter` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-filter.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconFilterProperties> |  |

### IconFilterProperties

**النوع:** الواجهة

```typescript
export interface IconFilterProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### ForgeIconSort

**النوع:** الوظيفة

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement
```

`ForgeIconSort` — مؤشر فرز ثنائي الشيفرون تم تأليفه مرة واحدة في JSX المحايد
لهجة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-sort.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSortProperties> |  |

### IconSortProperties

**النوع:** الواجهة

```typescript
export interface IconSortProperties
```

لم يتم تقديم أي وصف.

### اتجاه الفرز

**النوع:** النوع

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

اتجاه الفرز النشط، أو `undefined` عند عدم الفرز.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIconTableColumnAdd

**النوع:** الوظيفة

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement
```

`ForgeIconTableColumnAdd` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-table-column-add.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTableColumnAddProperties> |  |

### IconTableColumnAddProperties

**النوع:** الواجهة

```typescript
export interface IconTableColumnAddProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnRemove

**النوع:** الوظيفة

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement
```

`ForgeIconTableColumnRemove` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-table-column-remove.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTableColumnRemoveProperties> |  |

### IconTableColumnRemoveProperties

**النوع:** الواجهة

```typescript
export interface IconTableColumnRemoveProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

**النوع:** الوظيفة

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement
```

`ForgeIconTableRowAdd` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-table-row-add.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTableRowAddProperties> |  |

### IconTableRowAddProperties

**النوع:** الواجهة

```typescript
export interface IconTableRowAddProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowRemove

**النوع:** الوظيفة

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement
```

`ForgeIconTableRowRemove` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-table-row-remove.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTableRowRemoveProperties> |  |

### IconTableRowRemoveProperties

**النوع:** الواجهة

```typescript
export interface IconTableRowRemoveProperties
```

لم يتم تقديم أي وصف.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIconTable

**النوع:** الوظيفة

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement
```

`ForgeIconTable` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-table.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconTableProperties> |  |

### IconTableProperties

**النوع:** الواجهة

```typescript
export interface IconTableProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### ForgeIconDrawCircle

**النوع:** الوظيفة

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement
```

`ForgeIconDrawCircle` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-draw-circle.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDrawCircleProperties> |  |

### IconDrawCircleProperties

**النوع:** الواجهة

```typescript
export interface IconDrawCircleProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### ForgeIconDrawLine

**النوع:** الوظيفة

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement
```

`ForgeIconDrawLine` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-draw-line.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDrawLineProperties> |  |

### IconDrawLineProperties

**النوع:** الواجهة

```typescript
export interface IconDrawLineProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### ForgeIconDrawPolygon

**النوع:** الوظيفة

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement
```

`ForgeIconDrawPolygon` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-draw-polygon.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDrawPolygonProperties> |  |

### IconDrawPolygonProperties

**النوع:** الواجهة

```typescript
export interface IconDrawPolygonProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### ForgeIconDrawSquare

**النوع:** الوظيفة

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement
```

`ForgeIconDrawSquare` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-draw-square.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDrawSquareProperties> |  |

### IconDrawSquareProperties

**النوع:** الواجهة

```typescript
export interface IconDrawSquareProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### ForgeIconDrawTriangle

**النوع:** الوظيفة

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement
```

`ForgeIconDrawTriangle` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-draw-triangle.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDrawTriangleProperties> |  |

### IconDrawTriangleProperties

**النوع:** الواجهة

```typescript
export interface IconDrawTriangleProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### ForgeIconMove

**النوع:** الوظيفة

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement
```

`ForgeIconMove` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-move.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMoveProperties> |  |

### IconMoveProperties

**النوع:** الواجهة

```typescript
export interface IconMoveProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### ForgeIconPalette

**النوع:** الوظيفة

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement
```

`ForgeIconPalette` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-palette.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPaletteProperties> |  |

### IconPaletteProperties

**النوع:** الواجهة

```typescript
export interface IconPaletteProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### ForgeIconPencil

**النوع:** الوظيفة

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement
```

`ForgeIconPencil` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-pencil.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPencilProperties> |  |

### IconPencilProperties

**النوع:** الواجهة

```typescript
export interface IconPencilProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### ForgeIconRotateCcw

**النوع:** الوظيفة

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement
```

`ForgeIconRotateCcw` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-rotate-ccw.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconRotateCcwProperties> |  |

### IconRotateCcwProperties

**النوع:** الواجهة

```typescript
export interface IconRotateCcwProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### ForgeIconRotateCw

**النوع:** الوظيفة

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement
```

`ForgeIconRotateCw` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-rotate-cw.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconRotateCwProperties> |  |

### IconRotateCwProperties

**النوع:** الواجهة

```typescript
export interface IconRotateCwProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### ForgeIconScaleDown

**النوع:** الوظيفة

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement
```

`ForgeIconScaleDown` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-scale-down.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconScaleDownProperties> |  |

### IconScaleDownProperties

**النوع:** الواجهة

```typescript
export interface IconScaleDownProperties
```

لم يتم تقديم أي وصف.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### ForgeIconScaleUp

**النوع:** الوظيفة

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement
```

`ForgeIconScaleUp` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-scale-up.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconScaleUpProperties> |  |

### IconScaleUpProperties

**النوع:** الواجهة

```typescript
export interface IconScaleUpProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### ForgeIconCountryGlobe

**النوع:** الوظيفة

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement
```

علامة الكرة الأرضية مع لهجة البلد المعتمدة على البيانات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCountryGlobeProperties> |  |

### IconCountryGlobeProperties

**النوع:** الواجهة

```typescript
export interface IconCountryGlobeProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### ForgeIconFlag

**النوع:** الوظيفة

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement
```

أيقونة علم دولة محددة ومعتمدة على البيانات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconFlagProperties> |  |

### IconCountryCode

**النوع:** النوع

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

رموز البلدان المدعومة لمكون العلم المستند إلى البيانات.

### IconFlagProperties

**النوع:** الواجهة

```typescript
export interface IconFlagProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### ForgeIconGeodesic

**النوع:** الوظيفة

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement
```

`ForgeIconGeodesic` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-geodesic.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconGeodesicProperties> |  |

### IconGeodesicProperties

**النوع:** الواجهة

```typescript
export interface IconGeodesicProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### ForgeIconGlobe

**النوع:** الوظيفة

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement
```

`ForgeIconGlobe` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-globe.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconGlobeProperties> |  |

### IconGlobeProperties

**النوع:** الواجهة

```typescript
export interface IconGlobeProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### ForgeIconLanguage

**النوع:** الوظيفة

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement
```

`ForgeIconLanguage` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-language.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLanguageProperties> |  |

### IconLanguageProperties

**النوع:** الواجهة

```typescript
export interface IconLanguageProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### ForgeIconMapPin

**النوع:** الوظيفة

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement
```

`ForgeIconMapPin` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-map-pin.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMapPinProperties> |  |

### IconMapPinProperties

**النوع:** الواجهة

```typescript
export interface IconMapPinProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### ForgeIconLayer

**النوع:** الوظيفة

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement
```

رمز مكدس طبقة الخريطة لتبديل البيانات الجغرافية المرئية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLayerProperties> |  |

### IconLayerProperties

**النوع:** الواجهة

```typescript
export interface IconLayerProperties
```

لم يتم تقديم أي وصف.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### ForgeIconMapMarkerCluster

**النوع:** الوظيفة

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement
```

علامة خريطة مجمعة لنقاط الاهتمام الكثيفة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMapMarkerClusterProperties> |  |

### IconMapMarkerClusterProperties

**النوع:** الواجهة

```typescript
export interface IconMapMarkerClusterProperties
```

لم يتم تقديم أي وصف.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### ForgeIconCamera

**النوع:** الوظيفة

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement
```

`ForgeIconCamera` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-camera.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCameraProperties> |  |

### IconCameraProperties

**النوع:** الواجهة

```typescript
export interface IconCameraProperties
```

لم يتم تقديم أي وصف.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### ForgeIconImage

**النوع:** الوظيفة

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement
```

`ForgeIconImage` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-image.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconImageProperties> |  |

### IconImageProperties

**النوع:** الواجهة

```typescript
export interface IconImageProperties
```

لم يتم تقديم أي وصف.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### ForgeIconPause

**النوع:** الوظيفة

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement
```

`ForgeIconPause` — رمز وقفة مملوء (❙❙) تم تأليفه مرة واحدة في JSX المحايد
لهجة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-pause.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPauseProperties> |  |

### IconPauseProperties

**النوع:** الواجهة

```typescript
export interface IconPauseProperties
```

لم يتم تقديم أي وصف.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### ForgeIconPlay

**النوع:** الوظيفة

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement
```

`ForgeIconPlay` — مثلث مملوء (◀) تم تأليفه مرة واحدة في JSX المحايدة
لهجة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-play.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPlayProperties> |  |

### IconPlayProperties

**النوع:** الواجهة

```typescript
export interface IconPlayProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### ForgeIconArrow

**النوع:** الوظيفة

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement
```

`ForgeIconArrow` — سهم اتجاهي تم كتابته مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-arrow.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconArrowProperties> |  |

### IconArrowDirection

**النوع:** النوع

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

الاتجاه الذي يشير إليه السهم.

### IconArrowProperties

**النوع:** الواجهة

```typescript
export interface IconArrowProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### ForgeIconChevron

**النوع:** الوظيفة

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement
```

`ForgeIconChevron` — شيفرون اتجاهي تم تأليفه مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-chevron.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconChevronProperties> |  |

### IconChevronProperties

**النوع:** الواجهة

```typescript
export interface IconChevronProperties
```

لم يتم تقديم أي وصف.

### IconDirection

**النوع:** النوع

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

الاتجاه الذي تشير إليه شيفرون.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### ForgeIconChevrons

**النوع:** الوظيفة

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement
```

`ForgeIconChevrons` - شيفرون اتجاهي مزدوج (« ») تم تأليفه مرة واحدة في
لهجة JSX المحايدة، تُستخدم عادةً لعناصر التحكم في الانتقال إلى الأول / الانتقال إلى الأخير.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-chevrons.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconChevronsProperties> |  |

### IconChevronsDirection

**النوع:** النوع

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

الاتجاه الذي يشير إليه الشيفرون المزدوج.

### IconChevronsProperties

**النوع:** الواجهة

```typescript
export interface IconChevronsProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### ForgeIconClose

**النوع:** الوظيفة

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement
```

`ForgeIconClose` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-close.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCloseProperties> |  |

### IconCloseProperties

**النوع:** الواجهة

```typescript
export interface IconCloseProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### ForgeIconHome

**النوع:** الوظيفة

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement
```

`ForgeIconHome` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-home.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHomeProperties> |  |

### IconHomeProperties

**النوع:** الواجهة

```typescript
export interface IconHomeProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### ForgeIconJoin

**النوع:** الوظيفة

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement
```

`ForgeIconJoin` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-join.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconJoinProperties> |  |

### IconJoinProperties

**النوع:** الواجهة

```typescript
export interface IconJoinProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### ForgeIconMenu

**النوع:** الوظيفة

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement
```

`ForgeIconMenu` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-menu.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMenuProperties> |  |

### IconMenuProperties

**النوع:** الواجهة

```typescript
export interface IconMenuProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### ForgeIconMinus

**النوع:** الوظيفة

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement
```

`ForgeIconMinus` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-minus.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconMinusProperties> |  |

### IconMinusProperties

**النوع:** الواجهة

```typescript
export interface IconMinusProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

**النوع:** الوظيفة

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement
```

`ForgeIconPlus` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-plus.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPlusProperties> |  |

### IconPlusProperties

**النوع:** الواجهة

```typescript
export interface IconPlusProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### ForgeIconRefresh

**النوع:** الوظيفة

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement
```

`ForgeIconRefresh` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-refresh.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconRefreshProperties> |  |

### IconRefreshProperties

**النوع:** الواجهة

```typescript
export interface IconRefreshProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### ForgeIconSplit

**النوع:** الوظيفة

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement
```

`ForgeIconSplit` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-split.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSplitProperties> |  |

### IconSplitProperties

**النوع:** الواجهة

```typescript
export interface IconSplitProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIconExternalLink

**النوع:** الوظيفة

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement
```

`ForgeIconExternalLink` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-external-link.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconExternalLinkProperties> |  |

### خصائص أيقونةExternalLink

**النوع:** الواجهة

```typescript
export interface IconExternalLinkProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

**النوع:** الوظيفة

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement
```

`ForgeIconLink` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-link.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLinkProperties> |  |

### IconLinkProperties

**النوع:** الواجهة

```typescript
export interface IconLinkProperties
```

لم يتم تقديم أي وصف.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### ForgeIconSearch

**النوع:** الوظيفة

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement
```

`ForgeIconSearch` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-search.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSearchProperties> |  |

### IconSearchProperties

**النوع:** الواجهة

```typescript
export interface IconSearchProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### ForgeIconCloud

**النوع:** الوظيفة

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement
```

`ForgeIconCloud` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-cloud.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCloudProperties> |  |

### IconCloudProperties

**النوع:** الواجهة

```typescript
export interface IconCloudProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### ForgeIconDebug

**النوع:** الوظيفة

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement
```

`ForgeIconDebug` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-debug.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconDebugProperties> |  |

### IconDebugProperties

**النوع:** الواجهة

```typescript
export interface IconDebugProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### ForgeIconHeart

**النوع:** الوظيفة

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement
```

`ForgeIconHeart` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heart.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeartProperties> |  |

### IconHeartProperties

**النوع:** الواجهة

```typescript
export interface IconHeartProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### ForgeIconLightning

**النوع:** الوظيفة

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement
```

`ForgeIconLightning` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-lightning.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLightningProperties> |  |

### IconLightningProperties

**النوع:** الواجهة

```typescript
export interface IconLightningProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIconPuzzle

**النوع:** الوظيفة

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement
```

`ForgeIconPuzzle` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-puzzle.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconPuzzleProperties> |  |

### IconPuzzleProperties

**النوع:** الواجهة

```typescript
export interface IconPuzzleProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQrCode

**النوع:** الوظيفة

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement
```

`ForgeIconQrCode` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-qr-code.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconQrCodeProperties> |  |

### IconQrCodeProperties

**النوع:** الواجهة

```typescript
export interface IconQrCodeProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### إعدادات ForgeIcon

**النوع:** الوظيفة

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement
```

`ForgeIconSettings` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-settings.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSettingsProperties> |  |

### إعدادات أيقونةالخصائص

**النوع:** الواجهة

```typescript
export interface IconSettingsProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### ForgeIconStar

**النوع:** الوظيفة

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement
```

`ForgeIconStar` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-star.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconStarProperties> |  |

### IconStarProperties

**النوع:** الواجهة

```typescript
export interface IconStarProperties
```

لم يتم تقديم أي وصف.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### ForgeIconWrench

**النوع:** الوظيفة

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement
```

`ForgeIconWrench` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-wrench.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconWrenchProperties> |  |

### IconWrenchProperties

**النوع:** الواجهة

```typescript
export interface IconWrenchProperties
```

لم يتم تقديم أي وصف.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### ForgeIconRoute

**النوع:** الوظيفة

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement
```

خط طريق بعلامات بداية ووجهة مميزة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconRouteProperties> |  |

### IconRouteProperties

**النوع:** الواجهة

```typescript
export interface IconRouteProperties
```

لم يتم تقديم أي وصف.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### ForgeIconWaypoint

**النوع:** الوظيفة

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement
```

علامة إحداثية قابلة لإعادة الاستخدام للطرق وتراكبات الخرائط.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconWaypointProperties> |  |

### IconWaypointProperties

**النوع:** الواجهة

```typescript
export interface IconWaypointProperties
```

لم يتم تقديم أي وصف.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### ForgeIconLockOpen

**النوع:** الوظيفة

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement
```

`ForgeIconLockOpen` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-lock-open.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLockOpenProperties> |  |

### IconLockOpenProperties

**النوع:** الواجهة

```typescript
export interface IconLockOpenProperties
```

لم يتم تقديم أي وصف.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### ForgeIconLock

**النوع:** الوظيفة

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement
```

`ForgeIconLock` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-lock.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconLockProperties> |  |

### IconLockProperties

**النوع:** الواجهة

```typescript
export interface IconLockProperties
```

لم يتم تقديم أي وصف.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIconUser

**النوع:** الوظيفة

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement
```

`ForgeIconUser` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-user.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconUserProperties> |  |

### IconUserProperties

**النوع:** الواجهة

```typescript
export interface IconUserProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIconAlertCritical

**النوع:** الوظيفة

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement
```

`ForgeIconAlertCritical` - الصورة الرمزية للتنبيه الحرج ("!" داخل المثمن،
استحضار علامة التوقف) تم تأليفه مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-alert-critical.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlertCriticalProperties> |  |

### IconAlertCriticalProperties

**النوع:** الواجهة

```typescript
export interface IconAlertCriticalProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

**النوع:** الوظيفة

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement
```

`ForgeIconAlertInfo` - الصورة الرمزية للتنبيه المعلوماتي (حرف "i" داخل دائرة)
تم تأليفه مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-alert-info.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlertInfoProperties> |  |

### IconAlertInfoProperties

**النوع:** الواجهة

```typescript
export interface IconAlertInfoProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### ForgeIconAlertNeutral

**النوع:** الوظيفة

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement
```

`ForgeIconAlertNeutral` — حرف التنبيه المحايد (علامة "-" داخل دائرة)
تم تأليفه مرة واحدة بلهجة JSX المحايدة، للرسائل الإعلامية التي لا تحتوي على رقم
دلالة إيجابية أو سلبية.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-alert-neutral.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlertNeutralProperties> |  |

### IconAlertNeutralProperties

**النوع:** الواجهة

```typescript
export interface IconAlertNeutralProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### ForgeIconAlertWarning

**النوع:** الوظيفة

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement
```

`ForgeIconAlertWarning` - الصورة الرمزية للتنبيه التحذيري ("!" داخل المثلث)
تم تأليفه مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-alert-warning.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlertWarningProperties> |  |

### IconAlertWarningProperties

**النوع:** الواجهة

```typescript
export interface IconAlertWarningProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### ForgeIconAlert

**النوع:** الوظيفة

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement
```

`ForgeIconAlert` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-alert.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlertProperties> |  |

### IconAlertProperties

**النوع:** الواجهة

```typescript
export interface IconAlertProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### ForgeIconCheck

**النوع:** الوظيفة

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement
```

`ForgeIconCheck` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-check.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCheckProperties> |  |

### IconCheckProperties

**النوع:** الواجهة

```typescript
export interface IconCheckProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIconError

**النوع:** الوظيفة

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement
```

`ForgeIconError` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-error.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconErrorProperties> |  |

### IconErrorProperties

**النوع:** الواجهة

```typescript
export interface IconErrorProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### ForgeIconInfo

**النوع:** الوظيفة

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement
```

`ForgeIconInfo` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-info.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconInfoProperties> |  |

### IconInfoProperties

**النوع:** الواجهة

```typescript
export interface IconInfoProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### ForgeIconNotice

**النوع:** الوظيفة

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement
```

`ForgeIconNotice` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-notice.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconNoticeProperties> |  |

### IconNoticeProperties

**النوع:** الواجهة

```typescript
export interface IconNoticeProperties
```

لم يتم تقديم أي وصف.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### ForgeIconWarning

**النوع:** الوظيفة

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement
```

`ForgeIconWarning` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-warning.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconWarningProperties> |  |

### IconWarningProperties

**النوع:** الواجهة

```typescript
export interface IconWarningProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### ForgeIconAlignCenter

**النوع:** الوظيفة

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement
```

`ForgeIconAlignCenter` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-align-center.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlignCenterProperties> |  |

### IconAlignCenterProperties

**النوع:** الواجهة

```typescript
export interface IconAlignCenterProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### ForgeIconAlignJustify

**النوع:** الوظيفة

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement
```

`ForgeIconAlignJustify` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-align-justify.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlignJustifyProperties> |  |

### IconAlignJustifyProperties

**النوع:** الواجهة

```typescript
export interface IconAlignJustifyProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### ForgeIconAlignLeft

**النوع:** الوظيفة

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement
```

`ForgeIconAlignLeft` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-align-left.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlignLeftProperties> |  |

### IconAlignLeftProperties

**النوع:** الواجهة

```typescript
export interface IconAlignLeftProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### ForgeIconAlignRight

**النوع:** الوظيفة

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement
```

`ForgeIconAlignRight` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-align-right.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconAlignRightProperties> |  |

### IconAlignRightProperties

**النوع:** الواجهة

```typescript
export interface IconAlignRightProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIconBlockquote

**النوع:** الوظيفة

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement
```

`ForgeIconBlockquote` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-blockquote.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconBlockquoteProperties> |  |

### IconBlockquoteProperties

**النوع:** الواجهة

```typescript
export interface IconBlockquoteProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### ForgeIconBold

**النوع:** الوظيفة

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement
```

`ForgeIconBold` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-bold.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconBoldProperties> |  |

### IconBoldProperties

**النوع:** الواجهة

```typescript
export interface IconBoldProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### ForgeIconBulletList

**النوع:** الوظيفة

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement
```

`ForgeIconBulletList` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-bullet-list.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconBulletListProperties> |  |

### IconBulletListProperties

**النوع:** الواجهة

```typescript
export interface IconBulletListProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIconCodeBlock

**النوع:** الوظيفة

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement
```

`ForgeIconCodeBlock` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-code-block.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCodeBlockProperties> |  |

### IconCodeBlockProperties

**النوع:** الواجهة

```typescript
export interface IconCodeBlockProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

**النوع:** الوظيفة

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement
```

`ForgeIconCodeInline` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-code-inline.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCodeInlineProperties> |  |

### IconCodeInlineProperties

**النوع:** الواجهة

```typescript
export interface IconCodeInlineProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### ForgeIconHeadingFive

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement
```

`ForgeIconHeadingFive` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-five.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingFiveProperties> |  |

### IconHeadingFiveProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingFiveProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### ForgeIconHeadingFour

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement
```

`ForgeIconHeadingFour` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-four.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingFourProperties> |  |

### IconHeadingFourProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingFourProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### ForgeIconHeadingOne

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement
```

`ForgeIconHeadingOne` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-one.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingOneProperties> |  |

### IconHeadingOneProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingOneProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### ForgeIconHeadingSix

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement
```

`ForgeIconHeadingSix` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-six.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingSixProperties> |  |

### IconHeadingSixProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingSixProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### ForgeIconHeadingThree

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement
```

`ForgeIconHeadingThree` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-three.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingThreeProperties> |  |

### IconHeadingThreeProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingThreeProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### ForgeIconHeadingTwo

**النوع:** الوظيفة

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement
```

`ForgeIconHeadingTwo` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading-two.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingTwoProperties> |  |

### IconHeadingTwoProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingTwoProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### ForgeIconHeading

**النوع:** الوظيفة

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement
```

`ForgeIconHeading` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-heading.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconHeadingProperties> |  |

### IconHeadingProperties

**النوع:** الواجهة

```typescript
export interface IconHeadingProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### ForgeIconItalic

**النوع:** الوظيفة

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement
```

`ForgeIconItalic` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-italic.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconItalicProperties> |  |

### IconItalicProperties

**النوع:** الواجهة

```typescript
export interface IconItalicProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

**النوع:** الوظيفة

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement
```

`ForgeIconNumberedList` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-numbered-list.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconNumberedListProperties> |  |

### IconNumberedListProperties

**النوع:** الواجهة

```typescript
export interface IconNumberedListProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### ForgeIconStrikethrough

**النوع:** الوظيفة

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement
```

`ForgeIconStrikethrough` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-strikethrough.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconStrikethroughProperties> |  |

### IconStrikethroughProperties

**النوع:** الواجهة

```typescript
export interface IconStrikethroughProperties
```

لم يتم تقديم أي وصف.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### ForgeIconUnderline

**النوع:** الوظيفة

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement
```

`ForgeIconUnderline` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-underline.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconUnderlineProperties> |  |

### IconUnderlineProperties

**النوع:** الواجهة

```typescript
export interface IconUnderlineProperties
```

لم يتم تقديم أي وصف.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### ForgeIconCalendar

**النوع:** الوظيفة

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement
```

`ForgeIconCalendar` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-calendar.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconCalendarProperties> |  |

### IconCalendarProperties

**النوع:** الواجهة

```typescript
export interface IconCalendarProperties
```

لم يتم تقديم أي وصف.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### ForgeIconClock

**النوع:** الوظيفة

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement
```

`ForgeIconClock` — أيقونة تم تأليفها مرة واحدة بلهجة JSX المحايدة.

يتم تجميعه بشكل نظيف إلى كل من Vue 3 وReact عبر vite-plugin-forge
مترجم على مرحلتين. يقوم الرمز بشحن `@layer mp.icons` CSS الخاص به من خلال ملف
وحدة CSS المشتركة في الموقع `forge-icon-clock.module.scss`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconClockProperties> |  |

### IconClockProperties

**النوع:** الواجهة

```typescript
export interface IconClockProperties
```

لم يتم تقديم أي وصف.

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**النوع:** ثابت

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[]
```

لم يتم تقديم أي وصف.

### validateCompositions

**النوع:** الوظيفة

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مؤلفات | للقراءة فقط IconSymbolDefinition[] |  |
| المعرفات المتاحة | ReadonlySet<string> |  |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**النوع:** ثابت

```typescript
export const SUPPORTED_COUNTRY_CODES
```

رموز الدولة مع رموز العلم الأساسية في الصورة المتحركة المنشورة.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**النوع:** ثابت

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS
```

لم يتم تقديم أي وصف.

### ICON_SYMBOL_DEFINITIONS

**النوع:** ثابت

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[]
```

لم يتم تقديم أي وصف.

## `src/sprite/icon-use`

### ForgeIconUse

**النوع:** الوظيفة

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement
```

غلاف مشترك يمكن الوصول إليه يشير إلى هندسة الكائنات الأساسية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconUseProperties> |  |

### IconUseProperties

**النوع:** الواجهة

```typescript
export interface IconUseProperties
```

لم يتم تقديم أي وصف.

## `src/sprite/provider`

### IconSpriteContext

**النوع:** ثابت

```typescript
export const IconSpriteContext
```

لم يتم تقديم أي وصف.

### IconSpriteProvider

**النوع:** الوظيفة

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement
```

قم بتركيب مضيف كائن مضمّن واحد لشجرة فرعية من مكونات الأيقونات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خصائص | للقراءة فقط<IconSpriteProperties> |  |

### useIconHref

**النوع:** الوظيفة

```typescript
function useIconHref(symbolId: string): string
```

حل عنوان URL للرمز المحلي أو الخارجي لمجمع الأيقونات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| معرف الرمز | سلسلة |  |

## `src/sprite/serialize`

### serializeNode

**النوع:** الوظيفة

```typescript
function serializeNode(node: IconSvgNode): string
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | أيقونةSvgNode |  |

### تسلسل سبرايت

**النوع:** الوظيفة

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| تعريفات | للقراءة فقط IconSymbolDefinition[] |  |

### serializeSymbol

**النوع:** الوظيفة

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| تعريف | تعريف رمز الرمز |  |

## `src/sprite/types`

### IconSpriteContextValue

**النوع:** الواجهة

```typescript
export interface IconSpriteContextValue
```

لم يتم تقديم أي وصف.

### IconSpriteProperties

**النوع:** الواجهة

```typescript
export interface IconSpriteProperties
```

لم يتم تقديم أي وصف.

### IconSvgNode

**النوع:** الواجهة

```typescript
export interface IconSvgNode
```

لم يتم تقديم أي وصف.

### IconSymbolDefinition

**النوع:** الواجهة

```typescript
export interface IconSymbolDefinition
```

لم يتم تقديم أي وصف.

### IconSymbolUse

**النوع:** الواجهة

```typescript
export interface IconSymbolUse
```

لم يتم تقديم أي وصف.
