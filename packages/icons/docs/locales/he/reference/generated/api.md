# הפניה ל-@mission-platform/icons API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/icons/docs/reference/generated/api.md: [packages/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/icons`.

## `src/catalog`

### ICON_CATALOG

**סוג:** קבוע

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[]
```

הקטלוג שנבדק עבור ערכת האייקונים הקיימת.

### ICON_GAP_REVIEW

**סוג:** קבוע

```typescript
export const ICON_GAP_REVIEW
```

תוספות בעלות ערך גבוה ליישום לאחר הקמת טקסונומיית המקור.

### IconCatalogEntry

**סוג:** ממשק

```typescript
export interface IconCatalogEntry
```

הקצאת קטגוריה יציבה לכל מקור אייקונים בחבילה זו.

### validateIconCatalog

פונקציה **סוג:**

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void
```

אמת את הקטלוג הסטטי לפני השימוש בו ליצירת מטא נתונים של החבילה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| קטלוג | IconCatalogEntry[] לקריאה בלבד |  |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### ForgeIconBell

פונקציה **סוג:**

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement
```

`ForgeIconBell` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-bell.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconBellProperties> |  |

### IconBellProperties

**סוג:** ממשק

```typescript
export interface IconBellProperties
```

לא סופק תיאור.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### ForgeIconChat

פונקציה **סוג:**

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement
```

`ForgeIconChat` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-chat.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconChatProperties> |  |

### IconChatProperties

**סוג:** ממשק

```typescript
export interface IconChatProperties
```

לא סופק תיאור.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### ForgeIconMail

פונקציה **סוג:**

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement
```

`ForgeIconMail` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-mail.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMailProperties> |  |

### IconMailProperties

**סוג:** ממשק

```typescript
export interface IconMailProperties
```

לא סופק תיאור.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### ForgeIconPhone

פונקציה **סוג:**

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement
```

`ForgeIconPhone` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-phone.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPhoneProperties> |  |

### IconPhoneProperties

**סוג:** ממשק

```typescript
export interface IconPhoneProperties
```

לא סופק תיאור.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### ForgeIconSend

פונקציה **סוג:**

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement
```

`ForgeIconSend` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-send.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSendProperties> |  |

### IconSendProperties

**סוג:** ממשק

```typescript
export interface IconSendProperties
```

לא סופק תיאור.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### ForgeIconShare

פונקציה **סוג:**

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement
```

`ForgeIconShare` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-share.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconShareProperties> |  |

### IconShareProperties

**סוג:** ממשק

```typescript
export interface IconShareProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### ForgeIconCopy

פונקציה **סוג:**

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement
```

`ForgeIconCopy` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-copy.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCopyProperties> |  |

### IconCopyProperties

**סוג:** ממשק

```typescript
export interface IconCopyProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### ForgeIconEdit

פונקציה **סוג:**

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement
```

`ForgeIconEdit` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-edit.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconEditProperties> |  |

### IconEditProperties

**סוג:** ממשק

```typescript
export interface IconEditProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### ForgeIconEyeOff

פונקציה **סוג:**

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement
```

`ForgeIconEyeOff` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-eye-off.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconEyeOffProperties> |  |

### IconEyeOffProperties

**סוג:** ממשק

```typescript
export interface IconEyeOffProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### ForgeIconEye

פונקציה **סוג:**

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement
```

`ForgeIconEye` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-eye.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconEyeProperties> |  |

### IconEyeProperties

**סוג:** ממשק

```typescript
export interface IconEyeProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### ForgeIconRedo

פונקציה **סוג:**

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement
```

מחיל מחדש את עריכת התוכן האחרונה שבוטלה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconRedoProperties> |  |

### IconRedoProperties

**סוג:** ממשק

```typescript
export interface IconRedoProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### ForgeIconTrash

פונקציה **סוג:**

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement
```

`ForgeIconTrash` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-trash.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTrashProperties> |  |

### IconTrashProperties

**סוג:** ממשק

```typescript
export interface IconTrashProperties
```

לא סופק תיאור.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### ForgeIconUndo

פונקציה **סוג:**

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement
```

הופך את עריכת התוכן האחרונה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconUndoProperties> |  |

### IconUndoProperties

**סוג:** ממשק

```typescript
export interface IconUndoProperties
```

לא סופק תיאור.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIconDownload

פונקציה **סוג:**

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement
```

`ForgeIconDownload` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-download.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDownloadProperties> |  |

### IconDownloadProperties

**סוג:** ממשק

```typescript
export interface IconDownloadProperties
```

לא סופק תיאור.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconUpload

פונקציה **סוג:**

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement
```

`ForgeIconUpload` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-upload.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconUploadProperties> |  |

### IconUploadProperties

**סוג:** ממשק

```typescript
export interface IconUploadProperties
```

לא סופק תיאור.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### ForgeIconFilter

פונקציה **סוג:**

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement
```

`ForgeIconFilter` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-filter.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconFilterProperties> |  |

### IconFilterProperties

**סוג:** ממשק

```typescript
export interface IconFilterProperties
```

לא סופק תיאור.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### ForgeIconSort

פונקציה **סוג:**

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement
```

`ForgeIconSort` - מחוון מיון דו-שברון שנכתב פעם אחת ב-JSX הנייטרלי
דיאלקט.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-sort.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSortProperties> |  |

### IconSortProperties

**סוג:** ממשק

```typescript
export interface IconSortProperties
```

לא סופק תיאור.

### SortDirection

**סוג:** סוג

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

כיוון המיון הפעיל, או `undefined` כאשר לא ממוין.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIconTableColumnAdd

פונקציה **סוג:**

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement
```

`ForgeIconTableColumnAdd` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-table-column-add.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTableColumnAddProperties> |  |

### IconTableColumnAddProperties

**סוג:** ממשק

```typescript
export interface IconTableColumnAddProperties
```

לא סופק תיאור.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnRemove

פונקציה **סוג:**

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement
```

`ForgeIconTableColumnRemove` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-table-column-remove.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTableColumnRemoveProperties> |  |

### IconTableColumnRemoveProperties

**סוג:** ממשק

```typescript
export interface IconTableColumnRemoveProperties
```

לא סופק תיאור.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

פונקציה **סוג:**

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement
```

`ForgeIconTableRowAdd` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-table-row-add.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTableRowAddProperties> |  |

### IconTableRowAddProperties

**סוג:** ממשק

```typescript
export interface IconTableRowAddProperties
```

לא סופק תיאור.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowRemove

פונקציה **סוג:**

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement
```

`ForgeIconTableRowRemove` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-table-row-remove.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTableRowRemoveProperties> |  |

### IconTableRowRemoveProperties

**סוג:** ממשק

```typescript
export interface IconTableRowRemoveProperties
```

לא סופק תיאור.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIconTable

פונקציה **סוג:**

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement
```

`ForgeIconTable` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-table.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconTableProperties> |  |

### IconTableProperties

**סוג:** ממשק

```typescript
export interface IconTableProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### ForgeIconDrawCircle

פונקציה **סוג:**

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement
```

`ForgeIconDrawCircle` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-draw-circle.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDrawCircleProperties> |  |

### IconDrawCircleProperties

**סוג:** ממשק

```typescript
export interface IconDrawCircleProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### ForgeIconDrawLine

פונקציה **סוג:**

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement
```

`ForgeIconDrawLine` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-draw-line.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDrawLineProperties> |  |

### IconDrawLineProperties

**סוג:** ממשק

```typescript
export interface IconDrawLineProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### ForgeIconDrawPolygon

פונקציה **סוג:**

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement
```

`ForgeIconDrawPolygon` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-draw-polygon.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDrawPolygonProperties> |  |

### IconDrawPolygonProperties

**סוג:** ממשק

```typescript
export interface IconDrawPolygonProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### ForgeIconDrawSquare

פונקציה **סוג:**

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement
```

`ForgeIconDrawSquare` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-draw-square.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDrawSquareProperties> |  |

### IconDrawSquareProperties

**סוג:** ממשק

```typescript
export interface IconDrawSquareProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### ForgeIconDrawTriangle

פונקציה **סוג:**

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement
```

`ForgeIconDrawTriangle` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-draw-triangle.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDrawTriangleProperties> |  |

### IconDrawTriangleProperties

**סוג:** ממשק

```typescript
export interface IconDrawTriangleProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### ForgeIconMove

פונקציה **סוג:**

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement
```

`ForgeIconMove` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-move.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMoveProperties> |  |

### IconMoveProperties

**סוג:** ממשק

```typescript
export interface IconMoveProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### ForgeIconPalette

פונקציה **סוג:**

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement
```

`ForgeIconPalette` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-palette.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPaletteProperties> |  |

### IconPaletteProperties

**סוג:** ממשק

```typescript
export interface IconPaletteProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### ForgeIconPencil

פונקציה **סוג:**

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement
```

`ForgeIconPencil` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-pencil.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPencilProperties> |  |

### IconPencilProperties

**סוג:** ממשק

```typescript
export interface IconPencilProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### ForgeIconRotateCcw

פונקציה **סוג:**

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement
```

`ForgeIconRotateCcw` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-rotate-ccw.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconRotateCcwProperties> |  |

### IconRotateCcwProperties

**סוג:** ממשק

```typescript
export interface IconRotateCcwProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### ForgeIconRotateCw

פונקציה **סוג:**

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement
```

`ForgeIconRotateCw` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-rotate-cw.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconRotateCwProperties> |  |

### IconRotateCwProperties

**סוג:** ממשק

```typescript
export interface IconRotateCwProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### ForgeIconScaleDown

פונקציה **סוג:**

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement
```

`ForgeIconScaleDown` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-scale-down.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconScaleDownProperties> |  |

### IconScaleDownProperties

**סוג:** ממשק

```typescript
export interface IconScaleDownProperties
```

לא סופק תיאור.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### ForgeIconScaleUp

פונקציה **סוג:**

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement
```

`ForgeIconScaleUp` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-scale-up.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconScaleUpProperties> |  |

### IconScaleUpProperties

**סוג:** ממשק

```typescript
export interface IconScaleUpProperties
```

לא סופק תיאור.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### ForgeIconCountryGlobe

פונקציה **סוג:**

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement
```

סמן גלובוס עם מבטא מדינה מונע נתונים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCountryGlobeProperties> |  |

### IconCountryGlobeProperties

**סוג:** ממשק

```typescript
export interface IconCountryGlobeProperties
```

לא סופק תיאור.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### ForgeIconFlag

פונקציה **סוג:**

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement
```

סמל דגל מדינה מוגבל, מבוסס נתונים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconFlagProperties> |  |

### IconCountryCode

**סוג:** סוג

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

קודי מדינה נתמכים עבור רכיב הדגל מבוסס הנתונים.

### IconFlagProperties

**סוג:** ממשק

```typescript
export interface IconFlagProperties
```

לא סופק תיאור.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### ForgeIconGeodesic

פונקציה **סוג:**

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement
```

`ForgeIconGeodesic` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-geodesic.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconGeodesicProperties> |  |

### IconGeodesicProperties

**סוג:** ממשק

```typescript
export interface IconGeodesicProperties
```

לא סופק תיאור.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### ForgeIconGlobe

פונקציה **סוג:**

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement
```

`ForgeIconGlobe` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-globe.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconGlobeProperties> |  |

### IconGlobeProperties

**סוג:** ממשק

```typescript
export interface IconGlobeProperties
```

לא סופק תיאור.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### ForgeIconLanguage

פונקציה **סוג:**

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement
```

`ForgeIconLanguage` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-language.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLanguageProperties> |  |

### IconLanguageProperties

**סוג:** ממשק

```typescript
export interface IconLanguageProperties
```

לא סופק תיאור.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### ForgeIconMapPin

פונקציה **סוג:**

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement
```

`ForgeIconMapPin` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-map-pin.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMapPinProperties> |  |

### IconMapPinProperties

**סוג:** ממשק

```typescript
export interface IconMapPinProperties
```

לא סופק תיאור.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### ForgeIconLayer

פונקציה **סוג:**

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement
```

סמל מחסנית שכבת מפה למעבר בין נתונים גיאוגרפיים גלויים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLayerProperties> |  |

### IconLayerProperties

**סוג:** ממשק

```typescript
export interface IconLayerProperties
```

לא סופק תיאור.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### ForgeIconMapMarkerCluster

פונקציה **סוג:**

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement
```

סמן מפה מקובץ לנקודות עניין צפופות.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMapMarkerClusterProperties> |  |

### IconMapMarkerClusterProperties

**סוג:** ממשק

```typescript
export interface IconMapMarkerClusterProperties
```

לא סופק תיאור.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### ForgeIconCamera

פונקציה **סוג:**

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement
```

`ForgeIconCamera` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-camera.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCameraProperties> |  |

### IconCameraProperties

**סוג:** ממשק

```typescript
export interface IconCameraProperties
```

לא סופק תיאור.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### ForgeIconImage

פונקציה **סוג:**

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement
```

`ForgeIconImage` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-image.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconImageProperties> |  |

### IconImageProperties

**סוג:** ממשק

```typescript
export interface IconImageProperties
```

לא סופק תיאור.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### ForgeIconPause

פונקציה **סוג:**

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement
```

`ForgeIconPause` - גליף מלא הפסקה (❙❙) שכתב פעם אחת ב-JSX הנייטרלי
דיאלקט.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-pause.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPauseProperties> |  |

### IconPauseProperties

**סוג:** ממשק

```typescript
export interface IconPauseProperties
```

לא סופק תיאור.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### ForgeIconPlay

פונקציה **סוג:**

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement
```

`ForgeIconPlay` - משולש משחק מלא (▶) שנכתב פעם אחת ב-JSX הנייטרלי
דיאלקט.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-play.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPlayProperties> |  |

### IconPlayProperties

**סוג:** ממשק

```typescript
export interface IconPlayProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### ForgeIconArrow

פונקציה **סוג:**

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement
```

`ForgeIconArrow` - חץ כיוון שכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-arrow.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconArrowProperties> |  |

### IconArrowDirection

**סוג:** סוג

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

הכיוון אליו מצביע החץ.

### IconArrowProperties

**סוג:** ממשק

```typescript
export interface IconArrowProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### ForgeIconChevron

פונקציה **סוג:**

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement
```

`ForgeIconChevron` - שברון כיווני שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-chevron.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconChevronProperties> |  |

### IconChevronProperties

**סוג:** ממשק

```typescript
export interface IconChevronProperties
```

לא סופק תיאור.

### IconDirection

**סוג:** סוג

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

הכיוון אליו מצביע השברון.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### ForgeIconChvrons

פונקציה **סוג:**

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement
```

`ForgeIconChevrons` - שברון כפול כיווני (« ») שנכתב פעם אחת ב
ניב JSX ניטרלי, המשמש בדרך כלל עבור בקרות קפיצה לראשון / קפיצה אל האחרון.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-chevrons.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconChevronsProperties> |  |

### IconChvronsDirection

**סוג:** סוג

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

הכיוון שהשברון הכפול מצביע.

### IconChvronsProperties

**סוג:** ממשק

```typescript
export interface IconChevronsProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### ForgeIconClose

פונקציה **סוג:**

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement
```

`ForgeIconClose` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-close.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCloseProperties> |  |

### IconCloseProperties

**סוג:** ממשק

```typescript
export interface IconCloseProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### ForgeIconHome

פונקציה **סוג:**

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement
```

`ForgeIconHome` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-home.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHomeProperties> |  |

### IconHomeProperties

**סוג:** ממשק

```typescript
export interface IconHomeProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### ForgeIconJoin

פונקציה **סוג:**

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement
```

`ForgeIconJoin` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-join.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconJoinProperties> |  |

### IconJoinProperties

**סוג:** ממשק

```typescript
export interface IconJoinProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### ForgeIconMenu

פונקציה **סוג:**

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement
```

`ForgeIconMenu` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-menu.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMenuProperties> |  |

### IconMenuProperties

**סוג:** ממשק

```typescript
export interface IconMenuProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### ForgeIconMinus

פונקציה **סוג:**

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement
```

`ForgeIconMinus` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-minus.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconMinusProperties> |  |

### IconMinusProperties

**סוג:** ממשק

```typescript
export interface IconMinusProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

פונקציה **סוג:**

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement
```

`ForgeIconPlus` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-plus.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPlusProperties> |  |

### IconPlusProperties

**סוג:** ממשק

```typescript
export interface IconPlusProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### ForgeIconRefresh

פונקציה **סוג:**

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement
```

`ForgeIconRefresh` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-refresh.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconRefreshProperties> |  |

### IconRefreshProperties

**סוג:** ממשק

```typescript
export interface IconRefreshProperties
```

לא סופק תיאור.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### ForgeIconSplit

פונקציה **סוג:**

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement
```

`ForgeIconSplit` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-split.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSplitProperties> |  |

### IconSplitProperties

**סוג:** ממשק

```typescript
export interface IconSplitProperties
```

לא סופק תיאור.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIconExternalLink

פונקציה **סוג:**

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement
```

`ForgeIconExternalLink` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-external-link.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconExternalLinkProperties> |  |

### IconExternalLinkProperties

**סוג:** ממשק

```typescript
export interface IconExternalLinkProperties
```

לא סופק תיאור.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

פונקציה **סוג:**

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement
```

`ForgeIconLink` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-link.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLinkProperties> |  |

### IconLinkProperties

**סוג:** ממשק

```typescript
export interface IconLinkProperties
```

לא סופק תיאור.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### ForgeIconSearch

פונקציה **סוג:**

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement
```

`ForgeIconSearch` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-search.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSearchProperties> |  |

### IconSearchProperties

**סוג:** ממשק

```typescript
export interface IconSearchProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### ForgeIconCloud

פונקציה **סוג:**

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement
```

`ForgeIconCloud` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-cloud.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCloudProperties> |  |

### IconCloudProperties

**סוג:** ממשק

```typescript
export interface IconCloudProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### ForgeIconDebug

פונקציה **סוג:**

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement
```

`ForgeIconDebug` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-debug.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconDebugProperties> |  |

### IconDebugProperties

**סוג:** ממשק

```typescript
export interface IconDebugProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### ForgeIconHeart

פונקציה **סוג:**

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement
```

`ForgeIconHeart` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heart.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeartProperties> |  |

### IconHeartProperties

**סוג:** ממשק

```typescript
export interface IconHeartProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### ForgeIconLightning

פונקציה **סוג:**

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement
```

`ForgeIconLightning` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-lightning.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLightningProperties> |  |

### IconLightningProperties

**סוג:** ממשק

```typescript
export interface IconLightningProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIconPuzzle

פונקציה **סוג:**

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement
```

`ForgeIconPuzzle` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-puzzle.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconPuzzleProperties> |  |

### IconPuzzleProperties

**סוג:** ממשק

```typescript
export interface IconPuzzleProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQrCode

פונקציה **סוג:**

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement
```

`ForgeIconQrCode` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-qr-code.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconQrCodeProperties> |  |

### IconQrCodeProperties

**סוג:** ממשק

```typescript
export interface IconQrCodeProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### ForgeIconSettings

פונקציה **סוג:**

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement
```

`ForgeIconSettings` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-settings.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSettingsProperties> |  |

### IconSettingsProperties

**סוג:** ממשק

```typescript
export interface IconSettingsProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### ForgeIconStar

פונקציה **סוג:**

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement
```

`ForgeIconStar` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-star.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconStarProperties> |  |

### IconStarProperties

**סוג:** ממשק

```typescript
export interface IconStarProperties
```

לא סופק תיאור.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### ForgeIconWrench

פונקציה **סוג:**

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement
```

`ForgeIconWrench` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-wrench.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconWrenchProperties> |  |

### IconWrenchProperties

**סוג:** ממשק

```typescript
export interface IconWrenchProperties
```

לא סופק תיאור.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### ForgeIconRoute

פונקציה **סוג:**

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement
```

קו מסלול עם סמני התחלה ויעד ברורים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconRouteProperties> |  |

### IconRouteProperties

**סוג:** ממשק

```typescript
export interface IconRouteProperties
```

לא סופק תיאור.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### ForgeIconWaypoint

פונקציה **סוג:**

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement
```

סמן נקודת ציון רב פעמי עבור מסלולים ושכבות מפה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconWaypointProperties> |  |

### IconWaypointProperties

**סוג:** ממשק

```typescript
export interface IconWaypointProperties
```

לא סופק תיאור.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### ForgeIconLockOpen

פונקציה **סוג:**

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement
```

`ForgeIconLockOpen` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-lock-open.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLockOpenProperties> |  |

### IconLockOpenProperties

**סוג:** ממשק

```typescript
export interface IconLockOpenProperties
```

לא סופק תיאור.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### ForgeIconLock

פונקציה **סוג:**

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement
```

`ForgeIconLock` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-lock.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconLockProperties> |  |

### IconLockProperties

**סוג:** ממשק

```typescript
export interface IconLockProperties
```

לא סופק תיאור.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIconUser

פונקציה **סוג:**

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement
```

`ForgeIconUser` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-user.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconUserProperties> |  |

### IconUserProperties

**סוג:** ממשק

```typescript
export interface IconUserProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIconAlertCritical

פונקציה **סוג:**

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement
```

`ForgeIconAlertCritical` - גליף ההתראה הקריטי ("!" בתוך מתומן,
מעורר תמרור עצור) שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-alert-critical.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlertCriticalProperties> |  |

### IconAlertCriticalProperties

**סוג:** ממשק

```typescript
export interface IconAlertCriticalProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

פונקציה **סוג:**

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement
```

`ForgeIconAlertInfo` - גליף התראת המידע (סימן "i" בתוך עיגול)
נכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-alert-info.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlertInfoProperties> |  |

### IconAlertInfoProperties

**סוג:** ממשק

```typescript
export interface IconAlertInfoProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### ForgeIconAlertNeutral

פונקציה **סוג:**

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement
```

`ForgeIconAlertNeutral` - גליף ההתראה הנייטרלי ("–" בתוך עיגול)
נכתב פעם אחת בניב JSX הנייטרלי, עבור הודעות מידע עם מס'
קונוטציה חיובית או שלילית.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-alert-neutral.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlertNeutralProperties> |  |

### IconAlertNeutralProperties

**סוג:** ממשק

```typescript
export interface IconAlertNeutralProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### ForgeIconAlert אזהרה

פונקציה **סוג:**

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement
```

`ForgeIconAlertWarning` - גליף אזהרה (סימן "!" בתוך משולש)
נכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-alert-warning.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlertWarningProperties> |  |

### IconAlertWarningProperties

**סוג:** ממשק

```typescript
export interface IconAlertWarningProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### ForgeIconAlert

פונקציה **סוג:**

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement
```

`ForgeIconAlert` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-alert.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlertProperties> |  |

### IconAlertProperties

**סוג:** ממשק

```typescript
export interface IconAlertProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### ForgeIconCheck

פונקציה **סוג:**

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement
```

`ForgeIconCheck` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-check.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCheckProperties> |  |

### IconCheckProperties

**סוג:** ממשק

```typescript
export interface IconCheckProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIconError

פונקציה **סוג:**

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement
```

`ForgeIconError` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-error.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconErrorProperties> |  |

### IconErrorProperties

**סוג:** ממשק

```typescript
export interface IconErrorProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### ForgeIconInfo

פונקציה **סוג:**

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement
```

`ForgeIconInfo` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-info.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconInfoProperties> |  |

### IconInfoProperties

**סוג:** ממשק

```typescript
export interface IconInfoProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### ForgeIconNotice

פונקציה **סוג:**

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement
```

`ForgeIconNotice` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-notice.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconNoticeProperties> |  |

### IconNoticeProperties

**סוג:** ממשק

```typescript
export interface IconNoticeProperties
```

לא סופק תיאור.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### ForgeIconWarning

פונקציה **סוג:**

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement
```

`ForgeIconWarning` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-warning.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconWarningProperties> |  |

### IconWarningProperties

**סוג:** ממשק

```typescript
export interface IconWarningProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### ForgeIconAlignCenter

פונקציה **סוג:**

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement
```

`ForgeIconAlignCenter` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-align-center.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlignCenterProperties> |  |

### IconAlignCenterProperties

**סוג:** ממשק

```typescript
export interface IconAlignCenterProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### ForgeIconAlignJustify

פונקציה **סוג:**

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement
```

`ForgeIconAlignJustify` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-align-justify.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlignJustifyProperties> |  |

### IconAlignJustifyProperties

**סוג:** ממשק

```typescript
export interface IconAlignJustifyProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### ForgeIconAlignLeft

פונקציה **סוג:**

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement
```

`ForgeIconAlignLeft` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-align-left.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlignLeftProperties> |  |

### IconAlignLeftProperties

**סוג:** ממשק

```typescript
export interface IconAlignLeftProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### ForgeIconAlignRight

פונקציה **סוג:**

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement
```

`ForgeIconAlignRight` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-align-right.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconAlignRightProperties> |  |

### IconAlignRightProperties

**סוג:** ממשק

```typescript
export interface IconAlignRightProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIconBlockquote

פונקציה **סוג:**

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement
```

`ForgeIconBlockquote` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-blockquote.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconBlockquoteProperties> |  |

### IconBlockquoteProperties

**סוג:** ממשק

```typescript
export interface IconBlockquoteProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### ForgeIconBold

פונקציה **סוג:**

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement
```

`ForgeIconBold` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-bold.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconBoldProperties> |  |

### IconBoldProperties

**סוג:** ממשק

```typescript
export interface IconBoldProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### ForgeIconBulletList

פונקציה **סוג:**

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement
```

`ForgeIconBulletList` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-bullet-list.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconBulletListProperties> |  |

### IconBulletListProperties

**סוג:** ממשק

```typescript
export interface IconBulletListProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIconCodeBlock

פונקציה **סוג:**

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement
```

`ForgeIconCodeBlock` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-code-block.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCodeBlockProperties> |  |

### IconCodeBlockProperties

**סוג:** ממשק

```typescript
export interface IconCodeBlockProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

פונקציה **סוג:**

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement
```

`ForgeIconCodeInline` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-code-inline.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCodeInlineProperties> |  |

### IconCodeInlineProperties

**סוג:** ממשק

```typescript
export interface IconCodeInlineProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### ForgeIconHeadingFive

פונקציה **סוג:**

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement
```

`ForgeIconHeadingFive` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-five.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingFiveProperties> |  |

### IconHeadingFiveProperties

**סוג:** ממשק

```typescript
export interface IconHeadingFiveProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### ForgeIconHeadingFour

פונקציה **סוג:**

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement
```

`ForgeIconHeadingFour` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-four.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingFourProperties> |  |

### IconHeadingFourProperties

**סוג:** ממשק

```typescript
export interface IconHeadingFourProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### ForgeIconHeadingOne

פונקציה **סוג:**

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement
```

`ForgeIconHeadingOne` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-one.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingOneProperties> |  |

### IconHeadingOneProperties

**סוג:** ממשק

```typescript
export interface IconHeadingOneProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### ForgeIconHeadingSix

פונקציה **סוג:**

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement
```

`ForgeIconHeadingSix` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-six.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingSixProperties> |  |

### IconHeadingSixProperties

**סוג:** ממשק

```typescript
export interface IconHeadingSixProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### ForgeIconHeadingThree

פונקציה **סוג:**

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement
```

`ForgeIconHeadingThree` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-three.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingThreeProperties> |  |

### IconHeadingThreeProperties

**סוג:** ממשק

```typescript
export interface IconHeadingThreeProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### ForgeIconHeadingTwo

פונקציה **סוג:**

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement
```

`ForgeIconHeadingTwo` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading-two.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingTwoProperties> |  |

### IconHeadingTwoProperties

**סוג:** ממשק

```typescript
export interface IconHeadingTwoProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### ForgeIconHeading

פונקציה **סוג:**

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement
```

`ForgeIconHeading` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-heading.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconHeadingProperties> |  |

### IconHeadingProperties

**סוג:** ממשק

```typescript
export interface IconHeadingProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### ForgeIconItalic

פונקציה **סוג:**

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement
```

`ForgeIconItalic` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-italic.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconItalicProperties> |  |

### IconItalicProperties

**סוג:** ממשק

```typescript
export interface IconItalicProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

פונקציה **סוג:**

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement
```

`ForgeIconNumberedList` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-numbered-list.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconNumberedListProperties> |  |

### IconNumberedListProperties

**סוג:** ממשק

```typescript
export interface IconNumberedListProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### ForgeIconStrikethrough

פונקציה **סוג:**

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement
```

`ForgeIconStrikethrough` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-strikethrough.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconStrikethroughProperties> |  |

### IconStrikethroughProperties

**סוג:** ממשק

```typescript
export interface IconStrikethroughProperties
```

לא סופק תיאור.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### ForgeIconUnderline

פונקציה **סוג:**

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement
```

`ForgeIconUnderline` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-underline.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconUnderlineProperties> |  |

### IconUnderlineProperties

**סוג:** ממשק

```typescript
export interface IconUnderlineProperties
```

לא סופק תיאור.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### ForgeIconCalendar

פונקציה **סוג:**

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement
```

`ForgeIconCalendar` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-calendar.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconCalendarProperties> |  |

### IconCalendarProperties

**סוג:** ממשק

```typescript
export interface IconCalendarProperties
```

לא סופק תיאור.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### ForgeIconClock

פונקציה **סוג:**

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement
```

`ForgeIconClock` - סמל שנכתב פעם אחת בניב JSX הנייטרלי.

הוא מתחבר בצורה נקייה גם ל-Vue 3 וגם ל-React באמצעות vite-plugin-forge
מהדר דו-שלבי. הסמל שולח `@layer mp.icons` CSS משלו דרך ה-
מודול CSS משותף `forge-icon-clock.module.scss`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconClockProperties> |  |

### IconClockProperties

**סוג:** ממשק

```typescript
export interface IconClockProperties
```

לא סופק תיאור.

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**סוג:** קבוע

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[]
```

לא סופק תיאור.

### לאמת קומפוזיציות

פונקציה **סוג:**

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| יצירות | readonly IconSymbolDefinition[] |  |
| זמינים מזהים | ReadonlySet<string> |  |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**סוג:** קבוע

```typescript
export const SUPPORTED_COUNTRY_CODES
```

קודי מדינה עם סמלי דגל קנוניים בספרייט שפורסם.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**סוג:** קבוע

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS
```

לא סופק תיאור.

### ICON_SYMBOL_DEFINITIONS

**סוג:** קבוע

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[]
```

לא סופק תיאור.

## `src/sprite/icon-use`

### ForgeIconUse

פונקציה **סוג:**

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement
```

מעטפת נגישה משותפת המתייחסת לגיאומטריית ספרייט קנונית.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconUseProperties> |  |

### IconUseProperties

**סוג:** ממשק

```typescript
export interface IconUseProperties
```

לא סופק תיאור.

## `src/sprite/provider`

### IconSpriteContext

**סוג:** קבוע

```typescript
export const IconSpriteContext
```

לא סופק תיאור.

### IconSpriteProvider

פונקציה **סוג:**

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement
```

התקן מארח ספרייט מוטבע אחד עבור תת-עץ של רכיבי סמלים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<IconSpriteProperties> |  |

### useIconHref

פונקציה **סוג:**

```typescript
function useIconHref(symbolId: string): string
```

פתרו כתובת URL של סמל מקומי או חיצוני עבור מעטפת סמלים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| symbolId | מחרוזת |  |

## `src/sprite/serialize`

### serializeNode

פונקציה **סוג:**

```typescript
function serializeNode(node: IconSvgNode): string
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| node | IconSvgNode |  |

### בסדרהSprite

פונקציה **סוג:**

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| הגדרות | readonly IconSymbolDefinition[] |  |

### סמל בסידרה

פונקציה **סוג:**

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| הגדרה | IconSymbolDefinition |  |

## `src/sprite/types`

### IconSpriteContextValue

**סוג:** ממשק

```typescript
export interface IconSpriteContextValue
```

לא סופק תיאור.

### IconSpriteProperties

**סוג:** ממשק

```typescript
export interface IconSpriteProperties
```

לא סופק תיאור.

### IconSvgNode

**סוג:** ממשק

```typescript
export interface IconSvgNode
```

לא סופק תיאור.

### IconSymbolDefinition

**סוג:** ממשק

```typescript
export interface IconSymbolDefinition
```

לא סופק תיאור.

### IconSymbolUse

**סוג:** ממשק

```typescript
export interface IconSymbolUse
```

לא סופק תיאור.
