# @mission-platform/icons API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/icons/docs/reference/generated/api.md: [packages/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/icons` のパブリック ソース宣言から生成されます。

## `src/catalog`

### アイコンカタログ

**種類:** 定数

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[];
```

既存のアイコン セットのレビュー済みカタログ。

### ICON_GAP_REVIEW

**種類:** 定数

```typescript
export const ICON_GAP_REVIEW;
```

ソース分類が確立された後に実装する高価値の追加。

### アイコンカタログエントリ

**種類:** インターフェース

```typescript
export interface IconCatalogEntry
```

このパッケージ内のすべてのアイコン ソースに対する安定したカテゴリの割り当て。

### 検証アイコンカタログ

**種類:** 関数

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void;
```

静的カタログをパッケージ メタデータの生成に使用する前に検証してください。

#### パラメータ

| 名前     | タイプ             | 説明     |
| -------- | ------------------ | -------- |
| カタログ | IconCatalogEntry[] | readonly |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### 鍛造アイコンベル

**種類:** 関数

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement;
```

`ForgeIconBell` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-bell.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconBellProperties> |      |

### アイコンベルのプロパティ

**種類:** インターフェース

```typescript
export interface IconBellProperties
```

説明はありません。

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### 鍛造アイコンチャット

**種類:** 関数

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement;
```

`ForgeIconChat` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-chat.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconChatProperties> |      |

### アイコンチャットプロパティ

**種類:** インターフェース

```typescript
export interface IconChatProperties
```

説明はありません。

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### 鍛造アイコンメール

**種類:** 関数

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement;
```

`ForgeIconMail` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-mail.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMailProperties> |      |

### アイコンメールプロパティ

**種類:** インターフェース

```typescript
export interface IconMailProperties
```

説明はありません。

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### 鍛造アイコン電話

**種類:** 関数

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement;
```

`ForgeIconPhone` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-phone.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPhoneProperties> |      |

### アイコン電話のプロパティ

**種類:** インターフェース

```typescript
export interface IconPhoneProperties
```

説明はありません。

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### 鍛造アイコン送信

**種類:** 関数

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement;
```

`ForgeIconSend` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-send.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconSendProperties> |      |

### アイコン送信プロパティ

**種類:** インターフェース

```typescript
export interface IconSendProperties
```

説明はありません。

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### Forgeアイコン共有

**種類:** 関数

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement;
```

`ForgeIconShare` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-share.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconShareProperties> |      |

### アイコン共有プロパティ

**種類:** インターフェース

```typescript
export interface IconShareProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### 鍛造アイコンコピー

**種類:** 関数

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement;
```

`ForgeIconCopy` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-copy.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCopyProperties> |      |

### アイコンコピープロパティ

**種類:** インターフェース

```typescript
export interface IconCopyProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### 鍛造アイコン編集

**種類:** 関数

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement;
```

`ForgeIconEdit` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-edit.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconEditProperties> |      |

### アイコン編集プロパティ

**種類:** インターフェース

```typescript
export interface IconEditProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### 鍛造アイコン目オフ

**種類:** 関数

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement;
```

`ForgeIconEyeOff` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-eye-off.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconEyeOffProperties> |      |

### アイコン目オフプロパティ

**種類:** インターフェース

```typescript
export interface IconEyeOffProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### 鍛造アイコン目

**種類:** 関数

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement;
```

`ForgeIconEye` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-eye.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                          | 説明 |
| ---------- | ------------------------------- | ---- |
| プロパティ | 読み取り専用<IconEyeProperties> |      |

### アイコン目のプロパティ

**種類:** インターフェース

```typescript
export interface IconEyeProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### 鍛造アイコンやり直し

**種類:** 関数

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement;
```

最後に取り消したコンテンツ編集を再適用します。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconRedoProperties> |      |

### アイコンやり直しプロパティ

**種類:** インターフェース

```typescript
export interface IconRedoProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### 鍛造アイコンゴミ箱

**種類:** 関数

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement;
```

`ForgeIconTrash` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-trash.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconTrashProperties> |      |

### アイコンゴミ箱プロパティ

**種類:** インターフェース

```typescript
export interface IconTrashProperties
```

説明はありません。

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### 鍛造アイコン元に戻す

**種類:** 関数

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement;
```

最新のコンテンツ編集を元に戻します。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconUndoProperties> |      |

### アイコン元に戻すプロパティ

**種類:** インターフェース

```typescript
export interface IconUndoProperties
```

説明はありません。

## `src/components/content/files/forge-icon-download/forge-icon-download`

### Forgeアイコンダウンロード

**種類:** 関数

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement;
```

`ForgeIconDownload` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-download.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconDownloadProperties> |      |

### アイコンダウンロードプロパティ

**種類:** インターフェース

```typescript
export interface IconDownloadProperties
```

説明はありません。

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconアップロード

**種類:** 関数

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement;
```

`ForgeIconUpload` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-upload.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconUploadProperties> |      |

### アイコンアップロードプロパティ

**種類:** インターフェース

```typescript
export interface IconUploadProperties
```

説明はありません。

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### Forgeアイコンフィルター

**種類:** 関数

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement;
```

`ForgeIconFilter` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-filter.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconFilterProperties> |      |

### アイコンフィルタープロパティ

**種類:** インターフェース

```typescript
export interface IconFilterProperties
```

説明はありません。

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### 鍛造アイコンソート

**種類:** 関数

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement;
```

`ForgeIconSort` — ニュートラル JSX で一度作成された 2 つのシェブロン ソート インジケーター
方言。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-sort.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconSortProperties> |      |

### アイコン並べ替えプロパティ

**種類:** インターフェース

```typescript
export interface IconSortProperties
```

説明はありません。

### 並べ替え方向

**種類：**タイプ

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

アクティブなソート方向、またはソートされていない場合は `undefined`。

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIconTableColumnAdd

**種類:** 関数

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement;
```

`ForgeIconTableColumnAdd` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-table-column-add.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                     | 説明 |
| ---------- | ------------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconTableColumnAddProperties> |      |

### IconTableColumnAddProperties

**種類:** インターフェース

```typescript
export interface IconTableColumnAddProperties
```

説明はありません。

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnRemove

**種類:** 関数

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement;
```

`ForgeIconTableColumnRemove` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-table-column-remove.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                        | 説明 |
| ---------- | --------------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconTableColumnRemoveProperties> |      |

### IconTableColumnRemoveプロパティ

**種類:** インターフェース

```typescript
export interface IconTableColumnRemoveProperties
```

説明はありません。

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

**種類:** 関数

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement;
```

`ForgeIconTableRowAdd` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-table-row-add.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconTableRowAddProperties> |      |

### IconTableRowAddProperties

**種類:** インターフェース

```typescript
export interface IconTableRowAddProperties
```

説明はありません。

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowRemove

**種類:** 関数

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement;
```

`ForgeIconTableRowRemove` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-table-row-remove.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                     | 説明 |
| ---------- | ------------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconTableRowRemoveProperties> |      |

### IconTableRowRemoveプロパティ

**種類:** インターフェース

```typescript
export interface IconTableRowRemoveProperties
```

説明はありません。

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIconTable

**種類:** 関数

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement;
```

`ForgeIconTable` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-table.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconTableProperties> |      |

### アイコンテーブルのプロパティ

**種類:** インターフェース

```typescript
export interface IconTableProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### 鍛造アイコン描画サークル

**種類:** 関数

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement;
```

`ForgeIconDrawCircle` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-draw-circle.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconDrawCircleProperties> |      |

### アイコン描画円プロパティ

**種類:** インターフェース

```typescript
export interface IconDrawCircleProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### 鍛造アイコン描画ライン

**種類:** 関数

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement;
```

`ForgeIconDrawLine` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-draw-line.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconDrawLineProperties> |      |

### アイコン描画ラインプロパティ

**種類:** インターフェース

```typescript
export interface IconDrawLineProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### 鍛造アイコン描画ポリゴン

**種類:** 関数

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement;
```

`ForgeIconDrawPolygon` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-draw-polygon.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconDrawPolygonProperties> |      |

### アイコン描画ポリゴンプロパティ

**種類:** インターフェース

```typescript
export interface IconDrawPolygonProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### 鍛造アイコン描画正方形

**種類:** 関数

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement;
```

`ForgeIconDrawSquare` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-draw-square.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconDrawSquareProperties> |      |

### アイコン描画正方形プロパティ

**種類:** インターフェース

```typescript
export interface IconDrawSquareProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### 鍛造アイコン描画三角形

**種類:** 関数

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement;
```

`ForgeIconDrawTriangle` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-draw-triangle.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconDrawTriangleProperties> |      |

### アイコン描画三角形プロパティ

**種類:** インターフェース

```typescript
export interface IconDrawTriangleProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### 鍛造アイコン移動

**種類:** 関数

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement;
```

`ForgeIconMove` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-move.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMoveProperties> |      |

### アイコン移動プロパティ

**種類:** インターフェース

```typescript
export interface IconMoveProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### 鍛造アイコンパレット

**種類:** 関数

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement;
```

`ForgeIconPalette` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-palette.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPaletteProperties> |      |

### アイコンパレットプロパティ

**種類:** インターフェース

```typescript
export interface IconPaletteProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### 鍛造アイコン鉛筆

**種類:** 関数

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement;
```

`ForgeIconPencil` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-pencil.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPencilProperties> |      |

### アイコン鉛筆プロパティ

**種類:** インターフェース

```typescript
export interface IconPencilProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### 鍛造アイコン回転反時計回り

**種類:** 関数

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement;
```

`ForgeIconRotateCcw` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-rotate-ccw.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconRotateCcwProperties> |      |

### アイコン回転反時計回りプロパティ

**種類:** インターフェース

```typescript
export interface IconRotateCcwProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### 鍛造アイコン回転Cw

**種類:** 関数

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement;
```

`ForgeIconRotateCw` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-rotate-cw.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconRotateCwProperties> |      |

### アイコン回転Cwプロパティ

**種類:** インターフェース

```typescript
export interface IconRotateCwProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### 鍛造アイコンスケールダウン

**種類:** 関数

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement;
```

`ForgeIconScaleDown` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-scale-down.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconScaleDownProperties> |      |

### IconScaleDownプロパティ

**種類:** インターフェース

```typescript
export interface IconScaleDownProperties
```

説明はありません。

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### 鍛造アイコンスケールアップ

**種類:** 関数

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement;
```

`ForgeIconScaleUp` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-scale-up.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconScaleUpProperties> |      |

### IconScaleUpプロパティ

**種類:** インターフェース

```typescript
export interface IconScaleUpProperties
```

説明はありません。

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### 鍛冶アイコン国グローブ

**種類:** 関数

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement;
```

データに基づいたカントリーアクセントを備えた地球儀マーカー。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCountryGlobeProperties> |      |

### アイコン国地球儀プロパティ

**種類:** インターフェース

```typescript
export interface IconCountryGlobeProperties
```

説明はありません。

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### 鍛造アイコンフラグ

**種類:** 関数

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement;
```

境界のあるデータ駆動型の国旗アイコン。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconFlagProperties> |      |

### アイコン国コード

**種類：**タイプ

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

データ駆動型国旗コンポーネントでサポートされている国コード。

### アイコンフラグプロパティ

**種類:** インターフェース

```typescript
export interface IconFlagProperties
```

説明はありません。

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### 鍛造アイコン測地線

**種類:** 関数

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement;
```

`ForgeIconGeodesic` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-geodesic.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconGeodesicProperties> |      |

### アイコン測地線プロパティ

**種類:** インターフェース

```typescript
export interface IconGeodesicProperties
```

説明はありません。

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### 鍛造アイコングローブ

**種類:** 関数

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement;
```

`ForgeIconGlobe` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-globe.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconGlobeProperties> |      |

### アイコングローブプロパティ

**種類:** インターフェース

```typescript
export interface IconGlobeProperties
```

説明はありません。

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### 鍛造アイコン言語

**種類:** 関数

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement;
```

`ForgeIconLanguage` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-language.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconLanguageProperties> |      |

### アイコン言語プロパティ

**種類:** インターフェース

```typescript
export interface IconLanguageProperties
```

説明はありません。

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### 鍛造アイコンマップピン

**種類:** 関数

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement;
```

`ForgeIconMapPin` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-map-pin.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMapPinProperties> |      |

### アイコンマップピンプロパティ

**種類:** インターフェース

```typescript
export interface IconMapPinProperties
```

説明はありません。

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### アイコンレイヤーを鍛造する

**種類:** 関数

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement;
```

表示される地理データを切り替えるためのマップ レイヤー スタック シンボル。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconLayerProperties> |      |

### アイコンレイヤープロパティ

**種類:** インターフェース

```typescript
export interface IconLayerProperties
```

説明はありません。

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### Forgeアイコンマップマーカークラスター

**種類:** 関数

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement;
```

密集した関心のある地点を表すグループ化された地図マーカー。

#### パラメータ

| 名前       | タイプ                                       | 説明 |
| ---------- | -------------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMapMarkerClusterProperties> |      |

### アイコンマップマーカークラスタープロパティ

**種類:** インターフェース

```typescript
export interface IconMapMarkerClusterProperties
```

説明はありません。

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### 鍛造アイコンカメラ

**種類:** 関数

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement;
```

`ForgeIconCamera` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-camera.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCameraProperties> |      |

### アイコンカメラのプロパティ

**種類:** インターフェース

```typescript
export interface IconCameraProperties
```

説明はありません。

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### 鍛造アイコン画像

**種類:** 関数

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement;
```

`ForgeIconImage` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-image.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconImageProperties> |      |

### アイコン画像のプロパティ

**種類:** インターフェース

```typescript
export interface IconImageProperties
```

説明はありません。

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### 鍛造アイコン一時停止

**種類:** 関数

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement;
```

`ForgeIconPause` — ニュートラル JSX で一度作成された、塗りつぶされた一時停止 (❙❙) グリフ
方言。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-pause.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPauseProperties> |      |

### アイコン一時停止プロパティ

**種類:** インターフェース

```typescript
export interface IconPauseProperties
```

説明はありません。

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### フォージアイコンプレイ

**種類:** 関数

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement;
```

`ForgeIconPlay` — ニュートラル JSX で一度作成された塗りつぶされたプレイ (▶) トライアングル
方言。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-play.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPlayProperties> |      |

### アイコンプレイプロパティ

**種類:** インターフェース

```typescript
export interface IconPlayProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### 鍛造アイコン矢印

**種類:** 関数

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement;
```

`ForgeIconArrow` — ニュートラルな JSX 方言で一度作成された方向矢印。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-arrow.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconArrowProperties> |      |

### アイコン矢印方向

**種類：**タイプ

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

矢印の指す方向。

### アイコン矢印プロパティ

**種類:** インターフェース

```typescript
export interface IconArrowProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### 鍛造アイコンシェブロン

**種類:** 関数

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement;
```

`ForgeIconChevron` — 中立的な JSX 方言で一度作成された方向性シェブロン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-chevron.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconChevronProperties> |      |

### アイコンシェブロンプロパティ

**種類:** インターフェース

```typescript
export interface IconChevronProperties
```

説明はありません。

### アイコンの方向

**種類：**タイプ

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

山形が指す方向。

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### 鍛造アイコンシェブロン

**種類:** 関数

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement;
```

`ForgeIconChevrons` — で一度作成された方向性二重シェブロン (« »)
ニュートラルな JSX 方言。通常、最初にジャンプ/最後にジャンプするコントロールに使用されます。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-chevrons.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconChevronsProperties> |      |

### アイコン山形方向

**種類：**タイプ

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

二重山形が指す方向。

### アイコン山形プロパティ

**種類:** インターフェース

```typescript
export interface IconChevronsProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### 鍛造アイコン閉じる

**種類:** 関数

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement;
```

`ForgeIconClose` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-close.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCloseProperties> |      |

### アイコン閉じるプロパティ

**種類:** インターフェース

```typescript
export interface IconCloseProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### 鍛造アイコンホーム

**種類:** 関数

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement;
```

`ForgeIconHome` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-home.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHomeProperties> |      |

### アイコンホームプロパティ

**種類:** インターフェース

```typescript
export interface IconHomeProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### 鍛造アイコン結合

**種類:** 関数

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement;
```

`ForgeIconJoin` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-join.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconJoinProperties> |      |

### アイコン結合プロパティ

**種類:** インターフェース

```typescript
export interface IconJoinProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### 鍛造アイコンメニュー

**種類:** 関数

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement;
```

`ForgeIconMenu` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-menu.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMenuProperties> |      |

### アイコンメニュープロパティ

**種類:** インターフェース

```typescript
export interface IconMenuProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### 鍛造アイコンマイナス

**種類:** 関数

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement;
```

`ForgeIconMinus` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-minus.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconMinusProperties> |      |

### アイコンマイナスプロパティ

**種類:** インターフェース

```typescript
export interface IconMinusProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### フォージアイコンプラス

**種類:** 関数

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement;
```

`ForgeIconPlus` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-plus.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPlusProperties> |      |

### アイコンプラスのプロパティ

**種類:** インターフェース

```typescript
export interface IconPlusProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### 鍛造アイコン更新

**種類:** 関数

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement;
```

`ForgeIconRefresh` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-refresh.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconRefreshProperties> |      |

### アイコン更新プロパティ

**種類:** インターフェース

```typescript
export interface IconRefreshProperties
```

説明はありません。

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### 鍛造アイコン分割

**種類:** 関数

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement;
```

`ForgeIconSplit` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-split.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconSplitProperties> |      |

### アイコン分割プロパティ

**種類:** インターフェース

```typescript
export interface IconSplitProperties
```

説明はありません。

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIcon外部リンク

**種類:** 関数

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement;
```

`ForgeIconExternalLink` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-external-link.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconExternalLinkProperties> |      |

### アイコン外部リンクプロパティ

**種類:** インターフェース

```typescript
export interface IconExternalLinkProperties
```

説明はありません。

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

**種類:** 関数

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement;
```

`ForgeIconLink` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-link.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconLinkProperties> |      |

### アイコンリンクプロパティ

**種類:** インターフェース

```typescript
export interface IconLinkProperties
```

説明はありません。

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### 鍛造アイコン検索

**種類:** 関数

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement;
```

`ForgeIconSearch` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-search.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconSearchProperties> |      |

### アイコン検索プロパティ

**種類:** インターフェース

```typescript
export interface IconSearchProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### 鍛造アイコンクラウド

**種類:** 関数

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement;
```

`ForgeIconCloud` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-cloud.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCloudProperties> |      |

### アイコンクラウドプロパティ

**種類:** インターフェース

```typescript
export interface IconCloudProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### Forgeアイコンデバッグ

**種類:** 関数

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement;
```

`ForgeIconDebug` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-debug.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconDebugProperties> |      |

### アイコンデバッグプロパティ

**種類:** インターフェース

```typescript
export interface IconDebugProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### 鍛造アイコンハートアイコン

**種類:** 関数

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement;
```

`ForgeIconHeart` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heart.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeartProperties> |      |

### アイコンハートプロパティ

**種類:** インターフェース

```typescript
export interface IconHeartProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### 鍛造アイコン雷

**種類:** 関数

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement;
```

`ForgeIconLightning` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-lightning.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconLightningProperties> |      |

### アイコン稲妻プロパティ

**種類:** インターフェース

```typescript
export interface IconLightningProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### 鍛冶アイコンパズル

**種類:** 関数

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement;
```

`ForgeIconPuzzle` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-puzzle.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconPuzzleProperties> |      |

### アイコンパズルプロパティ

**種類:** インターフェース

```typescript
export interface IconPuzzleProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQrCode

**種類:** 関数

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement;
```

`ForgeIconQrCode` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-qr-code.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconQrCodeProperties> |      |

### IconQrCodeプロパティ

**種類:** インターフェース

```typescript
export interface IconQrCodeProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### Forgeアイコン設定

**種類:** 関数

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement;
```

`ForgeIconSettings` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-settings.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconSettingsProperties> |      |

### アイコン設定プロパティ

**種類:** インターフェース

```typescript
export interface IconSettingsProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### フォージアイコンスター

**種類:** 関数

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement;
```

`ForgeIconStar` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-star.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconStarProperties> |      |

### アイコンスタープロパティ

**種類:** インターフェース

```typescript
export interface IconStarProperties
```

説明はありません。

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### 鍛造アイコンレンチ

**種類:** 関数

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement;
```

`ForgeIconWrench` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-wrench.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconWrenchProperties> |      |

### アイコンレンチプロパティ

**種類:** インターフェース

```typescript
export interface IconWrenchProperties
```

説明はありません。

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### 鍛造アイコンルート

**種類:** 関数

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement;
```

明確な開始点と目的地のマーカーを持つルート ライン。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconRouteProperties> |      |

### アイコンルートプロパティ

**種類:** インターフェース

```typescript
export interface IconRouteProperties
```

説明はありません。

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### 鍛造アイコンウェイポイント

**種類:** 関数

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement;
```

ルートと地図オーバーレイ用の再利用可能なウェイポイント マーカー。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconWaypointProperties> |      |

### アイコンウェイポイントプロパティ

**種類:** インターフェース

```typescript
export interface IconWaypointProperties
```

説明はありません。

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### 鍛造アイコンロック開く

**種類:** 関数

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement;
```

`ForgeIconLockOpen` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-lock-open.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconLockOpenProperties> |      |

### アイコンロック開くプロパティ

**種類:** インターフェース

```typescript
export interface IconLockOpenProperties
```

説明はありません。

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### 鍛造アイコンロック

**種類:** 関数

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement;
```

`ForgeIconLock` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-lock.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconLockProperties> |      |

### アイコンロックプロパティ

**種類:** インターフェース

```typescript
export interface IconLockProperties
```

説明はありません。

## `src/components/security/access/forge-icon-user/forge-icon-user`

### Forgeアイコンユーザー

**種類:** 関数

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement;
```

`ForgeIconUser` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-user.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconUserProperties> |      |

### アイコンユーザープロパティ

**種類:** インターフェース

```typescript
export interface IconUserProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIconAlertクリティカル

**種類:** 関数

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement;
```

`ForgeIconAlertCritical` — クリティカル アラート グリフ (八角形内の「!」、
一時停止標識を呼び出す) はニュートラルな JSX 方言で作成されました。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-alert-critical.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                    | 説明 |
| ---------- | ----------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlertCriticalProperties> |      |

### アイコンアラート重要プロパティ

**種類:** インターフェース

```typescript
export interface IconAlertCriticalProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

**種類:** 関数

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement;
```

`ForgeIconAlertInfo` — 情報アラートグリフ (円の中の「i」)
一度は中立的な JSX 方言で作成されました。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-alert-info.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlertInfoProperties> |      |

### IconAlertInfoプロパティ

**種類:** インターフェース

```typescript
export interface IconAlertInfoProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### 鍛造アイコンアラートニュートラル

**種類:** 関数

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement;
```

`ForgeIconAlertNeutral` — ニュートラル アラート グリフ (円内の「-」)
中立的な JSX 方言で一度作成され、何も含まれない情報メッセージ用に作成されます。
ポジティブまたはネガティブな意味合い。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-alert-neutral.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlertNeutralProperties> |      |

### アイコンアラートニュートラルプロパティ

**種類:** インターフェース

```typescript
export interface IconAlertNeutralProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### 鍛造アイコンアラート警告

**種類:** 関数

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement;
```

`ForgeIconAlertWarning` — 警告アラートのグリフ (三角形内の「!」)
一度は中立的な JSX 方言で作成されました。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-alert-warning.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlertWarningProperties> |      |

### アイコンアラート警告プロパティ

**種類:** インターフェース

```typescript
export interface IconAlertWarningProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### 鍛造アイコンアラート

**種類:** 関数

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement;
```

`ForgeIconAlert` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-alert.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlertProperties> |      |

### アイコンアラートプロパティ

**種類:** インターフェース

```typescript
export interface IconAlertProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### 鍛造アイコンチェック

**種類:** 関数

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement;
```

`ForgeIconCheck` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-check.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCheckProperties> |      |

### アイコンチェックプロパティ

**種類:** インターフェース

```typescript
export interface IconCheckProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIconError

**種類:** 関数

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement;
```

`ForgeIconError` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-error.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconErrorProperties> |      |

### アイコンエラープロパティ

**種類:** インターフェース

```typescript
export interface IconErrorProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### Forgeアイコン情報

**種類:** 関数

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement;
```

`ForgeIconInfo` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-info.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconInfoProperties> |      |

### アイコン情報プロパティ

**種類:** インターフェース

```typescript
export interface IconInfoProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### 鍛造アイコン通知

**種類:** 関数

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement;
```

`ForgeIconNotice` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-notice.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconNoticeProperties> |      |

### アイコン通知プロパティ

**種類:** インターフェース

```typescript
export interface IconNoticeProperties
```

説明はありません。

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### 鍛造アイコン警告

**種類:** 関数

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement;
```

`ForgeIconWarning` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-warning.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconWarningProperties> |      |

### アイコン警告プロパティ

**種類:** インターフェース

```typescript
export interface IconWarningProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### 鍛造アイコン中央揃え

**種類:** 関数

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement;
```

`ForgeIconAlignCenter` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-align-center.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlignCenterProperties> |      |

### アイコンAlignCenterプロパティ

**種類:** インターフェース

```typescript
export interface IconAlignCenterProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### 鍛造アイコン整列位置調整

**種類:** 関数

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement;
```

`ForgeIconAlignJustify` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-align-justify.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlignJustifyProperties> |      |

### アイコンAlignJustifyプロパティ

**種類:** インターフェース

```typescript
export interface IconAlignJustifyProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### 鍛造アイコン左揃え

**種類:** 関数

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement;
```

`ForgeIconAlignLeft` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-align-left.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlignLeftProperties> |      |

### アイコン左揃えプロパティ

**種類:** インターフェース

```typescript
export interface IconAlignLeftProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### 鍛造アイコン右揃え

**種類:** 関数

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement;
```

`ForgeIconAlignRight` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-align-right.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconAlignRightProperties> |      |

### アイコン右揃えプロパティ

**種類:** インターフェース

```typescript
export interface IconAlignRightProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### 鍛造アイコンブロック引用

**種類:** 関数

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement;
```

`ForgeIconBlockquote` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-blockquote.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconBlockquoteProperties> |      |

### アイコンブロック引用プロパティ

**種類:** インターフェース

```typescript
export interface IconBlockquoteProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### 鍛造アイコン太字

**種類:** 関数

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement;
```

`ForgeIconBold` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-bold.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<IconBoldProperties> |      |

### アイコン太字プロパティ

**種類:** インターフェース

```typescript
export interface IconBoldProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### 鍛冶アイコン弾丸リスト

**種類:** 関数

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement;
```

`ForgeIconBulletList` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-bullet-list.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconBulletListProperties> |      |

### IconBulletListプロパティ

**種類:** インターフェース

```typescript
export interface IconBulletListProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIconCodeBlock

**種類:** 関数

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement;
```

`ForgeIconCodeBlock` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-code-block.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCodeBlockProperties> |      |

### IconCodeBlockProperties

**種類:** インターフェース

```typescript
export interface IconCodeBlockProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

**種類:** 関数

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement;
```

`ForgeIconCodeInline` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-code-inline.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconCodeInlineProperties> |      |

### IconCodeInlineProperties

**種類:** インターフェース

```typescript
export interface IconCodeInlineProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### 鍛造アイコン見出し 5

**種類:** 関数

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement;
```

`ForgeIconHeadingFive` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-five.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingFiveProperties> |      |

### IconHeadingFiveProperties

**種類:** インターフェース

```typescript
export interface IconHeadingFiveProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### 鍛造アイコン見出し 4

**種類:** 関数

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement;
```

`ForgeIconHeadingFour` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-four.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingFourProperties> |      |

### アイコン見出し4 プロパティ

**種類:** インターフェース

```typescript
export interface IconHeadingFourProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### 鍛造アイコン見出し 1

**種類:** 関数

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement;
```

`ForgeIconHeadingOne` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-one.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingOneProperties> |      |

### IconHeadingOneProperties

**種類:** インターフェース

```typescript
export interface IconHeadingOneProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### 鍛造アイコン見出し6

**種類:** 関数

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement;
```

`ForgeIconHeadingSix` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-six.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingSixProperties> |      |

### アイコン見出しSixProperties

**種類:** インターフェース

```typescript
export interface IconHeadingSixProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### 鍛造アイコン見出し 3

**種類:** 関数

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement;
```

`ForgeIconHeadingThree` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-three.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingThreeProperties> |      |

### アイコン見出し 3 つのプロパティ

**種類:** インターフェース

```typescript
export interface IconHeadingThreeProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### 鍛造アイコン見出し 2

**種類:** 関数

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement;
```

`ForgeIconHeadingTwo` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading-two.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                 | 説明 |
| ---------- | -------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingTwoProperties> |      |

### アイコン見出し 2 つのプロパティ

**種類:** インターフェース

```typescript
export interface IconHeadingTwoProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### 鍛造アイコン見出し

**種類:** 関数

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement;
```

`ForgeIconHeading` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-heading.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<IconHeadingProperties> |      |

### アイコン見出しプロパティ

**種類:** インターフェース

```typescript
export interface IconHeadingProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### 鍛造アイコン斜体

**種類:** 関数

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement;
```

`ForgeIconItalic` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-italic.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconItalicProperties> |      |

### アイコン斜体プロパティ

**種類:** インターフェース

```typescript
export interface IconItalicProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

**種類:** 関数

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement;
```

`ForgeIconNumberedList` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-numbered-list.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconNumberedListProperties> |      |

### IconNumberedListプロパティ

**種類:** インターフェース

```typescript
export interface IconNumberedListProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### 鍛造アイコン取り消し線

**種類:** 関数

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement;
```

`ForgeIconStrikethrough` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-strikethrough.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                    | 説明 |
| ---------- | ----------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconStrikethroughProperties> |      |

### アイコン取り消し線プロパティ

**種類:** インターフェース

```typescript
export interface IconStrikethroughProperties
```

説明はありません。

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### 鍛造アイコン下線

**種類:** 関数

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement;
```

`ForgeIconUnderline` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-underline.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                                | 説明 |
| ---------- | ------------------------------------- | ---- |
| プロパティ | 読み取り専用<IconUnderlineProperties> |      |

### アイコン下線プロパティ

**種類:** インターフェース

```typescript
export interface IconUnderlineProperties
```

説明はありません。

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### 鍛造アイコンカレンダー

**種類:** 関数

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement;
```

`ForgeIconCalendar` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-calendar.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<IconCalendarProperties> |      |

### アイコンカレンダープロパティ

**種類:** インターフェース

```typescript
export interface IconCalendarProperties
```

説明はありません。

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### 鍛造アイコン時計

**種類:** 関数

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement;
```

`ForgeIconClock` — ニュートラルな JSX 方言で一度作成されたアイコン。

vite-plugin-forge を介して、Vue 3 と React の両方に正常にコンパイルされます。
2段階のコンパイラ。アイコンには、独自の `@layer mp.icons` CSS が含まれています。
CSS モジュール `forge-icon-clock.module.scss` が同じ場所にあります。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<IconClockProperties> |      |

### アイコンクロックプロパティ

**種類:** インターフェース

```typescript
export interface IconClockProperties
```

説明はありません。

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**種類:** 定数

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[];
```

説明はありません。

### 構成を検証する

**種類:** 関数

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void;
```

説明はありません。

#### パラメータ

| 名前         | タイプ                              | 説明 |
| ------------ | ----------------------------------- | ---- |
| 作曲         | 読み取り専用 IconSymbolDefinition[] |      |
| 利用可能なID | ReadonlySet<string>                 |      |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**種類:** 定数

```typescript
export const SUPPORTED_COUNTRY_CODES;
```

公開されたスプライト内の正規の国旗シンボルを含む国コード。

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**種類:** 定数

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS;
```

説明はありません。

### ICON_SYMBOL_DEFINITIONS

**種類:** 定数

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[];
```

説明はありません。

## `src/sprite/icon-use`

### 鍛造アイコン使用

**種類:** 関数

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement;
```

正規のスプライト ジオメトリを参照する共有アクセス可能なラッパー。

#### パラメータ

| 名前       | タイプ                          | 説明 |
| ---------- | ------------------------------- | ---- |
| プロパティ | 読み取り専用<IconUseProperties> |      |

### アイコン使用プロパティ

**種類:** インターフェース

```typescript
export interface IconUseProperties
```

説明はありません。

## `src/sprite/provider`

### アイコンスプライトコンテキスト

**種類:** 定数

```typescript
export const IconSpriteContext;
```

説明はありません。

### アイコンスプライトプロバイダー

**種類:** 関数

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement;
```

アイコン コンポーネントのサブツリーに対して 1 つのインライン スプライト ホストをマウントします。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconSpriteProperties> |      |

### useIconHref

**種類:** 関数

```typescript
function useIconHref(symbolId: string): string;
```

アイコン ラッパーのローカルまたは外部シンボル URL を解決します。

#### パラメータ

| 名前       | タイプ | 説明 |
| ---------- | ------ | ---- |
| シンボルID | 文字列 |      |

## `src/sprite/serialize`

### シリアル化ノード

**種類:** 関数

```typescript
function serializeNode(node: IconSvgNode): string;
```

説明はありません。

#### パラメータ

| 名前 | タイプ            | 説明 |
| ---- | ----------------- | ---- |
| node | アイコンSvgノード |      |

### シリアライズスプライト

**種類:** 関数

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string;
```

説明はありません。

#### パラメータ

| 名前 | タイプ                              | 説明 |
| ---- | ----------------------------------- | ---- |
| 定義 | 読み取り専用 IconSymbolDefinition[] |      |

### シリアル化シンボル

**種類:** 関数

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string;
```

説明はありません。

#### パラメータ

| 名前 | タイプ               | 説明 |
| ---- | -------------------- | ---- |
| 定義 | アイコンシンボル定義 |      |

## `src/sprite/types`

### アイコンスプライトコンテキスト値

**種類:** インターフェース

```typescript
export interface IconSpriteContextValue
```

説明はありません。

### アイコンスプライトプロパティ

**種類:** インターフェース

```typescript
export interface IconSpriteProperties
```

説明はありません。

### アイコンSvgノード

**種類:** インターフェース

```typescript
export interface IconSvgNode
```

説明はありません。

### アイコンシンボル定義

**種類:** インターフェース

```typescript
export interface IconSymbolDefinition
```

説明はありません。

### アイコンシンボル使用

**種類:** インターフェース

```typescript
export interface IconSymbolUse
```

説明はありません。
