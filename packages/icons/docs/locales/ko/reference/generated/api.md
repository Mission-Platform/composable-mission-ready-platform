# @mission-platform/icons API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> packages/icons/docs/reference/generated/api.md: [packages/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/icons`의 공개 소스 선언에서 생성됩니다.

## `src/catalog`

### ICON_CATALOG

**종류:** 상수

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[]
```

기존 아이콘 세트에 대해 검토된 카탈로그입니다.

### ICON_GAP_REVIEW

**종류:** 상수

```typescript
export const ICON_GAP_REVIEW
```

소스 분류가 확립된 후 구현할 고부가가치 추가입니다.

### 아이콘카탈로그항목

**종류:** 인터페이스

```typescript
export interface IconCatalogEntry
```

이 패키지의 모든 아이콘 소스에 대한 안정적인 카테고리 할당입니다.

### 유효성 검사아이콘카탈로그

**종류:** 기능

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void
```

패키지 메타데이터를 생성하는 데 사용되기 전에 정적 카탈로그의 유효성을 검사하십시오.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 카탈로그 | 읽기 전용 IconCatalogEntry[] |  |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### 포지아이콘벨

**종류:** 기능

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement
```

`ForgeIconBell` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-bell.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconBellProperties> |  |

### 아이콘벨속성

**종류:** 인터페이스

```typescript
export interface IconBellProperties
```

설명이 제공되지 않았습니다.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### Forge아이콘채팅

**종류:** 기능

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement
```

`ForgeIconChat` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-chat.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconChatProperties> |  |

### IconChat속성

**종류:** 인터페이스

```typescript
export interface IconChatProperties
```

설명이 제공되지 않았습니다.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### Forge아이콘메일

**종류:** 기능

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement
```

`ForgeIconMail` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-mail.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMailProperties> |  |

### 아이콘메일속성

**종류:** 인터페이스

```typescript
export interface IconMailProperties
```

설명이 제공되지 않았습니다.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### Forge아이콘전화

**종류:** 기능

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement
```

`ForgeIconPhone` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-phone.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPhoneProperties> |  |

### 아이콘전화속성

**종류:** 인터페이스

```typescript
export interface IconPhoneProperties
```

설명이 제공되지 않았습니다.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### Forge아이콘보내기

**종류:** 기능

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement
```

`ForgeIconSend` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-send.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSendProperties> |  |

### 아이콘보내기속성

**종류:** 인터페이스

```typescript
export interface IconSendProperties
```

설명이 제공되지 않았습니다.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### Forge아이콘공유

**종류:** 기능

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement
```

`ForgeIconShare` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-share.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconShareProperties> |  |

### 아이콘공유속성

**종류:** 인터페이스

```typescript
export interface IconShareProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### 위조아이콘복사

**종류:** 기능

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement
```

`ForgeIconCopy` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-copy.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCopyProperties> |  |

### 아이콘복사속성

**종류:** 인터페이스

```typescript
export interface IconCopyProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### Forge아이콘편집

**종류:** 기능

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement
```

`ForgeIconEdit` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-edit.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconEditProperties> |  |

### 아이콘편집속성

**종류:** 인터페이스

```typescript
export interface IconEditProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### 포지아이콘눈꺼짐

**종류:** 기능

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement
```

`ForgeIconEyeOff` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-eye-off.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconEyeOffProperties> |  |

### 아이콘눈꺼짐속성

**종류:** 인터페이스

```typescript
export interface IconEyeOffProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### 포지아이콘눈

**종류:** 기능

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement
```

`ForgeIconEye` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-eye.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconEyeProperties> |  |

### 아이콘눈속성

**종류:** 인터페이스

```typescript
export interface IconEyeProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### Forge아이콘다시 실행

**종류:** 기능

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement
```

가장 최근에 실행 취소된 콘텐츠 편집 내용을 다시 적용합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconRedoProperties> |  |

### 아이콘다시 실행속성

**종류:** 인터페이스

```typescript
export interface IconRedoProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### 포지아이콘쓰레기

**종류:** 기능

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement
```

`ForgeIconTrash` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-trash.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTrashProperties> |  |

### 아이콘휴지통속성

**종류:** 인터페이스

```typescript
export interface IconTrashProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### Forge아이콘실행 취소

**종류:** 기능

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement
```

가장 최근의 콘텐츠 편집 내용을 되돌립니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconUndoProperties> |  |

### 아이콘실행 취소속성

**종류:** 인터페이스

```typescript
export interface IconUndoProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIcon다운로드

**종류:** 기능

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement
```

`ForgeIconDownload` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-download.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDownloadProperties> |  |

### 아이콘다운로드속성

**종류:** 인터페이스

```typescript
export interface IconDownloadProperties
```

설명이 제공되지 않았습니다.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### Forge아이콘업로드

**종류:** 기능

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement
```

`ForgeIconUpload` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-upload.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconUploadProperties> |  |

### 아이콘업로드속성

**종류:** 인터페이스

```typescript
export interface IconUploadProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### Forge아이콘필터

**종류:** 기능

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement
```

`ForgeIconFilter` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-filter.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconFilterProperties> |  |

### 아이콘필터속성

**종류:** 인터페이스

```typescript
export interface IconFilterProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### Forge아이콘정렬

**종류:** 기능

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement
```

`ForgeIconSort` — 중립 JSX에서 한 번 작성된 2개 갈매기형 정렬 표시기
방언.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-sort.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSortProperties> |  |

### 아이콘정렬속성

**종류:** 인터페이스

```typescript
export interface IconSortProperties
```

설명이 제공되지 않았습니다.

### 정렬방향

**종류:** 유형

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

활성 정렬 방향 또는 정렬되지 않은 경우 `undefined`입니다.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### Forge아이콘테이블열추가

**종류:** 기능

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement
```

`ForgeIconTableColumnAdd` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-table-column-add.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTableColumnAddProperties> |  |

### 아이콘테이블열추가속성

**종류:** 인터페이스

```typescript
export interface IconTableColumnAddProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### Forge아이콘테이블열제거

**종류:** 기능

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement
```

`ForgeIconTableColumnRemove` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-table-column-remove.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTableColumnRemoveProperties> |  |

### 아이콘테이블열제거속성

**종류:** 인터페이스

```typescript
export interface IconTableColumnRemoveProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### Forge아이콘테이블행추가

**종류:** 기능

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement
```

`ForgeIconTableRowAdd` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-table-row-add.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTableRowAddProperties> |  |

### 아이콘테이블행추가속성

**종류:** 인터페이스

```typescript
export interface IconTableRowAddProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### 단조아이콘테이블행제거

**종류:** 기능

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement
```

`ForgeIconTableRowRemove` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-table-row-remove.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTableRowRemoveProperties> |  |

### 아이콘테이블행제거속성

**종류:** 인터페이스

```typescript
export interface IconTableRowRemoveProperties
```

설명이 제공되지 않았습니다.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### Forge아이콘테이블

**종류:** 기능

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement
```

`ForgeIconTable` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-table.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconTableProperties> |  |

### 아이콘테이블속성

**종류:** 인터페이스

```typescript
export interface IconTableProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### 포지아이콘그리기원

**종류:** 기능

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement
```

`ForgeIconDrawCircle` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-draw-circle.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDrawCircleProperties> |  |

### 아이콘그리기원속성

**종류:** 인터페이스

```typescript
export interface IconDrawCircleProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### 포지아이콘그리기라인

**종류:** 기능

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement
```

`ForgeIconDrawLine` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-draw-line.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDrawLineProperties> |  |

### IconDrawLine속성

**종류:** 인터페이스

```typescript
export interface IconDrawLineProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### 포지아이콘그리기다각형

**종류:** 기능

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement
```

`ForgeIconDrawPolygon` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-draw-polygon.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDrawPolygonProperties> |  |

### 아이콘그리기다각형속성

**종류:** 인터페이스

```typescript
export interface IconDrawPolygonProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### 포지아이콘그리기광장

**종류:** 기능

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement
```

`ForgeIconDrawSquare` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-draw-square.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDrawSquareProperties> |  |

### 아이콘그리기스퀘어속성

**종류:** 인터페이스

```typescript
export interface IconDrawSquareProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### 포지아이콘그리기삼각형

**종류:** 기능

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement
```

`ForgeIconDrawTriangle` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-draw-triangle.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDrawTriangleProperties> |  |

### 아이콘그리기삼각형속성

**종류:** 인터페이스

```typescript
export interface IconDrawTriangleProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### 포지아이콘이동

**종류:** 기능

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement
```

`ForgeIconMove` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-move.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMoveProperties> |  |

### 아이콘이동속성

**종류:** 인터페이스

```typescript
export interface IconMoveProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### Forge아이콘팔레트

**종류:** 기능

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement
```

`ForgeIconPalette` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-palette.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPaletteProperties> |  |

### 아이콘팔레트속성

**종류:** 인터페이스

```typescript
export interface IconPaletteProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### 포지아이콘연필

**종류:** 기능

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement
```

`ForgeIconPencil` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-pencil.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPencilProperties> |  |

### 아이콘연필속성

**종류:** 인터페이스

```typescript
export interface IconPencilProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### 단조아이콘회전Ccw

**종류:** 기능

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement
```

`ForgeIconRotateCcw` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-rotate-ccw.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconRotateCcwProperties> |  |

### 아이콘회전Ccw속성

**종류:** 인터페이스

```typescript
export interface IconRotateCcwProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### 단조아이콘회전Cw

**종류:** 기능

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement
```

`ForgeIconRotateCw` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-rotate-cw.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconRotateCwProperties> |  |

### 아이콘회전Cw속성

**종류:** 인터페이스

```typescript
export interface IconRotateCwProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### 포지아이콘규모축소

**종류:** 기능

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement
```

`ForgeIconScaleDown` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-scale-down.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconScaleDownProperties> |  |

### 아이콘크기 축소속성

**종류:** 인터페이스

```typescript
export interface IconScaleDownProperties
```

설명이 제공되지 않았습니다.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### 포지아이콘규모확대

**종류:** 기능

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement
```

`ForgeIconScaleUp` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-scale-up.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconScaleUpProperties> |  |

### 아이콘ScaleUp속성

**종류:** 인터페이스

```typescript
export interface IconScaleUpProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### 포지아이콘국가글로브

**종류:** 기능

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement
```

데이터 기반 국가 악센트가 있는 지구본 마커입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCountryGlobeProperties> |  |

### 아이콘국가글로브속성

**종류:** 인터페이스

```typescript
export interface IconCountryGlobeProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### 포지아이콘플래그

**종류:** 기능

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement
```

제한된 데이터 기반 국가 국기 아이콘입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconFlagProperties> |  |

### 아이콘국가코드

**종류:** 유형

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

데이터 기반 플래그 구성 요소에 대해 지원되는 국가 코드입니다.

### 아이콘플래그속성

**종류:** 인터페이스

```typescript
export interface IconFlagProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### 단조아이콘측지선

**종류:** 기능

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement
```

`ForgeIconGeodesic` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-geodesic.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconGeodesicProperties> |  |

### 아이콘측지 속성

**종류:** 인터페이스

```typescript
export interface IconGeodesicProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### 포지아이콘글로브

**종류:** 기능

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement
```

`ForgeIconGlobe` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-globe.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconGlobeProperties> |  |

### 아이콘글로브속성

**종류:** 인터페이스

```typescript
export interface IconGlobeProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### Forge아이콘언어

**종류:** 기능

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement
```

`ForgeIconLanguage` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-language.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLanguageProperties> |  |

### 아이콘언어속성

**종류:** 인터페이스

```typescript
export interface IconLanguageProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### 포지아이콘지도핀

**종류:** 기능

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement
```

`ForgeIconMapPin` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-map-pin.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMapPinProperties> |  |

### 아이콘지도핀속성

**종류:** 인터페이스

```typescript
export interface IconMapPinProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### 단조아이콘레이어

**종류:** 기능

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement
```

표시되는 지리 데이터를 전환하기 위한 지도 레이어 스택 기호입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLayerProperties> |  |

### 아이콘레이어속성

**종류:** 인터페이스

```typescript
export interface IconLayerProperties
```

설명이 제공되지 않았습니다.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### Forge아이콘지도마커클러스터

**종류:** 기능

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement
```

밀집된 관심 지점을 표시하는 그룹화된 지도 표시입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMapMarkerClusterProperties> |  |

### 아이콘지도마커클러스터속성

**종류:** 인터페이스

```typescript
export interface IconMapMarkerClusterProperties
```

설명이 제공되지 않았습니다.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### Forge아이콘카메라

**종류:** 기능

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement
```

`ForgeIconCamera` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-camera.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCameraProperties> |  |

### 아이콘카메라속성

**종류:** 인터페이스

```typescript
export interface IconCameraProperties
```

설명이 제공되지 않았습니다.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### 단조아이콘이미지

**종류:** 기능

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement
```

`ForgeIconImage` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-image.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconImageProperties> |  |

### 아이콘이미지속성

**종류:** 인터페이스

```typescript
export interface IconImageProperties
```

설명이 제공되지 않았습니다.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### Forge아이콘일시 중지

**종류:** 기능

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement
```

`ForgeIconPause` — 중립 JSX에서 한 번 작성된 채워진 일시정지(❙❙) 글리프
방언.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-pause.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPauseProperties> |  |

### 아이콘일시 중지속성

**종류:** 인터페이스

```typescript
export interface IconPauseProperties
```

설명이 제공되지 않았습니다.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### Forge아이콘플레이

**종류:** 기능

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement
```

`ForgeIconPlay` — 중립 JSX에서 한 번 작성된 채워진 플레이(▶) 삼각형
방언.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-play.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPlayProperties> |  |

### IconPlay속성

**종류:** 인터페이스

```typescript
export interface IconPlayProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### 포지아이콘화살표

**종류:** 기능

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement
```

`ForgeIconArrow` — 중립 JSX 방언으로 한 번 작성된 방향 화살표입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-arrow.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconArrowProperties> |  |

### 아이콘화살방향

**종류:** 유형

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

화살표가 가리키는 방향입니다.

### 아이콘화살표속성

**종류:** 인터페이스

```typescript
export interface IconArrowProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### 포지아이콘쉐브론

**종류:** 기능

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement
```

`ForgeIconChevron` — 중립 JSX 방언으로 한 번 작성된 방향 쉐브론입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-chevron.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconChevronProperties> |  |

### 아이콘쉐브론속성

**종류:** 인터페이스

```typescript
export interface IconChevronProperties
```

설명이 제공되지 않았습니다.

### 아이콘방향

**종류:** 유형

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

쉐브론이 가리키는 방향입니다.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### 포지아이콘쉐브론

**종류:** 기능

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement
```

`ForgeIconChevrons` —
일반적으로 처음으로 점프/마지막으로 점프 컨트롤에 사용되는 중립 JSX 방언입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-chevrons.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconChevronsProperties> |  |

### 아이콘쉐브론방향

**종류:** 유형

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

이중 갈매기형이 가리키는 방향입니다.

### 아이콘갈매기형속성

**종류:** 인터페이스

```typescript
export interface IconChevronsProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### Forge아이콘닫기

**종류:** 기능

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement
```

`ForgeIconClose` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-close.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCloseProperties> |  |

### 아이콘닫기속성

**종류:** 인터페이스

```typescript
export interface IconCloseProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### Forge아이콘홈

**종류:** 기능

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement
```

`ForgeIconHome` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-home.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHomeProperties> |  |

### 아이콘홈속성

**종류:** 인터페이스

```typescript
export interface IconHomeProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### Forge아이콘가입

**종류:** 기능

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement
```

`ForgeIconJoin` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-join.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconJoinProperties> |  |

### IconJoin속성

**종류:** 인터페이스

```typescript
export interface IconJoinProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### Forge아이콘메뉴

**종류:** 기능

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement
```

`ForgeIconMenu` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-menu.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMenuProperties> |  |

### 아이콘메뉴속성

**종류:** 인터페이스

```typescript
export interface IconMenuProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### Forge아이콘마이너스

**종류:** 기능

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement
```

`ForgeIconMinus` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-minus.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconMinusProperties> |  |

### 아이콘빼기속성

**종류:** 인터페이스

```typescript
export interface IconMinusProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

**종류:** 기능

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement
```

`ForgeIconPlus` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-plus.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPlusProperties> |  |

### IconPlus속성

**종류:** 인터페이스

```typescript
export interface IconPlusProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### Forge아이콘새로 고침

**종류:** 기능

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement
```

`ForgeIconRefresh` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-refresh.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconRefreshProperties> |  |

### 아이콘새로고침속성

**종류:** 인터페이스

```typescript
export interface IconRefreshProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### 포지아이콘분할

**종류:** 기능

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement
```

`ForgeIconSplit` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-split.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSplitProperties> |  |

### 아이콘분할속성

**종류:** 인터페이스

```typescript
export interface IconSplitProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### Forge아이콘외부링크

**종류:** 기능

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement
```

`ForgeIconExternalLink` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-external-link.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconExternalLinkProperties> |  |

### 아이콘외부링크속성

**종류:** 인터페이스

```typescript
export interface IconExternalLinkProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### Forge아이콘링크

**종류:** 기능

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement
```

`ForgeIconLink` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-link.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLinkProperties> |  |

### IconLink속성

**종류:** 인터페이스

```typescript
export interface IconLinkProperties
```

설명이 제공되지 않았습니다.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### Forge아이콘검색

**종류:** 기능

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement
```

`ForgeIconSearch` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-search.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSearchProperties> |  |

### 아이콘검색속성

**종류:** 인터페이스

```typescript
export interface IconSearchProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### Forge아이콘클라우드

**종류:** 기능

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement
```

`ForgeIconCloud` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-cloud.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCloudProperties> |  |

### IconCloud속성

**종류:** 인터페이스

```typescript
export interface IconCloudProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### Forge아이콘디버그

**종류:** 기능

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement
```

`ForgeIconDebug` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-debug.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconDebugProperties> |  |

### 아이콘디버그속성

**종류:** 인터페이스

```typescript
export interface IconDebugProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### 포지아이콘심장

**종류:** 기능

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement
```

`ForgeIconHeart` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heart.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeartProperties> |  |

### 아이콘심장속성

**종류:** 인터페이스

```typescript
export interface IconHeartProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### 포지아이콘번개

**종류:** 기능

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement
```

`ForgeIconLightning` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-lightning.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLightningProperties> |  |

### 아이콘번개속성

**종류:** 인터페이스

```typescript
export interface IconLightningProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIcon퍼즐

**종류:** 기능

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement
```

`ForgeIconPuzzle` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-puzzle.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconPuzzleProperties> |  |

### 아이콘퍼즐속성

**종류:** 인터페이스

```typescript
export interface IconPuzzleProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQr코드

**종류:** 기능

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement
```

`ForgeIconQrCode` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-qr-code.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconQrCodeProperties> |  |

### IconQrCode속성

**종류:** 인터페이스

```typescript
export interface IconQrCodeProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### Forge아이콘 설정

**종류:** 기능

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement
```

`ForgeIconSettings` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-settings.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSettingsProperties> |  |

### 아이콘설정속성

**종류:** 인터페이스

```typescript
export interface IconSettingsProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### 포지아이콘스타

**종류:** 기능

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement
```

`ForgeIconStar` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-star.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconStarProperties> |  |

### 아이콘스타속성

**종류:** 인터페이스

```typescript
export interface IconStarProperties
```

설명이 제공되지 않았습니다.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### 포지아이콘렌치

**종류:** 기능

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement
```

`ForgeIconWrench` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-wrench.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconWrenchProperties> |  |

### 아이콘렌치속성

**종류:** 인터페이스

```typescript
export interface IconWrenchProperties
```

설명이 제공되지 않았습니다.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### Forge아이콘경로

**종류:** 기능

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement
```

뚜렷한 시작 및 목적지 표시가 있는 경로선입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconRouteProperties> |  |

### 아이콘경로속성

**종류:** 인터페이스

```typescript
export interface IconRouteProperties
```

설명이 제공되지 않았습니다.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### 포지아이콘웨이포인트

**종류:** 기능

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement
```

경로 및 지도 오버레이를 위한 재사용 가능한 웨이포인트 마커입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconWaypointProperties> |  |

### 아이콘Waypoint속성

**종류:** 인터페이스

```typescript
export interface IconWaypointProperties
```

설명이 제공되지 않았습니다.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### 포지아이콘자물쇠열다

**종류:** 기능

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement
```

`ForgeIconLockOpen` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-lock-open.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLockOpenProperties> |  |

### 아이콘잠금열기속성

**종류:** 인터페이스

```typescript
export interface IconLockOpenProperties
```

설명이 제공되지 않았습니다.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### Forge아이콘자물쇠

**종류:** 기능

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement
```

`ForgeIconLock` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-lock.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconLockProperties> |  |

### 아이콘잠금속성

**종류:** 인터페이스

```typescript
export interface IconLockProperties
```

설명이 제공되지 않았습니다.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIcon사용자

**종류:** 기능

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement
```

`ForgeIconUser` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-user.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconUserProperties> |  |

### 아이콘사용자속성

**종류:** 인터페이스

```typescript
export interface IconUserProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### Forge아이콘경고위험

**종류:** 기능

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement
```

`ForgeIconAlertCritical` — 중요한 경고 문자(팔각형 내부의 "!",
정지 신호를 불러일으키는 것)은 중립 JSX 방언으로 한 번 작성되었습니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-alert-critical.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlertCriticalProperties> |  |

### IconAlertCritical속성

**종류:** 인터페이스

```typescript
export interface IconAlertCriticalProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### Forge아이콘경고정보

**종류:** 기능

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement
```

`ForgeIconAlertInfo` — 정보 경고 문자(원 안의 "i")
중립 JSX 방언으로 한 번 작성되었습니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-alert-info.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlertInfoProperties> |  |

### 아이콘경고정보속성

**종류:** 인터페이스

```typescript
export interface IconAlertInfoProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### Forge아이콘경고중립

**종류:** 기능

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement
```

`ForgeIconAlertNeutral` — 중립 경고 문자(원 안의 "-")
중립 JSX 방언으로 한 번 작성되었으며,
긍정적이거나 부정적인 의미.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-alert-neutral.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlertNeutralProperties> |  |

### 아이콘경고중립속성

**종류:** 인터페이스

```typescript
export interface IconAlertNeutralProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### Forge아이콘경고경고

**종류:** 기능

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement
```

`ForgeIconAlertWarning` — 경고 경고 문자(삼각형 안의 "!")
중립 JSX 방언으로 한 번 작성되었습니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-alert-warning.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlertWarningProperties> |  |

### 아이콘경고경고속성

**종류:** 인터페이스

```typescript
export interface IconAlertWarningProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### Forge아이콘경고

**종류:** 기능

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement
```

`ForgeIconAlert` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-alert.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlertProperties> |  |

### 아이콘경고속성

**종류:** 인터페이스

```typescript
export interface IconAlertProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### Forge아이콘체크

**종류:** 기능

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement
```

`ForgeIconCheck` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-check.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCheckProperties> |  |

### IconCheck속성

**종류:** 인터페이스

```typescript
export interface IconCheckProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### Forge아이콘오류

**종류:** 기능

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement
```

`ForgeIconError` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-error.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconErrorProperties> |  |

### IconError속성

**종류:** 인터페이스

```typescript
export interface IconErrorProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### Forge아이콘정보

**종류:** 기능

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement
```

`ForgeIconInfo` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-info.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconInfoProperties> |  |

### 아이콘정보속성

**종류:** 인터페이스

```typescript
export interface IconInfoProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### Forge아이콘공지사항

**종류:** 기능

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement
```

`ForgeIconNotice` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-notice.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconNoticeProperties> |  |

### 아이콘공지사항속성

**종류:** 인터페이스

```typescript
export interface IconNoticeProperties
```

설명이 제공되지 않았습니다.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### Forge아이콘경고

**종류:** 기능

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement
```

`ForgeIconWarning` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-warning.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconWarningProperties> |  |

### 아이콘경고속성

**종류:** 인터페이스

```typescript
export interface IconWarningProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### Forge아이콘정렬센터

**종류:** 기능

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement
```

`ForgeIconAlignCenter` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-align-center.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlignCenterProperties> |  |

### 아이콘정렬센터속성

**종류:** 인터페이스

```typescript
export interface IconAlignCenterProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### Forge아이콘정렬Justify

**종류:** 기능

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement
```

`ForgeIconAlignJustify` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-align-justify.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlignJustifyProperties> |  |

### 아이콘정렬Justify속성

**종류:** 인터페이스

```typescript
export interface IconAlignJustifyProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### 포지아이콘정렬왼쪽

**종류:** 기능

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement
```

`ForgeIconAlignLeft` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-align-left.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlignLeftProperties> |  |

### 아이콘정렬왼쪽속성

**종류:** 인터페이스

```typescript
export interface IconAlignLeftProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### 포지아이콘정렬오른쪽

**종류:** 기능

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement
```

`ForgeIconAlignRight` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-align-right.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconAlignRightProperties> |  |

### 아이콘정렬오른쪽속성

**종류:** 인터페이스

```typescript
export interface IconAlignRightProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIconBlockquote

**종류:** 기능

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement
```

`ForgeIconBlockquote` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-blockquote.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconBlockquoteProperties> |  |

### IconBlockquote속성

**종류:** 인터페이스

```typescript
export interface IconBlockquoteProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### Forge아이콘굵은

**종류:** 기능

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement
```

`ForgeIconBold` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-bold.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconBoldProperties> |  |

### IconBold속성

**종류:** 인터페이스

```typescript
export interface IconBoldProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### 단조아이콘글머리 기호목록

**종류:** 기능

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement
```

`ForgeIconBulletList` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-bullet-list.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconBulletListProperties> |  |

### IconBulletList속성

**종류:** 인터페이스

```typescript
export interface IconBulletListProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### Forge아이콘코드블록

**종류:** 기능

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement
```

`ForgeIconCodeBlock` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-code-block.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCodeBlockProperties> |  |

### IconCodeBlock속성

**종류:** 인터페이스

```typescript
export interface IconCodeBlockProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCode인라인

**종류:** 기능

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement
```

`ForgeIconCodeInline` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-code-inline.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCodeInlineProperties> |  |

### IconCodeInline속성

**종류:** 인터페이스

```typescript
export interface IconCodeInlineProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### 포지아이콘제목다섯

**종류:** 기능

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement
```

`ForgeIconHeadingFive` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-five.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingFiveProperties> |  |

### 아이콘제목다섯속성

**종류:** 인터페이스

```typescript
export interface IconHeadingFiveProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### 포지아이콘제목네

**종류:** 기능

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement
```

`ForgeIconHeadingFour` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-four.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingFourProperties> |  |

### 아이콘제목4속성

**종류:** 인터페이스

```typescript
export interface IconHeadingFourProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### 포지아이콘제목하나

**종류:** 기능

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement
```

`ForgeIconHeadingOne` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-one.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingOneProperties> |  |

### 아이콘제목One속성

**종류:** 인터페이스

```typescript
export interface IconHeadingOneProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### 포지아이콘제목여섯

**종류:** 기능

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement
```

`ForgeIconHeadingSix` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-six.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingSixProperties> |  |

### 아이콘제목여섯속성

**종류:** 인터페이스

```typescript
export interface IconHeadingSixProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### 포지아이콘제목세

**종류:** 기능

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement
```

`ForgeIconHeadingThree` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-three.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingThreeProperties> |  |

### 아이콘제목세가지속성

**종류:** 인터페이스

```typescript
export interface IconHeadingThreeProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### 포지아이콘제목두

**종류:** 기능

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement
```

`ForgeIconHeadingTwo` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading-two.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingTwoProperties> |  |

### 아이콘제목두속성

**종류:** 인터페이스

```typescript
export interface IconHeadingTwoProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### Forge아이콘제목

**종류:** 기능

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement
```

`ForgeIconHeading` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-heading.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconHeadingProperties> |  |

### 아이콘제목속성

**종류:** 인터페이스

```typescript
export interface IconHeadingProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### Forge아이콘기울임체

**종류:** 기능

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement
```

`ForgeIconItalic` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-italic.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconItalicProperties> |  |

### 아이콘기울임꼴속성

**종류:** 인터페이스

```typescript
export interface IconItalicProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### 위조아이콘번호 매기기목록

**종류:** 기능

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement
```

`ForgeIconNumberedList` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-numbered-list.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconNumberedListProperties> |  |

### 아이콘번호가 매겨진 목록속성

**종류:** 인터페이스

```typescript
export interface IconNumberedListProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### Forge아이콘취소선

**종류:** 기능

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement
```

`ForgeIconStrikethrough` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-strikethrough.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconStrikethroughProperties> |  |

### 아이콘취소선속성

**종류:** 인터페이스

```typescript
export interface IconStrikethroughProperties
```

설명이 제공되지 않았습니다.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### Forge아이콘밑줄

**종류:** 기능

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement
```

`ForgeIconUnderline` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-underline.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconUnderlineProperties> |  |

### 아이콘밑줄속성

**종류:** 인터페이스

```typescript
export interface IconUnderlineProperties
```

설명이 제공되지 않았습니다.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### Forge아이콘달력

**종류:** 기능

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement
```

`ForgeIconCalendar` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-calendar.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconCalendarProperties> |  |

### 아이콘캘린더속성

**종류:** 인터페이스

```typescript
export interface IconCalendarProperties
```

설명이 제공되지 않았습니다.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### 포지아이콘시계

**종류:** 기능

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement
```

`ForgeIconClock` — 중립 JSX 방언으로 한 번 작성된 아이콘입니다.

vite-plugin-forge를 통해 Vue 3 및 React 모두로 깔끔하게 컴파일됩니다.
2단계 컴파일러. 아이콘은 자체 `@layer mp.icons` CSS를 다음을 통해 제공합니다.
같은 위치에 있는 CSS 모듈 `forge-icon-clock.module.scss`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconClockProperties> |  |

### 아이콘시계속성

**종류:** 인터페이스

```typescript
export interface IconClockProperties
```

설명이 제공되지 않았습니다.

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**종류:** 상수

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[]
```

설명이 제공되지 않았습니다.

### 유효성 검사구성

**종류:** 기능

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void
```

설명이 제공되지 않았습니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 작곡 | 읽기 전용 아이콘SymbolDefinition[] |  |
| 사용 가능한 ID | 읽기전용Set<string> |  |

## `src/sprite/country-flags`

### 지원_국가_코드

**종류:** 상수

```typescript
export const SUPPORTED_COUNTRY_CODES
```

게시된 스프라이트에 표준 플래그 기호가 있는 국가 코드입니다.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**종류:** 상수

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS
```

설명이 제공되지 않았습니다.

### ICON_SYMBOL_DEFINITIONS

**종류:** 상수

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[]
```

설명이 제공되지 않았습니다.

## `src/sprite/icon-use`

### Forge아이콘사용

**종류:** 기능

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement
```

표준 스프라이트 기하학을 참조하는 공유 액세스 가능한 래퍼입니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconUseProperties> |  |

### 아이콘사용속성

**종류:** 인터페이스

```typescript
export interface IconUseProperties
```

설명이 제공되지 않았습니다.

## `src/sprite/provider`

### 아이콘스프라이트컨텍스트

**종류:** 상수

```typescript
export const IconSpriteContext
```

설명이 제공되지 않았습니다.

### 아이콘스프라이트공급자

**종류:** 기능

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement
```

아이콘 구성 요소의 하위 트리에 대해 하나의 인라인 스프라이트 호스트를 마운트합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 속성 | 읽기 전용<IconSpriteProperties> |  |

### useIconHref

**종류:** 기능

```typescript
function useIconHref(symbolId: string): string
```

아이콘 래퍼에 대한 로컬 또는 외부 기호 URL을 확인합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 기호 ID | 문자열 |  |

## `src/sprite/serialize`

### 직렬화노드

**종류:** 기능

```typescript
function serializeNode(node: IconSvgNode): string
```

설명이 제공되지 않았습니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| node | IconSvgNode |  |

### 직렬화Sprite

**종류:** 기능

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string
```

설명이 제공되지 않았습니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 정의 | 읽기 전용 아이콘SymbolDefinition[] |  |

### 직렬화기호

**종류:** 기능

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string
```

설명이 제공되지 않았습니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 정의 | 아이콘심볼정의 |  |

## `src/sprite/types`

### IconSprite컨텍스트값

**종류:** 인터페이스

```typescript
export interface IconSpriteContextValue
```

설명이 제공되지 않았습니다.

### 아이콘스프라이트속성

**종류:** 인터페이스

```typescript
export interface IconSpriteProperties
```

설명이 제공되지 않았습니다.

### 아이콘SvgNode

**종류:** 인터페이스

```typescript
export interface IconSvgNode
```

설명이 제공되지 않았습니다.

### 아이콘기호정의

**종류:** 인터페이스

```typescript
export interface IconSymbolDefinition
```

설명이 제공되지 않았습니다.

### 아이콘기호사용

**종류:** 인터페이스

```typescript
export interface IconSymbolUse
```

설명이 제공되지 않았습니다.
