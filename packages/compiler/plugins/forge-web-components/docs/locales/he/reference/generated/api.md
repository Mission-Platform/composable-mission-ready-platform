# הפניה ל-@mission-platform/forge-plugin-web-components API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md: [packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### syntheseElementClass

פונקציה **סוג:**

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string;
```

הדפס את תת-המחלקה `ForgeElement` והרישום שלה לתוכנית מופחתת.

#### פרמטרים

| שם     | הקלד                       | תיאור |
| ------ | -------------------------- | ----- |
| תוכנית | WebComponentsLoweredModule |       |

## `src/emitters/module`

### emitWebComponentModule

פונקציה **סוג:**

```typescript
function emitWebComponentModule(
  module: SemanticModule,
  componentName: string = "CustomElement",
  componentFolders: ReadonlySet<string> = new Set(),
  lowered?: WebComponentsLoweredModule,
): EmittedWebComponentModule;
```

הפוך את כל המודול למקור היעד המקורי של Web-Components.

#### פרמטרים

| שם               | הקלד                       | תיאור |
| ---------------- | -------------------------- | ----- |
| מודול            | SemanticModule             |       |
| שם רכיב          | מחרוזת                     |       |
| componentFolders | ReadonlySet<string>        |       |
| הוריד            | WebComponentsLoweredModule |       |

## `src/index`

### forgeWebComponentsFramework

פונקציה **סוג:**

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin;
```

צור את הפלט TypeScript בלבד של רכיבי אינטרנט.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**סוג:** קבוע

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy;
```

פנימיים מוגנים על ידי זמן הריצה; שיוך טופס נשאר בהצטרפות.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**סוג:** קבוע

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy;
```

ברירת המחדל של תאימות נשמרת עבור רכיבים שנוצרו.

### inferWebComponentsHost

פונקציה **סוג:**

```typescript
function inferWebComponentsHost(
  returnNode: GenericRenderNode | undefined,
): WebComponentsHostPlan;
```

הסק מארח מובנה מותאם אישית רק משורש פנימי סטטי בודד.

רשימת ההיתרים היא שמרנית בכוונה: תג מהותי תקף
HTML אינו מבטיח שהוא בטוח או שימושי מובנה מותאם אישית
בסיס על פני יישומי DOM הנתמכים.

#### פרמטרים

| שם         | הקלד                          | תיאור |
| ---------- | ----------------------------- | ----- |
| returnNode | GenericRenderNode \| לא מוגדר |       |

### isWebComponentsLowered

פונקציה **סוג:**

```typescript
function isWebComponentsLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is WebComponentsLoweredModule;
```

צמצם תוכנית יעד לתוכנית Web-Components ללא ליהוק.

#### פרמטרים

| שם    | הקלד                            | תיאור |
| ----- | ------------------------------- | ----- |
| הוריד | TargetLoweredModule \| לא מוגדר |       |

### lowerWebComponentsModule

פונקציה **סוג:**

```typescript
function lowerWebComponentsModule(
  module: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

הורד IR ניטרלי לתוך כוונות היעד של Web-Components.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| מודול | SemanticModule |       |
| הקשר  | היעד הקשר      |       |

### lowerWebComponentsPlan

פונקציה **סוג:**

```typescript
function lowerWebComponentsPlan(
  module: SemanticModule,
  context: TargetContext,
): WebComponentsLoweredModule;
```

בנו את תוכנית היעד של Web-Components עבור מודול ניטרלי.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| מודול | SemanticModule |       |
| הקשר  | היעד הקשר      |       |

### UNKNOWN_TYPE

**סוג:** קבוע

```typescript
export const UNKNOWN_TYPE;
```

הסוג הנפלט כאשר אין סוג מוצהר או משוער זמין.

### WEB_COMPONENTS_FRAMEWORK

**סוג:** קבוע

```typescript
export const WEB_COMPONENTS_FRAMEWORK;
```

מזהה המסגרת של התוסף, והמפלה של התוכנית הנמוכה שלו.

### WEBCOMPONENTS_NATIVE_HOSTS

**סוג:** קבוע

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS;
```

טבלת התאימות לשורשים שניתן להתאים באופן בטוח מובנה.

### WebComponentsCleanupField

**סוג:** ממשק

```typescript
export interface WebComponentsCleanupField
```

שדה פרטי שומר על פונקציית הניקוי של האפקט בין התקשרויות חוזרות למחזור החיים.

### WebComponentsDivedBody

**סוג:** סוג

```typescript
export type WebComponentsDerivedBody = |
```

הגוף של getter נגזר, שכבר נכתב מחדש לטווח רכיב-מופע:
מפעל תזכירים תמציתי מחזיר ביטוי יחיד, גוף בלוק מביא
הצהרות משלו (שהמקבל חייב להפעיל ולא להחזיר).

### WebComponentsDivedValue

**סוג:** ממשק

```typescript
export interface WebComponentsDerivedValue
```

ערך ממוזכר, ירד ל-getter שחושב מחדש בקריאה.

### WebComponentsElementRef

**סוג:** ממשק

```typescript
export interface WebComponentsElementRef
```

כריכת `useRef`, מורידה לתא `{ current }` המוחזק על ידי האלמנט.

### WebComponentsGeneratedId

**סוג:** ממשק

```typescript
export interface WebComponentsGeneratedId
```

כריכת `useId()` נייטרלית, יורדת לשדה מופע שהוזן פעם אחת לכל אלמנט.

### WebComponentsHost FallbackReason

**סוג:** סוג

```typescript
export type WebComponentsHostFallbackReason =
  | "missing-root"
  | "fragment-root"
  | "dynamic-root"
  | "component-root"
  | "ambiguous-root"
  | "invalid-root"
  | "unsupported-root";
```

סיבות יציבות לכך שרכיב אינו יכול להשתמש במארח מובנה מותאם אישית.

### WebComponentsHostKind

**סוג:** סוג

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

טופס המארח המשמש אלמנט מותאם אישית שנוצר.

### WebComponentsHostPlan

**סוג:** ממשק

```typescript
export interface WebComponentsHostPlan
```

מטא-נתונים של בחירת מארח והזעקה עבור רכיב שנוצר.

### WebComponentsInternalsPolicy

**סוג:** ממשק

```typescript
export interface WebComponentsInternalsPolicy
```

יכולות ElementInternals מבוקשות על ידי רכיב שנוצר.

### WebComponentsLifecycle Callback

**סוג:** סוג

```typescript
export type WebComponentsLifecycleCallback =
  "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

החזרות מחזור חיים של רכיבים מותאמים אישית שתוכנית יכולה ליצור.

### WebComponentsLifecycleHook

**סוג:** ממשק

```typescript
export interface WebComponentsLifecycleHook
```

אחד יצר התקשרות חוזרת במחזור החיים וההצהרות שהוא מריץ.

### WebComponentsListKey

**סוג:** ממשק

```typescript
export interface WebComponentsListKey
```

מועמד מפתח לעיבוד רשימה נשמר עבור פלט הרשימה של היעד.

### WebComponentsLoweredModule

**סוג:** ממשק

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

תוכנית היעד של Web-Components שהופקה על ידי `lower` ושוכללה על ידי `optimize`.

### WebComponentsPromotedLocal

**סוג:** ממשק

```typescript
export interface WebComponentsPromotedLocal
```

קבוע עיבוד ראש **מקודם** לחבר אלמנט.

משגר תזכירים, התקשרות חוזרת למחזור החיים ומאתחל שדה כולם נפלטים
_מחוץ_ `render()`, אז מקומי שראש העיבוד מצהיר אינו קיים עבור
אותם. קידום ההצהרה לחבר הוא מה שגורם לקריאה כזו
ניתן לפתרון - דרך `this.<name>` - במקום להשתלשל.

רק הכרזה ללא השפעה מוכחת מקודמת (ראה
{@link promotedHeadLocals}); כל דבר אחר נשאר ב-`render()`.

### WebComponentsPropertyDeclaration

**סוג:** ממשק

```typescript
export interface WebComponentsPropertyDeclaration
```

מתאר נכס תגובתי `ForgeElement`, המשקף את החוזה של זמן הריצה המקורי.

### WebComponentsReactiveProperty

**סוג:** ממשק

```typescript
export interface WebComponentsReactiveProperty
```

**מאפיין** תגובתי: קלט הניתן להגדרה חיצונית שיקוף מתכונה.

### WebComponentsRegistrationOptions

**סוג:** ממשק

```typescript
export interface WebComponentsRegistrationOptions
```

אפשרויות `customElements.define` הנפלטות עבור מובנה מותאם אישית.

### WebComponentsRuntimeIports

**סוג:** ממשק

```typescript
export interface WebComponentsRuntimeImports
```

היבוא צריך מודול אלמנט שנוצר.

### WebComponentsSetupPhase

**סוג:** ממשק

```typescript
export interface WebComponentsSetupPhase
```

שלב **ההתקנה** החד פעמי של האלמנט.

זרע כגון `useState(parseTime(modelValue))` קורא תכונה תגובתית, ו
נכס מחזיק ערך רק לאחר אימוץ התכונות של המארח -
מה שקורה בחיבור, הרבה אחרי שהקונסטרוקטור רץ. גרוע מכך, ה
בדרך כלל מגיעים לערך באמצעות קבוע render-head (`const initial =
parseTime(modelValue);`) שלא ניתן היה להוכיח שהוא נטול אפקטים ולכן
נשאר ב-`render()`, כך שזרע בנאי יפנה לשם שכן
לא קיים שם בכלל.

זרע כזה נדחה ל-`ForgeElement.setup()`, שזמן הריצה קורא לו
לאחר אימוץ תכונות ולפני העיבוד הראשון, פעם אחת בדיוק לכל אלמנט

- חיבור מחדש לא חייב לחדש ולבטל את מה שהמשתמש שינה מאז.
  הצהרות הראש שהזרע צריך משוחזרות שם תחילה, לפי סדר ראש ו
  סגור באופן טרנזיטיבי, ו**רק** אלה.

הפעלה חוזרת במקום שמירה במטמון היא מכוונת. במקור הנייטרלי הראש
_הוא_ גוף הרכיב, אז `const initial = parseTime(modelValue);` כבר
פועל על כל עיבוד; הערכתו פעם נוספת במהלך ההגדרה אינה מבצעת שיחה
הרכיב המחבר אינו פועל כבר בכל מעבר. מאחסן אותו ב-a
במקום זאת _מקטין_ את מספר ההערכות ונותן את הערך an
זהות שהמקור מעולם לא הבטיח. רק `const` רגיל (או פונקציה)
ההצהרה מושמעת אי פעם מחדש - ראה {@link headReplay}.

### WebComponentsShadowPolicy

**סוג:** ממשק

```typescript
export interface WebComponentsShadowPolicy
```

מדיניות צל-שורש מוקלדת נשמרת על ידי תוכנית היעד.

### WebComponentsStateField

**סוג:** ממשק

```typescript
export interface WebComponentsStateField
```

שדה **מצב** תגובתי: תא פנימי שהמגדיר שלו מעבד מחדש את האלמנט.

### WebComponentsStaticTemplatePart

**סוג:** ממשק

```typescript
export interface WebComponentsStaticTemplatePart
```

נתח תבנית סטטי לחלוטין מונף לקבוע ברמת המודול.

### WebComponentsTemplatePlan

**סוג:** ממשק

```typescript
export interface WebComponentsTemplatePlan
```

תוכנית `render()` הנמוכה: הצהרות הראש שלה ותבנית ה-lit-html שלה.

## `src/optimize`

### optimizeWebComponentsModule

פונקציה **סוג:**

```typescript
function optimizeWebComponentsModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

בצע אופטימיזציה של כוונות היעד של רכיבי האינטרנט; מטרות אחרות עוברות.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| כוונות   | כוונות יעד            |       |
| אפשרויות | TargetOptimizeOptions |       |

### optimizeWebComponentsPlan

פונקציה **סוג:**

```typescript
function optimizeWebComponentsPlan(
  plan: WebComponentsLoweredModule,
  options: TargetOptimizeOptions,
): WebComponentsLoweredModule;
```

צמצם תוכנית רכיבי אינטרנט מופחתת, ותעד כל מעבר שרץ.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| תוכנית   | WebComponentsLoweredModule |       |
| אפשרויות | TargetOptimizeOptions      |       |

### WEB_COMPONENTS_OPTIMIZATIONS

**סוג:** קבוע

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS;
```

מזהים שתועדו ב-{@link WebComponentsLoweredModule.appliedOptimizations}.

### אופטימיזציה של רכיבי אינטרנט

**סוג:** סוג

```typescript
export type WebComponentsOptimization =
  (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

מזהה אופטימיזציה של Web-Components מוקלט.

## `src/runtime/hook-module`

### emitWebComponentHookModule

פונקציה **סוג:**

```typescript
function emitWebComponentHookModule(module: SemanticModule): string;
```

הפוך מודול וו ניטרלי למקור רכיבי האינטרנט שלו.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| מודול | SemanticModule |       |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**סוג:** קבוע

```typescript
export const HAS_SLOT_RUNTIME;
```

עוזר זמן הריצה המקורי שהסמן מוריד אליו, נקרא עם אלמנט המארח.

### isFunctionExpressionText

פונקציה **סוג:**

```typescript
function isFunctionExpressionText(text: string): boolean;
```

בין אם הטקסט הוא ביטוי **בעל ערך פונקציה** - חץ או א
ביטוי `function`.

ליצירת סגירה אין השפעה משל עצמה, אז קבוע כזה של render-head
ניתן לקדם לאיבר אלמנט (ראה `../lower`) אולם גופו לא טהור
הוא: הגוף עדיין פועל בדיוק כאשר הוא נקרא.

#### פרמטרים

| שם   | הקלד   | תיאור |
| ---- | ------ | ----- |
| טקסט | מחרוזת |       |

### isPureExpressionText

פונקציה **סוג:**

```typescript
function isPureExpressionText(text: string): boolean;
```

האם ביטוי הוא **בהוכחה** נקי מאפקטים: ללא שיחה, ללא `new`, לא
`await`, ללא הקצאה וללא תוספת, בכל עומק.

משמש כדי להחליט אם קבוע render-head עשוי להיות מקודם למשבר.
המבחן הוא תחבירי ופסימי במכוון - קריאה לעזר טהור
לא ניתן להבחין מקריאה למוטטור, כך שכל קריאה פוסלת את
הצהרה והיא נשארת ב-`render()`.

#### פרמטרים

| שם   | הקלד   | תיאור |
| ---- | ------ | ----- |
| טקסט | מחרוזת |       |

## `src/transformers/props-binding`

### מובילObjectPattern

פונקציה **סוג:**

```typescript
function leadingObjectPattern(text: string): string | undefined;
```

דפוס האובייקט ש*מתחיל* `text`, מבטל את כל מה שאחריו.

טקסט המקור של פרמטר נושא את ההערה שלו (`{ a, b }: Readonly<P>`),
אז יש לחתוך את התבנית לפני שניתן לפרק אותה.

#### פרמטרים

| שם   | הקלד   | תיאור |
| ---- | ------ | ----- |
| טקסט | מחרוזת |       |

### parsePropsBinding

פונקציה **סוג:**

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined;
```

לפרק תבנית אובייקט אבזרים.

מחזירה `undefined` כאשר הטקסט אינו תבנית אובייקט, או מכיל
ערך המודול הזה אינו דגם (מפתח מחושב, נניח) - לאחר מכן המתקשר שומר
התנהגותו הקודמת במקום לפעול בקריאה חלקית.

#### פרמטרים

| שם         | הקלד   | תיאור |
| ---------- | ------ | ----- |
| תבנית טקסט | מחרוזת |       |

### PropsBinding

**סוג:** ממשק

```typescript
export interface PropsBinding
```

תבנית אובייקט אביזרים, מפורקת לאברים שהוא קורא ולמקומיים שהוא קושר.

### PropsBindingEntry

**סוג:** ממשק

```typescript
export interface PropsBindingEntry
```

כניסה אחת של תבנית אובייקט אבזרים.

### propsBindingStatement

פונקציה **סוג:**

```typescript
function propsBindingStatement(
  binding: PropsBinding,
  isRead: (name: string) => boolean,
): string | undefined;
```

הפעל מחדש דפוס אביזרים כמו `const { … } = this;`, מוגבל לערכים
את המקומיים שלהם היקף היעד בעצם קורא.

הגבלתו שומרת על היקף מורם ללא כריכות שהוא לעולם לא משתמש בהן (אשר א
`noUnusedLocals` build ידחה) תוך השארת כל ערך שנשמר - ו
אז ברירת המחדל שלו - זהה בתים לזה שנכתב.

#### פרמטרים

| שם     | הקלד                    | תיאור |
| ------ | ----------------------- | ----- |
| מחייב  | PropsBinding            |       |
| isRead | (שם: מחרוזת) => בוליאני |       |

## `src/transformers/props-type`

### indexedAccessType

פונקציה **סוג:**

```typescript
function indexedAccessType(
  reference: PropsTypeReference,
  member: string,
): string;
```

ההערה עם גישה לאינדקס שחבר מסוג האביזרים נפלט בתור.

האופציונליות של החבר עצמו נשמרת בדיוק: חבר נדרש _לא_
התרחב עם `| undefined`. השדה של האלמנט מאוכלס רק פעם אחת
זמן הריצה מאמץ את התכונה שלו, כך שהוא באמת לא מוגדר בקונסטרוקטור -
אבל זה מתבטא על ידי פליטת השדה כ-`declare` (זמן הריצה הוא הבעלים של
accessor), אשר פוטר אותו מ-`strictPropertyInitialization` מבלי לעשות
כל קריאה של אביזר נדרש - `undefined`.

#### פרמטרים

| שם    | הקלד               | תיאור |
| ----- | ------------------ | ----- |
| הפניה | PropsTypeReference |       |
| חבר   | מחרוזת             |       |

### PropsTypeReference

**סוג:** ממשק

```typescript
export interface PropsTypeReference
```

ניתן להוסיף הערות נגד השדות של האביזרים לרכיב.

### resolvePropsTypeReference

פונקציה **סוג:**

```typescript
function resolvePropsTypeReference(
  annotation: string | undefined,
  declarations: readonly GenericStatement[],
): PropsTypeReference | undefined;
```

פתרו את הערת האביזרים כנגד ההצהרות השמורות של המודול.

מחזירה `undefined` כאשר ההערה אינה שם פשוט, או כאשר לא
כינוי ממשק / סוג של שם זה נשמר לצד מחלקת האלמנט -
בכל מקרה ההפניה לא תפתר במודול שנוצר.

#### פרמטרים

| שם     | הקלד                          | תיאור |
| ------ | ----------------------------- | ----- |
| ביאור  | מחרוזת \| לא מוגדר            |       |
| הצהרות | קריאה בלבד GenericStatement[] |       |

### typeMembers

פונקציה **סוג:**

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>;
```

האיברים שגוף ממשק/כינוי מסוג שמור מצהיר, ממופה ל-אם
כל אחד מהם הוא אופציונלי (`name?: T`).

רק חברים ברמה העליונה של הגוף הראשון `{…}` נאספים, ורק
היכן שחבר רשאי להתחיל באופן לגיטימי (מיד אחרי סד הפתיחה, `;`
או `,`). לכן חברים שנתרמו על ידי בסיס `extends`/`&` _לא_
דיווח - המתקשר מתייחס אליהם כאל לא ידוע ונופל אחורה, וזהו
כיוון בטוח.

#### פרמטרים

| שם              | הקלד   | תיאור |
| --------------- | ------ | ----- |
| DeclarationText | מחרוזת |       |

### unwrapPropsTypeName

פונקציה **סוג:**

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined;
```

צמצם את הערת האביזרים לשם הפשוט שאליו הוא מפנה, פתח את העטיפה
סוגריים ועטיפות גנריות המשמרות חברים שרכיב עשוי לכתוב.

#### פרמטרים

| שם   | הקלד               | תיאור |
| ---- | ------------------ | ----- |
| טקסט | מחרוזת \| לא מוגדר |       |
