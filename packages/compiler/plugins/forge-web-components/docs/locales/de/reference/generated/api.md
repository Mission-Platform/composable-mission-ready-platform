# @mission-platform/forge-plugin-web-components API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md: [packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### synthesizeElementClass

**Art:** Funktion

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string;
```

Drucken Sie die Unterklasse `ForgeElement` und ihre Registrierung für einen abgesenkten Plan.

#### Parameter

| Name   | Geben Sie                  | ein Beschreibung |
| ------ | -------------------------- | ---------------- |
| planen | WebComponentsLoweredModule |                  |

## `src/emitters/module`

### emitWebComponentModule

**Art:** Funktion

```typescript
function emitWebComponentModule(
  module: SemanticModule,
  componentName: string = "CustomElement",
  componentFolders: ReadonlySet<string> = new Set(),
  lowered?: WebComponentsLoweredModule,
): EmittedWebComponentModule;
```

Transformieren Sie das gesamte Modul in die native Web-Components-Zielquelle.

#### Parameter

| Name              | Geben Sie                  | ein Beschreibung |
| ----------------- | -------------------------- | ---------------- |
| Modul             | SemanticModule             |                  |
| Komponentenname   | Zeichenfolge               |                  |
| Komponentenordner | ReadonlySet<string>        |                  |
| abgesenkt         | WebComponentsLoweredModule |                  |

## `src/index`

### forgeWebComponentsFramework

**Art:** Funktion

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin;
```

Erstellen Sie das reine TypeScript-Webkomponenten-Ausgabe-Plugin.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**Art:** konstant

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy;
```

Interna werden durch die Laufzeit funktionsgesteuert; Form Association bleibt Opt-in.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**Art:** konstant

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy;
```

Kompatibilitätsstandardwerte werden für generierte Komponenten beibehalten.

### inferWebComponentsHost

**Art:** Funktion

```typescript
function inferWebComponentsHost(
  returnNode: GenericRenderNode | undefined,
): WebComponentsHostPlan;
```

Leiten Sie einen benutzerdefinierten integrierten Host nur von einem einzigen, statischen intrinsischen Root ab.

Die Zulassungsliste ist absichtlich konservativ: Ein intrinsisches Tag ist gültig
HTML garantiert nicht, dass es sich um eine sichere oder nützliche benutzerdefinierte Integration handelt
Basis für alle unterstützten DOM-Implementierungen.

#### Parameter

| Name       | Geben Sie                        | ein Beschreibung |
| ---------- | -------------------------------- | ---------------- |
| returnNode | GenericRenderNode \| undefiniert |                  |

### isWebComponentsLowered

**Art:** Funktion

```typescript
function isWebComponentsLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is WebComponentsLoweredModule;
```

Grenzen Sie einen Zielplan ohne Casting auf den Webkomponenten-Plan ein.

#### Parameter

| Name      | Geben Sie                          | ein Beschreibung |
| --------- | ---------------------------------- | ---------------- |
| abgesenkt | TargetLoweredModule \| undefiniert |                  |

### LowerWebComponentsModule

**Art:** Funktion

```typescript
function lowerWebComponentsModule(
  module: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

Senken Sie die neutrale IR in die Zielabsichten der Web-Komponenten.

#### Parameter

| Name    | Geben Sie      | ein Beschreibung |
| ------- | -------------- | ---------------- |
| Modul   | SemanticModule |                  |
| Kontext | Zielkontext    |                  |

### LowerWebComponentsPlan

**Art:** Funktion

```typescript
function lowerWebComponentsPlan(
  module: SemanticModule,
  context: TargetContext,
): WebComponentsLoweredModule;
```

Erstellen Sie den Web-Components-Zielplan für ein neutrales Modul.

#### Parameter

| Name    | Geben Sie      | ein Beschreibung |
| ------- | -------------- | ---------------- |
| Modul   | SemanticModule |                  |
| Kontext | Zielkontext    |                  |

### UNBEKANNTER_TYP

**Art:** konstant

```typescript
export const UNKNOWN_TYPE;
```

Der Typ, der ausgegeben wird, wenn weder ein deklarierter noch ein abgeleiteter Typ verfügbar ist.

### WEB_COMPONENTS_FRAMEWORK

**Art:** konstant

```typescript
export const WEB_COMPONENTS_FRAMEWORK;
```

Die Framework-ID des Plugins und der Diskriminator seines niedrigeren Plans.

### WEBCOMPONENTS_NATIVE_HOSTS

**Art:** konstant

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS;
```

Die Kompatibilitätstabelle für Roots, die problemlos integriert werden können.

### WebComponentsCleanupField

**Art:** Schnittstelle

```typescript
export interface WebComponentsCleanupField
```

Ein privates Feld, das die Bereinigungsfunktion eines Effekts zwischen Lebenszyklusrückrufen beibehält.

### WebComponentsDerivedBody

**Art:** Typ

```typescript
export type WebComponentsDerivedBody = |
```

Der Hauptteil eines abgeleiteten Getters, bereits in den Element-Instanz-Bereich umgeschrieben:
Eine prägnante Memo-Fabrik gibt einen einzelnen Ausdruck zurück, eine mit Blockkörper bringt einen
seine eigenen Anweisungen (die der Getter ausführen und nicht zurückgeben muss).

### WebComponentsDerivedValue

**Art:** Schnittstelle

```typescript
export interface WebComponentsDerivedValue
```

Ein gespeicherter Wert, der auf einen Getter reduziert wird, der beim Lesen neu berechnet wird.

### WebComponentsElementRef

**Art:** Schnittstelle

```typescript
export interface WebComponentsElementRef
```

Eine `useRef`-Bindung, abgesenkt auf eine `{ current }`-Zelle, die vom Element gehalten wird.

### WebComponentsGeneratedId

**Art:** Schnittstelle

```typescript
export interface WebComponentsGeneratedId
```

Eine neutrale `useId()`-Bindung, herabgesetzt auf ein Instanzfeld, das einmal pro Element gesetzt wird.

### WebComponentsHostFallbackReason

**Art:** Typ

```typescript
export type WebComponentsHostFallbackReason =
  | "missing-root"
  | "fragment-root"
  | "dynamic-root"
  | "component-root"
  | "ambiguous-root"
  | "invalid-root"
  | "unsupported-root";
```

Stabile Gründe, warum eine Komponente keinen angepassten integrierten Host verwenden kann.

### WebComponentsHostKind

**Art:** Typ

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

Das von einem generierten benutzerdefinierten Element verwendete Hostformular.

### WebComponentsHostPlan

**Art:** Schnittstelle

```typescript
export interface WebComponentsHostPlan
```

Hostauswahl- und Aufrufmetadaten für eine generierte Komponente.

### WebComponentsInternalsPolicy

**Art:** Schnittstelle

```typescript
export interface WebComponentsInternalsPolicy
```

ElementInternals-Funktionen, die von einer generierten Komponente angefordert werden.

### WebComponentsLifecycleCallback

**Art:** Typ

```typescript
export type WebComponentsLifecycleCallback =
  "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

Die Lebenszyklusrückrufe für benutzerdefinierte Elemente, die ein Plan generieren kann.

### WebComponentsLifecycleHook

**Art:** Schnittstelle

```typescript
export interface WebComponentsLifecycleHook
```

Ein generierter Lebenszyklusrückruf und die von ihm ausgeführten Anweisungen.

### WebComponentsListKey

**Art:** Schnittstelle

```typescript
export interface WebComponentsListKey
```

Ein Listenrendering-Schlüsselkandidat, der für die Listenausgabe des Ziels beibehalten wird.

### WebComponentsLoweredModule

**Art:** Schnittstelle

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

Der von `lower` erstellte und von `optimize` verfeinerte Web-Components-Zielplan.

### WebComponentsPromotedLocal

**Art:** Schnittstelle

```typescript
export interface WebComponentsPromotedLocal
```

Eine Render-Head-Konstante, die zu einem Elementmitglied **heraufgestuft** wurde.

Es werden ein Memo-Getter, ein Lebenszyklus-Callback und ein Feldinitialisierer ausgegeben
_außerhalb_ `render()`, also gibt es für ein vom Render-Head deklariertes Local kein Existieren
sie. Die Werbung für die Erklärung gegenüber einem Mitglied ist das, was eine solche Lektüre ausmacht
auflösbar – durch `this.<name>` – statt baumelnd.

Gefördert wird nur eine nachweislich wirkungsfreie Erklärung (vgl
{@link promoteHeadLocals}); alles andere bleibt in `render()`.

### WebComponentsPropertyDeclaration

**Art:** Schnittstelle

```typescript
export interface WebComponentsPropertyDeclaration
```

Ein `ForgeElement`-Deskriptor für reaktive Eigenschaften, der den Vertrag der nativen Laufzeit widerspiegelt.

### WebComponentsReactiveProperty

**Art:** Schnittstelle

```typescript
export interface WebComponentsReactiveProperty
```

Eine reaktive **Eigenschaft**: eine extern einstellbare Eingabe, die von einem Attribut gespiegelt wird.

### WebComponentsRegistrationOptions

**Art:** Schnittstelle

```typescript
export interface WebComponentsRegistrationOptions
```

Die `customElements.define`-Optionen, die für eine benutzerdefinierte integrierte Version ausgegeben werden.

### WebComponentsRuntimeImports

**Art:** Schnittstelle

```typescript
export interface WebComponentsRuntimeImports
```

Das importiert ein generiertes Elementmodul, das benötigt wird.

### WebComponentsSetupPhase

**Art:** Schnittstelle

```typescript
export interface WebComponentsSetupPhase
```

Die einmalige **Einrichtung**-Phase des Elements.

Ein Seed wie `useState(parseTime(modelValue))` liest eine reaktive Eigenschaft und
Eine Eigenschaft enthält erst dann einen Wert, wenn die Attribute des Hosts übernommen wurden.
Dies geschieht bei der Verbindung, lange nachdem der Konstruktor ausgeführt wurde. Schlimmer noch, die
Der Wert wird normalerweise über eine Render-Head-Konstante erreicht („const initial =
parseTime(modelValue);`), dessen Wirkungsfreiheit nicht nachgewiesen werden konnte und daher
bleibt in `render()`, sodass ein Konstruktor-Seed auf einen Namen verweisen würde, der dies tut
gibt es dort überhaupt nicht.

Ein solcher Startwert wird auf `ForgeElement.setup()` verschoben, das von der Laufzeit aufgerufen wird
nach der Attributübernahme und vor dem ersten Rendern genau einmal pro Element
– Eine erneute Verbindung darf nicht erneut erfolgen und verwerfen, was der Benutzer seitdem geändert hat.
Die Kopfaussagen, die der Seed benötigt, werden dort zuerst in der Kopfreihenfolge und wiederholt
transitiv geschlossen, und **nur** diese.

Das Wiedergeben statt Zwischenspeichern ist bewusst. In der neutralen Quelle der Kopf
_ist_ der Komponentenkörper, also `const initial = parseTime(modelValue);` bereits
läuft bei jedem Rendern; Eine erneute Auswertung während des Setups führt zu keinem Aufruf
Die erstellte Komponente führt nicht bereits bei jedem Durchgang eine Leistung aus. Zwischenspeichern in einem
Das Feld würde stattdessen die Anzahl der Bewertungen _reduzieren_ und den Wert an angeben
Identität, die die Quelle nie versprochen hat. Nur ein einfaches `const` (oder eine Funktion)
Die Deklaration wird jemals wiederholt – siehe {@link headReplay}.

### WebComponentsShadowPolicy

**Art:** Schnittstelle

```typescript
export interface WebComponentsShadowPolicy
```

Typisierte Shadow-Root-Richtlinie, die vom Zielplan beibehalten wird.

### WebComponentsStateField

**Art:** Schnittstelle

```typescript
export interface WebComponentsStateField
```

Ein reaktives **Zustandsfeld**: eine interne Zelle, deren Setter das Element neu rendert.

### WebComponentsStaticTemplatePart

**Art:** Schnittstelle

```typescript
export interface WebComponentsStaticTemplatePart
```

Ein vollständig statischer Vorlagenblock, der auf eine Konstante auf Modulebene gehoben wird.

### WebComponentsTemplatePlan

**Art:** Schnittstelle

```typescript
export interface WebComponentsTemplatePlan
```

Der abgesenkte `render()`-Plan: seine Hauptaussagen und seine Lit-HTML-Vorlage.

## `src/optimize`

### optimierenWebComponentsModule

**Art:** Funktion

```typescript
function optimizeWebComponentsModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

Optimieren Sie die Zielabsichten der Web-Komponenten. andere Ziele passieren.

#### Parameter

| Name      | Geben Sie             | ein Beschreibung |
| --------- | --------------------- | ---------------- |
| Absichten | Zielabsichten         |                  |
| Optionen  | TargetOptimizeOptions |                  |

### optimiereWebComponentsPlan

**Art:** Funktion

```typescript
function optimizeWebComponentsPlan(
  plan: WebComponentsLoweredModule,
  options: TargetOptimizeOptions,
): WebComponentsLoweredModule;
```

Verfeinern Sie einen reduzierten Web-Komponenten-Plan und zeichnen Sie jeden ausgeführten Durchgang auf.

#### Parameter

| Name     | Geben Sie                  | ein Beschreibung |
| -------- | -------------------------- | ---------------- |
| planen   | WebComponentsLoweredModule |                  |
| Optionen | TargetOptimizeOptions      |                  |

### WEB_COMPONENTS_OPTIMIZATIONS

**Art:** konstant

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS;
```

In {@link WebComponentsLoweredModule.appliedOptimizations} aufgezeichnete Bezeichner.

### WebComponentsOptimization

**Art:** Typ

```typescript
export type WebComponentsOptimization =
  (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

Eine aufgezeichnete Web-Components-Optimierungskennung.

## `src/runtime/hook-module`

### emitWebComponentHookModule

**Art:** Funktion

```typescript
function emitWebComponentHookModule(module: SemanticModule): string;
```

Transformieren Sie ein neutrales Hook-Modul in seine Web-Components-Quelle.

#### Parameter

| Name  | Geben Sie      | ein Beschreibung |
| ----- | -------------- | ---------------- |
| Modul | SemanticModule |                  |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**Art:** konstant

```typescript
export const HAS_SLOT_RUNTIME;
```

Der native Laufzeithelfer, auf den der Marker abgesetzt wird und der mit dem Host-Element aufgerufen wird.

### isFunctionExpressionText

**Art:** Funktion

```typescript
function isFunctionExpressionText(text: string): boolean;
```

Ob der Text ein **funktionswertiger** Ausdruck ist – ein Pfeil oder ein
`function`-Ausdruck.

Das Erstellen eines Abschlusses hat keine eigene Auswirkung, daher ist eine Render-Head-Konstante erforderlich
kann zu einem Elementmitglied hochgestuft werden (siehe `../lower`), auch wenn sein Körper unrein ist
ist: Der Körper läuft immer noch genau dann, wenn er aufgerufen wird.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Text | Zeichenfolge |                  |

### isPureExpressionText

**Art:** Funktion

```typescript
function isPureExpressionText(text: string): boolean;
```

Ob ein Ausdruck **nachweislich** frei von Effekten ist: kein Aufruf, kein `new`, nein
`await`, keine Zuweisung und kein Inkrement, in beliebiger Tiefe.

Wird verwendet, um zu entscheiden, ob eine Render-Head-Konstante zu einem Getter hochgestuft werden darf.
Der Test ist bewusst syntaktisch und pessimistisch – ein Aufruf an einen reinen Helfer
ist nicht von einem Aufruf an einen Mutator zu unterscheiden, daher disqualifiziert jeder Aufruf den
Anweisung und sie bleibt in `render()`.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Text | Zeichenfolge |                  |

## `src/transformers/props-binding`

### LeadingObjectPattern

**Art:** Funktion

```typescript
function leadingObjectPattern(text: string): string | undefined;
```

Das Objektmuster, das `text` _startet_ und alles Folgende verwirft.

Der Quelltext eines Parameters trägt seine Anmerkung (`{ a, b }: Readonly<P>`).
Daher muss das Muster ausgeschnitten werden, bevor es zerlegt werden kann.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Text | Zeichenfolge |                  |

### parsePropsBinding

**Art:** Funktion

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined;
```

Zerlegen Sie ein Requisitenobjektmuster.

Gibt `undefined` zurück, wenn der Text kein Objektmuster ist oder ein enthält
Dieses Modul modelliert keinen Eintrag (z. B. einen berechneten Schlüssel) – der Aufrufer behält ihn dann
sein bisheriges Verhalten, anstatt auf eine Teillesung zu reagieren.

#### Parameter

| Name       | Geben Sie    | ein Beschreibung |
| ---------- | ------------ | ---------------- |
| MusterText | Zeichenfolge |                  |

### PropsBinding

**Art:** Schnittstelle

```typescript
export interface PropsBinding
```

Ein Requisitenobjektmuster, zerlegt in die Mitglieder, die es liest, und die lokalen Elemente, die es bindet.

### PropsBindingEntry

**Art:** Schnittstelle

```typescript
export interface PropsBindingEntry
```

Ein Eintrag eines Requisitenobjektmusters.

### propsBindingStatement

**Art:** Funktion

```typescript
function propsBindingStatement(
  binding: PropsBinding,
  isRead: (name: string) => boolean,
): string | undefined;
```

Wiederholen Sie ein Requisitenmuster als `const { … } = this;`, beschränkt auf die Einträge
deren Locals der Zielbereich tatsächlich liest.

Durch die Einschränkung bleibt ein angehobener Bereich frei von Bindungen, die er nie verwendet (was z
Der `noUnusedLocals`-Build würde abgelehnt) und dabei jeden beibehaltenen Eintrag beibehalten – und
es ist also die Standardeinstellung – Byte-identisch mit der erstellten.

#### Parameter

| Name    | Geben Sie                       | ein Beschreibung |
| ------- | ------------------------------- | ---------------- |
| Bindung | PropsBinding                    |                  |
| isRead  | (Name: Zeichenfolge) => boolean |                  |

## `src/transformers/props-type`

### indexedAccessType

**Art:** Funktion

```typescript
function indexedAccessType(
  reference: PropsTypeReference,
  member: string,
): string;
```

Die Annotation mit indiziertem Zugriff, als die ein Mitglied des Typs props ausgegeben wird.

Die eigene Optionalität des Mitglieds wird genau beibehalten: Ein erforderliches Mitglied ist _nicht_
erweitert um `| undefined`. Das Feld des Elements wird nur einmal ausgefüllt
Die Laufzeit übernimmt ihr Attribut, sodass es im Konstruktor tatsächlich nicht festgelegt ist.
aber das wird ausgedrückt, indem das Feld als `declare` ausgegeben wird (die Laufzeit besitzt das
Accessor), wodurch es von `strictPropertyInitialization` ausgenommen wird, ohne dass es erforderlich ist
Jeder Lesevorgang einer erforderlichen Requisite, möglicherweise `undefined`.

#### Parameter

| Name     | Geben Sie          | ein Beschreibung |
| -------- | ------------------ | ---------------- |
| Referenz | PropsTypeReference |                  |
| Mitglied | Zeichenfolge       |                  |

### PropsTypeReference

**Art:** Schnittstelle

```typescript
export interface PropsTypeReference
```

Der Requisitentyp der Felder einer Komponente kann mit Anmerkungen versehen werden.

### „resolvePropsTypeReference“.

**Art:** Funktion

```typescript
function resolvePropsTypeReference(
  annotation: string | undefined,
  declarations: readonly GenericStatement[],
): PropsTypeReference | undefined;
```

Lösen Sie die Requisitenanmerkung anhand der beibehaltenen Deklarationen des Moduls auf.

Gibt `undefined` zurück, wenn die Anmerkung kein einfacher Name ist oder wenn nein
Schnittstellen-/Typ-Alias dieses Namens wird neben der Elementklasse beibehalten –
In beiden Fällen würde die Referenz im generierten Modul nicht aufgelöst werden.

#### Parameter

| Name        | Geben Sie                   | ein Beschreibung |
| ----------- | --------------------------- | ---------------- |
| Anmerkung   | Zeichenfolge \| undefiniert |                  |
| Erklärungen | readonly GenericStatement[] |                  |

### typeMembers

**Art:** Funktion

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>;
```

Die Mitglieder, die ein beibehaltener Schnittstellen-/Typ-Alias-Körper deklariert und dem ob zugeordnet ist
Jeder ist optional (`name?: T`).

Es werden nur Mitglieder auf der obersten Ebene des ersten `{…}`-Körpers erfasst, und zwar nur
wo ein Mitglied rechtmäßig beginnen darf (direkt nach der öffnenden Klammer, ein `;`
oder ein `,`). Mitglieder, die von einer `extends`/`&`-Basis beigesteuert wurden, sind daher _nicht_
gemeldet – der Anrufer behandelt sie als unbekannt und greift zurück, was der Fall ist
sichere Richtung.

#### Parameter

| Name             | Geben Sie    | ein Beschreibung |
| ---------------- | ------------ | ---------------- |
| Deklarationstext | Zeichenfolge |                  |

### unwrapPropsTypeName

**Art:** Funktion

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined;
```

Reduzieren Sie eine Requisitenanmerkung auf den einfachen Namen, auf den sie verweist, und entpacken Sie den
Klammern und mitgliedererhaltende generische Wrapper, die eine Komponente schreiben kann.

#### Parameter

| Name | Geben Sie                   | ein Beschreibung |
| ---- | --------------------------- | ---------------- |
| Text | Zeichenfolge \| undefiniert |                  |
