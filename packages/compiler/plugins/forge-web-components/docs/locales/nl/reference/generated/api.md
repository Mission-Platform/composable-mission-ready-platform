# @mission-platform/forge-plugin-web-components API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md: [packages/compiler/plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### syntheseElementClass

**Soort:** functie

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string;
```

Druk de `ForgeElement`-subklasse en de registratie ervan voor een verlaagd abonnement af.

#### Parameters

| Naam    | Typ                        | Beschrijving |
| ------- | -------------------------- | ------------ |
| plannen | WebComponentsLoweredModule |              |

## `src/emitters/module`

### emitWebComponentModule

**Soort:** functie

```typescript
function emitWebComponentModule(
  module: SemanticModule,
  componentName: string = "CustomElement",
  componentFolders: ReadonlySet<string> = new Set(),
  lowered?: WebComponentsLoweredModule,
): EmittedWebComponentModule;
```

Transformeer de hele module naar de oorspronkelijke doelbron voor webcomponenten.

#### Parameters

| Naam            | Typ                        | Beschrijving |
| --------------- | -------------------------- | ------------ |
| module          | SemantischeModule          |              |
| componentnaam   | tekenreeks                 |              |
| componentMappen | Alleen-lezenSet<string>    |              |
| verlaagd        | WebComponentsLoweredModule |              |

## `src/index`

### forgeWebComponentsFramework

**Soort:** functie

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin;
```

Maak de uitvoerplug-in Web Components die alleen voor TypeScript beschikbaar is.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**Soort:** constant

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy;
```

Internals worden door de runtime beperkt; formuliervereniging blijft opt-in.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**Soort:** constant

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy;
```

Standaardwaarden voor compatibiliteit blijven behouden voor gegenereerde componenten.

### afleidenWebComponentsHost

**Soort:** functie

```typescript
function inferWebComponentsHost(
  returnNode: GenericRenderNode | undefined,
): WebComponentsHostPlan;
```

Leid een op maat gemaakte ingebouwde host alleen af ​​op basis van een enkele, statische intrinsieke root.

De toelatingslijst is opzettelijk conservatief: een intrinsieke tag is geldig
HTML garandeert niet dat het een veilige of nuttige, op maat gemaakte ingebouwde versie is
basis voor de ondersteunde DOM-implementaties.

#### Parameters

| Naam       | Typ                                 | Beschrijving |
| ---------- | ----------------------------------- | ------------ |
| returnNode | GenericRenderNode \| ongedefinieerd |              |

### isWebComponentsVerlaagd

**Soort:** functie

```typescript
function isWebComponentsLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is WebComponentsLoweredModule;
```

Beperk een doelplan tot het webcomponentenplan zonder te casten.

#### Parameters

| Naam     | Typ                                   | Beschrijving |
| -------- | ------------------------------------- | ------------ |
| verlaagd | TargetLoweredModule \| ongedefinieerd |              |

### lagereWebComponentsModule

**Soort:** functie

```typescript
function lowerWebComponentsModule(
  module: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

Verlaag de neutrale IR in de doelintenties van de webcomponenten.

#### Parameters

| Naam    | Typ               | Beschrijving |
| ------- | ----------------- | ------------ |
| module  | SemantischeModule |              |
| context | DoelContext       |              |

### lagereWebComponentsPlan

**Soort:** functie

```typescript
function lowerWebComponentsPlan(
  module: SemanticModule,
  context: TargetContext,
): WebComponentsLoweredModule;
```

Bouw het Web-Components doelplan voor een neutrale module.

#### Parameters

| Naam    | Typ               | Beschrijving |
| ------- | ----------------- | ------------ |
| module  | SemantischeModule |              |
| context | DoelContext       |              |

### ONBEKEND_TYPE

**Soort:** constant

```typescript
export const UNKNOWN_TYPE;
```

Het type dat wordt verzonden wanneer er geen gedeclareerd of afgeleid type beschikbaar is.

### WEB_COMPONENTS_FRAMEWORK

**Soort:** constant

```typescript
export const WEB_COMPONENTS_FRAMEWORK;
```

De raamwerk-ID van de plug-in en de discriminator van het verlaagde plan.

### WEBCOMPONENTS_NATIVE_HOSTS

**Soort:** constant

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS;
```

De compatibiliteitstabel voor wortels die veilig ingebouwd kunnen worden op maat.

### WebComponentsCleanupField

**Soort:** interface

```typescript
export interface WebComponentsCleanupField
```

Een privéveld dat de opschoonfunctie van een effect behoudt tussen callbacks van de levenscyclus.

### WebComponentsDerivedBody

**Soort:** type

```typescript
export type WebComponentsDerivedBody = |
```

De hoofdtekst van een afgeleide getter, al herschreven in de scope van een elementinstantie:
een beknopte memofabriek retourneert een enkele uitdrukking, een blokvormige brengt
zijn eigen verklaringen (die de getter moet uitvoeren in plaats van retourneren).

### WebComponentsDerivedValue

**Soort:** interface

```typescript
export interface WebComponentsDerivedValue
```

Een opgeslagen waarde, verlaagd tot een getter die bij het lezen opnieuw wordt berekend.

### WebComponentsElementRef

**Soort:** interface

```typescript
export interface WebComponentsElementRef
```

Een `useRef`-binding, verlaagd naar een `{ current }`-cel die door het element wordt vastgehouden.

### WebComponentsGeneratedId

**Soort:** interface

```typescript
export interface WebComponentsGeneratedId
```

Een neutrale `useId()`-binding, verlaagd naar een instantieveld dat eenmaal per element wordt geplaatst.

### WebComponentenHostFallbackReden

**Soort:** type

```typescript
export type WebComponentsHostFallbackReason =
  | "missing-root"
  | "fragment-root"
  | "dynamic-root"
  | "component-root"
  | "ambiguous-root"
  | "invalid-root"
  | "unsupported-root";
```

Stabiele redenen waarom een ​​component geen aangepaste ingebouwde host kan gebruiken.

### WebComponentenHostKind

**Soort:** type

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

Het hostformulier dat wordt gebruikt door een gegenereerd aangepast element.

### WebComponentsHostPlan

**Soort:** interface

```typescript
export interface WebComponentsHostPlan
```

Hostselectie en aanroepmetagegevens voor een gegenereerde component.

### WebComponentsInternalsBeleid

**Soort:** interface

```typescript
export interface WebComponentsInternalsPolicy
```

ElementInternals-mogelijkheden aangevraagd door een gegenereerde component.

### WebComponentsLifecycleCallback

**Soort:** type

```typescript
export type WebComponentsLifecycleCallback =
  "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

De levenscyclus-callbacks van aangepaste elementen die een plan kan genereren.

### WebComponentsLifecycleHook

**Soort:** interface

```typescript
export interface WebComponentsLifecycleHook
```

Eén gegenereerde callback voor de levenscyclus en de instructies die worden uitgevoerd.

### WebComponentsListKey

**Soort:** interface

```typescript
export interface WebComponentsListKey
```

Een sleutelkandidaat voor lijstweergave die wordt behouden voor de lijstuitvoer van het doel.

### WebComponentsLoweredModule

**Soort:** interface

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

Het doelplan voor webcomponenten geproduceerd door `lower` en verfijnd door `optimize`.

### WebComponentenGepromootLokaal

**Soort:** interface

```typescript
export interface WebComponentsPromotedLocal
```

Een weergavekopconstante **gepromoveerd** naar een elementlid.

Er worden allemaal een memo-getter, een lifecycle callback en een veldinitialisator uitgezonden
_buiten_ `render()`, dus een lokale versie waarvan de renderkop aangeeft dat deze niet bestaat
zij. Het promoten van de verklaring bij een lid is wat een dergelijke lezing tot stand brengt
oplosbaar — via `this.<name>` — in plaats van te bungelen.

Alleen een aantoonbaar effectvrije verklaring wordt bevorderd (zie
{@link gepromootHeadLocals}); al het andere blijft in `render()`.

### WebComponentenEigendomsverklaring

**Soort:** interface

```typescript
export interface WebComponentsPropertyDeclaration
```

Een `ForgeElement`-descriptor voor reactieve eigenschappen, die het contract van de native runtime weerspiegelt.

### WebComponentenReactiveProperty

**Soort:** interface

```typescript
export interface WebComponentsReactiveProperty
```

Een reactieve **eigenschap**: een extern instelbare invoer gespiegeld vanuit een attribuut.

### WebComponentenRegistratieopties

**Soort:** interface

```typescript
export interface WebComponentsRegistrationOptions
```

De `customElements.define` opties uitgegeven voor een inbouw op maat.

### WebComponentsRuntimeImports

**Soort:** interface

```typescript
export interface WebComponentsRuntimeImports
```

De import die een gegenereerde elementmodule nodig heeft.

### WebComponentenInstellingFase

**Soort:** interface

```typescript
export interface WebComponentsSetupPhase
```

De eenmalige **instelfase** van het element.

Een zaadje zoals `useState(parseTime(modelValue))` leest een reactieve eigenschap, en
een eigenschap heeft pas een waarde als de attributen van de host zijn overgenomen -
wat gebeurt bij verbinding, lang nadat de constructor is uitgevoerd. Erger nog, de
waarde wordt meestal bereikt via een render-head-constante (`const initial =
parseTime(modelValue);`) waarvan niet kon worden bewezen dat het effectvrij was en daarom
blijft in `render()`, dus een constructor-seed zou verwijzen naar een naam die dat wel doet
bestaat daar helemaal niet.

Een dergelijk zaad wordt uitgesteld naar `ForgeElement.setup()`, dat door de runtime wordt aangeroepen
na de adoptie van het attribuut en vóór de eerste weergave, precies één keer per element
— een herverbinding mag niet opnieuw beginnen en weggooien wat de gebruiker sindsdien heeft gewijzigd.
De hoofdverklaringen die het zaad nodig heeft, worden daar eerst herhaald, in hoofdvolgorde en
transitief gesloten, en **alleen** die.

Herspelen in plaats van caching is opzettelijk. In de neutrale bron het hoofd
_is_ de componentbody, dus al `const initial = parseTime(modelValue);`
draait op elke weergave; Als u het nogmaals evalueert tijdens de installatie, wordt er geen oproep uitgevoerd
de geschreven component presteert niet al bij elke doorgang. Het cachen in een
veld zou in plaats daarvan het aantal evaluaties _verminderen_ en de waarde an geven
identiteit die de bron nooit heeft beloofd. Alleen een gewone `const` (of een functie)
declaratie wordt altijd opnieuw afgespeeld — zie {@link headReplay}.

### WebComponentenSchaduwbeleid

**Soort:** interface

```typescript
export interface WebComponentsShadowPolicy
```

Getypt schaduwwortelbeleid dat door het doelplan wordt behouden.

### WebComponentsStateField

**Soort:** interface

```typescript
export interface WebComponentsStateField
```

Een reactief **status**-veld: een interne cel waarvan de setter het element opnieuw weergeeft.

### WebComponentsStaticTemplatePart

**Soort:** interface

```typescript
export interface WebComponentsStaticTemplatePart
```

Een volledig statisch sjabloonstuk dat is gehesen tot een constante op moduleniveau.

### WebComponentenSjabloonPlan

**Soort:** interface

```typescript
export interface WebComponentsTemplatePlan
```

Het verlaagde `render()`-plan: de hoofdinstructies en het lit-html-sjabloon.

## `src/optimize`

### optimaliseerWebComponentsModule

**Soort:** functie

```typescript
function optimizeWebComponentsModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

Optimaliseer de doelintenties van de webcomponenten; andere doelen passeren.

#### Parameters

| Naam        | Typ                   | Beschrijving |
| ----------- | --------------------- | ------------ |
| bedoelingen | DoelIntenties         |              |
| opties      | TargetOptimizeOptions |              |

### optimaliseerWebComponentsPlan

**Soort:** functie

```typescript
function optimizeWebComponentsPlan(
  plan: WebComponentsLoweredModule,
  options: TargetOptimizeOptions,
): WebComponentsLoweredModule;
```

Verfijn een verlaagd webcomponentenplan en registreer elke uitgevoerde passage.

#### Parameters

| Naam    | Typ                        | Beschrijving |
| ------- | -------------------------- | ------------ |
| plannen | WebComponentsLoweredModule |              |
| opties  | TargetOptimizeOptions      |              |

### WEB_COMPONENTS_OPTIMIZATIES

**Soort:** constant

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS;
```

Identificatiegegevens vastgelegd in {@link WebComponentsLoweredModule.appliedOptimizations}.

### WebComponentenoptimalisatie

**Soort:** type

```typescript
export type WebComponentsOptimization =
  (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

Een geregistreerde optimalisatie-ID voor webcomponenten.

## `src/runtime/hook-module`

### emitWebComponentHookModule

**Soort:** functie

```typescript
function emitWebComponentHookModule(module: SemanticModule): string;
```

Transformeer een neutrale hook-module in zijn Web-Components-bron.

#### Parameters

| Naam   | Typ               | Beschrijving |
| ------ | ----------------- | ------------ |
| module | SemantischeModule |              |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**Soort:** constant

```typescript
export const HAS_SLOT_RUNTIME;
```

De native runtime-helper waarnaar de markering wordt verlaagd, aangeroepen met het hostelement.

### isFunctieExpressieTekst

**Soort:** functie

```typescript
function isFunctionExpressionText(text: string): boolean;
```

Of de tekst een **functiewaarde**-expressie is: een pijl of een
`function`-expressie.

Het creëren van een afsluiting heeft op zichzelf geen effect, dus zo'n render-head-constante
kan worden gepromoveerd tot een elementlid (zie `../lower`), hoe onzuiver zijn lichaam ook is
is: het lichaam loopt nog steeds precies wanneer het wordt opgeroepen.

#### Parameters

| Naam  | Typ        | Beschrijving |
| ----- | ---------- | ------------ |
| tekst | tekenreeks |              |

### isPureExpressionText

**Soort:** functie

```typescript
function isPureExpressionText(text: string): boolean;
```

Of een uitdrukking **aantoonbaar** vrij is van effecten: geen oproep, geen `new`, nee
`await`, geen toewijzing en geen verhoging, op elke diepte.

Wordt gebruikt om te beslissen of een render-head-constante kan worden gepromoveerd tot een getter.
De test is opzettelijk syntactisch en pessimistisch – een oproep aan een pure helper
is niet te onderscheiden van een oproep naar een mutator, dus elke oproep diskwalificeert de
instructie en deze blijft in `render()`.

#### Parameters

| Naam  | Typ        | Beschrijving |
| ----- | ---------- | ------------ |
| tekst | tekenreeks |              |

## `src/transformers/props-binding`

### leadingObjectPattern

**Soort:** functie

```typescript
function leadingObjectPattern(text: string): string | undefined;
```

Het objectpatroon dat `text` _start_, waarbij alles wat volgt wordt weggegooid.

De brontekst van een parameter draagt zijn annotatie (`{ a, b }: Readonly<P>`),
dus het patroon moet worden uitgesneden voordat het kan worden ontleed.

#### Parameters

| Naam  | Typ        | Beschrijving |
| ----- | ---------- | ------------ |
| tekst | tekenreeks |              |

### parsePropsBinding

**Soort:** functie

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined;
```

Ontleed een rekwisietenobjectpatroon.

Retourneert `undefined` wanneer de tekst geen objectpatroon is, of een
invoer die deze module niet modelleert (bijvoorbeeld een berekende sleutel) - de beller behoudt dan
zijn eerdere gedrag in plaats van te handelen op basis van een gedeeltelijke meting.

#### Parameters

| Naam         | Typ        | Beschrijving |
| ------------ | ---------- | ------------ |
| patroonTekst | tekenreeks |              |

### RekwisietenBindend

**Soort:** interface

```typescript
export interface PropsBinding
```

Een props-objectpatroon, opgesplitst in de leden die het leest en de lokale bewoners die het bindt.

### RekwisietenBindendInvoer

**Soort:** interface

```typescript
export interface PropsBindingEntry
```

Eén invoer van een rekwisietenobjectpatroon.

### rekwisietenBindingStatement

**Soort:** functie

```typescript
function propsBindingStatement(
  binding: PropsBinding,
  isRead: (name: string) => boolean,
): string | undefined;
```

Speel een rekwisietenpatroon opnieuw af als `const { … } = this;`, beperkt tot de vermeldingen
wiens lokale bevolking het doelbereik feitelijk luidt.

Door het te beperken blijft een opgeheven bereik vrij van bindingen die het nooit gebruikt (wat a
`noUnusedLocals` build zou weigeren) terwijl elke bewaarde invoer behouden blijft - en
dus de standaard is byte-identiek aan de geschreven versie.

#### Parameters

| Naam    | Typ                       | Beschrijving |
| ------- | ------------------------- | ------------ |
| bindend | RekwisietenBinden         |              |
| isLees  | (naam: string) => boolean |              |

## `src/transformers/props-type`

### geïndexeerdAccessType

**Soort:** functie

```typescript
function indexedAccessType(
  reference: PropsTypeReference,
  member: string,
): string;
```

De geïndexeerde toegangsannotatie waarmee een lid van het props-type wordt verzonden.

De eigen optioneelheid van het lid blijft precies behouden: een verplicht lid is _niet_
verbreed met `| undefined`. Het veld van het element wordt pas ingevuld zodra de
runtime neemt zijn attribuut over, dus het is echt uitgeschakeld in de constructor -
maar dat wordt uitgedrukt door het veld uit te zenden als `declare` (de runtime is eigenaar van de
accessor), waardoor het wordt vrijgesteld van `strictPropertyInitialization` zonder te maken
elke lezing van een vereiste prop, mogelijk `undefined`.

#### Parameters

| Naam       | Typ                 | Beschrijving |
| ---------- | ------------------- | ------------ |
| referentie | PropsTypeReferentie |              |
| lid        | tekenreeks          |              |

### PropsTypeReferentie

**Soort:** interface

```typescript
export interface PropsTypeReference
```

Het rekwisietentype waartegen de velden van een component kunnen worden geannoteerd.

### solvePropsTypeReference

**Soort:** functie

```typescript
function resolvePropsTypeReference(
  annotation: string | undefined,
  declarations: readonly GenericStatement[],
): PropsTypeReference | undefined;
```

Los de props-annotatie op tegen de bewaarde declaraties van de module.

Retourneert `undefined` als de annotatie geen gewone naam is, of als dat niet het geval is
interface / type-alias van die naam blijft behouden naast de elementklasse -
in beide gevallen zou de verwijzing niet worden omgezet in de gegenereerde module.

#### Parameters

| Naam         | Typ                             | Beschrijving |
| ------------ | ------------------------------- | ------------ |
| annotatie    | tekenreeks \| ongedefinieerd    |              |
| verklaringen | alleen-lezen GenericStatement[] |              |

### typeLeden

**Soort:** functie

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>;
```

De leden die een behouden interface/type-alias-instantie declareert, toegewezen aan of
elk is optioneel (`name?: T`).

Alleen leden op het hoogste niveau van de eerste `{…}`-instantie worden verzameld, en alleen
waar een lid legitiem mag starten (direct na het openingsaccolade, een `;`
of een `,`). Leden die zijn bijgedragen door een `extends`/`&`-basis zijn daarom _niet_
gerapporteerd - de beller behandelt ze als onbekend en valt terug, wat de
veilige richting.

#### Parameters

| Naam          | Typ        | Beschrijving |
| ------------- | ---------- | ------------ |
| aangifteTekst | tekenreeks |              |

### unwrapPropsTypeName

**Soort:** functie

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined;
```

Reduceer een rekwisietannotatie tot de gewone naam waarnaar deze verwijst, en pak de
haakjes en ledenbehoudende generieke wrappers die een component kan schrijven.

#### Parameters

| Naam  | Typ                          | Beschrijving |
| ----- | ---------------------------- | ------------ |
| tekst | tekenreeks \| ongedefinieerd |              |
