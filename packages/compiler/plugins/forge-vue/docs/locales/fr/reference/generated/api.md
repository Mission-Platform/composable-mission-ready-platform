# Référence API @mission-platform/forge-plugin-vue

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/compiler/plugins/forge-vue/docs/reference/generated/api.md: [packages/compiler/plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### ModuleExtraÉmis

**Genre :** interface

```typescript
export interface EmittedExtraModule
```

Un SFC auxiliaire que l'émetteur Vue génère à côté du module principal.

### ModuleVueEmis

**Genre :** interface

```typescript
export interface EmittedVueModule
```

Résultat de l'émetteur Vue : le SFC principal plus les SFC auxiliaires qu'il a générés.

### émettreVueModule

**Genre :** fonction

```typescript
function emitVueModule(
  module: SemanticModule,
  componentName: string,
  componentFolders?: ReadonlySet<string>,
  plan?: VueLoweredModule,
): EmittedVueModule;
```

Transformez un module de composants neutres en un SFC Vue (plus tous les SFC auxiliaires).

#### Paramètres

| Nom                    | Tapez               | Descriptif |
| ---------------------- | ------------------- | ---------- |
| modules                | Module Sémantique   |            |
| Nom du composant       | chaîne              |            |
| dossiers de composants | ReadonlySet<string> |            |
| planifier              | VueLoweredModule    |            |

## `src/emitters/hook-module`

### émettreVueHookModule

**Genre :** fonction

```typescript
function emitVueHookModule(module: SemanticModule): string;
```

Compilez un module hook neutre sur sa source composable Vue (`.ts`).

#### Paramètres

| Nom     | Tapez             | Descriptif |
| ------- | ----------------- | ---------- |
| modules | Module Sémantique |            |

## `src/index`

### forgeVueFramework

**Genre :** fonction

```typescript
function forgeVueFramework(): FrameworkOutputPlugin;
```

Créez le plug-in de sortie Vue et ses adaptateurs de compilateur Vite/Rolldown.

## `src/lower`

### isVueLowered

**Genre :** fonction

```typescript
function isVueLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is VueLoweredModule;
```

Affinez un plan cible au plan Vue sans lancer de diffusion.

#### Paramètres

| Nom     | Tapez                          | Descriptif |
| ------- | ------------------------------ | ---------- |
| abaissé | CibleLoweredModule \| indéfini |            |

### LowerVueModule

**Genre :** fonction

```typescript
function lowerVueModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

Abaissez un module neutre dans le plan cible Vue. Le module neutre se déplace
reste intact afin que les phases ultérieures conservent un accès complet à l'IR.

#### Paramètres

| Nom      | Tapez             | Descriptif |
| -------- | ----------------- | ---------- |
| ir       | Module Sémantique |            |
| contexte | Contexte cible    |            |

### VueComputedPlan

**Genre :** interface

```typescript
export interface VueComputedPlan
```

Un `useMemo` abaissé à un Vue `computed`.

### VueDynamicNodePlan

**Genre :** interface

```typescript
export interface VueDynamicNodePlan
```

Une cible de rendu `<component :is>`.

### VueEventPlan

**Genre :** interface

```typescript
export interface VueEventPlan
```

Un événement émis par le composant.

### VueListKeyPlan

**Genre :** interface

```typescript
export interface VueListKeyPlan
```

Une décision clé `v-for`.

### VueLoweredModule

**Genre :** interface

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

Le plan cible Vue produit par {@link lowerVueModule}.

### VuePropPlan

**Genre :** interface

```typescript
export interface VuePropPlan
```

Un membre du contrat `defineProps` réduit.

### VuePropsContrat

**Genre :** interface

```typescript
export interface VuePropsContract
```

La forme abaissée `defineProps`.

### VueRéfPlan

**Genre :** interface

```typescript
export interface VueRefPlan
```

Un `useState` réduit à une déclaration Vue `ref`.

### VueRuntimeImports

**Genre :** interface

```typescript
export interface VueRuntimeImports
```

Liaisons `vue` dont un plan a besoin.

### VueSlotPlan

**Genre :** interface

```typescript
export interface VueSlotPlan
```

Un emplacement rendu par le composant.

### VueStaticSubtreePlan

**Genre :** interface

```typescript
export interface VueStaticSubtreePlan
```

Un sous-arbre statiquement connu qui peut être hissé.

### VueTemplateRefPlan

**Genre :** interface

```typescript
export interface VueTemplateRefPlan
```

Un `useRef` réduit à une référence de modèle Vue (ou un simple `shallowRef`).

### VueWatcherPlan

**Genre :** interface

```typescript
export interface VueWatcherPlan
```

Un `useEffect` abaissé à un observateur Vue.

## `src/optimize`

### DEDUPE_COMPUTED

**Genre :** constante

```typescript
export const DEDUPE_COMPUTED;
```

Réduisez les déclarations `computed` qui partagent une usine.

### DROP_UNUSED_IMPORTS

**Genre :** constante

```typescript
export const DROP_UNUSED_IMPORTS;
```

Prune `vue` importe le plan affiné dont nous n'avons plus besoin.

### HOIST_STATIC_SUBTREES

**Genre :** constante

```typescript
export const HOIST_STATIC_SUBTREES;
```

Lever les sous-arbres entièrement statiques (constantes du module / `v-once`).

### INLINE_SINGLE_USE_REFS

**Genre :** constante

```typescript
export const INLINE_SINGLE_USE_REFS;
```

Émet un état jamais réaffecté en tant que `const` simple au lieu d'un `ref`.

### optimiserVueModule

**Genre :** fonction

```typescript
function optimizeVueModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

Affiner un plan Vue réduit. Les intentions portant un plan étranger (ou manquant) sont
est revenu intact, de sorte que l'optimiseur peut s'exécuter en toute sécurité dans n'importe quel pipeline.

#### Paramètres

| Nom        | Tapez                        | Descriptif |
| ---------- | ---------------------------- | ---------- |
| intentions | Intentions cibles            |            |
| options    | Options d'optimisation cible |            |

### STABLE_LIST_KEYS

**Genre :** constante

```typescript
export const STABLE_LIST_KEYS;
```

Conservez uniquement les clés de liste prouvées stables, en enregistrant les listes non saisies.

### VUE_OPTIMISATIONS

**Genre :** constante

```typescript
export const VUE_OPTIMIZATIONS: readonly string[];
```

Chaque identifiant d'optimisation que la cible Vue peut enregistrer, dans l'ordre des applications.
