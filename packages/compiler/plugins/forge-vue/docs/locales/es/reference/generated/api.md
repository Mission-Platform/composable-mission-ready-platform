# Referencia de API @mission-platform/forge-plugin-vue

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/compiler/plugins/forge-vue/docs/reference/generated/api.md: [packages/compiler/plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### MóduloExtraEmitido

**Tipo:** interfaz

```typescript
export interface EmittedExtraModule
```

Un SFC auxiliar que el emisor Vue genera junto al módulo primario.

### Módulo Vue emitido

**Tipo:** interfaz

```typescript
export interface EmittedVueModule
```

El resultado del emisor Vue: el SFC principal más cualquier SFC auxiliar que generó.

### emitVueModule

**Tipo:** función

```typescript
function emitVueModule(
  module: SemanticModule,
  componentName: string,
  componentFolders?: ReadonlySet<string>,
  plan?: VueLoweredModule,
): EmittedVueModule;
```

Transforme un módulo de componente neutro en un SFC Vue (más cualquier SFC auxiliar).

#### Parámetros

| Nombre                  | Tipo                    | Descripción |
| ----------------------- | ----------------------- | ----------- |
| módulo                  | Módulo Semántico        |             |
| nombredelcomponente     | cadena                  |             |
| carpetas de componentes | Sólo lecturaSet<string> |             |
| plano                   | Módulo VueBajado        |             |

## `src/emitters/hook-module`

### emitVueHookMódulo

**Tipo:** función

```typescript
function emitVueHookModule(module: SemanticModule): string;
```

Compile un módulo de enlace neutro en su fuente componible Vue (`.ts`).

#### Parámetros

| Nombre | Tipo             | Descripción |
| ------ | ---------------- | ----------- |
| módulo | Módulo Semántico |             |

## `src/index`

### forgeVueFramework

**Tipo:** función

```typescript
function forgeVueFramework(): FrameworkOutputPlugin;
```

Cree el complemento de salida Vue y sus adaptadores del compilador Vite/Rolldown.

## `src/lower`

### isVueLowered

**Tipo:** función

```typescript
function isVueLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is VueLoweredModule;
```

Limite un plan de destino al plan Vue sin realizar casting.

#### Parámetros

| Nombre | Tipo                             | Descripción |
| ------ | -------------------------------- | ----------- |
| bajado | TargetLoweredModule\| indefinido |             |

### módulo Vue inferior

**Tipo:** función

```typescript
function lowerVueModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

Baje un módulo neutral al plan objetivo Vue. El módulo neutro viaja
en intacto para que las fases posteriores mantengan acceso completo al IR.

#### Parámetros

| Nombre   | Tipo              | Descripción |
| -------- | ----------------- | ----------- |
| ir       | Módulo Semántico  |             |
| contexto | Contexto objetivo |             |

### VuePlan Computado

**Tipo:** interfaz

```typescript
export interface VueComputedPlan
```

Un `useMemo` rebajado a un Vue `computed`.

### VueDynamicNodePlan

**Tipo:** interfaz

```typescript
export interface VueDynamicNodePlan
```

Un objetivo de renderizado `<component :is>`.

### VueEventPlan

**Tipo:** interfaz

```typescript
export interface VueEventPlan
```

Un evento que emite el componente.

### VueListKeyPlan

**Tipo:** interfaz

```typescript
export interface VueListKeyPlan
```

Una decisión clave `v-for`.

### Módulo VueBajado

**Tipo:** interfaz

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

El plan de destino Vue producido por {@link lowerVueModule}.

### VuePropPlan

**Tipo:** interfaz

```typescript
export interface VuePropPlan
```

Un miembro del contrato `defineProps` rebajado.

### VuePropsContrato

**Tipo:** interfaz

```typescript
export interface VuePropsContract
```

La forma `defineProps` bajada.

### VueRefPlan

**Tipo:** interfaz

```typescript
export interface VueRefPlan
```

Un `useState` reducido a una declaración Vue `ref`.

### Importaciones de VueRuntime

**Tipo:** interfaz

```typescript
export interface VueRuntimeImports
```

El `vue` vincula las necesidades de un plan.

### VueSlotPlan

**Tipo:** interfaz

```typescript
export interface VueSlotPlan
```

Una ranura que representa el componente.

### VueStaticSubtreePlan

**Tipo:** interfaz

```typescript
export interface VueStaticSubtreePlan
```

Un subárbol conocido estáticamente que se puede elevar.

### VueTemplateRefPlan

**Tipo:** interfaz

```typescript
export interface VueTemplateRefPlan
```

Un `useRef` reducido a una referencia de plantilla Vue (o un `shallowRef` simple).

### VueWatcherPlan

**Tipo:** interfaz

```typescript
export interface VueWatcherPlan
```

Un `useEffect` reducido a un observador Vue.

## `src/optimize`

### DEDUPE_COMPUTED

**Tipo:** constante

```typescript
export const DEDUPE_COMPUTED;
```

Contraer declaraciones `computed` que comparten una fábrica.

### DROP_UNUSED_IMPORTS

**Tipo:** constante

```typescript
export const DROP_UNUSED_IMPORTS;
```

Prune `vue` importa el plan refinado que ya no necesita.

### HOIST_STATIC_SUBTREES

**Tipo:** constante

```typescript
export const HOIST_STATIC_SUBTREES;
```

Elevar subárboles totalmente estáticos (constantes de módulo/`v-once`).

### INLINE_SINGLE_USE_REFS

**Tipo:** constante

```typescript
export const INLINE_SINGLE_USE_REFS;
```

Emite un estado nunca reasignado como `const` simple en lugar de `ref`.

### optimizarVueModule

**Tipo:** función

```typescript
function optimizeVueModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

Refinar un plan Vue reducido. Las intenciones que llevan un plan extranjero (o faltante) son
devuelto intacto, por lo que es seguro ejecutar el optimizador en cualquier canalización.

#### Parámetros

| Nombre      | Tipo                                | Descripción |
| ----------- | ----------------------------------- | ----------- |
| intenciones | Intenciones objetivo                |             |
| opciones    | Opciones de optimización de destino |             |

### STABLE_LIST_KEYS

**Tipo:** constante

```typescript
export const STABLE_LIST_KEYS;
```

Mantenga sólo las claves de lista que demuestren ser estables y registre las listas que no estén claves.

### VUE_OPTIMIZACIONES

**Tipo:** constante

```typescript
export const VUE_OPTIMIZATIONS: readonly string[];
```

Cada identificador de optimización que el objetivo Vue puede registrar, en el orden de la aplicación.
