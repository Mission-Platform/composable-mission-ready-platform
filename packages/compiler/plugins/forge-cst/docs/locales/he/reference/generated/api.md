# הפניה ל-@mission-platform/forge-cst API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/compiler/plugins/forge-cst/docs/reference/generated/api.md: [packages/compiler/plugins/forge-cst/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/forge-cst`.

## `src/cst`

### getImportDeclarations

פונקציה **סוג:**

```typescript
function getImportDeclarations(program: Program): readonly ImportDeclaration[];
```

חלץ את כל הצהרות הייבוא ​​ברמה העליונה מתוכנית AST ממנותחת.

#### פרמטרים

| שם     | הקלד   | תיאור |
| ------ | ------ | ----- |
| תוכנית | תוכנית |       |

### parseCst

פונקציה **סוג:**

```typescript
function parseCst(
  source: string,
  options?: ParseCstOptions | string,
): ParseResult;
```

ניתוח טקסט מקור TypeScript/JavaScript/TSX לתוך עץ תחביר בטון (CST)
עם עמדות אסימונים, הערות ומטא נתונים מלאים של AST.

#### פרמטרים

| שם       | הקלד                      | תיאור |
| -------- | ------------------------- | ----- |
| מקור     | מחרוזת                    |       |
| אפשרויות | ParseCstOptions \| מחרוזת |       |

### ParseCstOptions

**סוג:** ממשק

```typescript
export interface ParseCstOptions extends ParserOptions
```

אפשרויות הגדרת ניתוח CST.

## `src/imports`

### ImportRewriteSpec

**סוג:** ממשק

```typescript
export interface ImportRewriteSpec
```

מפרט לשכתוב יבוא המכוון למודול ספציפי.

### RewriteImportsOptions

**סוג:** ממשק

```typescript
export interface RewriteImportsOptions
```

אפשרויות הגדרת שכתוב ייבוא ​​מבוסס CST.

### rewriteImportsWithCst

פונקציה **סוג:**

```typescript
function rewriteImportsWithCst(
  source: string,
  options: RewriteImportsOptions,
): RewriteResult;
```

שכתוב יבוא בקוד מקור TypeScript/TSX באמצעות עץ תחביר בטון מדויק (CST).

שומר על הערות, עיצוב מרובה שורות ושלמות AST ללא ניתוח שביר של ביטויים רגולריים.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| מקור     | מחרוזת                |       |
| אפשרויות | RewriteImportsOptions |       |

### RewriteResult

**סוג:** ממשק

```typescript
export interface RewriteResult
```

תוצאה של שינוי שכתוב בייבוא ​​CST.
