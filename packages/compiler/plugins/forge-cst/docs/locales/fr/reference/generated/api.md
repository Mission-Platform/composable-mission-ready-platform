# Référence API @mission-platform/forge-cst

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/compiler/plugins/forge-cst/docs/reference/generated/api.md: [packages/compiler/plugins/forge-cst/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/forge-cst`.

## `src/cst`

### getImportDeclarations

**Genre :** fonction

```typescript
function getImportDeclarations(program: Program): readonly ImportDeclaration[];
```

Extrayez toutes les déclarations d'importation de niveau supérieur d'un programme analysé AST.

#### Paramètres

| Nom       | Tapez     | Descriptif |
| --------- | --------- | ---------- |
| programme | Programme |            |

### parseCst

**Genre :** fonction

```typescript
function parseCst(
  source: string,
  options?: ParseCstOptions | string,
): ParseResult;
```

Analyser le texte source TypeScript/JavaScript/TSX dans un arbre syntaxique concret (CST)
avec les positions des jetons, les commentaires et les métadonnées AST complètes.

#### Paramètres

| Nom     | Tapez                     | Descriptif |
| ------- | ------------------------- | ---------- |
| source  | chaîne                    |            |
| options | ParseCstOptions \| chaîne |            |

### ParseCstOptions

**Genre :** interface

```typescript
export interface ParseCstOptions extends ParserOptions
```

Options configurant l’analyse CST.

## `src/imports`

### ImportRewriteSpec

**Genre :** interface

```typescript
export interface ImportRewriteSpec
```

Spécification pour la réécriture des importations ciblant un module spécifique.

### RéécritureImportsOptions

**Genre :** interface

```typescript
export interface RewriteImportsOptions
```

Options configurant la réécriture d’importation basée sur CST.

### réécrireImportsAvecCst

**Genre :** fonction

```typescript
function rewriteImportsWithCst(
  source: string,
  options: RewriteImportsOptions,
): RewriteResult;
```

Réécrivez les importations dans le code source TypeScript/TSX à l'aide d'un arbre syntaxique concret (CST) précis.

Préserve les commentaires, le formatage multiligne et l’intégrité AST sans analyse fragile des expressions régulières.

#### Paramètres

| Nom     | Tapez                    | Descriptif |
| ------- | ------------------------ | ---------- |
| source  | chaîne                   |            |
| options | RéécritureImportsOptions |            |

### Réécrire le résultat

**Genre :** interface

```typescript
export interface RewriteResult
```

Résultat d’une transformation de réécriture d’importation CST.
