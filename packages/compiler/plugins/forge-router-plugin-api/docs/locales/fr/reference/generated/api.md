# Référence API @mission-platform/forge-router-plugin-api

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/compiler/plugins/forge-router-plugin-api/docs/reference/generated/api.md: [packages/compiler/plugins/forge-router-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/forge-router-plugin-api`.

## `src/router`

### createRouterDiagnostic

**Genre :** fonction

```typescript
function createRouterDiagnostic(
  diagnostic: Omit<CompilerDiagnostic, "phase" | "fileName"> & {
    readonly fileName?: string;
  },
): CompilerDiagnostic;
```

Créez un diagnostic source-local pour une défaillance du compilateur de routeur.

#### Paramètres

| Nom        | Tapez                                                                                              | Descriptif |
| ---------- | -------------------------------------------------------------------------------------------------- | ---------- |
| diagnostic | Omit<CompilerDiagnostic, 'phase' \| 'fileName'> & { nom de fichier en lecture seule ? : chaîne ; } |            |

### définirForgeRouterPlugin

**Genre :** fonction

```typescript
function defineForgeRouterPlugin(plugin: T): T;
```

Validez les métadonnées du plug-in du routeur avant qu'elles n'entrent dans un pipeline du compilateur.

#### Paramètres

| Nom    | Tapez | Descriptif |
| ------ | ----- | ---------- |
| plugin | T     |            |

### définirForgeRouterTarget

**Genre :** fonction

```typescript
function defineForgeRouterTarget(
  options: ForgeRouterTargetOptions,
): RouterOutputPlugin;
```

Créez une cible déterministe qui réécrit les importations de routeurs neutres.

Préférez {@link ForgeRouterTargetOptions.runtimeModule} : il conserve les sites d'appel
(`useMpRouter().navigate`, `useMpRoute().query`, …) forme compatible par
importer des assistants d'exécution cibles plutôt que des hooks natifs nus avec différents
signatures. Les cibles peuvent toujours fournir des phases d'abaissement/génération personnalisées pour
routeurs basés sur des fichiers ou sur serveur uniquement.

#### Paramètres

| Nom     | Tapez                    | Descriptif |
| ------- | ------------------------ | ---------- |
| options | ForgeRouterTargetOptions |            |

### FORGE_ROUTER_RUNTIME_EXPORTS

**Genre :** constante

```typescript
export const FORGE_ROUTER_RUNTIME_EXPORTS;
```

Exportations d'exécution neutres qui préservent le contrat auteur du package.

Cibles qui ne peuvent pas renommer les hooks natifs 1:1 (par exemple celui de React
`useNavigate` vs neutre `useMpRouter().navigate`) doit exposer ces noms
à partir d'un module d'exécution cible et d'un point {@link ForgeRouterTargetOptions.runtimeModule}
à ce module.

### forgeRouterExtensionContracts

**Genre :** constante

```typescript
export const forgeRouterExtensionContracts: readonly RouterTargetExtensionContract[];
```

Contrats cibles réservés aux routeurs dont les adaptateurs propriétaires ne font pas partie de cette étape.

### ForgeRouterTargetOptions

**Genre :** interface

```typescript
export interface ForgeRouterTargetOptions
```

Configuration pour l’usine cible du routeur de réécriture source standard.

### Déclaration de routeur générée

**Genre :** interface

```typescript
export interface GeneratedRouterDeclaration
```

Une déclaration émise à côté d'un module de routeur généré.

### Module de routeur généré

**Genre :** interface

```typescript
export interface GeneratedRouterModule
```

Un module source généré renvoyé par une cible de routeur.

### MP_ROUTER_COMPILER_MARKER

**Genre :** constante

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

Marqueur stable partagé avec le package de routeur neutre.

### MP_ROUTER_MODULE

**Genre :** constante

```typescript
export const MP_ROUTER_MODULE;
```

Le package dont les importations sont comprises par la passe du compilateur du routeur.

### Adaptateurs de construction de routeur

**Genre :** interface

```typescript
export interface RouterBuildAdapters
```

Créez des hooks appartenant à une cible de routeur ; aucune dépendance de routeur n'est chargée par noyau.

### Capacité du routeur

**Genre :** type

```typescript
export type RouterCapability =
  "link" | "route" | "navigate" | "resolve" | "view";
```

Opérations abaissables indépendamment exposées par le contrat de routeur neutre.

### RouterCapabilityImport

**Genre :** interface

```typescript
export interface RouterCapabilityImport
```

Une importation neutre qui peut être réécrite par une cible de routeur sélectionnée.

### Module de capacité de routeur

**Genre :** interface

```typescript
export interface RouterCapabilityModule
```

IR neutre transmis aux plugins cibles du routeur. Il décrit uniquement la consommation.

### Utilisation de la capacité du routeur

**Genre :** interface

```typescript
export interface RouterCapabilityUse
```

Une utilisation d'un marqueur de routeur neutre, conservant les coordonnées de la source pour les diagnostics.

### RouterCapabilityUseKind

**Genre :** type

```typescript
export type RouterCapabilityUseKind = "call" | "jsx" | "reference";
```

Comment une liaison de routeur neutre est utilisée dans un module source.

### RouterNativeImport

**Genre :** interface

```typescript
export interface RouterNativeImport
```

Une importation native utilisée pour remplacer un marqueur de routeur neutre.

### Options d'optimisation du routeur

**Genre :** interface

```typescript
export interface RouterOptimizeOptions
```

Options neutres partagées par les optimiseurs de routeurs.

### Plugin de sortie de routeur

**Genre :** interface

```typescript
export interface RouterOutputPlugin
```

Plugin de compilateur de style Forge pour une cible de routeur native.

### Sélection du plugin de routeur

**Genre :** type

```typescript
export type RouterPluginSelection = RouterOutputPlugin | string;
```

Un plugin de routeur peut être fourni directement ou sélectionné par son identifiant stable.

### RouterTargetContext

**Genre :** interface

```typescript
export interface RouterTargetContext
```

Contexte d'application/framework sélectionné indépendamment de la cible du routeur.

### RouterTargetExtensionContract

**Genre :** interface

```typescript
export interface RouterTargetExtensionContract
```

Un contrat annoncé par un futur routeur cible sans expédier son adaptateur.

### PlanCibleRouteur

**Genre :** interface

```typescript
export interface RouterTargetPlan
```

Plan spécifique à la cible réalisé entre la descente et la génération.

### sélectionnezForgeRouterPlugin

**Genre :** fonction

```typescript
function selectForgeRouterPlugin(
  selection: RouterPluginSelection | undefined,
  plugins: readonly RouterOutputPlugin[] = [],
): RouterOutputPlugin | undefined;
```

Recherchez une cible de routeur directe ou sélectionnée par identifiant sans la coupler aux plugins d'interface utilisateur.

#### Paramètres

| Nom       | Tapez                                      | Descriptif |
| --------- | ------------------------------------------ | ---------- |
| sélection | Sélection du plugin de routeur \| indéfini |            |
| plugins   | lecture seule RouterOutputPlugin[]         |            |

### capacités de routeur non prises en charge

**Genre :** fonction

```typescript
function unsupportedRouterCapabilities(
  ir: RouterCapabilityModule,
  plugin: RouterOutputPlugin | undefined,
): readonly CompilerDiagnostic[];
```

Signalez les lacunes en matière de capacités avant qu'une cible n'ait la possibilité de mal compiler silencieusement un module.

#### Paramètres

| Nom    | Tapez                          | Descriptif |
| ------ | ------------------------------ | ---------- |
| ir     | Module de capacité de routeur  |            |
| plugin | RouterOutputPlugin \| indéfini |            |
