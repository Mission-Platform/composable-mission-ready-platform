# @mission-platform/forge-cms-storyblok הפניה ל-API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/compiler/plugins/forge-cms-storyblok/docs/reference/generated/api.md: [packages/compiler/plugins/forge-cms-storyblok/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב `@mission-platform/forge-cms-storyblok`.

## `src/fields`

### analysStoryblokComponent

פונקציה **סוג:**

```typescript
function analyzeStoryblokComponent(
  sourceFile: ts.SourceFile,
  names: StoryblokComponentNames,
  options: StoryblokProjectionOptions = {},
): AnalyzedStoryblokComponent;
```

נתח מודול רכיב ניטרלי עבור Storyblok.

#### פרמטרים

| שם         | הקלד                       | תיאור |
| ---------- | -------------------------- | ----- |
| sourceFile | ts.SourceFile              |       |
| שמות       | StoryblokComponentNames    |       |
| אפשרויות   | StoryblokProjectionOptions |       |

### contentFieldToStoryblokField

פונקציה **סוג:**

```typescript
function contentFieldToStoryblokField(
  field: ContentField,
  options: StoryblokProjectionOptions = {},
): StoryblokSchemaField;
```

מיפוי שדה ניטרלי אחד על גבי שדה סכימה של Storyblok.

סדר הכנסת המפתח הוא משמעותי: הנפלט `<folder>.json` מושווה
כנגד מה שה-CLI של Storyblok ייצר לפני שההשלכה הזו הוכללה,
כך `type`/`pos`/`description`/`translatable`/`options`/`default_value`/
`required` חייבים לשמור על הסדר ההיסטורי שלהם.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| שדה      | ContentField               |       |
| אפשרויות | StoryblokProjectionOptions |       |

### contentKindToStoryblokFieldType

פונקציה **סוג:**

```typescript
function contentKindToStoryblokFieldType(
  kind: ContentField["kind"],
): StoryblokFieldType;
```

שדה Storyblok הקלד תוכן נייטרלי ממפה.

#### פרמטרים

| שם  | הקלד                | תיאור |
| --- | ------------------- | ----- |
| סוג | ContentField["סוג"] |       |

### emitStoryblokComponent

פונקציה **סוג:**

```typescript
function emitStoryblokComponent(
  sourceFile: ts.SourceFile,
  names: StoryblokComponentNames,
  options: StoryblokProjectionOptions = {},
): StoryblokComponent;
```

שלח רק את האובייקט של רכיב Storyblok (תצורת הבלוק) עבור רכיב.

#### פרמטרים

| שם         | הקלד                       | תיאור |
| ---------- | -------------------------- | ----- |
| sourceFile | ts.SourceFile              |       |
| שמות       | StoryblokComponentNames    |       |
| אפשרויות   | StoryblokProjectionOptions |       |

### resolveStoryblokMetadata

פונקציה **סוג:**

```typescript
function resolveStoryblokMetadata(
  component: ContentComponent,
  options: StoryblokProjectionOptions = {},
): ResolvedStoryblokMetadata;
```

פתור מטא נתונים של עורך פעם אחת עבור רכיב.

הערת רכיב מפורשת מנצחת את ברירת המחדל של היעד. כששניהם לא
מסופק, השם הטכני נותן לסמל מזהה יציב ואת
שם hash נותן לו צבע Storyblok שניתן לחזור עליו.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| רכיב     | ContentComponent           |       |
| אפשרויות | StoryblokProjectionOptions |       |

### toStoryblokComponent

פונקציה **סוג:**

```typescript
function toStoryblokComponent(
  component: ContentComponent,
  options: StoryblokProjectionOptions = {},
): AnalyzedStoryblokComponent;
```

הקרן רכיב תוכן ניטרלי על האובייקט והשדות של רכיב Storyblok שלו.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| רכיב     | ContentComponent           |       |
| אפשרויות | StoryblokProjectionOptions |       |

## `src/runtime/solid`

### StoryblokComponent

פונקציה **סוג:**

```typescript
function StoryblokComponent(
  properties: StoryblokComponentProperties,
): JSX.Element;
```

לא סופק תיאור.

#### פרמטרים

| שם    | הקלד                         | תיאור |
| ----- | ---------------------------- | ----- |
| נכסים | StoryblokComponentProperties |       |

### StoryblokComponentProperties

**סוג:** ממשק

```typescript
export interface StoryblokComponentProperties
```

לא סופק תיאור.

### StoryblokComponentRegistry

**סוג:** סוג

```typescript
export type StoryblokComponentRegistry = Readonly<
  Record<string, StoryblokComponentType>
>;
```

לא סופק תיאור.

### StoryblokComponentType

**סוג:** סוג

```typescript
export type StoryblokComponentType = (properties:
```

לא סופק תיאור.

### StoryblokProvider

פונקציה **סוג:**

```typescript
function StoryblokProvider(
  properties: StoryblokProviderProperties,
): JSX.Element;
```

לא סופק תיאור.

#### פרמטרים

| שם    | הקלד                        | תיאור |
| ----- | --------------------------- | ----- |
| נכסים | StoryblokProviderProperties |       |

### StoryblokProviderProperties

**סוג:** ממשק

```typescript
export interface StoryblokProviderProperties
```

לא סופק תיאור.

## `src/storyblok`

### forgeStoryblokCms

פונקציה **סוג:**

```typescript
function forgeStoryblokCms(options: ForgeStoryblokCmsOptions): CmsOutputPlugin;
```

קשר את הקרנת Storyblok לתוסף פלט מסגרת בבעלות מתקשר.

#### פרמטרים

| שם       | הקלד                     | תיאור |
| -------- | ------------------------ | ----- |
| אפשרויות | ForgeStoryblokCmsOptions |       |

### ForgeStoryblokCmsOptions

**סוג:** ממשק

```typescript
export interface ForgeStoryblokCmsOptions
```

אפשרויות עבור {@link forgeStoryblokCms}.

### forgeStoryblokCmsTargets

פונקציה **סוג:**

```typescript
function forgeStoryblokCmsTargets(
  options: ForgeStoryblokCmsTargetsOptions,
): CmsOutputPlugin[];
```

לא סופק תיאור.

#### פרמטרים

| שם       | הקלד                            | תיאור |
| -------- | ------------------------------- | ----- |
| אפשרויות | ForgeStoryblokCmsTargetsOptions |       |

### ForgeStoryblokCmsTargetsOptions

**סוג:** ממשק

```typescript
export interface ForgeStoryblokCmsTargetsOptions
```

לא סופק תיאור.

## `src/types`

### שדה ניתוח

**סוג:** ממשק

```typescript
export interface AnalyzedField
```

אבזר אחד מנותח, נושא גם את שדה הסכימה שלו וגם את הנתונים הדרושים לחיווט עטיפה.

### AnalyzedStoryblokComponent

**סוג:** ממשק

```typescript
export interface AnalyzedStoryblokComponent
```

התוצאה המלאה של ניתוח רכיב ניטרלי אחד עבור Storyblok.

### StoryblokComponentNames

**סוג:** סוג

```typescript
export type StoryblokComponentNames = ContentComponentNamesInput;
```

זיהוי שמות של רכיב ניטרלי שהתגלה.

השמות עצמם נגזרים על ידי מודל התוכן המשותף; הכינוי הזה
שומר על החתימה הפונה ל-Storyblok קריא.

### StoryblokFieldType

**סוג:** סוג

```typescript
export type StoryblokFieldType =
  | "text"
  | "richtext"
  | "number"
  | "boolean"
  | "option"
  | "asset"
  | "multilink"
  | "bloks"
  | "tab"
  | "plugin";
```

שדה Storyblok מקליד את מפת סוגי התוכן הנייטרליים.

### StoryblokMetadataOptions

**סוג:** ממשק

```typescript
export interface StoryblokMetadataOptions
```

ברירות מחדל אופציונליות של עורך Storyblok המסופקות על ידי מתקשר למטרה.

### StoryblokPluginFieldOptions

**סוג:** ממשק

```typescript
export interface StoryblokPluginFieldOptions
```

תצורה עבור חוזה שדה סכימה מגובת הפלאגין של Storyblok.

### StoryblokProjectionOptions

**סוג:** ממשק

```typescript
export interface StoryblokProjectionOptions
```

אפשרויות משותפות לכל הקרנה של רכיב ניטרלי על Storyblok.

### StoryblokSchemaField

**סוג:** ממשק

```typescript
export interface StoryblokSchemaField
```

שדה סכמת Storyblok (הערך של ערך אחד ברכיב של רכיב `schema`).

## `src/wrappers`

### emitBlokDataType

פונקציה **סוג:**

```typescript
function emitBlokDataType(analyzed: AnalyzedStoryblokComponent): string;
```

בנה את המודפס `blok` סוג אביזר עבור עטיפה - `SbBlokData & { … }` עם אחד
חבר לכל שדה סכימה מנותח (ליטרל-איחוד `option`s, `SbBlokData[]` עבור
קינון `bloks`), כך שהצרכנים מקבלים ממשק מדויק במקום פתוח
`SbBlokData & Record<string, unknown>`. רכיב חסר שדה מתדרדר ל-
חשוף `SbBlokData`.

#### פרמטרים

| שם    | הקלד                       | תיאור |
| ----- | -------------------------- | ----- |
| מנותח | AnalyzedStoryblokComponent |       |

### emitStoryblokBlokWrapper

פונקציה **סוג:**

```typescript
function emitStoryblokBlokWrapper(
  analyzed: AnalyzedStoryblokComponent,
  publicName: string,
  options: StoryblokBlokWrapperOptions,
): string;
```

פליט את מקור ה-framework block לעטוף שקושר את Storyblok's `blok` נשען על
רכיב המסגרת הבנויה: א React/Solid `.tsx` מודול, א Vue/Svelte SFC,
או רכיב אינטרנט מקורי (`.ts`) אלמנט מותאם אישית.

#### פרמטרים

| שם         | הקלד                        | תיאור |
| ---------- | --------------------------- | ----- |
| מנותח      | AnalyzedStoryblokComponent  |       |
| publicName | מחרוזת                      |       |
| אפשרויות   | StoryblokBlokWrapperOptions |       |

### StoryblokBlokWrapperOptions

**סוג:** ממשק

```typescript
export interface StoryblokBlokWrapperOptions
```

אפשרויות עבור {@link emitStoryblokBlokWrapper}.
