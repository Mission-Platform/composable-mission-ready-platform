# @mission-platform/forge-cms-plugin-api API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> packages/compiler/plugins/forge-cms-plugin-api/docs/reference/generated/api.md: [packages/compiler/plugins/forge-cms-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

공개 소스 선언에서 생성됨 `@mission-platform/forge-cms-plugin-api`.

## `src/__fixtures__/components`

### 배지

**종류:** 상수

```typescript
export const BADGE;
```

리터럴 유니온 옵션, JSDoc 설명 및 `?? literal` 기본값.

### 배지이름

**종류:** 상수

```typescript
export const badgeNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 단추

**종류:** 상수

```typescript
export const BUTTON;
```

텍스트와 드롭된 콜백 소품으로 저하되는 공용체입니다.

### 버튼이름

**종류:** 상수

```typescript
export const buttonNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 계수기

**종류:** 상수

```typescript
export const COUNTER;
```

대화형 구성 요소: 상태 및 이벤트 핸들러는 수화된 섬을 강제합니다.

### 카운터 이름

**종류:** 상수

```typescript
export const counterNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 비어 있는

**종류:** 상수

```typescript
export const EMPTY;
```

소품이 전혀 없는 구성 요소입니다.

### 빈이름

**종류:** 상수

```typescript
export const emptyNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 그리드

**종류:** 상수

```typescript
export const GRID;
```

숫자 기본값이 있는 숫자 소품입니다.

### 그리드 이름

**종류:** 상수

```typescript
export const gridNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 공들여 나열한 것

**종류:** 상수

```typescript
export const LAYOUT;
```

기본 슬롯 옆에 명명된 슬롯입니다.

### 레이아웃 이름

**종류:** 상수

```typescript
export const layoutNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### 필수의

**종류:** 상수

```typescript
export const REQUIRED;
```

선택적인 소품과 함께 필수(선택 사항이 아닌) 소품입니다.

### 필수이름

**종류:** 상수

```typescript
export const requiredNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

### SITE_HEADER

**종류:** 상수

```typescript
export const SITE_HEADER;
```

사이트 전체 설정 소품이 있는 구성 요소입니다.

### 사이트헤더이름

**종류:** 상수

```typescript
export const siteHeaderNames: ContentComponentNamesInput;
```

설명이 제공되지 않았습니다.

## `src/__fixtures__/framework`

### 스텁프레임워크

**종류:** 기능

```typescript
function stubFramework(id = "react"): FrameworkOutputPlugin;
```

재정의 가능한 ID를 사용하여 스텁 프레임워크 플러그인을 만듭니다.

#### 매개변수

| 이름   | 유형 | 설명 |
| ------ | ---- | ---- |
| 아이디 |      |      |

## `src/analyze`

### 분석콘텐츠구성요소

**종류:** 기능

```typescript
function analyzeContentComponent(
  sourceFile: ts.SourceFile,
  names: ContentComponentNamesInput,
  semantic?: SemanticModule,
): ContentComponent;
```

중립 구성 요소 모듈을 분석하여 플랫폼 중립 콘텐츠를 반환합니다.
모델: 순서대로 작성 가능한 필드, 렌더링하는 슬롯 및 필요한지 여부
수화된 런타임.

#### 매개변수

| 이름     | 유형                      | 설명 |
| -------- | ------------------------- | ---- |
| 소스파일 | ts.소스파일               |      |
| 이름     | ContentComponentNames입력 |      |
| 의미     | 의미 모듈                 |      |

### CMS_COLOR_TAG

**종류:** 상수

```typescript
export const CMS_COLOR_TAG;
```

설명이 제공되지 않았습니다.

### CMS_COLOUR_TAG

**종류:** 상수

```typescript
export const CMS_COLOUR_TAG;
```

소스 주석에는 영국식 철자가 허용됩니다.

### CMS_ICON_TAG

**종류:** 상수

```typescript
export const CMS_ICON_TAG;
```

선택적 구성 요소 수준 편집기 메타데이터에 대한 JSDoc 태그입니다.

### CMS_SETTING_TAG

**종류:** 상수

```typescript
export const CMS_SETTING_TAG;
```

소품을 사이트 전체 CMS 설정으로 승격시키는 JSDoc 태그입니다.

### CMS_TAB_TAG

**종류:** 상수

```typescript
export const CMS_TAB_TAG;
```

편집기 탭에 소품을 할당하는 JSDoc 태그입니다.

### DEFAULT_SLOT_FIELD

**종류:** 상수

```typescript
export const DEFAULT_SLOT_FIELD;
```

필드 키는 기본 슬롯(구성요소의 `children`) 아래에 노출됩니다.

### 파생 콘텐츠 구성 요소 이름

**종류:** 기능

```typescript
function deriveContentComponentNames(
  names: ContentComponentNamesInput,
): ContentComponentNames;
```

호출자가 제공한 하위 집합에서 전체 이름 집합을 파생시킵니다.

#### 매개변수

| 이름 | 유형                      | 설명 |
| ---- | ------------------------- | ---- |
| 이름 | ContentComponentNames입력 |      |

### isInteractive모듈

**종류:** 기능

```typescript
function isInteractiveModule(semantic: SemanticModule | undefined): boolean;
```

중립 IR이 실제 런타임에서만 제공할 수 있는 동작을 수행하는 경우 참입니다.

#### 매개변수

| 이름 | 유형                       | 설명 |
| ---- | -------------------------- | ---- |
| 의미 | 의미 모듈 \| 정의되지 않음 |      |

## `src/classify`

### ASSET_TYPE_REFERENCES

**종류:** 상수

```typescript
export const ASSET_TYPE_REFERENCES: ReadonlySet<string>;
```

소품을 미디어 자산으로 표시하는 참조를 입력하세요.

### 분류된 필드 종류

**종류:** 유형

```typescript
export type ClassifiedFieldKind = ContentFieldKind | undefined;
```

prop을 분류한 결과(`undefined` → 소품을 떨어뜨립니다).

### 분류 유형

**종류:** 기능

```typescript
function classifyType(
  node: ts.TypeNode,
  aliases: Map<string, ts.TypeNode>,
  seen: Set<string> = new Set(),
): ClassifiedFieldKind;
```

소품 유형 분류 node 중립적인 콘텐츠 종류로 변환하여 로컬 별칭/공용체를 해결합니다.

#### 매개변수

| 이름 | 유형                      | 설명 |
| ---- | ------------------------- | ---- |
| node | ts.TypeNode               |      |
| 별칭 | 지도<string, ts.TypeNode> |      |
| 본   | 세트<string>              |      |

### CollectTypeAliases

**종류:** 기능

```typescript
function collectTypeAliases(
  sourceFile: ts.SourceFile,
): Map<string, ts.TypeNode>;
```

모든 최상위 수준 매핑 `type X = …` 해당 유형에 대한 별칭 node, 노조 결의를 위해.

#### 매개변수

| 이름     | 유형        | 설명 |
| -------- | ----------- | ---- |
| 소스파일 | ts.소스파일 |      |

### LINK_TYPE_REFERENCES

**종류:** 상수

```typescript
export const LINK_TYPE_REFERENCES: ReadonlySet<string>;
```

소품을 링크/URL로 표시하는 참조를 입력하세요.

### RICHTEXT_TYPE_REFERENCES

**종류:** 상수

```typescript
export const RICHTEXT_TYPE_REFERENCES: ReadonlySet<string>;
```

소품을 일반 텍스트가 아닌 서식 있는 서식 있는 텍스트로 표시하는 참조를 입력하세요.

### SLOT_TYPE_REFERENCES

**종류:** 상수

```typescript
export const SLOT_TYPE_REFERENCES: ReadonlySet<string>;
```

props가 중첩된 콘텐츠로 처리되는 유형 참조(`children`).

## `src/cms`

### Cms아티팩트

**종류:** 인터페이스

```typescript
export interface CmsArtifact
```

대상이 드라이버에 쓰기를 요청하는 단일 파일입니다.

### Cms아티팩트 종류

**종류:** 유형

```typescript
export type CmsArtifactKind =
  "schema" | "template" | "manifest" | "entry" | "declaration" | "module";
```

방출된 아티팩트가 대상의 출력 트리에서 수행하는 역할입니다.

### CmsIsland전략

**종류:** 유형

```typescript
export type CmsIslandStrategy = "none" | "framework";
```

대상이 구성 요소에 대한 대화형 런타임을 얻는 방법입니다.

### Cms출력플러그인

**종류:** 인터페이스

```typescript
export interface CmsOutputPlugin
```

구성 가능한 CMS 출력 플러그인입니다.

### CmsTargetContext

**종류:** 인터페이스

```typescript
export interface CmsTargetContext
```

이미터가 실행되는 빌드에 대해 필요할 수 있는 모든 것입니다.

### cms대상디렉토리

**종류:** 기능

```typescript
function cmsTargetDirectory(plugin: CmsOutputPlugin): string;
```

대상의 프레임워크별 출력이 기록되는 디렉터리 세그먼트입니다.

#### 매개변수

| 이름     | 유형            | 설명 |
| -------- | --------------- | ---- |
| 플러그인 | Cms출력플러그인 |      |

### Cms대상 ID

**종류:** 유형

```typescript
export type CmsTargetId = "storyblok" | "astro" | "ghost" | "jekyll" | "webflow" | (string &
```

내장된 CMS 대상 ID는 추가 문자열 ID에 열려 있습니다.

### 정의ForgeCms플러그인

**종류:** 기능

```typescript
function defineForgeCmsPlugin(plugin: T): T;
```

CMS 출력 플러그인을 검증하고 반환합니다.

구성 시 오류가 발생합니다. 잘못 선언된 대상은 절대로 발생해서는 안 됩니다.
드라이버에 도달하고 부분 트리를 자동으로 내보냅니다.

#### 매개변수

| 이름     | 유형 | 설명 |
| -------- | ---- | ---- |
| 플러그인 | 티   |      |

## `src/config`

### 정의ViteForgeCms라이브러리

**종류:** 기능

```typescript
function defineViteForgeCmsLibrary(
  options: ViteForgeCmsLibraryOptions,
): UserConfig;
```

만들기 Vite 하나의 CMS 대상에 대한 라이브러리 구성.

#### 매개변수

| 이름 | 유형                       | 설명 |
| ---- | -------------------------- | ---- |
| 옵션 | ViteForgeCmsLibraryOptions |      |

### ViteForgeCmsLibrary옵션

**종류:** 인터페이스

```typescript
export interface ViteForgeCmsLibraryOptions
```

{@link DefineViteForgeCmsLibrary}에 대한 옵션입니다.

## `src/content-model`

### 콘텐츠 구성 요소

**종류:** 인터페이스

```typescript
export interface ContentComponent
```

중립 콘텐츠 모델에 프로젝션된 구성 요소입니다.

### 콘텐츠구성요소메타데이터

**종류:** 인터페이스

```typescript
export interface ContentComponentMetadata
```

구성 요소에 주석이 달린 선택적 플랫폼 중립 메타데이터입니다.

### ContentComponentNames

**종류:** 인터페이스

```typescript
export interface ContentComponentNames
```

콘텐츠 플랫폼이 요구할 수 있는 모든 이름은 하나의 구성 요소에서 파생됩니다.

### ContentComponentNames입력

**종류:** 유형

```typescript
export type ContentComponentNamesInput = Pick<
  ContentComponentNames,
  "neutralName" | "publicName"
> &
  Partial<
    Pick<ContentComponentNames, "folder" | "propertiesType" | "sourceDir">
  >;
```

호출자가 제공하는 {@link ContentComponentNames}의 하위 집합입니다. 나머지는 파생됩니다.

### 콘텐츠기본값

**종류:** 유형

```typescript
export type ContentDefaultValue = string | number | boolean;
```

소품이 전달할 수 있는 리터럴 기본값입니다.

### 콘텐츠 필드

**종류:** 인터페이스

```typescript
export interface ContentField
```

구성 요소 소품에서 투영된 단일 작성 가능 필드입니다.

### 콘텐츠 필드 종류

**종류:** 유형

```typescript
export type ContentFieldKind = |
```

소품 종류의 콘텐츠 TypeScript 유형이 매핑됩니다.

`undefined` 여기서는 모델링된 적이 없습니다.
콘텐츠 필드(콜백, 불투명 객체)는 단순히
반환하는 분류기 `undefined` 종류 대신.

### 콘텐츠 필드 종류 이름

**종류:** 유형

```typescript
export type ContentFieldKindName = ContentFieldKind["kind"];
```

{@link ContentFieldKind}의 판별자입니다.

### 콘텐츠 필드

**종류:** 기능

```typescript
function contentFields(component: ContentComponent): readonly ContentField[];
```

슬롯이 아닌 구성 요소의 필드(스칼라, 작성 가능한 콘텐츠)입니다.

#### 매개변수

| 이름     | 유형            | 설명 |
| -------- | --------------- | ---- |
| 구성요소 | 콘텐츠 구성요소 |      |

### 슬롯필드

**종류:** 기능

```typescript
function slotFields(component: ContentComponent): readonly ContentField[];
```

중첩된 콘텐츠를 전달하는 구성 요소의 필드입니다.

#### 매개변수

| 이름     | 유형            | 설명 |
| -------- | --------------- | ---- |
| 구성요소 | 콘텐츠 구성요소 |      |

## `src/driver`

### generateCms아티팩트

**종류:** 기능

```typescript
function generateCmsArtifacts(
  options: GenerateCmsArtifactsOptions,
): GeneratedCmsTree;
```

하나의 CMS에 대해 전체 검색 → IR → 콘텐츠 모델 → 방출 → 쓰기 루프를 실행합니다.
target, 항목 모듈 경로와 작성된 모든 내용을 반환합니다.

#### 매개변수

| 이름 | 유형                 | 설명 |
| ---- | -------------------- | ---- |
| 옵션 | 생성CmsArtifacts옵션 |      |

### 생성Cms아티팩트옵션

**종류:** 인터페이스

```typescript
export interface GenerateCmsArtifactsOptions
```

{@link generateCmsArtifacts}에 대한 옵션입니다.

### 생성된Cms트리

**종류:** 인터페이스

```typescript
export interface GeneratedCmsTree
```

한 번의 목표 실행으로 생산된 모든 것.

## `src/island`

### 생성된섬

**종류:** 인터페이스

```typescript
export interface GeneratedIsland
```

생성된 섬 나무 또는 `undefined` 대상이 섬을 사용하지 않는 경우.

### 생성섬

**종류:** 기능

```typescript
function generateIsland(
  options: GenerateIslandOptions,
): GeneratedIsland | undefined;
```

대상에 대한 프레임워크 아일랜드 트리를 공동 생성하고 지정자를 반환합니다.
방출된 템플릿은 이를 가져와야 합니다.

#### 매개변수

| 이름 | 유형         | 설명 |
| ---- | ------------ | ---- |
| 옵션 | 섬 생성 옵션 |      |

### 섬 생성옵션

**종류:** 인터페이스

```typescript
export interface GenerateIslandOptions
```

{@link generateIsland}에 대한 옵션입니다.

### ISLAND_DIRECTORY

**종류:** 상수

```typescript
export const ISLAND_DIRECTORY;
```

대상 출력 디렉터리에 상대적인 디렉터리 이름, 섬이 생성됩니다.

## `src/names`

### 표시 이름

**종류:** 기능

```typescript
function toDisplayName(publicName: string): string;
```

공개 이름을 표시 이름(`InView` → `In View`).

#### 매개변수

| 이름     | 유형   | 설명 |
| -------- | ------ | ---- |
| 공개이름 | 문자열 |      |

### to케밥이름

**종류:** 기능

```typescript
function toKebabName(publicName: string): string;
```

공개 이름을 케밥 형식의 이름으로 변환합니다(`InView` → `in-view`).

#### 매개변수

| 이름     | 유형   | 설명 |
| -------- | ------ | ---- |
| 공개이름 | 문자열 |      |

### 기술 이름

**종류:** 기능

```typescript
function toTechnicalName(publicName: string): string;
```

공개 이름을 기술 이름(`InView` → `in_view`).

#### 매개변수

| 이름     | 유형   | 설명 |
| -------- | ------ | ---- |
| 공개이름 | 문자열 |      |

## `src/tsdown`

### cms캐시디렉터리

**종류:** 기능

```typescript
function cmsCacheDirectory(rootDir: string, target: CmsOutputPlugin): string;
```

대상의 생성된 트리가 기록되는 캐시 디렉터리입니다.

#### 매개변수

| 이름       | 유형            | 설명 |
| ---------- | --------------- | ---- |
| 루트디렉터 | 문자열          |      |
| 목표       | Cms출력플러그인 |      |

### cms출력디렉토리

**종류:** 기능

```typescript
function cmsOutputDirectory(rootDir: string, target: CmsOutputPlugin): string;
```

대상의 프레임워크별 모듈이 내보내지는 배포 디렉터리입니다.

#### 매개변수

| 이름       | 유형            | 설명 |
| ---------- | --------------- | ---- |
| 루트디렉터 | 문자열          |      |
| 목표       | Cms출력플러그인 |      |

### 정의TsdownForgeCms

**종류:** 기능

```typescript
function defineTsdownForgeCms(options: TsdownForgeCmsOptions): UserConfig;
```

하나의 CMS 대상에 대한 tsdown 구성을 만듭니다.

#### 매개변수

| 이름 | 유형               | 설명 |
| ---- | ------------------ | ---- |
| 옵션 | TsdownForgeCms옵션 |      |

### 정의TsdownForgeCmsAll

**종류:** 기능

```typescript
function defineTsdownForgeCmsAll(
  options: TsdownForgeCmsAllOptions,
): UserConfig[];
```

요청된 모든 CMS 대상에 대해 tsdown 구성을 만듭니다.

#### 매개변수

| 이름 | 유형                     | 설명 |
| ---- | ------------------------ | ---- |
| 옵션 | TsdownForgeCmsAllOptions |      |

### 해결구성요소모듈

**종류:** 기능

```typescript
function resolveComponentsModule(rootDir: string, explicit?: string): string;
```

패키지의 중립 구성 요소 배럴을 찾습니다.

#### 매개변수

| 이름       | 유형   | 설명 |
| ---------- | ------ | ---- |
| 루트디렉터 | 문자열 |      |
| 명시적인   | 문자열 |      |

### TsdownForgeCmsAllOptions

**종류:** 인터페이스

```typescript
export interface TsdownForgeCmsAllOptions
```

{@link DefineTsdownForgeCmsAll}에 대한 옵션입니다.

### TsdownForgeCms옵션

**종류:** 인터페이스

```typescript
export interface TsdownForgeCmsOptions
```

{@link DefineTsdownForgeCms}에 대한 옵션입니다.
