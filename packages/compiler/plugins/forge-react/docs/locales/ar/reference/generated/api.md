# @mission-platform/forge-plugin-react مرجع واجهة برمجة التطبيقات

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/compiler/plugins/forge-react/docs/reference/generated/api.md: [packages/compiler/plugins/forge-react/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-plugin-react`.

## `src/emitters/module`

### emitReactModule

**النوع:** الوظيفة

```typescript
function emitReactModule(
  module: SemanticModule,
  componentName?: string,
  plan: ReactModulePlan = planReactModule(module, componentName),
): string;
```

تحويل الوحدة بأكملها إلى React مصدر الهدف.

#### حدود

| الاسم          | اكتب            | الوصف |
| -------------- | --------------- | ----- |
| الوحدة النمطية | الوحدة الدلالية |       |
| اسم المكون     | سلسلة           |       |
| خطة            | ReactModulePlan |       |

## `src/index`

### forgeReactFramework

**النوع:** الوظيفة

```typescript
function forgeReactFramework(): FrameworkOutputPlugin;
```

إنشاء React البرنامج المساعد للإخراج ومحولات بناء JSX الأصلية الخاصة به.

## `src/lower`

### isReactLowered

**النوع:** الوظيفة

```typescript
function isReactLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is ReactLoweredModule;
```

تضييق نطاق الخطة المملوكة للهدف إلى React واحد.

#### حدود

| الاسم | اكتب                            | الوصف |
| ----- | ------------------------------- | ----- |
| خفضت  | TargetLoweredModule \| غير محدد |       |

### LowerReactModule

**النوع:** الوظيفة

```typescript
function lowerReactModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

قم بخفض الأشعة تحت الحمراء المحايدة إلى React الخطة التي تنفذها {@link TargetIntentions.lowered}.

#### حدود

| الاسم              | اكتب            | الوصف |
| ------------------ | --------------- | ----- |
| الأشعة تحت الحمراء | الوحدة الدلالية |       |
| السياق             | سياق الهدف      |       |

### PlanReactModule

**النوع:** الوظيفة

```typescript
function planReactModule(
  module: SemanticModule,
  componentName?: string,
): ReactModulePlan;
```

بناء React خطة لوحدة واحدة.

#### حدود

| الاسم          | اكتب            | الوصف |
| -------------- | --------------- | ----- |
| الوحدة النمطية | الوحدة الدلالية |       |
| اسم المكون     | سلسلة           |       |

### ReactClientDirectivePlan

**النوع:** الواجهة

```typescript
export interface ReactClientDirectivePlan
```

ما إذا كان يجب الاشتراك في الوحدة أم لا Reactوقت تشغيل العميل.

### ReactHookPlan

**النوع:** الواجهة

```typescript
export interface ReactHookPlan
```

كل خطاف يحتاجه المكون المنخفض.

### ReactI18nPlan

**النوع:** الواجهة

```typescript
export interface ReactI18nPlan
```

كيف محايدة `i18next` خرائط الاستخدام على Reactربط الترجمة.

### ReactImportPlan

**النوع:** الواجهة

```typescript
export interface ReactImportPlan
```

بالضبط ما هي الروابط التي تستوردها الوحدة التي تم إنشاؤها ومن أين.

### ReactLoweredModule

**النوع:** الواجهة

```typescript
export interface ReactLoweredModule extends TargetLoweredModule
```

ال React الهدف `lowered` العقد، يتم التمييز فيه على {@link REACT_FRAMEWORK}.

### ReactModulePlan

**النوع:** الواجهة

```typescript
export interface ReactModulePlan
```

كل React القرار المتخذ لوحدة واحدة.

### ReactPropPlan

**النوع:** الواجهة

```typescript
export interface ReactPropPlan
```

A React prop، مع نوعه الذي تم حله والاختيارية.

### ReactPropsParameter

**النوع:** الواجهة

```typescript
export interface ReactPropsParameter
```

كيف يربط المكون كائن الدعائم الخاص به.

## `src/optimize`

### optimReactModule

**النوع:** الوظيفة

```typescript
function optimizeReactModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

صقل React يخطط.

الرفع الثابت واختيار المفتاح المستقر مملوكان للأعلام المحايدة التي
أنتجت مدخلاتها: مع `staticMarking` تعطيل بقاء الأشجار الفرعية المحددة
مضمنة، ومع `stableKeyInference` يتم الاحتفاظ بتعطيل كل مفتاح مستنتج.

#### حدود

| الاسم  | اكتب                 | الوصف |
| ------ | -------------------- | ----- |
| نوايا  | النوايا المستهدفة    |       |
| خيارات | تارجتوبتيميزيوبتيونس |       |

### رد الفعل_التحسينات

**النوع:** ثابت

```typescript
export const REACT_OPTIMIZATIONS;
```

المعرفات المسجلة على خطة React تم تحسين المحسن.
