# Referencia de API @mission-platform/forge-plugin-api

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/compiler/plugins/forge-plugin-api/docs/reference/generated/api.md: [packages/compiler/plugins/forge-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/forge-plugin-api`.

## `src/compiler/ast`

### aplicar fuenteEdits

**Tipo:** función

```typescript
function applySourceEdits(source: string, edits: readonly SourceEdit[]): string;
```

Aplique ediciones de fuentes que no se superpongan sin exponer los nodos del analizador.

#### Parámetros

| Nombre    | Tipo                           | Descripción |
| --------- | ------------------------------ | ----------- |
| fuente    | cadena                         |             |
| ediciones | FuenteEditar[] de sólo lectura |             |

### CLASS_NAME_ATTRIBUTE

**Tipo:** constante

```typescript
export const CLASS_NAME_ATTRIBUTE;
```

No se proporciona descripción.

### COMPONENTES_JSX_MODULES

**Tipo:** constante

```typescript
export const COMPONENTS_JSX_MODULES;
```

No se proporciona descripción.

### nombredeleventoparapropiedad

**Tipo:** función

```typescript
function eventNameForProperty(propertyName: string): string;
```

Derive el nombre del evento representado por un accesorio `on<Event>` de estilo Vue.

#### Parámetros

| Nombre              | Tipo   | Descripción |
| ------------------- | ------ | ----------- |
| nombre de propiedad | cadena |             |

### ICONOS_JSX_MODULE

**Tipo:** constante

```typescript
export const ICONS_JSX_MODULE;
```

No se proporciona descripción.

### JSX_ATTRIBUTE_RENAMES

**Tipo:** constante

```typescript
export const JSX_ATTRIBUTE_RENAMES: ReadonlyMap<string, string>;
```

No se proporciona descripción.

### LOCAL_EFFECT_FILE

**Tipo:** constante

```typescript
export const LOCAL_EFFECT_FILE;
```

No se proporciona descripción.

### LOCAL_EFFECT_MODULE

**Tipo:** constante

```typescript
export const LOCAL_EFFECT_MODULE;
```

No se proporciona descripción.

### LOCAL_JSX_TYPE_NAMES

**Tipo:** constante

```typescript
export const LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>;
```

No se proporciona descripción.

### LOCAL_JSX_TYPES_FILE

**Tipo:** constante

```typescript
export const LOCAL_JSX_TYPES_FILE;
```

No se proporciona descripción.

### LOCAL_JSX_TYPES_MODULE

**Tipo:** constante

```typescript
export const LOCAL_JSX_TYPES_MODULE;
```

No se proporciona descripción.

### localEffectModuleSource

**Tipo:** función

```typescript
function localEffectModuleSource(framework: JsxFramework): string;
```

Emita el efecto auxiliar compartido Vue; otros objetivos no necesitan este archivo.

#### Parámetros

| Nombre | Tipo      | Descripción |
| ------ | --------- | ----------- |
| marco  | Marco Jsx |             |

### localJsxTypesModuleSource

**Tipo:** función

```typescript
function localJsxTypesModuleSource(framework: JsxFramework): string;
```

Emita declaraciones primitivas JSX locales del marco sin dependencias del analizador.

#### Parámetros

| Nombre | Tipo      | Descripción |
| ------ | --------- | ----------- |
| marco  | Marco Jsx |             |

### NEUTRAL_COMPILE_TIME_MARKERS

**Tipo:** constante

```typescript
export const NEUTRAL_COMPILE_TIME_MARKERS: ReadonlySet<string>;
```

No se proporciona descripción.

### NEUTRAL_CONTEXT_VALUES

**Tipo:** constante

```typescript
export const NEUTRAL_CONTEXT_VALUES: ReadonlySet<string>;
```

No se proporciona descripción.

### NEUTRAL_FRAMEWORK_COMPONENTS

**Tipo:** constante

```typescript
export const NEUTRAL_FRAMEWORK_COMPONENTS: ReadonlySet<string>;
```

No se proporciona descripción.

### MÓDULO_NEUTRAL

**Tipo:** constante

```typescript
export const NEUTRAL_MODULE;
```

No se proporciona descripción.

### NEUTRAL_RUNTIME_VALUES

**Tipo:** constante

```typescript
export const NEUTRAL_RUNTIME_VALUES: ReadonlySet<string>;
```

No se proporciona descripción.

### NEUTRAL_VUE_RUNTIME_HOOKS

**Tipo:** constante

```typescript
export const NEUTRAL_VUE_RUNTIME_HOOKS: ReadonlySet<string>;
```

No se proporciona descripción.

### REACT_ADAPTER_MODULE

**Tipo:** constante

```typescript
export const REACT_ADAPTER_MODULE;
```

No se proporciona descripción.

### REACT_TYPE_ALIASES

**Tipo:** constante

```typescript
export const REACT_TYPE_ALIASES: Readonly<Record<string, string>>;
```

No se proporciona descripción.

### Fuente

**Tipo:** interfaz

```typescript
export interface SourceEdit
```

Una edición de fuente estable, ordenada por desplazamientos en la fuente original.

### Importar estilo

**Tipo:** interfaz

```typescript
export interface StyleImport
```

Una importación de hoja de estilo llevada a un árbol plano generado.

### VUE_ADAPTER_MODULE

**Tipo:** constante

```typescript
export const VUE_ADAPTER_MODULE;
```

No se proporciona descripción.

### VUE_BUILTIN_COMPONENTS

**Tipo:** constante

```typescript
export const VUE_BUILTIN_COMPONENTS: ReadonlySet<string>;
```

No se proporciona descripción.

### VUE_LOCAL_JSX_TYPE_NAMES

**Tipo:** constante

```typescript
export const VUE_LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>;
```

No se proporciona descripción.

## `src/compiler/hoist-static`

### Resultado de elevación genérico

**Tipo:** interfaz

```typescript
export interface GenericHoistResult
```

No se proporciona descripción.

### Entrada estática elevada

**Tipo:** interfaz

```typescript
export interface HoistedStaticEntry
```

No se proporciona descripción.

### polipastoEstáticoRenderNodos

**Tipo:** función

```typescript
function hoistStaticRenderNodes(module: GenericModuleAst): GenericHoistResult;
```

Levante los registros de renderizado marcados y devuelva las entradas para el generador de destino.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| módulo | Módulo genéricoAst |             |

### MP_HOIST_PREFIX

**Tipo:** constante

```typescript
export const MP_HOIST_PREFIX;
```

No se proporciona descripción.

## `src/compiler/optimize`

### aplicar optimizaciónEditar

**Tipo:** función

```typescript
function applyOptimizationEdits(
  module: GenericModuleAst,
  edits: readonly SourceEdit[],
): GenericModuleAst;
```

Aplique ediciones de código fuente a un código fuente de módulo sin exponer un tipo de implementación de AST.

#### Parámetros

| Nombre    | Tipo                           | Descripción |
| --------- | ------------------------------ | ----------- |
| módulo    | Módulo genéricoAst             |             |
| ediciones | FuenteEditar[] de sólo lectura |             |

### constante booleano

**Tipo:** función

```typescript
function constantBoolean(expression: ExpressionLike): boolean | undefined;
```

Resuelva un booleano literal sin evaluar el código creado.

#### Parámetros

| Nombre    | Tipo           | Descripción |
| --------- | -------------- | ----------- |
| expresión | Expresión como |             |

### Resultado de optimización genérico

**Tipo:** interfaz

```typescript
export interface GenericOptimizationResult
```

No se proporciona descripción.

### tieneJsxKey

**Tipo:** función

```typescript
function hasJsxKey(attributes: readonly GenericAttribute[]): boolean;
```

Si una lista de atributos JSX genérica contiene una clave.

#### Parámetros

| Nombre    | Tipo                                | Descripción |
| --------- | ----------------------------------- | ----------- |
| atributos | Atributo genérico de sólo lectura[] |             |

### tieneMpStaticMarker

**Tipo:** función

```typescript
function hasMpStaticMarker(node: GenericRenderNode): boolean;
```

Si un JSX node genérico tiene el marcador estático privado.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |

### esCompileTimeConstant

**Tipo:** función

```typescript
function isCompileTimeConstant(expression: ExpressionLike): boolean;
```

Si el texto fuente es una expresión literal pura en tiempo de compilación.

#### Parámetros

| Nombre    | Tipo           | Descripción |
| --------- | -------------- | ----------- |
| expresión | Expresión como |             |

### MP_STATIC_ATTR

**Tipo:** constante

```typescript
export const MP_STATIC_ATTR;
```

No se proporciona descripción.

### optimizarGenericModule

**Tipo:** función

```typescript
function optimizeGenericModule(
  module: GenericModuleAst,
  _options: OptimizeOptions = {},
): GenericOptimizationResult;
```

Aplique la optimización a nivel de registro y devuelva las ediciones de origen por separado.

#### Parámetros

| Nombre    | Tipo                     | Descripción |
| --------- | ------------------------ | ----------- |
| módulo    | Módulo genéricoAst       |             |
| _opciones | Opciones de optimización |             |

### Opciones de optimización

**Tipo:** interfaz

```typescript
export interface OptimizeOptions
```

No se proporciona descripción.

### stripMpStaticAttributes

**Tipo:** función

```typescript
function stripMpStaticAttributes(
  attributes: readonly GenericAttribute[],
): readonly GenericAttribute[];
```

Devuelve una lista de atributos genéricos sin marcadores.

#### Parámetros

| Nombre    | Tipo                                | Descripción |
| --------- | ----------------------------------- | ----------- |
| atributos | Atributo genérico de sólo lectura[] |             |

### tiraMpStaticMarker

**Tipo:** función

```typescript
function stripMpStaticMarker(node: GenericRenderNode): GenericRenderNode;
```

Devuelve una representación genérica sin marcadores node, preservando recursivamente los intervalos de origen.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |

## `src/diagnostics`

### CompiladorDiagnóstico

**Tipo:** interfaz

```typescript
export interface CompilerDiagnostic
```

Un diagnóstico que conserva suficiente contexto de origen para generar informes a nivel de fase.

### Error de diagnóstico del compilador

**Tipo:** clase

```typescript
export class CompilerDiagnosticError extends Error
```

Se produce un error cuando una fase del compilador no puede producir un módulo de destino sólido.

### CompiladorDiagnósticoSeveridad

**Tipo:** tipo

```typescript
export type CompilerDiagnosticSeverity = "error" | "warning" | "info";
```

Gravedad de un diagnóstico del compilador.

### Fase del compilador

**Tipo:** tipo

```typescript
export type CompilerPhase =
  | "frontend"
  | "ir"
  | "inference"
  | "target-lowering"
  | "optimization"
  | "generation"
  | "build";
```

Fases de la canalización que pueden informar un diagnóstico del compilador.

### crearCompilerDiagnostic

**Tipo:** función

```typescript
function createCompilerDiagnostic(
  diagnostic: Omit<CompilerDiagnostic, "fileName"> & { fileName?: string },
): CompilerDiagnostic;
```

Cree un diagnóstico sin acoplar los contratos de fase a los nodos TypeScript.

#### Parámetros

| Nombre      | Tipo                                                                | Descripción |
| ----------- | ------------------------------------------------------------------- | ----------- |
| diagnóstico | Omitir<CompilerDiagnostic, "fileName"> & {nombredearchivo?: cadena} |             |

### formatoCompilerDiagnostic

**Tipo:** función

```typescript
function formatCompilerDiagnostic(diagnostic: CompilerDiagnostic): string;
```

Formatee un diagnóstico de un error del compilador sin perder su ubicación de origen.

#### Parámetros

| Nombre      | Tipo                  | Descripción |
| ----------- | --------------------- | ----------- |
| diagnóstico | CompiladorDiagnóstico |             |

### throwOnCompilerErrors

**Tipo:** función

```typescript
function throwOnCompilerErrors(
  diagnostics: readonly CompilerDiagnostic[] | undefined,
): void;
```

Cancelar una canalización del compilador cuando una fase informó uno o más errores.

#### Parámetros

| Nombre      | Tipo                                            | Descripción |
| ----------- | ----------------------------------------------- | ----------- |
| diagnóstico | solo lectura CompilerDiagnostic[] \| indefinido |             |

## `src/framework`

### FrameworkBuildAdapters

**Tipo:** interfaz

```typescript
export interface FrameworkBuildAdapters
```

Integraciones de compilación de marcos de tipo independiente.

### FrameworkOutputPlugin

**Tipo:** interfaz

```typescript
export interface FrameworkOutputPlugin
```

Un complemento de salida de marco post-IR componible.

### FrameworkOutputPluginSelección

**Tipo:** tipo

```typescript
export type FrameworkOutputPluginSelection = readonly FrameworkOutputPlugin[];
```

Selección de objetivos propiedad de la persona que llama; La validación rechaza los ID vacíos y duplicados.

### MarcoFuenteMetadatos

**Tipo:** interfaz

```typescript
export interface FrameworkSourceMetadata
```

Convenciones del árbol fuente requeridas por el controlador de compilación neutral del marco.

### Módulo extra generado

**Tipo:** interfaz

```typescript
export interface GeneratedExtraModule
```

Un módulo auxiliar generado escrito junto al módulo generado principal.

### Módulo generado

**Tipo:** interfaz

```typescript
export interface GeneratedModule
```

El artefacto fuente devuelto por un complemento de salida del marco.

### Contextogenerador

**Tipo:** tipo

```typescript
export type GeneratorContext = TargetContext;
```

El contexto pasó a los generadores de origen después de la reducción y optimización del objetivo.

### Marco Jsx

**Tipo:** tipo

```typescript
export type JsxFramework =
  "react" | "vue" | "svelte" | "solid" | "web-components";
```

ID de marco integrados actuales; Los complementos pueden utilizar cualquier ID de cadena adicional.

### Opciones de optimización neutrales

**Tipo:** interfaz

```typescript
export interface NeutralOptimizeOptions
```

Opciones de optimización neutrales compartidas por el compilador y los complementos de destino.

### Idioma de salida

**Tipo:** tipo

```typescript
export type OutputLanguage = "tsx" | "jsx" | "ts" | "vue" | "svelte" | "astro" | (string &
```

Idiomas de salida conocidos por Forge, con un punto de extensión de cadena abierta para nuevos objetivos.

### HostComponenteDestino

**Tipo:** interfaz

```typescript
export interface TargetComponentHost
```

Metadatos de invocación de host neutrales al destino compartidos por referencias de componentes generados.

### Contexto objetivo

**Tipo:** interfaz

```typescript
export interface TargetContext
```

Contexto compartido por reducción y optimización de objetivos.

### Intenciones objetivo

**Tipo:** interfaz

```typescript
export interface TargetIntentions
```

Envoltura de intención específica del objetivo; Los hechos neutrales permanecen disponibles para pases posteriores.

### Módulo objetivo bajado

**Tipo:** interfaz

```typescript
export interface TargetLoweredModule
```

El plan propiedad del objetivo producido por la fase `lower` de un complemento y refinado por su
Fase `optimize`. Cada objetivo declara su propia extensión de este contrato y
lo discrimina en {@link TargetLoweredModule.framework}, por lo que se puede elaborar un plan.
reducido sin casting, mientras que cada complemento aún satisface lo mismo
Forma {@link TargetIntentions}.

### Opciones de optimización de destino

**Tipo:** interfaz

```typescript
export interface TargetOptimizeOptions
```

Las opciones se pasan a un optimizador de destino sin cambiar las opciones públicas neutrales.

### TsdownBuildContext

**Tipo:** interfaz

```typescript
export interface TsdownBuildContext
```

Contexto compartido para paquetes de complementos tsdown/Rolldown propiedad del marco.

### ViteBuildContext

**Tipo:** interfaz

```typescript
export interface ViteBuildContext
```

Contexto compartido para paquetes de complementos Vite propiedad del marco.

## `src/index`

### definirForgeFramework

**Tipo:** constante

```typescript
export const defineForgeFramework;
```

Alias ​​que enfatiza que los paquetes de marco registran implementaciones de destino.

### definirForgeOutputPlugin

**Tipo:** función

```typescript
function defineForgeOutputPlugin(plugin: T): T;
```

Validar y devolver un complemento de salida del marco para que los consumidores lo registren.

#### Parámetros

| Nombre      | Tipo | Descripción |
| ----------- | ---- | ----------- |
| complemento | T    |             |

### ForgePluginId

**Tipo:** tipo

```typescript
export type ForgePluginId = FrameworkOutputPlugin["id"];
```

No se proporciona descripción.

### validarForgeOutputPlugin

**Tipo:** función

```typescript
function validateForgeOutputPlugin(
  plugin: unknown,
): asserts plugin is FrameworkOutputPlugin;
```

Valide los metadatos de destino independientemente de cualquier registro de marco.

#### Parámetros

| Nombre      | Tipo        | Descripción |
| ----------- | ----------- | ----------- |
| complemento | desconocido |             |

### validarForgeOutputPluginSelection

**Tipo:** función

```typescript
function validateForgeOutputPluginSelection(
  plugins: readonly T[],
): readonly T[];
```

Valide una selección de objetivos propiedad de la persona que llama, incluidas las identificaciones vacías y duplicadas.

#### Parámetros

| Nombre       | Tipo             | Descripción |
| ------------ | ---------------- | ----------- |
| complementos | sólo lectura T[] |             |

## `src/ir`

### atributoExpresiónTexto

**Tipo:** función

```typescript
function attributeExpressionText(
  node: GenericRenderNode,
  name: string,
): string | undefined;
```

Leer el texto de expresión de un atributo con nombre, cuando lo contiene.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |
| nombre | cadena                       |             |

### atributoStringValue

**Tipo:** función

```typescript
function attributeStringValue(
  node: GenericRenderNode,
  name: string,
): string | undefined;
```

Leer el valor de cadena estática de un atributo con nombre, cuando lo tenga.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |
| nombre | cadena                       |             |

### Intención de nodo dinámico

**Tipo:** interfaz

```typescript
export interface DynamicNodeIntention
```

Un componente dinámico o selección de elementos.

### EfectoIntención

**Tipo:** interfaz

```typescript
export interface EffectIntention
```

Un ciclo de vida del efecto y su expresión de limpieza opcional.

### EMPTY_SEMANTIC_INTENTIONS

**Tipo:** constante

```typescript
export const EMPTY_SEMANTIC_INTENTIONS: SemanticIntentions;
```

Hechos de intención vacía utilizados por accesorios y módulos sin nada que inferir.

### EMPTY_SPAN

**Tipo:** constante

```typescript
export const EMPTY_SPAN: SourceSpan;
```

Un tramo de ancho cero utilizado por nodos genéricos sintetizados.

### Intención del evento

**Tipo:** interfaz

```typescript
export interface EventIntention
```

Un enlace de evento que puede representarse mediante diferentes sintaxis de atributos de destino.

### encontrarAtributo

**Tipo:** función

```typescript
function findAttribute(
  node: GenericRenderNode,
  name: string,
): GenericJsxAttribute | undefined;
```

Busque el atributo nombrado en un render node.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |
| nombre | cadena                       |             |

### GenéricoAstNode

**Tipo:** tipo

```typescript
export type GenericAstNode =
  GenericImport | GenericStatement | GenericRenderNode;
```

No se proporciona descripción.

### Atributo genérico

**Tipo:** tipo

```typescript
export type GenericAttribute = GenericJsxAttribute | GenericJsxSpreadAttribute;
```

No se proporciona descripción.

### Valor de atributo genérico

**Tipo:** tipo

```typescript
export type GenericAttributeValue = |
```

El valor que lleva un atributo JSX.

### Tipo de enlace genérico

**Tipo:** tipo

```typescript
export type GenericBindingKind =
  "identifier" | "object-pattern" | "array-pattern";
```

La forma vinculante del parámetro de accesorios de un componente.

### Componente genérico

**Tipo:** interfaz

```typescript
export interface GenericComponent
```

La función del componente la interfaz reconoce en un módulo.

### Nodo de expresión genérica

**Tipo:** interfaz

```typescript
export interface GenericExpressionNode
```

Una interpolación `{ … }` en la posición secundaria JSX.

### Importación genérica

**Tipo:** interfaz

```typescript
export interface GenericImport
```

Un hecho de importación neutral al marco.

### AtributoJsxgenérico

**Tipo:** interfaz

```typescript
export interface GenericJsxAttribute
```

Un atributo JSX con nombre (`name`, `name="v"`, `name={expr}`).

### Atributo genérico JsxSpread

**Tipo:** interfaz

```typescript
export interface GenericJsxSpreadAttribute
```

Un atributo de extensión JSX (`{...props}`).

### Módulo genéricoAst

**Tipo:** interfaz

```typescript
export interface GenericModuleAst
```

Representación de fuente serializable compartida por todas las fases del compilador.

### Parámetro genérico

**Tipo:** interfaz

```typescript
export interface GenericParameter
```

Un parámetro de componente, su forma vinculante y su tipo declarado.

### GenéricoRenderNiño

**Tipo:** tipo

```typescript
export type GenericRenderChild =
  GenericRenderNode | GenericTextNode | GenericExpressionNode;
```

No se proporciona descripción.

### Nodo de renderizado genérico

**Tipo:** interfaz

```typescript
export interface GenericRenderNode
```

Un renderizado de marco neutral node; Los renderizadores de destino pueden reducir o conservar su expresión de origen.

### Declaración genérica

**Tipo:** interfaz

```typescript
export interface GenericStatement
```

Una declaración neutral del marco retenida por la interfaz.

### Tipo de declaración genérica

**Tipo:** tipo

```typescript
export type GenericStatementKind =
  | "import"
  | "interface"
  | "type-alias"
  | "enum"
  | "variable"
  | "function"
  | "class"
  | "export"
  | "expression"
  | "return"
  | "other";
```

Clasificación aproximada de una declaración retenida a nivel de módulo.

### Tipo de etiqueta genérica

**Tipo:** tipo

```typescript
export type GenericTagKind = "element" | "component" | "fragment" | "dynamic";
```

Cómo se resuelve la etiqueta de un node renderizado en el vocabulario de destino.

### Nodo de texto genérico

**Tipo:** interfaz

```typescript
export interface GenericTextNode
```

Texto literal entre etiquetas JSX.

### esNodoExpresión

**Tipo:** función

```typescript
function isExpressionNode(
  child: GenericRenderChild,
): child is GenericExpressionNode;
```

Si un hijo renderizado es una interpolación `{ … }`.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| niño   | GenericRenderChild |             |

### esRenderNode

**Tipo:** función

```typescript
function isRenderNode(child: GenericRenderChild): child is GenericRenderNode;
```

Si un elemento secundario de renderizado es un elemento/fragmento anidado node.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| niño   | GenericRenderChild |             |

### esNodoDeTexto

**Tipo:** función

```typescript
function isTextNode(child: GenericRenderChild): child is GenericTextNode;
```

Si un elemento secundario renderizado es texto JSX literal.

#### Parámetros

| Nombre | Tipo               | Descripción |
| ------ | ------------------ | ----------- |
| niño   | GenericRenderChild |             |

### ListaClaveIntención

**Tipo:** interfaz

```typescript
export interface ListKeyIntention
```

Un candidato clave estable para un renderizador de listas de objetivos.

### MemoIntención

**Tipo:** interfaz

```typescript
export interface MemoIntention
```

Un valor memorizado o derivado.

### PropIntención

**Tipo:** interfaz

```typescript
export interface PropIntention
```

Una declaración de accesorios neutral inferida de la firma de un componente o del tipo de accesorios.

### RefIntención

**Tipo:** interfaz

```typescript
export interface RefIntention
```

Una intención de árbitro independiente de la representación de árbitro del objetivo.

### renderNodeTagName

**Tipo:** función

```typescript
function renderNodeTagName(node: GenericRenderNode): string | undefined;
```

El nombre de etiqueta simple de una representación node o `undefined` para una etiqueta calculada.

#### Parámetros

| Nombre | Tipo                         | Descripción |
| ------ | ---------------------------- | ----------- |
| node   | Nodo de renderizado genérico |             |

### Intenciones Semánticas

**Tipo:** interfaz

```typescript
export interface SemanticIntentions
```

Hechos semánticos compartidos inferidos una vez antes de reducir el objetivo.

### Módulo Semántico

**Tipo:** interfaz

```typescript
export interface SemanticModule
```

El IR semántico escrito consumido por cada complemento de salida del marco.

### Intención de ranura

**Tipo:** interfaz

```typescript
export interface SlotIntention
```

Una ranura leída o una ranura pasada a un componente secundario.

### fuenteRespaldado

**Tipo:** función

```typescript
function sourceBacked(
  text: string,
  syntax: SourceBackedExpression["syntax"] = "expression",
  span: SourceSpan = EMPTY_SPAN,
): SourceBackedExpression;
```

Cree una expresión respaldada en código fuente a partir de texto ya impreso.

#### Parámetros

| Nombre   | Tipo                               | Descripción |
| -------- | ---------------------------------- | ----------- |
| texto    | cadena                             |             |
| sintaxis | SourceBackedExpression["sintaxis"] |             |
| lapso    | FuenteSpan                         |             |

### Expresión respaldada por origen

**Tipo:** interfaz

```typescript
export interface SourceBackedExpression
```

Una expresión o declaración que un objetivo puede conservar palabra por palabra cuando no tiene una regla de reducción.

### FuenteSpan

**Tipo:** interfaz

```typescript
export interface SourceSpan
```

Un rango de origen compartido por el AST genérico, los diagnósticos y las instantáneas.

### EstadoIntención

**Tipo:** interfaz

```typescript
export interface StateIntention
```

Una celda de estado y su definidor asociado, independientes de las primitivas de reactividad del objetivo.

### caminarRenderNodos

**Tipo:** función

```typescript
function walkRenderNodes(
  nodes: readonly GenericRenderNode[],
  visit: (node: GenericRenderNode) => void,
): void;
```

Recorra un árbol de renderizado en profundidad, incluido el marcado de expresión anidada.

#### Parámetros

| Nombre | Tipo                               | Descripción |
| ------ | ---------------------------------- | ----------- |
| nodos  | sólo lectura GenericRenderNode[]   |             |
| visita | (node: GenericRenderNode) => vacío |             |
