# @mission-platform/forge-plugin-api הפניה ל-API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/compiler/plugins/forge-plugin-api/docs/reference/generated/api.md: [packages/compiler/plugins/forge-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב `@mission-platform/forge-plugin-api`.

## `src/compiler/ast`

### להחיל עריכות מקור

פונקציה **סוג:**

```typescript
function applySourceEdits(source: string, edits: readonly SourceEdit[]): string;
```

החל עריכות מקור שאינן חופפות מבלי לחשוף צמתי מנתח.

#### פרמטרים

| שם     | הקלד                     | תיאור |
| ------ | ------------------------ | ----- |
| מקור   | מחרוזת                   |       |
| עריכות | עריכת מקור לקריאה בלבד[] |       |

### CLASS_NAME_ATTRIBUTE

**סוג:** קבוע

```typescript
export const CLASS_NAME_ATTRIBUTE;
```

לא סופק תיאור.

### COMPONENTS_JSX_MODULES

**סוג:** קבוע

```typescript
export const COMPONENTS_JSX_MODULES;
```

לא סופק תיאור.

### eventNameForProperty

פונקציה **סוג:**

```typescript
function eventNameForProperty(propertyName: string): string;
```

הגזר את שם האירוע המיוצג על ידי א Vue-סִגְנוֹן `on<Event>` לִתְמוֹך.

#### פרמטרים

| שם     | הקלד   | תיאור |
| ------ | ------ | ----- |
| שם נכס | מחרוזת |       |

### ICONS_JSX_MODULE

**סוג:** קבוע

```typescript
export const ICONS_JSX_MODULE;
```

לא סופק תיאור.

### JSX_ATTRIBUTE_RENAMES

**סוג:** קבוע

```typescript
export const JSX_ATTRIBUTE_RENAMES: ReadonlyMap<string, string>;
```

לא סופק תיאור.

### LOCAL_EFFECT_FILE

**סוג:** קבוע

```typescript
export const LOCAL_EFFECT_FILE;
```

לא סופק תיאור.

### LOCAL_EFFECT_MODULE

**סוג:** קבוע

```typescript
export const LOCAL_EFFECT_MODULE;
```

לא סופק תיאור.

### LOCAL_JSX_TYPE_NAMES

**סוג:** קבוע

```typescript
export const LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>;
```

לא סופק תיאור.

### LOCAL_JSX_TYPES_FILE

**סוג:** קבוע

```typescript
export const LOCAL_JSX_TYPES_FILE;
```

לא סופק תיאור.

### LOCAL_JSX_TYPES_MODULE

**סוג:** קבוע

```typescript
export const LOCAL_JSX_TYPES_MODULE;
```

לא סופק תיאור.

### localEffectModuleSource

פונקציה **סוג:**

```typescript
function localEffectModuleSource(framework: JsxFramework): string;
```

שחרר את המשותף Vue עוזר אפקט; יעדים אחרים אינם צריכים את הקובץ הזה.

#### פרמטרים

| שם    | הקלד         | תיאור |
| ----- | ------------ | ----- |
| מסגרת | JsxFramework |       |

### localJsxTypesModuleSource

פונקציה **סוג:**

```typescript
function localJsxTypesModuleSource(framework: JsxFramework): string;
```

שחרר הצהרות JSX פרימיטיביות מקומיות למסגרת ללא תלות מנתח.

#### פרמטרים

| שם    | הקלד         | תיאור |
| ----- | ------------ | ----- |
| מסגרת | JsxFramework |       |

### NEUTRAL_COMPILE_TIME_MARKERS

**סוג:** קבוע

```typescript
export const NEUTRAL_COMPILE_TIME_MARKERS: ReadonlySet<string>;
```

לא סופק תיאור.

### NEUTRAL_CONTEXT_VALUES

**סוג:** קבוע

```typescript
export const NEUTRAL_CONTEXT_VALUES: ReadonlySet<string>;
```

לא סופק תיאור.

### NEUTRAL_FRAMEWORK_COMPONENTS

**סוג:** קבוע

```typescript
export const NEUTRAL_FRAMEWORK_COMPONENTS: ReadonlySet<string>;
```

לא סופק תיאור.

### NEUTRAL_MODULE

**סוג:** קבוע

```typescript
export const NEUTRAL_MODULE;
```

לא סופק תיאור.

### NEUTRAL_RUNTIME_VALUES

**סוג:** קבוע

```typescript
export const NEUTRAL_RUNTIME_VALUES: ReadonlySet<string>;
```

לא סופק תיאור.

### NEUTRAL_VUE_RUNTIME_HOOKS

**סוג:** קבוע

```typescript
export const NEUTRAL_VUE_RUNTIME_HOOKS: ReadonlySet<string>;
```

לא סופק תיאור.

### REACT_ADAPTER_MODULE

**סוג:** קבוע

```typescript
export const REACT_ADAPTER_MODULE;
```

לא סופק תיאור.

### REACT_TYPE_ALIASES

**סוג:** קבוע

```typescript
export const REACT_TYPE_ALIASES: Readonly<Record<string, string>>;
```

לא סופק תיאור.

### עריכת מקור

**סוג:** ממשק

```typescript
export interface SourceEdit
```

עריכת מקור יציבה, מסודרת לפי קיזוז במקור המקורי.

### StyleIport

**סוג:** ממשק

```typescript
export interface StyleImport
```

ייבוא ​​גיליון סגנונות בוצע לתוך עץ שטוח שנוצר.

### VUE_ADAPTER_MODULE

**סוג:** קבוע

```typescript
export const VUE_ADAPTER_MODULE;
```

לא סופק תיאור.

### VUE_BUILTIN_COMPONENTS

**סוג:** קבוע

```typescript
export const VUE_BUILTIN_COMPONENTS: ReadonlySet<string>;
```

לא סופק תיאור.

### VUE_LOCAL_JSX_TYPE_NAMES

**סוג:** קבוע

```typescript
export const VUE_LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>;
```

לא סופק תיאור.

## `src/compiler/hoist-static`

### GenericHoistResult

**סוג:** ממשק

```typescript
export interface GenericHoistResult
```

לא סופק תיאור.

### HoistedStaticEntry

**סוג:** ממשק

```typescript
export interface HoistedStaticEntry
```

לא סופק תיאור.

### hoistStaticRenderNodes

פונקציה **סוג:**

```typescript
function hoistStaticRenderNodes(module: GenericModuleAst): GenericHoistResult;
```

הנף רשומות רינדור מסומנות והחזר את הערכים עבור מחולל היעד.

#### פרמטרים

| שם    | הקלד             | תיאור |
| ----- | ---------------- | ----- |
| מודול | GenericModuleAst |       |

### MP_HOIST_PREFIX

**סוג:** קבוע

```typescript
export const MP_HOIST_PREFIX;
```

לא סופק תיאור.

## `src/compiler/optimize`

### להחיל אופטימיזציה עריכות

פונקציה **סוג:**

```typescript
function applyOptimizationEdits(
  module: GenericModuleAst,
  edits: readonly SourceEdit[],
): GenericModuleAst;
```

החל עריכות מקור על מקור מודול מבלי לחשוף סוג יישום AST.

#### פרמטרים

| שם     | הקלד                     | תיאור |
| ------ | ------------------------ | ----- |
| מודול  | GenericModuleAst         |       |
| עריכות | עריכת מקור לקריאה בלבד[] |       |

### בוליאני קבוע

פונקציה **סוג:**

```typescript
function constantBoolean(expression: ExpressionLike): boolean | undefined;
```

פתרו בוליאן מילולי מבלי להעריך את הקוד המחבר.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| ביטוי | ExpressionLike |       |

### תוצאה כללית אופטימיזציה

**סוג:** ממשק

```typescript
export interface GenericOptimizationResult
```

לא סופק תיאור.

### hasJsxKey

פונקציה **סוג:**

```typescript
function hasJsxKey(attributes: readonly GenericAttribute[]): boolean;
```

האם רשימת תכונות JSX גנרית מכילה מפתח.

#### פרמטרים

| שם     | הקלד                          | תיאור |
| ------ | ----------------------------- | ----- |
| תכונות | קריאה בלבד GenericAttribute[] |       |

### hasMpStaticMarker

פונקציה **סוג:**

```typescript
function hasMpStaticMarker(node: GenericRenderNode): boolean;
```

בין אם JSX גנרי node יש את הסמן הסטטי הפרטי.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |

### isCompileTimeConstant

פונקציה **סוג:**

```typescript
function isCompileTimeConstant(expression: ExpressionLike): boolean;
```

האם טקסט המקור הוא ביטוי מילולי טהור בזמן הידור.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| ביטוי | ExpressionLike |       |

### MP_STATIC_ATTR

**סוג:** קבוע

```typescript
export const MP_STATIC_ATTR;
```

לא סופק תיאור.

### optimizeGenericModule

פונקציה **סוג:**

```typescript
function optimizeGenericModule(
  module: GenericModuleAst,
  _options: OptimizeOptions = {},
): GenericOptimizationResult;
```

החל אופטימיזציה ברמת שיא תוך החזרת כל עריכת מקור בנפרד.

#### פרמטרים

| שם        | הקלד                 | תיאור |
| --------- | -------------------- | ----- |
| מודול     | GenericModuleAst     |       |
| _אפשרויות | אופטימיזציה אפשרויות |       |

### OptimizeOptions

**סוג:** ממשק

```typescript
export interface OptimizeOptions
```

לא סופק תיאור.

### stripMpStaticAttributes

פונקציה **סוג:**

```typescript
function stripMpStaticAttributes(
  attributes: readonly GenericAttribute[],
): readonly GenericAttribute[];
```

החזר רשימת תכונות גנרית נטולת סמנים.

#### פרמטרים

| שם     | הקלד                          | תיאור |
| ------ | ----------------------------- | ----- |
| תכונות | קריאה בלבד GenericAttribute[] |       |

### stripMpStaticMarker

פונקציה **סוג:**

```typescript
function stripMpStaticMarker(node: GenericRenderNode): GenericRenderNode;
```

החזר עיבוד גנרי ללא סמן node, שמירה רקורסיבית על טווחי מקורות.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |

## `src/diagnostics`

### CompilerDiagnostic

**סוג:** ממשק

```typescript
export interface CompilerDiagnostic
```

אבחון ששומר על מספיק הקשר מקור לדיווח ברמת השלב.

### CompilerDiagnosticError

**סוג:** כיתה

```typescript
export class CompilerDiagnosticError extends Error
```

הועלתה שגיאה כאשר שלב מהדר אינו יכול להפיק מודול יעד קול.

### CompilerDiagnosticSeverity

**סוג:** סוג

```typescript
export type CompilerDiagnosticSeverity = "error" | "warning" | "info";
```

חומרת אבחון מהדר.

### שלב המהדר

**סוג:** סוג

```typescript
export type CompilerPhase =
  | "frontend"
  | "ir"
  | "inference"
  | "target-lowering"
  | "optimization"
  | "generation"
  | "build";
```

שלבי צנרת שיכולים לדווח על אבחון מהדר.

### createCompilerDiagnostic

פונקציה **סוג:**

```typescript
function createCompilerDiagnostic(
  diagnostic: Omit<CompilerDiagnostic, "fileName"> & { fileName?: string },
): CompilerDiagnostic;
```

צור אבחון מבלי לצמוד את חוזי השלב אליו TypeScript צמתים.

#### פרמטרים

| שם    | הקלד                                                                | תיאור |
| ----- | ------------------------------------------------------------------- | ----- |
| אבחון | לְהַשְׁמִיט<CompilerDiagnostic, "fileName"> & { fileName?: string } |       |

### formatCompilerDiagnostic

פונקציה **סוג:**

```typescript
function formatCompilerDiagnostic(diagnostic: CompilerDiagnostic): string;
```

עצב אבחון עבור שגיאת מהדר מבלי לאבד את מיקום המקור שלו.

#### פרמטרים

| שם    | הקלד               | תיאור |
| ----- | ------------------ | ----- |
| אבחון | CompilerDiagnostic |       |

### throwOnCompilerErrors

פונקציה **סוג:**

```typescript
function throwOnCompilerErrors(
  diagnostics: readonly CompilerDiagnostic[] | undefined,
): void;
```

בטל צינור מהדר כאשר שלב דיווח על שגיאה אחת או יותר.

#### פרמטרים

| שם    | הקלד                                         | תיאור |
| ----- | -------------------------------------------- | ----- |
| אבחון | לקריאה בלבד CompilerDiagnostic[] \| לא מוגדר |       |

## `src/framework`

### FrameworkBuildAdapters

**סוג:** ממשק

```typescript
export interface FrameworkBuildAdapters
```

שילובי בניית מסגרת שהוקלדו באופן עצמאי.

### Plugin FrameworkOutput

**סוג:** ממשק

```typescript
export interface FrameworkOutputPlugin
```

תוסף פלט פוסט-IR שניתן להרכבה.

### FrameworkOutputPluginSelection

**סוג:** סוג

```typescript
export type FrameworkOutputPluginSelection = readonly FrameworkOutputPlugin[];
```

בחירת יעד בבעלות מתקשר; אימות דוחה מזהים ריקים וכפולים.

### FrameworkSourceMetadata

**סוג:** ממשק

```typescript
export interface FrameworkSourceMetadata
```

מוסכמות עץ מקור הנדרשות על ידי מנהל התקן הבנייה ניטרלי במסגרת.

### GeneratedExtraModule

**סוג:** ממשק

```typescript
export interface GeneratedExtraModule
```

מודול עזר שנוצר כתוב לצד המודול המיוצר הראשי.

### GeneratedModule

**סוג:** ממשק

```typescript
export interface GeneratedModule
```

חפץ המקור שהוחזר על ידי תוסף פלט מסגרת.

### GeneratorContext

**סוג:** סוג

```typescript
export type GeneratorContext = TargetContext;
```

ההקשר הועבר למחוללי המקור לאחר הורדת יעד ואופטימיזציה.

### JsxFramework

**סוג:** סוג

```typescript
export type JsxFramework =
  "react" | "vue" | "svelte" | "solid" | "web-components";
```

מזהי מסגרת מובנים נוכחיים; תוספים עשויים להשתמש בכל מזהה מחרוזת נוסף.

### NeutralOptimizeOptions

**סוג:** ממשק

```typescript
export interface NeutralOptimizeOptions
```

אפשרויות אופטימיזציה ניטרליות המשותפות לפלאגין המהדר והיעד.

### OutputLanguage

**סוג:** סוג

```typescript
export type OutputLanguage = "tsx" | "jsx" | "ts" | "vue" | "svelte" | "astro" | (string &
```

שפות פלט המוכרות על ידי Forge, עם נקודת הרחבת מחרוזת פתוחה עבור יעדים חדשים.

### TargetComponentHost

**סוג:** ממשק

```typescript
export interface TargetComponentHost
```

מטא-נתונים של קריאת מארח ניטראלית ביעד משותפים על ידי הפניות של רכיבים שנוצרו.

### הקשר יעד

**סוג:** ממשק

```typescript
export interface TargetContext
```

הקשר משותף על ידי הורדת יעד ואופטימיזציה.

### כוונות יעד

**סוג:** ממשק

```typescript
export interface TargetIntentions
```

מעטפת כוונות ספציפית למטרה; עובדות ניטרליות נשארות זמינות למעברים מאוחרים יותר.

### TargetLoweredModule

**סוג:** ממשק

```typescript
export interface TargetLoweredModule
```

התוכנית בבעלות המטרה שהופקה על ידי תוסף `lower` שלב ומעודן על ידיו
`optimize` שָׁלָב. כל יעד מצהיר על הארכה משלו של חוזה זה ו
מפלה אותו ב-{@link TargetLoweredModule.framework}, כך שתוכנית יכולה להיות
הצטמצם ללא ליהוק בזמן שכל תוסף עדיין עונה על אותו הדבר
צורה של {@link TargetIntentions}.

### TargetOptimizeOptions

**סוג:** ממשק

```typescript
export interface TargetOptimizeOptions
```

אופציות הועברו למיטוב יעד מבלי לשנות את האפשרויות הנייטרליות לציבור.

### TsdownBuildContext

**סוג:** ממשק

```typescript
export interface TsdownBuildContext
```

הקשר משותף עבור חבילות תוספי tsdown/Rolldown בבעלות המסגרת.

### ViteBuildContext

**סוג:** ממשק

```typescript
export interface ViteBuildContext
```

הקשר משותף לבעלות המסגרת Vite חבילות תוספים.

## `src/index`

### defineForgeFramework

**סוג:** קבוע

```typescript
export const defineForgeFramework;
```

כינוי המדגיש שחבילות מסגרת רושמות יישומי יעד.

### defineForgeOutputPlugin

פונקציה **סוג:**

```typescript
function defineForgeOutputPlugin(plugin: T): T;
```

אמת והחזר תוסף פלט מסגרת לרישום על ידי צרכנים.

#### פרמטרים

| שם   | הקלד | תיאור |
| ---- | ---- | ----- |
| תוסף | ט    |       |

### ForgePluginId

**סוג:** סוג

```typescript
export type ForgePluginId = FrameworkOutputPlugin["id"];
```

לא סופק תיאור.

### validateForgeOutputPlugin

פונקציה **סוג:**

```typescript
function validateForgeOutputPlugin(
  plugin: unknown,
): asserts plugin is FrameworkOutputPlugin;
```

אמת מטא-נתונים של יעד ללא תלות ברישום מסגרת כלשהו.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| תוסף | לא ידוע |       |

### validateForgeOutputPluginSelection

פונקציה **סוג:**

```typescript
function validateForgeOutputPluginSelection(
  plugins: readonly T[],
): readonly T[];
```

אימות בחירת יעד בבעלות המתקשר, כולל מזהים ריקים ושכפולים.

#### פרמטרים

| שם     | הקלד           | תיאור |
| ------ | -------------- | ----- |
| תוספים | קריאה בלבד T[] |       |

## `src/ir`

### attributeExpressionText

פונקציה **סוג:**

```typescript
function attributeExpressionText(
  node: GenericRenderNode,
  name: string,
): string | undefined;
```

קרא טקסט ביטוי של תכונה בשם, כאשר הוא נושא אחד.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |
| שם   | מחרוזת            |       |

### attributeStringValue

פונקציה **סוג:**

```typescript
function attributeStringValue(
  node: GenericRenderNode,
  name: string,
): string | undefined;
```

קרא את ערך המחרוזת הסטטית של תכונה בעלת שם, כאשר יש לו כזה.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |
| שם   | מחרוזת            |       |

### DynamicNodeIntention

**סוג:** ממשק

```typescript
export interface DynamicNodeIntention
```

בחירת רכיב או אלמנט דינמי.

### EffectIntention

**סוג:** ממשק

```typescript
export interface EffectIntention
```

מחזור חיים של אפקט וביטוי הניקוי האופציונלי שלו.

### EMPTY_SEMANTIC_INTENTIONS

**סוג:** קבוע

```typescript
export const EMPTY_SEMANTIC_INTENTIONS: SemanticIntentions;
```

עובדות כוונות ריקות המשמשות מתקנים ומודולים שאין להם מה להסיק.

### EMPTY_SPAN

**סוג:** קבוע

```typescript
export const EMPTY_SPAN: SourceSpan;
```

טווח רוחב אפס המשמש צמתים גנריים מסונתזים.

### כוונת אירוע

**סוג:** ממשק

```typescript
export interface EventIntention
```

כריכת אירוע שיכולה להיות מיוצגת על ידי תחבירים שונים של תכונות יעד.

### findAttribute

פונקציה **סוג:**

```typescript
function findAttribute(
  node: GenericRenderNode,
  name: string,
): GenericJsxAttribute | undefined;
```

מצא את התכונה בעלת השם בעיבוד node.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |
| שם   | מחרוזת            |       |

### GenericAstNode

**סוג:** סוג

```typescript
export type GenericAstNode =
  GenericImport | GenericStatement | GenericRenderNode;
```

לא סופק תיאור.

### GenericAttribute

**סוג:** סוג

```typescript
export type GenericAttribute = GenericJsxAttribute | GenericJsxSpreadAttribute;
```

לא סופק תיאור.

### GenericAttributeValue

**סוג:** סוג

```typescript
export type GenericAttributeValue = |
```

הערך שמופיע על ידי תכונת JSX.

### GenericBindingKind

**סוג:** סוג

```typescript
export type GenericBindingKind =
  "identifier" | "object-pattern" | "array-pattern";
```

הצורה המחייבת של פרמטר האביזרים של רכיב.

### GenericComponent

**סוג:** ממשק

```typescript
export interface GenericComponent
```

פונקציית הרכיבים שהממשק זיהה במודול.

### GenericExpressionNode

**סוג:** ממשק

```typescript
export interface GenericExpressionNode
```

A `{ … }` אינטרפולציה בעמדת ילד JSX.

### GenericImport

**סוג:** ממשק

```typescript
export interface GenericImport
```

עובדת יבוא ניטרלי מסגרת.

### GenericJsxAttribute

**סוג:** ממשק

```typescript
export interface GenericJsxAttribute
```

מאפיין JSX בשם (`name`, `name="v"`, `name={expr}`).

### GenericJsxSpreadAttribute

**סוג:** ממשק

```typescript
export interface GenericJsxSpreadAttribute
```

תכונת התפשטות JSX (`{...props}`).

### GenericModuleAst

**סוג:** ממשק

```typescript
export interface GenericModuleAst
```

ייצוג מקור שניתן לסידרה המשותף לכל שלבי המהדר.

### פרמטר כללי

**סוג:** ממשק

```typescript
export interface GenericParameter
```

פרמטר רכיב, צורתו המחייבת והסוג המוצהר שלו.

### GenericRenderChild

**סוג:** סוג

```typescript
export type GenericRenderChild =
  GenericRenderNode | GenericTextNode | GenericExpressionNode;
```

לא סופק תיאור.

### GenericRenderNode

**סוג:** ממשק

```typescript
export interface GenericRenderNode
```

עיבוד ניטרלי למסגרת node; מעבדי יעד עשויים להוריד או לשמר את ביטוי המקור שלו.

### GenericStatement

**סוג:** ממשק

```typescript
export interface GenericStatement
```

הצהרה ניטרלית למסגרת שנשמרה על ידי החזית.

### GenericStatementKind

**סוג:** סוג

```typescript
export type GenericStatementKind =
  | "import"
  | "interface"
  | "type-alias"
  | "enum"
  | "variable"
  | "function"
  | "class"
  | "export"
  | "expression"
  | "return"
  | "other";
```

סיווג גס של משפט שנשמר ברמת המודול.

### GenericTagKind

**סוג:** סוג

```typescript
export type GenericTagKind = "element" | "component" | "fragment" | "dynamic";
```

כמה עיבוד nodeהתג של נפתר באוצר המילים של היעד.

### GenericTextNode

**סוג:** ממשק

```typescript
export interface GenericTextNode
```

טקסט מילולי בין תגי JSX.

### isExpressionNode

פונקציה **סוג:**

```typescript
function isExpressionNode(
  child: GenericRenderChild,
): child is GenericExpressionNode;
```

האם ילד עיבוד הוא א `{ … }` שִׁרבּוּב.

#### פרמטרים

| שם  | הקלד               | תיאור |
| --- | ------------------ | ----- |
| ילד | GenericRenderChild |       |

### isRenderNode

פונקציה **סוג:**

```typescript
function isRenderNode(child: GenericRenderChild): child is GenericRenderNode;
```

האם צאצא לעיבוד הוא אלמנט/פרגמנט מקונן node.

#### פרמטרים

| שם  | הקלד               | תיאור |
| --- | ------------------ | ----- |
| ילד | GenericRenderChild |       |

### isTextNode

פונקציה **סוג:**

```typescript
function isTextNode(child: GenericRenderChild): child is GenericTextNode;
```

האם עיבוד ילד הוא טקסט JSX מילולי.

#### פרמטרים

| שם  | הקלד               | תיאור |
| --- | ------------------ | ----- |
| ילד | GenericRenderChild |       |

### ListKeyIntention

**סוג:** ממשק

```typescript
export interface ListKeyIntention
```

מועמד מפתח יציב למעבד רשימת יעדים.

### MemoIntention

**סוג:** ממשק

```typescript
export interface MemoIntention
```

ערך ממוזכר או נגזר.

### PropIntention

**סוג:** ממשק

```typescript
export interface PropIntention
```

הצהרת אבזר נייטרלית הנגזרת מחתימת רכיב או סוג אביזרים.

### RefIntention

**סוג:** ממשק

```typescript
export interface RefIntention
```

כוונת ר"פ שאינה תלויה בייצוג הר"פ של המטרה.

### renderNodeTagName

פונקציה **סוג:**

```typescript
function renderNodeTagName(node: GenericRenderNode): string | undefined;
```

שם התג הפשוט של עיבוד node, או `undefined` עבור תג מחושב.

#### פרמטרים

| שם   | הקלד              | תיאור |
| ---- | ----------------- | ----- |
| node | GenericRenderNode |       |

### כוונות סמנטיות

**סוג:** ממשק

```typescript
export interface SemanticIntentions
```

עובדות סמנטיות משותפות הושקו פעם אחת לפני הורדת יעד.

### מודול סמנטי

**סוג:** ממשק

```typescript
export interface SemanticModule
```

ה-IR הסמנטי המוקלד שנצרך על ידי כל תוסף פלט מסגרת.

### SlotIntention

**סוג:** ממשק

```typescript
export interface SlotIntention
```

חריץ שנקרא או חריץ הועבר לרכיב צאצא.

### מקור מגובה

פונקציה **סוג:**

```typescript
function sourceBacked(
  text: string,
  syntax: SourceBackedExpression["syntax"] = "expression",
  span: SourceSpan = EMPTY_SPAN,
): SourceBackedExpression;
```

בנה ביטוי מגובה מקור מטקסט שכבר מודפס.

#### פרמטרים

| שם    | הקלד                            | תיאור |
| ----- | ------------------------------- | ----- |
| טקסט  | מחרוזת                          |       |
| תחביר | SourceBackedExpression["תחביר"] |       |
| span  | SourceSpan                      |       |

### SourceBackedExpression

**סוג:** ממשק

```typescript
export interface SourceBackedExpression
```

ביטוי או אמירה שמטרה עשויה לשמר מילה במילה כאשר אין לה כלל הורדה.

### SourceSpan

**סוג:** ממשק

```typescript
export interface SourceSpan
```

טווח מקור משותף ל-AST הגנרי, אבחון ותמונות מצב.

### כוונת המדינה

**סוג:** ממשק

```typescript
export interface StateIntention
```

תא מצב והמגדיר הקשור אליו, ללא תלות בפרימיטיבים של תגובתיות מטרה.

### walkRenderNodes

פונקציה **סוג:**

```typescript
function walkRenderNodes(
  nodes: readonly GenericRenderNode[],
  visit: (node: GenericRenderNode) => void,
): void;
```

בצע עיבוד עץ לעומק ראשון, כולל סימון ביטוי מקונן.

#### פרמטרים

| שם    | הקלד                             | תיאור |
| ----- | -------------------------------- | ----- |
| צמתים | לקריאה בלבד GenericRenderNode[]  |       |
| בקר   | (node: GenericRenderNode) => ריק |       |
