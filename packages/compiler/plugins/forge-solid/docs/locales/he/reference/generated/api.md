# הפניה ל-@mission-platform/forge-plugin-solid API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/compiler/plugins/forge-solid/docs/reference/generated/api.md: [packages/compiler/plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/forge-plugin-solid`.

## `src/emitters/hook-module`

### emitSolidHookModule

פונקציה **סוג:**

```typescript
function emitSolidHookModule(module: SemanticModule): string;
```

הפוך מודול וו ניטרלי למקור SolidJS שלו.

#### פרמטרים

| שם    | הקלד           | תיאור |
| ----- | -------------- | ----- |
| מודול | SemanticModule |       |

## `src/emitters/module`

### emitSolidModule

פונקציה **סוג:**

```typescript
function emitSolidModule(
  module: SemanticModule,
  options: SolidEmitOptions = {},
): GeneratedSolidModule;
```

הפוך מודול רכיב ניטרלי למקור SolidJS שלו.

#### פרמטרים

| שם       | הקלד             | תיאור |
| -------- | ---------------- | ----- |
| מודול    | SemanticModule   |       |
| אפשרויות | SolidEmitOptions |       |

### GeneratedSolidModule

**סוג:** ממשק

```typescript
export interface GeneratedSolidModule
```

מקור SolidJS מיוצר עבור מודול ניטרלי אחד.

### SolidEmitOptions

**סוג:** ממשק

```typescript
export interface SolidEmitOptions
```

אפשרויות השולטות כיצד מודול נפלט.

## `src/index`

### forgeSolidFramework

פונקציה **סוג:**

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin;
```

צור את הפלאגין Solid ואת מתאמי Vite/Rolldown JSX שלו.

## `src/lower`

### isSolidLowered

פונקציה **סוג:**

```typescript
function isSolidLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is SolidLoweredModule;
```

צמצם תוכנית יעד לתוכנית Solid; `false` עבור כל תוכנית של מסגרת אחרת.

#### פרמטרים

| שם    | הקלד                            | תיאור |
| ----- | ------------------------------- | ----- |
| הוריד | TargetLoweredModule \| לא מוגדר |       |

### lowerSolidModule

פונקציה **סוג:**

```typescript
function lowerSolidModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

הורד IR ניטרלי לתוך עטיפת כוונת המטרה של Solid, נושאת את התוכנית.

#### פרמטרים

| שם   | הקלד           | תיאור |
| ---- | -------------- | ----- |
| ir   | SemanticModule |       |
| הקשר | היעד הקשר      |       |

### planSolidImports

פונקציה **סוג:**

```typescript
function planSolidImports(
  plan: SolidLoweringPlan,
  conditional: boolean,
): string[];
```

שמות הערך המדויקים של `solid-js` שתוכנית דורשת, בסדר אלפביתי כך א
הפעלה מחדש מייצרת רשימה זהה. חישוב מחדש זה מה שעושה
`solid:drop-unused-imports` פונקציה טהורה של התוכנית.

#### פרמטרים

| שם     | הקלד              | תיאור |
| ------ | ----------------- | ----- |
| תוכנית | SolidLoweringPlan |       |
| מותנה  | בוליאני           |       |

### planSolidModule

פונקציה **סוג:**

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan;
```

בנה את תוכנית Solid עבור עובדות סמנטיות של מודול.

#### פרמטרים

| שם  | הקלד           | תיאור |
| --- | -------------- | ----- |
| ir  | SemanticModule |       |

### SOLID_FRAMEWORK

**סוג:** קבוע

```typescript
export const SOLID_FRAMEWORK;
```

מזהה המסגרת של כל תוכנית Solid מופלה.

### SolidDynamicPlan

**סוג:** ממשק

```typescript
export interface SolidDynamicPlan
```

`<Dynamic is={…}>` node ירד לכתב היפר של Solid.

### SolidEffectPlan

**סוג:** ממשק

```typescript
export interface SolidEffectPlan
```

`useEffect` ירד ל-`createEffect`/`onMount`, עם `onCleanup` האופציונלי שלו.

### SolidListKeyPlan

**סוג:** ממשק

```typescript
export interface SolidListKeyPlan
```

הקרנת רשימה והמפתח Solid שומר עבורה.

### SolidLoweredModule

**סוג:** ממשק

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

הארכת Solid של חוזה תוכנית היעד המשותפת.

### SolidLoweringPlan

**סוג:** ממשק

```typescript
export interface SolidLoweringPlan
```

ההחלטות בצורת Solid שנלקחו עבור מודול אחד.

### SolidMemoizedExpression

**סוג:** ממשק

```typescript
export interface SolidMemoizedExpression
```

ביטוי דינמי חוזר של ילד מקודם לתזכיר משלו.

### SolidMemoPlan

**סוג:** ממשק

```typescript
export interface SolidMemoPlan
```

כריכת `useMemo` ירדה ל-`createMemo`, או מקופלת לקבוע.

### SolidPropPlan

**סוג:** ממשק

```typescript
export interface SolidPropPlan
```

אביזר מוצהר אחד ואיך Solid קורא אותו.

### SolidRefPlan

**סוג:** ממשק

```typescript
export interface SolidRefPlan
```

מיכל `useRef` וסוג האלמנט שהוא מכיל.

### SolidSignalPlan

**סוג:** ממשק

```typescript
export interface SolidSignalPlan
```

תא `useState` ירד לאות Solid.

### SolidSlotPlan

**סוג:** ממשק

```typescript
export interface SolidSlotPlan
```

קריאת משבצת, המתבטאת כפי שחבר האביזרים Solid קורא אותו.

## `src/optimize`

### COLLAPSE_SINGLE_CHILD_FRAGMENTS

**סוג:** קבוע

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS;
```

זרוק קטע שעוטף אלמנט אחד בדיוק.

### DROP_UNUSED_IMPORTS

**סוג:** קבוע

```typescript
export const DROP_UNUSED_IMPORTS;
```

חשב מחדש את ייבוא ​​`solid-js` מהתוכנית המעודנת.

### HOIST_STATIC_SUBTREES

**סוג:** קבוע

```typescript
export const HOIST_STATIC_SUBTREES;
```

קדם את תתי העצים `__mpStatic` לקבועים ברמת המודול.

### MEMOIZE_DYNAMIC_EXPRESSIONS

**סוג:** קבוע

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS;
```

קדם ביטוי ילד דינמי חוזר לתזכיר בודד.

### MP_MEMO_PREFIX

**סוג:** קבוע

```typescript
export const MP_MEMO_PREFIX;
```

קידומת לכריכות התזכיר שמציגה `solid:memoize-dynamic-expressions`.

### optimizeSolidModule

פונקציה **סוג:**

```typescript
function optimizeSolidModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

חידד תוכנית Solid מופחתת. מעטפת כוונות שאינה נושאת תוכנית Solid
(קריאה ישירה, או של יעד אחר) מוחזרת ללא נגיעה.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| כוונות   | כוונות יעד            |       |
| אפשרויות | TargetOptimizeOptions |       |

### STABLE_LIST_KEYS

**סוג:** קבוע

```typescript
export const STABLE_LIST_KEYS;
```

שמור רק על מקשי הרשימה שהחזית הוכחה יציבה.
