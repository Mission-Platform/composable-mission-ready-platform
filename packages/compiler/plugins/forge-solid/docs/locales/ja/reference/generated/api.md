# @mission-platform/forge-plugin-solid API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/compiler/plugins/forge-solid/docs/reference/generated/api.md: [packages/compiler/plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/forge-plugin-solid` のパブリック ソース宣言から生成されます。

## `src/emitters/hook-module`

### ソリッドフックモジュールを放出する

**種類:** 関数

```typescript
function emitSolidHookModule(module: SemanticModule): string;
```

ニュートラル フック モジュールを SolidJS ソースに変換します。

#### パラメータ

| 名前       | タイプ                   | 説明 |
| ---------- | ------------------------ | ---- |
| モジュール | セマンティックモジュール |      |

## `src/emitters/module`

### エミットソリッドモジュール

**種類:** 関数

```typescript
function emitSolidModule(
  module: SemanticModule,
  options: SolidEmitOptions = {},
): GeneratedSolidModule;
```

ニュートラルコンポーネントモジュールをSolidJSソースに変換します。

#### パラメータ

| 名前       | タイプ                   | 説明 |
| ---------- | ------------------------ | ---- |
| モジュール | セマンティックモジュール |      |
| オプション | SolidEmitオプション      |      |

### 生成されたソリッドモジュール

**種類:** インターフェース

```typescript
export interface GeneratedSolidModule
```

1 つのニュートラル モジュール用に生成された SolidJS ソース。

### SolidEmitオプション

**種類:** インターフェース

```typescript
export interface SolidEmitOptions
```

モジュールの発行方法を制御するオプション。

## `src/index`

### forgeSolidFramework

**種類:** 関数

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin;
```

Solid 出力プラグインとその Vite/Rolldown JSX アダプターを作成します。

## `src/lower`

### 固体低下しています

**種類:** 関数

```typescript
function isSolidLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is SolidLoweredModule;
```

ターゲット プランを Solid プランに絞り込みます。他のフレームワークのプランの場合は `false`。

#### パラメータ

| 名前   | タイプ                       | 説明 |
| ------ | ---------------------------- | ---- |
| 下げた | TargetLoweredModule \|未定義 |      |

### 下部ソリッドモジュール

**種類:** 関数

```typescript
function lowerSolidModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

ニュートラル IR を Solid のターゲット意図ラッパーに下げ、計画を実行します。

#### パラメータ

| 名前         | タイプ                   | 説明 |
| ------------ | ------------------------ | ---- |
| 赤外線       | セマンティックモジュール |      |
| コンテキスト | ターゲットコンテキスト   |      |

### planSolidImports

**種類:** 関数

```typescript
function planSolidImports(
  plan: SolidLoweringPlan,
  conditional: boolean,
): string[];
```

プランに必要な正確な `solid-js` 値の名前は、アルファベット順に並べられています。
再実行すると同一のリストが生成されます。これを再計算すると、
`solid:drop-unused-imports` は計画の純粋な機能です。

#### パラメータ

| 名前     | タイプ                   | 説明 |
| -------- | ------------------------ | ---- |
| 計画     | ソリッドローリングプラン |      |
| 条件付き | ブール値                 |      |

### プランソリッドモジュール

**種類:** 関数

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan;
```

モジュールのセマンティック ファクトの Solid プランを構築します。

#### パラメータ

| 名前   | タイプ                   | 説明 |
| ------ | ------------------------ | ---- |
| 赤外線 | セマンティックモジュール |      |

### SOLID_FRAMEWORK

**種類:** 定数

```typescript
export const SOLID_FRAMEWORK;
```

Solid プランごとに識別されるフレームワーク識別子。

### ソリッドダイナミックプラン

**種類:** インターフェース

```typescript
export interface SolidDynamicPlan
```

`<Dynamic is={…}>` node は Solid のハイパースクリプトに変換されます。

### ソリッドエフェクトプラン

**種類:** インターフェース

```typescript
export interface SolidEffectPlan
```

`useEffect` は、オプションの `onCleanup` を使用して `createEffect`/`onMount` に引き下げられました。

### SolidListKeyPlan

**種類:** インターフェース

```typescript
export interface SolidListKeyPlan
```

リスト射影とそのキー Solid が保持します。

### ソリッドローワーモジュール

**種類:** インターフェース

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

Solid 共有ターゲットプラン契約の延長。

### 固体降下計画

**種類:** インターフェース

```typescript
export interface SolidLoweringPlan
```

1 つのモジュールに対して行われた Solid 形式の決定。

### SolidMemoizedExpression

**種類:** インターフェース

```typescript
export interface SolidMemoizedExpression
```

繰り返される動的子式は、独自のメモにプロモートされます。

### ソリッドメモプラン

**種類:** インターフェース

```typescript
export interface SolidMemoPlan
```

`useMemo` バインディングが `createMemo` に下げられるか、定数に折りたたまれます。

### ソリッドプロッププラン

**種類:** インターフェース

```typescript
export interface SolidPropPlan
```

宣言された 1 つの prop と、Solid がそれを読み取る方法。

### ソリッドリファレンスプラン

**種類:** インターフェース

```typescript
export interface SolidRefPlan
```

`useRef` コンテナーとそれが保持する要素のタイプ。

### ソリッドシグナルプラン

**種類:** インターフェース

```typescript
export interface SolidSignalPlan
```

`useState` セルが Solid 信号に低下しました。

### ソリッドスロットプラン

**種類:** インターフェース

```typescript
export interface SolidSlotPlan
```

スロットの読み取り。props メンバー Solid の読み取り元として表現されます。

## `src/optimize`

### COLLAPSE_SINGLE_CHILD_FRAGMENTS

**種類:** 定数

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS;
```

ちょうど 1 つの要素をラップするフラグメントをドロップします。

### DROP_UNUSED_IMPORTS

**種類:** 定数

```typescript
export const DROP_UNUSED_IMPORTS;
```

洗練された計画から `solid-js` インポートを再計算します。

### HOIST_STATIC_SUBTREES

**種類:** 定数

```typescript
export const HOIST_STATIC_SUBTREES;
```

`__mpStatic` サブツリーをモジュール レベルの定数にプロモートします。

### MEMOIZE_DYNAMIC_EXPRESSIONS

**種類:** 定数

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS;
```

繰り返される動的子式を単一のメモにプロモートします。

### MP_MEMO_PREFIX

**種類:** 定数

```typescript
export const MP_MEMO_PREFIX;
```

`solid:memoize-dynamic-expressions` が導入するメモ バインディングのプレフィックス。

### ソリッドモジュールの最適化

**種類:** 関数

```typescript
function optimizeSolidModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

引き下げられた Solid プランを改良します。 Solid プランを持たないインテンション ラッパー
(直接呼び出しまたは別のターゲットの) はそのまま返されます。

#### パラメータ

| 名前       | タイプ                     | 説明 |
| ---------- | -------------------------- | ---- |
| 意図       | ターゲットインテンション   |      |
| オプション | ターゲット最適化オプション |      |

### STABLE_LIST_KEYS

**種類:** 定数

```typescript
export const STABLE_LIST_KEYS;
```

フロントエンドが安定していることが証明されたリスト キーのみを保持します。
