# Forge Figma repository bridge

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/forge-figma-bridge/docs/index.md: [packages/forge-figma-bridge/docs/index.md](../../index.md)
> Lingua: Italiano (it)

il bridge accepts un reviewed `ForgeRepositoryExportRequest` over `POST /export` e writes il bundle into one di its explicitly configured repository roots. Configure roots con il CLI's `--root <id>=<absolute-path>` option.
