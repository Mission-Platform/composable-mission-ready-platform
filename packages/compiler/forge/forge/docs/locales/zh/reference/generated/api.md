# @mission-platform/forge API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/compiler/forge/forge/docs/reference/generated/api.md: [packages/compiler/forge/forge/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/forge` 中的公共源声明生成。

## `src/adapters/react`

### HTML内容

**种类：**功能

```typescript
function HtmlContent(properties: HtmlContentProperties): ReactElement;
```

使用 `dangerouslySetInnerHTML` 将受信任的 HTML 渲染到 React 主机中。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 属性 | Html 内容属性 |      |

### 渲染到反应

**种类：**功能

```typescript
function renderToReact(element: MpElement): ReactElement;
```

将中性的 {@link MpElement} 树渲染为 React 元素。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 元素 | Mp元素 |      |

### 传送

**种类：**功能

```typescript
function Teleport({ to = 'body', disabled = false, children }: TeleportProperties): ReactNode;
```

中性 `<Teleport>` 原语的 React 构建 - 一个薄包装
`react-dom` 的 {@link createPortal}。 `@mission-platform/vite-plugin-forge`
将组件的 `import { Teleport } from '@mission-platform/forge'` 重写为
导入此组件，因此一次写入 `<Teleport to="body">…</Teleport>`
编译为本机 React 门户。

当 `to` 已经是 DOM **元素**时（常见的客户端情况 - 例如
解决了调用者提交的 `<dialog>` 或 `document.body`）孩子们是
在渲染期间**同步进入其中**，因此它们安装在同一个中
打开它们的提交。这与 Vue 的 `<Teleport>` 匹配（解析其
同步目标），并且是正确性所必需的：一个门户面板，
使用 Popover API 将自身提升到浏览器顶层
（下一帧上的 `popover="manual"` + `showPopover()`）必须已经是
当该帧运行时安装 - 通过渲染延迟门户会丢失
race，离开面板 `display:none`（未显示的 UA 默认值
弹出窗口），因此不可见。

只有选择器 **string** 需要后安装 `document.querySelector`，其中
保持组件 SSR 安全：在服务器渲染期间以及第一个客户端
渲染字符串目标 — 它不渲染任何内容（或者，当 `disabled` 时，
子项到位），并且 `createPortal` 仅在 DOM 目标存在时运行。

#### 参数

| 名称                                | 类型     | 描述 |
| ----------------------------------- | -------- | ---- |
| { to = 'body', 禁用 = false, 儿童 } | 传送属性 |      |

### 传送属性

**种类：**接口

```typescript
export interface TeleportProperties
```

React {@link Teleport} 组件接受的属性。

### 反应组件

**种类：**功能

```typescript
function toReactComponent(component: MpComponent<P>, displayName?: string): FunctionComponent<P>;
```

将中性组件包装为一级React功能组件，准备
由 `react-dom` 呈现或在其他 React 组件内组成。

#### 参数

| 名称     | 类型           | 描述 |
| -------- | -------------- | ---- |
| 组件     | MpComponent<P> |      |
| 显示名称 | 字符串         |      |

### 过渡

**种类：**功能

```typescript
function Transition({ name = 'v', appear = false, duration, children, ...overrides }: TransitionProperties): ReactNode;
```

中性 `<Transition>` 原语的 React 构建 - 一个小型 CSS 类
镜像 Vue 内置 `<Transition>` 的驱动程序。它为单个
通过切换 `<name>-enter-*` / `<name>-leave-*` 有条件渲染的子级
类（因此相同的样式表驱动两个框架）并保留子项
通过其离开动画安装。 `@mission-platform/vite-plugin-forge`
将组件的 `import { Transition } from '@mission-platform/forge'` 重写为
将此组件导入 React 目标。

它是 SSR 安全的：服务器渲染（和第一个客户端绘制）渲染子级
就位 - 匹配 Vue 的 SSR 输出 - 并且动画仅在之后运行。

#### 参数

| 名称                                                    | 类型     | 描述 |
| ------------------------------------------------------- | -------- | ---- |
| { name = 'v'，appear = false，持续时间，子项，...覆盖 } | 过渡属性 |      |

### 过渡集团

**种类：**功能

```typescript
function TransitionGroup({
  name = 'v',
  tag,
  moveClass,
  appear = false,
  duration,
  children,
  ...overrides
}: TransitionGroupProperties): ReactNode;
```

中性 `<TransitionGroup>` 原语的 React 构建 — CSS 类
镜像 Vue 内置 `<TransitionGroup>` 的驱动程序。它为**键控设置动画
列表**：输入子项获取 `<name>-enter-*`，让子项保持安装状态
通过他们的 `<name>-leave-*` 动画，以及幸存的孩子发生了变化
位置使用 `<name>-move` 进行 FLIP 动画（相同的样式表驱动两个
框架）。 `@mission-platform/vite-plugin-forge` 重写组件的
`import { TransitionGroup } from '@mission-platform/forge'` 将其导入到
React 目标。

它是 SSR 安全的：服务器渲染（和第一个客户端绘制）渲染
孩子就位 - 匹配 Vue 的 SSR 输出 - 并且动画仅运行
之后。转换类应用于每个子级的 DOM node，因此 —
就像单个 {@link Transition} ——它们对 DOM 元素子元素生效；
本身就是（非引用转发）组件的子组件仍然可以安装/卸载
正确但没有收到任何类别（与中性 SSR 就地基线匹配）。

#### 参数

| 名称                                                                             | 类型       | 描述 |
| -------------------------------------------------------------------------------- | ---------- | ---- |
| { name = 'v'、tag、moveClass、appear = false、duration、children、...overrides } | 过渡组属性 |      |

### 过渡组属性

**种类：**接口

```typescript
export interface TransitionGroupProperties extends TransitionClassOverrides
```

React {@link TransitionGroup} 组件接受的属性。

### 过渡属性

**种类：**接口

```typescript
export interface TransitionProperties extends TransitionClassOverrides
```

React {@link Transition} 组件接受的属性。

## `src/adapters/solid`

### HTML内容

**种类：**功能

```typescript
function HtmlContent(properties: HtmlContentProperties): JSX.Element;
```

中性 `<HtmlContent>` 原语的 Solid 版本。 Solid 的
`innerHTML` 属性是本机基于替换的原始内容操作。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 属性 | Html 内容属性 |      |

### 传送

**种类：**功能

```typescript
function Teleport(properties: TeleportProperties): JSX.Element;
```

中性 `<Teleport>` 原语的 Solid 构建 - 一个薄包装
Solid 的{@link 门户}。 `@mission-platform/vite-plugin-forge` 重写
组件的 `import { Teleport } from '@mission-platform/forge'` 来导入它
组件，因此一次写入的 `<Teleport to="body">…</Teleport>` 编译为
本机 Solid 门户。

门户目标在客户端上解析，因此该组件是 SSR 安全的：
在服务器渲染期间 - 并且在 DOM 目标存在之前 - 它会渲染
什么都没有（或者，当 `disabled` 时，孩子就位），并且仅 {@link Portal}
一旦真正的目标可用，就会安装。

#### 参数

| 名称 | 类型     | 描述 |
| ---- | -------- | ---- |
| 属性 | 传送属性 |      |

### 传送属性

**种类：**接口

```typescript
export interface TeleportProperties
```

Solid {@link Teleport} 组件接受的属性。

### 过渡

**种类：**功能

```typescript
function Transition(properties: TransitionProperties): JSX.Element;
```

中性 `<Transition>` 原语的 Solid 版本。最小正确
将子项呈现**到位**的实现 - 与 React 匹配
适配器的 SSR / 适配器奇偶校验路径和中性运行时基线 - 所以
一次写入组件类型检查和安装，无需引入第三方
Solid 转换库。进入/离开课堂动画可以稍后分层；
道具表面已匹配 Vue/React（`name`、`appear`、`duration` 和
每相类覆盖）。

`@mission-platform/vite-plugin-forge` 重写组件的
`import { Transition } from '@mission-platform/forge'` 将其导入到
Solid 目标。

#### 参数

| 名称 | 类型     | 描述 |
| ---- | -------- | ---- |
| 属性 | 过渡属性 |      |

### 过渡集团

**种类：**功能

```typescript
function TransitionGroup(properties: TransitionGroupProperties): JSX.Element;
```

中性 `<TransitionGroup>` 原语的 Solid 版本。喜欢
{@link Transition}，这是一个最小的正确实现，可以呈现
子项**到位**（React 适配器的 SSR/适配器奇偶校验基线），因此
生成 Solid 组件类型检查，无需第三方列表转换
图书馆。道具表面匹配 Vue/React（`name`、`tag`、`moveClass`、
`appear`、`duration` 和每相类覆盖）用于一次写入
平价。

`@mission-platform/vite-plugin-forge` 重写组件的
`import { TransitionGroup } from '@mission-platform/forge'` 将其导入到
Solid 目标。

#### 参数

| 名称 | 类型       | 描述 |
| ---- | ---------- | ---- |
| 属性 | 过渡组属性 |      |

### 过渡组属性

**种类：**接口

```typescript
export interface TransitionGroupProperties extends TransitionClassOverrides
```

Solid {@link TransitionGroup} 组件接受的属性。

### 过渡属性

**种类：**接口

```typescript
export interface TransitionProperties extends TransitionClassOverrides
```

Solid {@link Transition} 组件接受的属性。

## `src/adapters/svelte`

### HTML内容

**种类：**功能

```typescript
function HtmlContent(properties: HtmlContentProperties): Snippet;
```

Svelte 适配器的原始内容实现。 `createRawSnippet` 是
Svelte 的 `{@html}` 操作的运行时形式及其设置回调保留
手写适配器使用的主机引用和事件转发。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 属性 | Html 内容属性 |      |

### 传送

**种类：**功能

```typescript
function Teleport(properties: TeleportProperties): Snippet;
```

为 Svelte SSR 和客户端奇偶校验渲染 Forge 子代码片段。

#### 参数

| 名称 | 类型     | 描述 |
| ---- | -------- | ---- |
| 属性 | 传送属性 |      |

### 传送属性

**种类：**接口

```typescript
export interface TeleportProperties
```

Svelte {@link Teleport} 原语接受的属性。

### 过渡

**种类：**功能

```typescript
function Transition(properties: TransitionProperties): Snippet;
```

渲染单个过渡子项；动画由本机 Svelte 标记提供。

#### 参数

| 名称 | 类型     | 描述 |
| ---- | -------- | ---- |
| 属性 | 过渡属性 |      |

### 过渡集团

**种类：**功能

```typescript
function TransitionGroup(properties: TransitionGroupProperties): Snippet;
```

为原生 Svelte 键控块渲染过渡组子级。

#### 参数

| 名称 | 类型       | 描述 |
| ---- | ---------- | ---- |
| 属性 | 过渡组属性 |      |

### 过渡组属性

**种类：**接口

```typescript
export interface TransitionGroupProperties extends TransitionProperties
```

Svelte {@link TransitionGroup} 原语接受的属性。

### 过渡属性

**种类：**接口

```typescript
export interface TransitionProperties
```

Svelte {@link Transition} 原语接受的属性。

## `src/adapters/vue`

### 创建上下文

**种类：**功能

```typescript
function createContext(defaultValue: T): VueContext<T>;
```

中性 `createContext` 原语的 Vue 版本。返回一个句柄，其
`Provider` 是一个真实组件，`provide()` 将其 `value` 设为默认值
插槽，因此一次性写入 `<Ctx.Provider value={…}><Slot /></Ctx.Provider>`
编译为本机 Vue 提供。 `@mission-platform/vite-plugin-forge` 重写
组件的 `import { createContext, useContext } from '@mission-platform/forge'`
将它们导入 Vue 目标。

#### 参数

| 名称   | 类型 | 描述 |
| ------ | ---- | ---- |
| 默认值 | T    |      |

### HTML内容

**种类：**功能

```typescript
function HtmlContent(properties: HtmlContentProperties): VNode;
```

使用渲染函数 `innerHTML` 绑定将可信 HTML 渲染到 Vue 主机中。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 属性 | Html 内容属性 |      |

### 渲染到Vue

**种类：**功能

```typescript
function renderToVue(element: MpElement): VNode;
```

将中性 {@link MpElement} 树渲染为 Vue `VNode`。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 元素 | Mp元素 |      |

### 到Vue组件

**种类：**功能

```typescript
function toVueComponent(component: MpComponent<P>, name?: string): FunctionalComponent<P>;
```

将中性组件包装为 Vue 功能组件。传入属性
成为中立道具，默认插槽（如果有）将作为中立转发
`children`，并且每个命名槽都以其名称转发（作为作用域槽
函数），因此组件中的 `<Slot name="…" />` 会针对它进行解析，并且
结果可以像任何其他 Vue 组件一样使用。

#### 参数

| 名称 | 类型           | 描述 |
| ---- | -------------- | ---- |
| 组件 | MpComponent<P> |      |
| 名称 | 字符串         |      |

### 使用上下文

**种类：**功能

```typescript
function useContext(context: VueContext<T>): T;
```

中性 `useContext` 原语的 Vue 构建 - 一个薄包装
`inject()` 在没有封闭时回退到上下文的默认值
提供商提供了一份。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 背景 | VueContext<T> |      |

### Vue上下文

**种类：**接口

```typescript
export interface VueContext<T>
```

Vue 上下文句柄：`provide()` 支持的 `Provider` 加上 `inject()` 密钥​​。

## `src/adapters/web-components`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**种类：**常数

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy;
```

内部是无害的能力门控；表单关联仍处于选择加入状态。

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**种类：**常数

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy;
```

当生成的类不覆盖策略时使用兼容性默认值。

### Dom模板结果

**种类：**类

```typescript
export class DomTemplateResult
```

具有稳定定义标识的直接 DOM 生成结果。

### 动态元素

**种类：**功能

```typescript
function dynamicElement(
  tag: unknown,
  properties: Readonly<Record<string, unknown>>,
  ...children: readonly unknown[]
): DynamicElementResult;
```

构建一个本机 Web 组件元素，其标签在运行时选择。

#### 参数

| 名称 | 类型                          | 描述 |
| ---- | ----------------------------- | ---- |
| 标签 | 未知                          |      |
| 属性 | 只读<Record<string, unknown>> |      |
| 儿童 | 只读未知[]                    |      |

### 动态元素结果

**种类：**类

```typescript
export class DynamicElementResult
```

计算 JSX 标记的运行时选择元素及其子元素。

### 锻造元素

**种类：**类

```typescript
export class ForgeElement extends ForgeHTMLElement
```

用于生成的自定义元素的本机 `HTMLElement` 基类 — a
`LitElement` 的无依赖性替代品。

子类通过 `static properties` 声明其反应表面（镜像
Lit)：每个键都成为一个原型访问器，其设置器安排一个
微任务重新渲染，每个非 `state` 键都会观察其小写字母
属性。 `render()` 返回元素的 {@link html}`…` 模板（已渲染）
进入开放的影子根。反应式提交更新现有模板
增量：位置列表重用兼容的条目和插槽出口
保留其原始投影节点，而无关部分发生变化。

### ForgeElement构造函数

**种类：**接口

```typescript
export interface ForgeElementConstructor
```

由生成的 Forge 元素实现的静态样式表合约。

URL 保存在元素类上而不是文档中，因此存在阴影
root 可以加载文档级样式无法到达的已编译 sidecar。

### ForgeElementHostConstructor

**种类：**类型

```typescript
export type ForgeElementHostConstructor = new (...args: any[]) => HTMLElement;
```

{@link ForgeElementMixin} 接受的可构造原生元素类。

### ForgeElementMixin

**种类：**功能

```typescript
function ForgeElementMixin(Base: TBase): TBase & typeof ForgeElement;
```

将 Forge 的独立于主机的运行时添加到本机元素构造函数中。
定制内置程序不能同时扩展 `ForgeElement` 和 `HTMLDivElement`；
这个 mixin 复制行为，同时保留本机原型链。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 基地 | 数据库 |      |

### ForgeSlotMarker

**种类：**接口

```typescript
export interface ForgeSlotMarker extends HTMLElement
```

Web 组件编译器针对不明确的槽发出的运行时标记。

### 有槽内容

**种类：**功能

```typescript
function hasSlotContent(host: Element, name?: string): boolean;
```

是否为 `host` 提供了其插槽之一的内容 - 本机目标的
替代中性 `hasSlot('x')` 标记。

`hasSlot` 是编译时词汇表：Vue 将其降低为 `!!slots.x`，React 将其降低为
`properties.x != null`，而自定义元素两者都没有，因此进行检查
相反，针对 DOM。生成的元素渲染成 **开放阴影
root**，这意味着消费者项目的内容保留在主机中
**light** DOM 作为其子级 - 命名槽由携带的子级填充
`slot="<name>"`，以及任何不携带任何子项的默认插槽。仅
考虑直接子代，就像插槽分配本身一样，并且
仅空白文本 node 不算作内容。

孩子们被步行而不是用 `[slot="…"]` 查询，所以插槽名称
不需要 CSS 转义，因此是一个裸文本子项（没有选择器可以匹配）
仍然填充默认槽。

请注意，只有当主机的子节点存在时，槽才可被观察到：
解析器对用初始 HTML 编写的元素的升级，`connectedCallback`
在附加它们之前运行。稍后投影的内容会重新渲染元素
通过通常的反应路径。

#### 参数

| 名称   | 类型   | 描述 |
| ------ | ------ | ---- |
| 主持人 | 元素   |      |
| 名称   | 字符串 |      |

### html

**种类：**功能

```typescript
function html(strings: TemplateStringsArray, ...values: readonly unknown[]): TemplateResult;
```

标记模板工厂。捕获调用站点的静态字符串和动态字符串
值转化为 {@link TemplateResult} 以便 {@link render} 实现为 DOM。

#### 参数

| 名称   | 类型           | 描述 |
| ------ | -------------- | ---- |
| 字符串 | 模板字符串数组 |      |
| 价值观 | 只读未知[]     |      |

### HTML内容

**种类：**功能

```typescript
function HtmlContent(properties: HtmlContentProperties): HtmlContentResult;
```

构建本机 Web 组件原始内容结果，无需转义 `html`。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 属性 | Html 内容属性 |      |

### Html内容属性

**种类：**接口

```typescript
export interface HtmlContentProperties
```

本机 Web 组件 HtmlContent 帮助器接受的属性。

### Html内容结果

**种类：**类

```typescript
export class HtmlContentResult
```

本机主机加上受信任的原始子标记，由 {@link render} 呈现。

### 没有什么

**种类：**常数

```typescript
export const nothing: unique symbol;
```

Sentinel 呈现为“无值”：在子位置它不产生任何内容，在
属性位置它删除属性 - 本地等效项
lit-html 的 `nothing`。

### 财产声明

**种类：**接口

```typescript
export interface PropertyDeclaration
```

{@link ForgeElement} 子类上的反应式属性声明。

### 原始HTML

**种类：**类

```typescript
export class RawHtml
```

本机 Web 组件适配器使用的可信原始 HTML 值。

### 使成为

**种类：**功能

```typescript
function render(
  result: TemplateResult | DomTemplateResult | DynamicElementResult | HtmlContentResult,
  container: ParentNode,
): void;
```

使用持久模板实例将结果渲染到 `container` 中。静态
保留 DOM 和本机表单状态，而仅更改绑定范围
更新；不兼容的模板类​​型会替换渲染器拥有的内容。

#### 参数

| 名称   | 类型                                                         | 描述 |
| ------ | ------------------------------------------------------------ | ---- |
| 结果   | 模板结果 \| DomTemplateResult \|动态元素结果 \| Html内容结果 |      |
| 集装箱 | 父节点                                                       |      |

### 解析ForgeSlotMarkers

**种类：**功能

```typescript
function resolveForgeSlotMarkers(
  root: ParentNode,
  owner: ForgeElement,
  sourceChildren: readonly Node[],
  destinationRoot: ShadowRoot,
): void;
```

解析渲染片段中编译器发出的运行时槽标记。

没有内容属性的标记成为本机插槽，保留
浏览器的后备和赋值语义，除非相同的插槽
在渲染树中重复。重复出口实现了克隆
原始的 light-DOM 子级，因此每个出口都会接收确定性内容。
当内容标记的值是所有者的时，内容标记将降低到本机插槽
原来的light-DOMchildren和outlet不重复；否则他们的
价值的实现是最后的手段。

#### 参数

| 名称     | 类型        | 描述 |
| -------- | ----------- | ---- |
| 根       | 父节点      |      |
| 业主     | 锻造元素    |      |
| 来源儿童 | 只读 Node[] |      |
| 目的地根 | 影子根      |      |

### 模板结果

**种类：**类

```typescript
export class TemplateResult
```

解析后的 `html\`…\`` 结果：静态字符串加上插值。

### 不安全的HTML

**种类：**功能

```typescript
function unsafeHtml(value: string): RawHtml;
```

将字符串标记为本机 Web 组件路径的受信任子标记。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 价值 | 字符串 |      |

### 用户ID

**种类：**功能

```typescript
function useId(): string;
```

本机目标替换 React 的 `useId`。

中性词汇表为将控件与其关联的 ID 提供 `useId()`
`<label for>` / `aria-describedby`； React 将其映射到自己的 `useId` 和 Solid
到 `createUniqueId`，但自定义元素没有可以借用的框架运行时
一个来自——所以就是这样。

模块级计数器在这里就足够了，并且故意比
随机/散列生成器：ID 只需要在**文档中**是唯一的，
编译器将 `const x = useId()` 从 `render()` 提升为
实例字段，因此每个元素实例只调用一次，然后
在其整个生命周期中保留其 id。因此，连续调用永远不会
碰撞，不需要依赖项（nanoid，`crypto.randomUUID`），并且
结果稳定且可调试（`forge-1`、`forge-2`，...）。该值为
有效的 HTML `id` 和有效的 CSS 标识符，因此可以在选择器中使用
没有逃脱。

### WebComponentsInternalsPolicy

**种类：**接口

```typescript
export interface WebComponentsInternalsPolicy
```

针对平台的 {@link ElementInternals} API 的功能请求。

### WebComponentShadowPolicy

**种类：**接口

```typescript
export interface WebComponentsShadowPolicy
```

生成的 Forge 元素接受类型化的影子根选项。

## `src/runtime/class-names`

### 类名

**种类：**功能

```typescript
function classNames(...values: readonly ClassValue[]): string;
```

将任意数量的 {@link ClassValue} 合并到一个以空格分隔的类中
字符串，删除虚假条目并删除重复的类名（
第一次出现获胜）。当没有任何活动时返回空字符串。

#### 参数

| 名称   | 类型              | 描述 |
| ------ | ----------------- | ---- |
| 价值观 | 只读 ClassValue[] |      |

### 类值

**种类：**类型

```typescript
export type ClassValue = | string | number | bigint | null | undefined | boolean |
```

{@link classNames} 接受的单个值。

## `src/runtime/context`

### 创建上下文

**种类：**功能

```typescript
function createContext(defaultValue: T): MpContext<T>;
```

使用给定的默认值创建一个框架中立的上下文。返回一个
携带 `Provider` 元素和默认值的句柄；读取当前值
与{@link useContext}。

#### 参数

| 名称   | 类型 | 描述 |
| ------ | ---- | ---- |
| 默认值 | T    |      |

### isContextProvider

**种类：**功能

```typescript
function isContextProvider(value: unknown): value is MpContextProvider<unknown>;
```

值是否是上下文{@link MpContext.Provider}函数。

#### 参数

| 名称 | 类型 | 描述 |
| ---- | ---- | ---- |
| 价值 | 未知 |      |

### MP_上下文

**种类：**常数

```typescript
export const MP_CONTEXT: unique symbol;
```

运行时适配器用于识别上下文的属性{@link MpContext.Provider}
在行走中性树时函数（并恢复它提供的上下文）。

### MP上下文

**种类：**组件

```typescript
export interface MpContext<T>
```

由 {@link createContext} 返回的框架中立上下文句柄。

### MpContextProvider

**种类：**组件

```typescript
export interface MpContextProvider<T> extends MpComponent<MpContextProviderProperties<T>>
```

上下文提供者函数，用它提供的上下文进行标记。

### MpContextProvider 属性

**种类：**组件

```typescript
export interface MpContextProviderProperties<T>
```

{@link MpContext.Provider} 元素接受的属性。

### 使用上下文

**种类：**功能

```typescript
function useContext(context: MpContext<T>): T;
```

读取上下文的当前值：最近的封闭值
`<Ctx.Provider>` 的 `value`，或者没有上下文时的默认值。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| 背景 | MpContext<T> |      |

## `src/runtime/dynamic`

### 动态的

**种类：**常数

```typescript
export const Dynamic: MpComponent<MpDynamicProperties>;
```

用作动态组件的元素 `type` 的标记
（`<Dynamic is={…}>…</Dynamic>`）。

编写的组件从 `@mission-platform/forge` 导入它；两个运行时
适配器和构建时编译器会专门识别它。从来都不是
直接渲染 - 适配器拦截它（使用以下命令渲染 `is`
转发的属性/子项），编译器将其重写为目标
框架的动态组件形式。

### Mp动态属性

**种类：**组件

```typescript
export interface MpDynamicProperties extends MpPropertyBag
```

{@link Dynamic} 元素接受的属性。

这是合法保留 **开袋** 的少数声明之一：
除 `is` 之外的每个属性都将逐字转发到元素 `is`
解析为，并且该元素仅在运行时才知道，因此它的属性
此处无法枚举 Accepts。组件 props 接口必须*不*
遵循这种模式 - 请参阅{@link MpPropertyBag}。

## `src/runtime/h`

### h

**种类：**功能

```typescript
function h(
  type: MpElementType | ((properties: P) => MpElement),
  properties?: P | null,
  ...children: MpChild[]
): MpElement;
```

经典的 JSX 工厂 (`jsxFactory: 'h'`)。

针对此运行时编写的每个 JSX 表达式都会编译为对
`h`，它构建了一个框架中立的{@link MpElement}。儿童可能会到达
作为尾随可变参数（经典变换）或通过
`properties.children`；两者都被标准化为单个平面数组。

#### 参数

| 名称 | 类型                                      | 描述 |
| ---- | ----------------------------------------- | ---- |
| 类型 | MpElementType \| ((属性：P) => MpElement) |      |
| 属性 | P                                         | 空   |     |
| 儿童 | MpChild[]                                 |      |

## `src/runtime/hooks`

### Mp依赖列表

**种类：**组件

```typescript
export type MpDependencyList = readonly unknown[];
```

控制效果/备忘录何时重新运行的依赖项列表。

### Mp效果回调

**种类：**组件

```typescript
export type MpEffectCallback = () => void | MpEffectCleanup;
```

由 {@link useEffect} 运行的效果回调。

### Mp效果清理

**种类：**组件

```typescript
export type MpEffectCleanup = () => void;
```

清理函数效果可能会返回。

### 参考文献

**种类：**组件

```typescript
export interface MpRef<T>
```

可变引用容器，镜像 React 的 `MutableRefObject`。

### Mp设置状态

**种类：**组件

```typescript
export type MpSetState<T> = (value: T | ((previous: T) => T)) => void;
```

将一段状态更新为新值或通过更新器函数。

### 使用回调

**种类：**功能

```typescript
function useCallback(callback: T, _dependencies?: MpDependencyList): T;
```

中立 `useCallback`。返回渲染时未更改的回调。

#### 参数

| 名称      | 类型             | 描述             |
| --------- | ---------------- | ---------------- |
| 回调      | T                |                  |
| _依赖关系 | MpDependencyList | MpDependencyList |     |

### 使用效果

**种类：**功能

```typescript
function useEffect(_effect: MpEffectCallback, _dependencies?: MpDependencyList): void;
```

中立 `useEffect`。无操作：效果模型渲染后副作用，其中
不要在单个 SSR/适配器渲染期间运行。

#### 参数

| 名称      | 类型             | 描述             |
| --------- | ---------------- | ---------------- |
| _效果     | MpEffectCallback | MpEffectCallback |     |
| _依赖关系 | MpDependencyList | MpDependencyList |     |

### 用户ID

**种类：**功能

```typescript
function useId(): string;
```

中立 `useId`。镜像 React 和 Vue 的 `useId`：返回稳定、独特的
组件实例的 id 字符串，适用于标签 / `aria-describedby`
没有手卷柜台的协会。基线实施手
输出一个进程递增 ID（`:mp0:`、`:mp1:`、...）；构建时插件
将此导入交换为每个框架的本机 `useId`（React / Vue），而
运行时适配器和 SSR 共享这种确定性形式，因此单个渲染
保持稳定（并且在两个框架中都是相同的）。

### 使用备忘录

**种类：**功能

```typescript
function useMemo(factory: () => T, _dependencies?: MpDependencyList): T;
```

中立 `useMemo`。为渲染计算一次值。

#### 参数

| 名称      | 类型             | 描述             |
| --------- | ---------------- | ---------------- |
| 工厂      | () => T          |                  |
| _依赖关系 | MpDependencyList | MpDependencyList |     |

### 使用引用

**种类：**功能

```typescript
function useRef(initial: T): MpRef<T>;
```

中立 `useRef`。返回单个的新 `{ current }` 容器
渲染；框架运行时在渲染过程中保留它。

#### 参数

| 名称 | 类型 | 描述 |
| ---- | ---- | ---- |
| 初始 | T    |      |

### 使用状态

**种类：**功能

```typescript
function useState(initial: T | (() => T)): [T, MpSetState<T>];
```

中立 `useState`。基线实现返回初始值并
一个无操作设置器（如果没有框架重新渲染，状态永远不会改变），这是
单个 SSR/适配器渲染的正确行为。

#### 参数

| 名称 | 类型 | 描述      |
| ---- | ---- | --------- |
| 初始 | T    | (() => T) |     |

## `src/runtime/html-content`

### HTML内容

**种类：**常数

```typescript
export const HtmlContent: MpComponent<HtmlContentProperties>;
```

标记用作受信任的原始 HTML 内容的元素类型。

中立适配器和 Forge 编译器拦截此标记并降低
它到他们的原生原始内容操作。决不能直接调用它。

### Html内容属性

**种类：**接口

```typescript
export interface HtmlContentProperties extends MpPropertyBag
```

{@link HtmlContent} 原语接受的属性。

除了 `html`/`as` 之外，每个剩余属性都会逐字转发到主机
元素（属性、事件处理程序、`ref`），因此声明保持开放状态
bag 而不是枚举整个 DOM 属性表面。

## `src/runtime/slots`

### 有槽位

**种类：**功能

```typescript
function hasSlot(name?: string): boolean;
```

封闭组件是否被赋予槽的内容 - 中性
Vue 的 `$slots.x` / React `properties.x != null` 检查的对应项。安
省略（或 `'default'`）名称以默认插槽（`children`）为目标：

```tsx
{
  hasSlot('footer') ? (
    <footer>
      <Slot name="footer" />
    </footer>
  ) : undefined;
}
```

`@mission-platform/vite-plugin-forge` 将调用直接重写为每个
框架的本机存在检查 - Vue 的 `!!slots.x`（`$slots.x` 在
`v-if`) 和 React 的 `properties.x != null` — 因此一次写入组件可以
仅当槽已填充时才渲染可选包装区域
框架。这里的实现是运行时适配器/SSR基线，
读取当前组件的插槽范围。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 名称 | 字符串 |      |

### Mp渲染属性

**种类：**组件

```typescript
export type MpRenderProperty<S = MpPropertyBag> = (scope: S) => MpChild | readonly MpChild[];
```

**render-prop /scoped-slot** 函数：给定它返回的插槽范围
要呈现的内容。一次写入组件通过传递来呈现作用域槽
`<Slot name="…" item={item} index={i} />` 上属性的范围；的
消费者提供匹配函数（通常为 `children`），
编译器映射到 Vue 作用域槽 (`slots.x?.(scope)`) 和 React
渲染属性调用（`props.x?.(scope)`）。

### MpSlot内容

**种类：**组件

```typescript
export type MpSlotContent = MpChild | readonly MpChild[] | MpRenderProperty;
```

为槽提供的内容 - 可渲染的子项或函数
返回它们的插槽（作用域）道具（作用域插槽）。

### MpSlot 属性

**种类：**组件

```typescript
export interface MpSlotProperties extends MpPropertyBag
```

{@link Slot} 元素接受的属性。

`<Slot>` 是一个真正接受任意额外内容的中立元素
属性：**范围**槽将其范围作为属性传递
(`<Slot name="row" item={item} index={i} />`)，因此声明保留
打开袋子而不是枚举它不知道的属性。

### 弹出槽范围

**种类：**功能

```typescript
function popSlotScope(): void;
```

扩展组件的子树后，弹出当前槽范围。

### 推槽范围

**种类：**功能

```typescript
function pushSlotScope(properties: MpPropertyBag): void;
```

将要扩展的组件的 props 推到插槽范围上。

#### 参数

| 名称 | 类型          | 描述          |
| ---- | ------------- | ------------- |
| 属性 | MpPropertyBag | MpPropertyBag |     |

### 解析槽位

**种类：**功能

```typescript
function resolveSlot(properties: MpSlotProperties, fallback: readonly MpChild[]): MpChild | readonly MpChild[];
```

根据封闭的内容解析 `<Slot name="…" />` 的内容
组件的 props，当没有任何东西时，回落到插槽自己的孩子
提供。作用域插槽（函数值插槽属性）通过
slot 元素的剩余（非 `name`）属性。

#### 参数

| 名称 | 类型           | 描述 |
| ---- | -------------- | ---- |
| 属性 | MpSlot 属性    |      |
| 后备 | 只读 MpChild[] |      |

## `src/runtime/teleport`

### MpTeleport 属性

**种类：**组件

```typescript
export interface MpTeleportProperties
```

{@link Teleport} 元素接受的属性。

### 传送

**种类：**常数

```typescript
export const Teleport: MpComponent<MpTeleportProperties>;
```

用作门户 (`<Teleport to="…">…</Teleport>`) 元素 `type` 的标记。

编写的组件从 `@mission-platform/forge` 导入它；两个运行时
适配器和构建时编译器会专门识别它。从来都不是
直接渲染——适配器拦截它（在
place），编译器将其导入重新映射到目标框架的门户。

## `src/runtime/transition`

### MpTransitionGroup 属性

**种类：**组件

```typescript
export interface MpTransitionGroupProperties
```

{@link TransitionGroup} 元素接受的属性。

### MpTransition 属性

**种类：**组件

```typescript
export interface MpTransitionProperties
```

{@link Transition} 元素接受的属性。

### 过渡

**种类：**常数

```typescript
export const Transition: MpComponent<MpTransitionProperties>;
```

用作进入/离开转换的元素 `type` 的标记
（`<Transition name="…">…</Transition>`）。

编写的组件从 `@mission-platform/forge` 导入它；两个运行时
适配器和构建时编译器会专门识别它。从来都不是
直接渲染 - 适配器拦截它（将其子级渲染到位）
并且编译器将其导入重新映射到目标框架的转换。

### 过渡集团

**种类：**常数

```typescript
export const TransitionGroup: MpComponent<MpTransitionGroupProperties>;
```

用作**列表**进入/离开/移动转换的元素 `type` 的标记
（`<TransitionGroup name="…">{items.map(…)}</TransitionGroup>`），
{@link Transition} 的多子对应项。

一次写入组件对一个键控列表进行动画处理——一个 toast 堆栈、一个可重新排序的列表
列表 — 通过将映射的子项包装在 `<TransitionGroup>` 中，与
Vue 的内置 `<TransitionGroup>` 也是如此。就像{@link Transition} 一样，它从来都不是
直接调用：适配器通过身份拦截它（呈现其
SSR 的孩子就位）和 `@mission-platform/vite-plugin-forge` 重新映射其
导入到每个框架的本机组转换 - Vue 的内置
`<TransitionGroup>` 和 `@mission-platform/forge/react` CSS 级驱动程序。

## `src/runtime/types`

### 分段

**种类：**常数

```typescript
export const Fragment: MpFragment;
```

用作片段元素 `type` 的标记 (`<>…</>`)，即经典
转换的 `jsxFragmentFactory`。

与 {@link Slot}、{@link Transition} 和 {@link TransitionGroup} 一样，它是一个
功能组件标记而不是裸露的 `symbol`：在经典 `h` 下
工厂 片段工厂必须是 **可调用** JSX 类型，否则
创作 `<>…</>`（或显式 `<Fragment>`）无法进行类型检查
TS2604（“没有任何构造或调用签名”）。从来都不是
但实际上是被调用的——运行时适配器通过身份拦截它
(`type === Fragment`) 和构建时编译器
(`@mission-platform/vite-plugin-forge`) 将片段降低到每个框架的
自己的表单（React 的 `<>`、Vue 的内联子项）。直接调用是一个bug。

### 是Mp元素

**种类：**功能

```typescript
function isMpElement(value: unknown): value is MpElement;
```

将任意值缩小为 {@link MpElement}。

#### 参数

| 名称 | 类型 | 描述 |
| ---- | ---- | ---- |
| 价值 | 未知 |      |

### 儿童MP

**种类：**组件

```typescript
export type MpChild = MpElement | string | number | boolean | null | undefined;
```

中性树中的单个子槽。

### Mp组件

**种类：**组件

```typescript
export type MpComponent<P = MpPropertyBag> = (properties: P) => MpElement;
```

用中性 JSX 方言编写的组件：其纯函数
返回单个 {@link MpElement} 的属性。

`P` 是故意**不受约束**：组件的 props 接口
仅声明它实际接受的属性，并且这样的接口具有
没有隐式索引签名，因此将 `P` 限制为记录类型将
拒绝每一个诚实的道具声明。

### Mp元素

**种类：**组件

```typescript
export interface MpElement
```

由 {@link h} 生成的框架中立虚拟树中的 node。

### Mp元素类型

**种类：**组件

```typescript
export type MpElementType = string | MpFragment | MpComponent;
```

任何可能作为元素类型出现在中性树中的东西。

### Mp财产袋

**种类：**组件

```typescript
export type MpPropertyBag = Record<string, unknown>;
```

由 {@link MpElement} 携带的 **运行时** 属性/道具包。

这是故意的一个简单的、无类型的记录：这就是 JSX 工厂的内容
构建以及在遍历中性树时适配器索引到的内容。它是
*不是*组件 props 的基类型——组件准确地声明了
它接受的属性（包括 `children?: MpChild | readonly MpChild[]`
当它渲染它们时），以便多余的属性检查和 `keyof` 保留
对每个编译目标都有意义。每个元素的少数属性
接受，无论在{@link MpReservedProperties}中声明一次。

### Mp保留属性

**种类：**组件

```typescript
export interface MpReservedProperties
```

每个 JSX 元素在属性之上接受的**保留**属性
它声明自己，通过选择加入连接为 `JSX.IntrinsicAttributes`
`@mission-platform/forge/jsx-globals` 打字。

- `key` 标识渲染列表中的条目，以便目标框架可以
  跨更新进行协调（React 的 `key`、Vue 的 `:key`）。
- `slot` 将此元素路由到其父组件的**命名槽**
  （`<ForgeIcon slot="start" />`）。编译器将其转换为 Vue
  `<template #start>` 块/React `start` 属性和运行时适配器
  将其折叠到父级的道具中（请参阅 `collectSlottedChildren`）。省略（或
  `"default"`) 表示默认插槽。

两者都不是组件自己的 props 合约的一部分——两者都由组件读取
_父级_（协调器、槽路由器），绝不是由组件本身 -
所以这里的每个元素都接受它们，而不是在每个元素上声明它们
道具界面。

### 投币口

**种类：**常数

```typescript
export const Slot: MpComponent;
```

用作命名槽 (`<Slot name="…" />`) 的元素 `type` 的标记。

与 {@link Fragment} 一样，它是一个功能组件标记，因此经典的 `h`
工厂接受 `<Slot name="…" />` 作为有效的 JSX 元素类型。
但它从未被实际调用：构建时编译器
(`@mission-platform/vite-plugin-forge`) 将每个 `<Slot>` 重写到目标
框架自己的槽机制，运行时适配器通过以下方式拦截它
任何调用之前的身份 (`type === Slot`)。直接调用是一个bug。
