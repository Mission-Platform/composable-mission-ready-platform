# مرجع @mission-platform/forge-web-script-cli API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/forge-web-script-cli/docs/reference/generated/api.md: [packages/forge-web-script-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-web-script-cli`.

## `src/args`

### FORGE_WEB_SCRIPT_CLI_USAGE

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_CLI_USAGE;
```

لم يتم تقديم أي وصف.

### ForgeWebScriptCliCommand

**النوع:** النوع

```typescript
export type ForgeWebScriptCliCommand = 'check' | 'compile' | 'trace';
```

لم يتم تقديم أي وصف.

### ForgeWebScriptCliOptions

**النوع:** الواجهة

```typescript
export interface ForgeWebScriptCliOptions
```

لم يتم تقديم أي وصف.

### ForgeWebScriptCliTraceOptions

**النوع:** الواجهة

```typescript
export interface ForgeWebScriptCliTraceOptions
```

لم يتم تقديم أي وصف.

### ForgeWebScriptCliUsageError

**النوع:** فئة

```typescript
export class ForgeWebScriptCliUsageError extends Error
```

لم يتم تقديم أي وصف.

### parseForgeWebScriptCliArgs

**النوع:** الوظيفة

```typescript
function parseForgeWebScriptCliArgs(argv: readonly string[], cwd = process.cwd()): ForgeWebScriptCliOptions;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب                | الوصف |
| ----- | ------------------- | ----- |
| argv  | سلسلة للقراءة فقط[] |       |
| كود   |                     |       |

## `src/main`

### FORGE_WEB_SCRIPT_CLI_COMPILATION_EXIT_CODE

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_CLI_COMPILATION_EXIT_CODE;
```

لم يتم تقديم أي وصف.

### FORGE_WEB_SCRIPT_CLI_USAGE_EXIT_CODE

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_CLI_USAGE_EXIT_CODE;
```

لم يتم تقديم أي وصف.

### ForgeWebScriptCliIo

**النوع:** الواجهة

```typescript
export interface ForgeWebScriptCliIo
```

لم يتم تقديم أي وصف.

### runForgeWebScriptCli

**النوع:** الوظيفة

```typescript
function runForgeWebScriptCli(
  argv: readonly string[] = process.argv.slice(2),
  io: ForgeWebScriptCliIo = defaultIo,
  cwd = process.cwd(),
): Promise<number>;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب                  | الوصف |
| ----- | --------------------- | ----- |
| argv  | سلسلة للقراءة فقط[]   |       |
| ايو   | فورجي ويب سكريبت كليو |       |
| كود   |                       |       |

## `src/output`

### artifactFilesFor

**النوع:** الوظيفة

```typescript
function artifactFilesFor(artifact: ForgeWebScriptArtifact): ForgeWebScriptCliArtifactFiles;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم      | اكتب                   | الوصف |
| ---------- | ---------------------- | ----- |
| قطعة أثرية | ForgeWebScriptArtifact |       |

### forgeWebScriptArtifactBaseName

**النوع:** الوظيفة

```typescript
function forgeWebScriptArtifactBaseName(entryFileName: string): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم           | اكتب  | الوصف |
| --------------- | ----- | ----- |
| اسم ملف الإدخال | سلسلة |       |

### ForgeWebScriptCliArtifactFiles

**النوع:** الواجهة

```typescript
export interface ForgeWebScriptCliArtifactFiles
```

لم يتم تقديم أي وصف.

### formatForgeWebScriptDiagnostics

**النوع:** الوظيفة

```typescript
function formatForgeWebScriptDiagnostics(
  diagnostics: readonly {
    readonly code: string;
    readonly severity: string;
    readonly phase: string;
    readonly message: string;
    readonly fileName: string;
    readonly span: {
      readonly line: number;
      readonly column: number;
      readonly endLine: number;
      readonly endColumn: number;
    };
    readonly hint?: string;
  }[],
): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم   | اكتب                                                                                                                                                                                                                                                                                                                   | الوصف |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| التشخيص | للقراءة فقط {رمز للقراءة فقط: سلسلة؛ خطورة للقراءة فقط: سلسلة؛ مرحلة القراءة فقط: سلسلة؛ رسالة للقراءة فقط: سلسلة؛ اسم الملف للقراءة فقط: سلسلة؛ نطاق للقراءة فقط: { سطر للقراءة فقط: رقم؛ عمود للقراءة فقط: الرقم؛ خط النهاية للقراءة فقط: الرقم؛ عمود النهاية للقراءة فقط: الرقم؛ }; تلميح للقراءة فقط ؟: سلسلة؛ }[] |       |

### OutputDirectoryFor

**النوع:** الوظيفة

```typescript
function outputDirectoryFor(
  entryFileName: string,
  outputDirectory = path.join(path.dirname(entryFileName), 'dist'),
): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم           | اكتب  | الوصف |
| --------------- | ----- | ----- |
| اسم ملف الإدخال | سلسلة |       |
| دليل الإخراج    |       |       |

### writeForgeWebScriptArtifacts

**النوع:** الوظيفة

```typescript
function writeForgeWebScriptArtifacts(
  outputDirectory: string,
  entryFileName: string,
  artifact: ForgeWebScriptArtifact,
): Promise<readonly string[]>;
```

اكتب المجموعة الكاملة من خلال دليل مؤقت وأعد تسمية كل ملف إلى مكانه.

#### حدود

| الاسم           | اكتب                   | الوصف |
| --------------- | ---------------------- | ----- |
| دليل الإخراج    | سلسلة                  |       |
| اسم ملف الإدخال | سلسلة                  |       |
| قطعة أثرية      | ForgeWebScriptArtifact |       |
