# הפניה ל-@mission-platform/forge-web-script-stdlib API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/forge-web-script-stdlib/docs/reference/generated/api.md: [packages/forge-web-script-stdlib/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/forge-web-script-stdlib`.

## `fws/option`

### זה_כמה

**סוג:** fws-export

```typescript
fn is_some(value: Option<T>) -> bool
```

מחזירה true כאשר האפשרות מכילה ערך.

#### פרמטרים

| שם  | הקלד      | תיאור              |
| --- | --------- | ------------------ |
| ערך | אפשרות<T> | ערך אפשרות לבדיקה. |

#### חוֹזֶה

- **@param:** ערך ערך אפשרות לבדיקה.
- **@returns:** `true` עבור `Some`, אחרת `false`.

### אוֹפְּצִיָה

**סוג:** fws-export

```typescript
enum Option
```

מיכל ערך אופציונלי בשימוש בכל חבילות Forge Web Script.

`None` מייצג את היעדר ערך. `Some(T)` נושא ערך נוכחי מסוג `T`. העדיפו את `match` על פני צ'קים בוליאניים כאשר שני הסניפים צריכים את המטען.

## `fws/result`

### זה_בסדר

**סוג:** fws-export

```typescript
fn is_ok(value: Result<T, E>) -> bool
```

מחזירה אמת כאשר התוצאה מוצלחת.

#### פרמטרים

| שם  | הקלד        | תיאור             |
| --- | ----------- | ----------------- |
| ערך | תוצאה<T, E> | ערך תוצאה לבדיקה. |

#### חוֹזֶה

- **@param:** ערך ערך תוצאה לבדיקה.
- **@returns:** `true` עבור `Ok`, אחרת `false`.

### תוֹצָאָה

**סוג:** fws-export

```typescript
enum Result
```

תוצאה של ניתוח שגוי.

`Ok(T)` נושא ערך מוצלח. `Error(E)` נושא כשל שניתן לשחזר. השתמש ב-`match` כדי לטפל בשתי הגרסאות ללא יוצא מן הכלל.

## `src/index`

### FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE

**סוג:** קבוע

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE;
```

נתיב יחסי למודול הצהרת אסינכרון המשמש צרכני מהדר.

### FORGE_WEB_SCRIPT_STDLIB_IDENTITY

**סוג:** קבוע

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_IDENTITY;
```

חוזה זהות וייצוג יציב עבור הספרייה הסטנדרטית.
ערכים מצטברים משתמשים בפריסות מונומורפיות במידת האפשר ובמתאר
גבולות לערכים שחוצים את המארח/Wasm ABI.

### FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT

**סוג:** קבוע

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT;
```

ספרייה המכילה את מודולי ההצהרה של Forge Web Script שנשלחו.
