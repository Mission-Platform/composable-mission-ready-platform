# @mission-platform/forge-web-script-wasm API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/forge-web-script-wasm/docs/reference/generated/api.md: [packages/forge-web-script-wasm/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/forge-web-script-wasm` 中的公共源声明生成。

## `src/collection-runtime`

### buildForgeWebScriptWasmCollectionRuntimeBodies

**种类：**功能

```typescript
function buildForgeWebScriptWasmCollectionRuntimeBodies(): readonly ForgeWebScriptWasmRuntimeBody[];
```

没有提供描述。

### buildForgeWebScriptWasmCollectionRuntimeWasmBodies

**种类：**功能

```typescript
function buildForgeWebScriptWasmCollectionRuntimeWasmBodies(allocatorIndex: number): readonly number[][];
```

运行时主体使用 i32 句柄。数组是[长度，元素...]和向量
是[数据指针，长度，容量]，具有四字节标量元素槽。
数组迭代器是[数组句柄，下一个索引]。迭代器结果使用
打包 i64 `(done << 32) | i32 value` ABI。

#### 参数

| 名称       | 类型 | 描述 |
| ---------- | ---- | ---- |
| 分配器索引 | 数量 |      |

### ForgeWebScriptWasmRuntimeBody

**种类：**接口

```typescript
export interface ForgeWebScriptWasmRuntimeBody
```

没有提供描述。

## `src/contracts`

### ForgeWebScriptTarget功能

**种类：**接口

```typescript
export interface ForgeWebScriptTargetFeatures
```

没有提供描述。

### ForgeWebScriptWasmArtifact元数据

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactMetadata
```

没有提供描述。

### ForgeWebScriptWasmAsyncCapability

**种类：**类型

```typescript
export type ForgeWebScriptWasmAsyncCapability = 'scheduler.microtask' | 'scheduler.worker';
```

没有提供描述。

### ForgeWebScriptWasmAsyncContract

**种类：**接口

```typescript
export interface ForgeWebScriptWasmAsyncContract
```

没有提供描述。

### ForgeWebScriptWasmBackendInput

**种类：**接口

```typescript
export interface ForgeWebScriptWasmBackendInput
```

没有提供描述。

### ForgeWebScriptWasmBackendResult

**种类：**接口

```typescript
export interface ForgeWebScriptWasmBackendResult
```

没有提供描述。

### ForgeWebScriptWasmBinaryOperator

**种类：**类型

```typescript
export type ForgeWebScriptWasmBinaryOperator =
  '!=' | '%' | '&&' | '*' | '+' | '-' | '/' | '<' | '<=' | '==' | '>' | '>=' | '||';
```

没有提供描述。

### ForgeWebScriptWasmCapabilityImport

**种类：**接口

```typescript
export interface ForgeWebScriptWasmCapabilityImport
```

没有提供描述。

### ForgeWebScriptWasmCollectionLayout

**种类：**接口

```typescript
export interface ForgeWebScriptWasmCollectionLayout
```

没有提供描述。

### ForgeWebScriptWasm编译器提示

**种类：**接口

```typescript
export interface ForgeWebScriptWasmCompilerHints
```

没有提供描述。

### ForgeWebScriptWasm诊断

**种类：**接口

```typescript
export interface ForgeWebScriptWasmDiagnostic
```

没有提供描述。

### ForgeWebScriptWasmEnumDeclaration

**种类：**接口

```typescript
export interface ForgeWebScriptWasmEnumDeclaration
```

没有提供描述。

### ForgeWebScriptWasmExpression

**种类：**类型

```typescript
export type ForgeWebScriptWasmExpression = |
```

没有提供描述。

### ForgeWebScriptWasmFeatureRequirements

**种类：**接口

```typescript
export interface ForgeWebScriptWasmFeatureRequirements
```

没有提供描述。

### ForgeWebScriptWasmFunction

**种类：**接口

```typescript
export interface ForgeWebScriptWasmFunction
```

没有提供描述。

### ForgeWebScriptWasm通用专业化

**种类：**接口

```typescript
export interface ForgeWebScriptWasmGenericSpecialization
```

没有提供描述。

### ForgeWebScriptWasmIteratorBoundaryDescriptor

**种类：**接口

```typescript
export interface ForgeWebScriptWasmIteratorBoundaryDescriptor
```

没有提供描述。

### ForgeWebScriptWasmIteratorCapability

**种类：**类型

```typescript
export type ForgeWebScriptWasmIteratorCapability = 'linear' | 'random-access';
```

没有提供描述。

### ForgeWebScriptWasmIteratorCapabilityDescriptor

**种类：**接口

```typescript
export interface ForgeWebScriptWasmIteratorCapabilityDescriptor
```

没有提供描述。

### ForgeWebScriptWasmIteratorExport

**种类：**接口

```typescript
export interface ForgeWebScriptWasmIteratorExport
```

没有提供描述。

### ForgeWebScriptWasmLogger

**种类：**接口

```typescript
export interface ForgeWebScriptWasmLogger
```

没有提供描述。

### ForgeWebScriptWasm 模块

**种类：**接口

```typescript
export interface ForgeWebScriptWasmModule
```

没有提供描述。

### ForgeWebScriptWasm参数

**种类：**接口

```typescript
export interface ForgeWebScriptWasmParameter
```

没有提供描述。

### ForgeWebScriptWasmPrimitiveType

**种类：**类型

```typescript
export type ForgeWebScriptWasmPrimitiveType =
  'bool' | 'bytes' | 'f32' | 'f64' | 'i32' | 'i64' | 'string' | 'u32' | 'u64' | 'unit';
```

没有提供描述。

### ForgeWebScriptWasmSourceImport

**种类：**接口

```typescript
export interface ForgeWebScriptWasmSourceImport
```

没有提供描述。

### ForgeWebScriptWasmSourceSpan

**种类：**接口

```typescript
export interface ForgeWebScriptWasmSourceSpan
```

没有提供描述。

### ForgeWebScriptWasm标准库操作

**种类：**类型

```typescript
export type ForgeWebScriptWasmStandardLibraryOperation =
  | 'full-match'
  | 'prefix-match'
  | 'search'
  | 'full-capture-start'
  | 'full-capture-end'
  | 'prefix-capture-start'
  | 'prefix-capture-end'
  | 'search-capture-start'
  | 'search-capture-end'
  | 'string-concat'
  | 'string-length'
  | 'string-byte-at'
  | 'string-starts-with'
  | 'string-slice'
  | 'string-to-i32'
  | 'bytes-length'
  | 'bytes-length-u32'
  | 'bytes-byte-at'
  | 'bytes-byte-at-u32'
  | 'bytes-slice'
  | 'memory-alloc'
  | 'memory-dealloc'
  | 'memory-realloc'
  | 'memory-load-u32'
  | 'memory-store-u32'
  | 'memory-load-f64'
  | 'memory-store-f64'
  | 'f64-from-u32'
  | 'vector-new'
  | 'vector-push'
  | 'vector-get'
  | 'vector-set'
  | 'vector-length'
  | 'vector-pop'
  | 'array-new'
  | 'array-get'
  | 'array-set'
  | 'array-length'
  | 'array-iter'
  | 'iterator-next'
  | 'set-has'
  | 'map-get'
  | 'ecs-query'
  | 'ecs-transition'
  | 'signal-schedule'
  | 'async-schedule-microtask'
  | 'async-worker-post'
  | 'async-worker-receive';
```

没有提供描述。

### ForgeWebScriptWasm语句

**种类：**类型

```typescript
export type ForgeWebScriptWasmStatement = |
```

没有提供描述。

### ForgeWebScriptWasmTypeName

**种类：**接口

```typescript
export interface ForgeWebScriptWasmTypeName
```

没有提供描述。

## `src/emitter`

### 编译ForgeWebScriptWasm

**种类：**功能

```typescript
function compileForgeWebScriptWasm(
  input: ForgeWebScriptWasmBackendInput,
  fileName = '<input>',
): ForgeWebScriptWasmBackendResult;
```

没有提供描述。

#### 参数

| 名称   | 类型                           | 描述 |
| ------ | ------------------------------ | ---- |
| 输入   | ForgeWebScriptWasmBackendInput |      |
| 文件名 |                                |      |

## `src/verifier`

### ForgeWebScriptWasmArtifactManifest

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactManifest
```

验证者有意使用结构清单以避免包循环。

### ForgeWebScriptWasmArtifactVerificationDiagnostic

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactVerificationDiagnostic
```

没有提供描述。

### ForgeWebScriptWasmArtifactVerificationInput

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactVerificationInput
```

没有提供描述。

### ForgeWebScriptWasmArtifactVerificationPolicy

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactVerificationPolicy
```

没有提供描述。

### ForgeWebScriptWasmArtifactVerificationResult

**种类：**接口

```typescript
export interface ForgeWebScriptWasmArtifactVerificationResult
```

没有提供描述。

### ForgeWebScriptWasmArtifactVerificationSeverity

**种类：**类型

```typescript
export type ForgeWebScriptWasmArtifactVerificationSeverity = 'error' | 'warning' | 'info';
```

没有提供描述。

### ForgeWebScriptWasmManifestFunction

**种类：**接口

```typescript
export interface ForgeWebScriptWasmManifestFunction
```

没有提供描述。

### ForgeWebScriptWasmManifestImport

**种类：**接口

```typescript
export interface ForgeWebScriptWasmManifestImport
```

没有提供描述。

### ForgeWebScriptWasmManifestParameter

**种类：**接口

```typescript
export interface ForgeWebScriptWasmManifestParameter
```

没有提供描述。

### ForgeWebScriptWasmMemoryLayout

**种类：**接口

```typescript
export interface ForgeWebScriptWasmMemoryLayout
```

没有提供描述。

### verifyForgeWebScriptWasmArtifact

**种类：**功能

```typescript
function verifyForgeWebScriptWasmArtifact(
  input: ForgeWebScriptWasmArtifactVerificationInput,
): ForgeWebScriptWasmArtifactVerificationResult;
```

没有提供描述。

#### 参数

| 名称 | 类型                                        | 描述                                        |
| ---- | ------------------------------------------- | ------------------------------------------- |
| 输入 | ForgeWebScriptWasmArtifactVerificationInput | ForgeWebScriptWasmArtifactVerificationInput |     |

## `src/wat`

### ForgeWebScriptWasmWatMetadata

**种类：**接口

```typescript
export interface ForgeWebScriptWasmWatMetadata
```

没有提供描述。

### renderForgeWebScriptWasmWat

**种类：**功能

```typescript
function renderForgeWebScriptWasmWat(
  module: ForgeWebScriptWasmModule,
  metadata: ForgeWebScriptWasmWatMetadata = {},
): string;
```

没有提供描述。

#### 参数

| 名称   | 类型                          | 描述 |
| ------ | ----------------------------- | ---- |
| 模块   | ForgeWebScriptWasmModule      |      |
| 元数据 | ForgeWebScriptWasmWatMetadata |      |
