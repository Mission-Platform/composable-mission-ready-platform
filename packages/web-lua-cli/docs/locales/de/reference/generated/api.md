# @mission-platform/web-lua-cli API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/web-lua-cli/docs/reference/generated/api.md: [packages/web-lua-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/web-lua-cli`.

## `src/args`

### parseWebLuaCliArgs

**Art:** Funktion

```typescript
function parseWebLuaCliArgs(argv: readonly string[]): WebLuaCliOptions;
```

Keine Beschreibung angegeben.

#### Parameter

| Name | Geben Sie                   | ein Beschreibung |
| ---- | --------------------------- | ---------------- |
| argv | schreibgeschützter String[] |                  |

### WEB_LUA_CLI_USAGE

**Art:** konstant

```typescript
export const WEB_LUA_CLI_USAGE;
```

Keine Beschreibung angegeben.

### WebLuaCliOptions

**Art:** Schnittstelle

```typescript
export interface WebLuaCliOptions
```

Keine Beschreibung angegeben.

### WebLuaCliUsageError

**Art:** Klasse

```typescript
export class WebLuaCliUsageError extends Error
```

Keine Beschreibung angegeben.

## `src/main`

### runWebLuaCli

**Art:** Funktion

```typescript
function runWebLuaCli(
  argv: readonly string[] = process.argv.slice(2),
  io: WebLuaCliIo = defaultIo,
  cwd = process.cwd(),
  runtimeFactory: WebLuaRuntimeFactory = (options) =>
    createWebLuaRuntime({
      capabilities: options.capabilities,
      hostAdapter: options.hostAdapter,
      onOutput: options.onOutput,
      onError: options.onError,
    }),
  sourceReader: WebLuaSourceReader = readSource,
): Promise<number>;
```

Keine Beschreibung angegeben.

#### Parameter

| Name           | Geben Sie                   | ein Beschreibung |
| -------------- | --------------------------- | ---------------- |
| argv           | schreibgeschützter String[] |                  |
| io             | WebLuaCliIo                 |                  |
| cwd            |                             |                  |
| runtimeFactory | WebLuaRuntimeFactory        |                  |
| sourceReader   | WebLuaSourceReader          |                  |

### StripLuaShebang

**Art:** Funktion

```typescript
function stripLuaShebang(source: string): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name   | Geben Sie    | ein Beschreibung |
| ------ | ------------ | ---------------- |
| Quelle | Zeichenfolge |                  |

### WEB_LUA_CLI_RUNTIME_EXIT_CODE

**Art:** konstant

```typescript
export const WEB_LUA_CLI_RUNTIME_EXIT_CODE;
```

Keine Beschreibung angegeben.

### WEB_LUA_CLI_USAGE_EXIT_CODE

**Art:** konstant

```typescript
export const WEB_LUA_CLI_USAGE_EXIT_CODE;
```

Keine Beschreibung angegeben.

### WebLuaCliErrorFrame

**Art:** Schnittstelle

```typescript
export interface WebLuaCliErrorFrame
```

Keine Beschreibung angegeben.

### WebLuaCliIo

**Art:** Schnittstelle

```typescript
export interface WebLuaCliIo
```

Keine Beschreibung angegeben.

### WebLuaCliResultFrame

**Art:** Schnittstelle

```typescript
export interface WebLuaCliResultFrame
```

Keine Beschreibung angegeben.

### WebLuaCliRuntimeOptions

**Art:** Schnittstelle

```typescript
export interface WebLuaCliRuntimeOptions
```

Keine Beschreibung angegeben.

## `src/suite`

### DEFAULT_WEB_LUA_SUITE_DIRECTORY

**Art:** konstant

```typescript
export const DEFAULT_WEB_LUA_SUITE_DIRECTORY;
```

Keine Beschreibung angegeben.

### LUA_5_5_1_SUITE_MANIFEST

**Art:** konstant

```typescript
export const LUA_5_5_1_SUITE_MANIFEST;
```

Die Dateien werden direkt oder indirekt vom Upstream-Lua 5.5.1 `all.lua` geladen.

### runWebLuaSuite

**Art:** Funktion

```typescript
function runWebLuaSuite(
  options: WebLuaSuiteRunOptions = {},
): Promise<WebLuaSuiteResult>;
```

Keine Beschreibung angegeben.

#### Parameter

| Name     | Geben Sie             | ein Beschreibung |
| -------- | --------------------- | ---------------- |
| Optionen | WebLuaSuiteRunOptions |                  |

### WEB_LUA_5_5_1_SUITE_NAME

**Art:** konstant

```typescript
export const WEB_LUA_5_5_1_SUITE_NAME;
```

Keine Beschreibung angegeben.

### WEB_LUA_SUITE_DEFAULT_EXCLUSIONS

**Art:** konstant

```typescript
export const WEB_LUA_SUITE_DEFAULT_EXCLUSIONS;
```

Keine Beschreibung angegeben.

### WEB_LUA_SUITE_ENTRY_FILE

**Art:** konstant

```typescript
export const WEB_LUA_SUITE_ENTRY_FILE;
```

Keine Beschreibung angegeben.

### WEB_LUA_SUITE_FINAL_MARKER

**Art:** konstant

```typescript
export const WEB_LUA_SUITE_FINAL_MARKER;
```

Keine Beschreibung angegeben.

### WEB_LUA_SUITE_MANIFEST

**Art:** konstant

```typescript
export const WEB_LUA_SUITE_MANIFEST;
```

Keine Beschreibung angegeben.

### WebLuaSuiteCaseKind

**Art:** Typ

```typescript
export type WebLuaSuiteCaseKind =
  "portable" | "capability-gated" | "unsupported";
```

Keine Beschreibung angegeben.

### WebLuaSuiteCliRunner

**Art:** Typ

```typescript
export type WebLuaSuiteCliRunner = (
  argv: readonly string[],
  io: WebLuaCliIo,
  cwd: string,
  sourceReader?: WebLuaSuiteSourceReader,
) => Promise<number>;
```

Keine Beschreibung angegeben.

### WebLuaSuiteManifestEntry

**Art:** Schnittstelle

```typescript
export interface WebLuaSuiteManifestEntry
```

Keine Beschreibung angegeben.

### WebLuaSuiteResult

**Art:** Schnittstelle

```typescript
export interface WebLuaSuiteResult
```

Keine Beschreibung angegeben.

### WebLuaSuiteRunOptions

**Art:** Schnittstelle

```typescript
export interface WebLuaSuiteRunOptions
```

Keine Beschreibung angegeben.

### WebLuaSuiteSourceReader

**Art:** Typ

```typescript
export type WebLuaSuiteSourceReader = (
  source: string,
  cwd: string,
) => Promise<string>;
```

Keine Beschreibung angegeben.

### WebLuaSuiteSummary

**Art:** Schnittstelle

```typescript
export interface WebLuaSuiteSummary
```

Keine Beschreibung angegeben.
