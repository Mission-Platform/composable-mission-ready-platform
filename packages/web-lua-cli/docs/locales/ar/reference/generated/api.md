# مرجع @mission-platform/web-lua-cli API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/web-lua-cli/docs/reference/generated/api.md: [packages/web-lua-cli/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/web-lua-cli`.

## `src/args`

### parseWebLuaCliArgs

**النوع:** الوظيفة

```typescript
function parseWebLuaCliArgs(argv: readonly string[]): WebLuaCliOptions;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب                | الوصف |
| ----- | ------------------- | ----- |
| argv  | سلسلة للقراءة فقط[] |       |

### WEB_LUA_CLI_USAGE

**النوع:** ثابت

```typescript
export const WEB_LUA_CLI_USAGE;
```

لم يتم تقديم أي وصف.

### WebLuaCliOptions

**النوع:** الواجهة

```typescript
export interface WebLuaCliOptions
```

لم يتم تقديم أي وصف.

### WebLuaCliUsageError

**النوع:** فئة

```typescript
export class WebLuaCliUsageError extends Error
```

لم يتم تقديم أي وصف.

## `src/main`

### runWebLuaCli

**النوع:** الوظيفة

```typescript
function runWebLuaCli(
  argv: readonly string[] = process.argv.slice(2),
  io: WebLuaCliIo = defaultIo,
  cwd = process.cwd(),
  runtimeFactory: WebLuaRuntimeFactory = (options) =>
    (
      createWebLuaRuntime as unknown as (
        artifact: undefined,
        options: WebLuaCliRuntimeOptions,
      ) => Promise<WebLuaCliRuntime>
    )(undefined, options),
  sourceReader: WebLuaSourceReader = readSource,
): Promise<number>;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم               | اكتب                 | الوصف |
| ------------------- | -------------------- | ----- |
| argv                | سلسلة للقراءة فقط[]  |       |
| ايو                 | WebLuaCliIo          |       |
| كود                 |                      |       |
| وقت التشغيل فاكتوري | WebLuaRuntimeFactory |       |
| القارئ المصدر       | WebLuaSourceReader   |       |

### stripLuaShebang

**النوع:** الوظيفة

```typescript
function stripLuaShebang(source: string): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم  | اكتب  | الوصف |
| ------ | ----- | ----- |
| المصدر | سلسلة |       |

### WEB_LUA_CLI_RUNTIME_EXIT_CODE

**النوع:** ثابت

```typescript
export const WEB_LUA_CLI_RUNTIME_EXIT_CODE;
```

لم يتم تقديم أي وصف.

### WEB_LUA_CLI_USAGE_EXIT_CODE

**النوع:** ثابت

```typescript
export const WEB_LUA_CLI_USAGE_EXIT_CODE;
```

لم يتم تقديم أي وصف.

### WebLuaCliErrorFrame

**النوع:** الواجهة

```typescript
export interface WebLuaCliErrorFrame
```

لم يتم تقديم أي وصف.

### WebLuaCliIo

**النوع:** الواجهة

```typescript
export interface WebLuaCliIo
```

لم يتم تقديم أي وصف.

### WebLuaCliResultFrame

**النوع:** الواجهة

```typescript
export interface WebLuaCliResultFrame
```

لم يتم تقديم أي وصف.

### WebLuaCliRuntimeOptions

**النوع:** الواجهة

```typescript
export interface WebLuaCliRuntimeOptions
```

لم يتم تقديم أي وصف.

## `src/suite`

### DEFAULT_WEB_LUA_SUITE_DIRECTORY

**النوع:** ثابت

```typescript
export const DEFAULT_WEB_LUA_SUITE_DIRECTORY;
```

لم يتم تقديم أي وصف.

### LUA_5_5_1_SUITE_MANIFEST

**النوع:** ثابت

```typescript
export const LUA_5_5_1_SUITE_MANIFEST;
```

الملفات التي تم تحميلها بشكل مباشر أو غير مباشر بواسطة Lua 5.5.1 `all.lua`.

### runWebLuaSuite

**النوع:** الوظيفة

```typescript
function runWebLuaSuite(
  options: WebLuaSuiteRunOptions = {},
): Promise<WebLuaSuiteResult>;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم  | اكتب                  | الوصف |
| ------ | --------------------- | ----- |
| خيارات | WebLuaSuiteRunOptions |       |

### WEB_LUA_5_5_1_SUITE_NAME

**النوع:** ثابت

```typescript
export const WEB_LUA_5_5_1_SUITE_NAME;
```

لم يتم تقديم أي وصف.

### WEB_LUA_SUITE_DEFAULT_EXCLUSIONS

**النوع:** ثابت

```typescript
export const WEB_LUA_SUITE_DEFAULT_EXCLUSIONS;
```

لم يتم تقديم أي وصف.

### WEB_LUA_SUITE_ENTRY_FILE

**النوع:** ثابت

```typescript
export const WEB_LUA_SUITE_ENTRY_FILE;
```

لم يتم تقديم أي وصف.

### WEB_LUA_SUITE_FINAL_MARKER

**النوع:** ثابت

```typescript
export const WEB_LUA_SUITE_FINAL_MARKER;
```

لم يتم تقديم أي وصف.

### WEB_LUA_SUITE_MANIFEST

**النوع:** ثابت

```typescript
export const WEB_LUA_SUITE_MANIFEST;
```

لم يتم تقديم أي وصف.

### WebLuaSuiteCaseKind

**النوع:** النوع

```typescript
export type WebLuaSuiteCaseKind =
  "portable" | "capability-gated" | "unsupported";
```

لم يتم تقديم أي وصف.

### WebLuaSuiteCliRunner

**النوع:** النوع

```typescript
export type WebLuaSuiteCliRunner = (
  argv: readonly string[],
  io: WebLuaCliIo,
  cwd: string,
  sourceReader?: WebLuaSuiteSourceReader,
) => Promise<number>;
```

لم يتم تقديم أي وصف.

### WebLuaSuiteManifestEntry

**النوع:** الواجهة

```typescript
export interface WebLuaSuiteManifestEntry
```

لم يتم تقديم أي وصف.

### WebLuaSuiteResult

**النوع:** الواجهة

```typescript
export interface WebLuaSuiteResult
```

لم يتم تقديم أي وصف.

### WebLuaSuiteRunOptions

**النوع:** الواجهة

```typescript
export interface WebLuaSuiteRunOptions
```

لم يتم تقديم أي وصف.

### WebLuaSuiteSourceReader

**النوع:** النوع

```typescript
export type WebLuaSuiteSourceReader = (
  source: string,
  cwd: string,
) => Promise<string>;
```

لم يتم تقديم أي وصف.

### ملخص WebLuaSuite

**النوع:** الواجهة

```typescript
export interface WebLuaSuiteSummary
```

لم يتم تقديم أي وصف.
