{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["build:sprite"]
    },
    "build:sprite": {
      "dependsOn": ["build:forge"],
      "outputs": ["dist/icons.svg"]
    }
  }
}
