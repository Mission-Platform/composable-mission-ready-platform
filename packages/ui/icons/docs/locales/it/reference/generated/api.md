# Riferimento API @mission-platform/icons

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/ui/icons/docs/reference/generated/api.md: [packages/ui/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/icons`.

## `src/catalog`

### ICON_CATALOGO

**Genere:** costante

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[];
```

Il catalogo rivisto per il set di icone esistente.

### ICON_GAP_REVIEW

**Genere:** costante

```typescript
export const ICON_GAP_REVIEW;
```

Aggiunte di alto valore da implementare dopo aver stabilito la tassonomia di origine.

### IconCatalogEntry

**Tipo:** interfaccia

```typescript
export interface IconCatalogEntry
```

Un'assegnazione di categoria stabile per ogni fonte di icone in questo pacchetto.

### validateIconCatalog

**Tipo:** funzione

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void;
```

Convalidare il catalogo statico prima che venga utilizzato per generare i metadati del pacchetto.

#### Parametri

| Nome     | Digitare                        | Descrizione |
| -------- | ------------------------------- | ----------- |
| catalogo | sola lettura IconCatalogEntry[] |             |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### ForgeIconBell

**Tipo:** funzione

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement;
```

`ForgeIconBell`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-bell.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconBellProperties> |             |

### ProprietàIconBell

**Tipo:** interfaccia

```typescript
export interface IconBellProperties
```

Nessuna descrizione fornita.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### ForgeIconChat

**Tipo:** funzione

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement;
```

`ForgeIconChat`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-chat.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconChatProperties> |             |

### IconChatProperties

**Tipo:** interfaccia

```typescript
export interface IconChatProperties
```

Nessuna descrizione fornita.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### ForgeIconMail

**Tipo:** funzione

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement;
```

`ForgeIconMail`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-mail.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconMailProperties> |             |

### IconMailProperties

**Tipo:** interfaccia

```typescript
export interface IconMailProperties
```

Nessuna descrizione fornita.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### ForgeIconPhone

**Tipo:** funzione

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement;
```

`ForgeIconPhone`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-phone.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconPhoneProperties> |             |

### IconPhoneProperties

**Tipo:** interfaccia

```typescript
export interface IconPhoneProperties
```

Nessuna descrizione fornita.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### ForgeIconSend

**Tipo:** funzione

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement;
```

`ForgeIconSend`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-send.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconSendProperties> |             |

### IconSendProperties

**Tipo:** interfaccia

```typescript
export interface IconSendProperties
```

Nessuna descrizione fornita.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### ForgeIconShare

**Tipo:** funzione

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement;
```

`ForgeIconShare`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-share.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconShareProperties> |             |

### IconShareProperties

**Tipo:** interfaccia

```typescript
export interface IconShareProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### ForgeIconCopy

**Tipo:** funzione

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement;
```

`ForgeIconCopy`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-copy.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconCopyProperties> |             |

### IconCopyProperties

**Tipo:** interfaccia

```typescript
export interface IconCopyProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### ForgeIconModifica

**Tipo:** funzione

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement;
```

`ForgeIconEdit`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-edit.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconEditProperties> |             |

### IconEditProperties

**Tipo:** interfaccia

```typescript
export interface IconEditProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### ForgeIconEyeOff

**Tipo:** funzione

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement;
```

`ForgeIconEyeOff`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-eye-off.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconEyeOffProperties> |             |

### IconEyeOffProperties

**Tipo:** interfaccia

```typescript
export interface IconEyeOffProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### ForgeIconEye

**Tipo:** funzione

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement;
```

`ForgeIconEye`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-eye.module.scss`.

#### Parametri

| Nome      | Digitare                        | Descrizione |
| --------- | ------------------------------- | ----------- |
| proprietà | Sola lettura<IconEyeProperties> |             |

### IconEyeProperties

**Tipo:** interfaccia

```typescript
export interface IconEyeProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### ForgeIconRedo

**Tipo:** funzione

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement;
```

Riapplica la modifica del contenuto annullata più recentemente.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconRedoProperties> |             |

### IconRedoProperties

**Tipo:** interfaccia

```typescript
export interface IconRedoProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### ForgeIconTrash

**Tipo:** funzione

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement;
```

`ForgeIconTrash`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-trash.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconTrashProperties> |             |

### IconTrashProperties

**Tipo:** interfaccia

```typescript
export interface IconTrashProperties
```

Nessuna descrizione fornita.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### ForgeIconUndo

**Tipo:** funzione

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement;
```

Annulla la modifica del contenuto più recente.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconUndoProperties> |             |

### IconaAnnullaProprietà

**Tipo:** interfaccia

```typescript
export interface IconUndoProperties
```

Nessuna descrizione fornita.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIconDownload

**Tipo:** funzione

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement;
```

`ForgeIconDownload`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-download.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconDownloadProperties> |             |

### IconaScaricaProprietà

**Tipo:** interfaccia

```typescript
export interface IconDownloadProperties
```

Nessuna descrizione fornita.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconUpload

**Tipo:** funzione

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement;
```

`ForgeIconUpload`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-upload.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconUploadProperties> |             |

### IconUploadProperties

**Tipo:** interfaccia

```typescript
export interface IconUploadProperties
```

Nessuna descrizione fornita.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### ForgeIconFilter

**Tipo:** funzione

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement;
```

`ForgeIconFilter`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-filter.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconFilterProperties> |             |

### IconFilterProperties

**Tipo:** interfaccia

```typescript
export interface IconFilterProperties
```

Nessuna descrizione fornita.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### ForgeIconSort

**Tipo:** funzione

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement;
```

`ForgeIconSort`: un indicatore di ordinamento a due chevron creato una volta nel JSX neutro
dialetto.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-sort.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconSortProperties> |             |

### IconSortProperties

**Tipo:** interfaccia

```typescript
export interface IconSortProperties
```

Nessuna descrizione fornita.

### SortDirection

**Genere:** tipo

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

La direzione di ordinamento attiva o `undefined` se non ordinato.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIconTableColumnAdd

**Tipo:** funzione

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement;
```

`ForgeIconTableColumnAdd`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-table-column-add.module.scss`.

#### Parametri

| Nome      | Digitare                                   | Descrizione |
| --------- | ------------------------------------------ | ----------- |
| proprietà | Sola lettura<IconTableColumnAddProperties> |             |

### IconTableColumnAddProperties

**Tipo:** interfaccia

```typescript
export interface IconTableColumnAddProperties
```

Nessuna descrizione fornita.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnRemove

**Tipo:** funzione

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement;
```

`ForgeIconTableColumnRemove`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-table-column-remove.module.scss`.

#### Parametri

| Nome      | Digitare                                      | Descrizione |
| --------- | --------------------------------------------- | ----------- |
| proprietà | Sola lettura<IconTableColumnRemoveProperties> |             |

### IconTableColumnRemoveProperties

**Tipo:** interfaccia

```typescript
export interface IconTableColumnRemoveProperties
```

Nessuna descrizione fornita.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

**Tipo:** funzione

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement;
```

`ForgeIconTableRowAdd`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-table-row-add.module.scss`.

#### Parametri

| Nome      | Digitare                                | Descrizione |
| --------- | --------------------------------------- | ----------- |
| proprietà | Sola lettura<IconTableRowAddProperties> |             |

### IconTableRowAddProperties

**Tipo:** interfaccia

```typescript
export interface IconTableRowAddProperties
```

Nessuna descrizione fornita.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowRemove

**Tipo:** funzione

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement;
```

`ForgeIconTableRowRemove`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-table-row-remove.module.scss`.

#### Parametri

| Nome      | Digitare                                   | Descrizione |
| --------- | ------------------------------------------ | ----------- |
| proprietà | Sola lettura<IconTableRowRemoveProperties> |             |

### IconTableRowRemoveProperties

**Tipo:** interfaccia

```typescript
export interface IconTableRowRemoveProperties
```

Nessuna descrizione fornita.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIconTable

**Tipo:** funzione

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement;
```

`ForgeIconTable`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-table.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconTableProperties> |             |

### IconTableProperties

**Tipo:** interfaccia

```typescript
export interface IconTableProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### ForgeIconDrawCircle

**Tipo:** funzione

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement;
```

`ForgeIconDrawCircle`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-draw-circle.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconDrawCircleProperties> |             |

### IconDrawCircleProperties

**Tipo:** interfaccia

```typescript
export interface IconDrawCircleProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### ForgeIconDrawLine

**Tipo:** funzione

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement;
```

`ForgeIconDrawLine`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-draw-line.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconDrawLineProperties> |             |

### IconDrawLineProperties

**Tipo:** interfaccia

```typescript
export interface IconDrawLineProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### ForgeIconDrawPolygon

**Tipo:** funzione

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement;
```

`ForgeIconDrawPolygon`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-draw-polygon.module.scss`.

#### Parametri

| Nome      | Digitare                                | Descrizione |
| --------- | --------------------------------------- | ----------- |
| proprietà | Sola lettura<IconDrawPolygonProperties> |             |

### Proprietà IconDrawPolygon

**Tipo:** interfaccia

```typescript
export interface IconDrawPolygonProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### ForgeIconDrawSquare

**Tipo:** funzione

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement;
```

`ForgeIconDrawSquare`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-draw-square.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconDrawSquareProperties> |             |

### IconDrawSquareProperties

**Tipo:** interfaccia

```typescript
export interface IconDrawSquareProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### ForgeIconDrawTriangle

**Tipo:** funzione

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement;
```

`ForgeIconDrawTriangle`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-draw-triangle.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconDrawTriangleProperties> |             |

### IconDrawTriangleProperties

**Tipo:** interfaccia

```typescript
export interface IconDrawTriangleProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### ForgeIconMove

**Tipo:** funzione

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement;
```

`ForgeIconMove`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-move.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconMoveProperties> |             |

### IconMoveProperties

**Tipo:** interfaccia

```typescript
export interface IconMoveProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### ForgeIconPalette

**Tipo:** funzione

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement;
```

`ForgeIconPalette`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-palette.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconPaletteProperties> |             |

### IconPaletteProperties

**Tipo:** interfaccia

```typescript
export interface IconPaletteProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### ForgeIconPencil

**Tipo:** funzione

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement;
```

`ForgeIconPencil`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-pencil.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconPencilProperties> |             |

### Proprietà IconMatita

**Tipo:** interfaccia

```typescript
export interface IconPencilProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### ForgeIconRotateCcw

**Tipo:** funzione

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement;
```

`ForgeIconRotateCcw`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-rotate-ccw.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconRotateCcwProperties> |             |

### IconRotateCcwProperties

**Tipo:** interfaccia

```typescript
export interface IconRotateCcwProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### ForgeIconRotateCw

**Tipo:** funzione

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement;
```

`ForgeIconRotateCw`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-rotate-cw.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconRotateCwProperties> |             |

### IconRotateCwProperties

**Tipo:** interfaccia

```typescript
export interface IconRotateCwProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### ForgeIconScaleDown

**Tipo:** funzione

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement;
```

`ForgeIconScaleDown`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-scale-down.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconScaleDownProperties> |             |

### IconScaleDownProperties

**Tipo:** interfaccia

```typescript
export interface IconScaleDownProperties
```

Nessuna descrizione fornita.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### ForgeIconScaleUp

**Tipo:** funzione

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement;
```

`ForgeIconScaleUp`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-scale-up.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconScaleUpProperties> |             |

### IconScaleUpProperties

**Tipo:** interfaccia

```typescript
export interface IconScaleUpProperties
```

Nessuna descrizione fornita.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### ForgeIconCountryGlobe

**Tipo:** funzione

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement;
```

Un indicatore del globo con un accento nazionale basato sui dati.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconCountryGlobeProperties> |             |

### IconCountryGlobeProperties

**Tipo:** interfaccia

```typescript
export interface IconCountryGlobeProperties
```

Nessuna descrizione fornita.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### ForgeIconFlag

**Tipo:** funzione

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement;
```

Un'icona con la bandiera di un paese delimitata e basata sui dati.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconFlagProperties> |             |

### IconCodicePaese

**Genere:** tipo

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

Codici paese supportati per il componente flag basato sui dati.

### IconFlagProperties

**Tipo:** interfaccia

```typescript
export interface IconFlagProperties
```

Nessuna descrizione fornita.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### ForgeIconGeodesic

**Tipo:** funzione

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement;
```

`ForgeIconGeodesic`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-geodesic.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconGeodesicProperties> |             |

### IconaProprietàgeodetiche

**Tipo:** interfaccia

```typescript
export interface IconGeodesicProperties
```

Nessuna descrizione fornita.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### ForgeIconGlobe

**Tipo:** funzione

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement;
```

`ForgeIconGlobe`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-globe.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconGlobeProperties> |             |

### IconGlobeProperties

**Tipo:** interfaccia

```typescript
export interface IconGlobeProperties
```

Nessuna descrizione fornita.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### ForgeIconLanguage

**Tipo:** funzione

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement;
```

`ForgeIconLanguage`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-language.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconLanguageProperties> |             |

### IconLanguageProperties

**Tipo:** interfaccia

```typescript
export interface IconLanguageProperties
```

Nessuna descrizione fornita.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### ForgeIconMapPin

**Tipo:** funzione

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement;
```

`ForgeIconMapPin`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-map-pin.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconMapPinProperties> |             |

### IconMapPinProperties

**Tipo:** interfaccia

```typescript
export interface IconMapPinProperties
```

Nessuna descrizione fornita.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### ForgeIconLayer

**Tipo:** funzione

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement;
```

Un simbolo di stack a livello di mappa per attivare/disattivare i dati geografici visibili.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconLayerProperties> |             |

### IconLayerProperties

**Tipo:** interfaccia

```typescript
export interface IconLayerProperties
```

Nessuna descrizione fornita.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### ForgeIconMapMarkerCluster

**Tipo:** funzione

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement;
```

Un indicatore di mappa raggruppato per punti di interesse densi.

#### Parametri

| Nome      | Digitare                                     | Descrizione |
| --------- | -------------------------------------------- | ----------- |
| proprietà | Sola lettura<IconMapMarkerClusterProperties> |             |

### IconMapMarkerClusterProperties

**Tipo:** interfaccia

```typescript
export interface IconMapMarkerClusterProperties
```

Nessuna descrizione fornita.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### ForgeIconCamera

**Tipo:** funzione

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement;
```

`ForgeIconCamera`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-camera.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconCameraProperties> |             |

### IconCameraProperties

**Tipo:** interfaccia

```typescript
export interface IconCameraProperties
```

Nessuna descrizione fornita.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### ForgeIconImage

**Tipo:** funzione

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement;
```

`ForgeIconImage`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-image.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconImageProperties> |             |

### IconImageProperties

**Tipo:** interfaccia

```typescript
export interface IconImageProperties
```

Nessuna descrizione fornita.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### ForgeIconPause

**Tipo:** funzione

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement;
```

`ForgeIconPause`: un glifo con pausa piena (❙❙) creato una volta nel JSX neutro
dialetto.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-pause.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconPauseProperties> |             |

### IconPauseProperties

**Tipo:** interfaccia

```typescript
export interface IconPauseProperties
```

Nessuna descrizione fornita.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### ForgeIconPlay

**Tipo:** funzione

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement;
```

`ForgeIconPlay` — un triangolo di gioco pieno (▲) creato una volta nel JSX neutrale
dialetto.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-play.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconPlayProperties> |             |

### IconPlayProperties

**Tipo:** interfaccia

```typescript
export interface IconPlayProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### ForgeIconArrow

**Tipo:** funzione

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement;
```

`ForgeIconArrow`: una freccia direzionale creata una volta nel dialetto JSX neutro.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-arrow.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconArrowProperties> |             |

### IconaFrecciaDirezione

**Genere:** tipo

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

La direzione in cui punta la freccia.

### IconArrowProperties

**Tipo:** interfaccia

```typescript
export interface IconArrowProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### ForgeIconChevron

**Tipo:** funzione

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement;
```

`ForgeIconChevron` — un simbolo direzionale scritto una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-chevron.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconChevronProperties> |             |

### IconChevronProperties

**Tipo:** interfaccia

```typescript
export interface IconChevronProperties
```

Nessuna descrizione fornita.

### IconaDirezione

**Genere:** tipo

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

La direzione in cui punta la freccia.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### ForgeIconChevrons

**Tipo:** funzione

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement;
```

`ForgeIconChevrons` — un doppio gallone direzionale (« ») creato una volta nel
Dialetto JSX neutro, tipicamente utilizzato per i controlli salta al primo/vai all'ultimo.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-chevrons.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconChevronsProperties> |             |

### IconaChevronDirezione

**Genere:** tipo

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

La direzione in cui punta il doppio gallone.

### IconChevronsProperties

**Tipo:** interfaccia

```typescript
export interface IconChevronsProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### ForgeIconClose

**Tipo:** funzione

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement;
```

`ForgeIconClose`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-close.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconCloseProperties> |             |

### IconaChiudiProprietà

**Tipo:** interfaccia

```typescript
export interface IconCloseProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### ForgeIconHome

**Tipo:** funzione

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement;
```

`ForgeIconHome`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-home.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconHomeProperties> |             |

### IconaHomeProprietà

**Tipo:** interfaccia

```typescript
export interface IconHomeProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### ForgeIconJoin

**Tipo:** funzione

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement;
```

`ForgeIconJoin`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-join.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconJoinProperties> |             |

### IconJoinProperties

**Tipo:** interfaccia

```typescript
export interface IconJoinProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### ForgeIconMenu

**Tipo:** funzione

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement;
```

`ForgeIconMenu`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-menu.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconMenuProperties> |             |

### IconMenuProperties

**Tipo:** interfaccia

```typescript
export interface IconMenuProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### ForgeIconMinus

**Tipo:** funzione

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement;
```

`ForgeIconMinus`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-minus.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconMinusProperties> |             |

### IconMinusProperties

**Tipo:** interfaccia

```typescript
export interface IconMinusProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

**Tipo:** funzione

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement;
```

`ForgeIconPlus`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-plus.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconPlusProperties> |             |

### IconPlusProperties

**Tipo:** interfaccia

```typescript
export interface IconPlusProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### ForgeIconRefresh

**Tipo:** funzione

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement;
```

`ForgeIconRefresh`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-refresh.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconRefreshProperties> |             |

### IconaAggiornaProprietà

**Tipo:** interfaccia

```typescript
export interface IconRefreshProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### ForgeIconSplit

**Tipo:** funzione

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement;
```

`ForgeIconSplit`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-split.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconSplitProperties> |             |

### IconSplitProperties

**Tipo:** interfaccia

```typescript
export interface IconSplitProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIconExternalLink

**Tipo:** funzione

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement;
```

`ForgeIconExternalLink`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-external-link.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconExternalLinkProperties> |             |

### IconExternalLinkProperties

**Tipo:** interfaccia

```typescript
export interface IconExternalLinkProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

**Tipo:** funzione

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement;
```

`ForgeIconLink`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-link.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconLinkProperties> |             |

### IconLinkProperties

**Tipo:** interfaccia

```typescript
export interface IconLinkProperties
```

Nessuna descrizione fornita.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### ForgeIconSearch

**Tipo:** funzione

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement;
```

`ForgeIconSearch`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-search.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconSearchProperties> |             |

### IconSearchProperties

**Tipo:** interfaccia

```typescript
export interface IconSearchProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### ForgeIconCloud

**Tipo:** funzione

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement;
```

`ForgeIconCloud`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-cloud.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconCloudProperties> |             |

### IconCloudProperties

**Tipo:** interfaccia

```typescript
export interface IconCloudProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### ForgeIconDebug

**Tipo:** funzione

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement;
```

`ForgeIconDebug`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-debug.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconDebugProperties> |             |

### IconDebugProperties

**Tipo:** interfaccia

```typescript
export interface IconDebugProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### ForgeIconHeart

**Tipo:** funzione

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement;
```

`ForgeIconHeart`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heart.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeartProperties> |             |

### IconHeartProperties

**Tipo:** interfaccia

```typescript
export interface IconHeartProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### ForgeIconLightning

**Tipo:** funzione

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement;
```

`ForgeIconLightning`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-lightning.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconLightningProperties> |             |

### IconaLightningProprietà

**Tipo:** interfaccia

```typescript
export interface IconLightningProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIconPuzzle

**Tipo:** funzione

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement;
```

`ForgeIconPuzzle`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-puzzle.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconPuzzleProperties> |             |

### IconPuzzleProperties

**Tipo:** interfaccia

```typescript
export interface IconPuzzleProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQrCode

**Tipo:** funzione

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement;
```

`ForgeIconQrCode`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-qr-code.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconQrCodeProperties> |             |

### IconQrCodeProperties

**Tipo:** interfaccia

```typescript
export interface IconQrCodeProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### ForgeIconSettings

**Tipo:** funzione

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement;
```

`ForgeIconSettings`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-settings.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconSettingsProperties> |             |

### ProprietàImpostazioniicona

**Tipo:** interfaccia

```typescript
export interface IconSettingsProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### ForgeIconStar

**Tipo:** funzione

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement;
```

`ForgeIconStar`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-star.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconStarProperties> |             |

### IconStarProperties

**Tipo:** interfaccia

```typescript
export interface IconStarProperties
```

Nessuna descrizione fornita.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### ForgeIconWrench

**Tipo:** funzione

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement;
```

`ForgeIconWrench`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-wrench.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconWrenchProperties> |             |

### IconWrenchProperties

**Tipo:** interfaccia

```typescript
export interface IconWrenchProperties
```

Nessuna descrizione fornita.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### ForgeIconRoute

**Tipo:** funzione

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement;
```

Una linea di percorso con indicatori di partenza e di destinazione distinti.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconRouteProperties> |             |

### IconRouteProperties

**Tipo:** interfaccia

```typescript
export interface IconRouteProperties
```

Nessuna descrizione fornita.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### ForgeIconWaypoint

**Tipo:** funzione

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement;
```

Un indicatore di waypoint riutilizzabile per percorsi e sovrapposizioni di mappe.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconWaypointProperties> |             |

### IconaWaypointProprietà

**Tipo:** interfaccia

```typescript
export interface IconWaypointProperties
```

Nessuna descrizione fornita.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### ForgeIconLockOpen

**Tipo:** funzione

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement;
```

`ForgeIconLockOpen`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-lock-open.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconLockOpenProperties> |             |

### IconLockOpenProperties

**Tipo:** interfaccia

```typescript
export interface IconLockOpenProperties
```

Nessuna descrizione fornita.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### ForgeIconLock

**Tipo:** funzione

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement;
```

`ForgeIconLock`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-lock.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconLockProperties> |             |

### IconLockProperties

**Tipo:** interfaccia

```typescript
export interface IconLockProperties
```

Nessuna descrizione fornita.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIconUser

**Tipo:** funzione

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement;
```

`ForgeIconUser`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-user.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconUserProperties> |             |

### IconUserProperties

**Tipo:** interfaccia

```typescript
export interface IconUserProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIconAlertCritical

**Tipo:** funzione

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement;
```

`ForgeIconAlertCritical` — il glifo di avviso critico (un "!" all'interno di un ottagono,
evocando un segnale di stop) scritto una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-alert-critical.module.scss`.

#### Parametri

| Nome      | Digitare                                  | Descrizione |
| --------- | ----------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlertCriticalProperties> |             |

### IconAlertCriticalProperties

**Tipo:** interfaccia

```typescript
export interface IconAlertCriticalProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

**Tipo:** funzione

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement;
```

`ForgeIconAlertInfo` — il glifo di avviso informativo (una "i" all'interno di un cerchio)
scritto una volta nel dialetto neutrale JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-alert-info.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlertInfoProperties> |             |

### IconAlertInfoProperties

**Tipo:** interfaccia

```typescript
export interface IconAlertInfoProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### ForgeIconAlertNeutral

**Tipo:** funzione

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement;
```

`ForgeIconAlertNeutral` — il glifo di avviso neutro (un "-" all'interno di un cerchio)
scritto una volta nel dialetto JSX neutro, per messaggi informativi con n
connotazione positiva o negativa.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-alert-neutral.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlertNeutralProperties> |             |

### IconAlertNeutralProperties

**Tipo:** interfaccia

```typescript
export interface IconAlertNeutralProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### ForgeIconAlertWarning

**Tipo:** funzione

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement;
```

`ForgeIconAlertWarning`: il glifo di avviso (un "!" all'interno di un triangolo)
scritto una volta nel dialetto neutrale JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-alert-warning.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlertWarningProperties> |             |

### IconAlertWarningProperties

**Tipo:** interfaccia

```typescript
export interface IconAlertWarningProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### ForgeIconAlert

**Tipo:** funzione

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement;
```

`ForgeIconAlert`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-alert.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlertProperties> |             |

### IconAlertProperties

**Tipo:** interfaccia

```typescript
export interface IconAlertProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### ForgeIconCheck

**Tipo:** funzione

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement;
```

`ForgeIconCheck`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-check.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconCheckProperties> |             |

### IconCheckProperties

**Tipo:** interfaccia

```typescript
export interface IconCheckProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIconError

**Tipo:** funzione

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement;
```

`ForgeIconError`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-error.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconErrorProperties> |             |

### IconErrorProperties

**Tipo:** interfaccia

```typescript
export interface IconErrorProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### ForgeIconInfo

**Tipo:** funzione

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement;
```

`ForgeIconInfo`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-info.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconInfoProperties> |             |

### IconInfoProperties

**Tipo:** interfaccia

```typescript
export interface IconInfoProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### ForgeIconNotice

**Tipo:** funzione

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement;
```

`ForgeIconNotice`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-notice.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconNoticeProperties> |             |

### IconNoticeProperties

**Tipo:** interfaccia

```typescript
export interface IconNoticeProperties
```

Nessuna descrizione fornita.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### ForgeIconWarning

**Tipo:** funzione

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement;
```

`ForgeIconWarning`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-warning.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconWarningProperties> |             |

### IconaAvvisoProprietà

**Tipo:** interfaccia

```typescript
export interface IconWarningProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### ForgeIconAlignCenter

**Tipo:** funzione

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement;
```

`ForgeIconAlignCenter`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-align-center.module.scss`.

#### Parametri

| Nome      | Digitare                                | Descrizione |
| --------- | --------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlignCenterProperties> |             |

### IconAlignCenterProperties

**Tipo:** interfaccia

```typescript
export interface IconAlignCenterProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### ForgeIconAlignJustify

**Tipo:** funzione

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement;
```

`ForgeIconAlignJustify`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-align-justify.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlignJustifyProperties> |             |

### IconAlignJustifyProperties

**Tipo:** interfaccia

```typescript
export interface IconAlignJustifyProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### ForgeIconAlignLeft

**Tipo:** funzione

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement;
```

`ForgeIconAlignLeft`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-align-left.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlignLeftProperties> |             |

### IconAlignLeftProperties

**Tipo:** interfaccia

```typescript
export interface IconAlignLeftProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### ForgeIconAlignRight

**Tipo:** funzione

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement;
```

`ForgeIconAlignRight`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-align-right.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconAlignRightProperties> |             |

### IconAlignRightProperties

**Tipo:** interfaccia

```typescript
export interface IconAlignRightProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIconBlockquote

**Tipo:** funzione

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement;
```

`ForgeIconBlockquote`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-blockquote.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconBlockquoteProperties> |             |

### IconBlockquoteProperties

**Tipo:** interfaccia

```typescript
export interface IconBlockquoteProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### ForgeIconBold

**Tipo:** funzione

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement;
```

`ForgeIconBold`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-bold.module.scss`.

#### Parametri

| Nome      | Digitare                         | Descrizione |
| --------- | -------------------------------- | ----------- |
| proprietà | Sola lettura<IconBoldProperties> |             |

### IconBoldProperties

**Tipo:** interfaccia

```typescript
export interface IconBoldProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### ForgeIconBulletList

**Tipo:** funzione

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement;
```

`ForgeIconBulletList`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-bullet-list.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconBulletListProperties> |             |

### IconBulletListProperties

**Tipo:** interfaccia

```typescript
export interface IconBulletListProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIconCodeBlock

**Tipo:** funzione

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement;
```

`ForgeIconCodeBlock`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-code-block.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconCodeBlockProperties> |             |

### IconCodeBlockProperties

**Tipo:** interfaccia

```typescript
export interface IconCodeBlockProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

**Tipo:** funzione

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement;
```

`ForgeIconCodeInline`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-code-inline.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconCodeInlineProperties> |             |

### IconCodeInlineProperties

**Tipo:** interfaccia

```typescript
export interface IconCodeInlineProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### ForgeIconHeadingFive

**Tipo:** funzione

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement;
```

`ForgeIconHeadingFive`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-five.module.scss`.

#### Parametri

| Nome      | Digitare                                | Descrizione |
| --------- | --------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingFiveProperties> |             |

### IconaIntestazioneCinqueProprietà

**Tipo:** interfaccia

```typescript
export interface IconHeadingFiveProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### ForgeIconHeadingFour

**Tipo:** funzione

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement;
```

`ForgeIconHeadingFour`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-four.module.scss`.

#### Parametri

| Nome      | Digitare                                | Descrizione |
| --------- | --------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingFourProperties> |             |

### IconaIntestazioneQuattroProprietà

**Tipo:** interfaccia

```typescript
export interface IconHeadingFourProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### ForgeIconHeadingOne

**Tipo:** funzione

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement;
```

`ForgeIconHeadingOne`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-one.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingOneProperties> |             |

### IconHeadingOneProperties

**Tipo:** interfaccia

```typescript
export interface IconHeadingOneProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### ForgeIconHeadingSix

**Tipo:** funzione

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement;
```

`ForgeIconHeadingSix`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-six.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingSixProperties> |             |

### IconaIntestazioneSeiProprietà

**Tipo:** interfaccia

```typescript
export interface IconHeadingSixProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### ForgeIconHeadingThree

**Tipo:** funzione

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement;
```

`ForgeIconHeadingThree`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-three.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingThreeProperties> |             |

### IconaIntestazioneTreProprietà

**Tipo:** interfaccia

```typescript
export interface IconHeadingThreeProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### ForgeIconHeadingTwo

**Tipo:** funzione

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement;
```

`ForgeIconHeadingTwo`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading-two.module.scss`.

#### Parametri

| Nome      | Digitare                               | Descrizione |
| --------- | -------------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingTwoProperties> |             |

### IconaIntestazioneDueProprietà

**Tipo:** interfaccia

```typescript
export interface IconHeadingTwoProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### ForgeIconHeading

**Tipo:** funzione

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement;
```

`ForgeIconHeading`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-heading.module.scss`.

#### Parametri

| Nome      | Digitare                            | Descrizione |
| --------- | ----------------------------------- | ----------- |
| proprietà | Sola lettura<IconHeadingProperties> |             |

### IconHeadingProperties

**Tipo:** interfaccia

```typescript
export interface IconHeadingProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### ForgeIconItalic

**Tipo:** funzione

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement;
```

`ForgeIconItalic`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-italic.module.scss`.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconItalicProperties> |             |

### IconItalicProperties

**Tipo:** interfaccia

```typescript
export interface IconItalicProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

**Tipo:** funzione

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement;
```

`ForgeIconNumberedList`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-numbered-list.module.scss`.

#### Parametri

| Nome      | Digitare                                 | Descrizione |
| --------- | ---------------------------------------- | ----------- |
| proprietà | Sola lettura<IconNumberedListProperties> |             |

### ProprietàListaNumerataIcona

**Tipo:** interfaccia

```typescript
export interface IconNumberedListProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### ForgeIconStrikethrough

**Tipo:** funzione

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement;
```

`ForgeIconStrikethrough`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-strikethrough.module.scss`.

#### Parametri

| Nome      | Digitare                                  | Descrizione |
| --------- | ----------------------------------------- | ----------- |
| proprietà | Sola lettura<IconStrikethroughProperties> |             |

### IconaStrikethroughProperties

**Tipo:** interfaccia

```typescript
export interface IconStrikethroughProperties
```

Nessuna descrizione fornita.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### ForgeIconUnderline

**Tipo:** funzione

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement;
```

`ForgeIconUnderline`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-underline.module.scss`.

#### Parametri

| Nome      | Digitare                              | Descrizione |
| --------- | ------------------------------------- | ----------- |
| proprietà | Sola lettura<IconUnderlineProperties> |             |

### IconaSottolineatoProprietà

**Tipo:** interfaccia

```typescript
export interface IconUnderlineProperties
```

Nessuna descrizione fornita.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### ForgeIconCalendar

**Tipo:** funzione

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement;
```

`ForgeIconCalendar`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-calendar.module.scss`.

#### Parametri

| Nome      | Digitare                             | Descrizione |
| --------- | ------------------------------------ | ----------- |
| proprietà | Sola lettura<IconCalendarProperties> |             |

### IconCalendarProperties

**Tipo:** interfaccia

```typescript
export interface IconCalendarProperties
```

Nessuna descrizione fornita.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### ForgeIconClock

**Tipo:** funzione

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement;
```

`ForgeIconClock`: un'icona creata una volta nel dialetto neutro JSX.

Si compila in modo pulito sia su Vue 3 che su React tramite vite-plugin-forge
compilatore a due fasi. L'icona invia il proprio CSS `@layer mp.icons` tramite il file
Modulo CSS co-localizzato `forge-icon-clock.module.scss`.

#### Parametri

| Nome      | Digitare                          | Descrizione |
| --------- | --------------------------------- | ----------- |
| proprietà | Sola lettura<IconClockProperties> |             |

### IconClockProperties

**Tipo:** interfaccia

```typescript
export interface IconClockProperties
```

Nessuna descrizione fornita.

## `src/sprite/compositions`

### ICON_COMPOSIZIONI

**Genere:** costante

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[];
```

Nessuna descrizione fornita.

### validateCompositions

**Tipo:** funzione

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void;
```

Nessuna descrizione fornita.

#### Parametri

| Nome           | Digitare                            | Descrizione |
| -------------- | ----------------------------------- | ----------- |
| composizioni   | sola lettura IconSymbolDefinition[] |             |
| ID disponibili | Sola letturaSet<string>             |             |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**Genere:** costante

```typescript
export const SUPPORTED_COUNTRY_CODES;
```

Codici paese con simboli di bandiera canonici nello sprite pubblicato.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**Genere:** costante

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS;
```

Nessuna descrizione fornita.

### ICON_SYMBOL_DEFINITIONS

**Genere:** costante

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[];
```

Nessuna descrizione fornita.

## `src/sprite/icon-use`

### ForgeIconUse

**Tipo:** funzione

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement;
```

Wrapper accessibile condiviso che fa riferimento alla geometria canonica dello sprite.

#### Parametri

| Nome      | Digitare                        | Descrizione |
| --------- | ------------------------------- | ----------- |
| proprietà | Sola lettura<IconUseProperties> |             |

### IconUseProperties

**Tipo:** interfaccia

```typescript
export interface IconUseProperties
```

Nessuna descrizione fornita.

## `src/sprite/provider`

### IconSpriteContext

**Genere:** costante

```typescript
export const IconSpriteContext;
```

Nessuna descrizione fornita.

### IconSpriteProvider

**Tipo:** funzione

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement;
```

Monta un host sprite in linea per un sottoalbero di componenti di icone.

#### Parametri

| Nome      | Digitare                           | Descrizione |
| --------- | ---------------------------------- | ----------- |
| proprietà | Sola lettura<IconSpriteProperties> |             |

### useIconHref

**Tipo:** funzione

```typescript
function useIconHref(symbolId: string): string;
```

Risolvere un URL di simbolo locale o esterno per un wrapper di icone.

#### Parametri

| Nome      | Digitare | Descrizione |
| --------- | -------- | ----------- |
| simboloId | stringa  |             |

## `src/sprite/serialize`

### serializeNode

**Tipo:** funzione

```typescript
function serializeNode(node: IconSvgNode): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome | Digitare    | Descrizione |
| ---- | ----------- | ----------- |
| node | IconSvgNode |             |

### serializeSprite

**Tipo:** funzione

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                            | Descrizione |
| ----------- | ----------------------------------- | ----------- |
| definizioni | sola lettura IconSymbolDefinition[] |             |

### serializeSymbol

**Tipo:** funzione

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string;
```

Nessuna descrizione fornita.

#### Parametri

| Nome        | Digitare                | Descrizione |
| ----------- | ----------------------- | ----------- |
| definizione | IconaSimboloDefinizione |             |

## `src/sprite/types`

### IconSpriteContextValue

**Tipo:** interfaccia

```typescript
export interface IconSpriteContextValue
```

Nessuna descrizione fornita.

### IconSpriteProperties

**Tipo:** interfaccia

```typescript
export interface IconSpriteProperties
```

Nessuna descrizione fornita.

### IconSvgNode

**Tipo:** interfaccia

```typescript
export interface IconSvgNode
```

Nessuna descrizione fornita.

### IconaSimboloDefinizione

**Tipo:** interfaccia

```typescript
export interface IconSymbolDefinition
```

Nessuna descrizione fornita.

### IconaSimboloUso

**Tipo:** interfaccia

```typescript
export interface IconSymbolUse
```

Nessuna descrizione fornita.
