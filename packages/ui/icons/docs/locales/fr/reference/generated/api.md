# Référence API @mission-platform/icons

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/ui/icons/docs/reference/generated/api.md: [packages/ui/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/icons`.

## `src/catalog`

### ICON_CATALOG

**Genre :** constante

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[];
```

Le catalogue révisé pour le jeu d'icônes existant.

### ICON_GAP_REVIEW

**Genre :** constante

```typescript
export const ICON_GAP_REVIEW;
```

Ajouts à haute valeur ajoutée à mettre en œuvre une fois la taxonomie source établie.

### IcôneEntréeCatalogue

**Genre :** interface

```typescript
export interface IconCatalogEntry
```

Une attribution de catégorie stable pour chaque source d'icônes de ce package.

### validateIconCatalog

**Genre :** fonction

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void;
```

Validez le catalogue statique avant de l’utiliser pour générer des métadonnées de package.

#### Paramètres

| Nom       | Tapez                            | Descriptif |
| --------- | -------------------------------- | ---------- |
| catalogue | lecture seule IconCatalogEntry[] |            |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### ForgeIcôneCloche

**Genre :** fonction

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement;
```

`ForgeIconBell` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-bell.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconBellProperties> |            |

### IcôneBellPropriétés

**Genre :** interface

```typescript
export interface IconBellProperties
```

Aucune description fournie.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### ForgeIcôneChat

**Genre :** fonction

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement;
```

`ForgeIconChat` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-chat.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconChatProperties> |            |

### IconChatPropriétés

**Genre :** interface

```typescript
export interface IconChatProperties
```

Aucune description fournie.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### ForgeIconMail

**Genre :** fonction

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement;
```

`ForgeIconMail` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-mail.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconMailProperties> |            |

### IconMailPropriétés

**Genre :** interface

```typescript
export interface IconMailProperties
```

Aucune description fournie.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### ForgeIcôneTéléphone

**Genre :** fonction

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement;
```

`ForgeIconPhone` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-phone.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconPhoneProperties> |            |

### PropriétésIcôneTéléphone

**Genre :** interface

```typescript
export interface IconPhoneProperties
```

Aucune description fournie.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### ForgeIcôneEnvoyer

**Genre :** fonction

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement;
```

`ForgeIconSend` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-send.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconSendProperties> |            |

### IconSendPropriétés

**Genre :** interface

```typescript
export interface IconSendProperties
```

Aucune description fournie.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### ForgeIconPartager

**Genre :** fonction

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement;
```

`ForgeIconShare` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-share.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconShareProperties> |            |

### Propriétés de partage d'icônes

**Genre :** interface

```typescript
export interface IconShareProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### ForgeIcôneCopie

**Genre :** fonction

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement;
```

`ForgeIconCopy` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-copy.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconCopyProperties> |            |

### Propriétés de copie d'icône

**Genre :** interface

```typescript
export interface IconCopyProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### ForgeIcône

**Genre :** fonction

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement;
```

`ForgeIconEdit` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-edit.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconEditProperties> |            |

### IconEditPropriétés

**Genre :** interface

```typescript
export interface IconEditProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### ForgeIconEyeOff

**Genre :** fonction

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement;
```

`ForgeIconEyeOff` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-eye-off.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconEyeOffProperties> |            |

### IconEyeOffPropriétés

**Genre :** interface

```typescript
export interface IconEyeOffProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### ForgeIcôneŒil

**Genre :** fonction

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement;
```

`ForgeIconEye` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-eye.module.scss`.

#### Paramètres

| Nom        | Tapez                            | Descriptif |
| ---------- | -------------------------------- | ---------- |
| propriétés | Lecture seule<IconEyeProperties> |            |

### PropriétésIcôneOeil

**Genre :** interface

```typescript
export interface IconEyeProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### ForgeIcôneRefaire

**Genre :** fonction

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement;
```

Réapplique la dernière modification de contenu annulée.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconRedoProperties> |            |

### IconRedoPropriétés

**Genre :** interface

```typescript
export interface IconRedoProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### ForgeIcôneCorbeille

**Genre :** fonction

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement;
```

`ForgeIconTrash` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-trash.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconTrashProperties> |            |

### Propriétés de la corbeille d'icônes

**Genre :** interface

```typescript
export interface IconTrashProperties
```

Aucune description fournie.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### ForgeIcôneAnnuler

**Genre :** fonction

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement;
```

Annule la modification de contenu la plus récente.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconUndoProperties> |            |

### IcôneAnnulerPropriétés

**Genre :** interface

```typescript
export interface IconUndoProperties
```

Aucune description fournie.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIconTélécharger

**Genre :** fonction

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement;
```

`ForgeIconDownload` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-download.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDownloadProperties> |            |

### Propriétés de téléchargement d'icônes

**Genre :** interface

```typescript
export interface IconDownloadProperties
```

Aucune description fournie.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconTélécharger

**Genre :** fonction

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement;
```

`ForgeIconUpload` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-upload.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconUploadProperties> |            |

### Propriétés de téléchargement d'icônes

**Genre :** interface

```typescript
export interface IconUploadProperties
```

Aucune description fournie.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### ForgeIconFilter

**Genre :** fonction

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement;
```

`ForgeIconFilter` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-filter.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconFilterProperties> |            |

### Propriétés du filtre d'icônes

**Genre :** interface

```typescript
export interface IconFilterProperties
```

Aucune description fournie.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### ForgeIcôneTri

**Genre :** fonction

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement;
```

`ForgeIconSort` — un indicateur de tri à deux chevrons créé une fois dans le JSX neutre
dialecte.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-sort.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconSortProperties> |            |

### Propriétés de tri d'icônes

**Genre :** interface

```typescript
export interface IconSortProperties
```

Aucune description fournie.

### DirectionTri

**Genre :** type

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

Le sens de tri actif, ou `undefined` lorsqu'il n'est pas trié.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIcôneTableColonneAjouter

**Genre :** fonction

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement;
```

`ForgeIconTableColumnAdd` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-table-column-add.module.scss`.

#### Paramètres

| Nom        | Tapez                                       | Descriptif |
| ---------- | ------------------------------------------- | ---------- |
| propriétés | Lecture seule<IconTableColumnAddProperties> |            |

### IconTableColumnAddProperties

**Genre :** interface

```typescript
export interface IconTableColumnAddProperties
```

Aucune description fournie.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnSupprimer

**Genre :** fonction

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement;
```

`ForgeIconTableColumnRemove` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-table-column-remove.module.scss`.

#### Paramètres

| Nom        | Tapez                                          | Descriptif |
| ---------- | ---------------------------------------------- | ---------- |
| propriétés | Lecture seule<IconTableColumnRemoveProperties> |            |

### IconTableColumnRemoveProperties

**Genre :** interface

```typescript
export interface IconTableColumnRemoveProperties
```

Aucune description fournie.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

**Genre :** fonction

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement;
```

`ForgeIconTableRowAdd` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-table-row-add.module.scss`.

#### Paramètres

| Nom        | Tapez                                    | Descriptif |
| ---------- | ---------------------------------------- | ---------- |
| propriétés | Lecture seule<IconTableRowAddProperties> |            |

### IconTableRowAddProperties

**Genre :** interface

```typescript
export interface IconTableRowAddProperties
```

Aucune description fournie.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowSupprimer

**Genre :** fonction

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement;
```

`ForgeIconTableRowRemove` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-table-row-remove.module.scss`.

#### Paramètres

| Nom        | Tapez                                       | Descriptif |
| ---------- | ------------------------------------------- | ---------- |
| propriétés | Lecture seule<IconTableRowRemoveProperties> |            |

### IconTableRowRemoveProperties

**Genre :** interface

```typescript
export interface IconTableRowRemoveProperties
```

Aucune description fournie.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIcôneTable

**Genre :** fonction

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement;
```

`ForgeIconTable` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-table.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconTableProperties> |            |

### Propriétés de la table d'icônes

**Genre :** interface

```typescript
export interface IconTableProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### ForgeIcôneDessinerCercle

**Genre :** fonction

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement;
```

`ForgeIconDrawCircle` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-draw-circle.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDrawCircleProperties> |            |

### IconDrawCirclePropriétés

**Genre :** interface

```typescript
export interface IconDrawCircleProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### ForgeIcôneDrawLine

**Genre :** fonction

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement;
```

`ForgeIconDrawLine` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-draw-line.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDrawLineProperties> |            |

### Propriétés d'icôneDrawLine

**Genre :** interface

```typescript
export interface IconDrawLineProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### ForgeIcôneDessinerPolygone

**Genre :** fonction

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement;
```

`ForgeIconDrawPolygon` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-draw-polygon.module.scss`.

#### Paramètres

| Nom        | Tapez                                    | Descriptif |
| ---------- | ---------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDrawPolygonProperties> |            |

### IconDrawPolygonPropriétés

**Genre :** interface

```typescript
export interface IconDrawPolygonProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### ForgeIcôneDessinerCarré

**Genre :** fonction

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement;
```

`ForgeIconDrawSquare` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-draw-square.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDrawSquareProperties> |            |

### IcôneDessinerCarréPropriétés

**Genre :** interface

```typescript
export interface IconDrawSquareProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### ForgeIcôneDessinerTriangle

**Genre :** fonction

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement;
```

`ForgeIconDrawTriangle` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-draw-triangle.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconDrawTriangleProperties> |            |

### IcôneDessinerTrianglePropriétés

**Genre :** interface

```typescript
export interface IconDrawTriangleProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### ForgeIcôneDéplacer

**Genre :** fonction

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement;
```

`ForgeIconMove` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-move.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconMoveProperties> |            |

### IconMovePropriétés

**Genre :** interface

```typescript
export interface IconMoveProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### ForgeIcônePalette

**Genre :** fonction

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement;
```

`ForgeIconPalette` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-palette.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconPaletteProperties> |            |

### Propriétés de la palette d'icônes

**Genre :** interface

```typescript
export interface IconPaletteProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### ForgeIcôneCrayon

**Genre :** fonction

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement;
```

`ForgeIconPencil` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-pencil.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconPencilProperties> |            |

### IcôneCrayonPropriétés

**Genre :** interface

```typescript
export interface IconPencilProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### ForgeIcôneRotationCcw

**Genre :** fonction

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement;
```

`ForgeIconRotateCcw` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-rotate-ccw.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconRotateCcwProperties> |            |

### IconRotateCcwPropriétés

**Genre :** interface

```typescript
export interface IconRotateCcwProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### ForgeIcôneRotationCw

**Genre :** fonction

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement;
```

`ForgeIconRotateCw` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-rotate-cw.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconRotateCwProperties> |            |

### IconRotateCwPropriétés

**Genre :** interface

```typescript
export interface IconRotateCwProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### ForgeIcôneÉchelleDown

**Genre :** fonction

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement;
```

`ForgeIconScaleDown` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-scale-down.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconScaleDownProperties> |            |

### Propriétés IconScaleDown

**Genre :** interface

```typescript
export interface IconScaleDownProperties
```

Aucune description fournie.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### ForgeIconScaleUp

**Genre :** fonction

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement;
```

`ForgeIconScaleUp` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-scale-up.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconScaleUpProperties> |            |

### Propriétés IconScaleUp

**Genre :** interface

```typescript
export interface IconScaleUpProperties
```

Aucune description fournie.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### ForgeIcônePaysGlobe

**Genre :** fonction

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement;
```

Un marqueur de globe avec un accent de pays basé sur les données.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconCountryGlobeProperties> |            |

### IcônePaysGlobePropriétés

**Genre :** interface

```typescript
export interface IconCountryGlobeProperties
```

Aucune description fournie.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### ForgeIcôneDrapeau

**Genre :** fonction

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement;
```

Une icône de drapeau de pays délimitée et basée sur les données.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconFlagProperties> |            |

### IcôneCountryCode

**Genre :** type

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

Codes de pays pris en charge pour le composant d'indicateur basé sur les données.

### IcôneDrapeauPropriétés

**Genre :** interface

```typescript
export interface IconFlagProperties
```

Aucune description fournie.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### ForgeIcôneGéodésique

**Genre :** fonction

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement;
```

`ForgeIconGeodesic` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-geodesic.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconGeodesicProperties> |            |

### PropriétésIcôneGéodésique

**Genre :** interface

```typescript
export interface IconGeodesicProperties
```

Aucune description fournie.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### ForgeIcôneGlobe

**Genre :** fonction

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement;
```

`ForgeIconGlobe` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-globe.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconGlobeProperties> |            |

### PropriétésIcônGlobe

**Genre :** interface

```typescript
export interface IconGlobeProperties
```

Aucune description fournie.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### ForgeIcôneLangue

**Genre :** fonction

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement;
```

`ForgeIconLanguage` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-language.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconLanguageProperties> |            |

### PropriétésIcôneLangue

**Genre :** interface

```typescript
export interface IconLanguageProperties
```

Aucune description fournie.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### ForgeIcôneCartePin

**Genre :** fonction

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement;
```

`ForgeIconMapPin` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-map-pin.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconMapPinProperties> |            |

### IconMapPinPropriétés

**Genre :** interface

```typescript
export interface IconMapPinProperties
```

Aucune description fournie.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### ForgeIcôneCouche

**Genre :** fonction

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement;
```

Un symbole de pile de couches de carte pour basculer les données géographiques visibles.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconLayerProperties> |            |

### Propriétés de la couche d'icône

**Genre :** interface

```typescript
export interface IconLayerProperties
```

Aucune description fournie.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### ForgeIcôneCarteMarqueurCluster

**Genre :** fonction

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement;
```

Un marqueur cartographique groupé pour les points d'intérêt denses.

#### Paramètres

| Nom        | Tapez                                         | Descriptif |
| ---------- | --------------------------------------------- | ---------- |
| propriétés | Lecture seule<IconMapMarkerClusterProperties> |            |

### IconMapMarkerClusterPropriétés

**Genre :** interface

```typescript
export interface IconMapMarkerClusterProperties
```

Aucune description fournie.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### ForgeIcôneCaméra

**Genre :** fonction

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement;
```

`ForgeIconCamera` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-camera.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconCameraProperties> |            |

### Propriétés de la caméra d'icône

**Genre :** interface

```typescript
export interface IconCameraProperties
```

Aucune description fournie.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### ForgeIcôneImage

**Genre :** fonction

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement;
```

`ForgeIconImage` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-image.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconImageProperties> |            |

### Propriétés de l'image d'icône

**Genre :** interface

```typescript
export interface IconImageProperties
```

Aucune description fournie.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### ForgeIcônePause

**Genre :** fonction

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement;
```

`ForgeIconPause` — un glyphe de pause rempli (❙❙) créé une fois dans le JSX neutre
dialecte.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-pause.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconPauseProperties> |            |

### PropriétésIcônePause

**Genre :** interface

```typescript
export interface IconPauseProperties
```

Aucune description fournie.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### ForgeIcôneJouer

**Genre :** fonction

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement;
```

`ForgeIconPlay` — un triangle de jeu rempli (▶) créé une fois dans le JSX neutre
dialecte.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-play.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconPlayProperties> |            |

### Propriétés IconPlay

**Genre :** interface

```typescript
export interface IconPlayProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### ForgeIcôneFlèche

**Genre :** fonction

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement;
```

`ForgeIconArrow` — une flèche directionnelle créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-arrow.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconArrowProperties> |            |

### IcôneFlècheDirection

**Genre :** type

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

La direction vers laquelle pointe la flèche.

### Propriétés de la flèche d'icône

**Genre :** interface

```typescript
export interface IconArrowProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### ForgeIcôneChevron

**Genre :** fonction

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement;
```

`ForgeIconChevron` — un chevron directionnel créé une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-chevron.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconChevronProperties> |            |

### IcôneChevronPropriétés

**Genre :** interface

```typescript
export interface IconChevronProperties
```

Aucune description fournie.

### IcôneDirection

**Genre :** type

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

La direction vers laquelle pointe le chevron.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### ForgeIcôneChevrons

**Genre :** fonction

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement;
```

`ForgeIconChevrons` — un double chevron directionnel (« ») créé une fois dans le
Dialecte JSX neutre, généralement utilisé pour les contrôles passer au premier/saut au dernier.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-chevrons.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconChevronsProperties> |            |

### IcôneChevronsDirection

**Genre :** type

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

La direction vers laquelle pointe le double chevron.

### IcôneChevronsPropriétés

**Genre :** interface

```typescript
export interface IconChevronsProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### ForgeIcôneFermer

**Genre :** fonction

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement;
```

`ForgeIconClose` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-close.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconCloseProperties> |            |

### IcôneFermerPropriétés

**Genre :** interface

```typescript
export interface IconCloseProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### ForgeIconAccueil

**Genre :** fonction

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement;
```

`ForgeIconHome` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-home.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconHomeProperties> |            |

### IconHomePropriétés

**Genre :** interface

```typescript
export interface IconHomeProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### ForgeIcôneRejoindre

**Genre :** fonction

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement;
```

`ForgeIconJoin` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-join.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconJoinProperties> |            |

### IcôneJoinPropriétés

**Genre :** interface

```typescript
export interface IconJoinProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### ForgeIcôneMenu

**Genre :** fonction

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement;
```

`ForgeIconMenu` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-menu.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconMenuProperties> |            |

### IcôneMenuPropriétés

**Genre :** interface

```typescript
export interface IconMenuProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### ForgeIcôneMoins

**Genre :** fonction

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement;
```

`ForgeIconMinus` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-minus.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconMinusProperties> |            |

### IcôneMinusPropriétés

**Genre :** interface

```typescript
export interface IconMinusProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

**Genre :** fonction

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement;
```

`ForgeIconPlus` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-plus.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconPlusProperties> |            |

### Propriétés IconPlus

**Genre :** interface

```typescript
export interface IconPlusProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### ForgeIconActualiser

**Genre :** fonction

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement;
```

`ForgeIconRefresh` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-refresh.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconRefreshProperties> |            |

### IconRefreshPropriétés

**Genre :** interface

```typescript
export interface IconRefreshProperties
```

Aucune description fournie.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### ForgeIcôneSplit

**Genre :** fonction

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement;
```

`ForgeIconSplit` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-split.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconSplitProperties> |            |

### IconSplitPropriétés

**Genre :** interface

```typescript
export interface IconSplitProperties
```

Aucune description fournie.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIconExternalLink

**Genre :** fonction

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement;
```

`ForgeIconExternalLink` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-external-link.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconExternalLinkProperties> |            |

### IconExternalLinkProperties

**Genre :** interface

```typescript
export interface IconExternalLinkProperties
```

Aucune description fournie.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

**Genre :** fonction

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement;
```

`ForgeIconLink` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-link.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconLinkProperties> |            |

### PropriétésIconLink

**Genre :** interface

```typescript
export interface IconLinkProperties
```

Aucune description fournie.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### ForgeIconRecherche

**Genre :** fonction

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement;
```

`ForgeIconSearch` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-search.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconSearchProperties> |            |

### Propriétés de recherche d'icônes

**Genre :** interface

```typescript
export interface IconSearchProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### ForgeIcôneCloud

**Genre :** fonction

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement;
```

`ForgeIconCloud` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-cloud.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconCloudProperties> |            |

### Propriétés IconCloud

**Genre :** interface

```typescript
export interface IconCloudProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### ForgeIconDebug

**Genre :** fonction

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement;
```

`ForgeIconDebug` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-debug.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconDebugProperties> |            |

### IconDebugPropriétés

**Genre :** interface

```typescript
export interface IconDebugProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### ForgeIcôneCoeur

**Genre :** fonction

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement;
```

`ForgeIconHeart` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heart.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeartProperties> |            |

### PropriétésIcôneCœur

**Genre :** interface

```typescript
export interface IconHeartProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### ForgeIcôneLa Foudre

**Genre :** fonction

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement;
```

`ForgeIconLightning` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-lightning.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconLightningProperties> |            |

### Propriétés d'IcôneLightning

**Genre :** interface

```typescript
export interface IconLightningProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIcôneCasse-tête

**Genre :** fonction

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement;
```

`ForgeIconPuzzle` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-puzzle.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconPuzzleProperties> |            |

### IcônePuzzlePropriétés

**Genre :** interface

```typescript
export interface IconPuzzleProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIcôneQrCode

**Genre :** fonction

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement;
```

`ForgeIconQrCode` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-qr-code.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconQrCodeProperties> |            |

### IcôneQrCodePropriétés

**Genre :** interface

```typescript
export interface IconQrCodeProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### ForgeIconParamètres

**Genre :** fonction

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement;
```

`ForgeIconSettings` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-settings.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconSettingsProperties> |            |

### IcôneParamètresPropriétés

**Genre :** interface

```typescript
export interface IconSettingsProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### ForgeIcôneÉtoile

**Genre :** fonction

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement;
```

`ForgeIconStar` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-star.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconStarProperties> |            |

### PropriétésIcôneÉtoile

**Genre :** interface

```typescript
export interface IconStarProperties
```

Aucune description fournie.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### ForgeIcôneClé

**Genre :** fonction

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement;
```

`ForgeIconWrench` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-wrench.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconWrenchProperties> |            |

### Propriétés d'IconWrench

**Genre :** interface

```typescript
export interface IconWrenchProperties
```

Aucune description fournie.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### ForgeIcôneRoute

**Genre :** fonction

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement;
```

Une ligne d'itinéraire avec des marqueurs de départ et de destination distincts.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconRouteProperties> |            |

### PropriétésIcôneRoute

**Genre :** interface

```typescript
export interface IconRouteProperties
```

Aucune description fournie.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### ForgeIcôneWaypoint

**Genre :** fonction

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement;
```

Un marqueur de waypoint réutilisable pour les itinéraires et les superpositions de cartes.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconWaypointProperties> |            |

### Propriétés d'icôneWaypoint

**Genre :** interface

```typescript
export interface IconWaypointProperties
```

Aucune description fournie.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### ForgeIcôneVerrouillageOuvert

**Genre :** fonction

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement;
```

`ForgeIconLockOpen` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-lock-open.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconLockOpenProperties> |            |

### IconLockOpenProperties

**Genre :** interface

```typescript
export interface IconLockOpenProperties
```

Aucune description fournie.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### ForgeIconLock

**Genre :** fonction

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement;
```

`ForgeIconLock` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-lock.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconLockProperties> |            |

### Propriétés IconLock

**Genre :** interface

```typescript
export interface IconLockProperties
```

Aucune description fournie.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIconUtilisateur

**Genre :** fonction

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement;
```

`ForgeIconUser` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-user.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconUserProperties> |            |

### PropriétésIcôneUtilisateur

**Genre :** interface

```typescript
export interface IconUserProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIcôneAlerteCritique

**Genre :** fonction

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement;
```

`ForgeIconAlertCritical` — le glyphe d'alerte critique (un "!" à l'intérieur d'un octogone,
évoquant un panneau d'arrêt) rédigé une fois dans le dialecte neutre JSX.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-alert-critical.module.scss`.

#### Paramètres

| Nom        | Tapez                                      | Descriptif |
| ---------- | ------------------------------------------ | ---------- |
| propriétés | Lecture seule<IconAlertCriticalProperties> |            |

### IcôneAlerteCritiquePropriétés

**Genre :** interface

```typescript
export interface IconAlertCriticalProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

**Genre :** fonction

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement;
```

`ForgeIconAlertInfo` — le glyphe d'alerte informatif (un « i » à l'intérieur d'un cercle)
rédigé une fois dans le dialecte neutre JSX.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-alert-info.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlertInfoProperties> |            |

### IconAlertInfoPropriétés

**Genre :** interface

```typescript
export interface IconAlertInfoProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### ForgeIcôneAlerteNeutre

**Genre :** fonction

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement;
```

`ForgeIconAlertNeutral` — le glyphe d'alerte neutre (un "-" à l'intérieur d'un cercle)
rédigé une fois dans le dialecte JSX neutre, pour les messages d'information sans
connotation positive ou négative.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-alert-neutral.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlertNeutralProperties> |            |

### IcôneAlerteNeutrePropriétés

**Genre :** interface

```typescript
export interface IconAlertNeutralProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### ForgeIconAlerteAvertissement

**Genre :** fonction

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement;
```

`ForgeIconAlertWarning` — le glyphe d'alerte d'avertissement (un "!" à l'intérieur d'un triangle)
rédigé une fois dans le dialecte neutre JSX.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-alert-warning.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlertWarningProperties> |            |

### IconAlertWarningPropriétés

**Genre :** interface

```typescript
export interface IconAlertWarningProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### ForgeIconAlerte

**Genre :** fonction

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement;
```

`ForgeIconAlert` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-alert.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlertProperties> |            |

### Propriétés d'alerte d'icône

**Genre :** interface

```typescript
export interface IconAlertProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### ForgeIconCheck

**Genre :** fonction

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement;
```

`ForgeIconCheck` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-check.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconCheckProperties> |            |

### IconCheckPropriétés

**Genre :** interface

```typescript
export interface IconCheckProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIcôneErreur

**Genre :** fonction

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement;
```

`ForgeIconError` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-error.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconErrorProperties> |            |

### Propriétés d'erreur d'icône

**Genre :** interface

```typescript
export interface IconErrorProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### ForgeIconInfo

**Genre :** fonction

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement;
```

`ForgeIconInfo` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-info.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconInfoProperties> |            |

### IconInfoPropriétés

**Genre :** interface

```typescript
export interface IconInfoProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### ForgeIcôneAvis

**Genre :** fonction

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement;
```

`ForgeIconNotice` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-notice.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconNoticeProperties> |            |

### PropriétésIcôneAvis

**Genre :** interface

```typescript
export interface IconNoticeProperties
```

Aucune description fournie.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### ForgeIcôneAvertissement

**Genre :** fonction

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement;
```

`ForgeIconWarning` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-warning.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconWarningProperties> |            |

### Propriétés d'avertissement d'icône

**Genre :** interface

```typescript
export interface IconWarningProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### ForgeIconAlignCenter

**Genre :** fonction

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement;
```

`ForgeIconAlignCenter` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-align-center.module.scss`.

#### Paramètres

| Nom        | Tapez                                    | Descriptif |
| ---------- | ---------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlignCenterProperties> |            |

### IconAlignCenterPropriétés

**Genre :** interface

```typescript
export interface IconAlignCenterProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### ForgeIcôneAlignerJustifier

**Genre :** fonction

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement;
```

`ForgeIconAlignJustify` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-align-justify.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlignJustifyProperties> |            |

### IconAlignJustifyProperties

**Genre :** interface

```typescript
export interface IconAlignJustifyProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### ForgeIcôneAlignerGauche

**Genre :** fonction

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement;
```

`ForgeIconAlignLeft` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-align-left.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlignLeftProperties> |            |

### IconAlignLeftPropriétés

**Genre :** interface

```typescript
export interface IconAlignLeftProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### ForgeIcôneAligner à droite

**Genre :** fonction

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement;
```

`ForgeIconAlignRight` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-align-right.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconAlignRightProperties> |            |

### IconAlignRightPropriétés

**Genre :** interface

```typescript
export interface IconAlignRightProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIcôneBlocquote

**Genre :** fonction

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement;
```

`ForgeIconBlockquote` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-blockquote.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconBlockquoteProperties> |            |

### IconBlockquotePropriétés

**Genre :** interface

```typescript
export interface IconBlockquoteProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### ForgeIcôneGras

**Genre :** fonction

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement;
```

`ForgeIconBold` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-bold.module.scss`.

#### Paramètres

| Nom        | Tapez                             | Descriptif |
| ---------- | --------------------------------- | ---------- |
| propriétés | Lecture seule<IconBoldProperties> |            |

### IcôneGrasPropriétés

**Genre :** interface

```typescript
export interface IconBoldProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### ForgeIcôneBulletListe

**Genre :** fonction

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement;
```

`ForgeIconBulletList` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-bullet-list.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconBulletListProperties> |            |

### IconBulletListPropriétés

**Genre :** interface

```typescript
export interface IconBulletListProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIcôneCodeBloc

**Genre :** fonction

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement;
```

`ForgeIconCodeBlock` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-code-block.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconCodeBlockProperties> |            |

### IconCodeBlockPropriétés

**Genre :** interface

```typescript
export interface IconCodeBlockProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

**Genre :** fonction

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement;
```

`ForgeIconCodeInline` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-code-inline.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconCodeInlineProperties> |            |

### IconCodeInlineProperties

**Genre :** interface

```typescript
export interface IconCodeInlineProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### ForgeIcôneTitreCinq

**Genre :** fonction

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement;
```

`ForgeIconHeadingFive` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-five.module.scss`.

#### Paramètres

| Nom        | Tapez                                    | Descriptif |
| ---------- | ---------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingFiveProperties> |            |

### IcôneTitreCinqPropriétés

**Genre :** interface

```typescript
export interface IconHeadingFiveProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### ForgeIcôneTitreQuatre

**Genre :** fonction

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement;
```

`ForgeIconHeadingFour` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-four.module.scss`.

#### Paramètres

| Nom        | Tapez                                    | Descriptif |
| ---------- | ---------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingFourProperties> |            |

### IcôneTitreQuatrePropriétés

**Genre :** interface

```typescript
export interface IconHeadingFourProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### ForgeIcôneTitreUn

**Genre :** fonction

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement;
```

`ForgeIconHeadingOne` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-one.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingOneProperties> |            |

### IconHeadingOnePropriétés

**Genre :** interface

```typescript
export interface IconHeadingOneProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### ForgeIcôneTitreSix

**Genre :** fonction

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement;
```

`ForgeIconHeadingSix` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-six.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingSixProperties> |            |

### IcôneTitreSixPropriétés

**Genre :** interface

```typescript
export interface IconHeadingSixProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### ForgeIcôneTitreTrois

**Genre :** fonction

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement;
```

`ForgeIconHeadingThree` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-three.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingThreeProperties> |            |

### IcôneTitreTroisPropriétés

**Genre :** interface

```typescript
export interface IconHeadingThreeProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### ForgeIcôneTitreDeux

**Genre :** fonction

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement;
```

`ForgeIconHeadingTwo` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading-two.module.scss`.

#### Paramètres

| Nom        | Tapez                                   | Descriptif |
| ---------- | --------------------------------------- | ---------- |
| propriétés | Lecture seule<IconHeadingTwoProperties> |            |

### IcôneTitreDeuxPropriétés

**Genre :** interface

```typescript
export interface IconHeadingTwoProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### ForgeIcôneEn-tête

**Genre :** fonction

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement;
```

`ForgeIconHeading` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-heading.module.scss`.

#### Paramètres

| Nom        | Tapez                                | Descriptif |
| ---------- | ------------------------------------ | ---------- |
| propriétés | Lecture seule<IconHeadingProperties> |            |

### Propriétés d'en-tête d'icône

**Genre :** interface

```typescript
export interface IconHeadingProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### ForgeIcôneItalique

**Genre :** fonction

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement;
```

`ForgeIconItalic` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-italic.module.scss`.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconItalicProperties> |            |

### PropriétésIcôneItalique

**Genre :** interface

```typescript
export interface IconItalicProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

**Genre :** fonction

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement;
```

`ForgeIconNumberedList` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-numbered-list.module.scss`.

#### Paramètres

| Nom        | Tapez                                     | Descriptif |
| ---------- | ----------------------------------------- | ---------- |
| propriétés | Lecture seule<IconNumberedListProperties> |            |

### IconNumberedListPropriétés

**Genre :** interface

```typescript
export interface IconNumberedListProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### ForgeIcôneBarré

**Genre :** fonction

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement;
```

`ForgeIconStrikethrough` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-strikethrough.module.scss`.

#### Paramètres

| Nom        | Tapez                                      | Descriptif |
| ---------- | ------------------------------------------ | ---------- |
| propriétés | Lecture seule<IconStrikethroughProperties> |            |

### IcôneBarréPropriétés

**Genre :** interface

```typescript
export interface IconStrikethroughProperties
```

Aucune description fournie.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### ForgeIcôneSouligner

**Genre :** fonction

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement;
```

`ForgeIconUnderline` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-underline.module.scss`.

#### Paramètres

| Nom        | Tapez                                  | Descriptif |
| ---------- | -------------------------------------- | ---------- |
| propriétés | Lecture seule<IconUnderlineProperties> |            |

### Propriétés d'icônesoulignement

**Genre :** interface

```typescript
export interface IconUnderlineProperties
```

Aucune description fournie.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### ForgeIcôneCalendrier

**Genre :** fonction

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement;
```

`ForgeIconCalendar` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-calendar.module.scss`.

#### Paramètres

| Nom        | Tapez                                 | Descriptif |
| ---------- | ------------------------------------- | ---------- |
| propriétés | Lecture seule<IconCalendarProperties> |            |

### IcôneCalendrierPropriétés

**Genre :** interface

```typescript
export interface IconCalendarProperties
```

Aucune description fournie.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### ForgeIcôneHorloge

**Genre :** fonction

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement;
```

`ForgeIconClock` — une icône créée une fois dans le dialecte JSX neutre.

Il se compile proprement sur Vue 3 et React via le vite-plugin-forge
compilateur en deux étapes. L'icône envoie son propre CSS `@layer mp.icons` via le
Module CSS colocalisé `forge-icon-clock.module.scss`.

#### Paramètres

| Nom        | Tapez                              | Descriptif |
| ---------- | ---------------------------------- | ---------- |
| propriétés | Lecture seule<IconClockProperties> |            |

### Propriétés de l'horloge d'icône

**Genre :** interface

```typescript
export interface IconClockProperties
```

Aucune description fournie.

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**Genre :** constante

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[];
```

Aucune description fournie.

### validerCompositions

**Genre :** fonction

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void;
```

Aucune description fournie.

#### Paramètres

| Nom                      | Tapez                                | Descriptif |
| ------------------------ | ------------------------------------ | ---------- |
| compositions             | lecture seule IconSymbolDefinition[] |            |
| identifiants disponibles | ReadonlySet<string>                  |            |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**Genre :** constante

```typescript
export const SUPPORTED_COUNTRY_CODES;
```

Codes de pays avec symboles de drapeau canoniques dans le sprite publié.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**Genre :** constante

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS;
```

Aucune description fournie.

### ICON_SYMBOL_DEFINITIONS

**Genre :** constante

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[];
```

Aucune description fournie.

## `src/sprite/icon-use`

### ForgeIcôneUtilisation

**Genre :** fonction

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement;
```

Wrapper accessible partagé qui fait référence à la géométrie canonique du sprite.

#### Paramètres

| Nom        | Tapez                            | Descriptif |
| ---------- | -------------------------------- | ---------- |
| propriétés | Lecture seule<IconUseProperties> |            |

### IcôneUtiliserPropriétés

**Genre :** interface

```typescript
export interface IconUseProperties
```

Aucune description fournie.

## `src/sprite/provider`

### IcôneSpriteContext

**Genre :** constante

```typescript
export const IconSpriteContext;
```

Aucune description fournie.

### IconSpriteProvider

**Genre :** fonction

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement;
```

Montez un hôte de sprite en ligne pour un sous-arbre de composants d'icônes.

#### Paramètres

| Nom        | Tapez                               | Descriptif |
| ---------- | ----------------------------------- | ---------- |
| propriétés | Lecture seule<IconSpriteProperties> |            |

### useIconHref

**Genre :** fonction

```typescript
function useIconHref(symbolId: string): string;
```

Résolvez une URL de symbole local ou externe pour un wrapper d'icône.

#### Paramètres

| Nom        | Tapez  | Descriptif |
| ---------- | ------ | ---------- |
| ID symbole | chaîne |            |

## `src/sprite/serialize`

### sérialiserNode

**Genre :** fonction

```typescript
function serializeNode(node: IconSvgNode): string;
```

Aucune description fournie.

#### Paramètres

| Nom  | Tapez        | Descriptif |
| ---- | ------------ | ---------- |
| node | IcôneSvgNode |            |

### sérialiserSprite

**Genre :** fonction

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string;
```

Aucune description fournie.

#### Paramètres

| Nom         | Tapez                                | Descriptif |
| ----------- | ------------------------------------ | ---------- |
| définitions | lecture seule IconSymbolDefinition[] |            |

### sérialiserSymbole

**Genre :** fonction

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string;
```

Aucune description fournie.

#### Paramètres

| Nom        | Tapez                  | Descriptif |
| ---------- | ---------------------- | ---------- |
| définition | IcôneSymboleDéfinition |            |

## `src/sprite/types`

### IconSpriteContextValue

**Genre :** interface

```typescript
export interface IconSpriteContextValue
```

Aucune description fournie.

### PropriétésIcônSprite

**Genre :** interface

```typescript
export interface IconSpriteProperties
```

Aucune description fournie.

### IcôneSvgNode

**Genre :** interface

```typescript
export interface IconSvgNode
```

Aucune description fournie.

### IcôneSymboleDéfinition

**Genre :** interface

```typescript
export interface IconSymbolDefinition
```

Aucune description fournie.

### IcôneSymboleUtilisation

**Genre :** interface

```typescript
export interface IconSymbolUse
```

Aucune description fournie.
