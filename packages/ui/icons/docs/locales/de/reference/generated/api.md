# @mission-platform/icons API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/ui/icons/docs/reference/generated/api.md: [packages/ui/icons/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/icons`.

## `src/catalog`

### ICON_CATALOG

**Art:** konstant

```typescript
export const ICON_CATALOG: readonly IconCatalogEntry[];
```

Der überprüfte Katalog für den vorhandenen Symbolsatz.

### ICON_GAP_REVIEW

**Art:** konstant

```typescript
export const ICON_GAP_REVIEW;
```

Hochwertige Ergänzungen, die nach der Festlegung der Quelltaxonomie implementiert werden müssen.

### IconCatalogEntry

**Art:** Schnittstelle

```typescript
export interface IconCatalogEntry
```

Eine stabile Kategoriezuweisung für jede Symbolquelle in diesem Paket.

### validierenIconCatalog

**Art:** Funktion

```typescript
function validateIconCatalog(catalog: readonly IconCatalogEntry[] = ICON_CATALOG): void;
```

Validieren Sie den statischen Katalog, bevor er zum Generieren von Paketmetadaten verwendet wird.

#### Parameter

| Name    | Geben Sie                           | ein Beschreibung |
| ------- | ----------------------------------- | ---------------- |
| Katalog | schreibgeschützt IconCatalogEntry[] |                  |

## `src/components/communication/messaging/forge-icon-bell/forge-icon-bell`

### ForgeIconBell

**Art:** Funktion

```typescript
function ForgeIconBell(properties: Readonly<IconBellProperties>): MpElement;
```

`ForgeIconBell` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-bell.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconBellProperties> |                  |

### IconBellProperties

**Art:** Schnittstelle

```typescript
export interface IconBellProperties
```

Keine Beschreibung angegeben.

## `src/components/communication/messaging/forge-icon-chat/forge-icon-chat`

### ForgeIconChat

**Art:** Funktion

```typescript
function ForgeIconChat(properties: Readonly<IconChatProperties>): MpElement;
```

`ForgeIconChat` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-chat.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconChatProperties> |                  |

### IconChatProperties

**Art:** Schnittstelle

```typescript
export interface IconChatProperties
```

Keine Beschreibung angegeben.

## `src/components/communication/messaging/forge-icon-mail/forge-icon-mail`

### ForgeIconMail

**Art:** Funktion

```typescript
function ForgeIconMail(properties: Readonly<IconMailProperties>): MpElement;
```

`ForgeIconMail` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-mail.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMailProperties> |                  |

### IconMailProperties

**Art:** Schnittstelle

```typescript
export interface IconMailProperties
```

Keine Beschreibung angegeben.

## `src/components/communication/messaging/forge-icon-phone/forge-icon-phone`

### ForgeIconPhone

**Art:** Funktion

```typescript
function ForgeIconPhone(properties: Readonly<IconPhoneProperties>): MpElement;
```

`ForgeIconPhone` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-phone.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPhoneProperties> |                  |

### IconPhoneProperties

**Art:** Schnittstelle

```typescript
export interface IconPhoneProperties
```

Keine Beschreibung angegeben.

## `src/components/communication/messaging/forge-icon-send/forge-icon-send`

### ForgeIconSend

**Art:** Funktion

```typescript
function ForgeIconSend(properties: Readonly<IconSendProperties>): MpElement;
```

`ForgeIconSend` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-send.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSendProperties> |                  |

### IconSendProperties

**Art:** Schnittstelle

```typescript
export interface IconSendProperties
```

Keine Beschreibung angegeben.

## `src/components/communication/sharing/forge-icon-share/forge-icon-share`

### ForgeIconShare

**Art:** Funktion

```typescript
function ForgeIconShare(properties: Readonly<IconShareProperties>): MpElement;
```

`ForgeIconShare` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-share.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconShareProperties> |                  |

### IconShareProperties

**Art:** Schnittstelle

```typescript
export interface IconShareProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-copy/forge-icon-copy`

### ForgeIconCopy

**Art:** Funktion

```typescript
function ForgeIconCopy(properties: Readonly<IconCopyProperties>): MpElement;
```

`ForgeIconCopy` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-copy.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCopyProperties> |                  |

### IconCopyProperties

**Art:** Schnittstelle

```typescript
export interface IconCopyProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-edit/forge-icon-edit`

### ForgeIconEdit

**Art:** Funktion

```typescript
function ForgeIconEdit(properties: Readonly<IconEditProperties>): MpElement;
```

`ForgeIconEdit` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-edit.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconEditProperties> |                  |

### IconEditProperties

**Art:** Schnittstelle

```typescript
export interface IconEditProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-eye-off/forge-icon-eye-off`

### ForgeIconEyeOff

**Art:** Funktion

```typescript
function ForgeIconEyeOff(properties: Readonly<IconEyeOffProperties>): MpElement;
```

`ForgeIconEyeOff` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-eye-off.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconEyeOffProperties> |                  |

### IconEyeOffProperties

**Art:** Schnittstelle

```typescript
export interface IconEyeOffProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-eye/forge-icon-eye`

### ForgeIconEye

**Art:** Funktion

```typescript
function ForgeIconEye(properties: Readonly<IconEyeProperties>): MpElement;
```

`ForgeIconEye` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-eye.module.scss`.

#### Parameter

| Name          | Geben Sie                           | ein Beschreibung |
| ------------- | ----------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconEyeProperties> |                  |

### IconEyeProperties

**Art:** Schnittstelle

```typescript
export interface IconEyeProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-redo/forge-icon-redo`

### ForgeIconRedo

**Art:** Funktion

```typescript
function ForgeIconRedo(properties: Readonly<IconRedoProperties>): MpElement;
```

Wendet die zuletzt rückgängig gemachte Inhaltsbearbeitung erneut an.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconRedoProperties> |                  |

### IconRedoProperties

**Art:** Schnittstelle

```typescript
export interface IconRedoProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-trash/forge-icon-trash`

### ForgeIconTrash

**Art:** Funktion

```typescript
function ForgeIconTrash(properties: Readonly<IconTrashProperties>): MpElement;
```

`ForgeIconTrash` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-trash.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTrashProperties> |                  |

### IconTrashProperties

**Art:** Schnittstelle

```typescript
export interface IconTrashProperties
```

Keine Beschreibung angegeben.

## `src/components/content/editing/forge-icon-undo/forge-icon-undo`

### ForgeIconUndo

**Art:** Funktion

```typescript
function ForgeIconUndo(properties: Readonly<IconUndoProperties>): MpElement;
```

Macht die letzte Inhaltsbearbeitung rückgängig.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconUndoProperties> |                  |

### IconUndoProperties

**Art:** Schnittstelle

```typescript
export interface IconUndoProperties
```

Keine Beschreibung angegeben.

## `src/components/content/files/forge-icon-download/forge-icon-download`

### ForgeIconDownload

**Art:** Funktion

```typescript
function ForgeIconDownload(properties: Readonly<IconDownloadProperties>): MpElement;
```

`ForgeIconDownload` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-download.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDownloadProperties> |                  |

### IconDownloadProperties

**Art:** Schnittstelle

```typescript
export interface IconDownloadProperties
```

Keine Beschreibung angegeben.

## `src/components/content/files/forge-icon-upload/forge-icon-upload`

### ForgeIconUpload

**Art:** Funktion

```typescript
function ForgeIconUpload(properties: Readonly<IconUploadProperties>): MpElement;
```

`ForgeIconUpload` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-upload.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconUploadProperties> |                  |

### IconUploadProperties

**Art:** Schnittstelle

```typescript
export interface IconUploadProperties
```

Keine Beschreibung angegeben.

## `src/components/data/filtering/forge-icon-filter/forge-icon-filter`

### ForgeIconFilter

**Art:** Funktion

```typescript
function ForgeIconFilter(properties: Readonly<IconFilterProperties>): MpElement;
```

`ForgeIconFilter` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-filter.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconFilterProperties> |                  |

### IconFilterProperties

**Art:** Schnittstelle

```typescript
export interface IconFilterProperties
```

Keine Beschreibung angegeben.

## `src/components/data/tables/forge-icon-sort/forge-icon-sort`

### ForgeIconSort

**Art:** Funktion

```typescript
function ForgeIconSort(properties: Readonly<IconSortProperties>): MpElement;
```

`ForgeIconSort` – ein Zwei-Chevron-Sortierungsindikator, der einmal im neutralen JSX erstellt wurde
Dialekt.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-sort.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSortProperties> |                  |

### IconSortProperties

**Art:** Schnittstelle

```typescript
export interface IconSortProperties
```

Keine Beschreibung angegeben.

### Sortierrichtung

**Art:** Typ

```typescript
export type SortDirection = 'asc' | 'desc' | undefined;
```

Die aktive Sortierrichtung oder `undefined`, wenn unsortiert.

## `src/components/data/tables/forge-icon-table-column-add/forge-icon-table-column-add`

### ForgeIconTableColumnAdd

**Art:** Funktion

```typescript
function ForgeIconTableColumnAdd(properties: Readonly<IconTableColumnAddProperties>): MpElement;
```

`ForgeIconTableColumnAdd` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-table-column-add.module.scss`.

#### Parameter

| Name          | Geben Sie                                      | ein Beschreibung |
| ------------- | ---------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTableColumnAddProperties> |                  |

### IconTableColumnAddProperties

**Art:** Schnittstelle

```typescript
export interface IconTableColumnAddProperties
```

Keine Beschreibung angegeben.

## `src/components/data/tables/forge-icon-table-column-remove/forge-icon-table-column-remove`

### ForgeIconTableColumnRemove

**Art:** Funktion

```typescript
function ForgeIconTableColumnRemove(properties: Readonly<IconTableColumnRemoveProperties>): MpElement;
```

`ForgeIconTableColumnRemove` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-table-column-remove.module.scss`.

#### Parameter

| Name          | Geben Sie                                         | ein Beschreibung |
| ------------- | ------------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTableColumnRemoveProperties> |                  |

### IconTableColumnRemoveProperties

**Art:** Schnittstelle

```typescript
export interface IconTableColumnRemoveProperties
```

Keine Beschreibung angegeben.

## `src/components/data/tables/forge-icon-table-row-add/forge-icon-table-row-add`

### ForgeIconTableRowAdd

**Art:** Funktion

```typescript
function ForgeIconTableRowAdd(properties: Readonly<IconTableRowAddProperties>): MpElement;
```

`ForgeIconTableRowAdd` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-table-row-add.module.scss`.

#### Parameter

| Name          | Geben Sie                                   | ein Beschreibung |
| ------------- | ------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTableRowAddProperties> |                  |

### IconTableRowAddProperties

**Art:** Schnittstelle

```typescript
export interface IconTableRowAddProperties
```

Keine Beschreibung angegeben.

## `src/components/data/tables/forge-icon-table-row-remove/forge-icon-table-row-remove`

### ForgeIconTableRowRemove

**Art:** Funktion

```typescript
function ForgeIconTableRowRemove(properties: Readonly<IconTableRowRemoveProperties>): MpElement;
```

`ForgeIconTableRowRemove` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-table-row-remove.module.scss`.

#### Parameter

| Name          | Geben Sie                                      | ein Beschreibung |
| ------------- | ---------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTableRowRemoveProperties> |                  |

### IconTableRowRemoveProperties

**Art:** Schnittstelle

```typescript
export interface IconTableRowRemoveProperties
```

Keine Beschreibung angegeben.

## `src/components/data/tables/forge-icon-table/forge-icon-table`

### ForgeIconTable

**Art:** Funktion

```typescript
function ForgeIconTable(properties: Readonly<IconTableProperties>): MpElement;
```

`ForgeIconTable` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-table.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconTableProperties> |                  |

### IconTableProperties

**Art:** Schnittstelle

```typescript
export interface IconTableProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-draw-circle/forge-icon-draw-circle`

### ForgeIconDrawCircle

**Art:** Funktion

```typescript
function ForgeIconDrawCircle(properties: Readonly<IconDrawCircleProperties>): MpElement;
```

`ForgeIconDrawCircle` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-draw-circle.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDrawCircleProperties> |                  |

### IconDrawCircleProperties

**Art:** Schnittstelle

```typescript
export interface IconDrawCircleProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-draw-line/forge-icon-draw-line`

### ForgeIconDrawLine

**Art:** Funktion

```typescript
function ForgeIconDrawLine(properties: Readonly<IconDrawLineProperties>): MpElement;
```

`ForgeIconDrawLine` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-draw-line.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDrawLineProperties> |                  |

### IconDrawLineProperties

**Art:** Schnittstelle

```typescript
export interface IconDrawLineProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-draw-polygon/forge-icon-draw-polygon`

### ForgeIconDrawPolygon

**Art:** Funktion

```typescript
function ForgeIconDrawPolygon(properties: Readonly<IconDrawPolygonProperties>): MpElement;
```

`ForgeIconDrawPolygon` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-draw-polygon.module.scss`.

#### Parameter

| Name          | Geben Sie                                   | ein Beschreibung |
| ------------- | ------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDrawPolygonProperties> |                  |

### IconDrawPolygonProperties

**Art:** Schnittstelle

```typescript
export interface IconDrawPolygonProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-draw-square/forge-icon-draw-square`

### ForgeIconDrawSquare

**Art:** Funktion

```typescript
function ForgeIconDrawSquare(properties: Readonly<IconDrawSquareProperties>): MpElement;
```

`ForgeIconDrawSquare` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-draw-square.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDrawSquareProperties> |                  |

### IconDrawSquareProperties

**Art:** Schnittstelle

```typescript
export interface IconDrawSquareProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-draw-triangle/forge-icon-draw-triangle`

### ForgeIconDrawTriangle

**Art:** Funktion

```typescript
function ForgeIconDrawTriangle(properties: Readonly<IconDrawTriangleProperties>): MpElement;
```

`ForgeIconDrawTriangle` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-draw-triangle.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDrawTriangleProperties> |                  |

### IconDrawTriangleProperties

**Art:** Schnittstelle

```typescript
export interface IconDrawTriangleProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-move/forge-icon-move`

### ForgeIconMove

**Art:** Funktion

```typescript
function ForgeIconMove(properties: Readonly<IconMoveProperties>): MpElement;
```

`ForgeIconMove` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-move.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMoveProperties> |                  |

### IconMoveProperties

**Art:** Schnittstelle

```typescript
export interface IconMoveProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-palette/forge-icon-palette`

### ForgeIconPalette

**Art:** Funktion

```typescript
function ForgeIconPalette(properties: Readonly<IconPaletteProperties>): MpElement;
```

`ForgeIconPalette` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-palette.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPaletteProperties> |                  |

### IconPaletteProperties

**Art:** Schnittstelle

```typescript
export interface IconPaletteProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-pencil/forge-icon-pencil`

### ForgeIconPencil

**Art:** Funktion

```typescript
function ForgeIconPencil(properties: Readonly<IconPencilProperties>): MpElement;
```

`ForgeIconPencil` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-pencil.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPencilProperties> |                  |

### IconPencilProperties

**Art:** Schnittstelle

```typescript
export interface IconPencilProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-rotate-ccw/forge-icon-rotate-ccw`

### ForgeIconRotateCcw

**Art:** Funktion

```typescript
function ForgeIconRotateCcw(properties: Readonly<IconRotateCcwProperties>): MpElement;
```

`ForgeIconRotateCcw` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-rotate-ccw.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconRotateCcwProperties> |                  |

### IconRotateCcwProperties

**Art:** Schnittstelle

```typescript
export interface IconRotateCcwProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-rotate-cw/forge-icon-rotate-cw`

### ForgeIconRotateCw

**Art:** Funktion

```typescript
function ForgeIconRotateCw(properties: Readonly<IconRotateCwProperties>): MpElement;
```

`ForgeIconRotateCw` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-rotate-cw.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconRotateCwProperties> |                  |

### IconRotateCwProperties

**Art:** Schnittstelle

```typescript
export interface IconRotateCwProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-scale-down/forge-icon-scale-down`

### ForgeIconScaleDown

**Art:** Funktion

```typescript
function ForgeIconScaleDown(properties: Readonly<IconScaleDownProperties>): MpElement;
```

`ForgeIconScaleDown` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-scale-down.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconScaleDownProperties> |                  |

### IconScaleDownProperties

**Art:** Schnittstelle

```typescript
export interface IconScaleDownProperties
```

Keine Beschreibung angegeben.

## `src/components/drawing/transform/forge-icon-scale-up/forge-icon-scale-up`

### ForgeIconScaleUp

**Art:** Funktion

```typescript
function ForgeIconScaleUp(properties: Readonly<IconScaleUpProperties>): MpElement;
```

`ForgeIconScaleUp` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-scale-up.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconScaleUpProperties> |                  |

### IconScaleUpProperties

**Art:** Schnittstelle

```typescript
export interface IconScaleUpProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/countries/forge-icon-country-globe/forge-icon-country-globe`

### ForgeIconCountryGlobe

**Art:** Funktion

```typescript
function ForgeIconCountryGlobe(properties: Readonly<IconCountryGlobeProperties>): MpElement;
```

Ein Globusmarker mit einem datengesteuerten Länderakzent.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCountryGlobeProperties> |                  |

### IconCountryGlobeProperties

**Art:** Schnittstelle

```typescript
export interface IconCountryGlobeProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/countries/forge-icon-flag/forge-icon-flag`

### ForgeIconFlag

**Art:** Funktion

```typescript
function ForgeIconFlag(properties: Readonly<IconFlagProperties>): MpElement;
```

Ein begrenztes, datengesteuertes Landesflaggensymbol.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconFlagProperties> |                  |

### IconCountryCode

**Art:** Typ

```typescript
export type IconCountryCode = (typeof SUPPORTED_COUNTRY_CODES)[number];
```

Unterstützte Ländercodes für die datengesteuerte Flaggenkomponente.

### IconFlagProperties

**Art:** Schnittstelle

```typescript
export interface IconFlagProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/geography/forge-icon-geodesic/forge-icon-geodesic`

### ForgeIconGeodesic

**Art:** Funktion

```typescript
function ForgeIconGeodesic(properties: Readonly<IconGeodesicProperties>): MpElement;
```

`ForgeIconGeodesic` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-geodesic.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconGeodesicProperties> |                  |

### IconGeodesicProperties

**Art:** Schnittstelle

```typescript
export interface IconGeodesicProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/geography/forge-icon-globe/forge-icon-globe`

### ForgeIconGlobe

**Art:** Funktion

```typescript
function ForgeIconGlobe(properties: Readonly<IconGlobeProperties>): MpElement;
```

`ForgeIconGlobe` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-globe.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconGlobeProperties> |                  |

### IconGlobeProperties

**Art:** Schnittstelle

```typescript
export interface IconGlobeProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/geography/forge-icon-language/forge-icon-language`

### ForgeIconLanguage

**Art:** Funktion

```typescript
function ForgeIconLanguage(properties: Readonly<IconLanguageProperties>): MpElement;
```

`ForgeIconLanguage` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-language.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLanguageProperties> |                  |

### IconLanguageProperties

**Art:** Schnittstelle

```typescript
export interface IconLanguageProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/geography/forge-icon-map-pin/forge-icon-map-pin`

### ForgeIconMapPin

**Art:** Funktion

```typescript
function ForgeIconMapPin(properties: Readonly<IconMapPinProperties>): MpElement;
```

`ForgeIconMapPin` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-map-pin.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMapPinProperties> |                  |

### IconMapPinProperties

**Art:** Schnittstelle

```typescript
export interface IconMapPinProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/layers/forge-icon-layer/forge-icon-layer`

### ForgeIconLayer

**Art:** Funktion

```typescript
function ForgeIconLayer(properties: Readonly<IconLayerProperties>): MpElement;
```

Ein Kartenebenen-Stapelsymbol zum Umschalten sichtbarer geografischer Daten.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLayerProperties> |                  |

### IconLayerProperties

**Art:** Schnittstelle

```typescript
export interface IconLayerProperties
```

Keine Beschreibung angegeben.

## `src/components/maps/markers/forge-icon-map-marker-cluster/forge-icon-map-marker-cluster`

### ForgeIconMapMarkerCluster

**Art:** Funktion

```typescript
function ForgeIconMapMarkerCluster(properties: Readonly<IconMapMarkerClusterProperties>): MpElement;
```

Eine gruppierte Kartenmarkierung für Sehenswürdigkeiten mit hoher Dichte.

#### Parameter

| Name          | Geben Sie                                        | ein Beschreibung |
| ------------- | ------------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMapMarkerClusterProperties> |                  |

### IconMapMarkerClusterProperties

**Art:** Schnittstelle

```typescript
export interface IconMapMarkerClusterProperties
```

Keine Beschreibung angegeben.

## `src/components/media/capture/forge-icon-camera/forge-icon-camera`

### ForgeIconCamera

**Art:** Funktion

```typescript
function ForgeIconCamera(properties: Readonly<IconCameraProperties>): MpElement;
```

`ForgeIconCamera` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-camera.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCameraProperties> |                  |

### IconCameraProperties

**Art:** Schnittstelle

```typescript
export interface IconCameraProperties
```

Keine Beschreibung angegeben.

## `src/components/media/capture/forge-icon-image/forge-icon-image`

### ForgeIconImage

**Art:** Funktion

```typescript
function ForgeIconImage(properties: Readonly<IconImageProperties>): MpElement;
```

`ForgeIconImage` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-image.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconImageProperties> |                  |

### IconImageProperties

**Art:** Schnittstelle

```typescript
export interface IconImageProperties
```

Keine Beschreibung angegeben.

## `src/components/media/playback/forge-icon-pause/forge-icon-pause`

### ForgeIconPause

**Art:** Funktion

```typescript
function ForgeIconPause(properties: Readonly<IconPauseProperties>): MpElement;
```

`ForgeIconPause` – ein gefülltes Pausenzeichen (❙❙), das einmal im neutralen JSX erstellt wurde
Dialekt.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-pause.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPauseProperties> |                  |

### IconPauseProperties

**Art:** Schnittstelle

```typescript
export interface IconPauseProperties
```

Keine Beschreibung angegeben.

## `src/components/media/playback/forge-icon-play/forge-icon-play`

### ForgeIconPlay

**Art:** Funktion

```typescript
function ForgeIconPlay(properties: Readonly<IconPlayProperties>): MpElement;
```

`ForgeIconPlay` – ein gefülltes Spieldreieck (▶), das einmal im neutralen JSX erstellt wurde
Dialekt.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-play.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPlayProperties> |                  |

### IconPlayProperties

**Art:** Schnittstelle

```typescript
export interface IconPlayProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-arrow/forge-icon-arrow`

### ForgeIconArrow

**Art:** Funktion

```typescript
function ForgeIconArrow(properties: Readonly<IconArrowProperties>): MpElement;
```

`ForgeIconArrow` – ein Richtungspfeil, der einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-arrow.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconArrowProperties> |                  |

### IconArrowDirection

**Art:** Typ

```typescript
export type IconArrowDirection = 'up' | 'right' | 'down' | 'left';
```

Die Richtung, in die der Pfeil zeigt.

### IconArrowProperties

**Art:** Schnittstelle

```typescript
export interface IconArrowProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-chevron/forge-icon-chevron`

### ForgeIconChevron

**Art:** Funktion

```typescript
function ForgeIconChevron(properties: Readonly<IconChevronProperties>): MpElement;
```

`ForgeIconChevron` – ein Richtungspfeil, der einmal im neutralen JSX-Dialekt verfasst wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-chevron.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconChevronProperties> |                  |

### IconChevronProperties

**Art:** Schnittstelle

```typescript
export interface IconChevronProperties
```

Keine Beschreibung angegeben.

### IconDirection

**Art:** Typ

```typescript
export type IconDirection = 'up' | 'right' | 'down' | 'left';
```

Die Richtung, in die der Chevron zeigt.

## `src/components/navigation/controls/forge-icon-chevrons/forge-icon-chevrons`

### ForgeIconChevrons

**Art:** Funktion

```typescript
function ForgeIconChevrons(properties: Readonly<IconChevronsProperties>): MpElement;
```

`ForgeIconChevrons` – ein direktionaler Doppel-Chevron („“), der einmal im Jahr erstellt wurde
Neutraler JSX-Dialekt, der normalerweise für die Steuerung „Sprung zum Ersten“ und „Sprung zum Letzten“ verwendet wird.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-chevrons.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconChevronsProperties> |                  |

### IconChevronsRichtung

**Art:** Typ

```typescript
export type IconChevronsDirection = 'up' | 'right' | 'down' | 'left';
```

Die Richtung, in die der Doppelwinkel zeigt.

### IconChevronsProperties

**Art:** Schnittstelle

```typescript
export interface IconChevronsProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-close/forge-icon-close`

### ForgeIconClose

**Art:** Funktion

```typescript
function ForgeIconClose(properties: Readonly<IconCloseProperties>): MpElement;
```

`ForgeIconClose` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-close.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCloseProperties> |                  |

### IconCloseProperties

**Art:** Schnittstelle

```typescript
export interface IconCloseProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-home/forge-icon-home`

### ForgeIconHome

**Art:** Funktion

```typescript
function ForgeIconHome(properties: Readonly<IconHomeProperties>): MpElement;
```

`ForgeIconHome` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-home.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHomeProperties> |                  |

### IconHomeProperties

**Art:** Schnittstelle

```typescript
export interface IconHomeProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-join/forge-icon-join`

### ForgeIconJoin

**Art:** Funktion

```typescript
function ForgeIconJoin(properties: Readonly<IconJoinProperties>): MpElement;
```

`ForgeIconJoin` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-join.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconJoinProperties> |                  |

### IconJoinProperties

**Art:** Schnittstelle

```typescript
export interface IconJoinProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-menu/forge-icon-menu`

### ForgeIconMenu

**Art:** Funktion

```typescript
function ForgeIconMenu(properties: Readonly<IconMenuProperties>): MpElement;
```

`ForgeIconMenu` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-menu.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMenuProperties> |                  |

### IconMenuProperties

**Art:** Schnittstelle

```typescript
export interface IconMenuProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-minus/forge-icon-minus`

### ForgeIconMinus

**Art:** Funktion

```typescript
function ForgeIconMinus(properties: Readonly<IconMinusProperties>): MpElement;
```

`ForgeIconMinus` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-minus.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconMinusProperties> |                  |

### IconMinusProperties

**Art:** Schnittstelle

```typescript
export interface IconMinusProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-plus/forge-icon-plus`

### ForgeIconPlus

**Art:** Funktion

```typescript
function ForgeIconPlus(properties: Readonly<IconPlusProperties>): MpElement;
```

`ForgeIconPlus` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-plus.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPlusProperties> |                  |

### IconPlusProperties

**Art:** Schnittstelle

```typescript
export interface IconPlusProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-refresh/forge-icon-refresh`

### ForgeIconRefresh

**Art:** Funktion

```typescript
function ForgeIconRefresh(properties: Readonly<IconRefreshProperties>): MpElement;
```

`ForgeIconRefresh` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-refresh.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconRefreshProperties> |                  |

### IconRefreshProperties

**Art:** Schnittstelle

```typescript
export interface IconRefreshProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/controls/forge-icon-split/forge-icon-split`

### ForgeIconSplit

**Art:** Funktion

```typescript
function ForgeIconSplit(properties: Readonly<IconSplitProperties>): MpElement;
```

`ForgeIconSplit` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-split.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSplitProperties> |                  |

### IconSplitProperties

**Art:** Schnittstelle

```typescript
export interface IconSplitProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/links/forge-icon-external-link/forge-icon-external-link`

### ForgeIconExternalLink

**Art:** Funktion

```typescript
function ForgeIconExternalLink(properties: Readonly<IconExternalLinkProperties>): MpElement;
```

`ForgeIconExternalLink` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-external-link.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconExternalLinkProperties> |                  |

### IconExternalLinkProperties

**Art:** Schnittstelle

```typescript
export interface IconExternalLinkProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/links/forge-icon-link/forge-icon-link`

### ForgeIconLink

**Art:** Funktion

```typescript
function ForgeIconLink(properties: Readonly<IconLinkProperties>): MpElement;
```

`ForgeIconLink` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-link.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLinkProperties> |                  |

### IconLinkProperties

**Art:** Schnittstelle

```typescript
export interface IconLinkProperties
```

Keine Beschreibung angegeben.

## `src/components/navigation/search/forge-icon-search/forge-icon-search`

### ForgeIconSearch

**Art:** Funktion

```typescript
function ForgeIconSearch(properties: Readonly<IconSearchProperties>): MpElement;
```

`ForgeIconSearch` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-search.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSearchProperties> |                  |

### IconSearchProperties

**Art:** Schnittstelle

```typescript
export interface IconSearchProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-cloud/forge-icon-cloud`

### ForgeIconCloud

**Art:** Funktion

```typescript
function ForgeIconCloud(properties: Readonly<IconCloudProperties>): MpElement;
```

`ForgeIconCloud` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-cloud.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCloudProperties> |                  |

### IconCloudProperties

**Art:** Schnittstelle

```typescript
export interface IconCloudProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-debug/forge-icon-debug`

### ForgeIconDebug

**Art:** Funktion

```typescript
function ForgeIconDebug(properties: Readonly<IconDebugProperties>): MpElement;
```

`ForgeIconDebug` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-debug.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconDebugProperties> |                  |

### IconDebugProperties

**Art:** Schnittstelle

```typescript
export interface IconDebugProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-heart/forge-icon-heart`

### ForgeIconHeart

**Art:** Funktion

```typescript
function ForgeIconHeart(properties: Readonly<IconHeartProperties>): MpElement;
```

`ForgeIconHeart` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heart.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeartProperties> |                  |

### IconHeartProperties

**Art:** Schnittstelle

```typescript
export interface IconHeartProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-lightning/forge-icon-lightning`

### ForgeIconLightning

**Art:** Funktion

```typescript
function ForgeIconLightning(properties: Readonly<IconLightningProperties>): MpElement;
```

`ForgeIconLightning` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-lightning.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLightningProperties> |                  |

### IconLightningProperties

**Art:** Schnittstelle

```typescript
export interface IconLightningProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-puzzle/forge-icon-puzzle`

### ForgeIconPuzzle

**Art:** Funktion

```typescript
function ForgeIconPuzzle(properties: Readonly<IconPuzzleProperties>): MpElement;
```

`ForgeIconPuzzle` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-puzzle.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconPuzzleProperties> |                  |

### IconPuzzleProperties

**Art:** Schnittstelle

```typescript
export interface IconPuzzleProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-qr-code/forge-icon-qr-code`

### ForgeIconQrCode

**Art:** Funktion

```typescript
function ForgeIconQrCode(properties: Readonly<IconQrCodeProperties>): MpElement;
```

`ForgeIconQrCode` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-qr-code.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconQrCodeProperties> |                  |

### IconQrCodeProperties

**Art:** Schnittstelle

```typescript
export interface IconQrCodeProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-settings/forge-icon-settings`

### ForgeIconSettings

**Art:** Funktion

```typescript
function ForgeIconSettings(properties: Readonly<IconSettingsProperties>): MpElement;
```

`ForgeIconSettings` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-settings.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSettingsProperties> |                  |

### IconSettingsProperties

**Art:** Schnittstelle

```typescript
export interface IconSettingsProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-star/forge-icon-star`

### ForgeIconStar

**Art:** Funktion

```typescript
function ForgeIconStar(properties: Readonly<IconStarProperties>): MpElement;
```

`ForgeIconStar` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-star.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconStarProperties> |                  |

### IconStarProperties

**Art:** Schnittstelle

```typescript
export interface IconStarProperties
```

Keine Beschreibung angegeben.

## `src/components/objects/system/forge-icon-wrench/forge-icon-wrench`

### ForgeIconWrench

**Art:** Funktion

```typescript
function ForgeIconWrench(properties: Readonly<IconWrenchProperties>): MpElement;
```

`ForgeIconWrench` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-wrench.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconWrenchProperties> |                  |

### IconWrenchProperties

**Art:** Schnittstelle

```typescript
export interface IconWrenchProperties
```

Keine Beschreibung angegeben.

## `src/components/routing/directions/forge-icon-route/forge-icon-route`

### ForgeIconRoute

**Art:** Funktion

```typescript
function ForgeIconRoute(properties: Readonly<IconRouteProperties>): MpElement;
```

Eine Routenlinie mit deutlichen Start- und Zielmarkierungen.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconRouteProperties> |                  |

### IconRouteProperties

**Art:** Schnittstelle

```typescript
export interface IconRouteProperties
```

Keine Beschreibung angegeben.

## `src/components/routing/directions/forge-icon-waypoint/forge-icon-waypoint`

### ForgeIconWaypoint

**Art:** Funktion

```typescript
function ForgeIconWaypoint(properties: Readonly<IconWaypointProperties>): MpElement;
```

Eine wiederverwendbare Wegpunktmarkierung für Routen und Kartenüberlagerungen.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconWaypointProperties> |                  |

### IconWaypointProperties

**Art:** Schnittstelle

```typescript
export interface IconWaypointProperties
```

Keine Beschreibung angegeben.

## `src/components/security/access/forge-icon-lock-open/forge-icon-lock-open`

### ForgeIconLockOpen

**Art:** Funktion

```typescript
function ForgeIconLockOpen(properties: Readonly<IconLockOpenProperties>): MpElement;
```

`ForgeIconLockOpen` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-lock-open.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLockOpenProperties> |                  |

### IconLockOpenProperties

**Art:** Schnittstelle

```typescript
export interface IconLockOpenProperties
```

Keine Beschreibung angegeben.

## `src/components/security/access/forge-icon-lock/forge-icon-lock`

### ForgeIconLock

**Art:** Funktion

```typescript
function ForgeIconLock(properties: Readonly<IconLockProperties>): MpElement;
```

`ForgeIconLock` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-lock.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconLockProperties> |                  |

### IconLockProperties

**Art:** Schnittstelle

```typescript
export interface IconLockProperties
```

Keine Beschreibung angegeben.

## `src/components/security/access/forge-icon-user/forge-icon-user`

### ForgeIconUser

**Art:** Funktion

```typescript
function ForgeIconUser(properties: Readonly<IconUserProperties>): MpElement;
```

`ForgeIconUser` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-user.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconUserProperties> |                  |

### IconUserProperties

**Art:** Schnittstelle

```typescript
export interface IconUserProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-alert-critical/forge-icon-alert-critical`

### ForgeIconAlertCritical

**Art:** Funktion

```typescript
function ForgeIconAlertCritical(properties: Readonly<IconAlertCriticalProperties>): MpElement;
```

`ForgeIconAlertCritical` – das kritische Alarmsymbol (ein „!“ in einem Achteck,
das an ein Stoppschild erinnert) wurde einmal im neutralen JSX-Dialekt verfasst.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-alert-critical.module.scss`.

#### Parameter

| Name          | Geben Sie                                     | ein Beschreibung |
| ------------- | --------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlertCriticalProperties> |                  |

### IconAlertCriticalProperties

**Art:** Schnittstelle

```typescript
export interface IconAlertCriticalProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-alert-info/forge-icon-alert-info`

### ForgeIconAlertInfo

**Art:** Funktion

```typescript
function ForgeIconAlertInfo(properties: Readonly<IconAlertInfoProperties>): MpElement;
```

`ForgeIconAlertInfo` – das Informationswarnungssymbol (ein „i“ in einem Kreis)
einmal im neutralen JSX-Dialekt verfasst.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-alert-info.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlertInfoProperties> |                  |

### IconAlertInfoProperties

**Art:** Schnittstelle

```typescript
export interface IconAlertInfoProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-alert-neutral/forge-icon-alert-neutral`

### ForgeIconAlertNeutral

**Art:** Funktion

```typescript
function ForgeIconAlertNeutral(properties: Readonly<IconAlertNeutralProperties>): MpElement;
```

`ForgeIconAlertNeutral` – das neutrale Alarmzeichen (ein „–“ innerhalb eines Kreises)
einmal im neutralen JSX-Dialekt verfasst, für Informationsnachrichten mit Nr
positive oder negative Konnotation.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-alert-neutral.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlertNeutralProperties> |                  |

### IconAlertNeutralProperties

**Art:** Schnittstelle

```typescript
export interface IconAlertNeutralProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-alert-warning/forge-icon-alert-warning`

### ForgeIconAlertWarning

**Art:** Funktion

```typescript
function ForgeIconAlertWarning(properties: Readonly<IconAlertWarningProperties>): MpElement;
```

`ForgeIconAlertWarning` – das Warnsymbol (ein „!“ in einem Dreieck)
einmal im neutralen JSX-Dialekt verfasst.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-alert-warning.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlertWarningProperties> |                  |

### IconAlertWarningProperties

**Art:** Schnittstelle

```typescript
export interface IconAlertWarningProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-alert/forge-icon-alert`

### ForgeIconAlert

**Art:** Funktion

```typescript
function ForgeIconAlert(properties: Readonly<IconAlertProperties>): MpElement;
```

`ForgeIconAlert` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-alert.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlertProperties> |                  |

### IconAlertProperties

**Art:** Schnittstelle

```typescript
export interface IconAlertProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-check/forge-icon-check`

### ForgeIconCheck

**Art:** Funktion

```typescript
function ForgeIconCheck(properties: Readonly<IconCheckProperties>): MpElement;
```

`ForgeIconCheck` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-check.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCheckProperties> |                  |

### IconCheckProperties

**Art:** Schnittstelle

```typescript
export interface IconCheckProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-error/forge-icon-error`

### ForgeIconError

**Art:** Funktion

```typescript
function ForgeIconError(properties: Readonly<IconErrorProperties>): MpElement;
```

`ForgeIconError` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-error.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconErrorProperties> |                  |

### IconErrorProperties

**Art:** Schnittstelle

```typescript
export interface IconErrorProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-info/forge-icon-info`

### ForgeIconInfo

**Art:** Funktion

```typescript
function ForgeIconInfo(properties: Readonly<IconInfoProperties>): MpElement;
```

`ForgeIconInfo` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-info.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconInfoProperties> |                  |

### IconInfoProperties

**Art:** Schnittstelle

```typescript
export interface IconInfoProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-notice/forge-icon-notice`

### ForgeIconNotice

**Art:** Funktion

```typescript
function ForgeIconNotice(properties: Readonly<IconNoticeProperties>): MpElement;
```

`ForgeIconNotice` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-notice.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconNoticeProperties> |                  |

### IconNoticeProperties

**Art:** Schnittstelle

```typescript
export interface IconNoticeProperties
```

Keine Beschreibung angegeben.

## `src/components/status/feedback/forge-icon-warning/forge-icon-warning`

### ForgeIconWarning

**Art:** Funktion

```typescript
function ForgeIconWarning(properties: Readonly<IconWarningProperties>): MpElement;
```

`ForgeIconWarning` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-warning.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconWarningProperties> |                  |

### IconWarningProperties

**Art:** Schnittstelle

```typescript
export interface IconWarningProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-align-center/forge-icon-align-center`

### ForgeIconAlignCenter

**Art:** Funktion

```typescript
function ForgeIconAlignCenter(properties: Readonly<IconAlignCenterProperties>): MpElement;
```

`ForgeIconAlignCenter` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-align-center.module.scss`.

#### Parameter

| Name          | Geben Sie                                   | ein Beschreibung |
| ------------- | ------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlignCenterProperties> |                  |

### IconAlignCenterProperties

**Art:** Schnittstelle

```typescript
export interface IconAlignCenterProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-align-justify/forge-icon-align-justify`

### ForgeIconAlignJustify

**Art:** Funktion

```typescript
function ForgeIconAlignJustify(properties: Readonly<IconAlignJustifyProperties>): MpElement;
```

`ForgeIconAlignJustify` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-align-justify.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlignJustifyProperties> |                  |

### IconAlignJustifyProperties

**Art:** Schnittstelle

```typescript
export interface IconAlignJustifyProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-align-left/forge-icon-align-left`

### ForgeIconAlignLeft

**Art:** Funktion

```typescript
function ForgeIconAlignLeft(properties: Readonly<IconAlignLeftProperties>): MpElement;
```

`ForgeIconAlignLeft` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-align-left.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlignLeftProperties> |                  |

### IconAlignLeftProperties

**Art:** Schnittstelle

```typescript
export interface IconAlignLeftProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-align-right/forge-icon-align-right`

### ForgeIconAlignRight

**Art:** Funktion

```typescript
function ForgeIconAlignRight(properties: Readonly<IconAlignRightProperties>): MpElement;
```

`ForgeIconAlignRight` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-align-right.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconAlignRightProperties> |                  |

### IconAlignRightProperties

**Art:** Schnittstelle

```typescript
export interface IconAlignRightProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-blockquote/forge-icon-blockquote`

### ForgeIconBlockquote

**Art:** Funktion

```typescript
function ForgeIconBlockquote(properties: Readonly<IconBlockquoteProperties>): MpElement;
```

`ForgeIconBlockquote` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-blockquote.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconBlockquoteProperties> |                  |

### IconBlockquoteProperties

**Art:** Schnittstelle

```typescript
export interface IconBlockquoteProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-bold/forge-icon-bold`

### ForgeIconBold

**Art:** Funktion

```typescript
function ForgeIconBold(properties: Readonly<IconBoldProperties>): MpElement;
```

`ForgeIconBold` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-bold.module.scss`.

#### Parameter

| Name          | Geben Sie                            | ein Beschreibung |
| ------------- | ------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconBoldProperties> |                  |

### IconBoldProperties

**Art:** Schnittstelle

```typescript
export interface IconBoldProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-bullet-list/forge-icon-bullet-list`

### ForgeIconBulletList

**Art:** Funktion

```typescript
function ForgeIconBulletList(properties: Readonly<IconBulletListProperties>): MpElement;
```

`ForgeIconBulletList` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-bullet-list.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconBulletListProperties> |                  |

### IconBulletListProperties

**Art:** Schnittstelle

```typescript
export interface IconBulletListProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-code-block/forge-icon-code-block`

### ForgeIconCodeBlock

**Art:** Funktion

```typescript
function ForgeIconCodeBlock(properties: Readonly<IconCodeBlockProperties>): MpElement;
```

`ForgeIconCodeBlock` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-code-block.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCodeBlockProperties> |                  |

### IconCodeBlockProperties

**Art:** Schnittstelle

```typescript
export interface IconCodeBlockProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-code-inline/forge-icon-code-inline`

### ForgeIconCodeInline

**Art:** Funktion

```typescript
function ForgeIconCodeInline(properties: Readonly<IconCodeInlineProperties>): MpElement;
```

`ForgeIconCodeInline` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-code-inline.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCodeInlineProperties> |                  |

### IconCodeInlineProperties

**Art:** Schnittstelle

```typescript
export interface IconCodeInlineProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-five/forge-icon-heading-five`

### ForgeIconHeadingFive

**Art:** Funktion

```typescript
function ForgeIconHeadingFive(properties: Readonly<IconHeadingFiveProperties>): MpElement;
```

`ForgeIconHeadingFive` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-five.module.scss`.

#### Parameter

| Name          | Geben Sie                                   | ein Beschreibung |
| ------------- | ------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingFiveProperties> |                  |

### IconHeadingFiveProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingFiveProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-four/forge-icon-heading-four`

### ForgeIconHeadingFour

**Art:** Funktion

```typescript
function ForgeIconHeadingFour(properties: Readonly<IconHeadingFourProperties>): MpElement;
```

`ForgeIconHeadingFour` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-four.module.scss`.

#### Parameter

| Name          | Geben Sie                                   | ein Beschreibung |
| ------------- | ------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingFourProperties> |                  |

### IconHeadingFourProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingFourProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-one/forge-icon-heading-one`

### ForgeIconHeadingOne

**Art:** Funktion

```typescript
function ForgeIconHeadingOne(properties: Readonly<IconHeadingOneProperties>): MpElement;
```

`ForgeIconHeadingOne` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-one.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingOneProperties> |                  |

### IconHeadingOneProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingOneProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-six/forge-icon-heading-six`

### ForgeIconHeadingSix

**Art:** Funktion

```typescript
function ForgeIconHeadingSix(properties: Readonly<IconHeadingSixProperties>): MpElement;
```

`ForgeIconHeadingSix` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-six.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingSixProperties> |                  |

### IconHeadingSixProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingSixProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-three/forge-icon-heading-three`

### ForgeIconHeadingThree

**Art:** Funktion

```typescript
function ForgeIconHeadingThree(properties: Readonly<IconHeadingThreeProperties>): MpElement;
```

`ForgeIconHeadingThree` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-three.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingThreeProperties> |                  |

### IconHeadingThreeProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingThreeProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading-two/forge-icon-heading-two`

### ForgeIconHeadingTwo

**Art:** Funktion

```typescript
function ForgeIconHeadingTwo(properties: Readonly<IconHeadingTwoProperties>): MpElement;
```

`ForgeIconHeadingTwo` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading-two.module.scss`.

#### Parameter

| Name          | Geben Sie                                  | ein Beschreibung |
| ------------- | ------------------------------------------ | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingTwoProperties> |                  |

### IconHeadingTwoProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingTwoProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-heading/forge-icon-heading`

### ForgeIconHeading

**Art:** Funktion

```typescript
function ForgeIconHeading(properties: Readonly<IconHeadingProperties>): MpElement;
```

`ForgeIconHeading` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-heading.module.scss`.

#### Parameter

| Name          | Geben Sie                               | ein Beschreibung |
| ------------- | --------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconHeadingProperties> |                  |

### IconHeadingProperties

**Art:** Schnittstelle

```typescript
export interface IconHeadingProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-italic/forge-icon-italic`

### ForgeIconItalic

**Art:** Funktion

```typescript
function ForgeIconItalic(properties: Readonly<IconItalicProperties>): MpElement;
```

`ForgeIconItalic` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-italic.module.scss`.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconItalicProperties> |                  |

### IconItalicProperties

**Art:** Schnittstelle

```typescript
export interface IconItalicProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-numbered-list/forge-icon-numbered-list`

### ForgeIconNumberedList

**Art:** Funktion

```typescript
function ForgeIconNumberedList(properties: Readonly<IconNumberedListProperties>): MpElement;
```

`ForgeIconNumberedList` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-numbered-list.module.scss`.

#### Parameter

| Name          | Geben Sie                                    | ein Beschreibung |
| ------------- | -------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconNumberedListProperties> |                  |

### IconNumberedListProperties

**Art:** Schnittstelle

```typescript
export interface IconNumberedListProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-strikethrough/forge-icon-strikethrough`

### ForgeIconStrikethrough

**Art:** Funktion

```typescript
function ForgeIconStrikethrough(properties: Readonly<IconStrikethroughProperties>): MpElement;
```

`ForgeIconStrikethrough` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-strikethrough.module.scss`.

#### Parameter

| Name          | Geben Sie                                     | ein Beschreibung |
| ------------- | --------------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconStrikethroughProperties> |                  |

### IconStrikethroughProperties

**Art:** Schnittstelle

```typescript
export interface IconStrikethroughProperties
```

Keine Beschreibung angegeben.

## `src/components/text/formatting/forge-icon-underline/forge-icon-underline`

### ForgeIconUnderline

**Art:** Funktion

```typescript
function ForgeIconUnderline(properties: Readonly<IconUnderlineProperties>): MpElement;
```

`ForgeIconUnderline` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-underline.module.scss`.

#### Parameter

| Name          | Geben Sie                                 | ein Beschreibung |
| ------------- | ----------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconUnderlineProperties> |                  |

### IconUnderlineProperties

**Art:** Schnittstelle

```typescript
export interface IconUnderlineProperties
```

Keine Beschreibung angegeben.

## `src/components/time/calendar/forge-icon-calendar/forge-icon-calendar`

### ForgeIconCalendar

**Art:** Funktion

```typescript
function ForgeIconCalendar(properties: Readonly<IconCalendarProperties>): MpElement;
```

`ForgeIconCalendar` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-calendar.module.scss`.

#### Parameter

| Name          | Geben Sie                                | ein Beschreibung |
| ------------- | ---------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconCalendarProperties> |                  |

### IconCalendarProperties

**Art:** Schnittstelle

```typescript
export interface IconCalendarProperties
```

Keine Beschreibung angegeben.

## `src/components/time/calendar/forge-icon-clock/forge-icon-clock`

### ForgeIconClock

**Art:** Funktion

```typescript
function ForgeIconClock(properties: Readonly<IconClockProperties>): MpElement;
```

`ForgeIconClock` – ein Symbol, das einmal im neutralen JSX-Dialekt erstellt wurde.

Es lässt sich über das vite-plugin-forge sauber zu Vue 3 und React kompilieren
Zweistufiger Compiler. Das Symbol versendet sein eigenes `@layer mp.icons` CSS über das
Co-lokalisiertes CSS-Modul `forge-icon-clock.module.scss`.

#### Parameter

| Name          | Geben Sie                             | ein Beschreibung |
| ------------- | ------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconClockProperties> |                  |

### IconClockProperties

**Art:** Schnittstelle

```typescript
export interface IconClockProperties
```

Keine Beschreibung angegeben.

## `src/sprite/compositions`

### ICON_COMPOSITIONS

**Art:** konstant

```typescript
export const ICON_COMPOSITIONS: readonly IconSymbolDefinition[];
```

Keine Beschreibung angegeben.

### validierenKompositionen

**Art:** Funktion

```typescript
function validateCompositions(compositions: readonly IconSymbolDefinition[], availableIds: ReadonlySet<string>): void;
```

Keine Beschreibung angegeben.

#### Parameter

| Name           | Geben Sie                               | ein Beschreibung |
| -------------- | --------------------------------------- | ---------------- |
| Kompositionen  | schreibgeschützt IconSymbolDefinition[] |                  |
| verfügbare IDs | ReadonlySet<string>                     |                  |

## `src/sprite/country-flags`

### SUPPORTED_COUNTRY_CODES

**Art:** konstant

```typescript
export const SUPPORTED_COUNTRY_CODES;
```

Ländercodes mit kanonischen Flaggensymbolen im veröffentlichten Sprite.

## `src/sprite/definitions`

### COUNTRY_FLAG_SYMBOL_IDS

**Art:** konstant

```typescript
export const COUNTRY_FLAG_SYMBOL_IDS;
```

Keine Beschreibung angegeben.

### ICON_SYMBOL_DEFINITIONS

**Art:** konstant

```typescript
export const ICON_SYMBOL_DEFINITIONS: readonly IconSymbolDefinition[];
```

Keine Beschreibung angegeben.

## `src/sprite/icon-use`

### ForgeIconUse

**Art:** Funktion

```typescript
function ForgeIconUse(properties: Readonly<IconUseProperties>): MpElement;
```

Gemeinsam zugänglicher Wrapper, der auf die kanonische Sprite-Geometrie verweist.

#### Parameter

| Name          | Geben Sie                           | ein Beschreibung |
| ------------- | ----------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconUseProperties> |                  |

### IconUseProperties

**Art:** Schnittstelle

```typescript
export interface IconUseProperties
```

Keine Beschreibung angegeben.

## `src/sprite/provider`

### IconSpriteContext

**Art:** konstant

```typescript
export const IconSpriteContext;
```

Keine Beschreibung angegeben.

### IconSpriteProvider

**Art:** Funktion

```typescript
function IconSpriteProvider(properties: Readonly<IconSpriteProperties>): MpElement;
```

Mounten Sie einen Inline-Sprite-Host für einen Teilbaum von Symbolkomponenten.

#### Parameter

| Name          | Geben Sie                              | ein Beschreibung |
| ------------- | -------------------------------------- | ---------------- |
| Eigenschaften | Schreibgeschützt<IconSpriteProperties> |                  |

### useIconHref

**Art:** Funktion

```typescript
function useIconHref(symbolId: string): string;
```

Lösen Sie eine lokale oder externe Symbol-URL für einen Symbol-Wrapper auf.

#### Parameter

| Name     | Geben Sie    | ein Beschreibung |
| -------- | ------------ | ---------------- |
| symbolId | Zeichenfolge |                  |

## `src/sprite/serialize`

### serializeNode

**Art:** Funktion

```typescript
function serializeNode(node: IconSvgNode): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name | Geben Sie   | ein Beschreibung |
| ---- | ----------- | ---------------- |
| node | IconSvgNode |                  |

### serializeSprite

**Art:** Funktion

```typescript
function serializeSprite(definitions: readonly IconSymbolDefinition[]): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name         | Geben Sie                               | ein Beschreibung |
| ------------ | --------------------------------------- | ---------------- |
| Definitionen | schreibgeschützt IconSymbolDefinition[] |                  |

### serializeSymbol

**Art:** Funktion

```typescript
function serializeSymbol(definition: IconSymbolDefinition): string;
```

Keine Beschreibung angegeben.

#### Parameter

| Name       | Geben Sie            | ein Beschreibung |
| ---------- | -------------------- | ---------------- |
| Definition | IconSymbolDefinition |                  |

## `src/sprite/types`

### IconSpriteContextValue

**Art:** Schnittstelle

```typescript
export interface IconSpriteContextValue
```

Keine Beschreibung angegeben.

### IconSpriteProperties

**Art:** Schnittstelle

```typescript
export interface IconSpriteProperties
```

Keine Beschreibung angegeben.

### IconSvgNode

**Art:** Schnittstelle

```typescript
export interface IconSvgNode
```

Keine Beschreibung angegeben.

### IconSymbolDefinition

**Art:** Schnittstelle

```typescript
export interface IconSymbolDefinition
```

Keine Beschreibung angegeben.

### IconSymbolUse

**Art:** Schnittstelle

```typescript
export interface IconSymbolUse
```

Keine Beschreibung angegeben.
