# @mission-platform/theme

Framework-neutral theme UI and runtime contracts for the Mission Platform.

The package exports `ForgeThemeToggle`, `ForgeThemeProvider`, and `ForgeThemeComposer`, plus the observable theme and
token-composer store APIs. Use the package's bare import with the appropriate `mp:*` framework condition.
