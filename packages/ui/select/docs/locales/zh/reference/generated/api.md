# @mission-platform/select API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/ui/select/docs/reference/generated/api.md: [packages/ui/select/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/select` 中的公共源声明生成。

## `src/components/atoms/forge-tag/forge-tag`

### 伪造标签

**种类：**功能

```typescript
function ForgeTag(properties: Readonly<TagProperties>): MpElement;
```

`ForgeTag` — 一个紧凑的圆形标签，用中性 JSX 方言创作一次
并由 `@mission-platform/vite-plugin-forge` 直接编译为 React 或 Vue。

它渲染其 `label` （通过组合中性 {@link ForgeTypography}）
色调/大小，以及 — 当 `removable` 而不是 `disabled` 时 — 删除按钮
触发 `onRemove` 回调。它通过同位 CSS 拥有自己的样式
模块 `forge-tag.module.scss`，与框架中立组装
{@link classNames} 帮助器。

删除按钮呈现一次写入 `@mission-platform/icons`
`ForgeIconClose`（本身编译为 React/Vue）并触发跨框架
callback-prop `onRemove`（Vue `remove` 发出替代品）。

#### 参数

| 名称 | 类型                | 描述 |
| ---- | ------------------- | ---- |
| 属性 | 只读<TagProperties> |      |

### 标签属性

**种类：**接口

```typescript
export interface TagProperties
```

没有提供描述。

### 标签大小

**种类：**类型

```typescript
export type TagSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

规范 2xs → 2xl 尺寸比例。

### 标签变体

**种类：**类型

```typescript
export type TagVariant =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

标签的语气。

## `src/components/molecules/forge-language-switcher/forge-language-switcher`

### Forge语言切换器

**种类：**功能

```typescript
function ForgeLanguageSwitcher(properties: Readonly<ForgeLanguageSwitcherProperties>): MpElement;
```

没有提供描述。

#### 参数

| 名称 | 类型                                  | 描述 |
| ---- | ------------------------------------- | ---- |
| 属性 | 只读<ForgeLanguageSwitcherProperties> |      |

### Forge语言切换选项

**种类：**接口

```typescript
export interface ForgeLanguageSwitcherOption
```

没有提供描述。

### ForgeLanguageSwitcher 属性

**种类：**接口

```typescript
export interface ForgeLanguageSwitcherProperties
```

没有提供描述。

## `src/components/molecules/forge-multiselect/forge-multiselect`

### 锻造多选

**种类：**功能

```typescript
function ForgeMultiselect(properties: Readonly<MultiselectProperties>): MpElement;
```

`ForgeMultiselect` — 带有搜索字段的基于标签的多选组合框，
用中性 JSX 方言编写一次并直接编译为 React 或
Vue 由 `@mission-platform/vite-plugin-forge` 提供。

选定的值呈现为可移动的{@link ForgeTag}芯片；内联搜索
输入过滤列表框中显示的其余选项。选择是
通过已建立的 `modelValue`（`(string | number)[]`）进行控制 +
`onUpdateModelValue`/`onChange` 回调属性约定，以及直观的
隐藏的本机 `<select multiple>` 保持自动填充/本机提交工作。
它通过并置的 CSS 模块拥有自己的样式
`forge-multiselect.module.scss`。

过滤后的列表框通过一次写入 **`ForgeDropdown`** 呈现
（本身编译为 React/Vue），通过中性命名槽 **passing** 组成
语法：标签/搜索组合框被传递到下拉列表的 `trigger` 插槽
(`slot="trigger"`) 并且 `<ul role="listbox">` 成为其默认插槽，因此
该面板通过 `<Teleport>` 传送并通过 CSS Anchor 锚定
定位而不是就地列表。内部 `useState` 打开标志是
通过 `onUpdateOpen` 回调与下拉列表保持同步。

原始 Vue SFC 的其他替换：搜索查询变为
内部 `useState` 字符串； `useId` 可组合项映射到
框架原生 `useId` 钩子； V 形是一次性写入 `@mission-platform/icons`
`ForgeIconChevron`（通过其 `direction` 属性旋转，本身编译为
React/Vue); `useI18n` 字符串变为纯文本；并且 `v-model` + 发出
成为回调道具。

#### 参数

| 名称 | 类型                        | 描述 |
| ---- | --------------------------- | ---- |
| 属性 | 只读<MultiselectProperties> |      |

### 多选选项

**种类：**接口

```typescript
export interface MultiselectOption
```

单个可选选项。

### 多选属性

**种类：**接口

```typescript
export interface MultiselectProperties
```

没有提供描述。

### 多选尺寸

**种类：**类型

```typescript
export type MultiselectSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

规范控制尺寸比例。

## `src/components/molecules/forge-select/forge-select`

### 锻造选择

**种类：**功能

```typescript
function ForgeSelect(properties: Readonly<SelectProperties>): MpElement;
```

`ForgeSelect` — 在中性 JSX 中创作一次的自定义组合框/选择
方言并直接编译为 React 或 Vue
`@mission-platform/vite-plugin-forge`。

默认情况下（`searchable`，默认值）触发器是一个过滤的文本字段
用户键入的选项 - 镜像 {@link ForgeMultiselect} - 所以一个值
可以通过搜索而不是滚动找到；设置 `searchable={false}` 为
普通按钮触发器。无论哪种方式，列表框都由视觉上隐藏的支持
本机 `<select>` 因此浏览器自动填充和本机表单提交继续工作。
使用已建立的 `modelValue` + 控制选择
`onUpdateModelValue`/`onChange` 回调属性约定。它有它的风格
通过位于同一位置的 CSS 模块 `forge-select.module.scss`。

浮动列表框通过一次写入 **`ForgeDropdown`** 进行渲染
（本身编译为 React/Vue），通过中性命名槽 **passing** 组成
语法：组合框包装器被传递到下拉列表的 `trigger` 插槽
(`slot="trigger"`) 并且 `<ul role="listbox">` 成为其默认插槽，因此
该面板通过 `<Teleport>` 传送并通过 CSS Anchor 锚定
定位而不是就地绝对定位列表。内部
`useState` 打开标志通过其下拉列表保持同步
`onUpdateOpen` 回调。

原始 Vue SFC 的其他替换：`useId` 可组合项映射到
框架原生 `useId` 挂钩； V 形是一次写入
`@mission-platform/icons` `ForgeIconChevron`（通过其 `direction` 属性旋转，
本身编译为 React/Vue)； `useI18n` 字符串变为纯文本；和
`v-model` + 发出成为回调道具。 `start`/`end` 命名插槽是
保留为中性命名槽。

#### 参数

| 名称 | 类型                   | 描述 |
| ---- | ---------------------- | ---- |
| 属性 | 只读<SelectProperties> |      |

### 选择选项

**种类：**接口

```typescript
export interface SelectOption
```

单个可选选项。

### 选择属性

**种类：**接口

```typescript
export interface SelectProperties
```

没有提供描述。

### 选择尺寸

**种类：**类型

```typescript
export type SelectSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

规范控制尺寸比例。
