import { defineLibraryConfig } from '@mission-platform/vite-config';

export default defineLibraryConfig({
  rootDir: import.meta.dirname,
  name: 'MissionPlatformEmailComponents',
  fileName: 'email-components',
});
