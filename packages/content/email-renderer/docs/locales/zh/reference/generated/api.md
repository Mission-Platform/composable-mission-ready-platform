# @mission-platform/email-renderer API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/email-renderer/docs/reference/generated/api.md: [packages/email-renderer/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/email-renderer` 中的公共源声明生成。

## `src/adapters/react`

### 渲染到电子邮件反应

**种类：**功能

```typescript
function renderToEmailReact(node: EmailNode): ReactElement;
```

将共享 Forge 树渲染为 React 元素以供浏览器预览。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

### toEmailReactComponent

**种类：**功能

```typescript
function toEmailReactComponent(component: MpComponent<P>, name?: string): FunctionComponent<P>;
```

将 Forge 电子邮件组件包装为 React 函数组件。

#### 参数

| 名称 | 类型           | 描述 |
| ---- | -------------- | ---- |
| 组件 | MpComponent<P> |      |
| 名称 | 字符串         |      |

## `src/adapters/solid`

### 渲染到电子邮件实体

**种类：**功能

```typescript
function renderToEmailSolid(node: EmailNode): JSX.Element;
```

将共享 Forge 树渲染为 Solid JSX 以供浏览器预览。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

### toEmailSolidComponent

**种类：**功能

```typescript
function toEmailSolidComponent(component: Component<P>): Component<P>;
```

将 Forge 组件包装为 Solid 组件。

#### 参数

| 名称 | 类型    | 描述 |
| ---- | ------- | ---- |
| 组件 | 组件<P> |      |

## `src/adapters/svelte`

### 渲染到电子邮件Svelte

**种类：**功能

```typescript
function renderToEmailSvelte(node: EmailNode): Snippet;
```

将共享 Forge 树转换为 `{@render ...}` 的 Svelte 5 原始片段。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

## `src/adapters/vue`

### 渲染到电子邮件Vue

**种类：**功能

```typescript
function renderToEmailVue(node: EmailNode): VNode;
```

将共享 Forge 树渲染为 Vue VNode 以供浏览器预览。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

### toEmailVue组件

**种类：**功能

```typescript
function toEmailVueComponent(component: MpComponent<P>, name?: string): FunctionalComponent<P>;
```

将 Forge 电子邮件组件包装为 Vue 功能组件。

#### 参数

| 名称 | 类型           | 描述 |
| ---- | -------------- | ---- |
| 组件 | MpComponent<P> |      |
| 名称 | 字符串         |      |

## `src/adapters/web-components`

### 渲染到电子邮件Web组件

**种类：**功能

```typescript
function renderToEmailWebComponent(node: EmailNode): TemplateResult;
```

为 Web 组件主机创建本机 Forge 模板结果。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

## `src/markdown/render-markdown`

### 渲染Markdown

**种类：**功能

```typescript
function renderMarkdown(markdown: string, options: MarkdownRenderOptions = {}): MarkdownDocument;
```

将安全的 Markdown 令牌转换为电子邮件组件使用的同一 Forge 树。

#### 参数

| 名称 | 类型             | 描述 |
| ---- | ---------------- | ---- |
| 降价 | 字符串           |      |
| 选项 | Markdown渲染选项 |      |

## `src/markdown/types`

### Markdown文档

**种类：**接口

```typescript
export interface MarkdownDocument
```

将 Markdown 解析到共享 Forge 树管道的结果。

### Markdown渲染选项

**种类：**接口

```typescript
export interface MarkdownRenderOptions
```

控制 Markdown 树生成的选项。

## `src/render/attributes`

### 序列化属性

**种类：**功能

```typescript
function serializeAttributes(properties: EmailProperties): string;
```

按确定性顺序序列化一组列入白名单的 Forge 属性。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| 属性 | 电子邮件属性 |      |

## `src/render/escape`

### 转义HTML

**种类：**功能

```typescript
function escapeHtml(value: string): string;
```

对 HTML 序列化的文本和属性内容进行转义。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 价值 | 字符串 |      |

### 验证网址

**种类：**功能

```typescript
function validateUrl(value: string, attribute: string): string;
```

拒绝可以执行代码或加载意外资源的方案。

#### 参数

| 名称 | 类型   | 描述 |
| ---- | ------ | ---- |
| 价值 | 字符串 |      |
| 属性 | 字符串 |      |

## `src/render/render-email`

### 渲染电子邮件

**种类：**功能

```typescript
function renderEmail(node: EmailNode, options: RenderEmailOptions = {}): string;
```

将 Forge 电子邮件树渲染为完整的、无框架的 HTML 文档。

#### 参数

| 名称 | 类型             | 描述 |
| ---- | ---------------- | ---- |
| node | 电子邮件节点     |      |
| 选项 | 渲染电子邮件选项 |      |

### 渲染电子邮件片段

**种类：**功能

```typescript
function renderEmailFragment(node: EmailNode): string;
```

将 Forge 树渲染为浏览器适配器主机的安全 HTML 片段。

#### 参数

| 名称 | 类型         | 描述 |
| ---- | ------------ | ---- |
| node | 电子邮件节点 |      |

## `src/render/style`

### 序列化样式

**种类：**功能

```typescript
function serializeStyle(style: EmailStyle | string): string;
```

以确定性属性顺序序列化内联样式，无需 CSS 变量。

#### 参数

| 名称 | 类型                  | 描述 |
| ---- | --------------------- | ---- |
| 风格 | 电子邮件样式 \|字符串 |      |

## `src/render/types`

### 电子邮件组件

**种类：**类型

```typescript
export type EmailComponent<P extends MpPropertyBag = MpPropertyBag> = MpComponent<P>;
```

可用于撰写电子邮件节点的 Forge 组件。

### 电子邮件节点

**种类：**类型

```typescript
export type EmailNode = MpElement;
```

电子邮件序列化程序接受的框架中立的 Forge 元素。

### 电子邮件属性

**种类：**类型

```typescript
export type EmailProperties = MpPropertyBag &
```

验证后传递给序列化 Forge 元素的属性。

### 电子邮件样式

**种类：**类型

```typescript
export type EmailStyle = Readonly<Record<string, EmailStyleValue>>;
```

其键按词汇顺序序列化的样式对象。

### 电子邮件样式值

**种类：**类型

```typescript
export type EmailStyleValue = number | string | EmailStyleValueWithFallback | null | undefined;
```

没有提供描述。

### EmailStyleValueWithFallback

**种类：**接口

```typescript
export interface EmailStyleValueWithFallback
```

确定性内联样式序列化器接受的值。

### 渲染电子邮件选项

**种类：**接口

```typescript
export interface RenderEmailOptions
```

用于呈现完整电子邮件文档的选项。
