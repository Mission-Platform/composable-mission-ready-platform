# @mission-platform/hunspell API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/hunspell/docs/reference/generated/api.md: [packages/hunspell/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/hunspell` 中的公共源声明生成。

## `src/composables/use-hunspell-monaco/use-hunspell-monaco`

### useHunspell摩纳哥

**种类：**功能

```typescript
function useHunspellMonaco(
  editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>,
  enabled: MaybeRefOrGetter<boolean>,
  languageReference: MaybeRefOrGetter<string>,
): void;
```

Vue 可组合项，将 Hunspell 拼写检查集成到 Monaco 编辑器中。

命令式集成存在于与框架无关的环境中
{@link AttachHunspellMonaco}（与一次写入共享
`@mission-platform/components` 编辑器）；这个可组合项添加了反应性
接线：每当 `enabled`/`editor` 更改并处置时，它会（重新）连接
卸载，并在 `language` 更改时重新检查。

#### 参数

| 名称     | 类型                                                               | 描述 |
| -------- | ------------------------------------------------------------------ | ---- |
| 编辑参考 | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |      |
| 已启用   | MaybeRefOrGetter<boolean>                                          |      |
| 语言参考 | MaybeRefOrGetter<string>                                           |      |

## `src/wasm/hunspell.d`

### 汉斯拼写检查器

**种类：**接口

```typescript
export interface HunspellChecker extends ClassHandle
```

没有提供描述。

### Hunspell模块

**种类：**类型

```typescript
export type MainModule = WasmModule & typeof RuntimeExports & EmbindModule;
```

没有提供描述。

### 字符串向量

**种类：**接口

```typescript
export interface StringVector extends ClassHandle, Iterable<string>
```

没有提供描述。

### Token结果

**种类：**类型

```typescript
export type TokenResult =
```

没有提供描述。

### 令牌结果向量

**种类：**接口

```typescript
export interface TokenResultVector extends ClassHandle, Iterable<TokenResult>
```

没有提供描述。
