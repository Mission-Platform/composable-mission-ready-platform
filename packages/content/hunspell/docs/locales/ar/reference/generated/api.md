# مرجع @mission-platform/hunspell API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/hunspell/docs/reference/generated/api.md: [packages/hunspell/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/hunspell`.

## `src/composables/use-hunspell-monaco/use-hunspell-monaco`

### useHunspellMonaco

**النوع:** الوظيفة

```typescript
function useHunspellMonaco(
  editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>,
  enabled: MaybeRefOrGetter<boolean>,
  languageReference: MaybeRefOrGetter<string>,
): void;
```

Vue قابل للتركيب ويدمج التدقيق الإملائي Hunspell في محرر Monaco.

التكامل الحتمي يعيش في الإطار الحيادي
{@link AttachHunspellMonaco} (تمت مشاركته مع الكتابة مرة واحدة
محرر `@mission-platform/components`)؛ يضيف هذا التركيب رد الفعل
الأسلاك: يتم (إعادة) توصيلها عند تغيير `enabled`/`editor` والتخلص منها
قم بإلغاء التحميل وإعادة التحقق عند تغيير `language`.

#### حدود

| الاسم       | اكتب                                                               | الوصف |
| ----------- | ------------------------------------------------------------------ | ----- |
| مرجع المحرر | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |       |
| ممكّن       | MaybeRefOrGetter<boolean>                                          |       |
| مرجع اللغة  | MaybeRefOrGetter<string>                                           |       |

## `src/wasm/hunspell.d`

### HunspellChecker

**النوع:** الواجهة

```typescript
export interface HunspellChecker extends ClassHandle
```

لم يتم تقديم أي وصف.

### HunspellModule

**النوع:** النوع

```typescript
export type MainModule = WasmModule & typeof RuntimeExports & EmbindModule;
```

لم يتم تقديم أي وصف.

### StringVector

**النوع:** الواجهة

```typescript
export interface StringVector extends ClassHandle, Iterable<string>
```

لم يتم تقديم أي وصف.

### TokenResult

**النوع:** النوع

```typescript
export type TokenResult =
```

لم يتم تقديم أي وصف.

### TokenResultVector

**النوع:** الواجهة

```typescript
export interface TokenResultVector extends ClassHandle, Iterable<TokenResult>
```

لم يتم تقديم أي وصف.
