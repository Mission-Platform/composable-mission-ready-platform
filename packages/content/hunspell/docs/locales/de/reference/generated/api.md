# @mission-platform/hunspell API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/hunspell/docs/reference/generated/api.md: [packages/hunspell/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/hunspell`.

## `src/composables/use-hunspell-monaco/use-hunspell-monaco`

### Benutze HunspellMonaco

**Art:** Funktion

```typescript
function useHunspellMonaco(
  editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>,
  enabled: MaybeRefOrGetter<boolean>,
  languageReference: MaybeRefOrGetter<string>,
): void;
```

Vue Composable, das die Hunspell-Rechtschreibprüfung in einen Monaco-Editor integriert.

Die imperative Integration lebt im Rahmen-Agnostischen
{@link attachmentHunspellMonaco} (gemeinsam mit dem Write-Once
`@mission-platform/components`-Editor); Dieses zusammensetzbare Element fügt das Reaktive hinzu
Verkabelung: Es wird bei jeder Änderung von `enabled`/`editor` (wieder) angeschlossen und entsorgt
unmounten und erneut prüfen, wenn sich `language` ändert.

#### Parameter

| Name            | Geben Sie                                                          | ein Beschreibung |
| --------------- | ------------------------------------------------------------------ | ---------------- |
| editorReference | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |                  |
| aktiviert       | MaybeRefOrGetter<boolean>                                          |                  |
| Sprachreferenz  | MaybeRefOrGetter<string>                                           |                  |

## `src/wasm/hunspell.d`

### HunspellChecker

**Art:** Schnittstelle

```typescript
export interface HunspellChecker extends ClassHandle
```

Keine Beschreibung angegeben.

### HunspellModule

**Art:** Typ

```typescript
export type MainModule = WasmModule & typeof RuntimeExports & EmbindModule;
```

Keine Beschreibung angegeben.

### StringVector

**Art:** Schnittstelle

```typescript
export interface StringVector extends ClassHandle, Iterable<string>
```

Keine Beschreibung angegeben.

### TokenResult

**Art:** Typ

```typescript
export type TokenResult =
```

Keine Beschreibung angegeben.

### TokenResultVector

**Art:** Schnittstelle

```typescript
export interface TokenResultVector extends ClassHandle, Iterable<TokenResult>
```

Keine Beschreibung angegeben.
