# הפניה ל-@mission-platform/hunspell API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/hunspell/docs/reference/generated/api.md: [packages/hunspell/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/hunspell`.

## `src/composables/use-hunspell-monaco/use-hunspell-monaco`

### השתמש ב-Hunspell מונקו

פונקציה **סוג:**

```typescript
function useHunspellMonaco(
  editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>,
  enabled: MaybeRefOrGetter<boolean>,
  languageReference: MaybeRefOrGetter<string>,
): void;
```

Vue חיבור המשלב בדיקת איות של Hunspell בעורך של מונקו.

האינטגרציה הציווית חיה במסגרת-אגנוסטית
{@link attachHunspellMonaco} (שותף עם הכתיבה פעם אחת
עורך `@mission-platform/components`); החיבור הזה מוסיף את התגובה
חיווט: הוא מתחבר (מחדש) בכל פעם ש-`enabled`/`editor` משתנה ומסלק על
unmount, ובודק מחדש כאשר `language` משתנה.

#### פרמטרים

| שם         | הקלד                                                               | תיאור |
| ---------- | ------------------------------------------------------------------ | ----- |
| עורך הפניה | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |       |
| מופעל      | MaybeRefOrGetter<boolean>                                          |       |
| שפה הפניה  | MaybeRefOrGetter<string>                                           |       |

## `src/wasm/hunspell.d`

### בודק Hunspell

**סוג:** ממשק

```typescript
export interface HunspellChecker extends ClassHandle
```

לא סופק תיאור.

### HunspellModule

**סוג:** סוג

```typescript
export type MainModule = WasmModule & typeof RuntimeExports & EmbindModule;
```

לא סופק תיאור.

### StringVector

**סוג:** ממשק

```typescript
export interface StringVector extends ClassHandle, Iterable<string>
```

לא סופק תיאור.

### TokenResult

**סוג:** סוג

```typescript
export type TokenResult =
```

לא סופק תיאור.

### TokenResultVector

**סוג:** ממשק

```typescript
export interface TokenResultVector extends ClassHandle, Iterable<TokenResult>
```

לא סופק תיאור.
