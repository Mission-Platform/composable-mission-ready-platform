{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "type-check"]
    },
    "build:wasm": {
      "cache": false,
      "outputs": ["src/wasm/**"]
    },
    "build:wasm:clean": {
      "cache": false,
      "outputs": ["src/wasm/**"]
    }
  }
}
