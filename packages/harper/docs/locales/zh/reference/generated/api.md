# @mission-platform/harper API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/harper/docs/reference/generated/api.md: [packages/harper/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/harper` 中的公共源声明生成。

## `src/composables/use-harper-monaco/use-harper-monaco`

### 使用Harper摩纳哥

**种类：**功能

```typescript
function useHarperMonaco(
  editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>,
  enabled: MaybeRefOrGetter<boolean>,
  languageReference: MaybeRefOrGetter<string>,
): void;
```

Vue 可组合项将 Harper 语法和风格检查集成到
摩纳哥编辑器实例。

命令式集成存在于与框架无关的环境中
{@link AttachHarperMonaco}（与一次写入共享
`@mission-platform/components` 编辑器）；这个可组合项添加了反应性
接线：每当 `enabled`/`editor` 更改并处置时，它会（重新）连接
卸载，并在 `language` 更改时重新检查。

#### 参数

| 名称     | 类型                                                               | 描述 |
| -------- | ------------------------------------------------------------------ | ---- |
| 编辑参考 | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |      |
| 已启用   | MaybeRefOrGetter<boolean>                                          |      |
| 语言参考 | MaybeRefOrGetter<string>                                           |      |

#### 合同

- **@param:** - 包装 Monaco 编辑器实例的 `MaybeRefOrGetter`。
- **@param:** - 用于打开/关闭检查的 `MaybeRefOrGetter<boolean>`。
- **@param:** - 用于编辑器语言的 `MaybeRefOrGetter<string>`
  （在注册代码操作提供程序时使用）。

## `src/types`

### 哈珀特刊

**种类：**接口

```typescript
export interface HarperIssue
```

Harper 报告的单个语法或风格问题。

位置是检查文本中从零开始的字符偏移量，匹配
harper.js WebAssembly 引擎返回的跨度数据。

### 哈珀工人请求

**种类：**接口

```typescript
export interface HarperWorkerRequest
```

从主线程发送到 Harper 工作线程的消息。

### 哈珀工人回应

**种类：**类型

```typescript
export type HarperWorkerResponse = HarperIssue[];
```

从 Harper 工作线程发送回主线程的消息。
