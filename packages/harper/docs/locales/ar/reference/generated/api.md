# مرجع @mission-platform/harper API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/harper/docs/reference/generated/api.md: [packages/harper/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/harper`.

## `src/composables/use-harper-monaco/use-harper-monaco`

### useHarperMonaco

**النوع:** الوظيفة

```typescript
function useHarperMonaco(editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>, enabled: MaybeRefOrGetter<boolean>, languageReference: MaybeRefOrGetter<string>): void
```

Vue قابل للتركيب والذي يدمج التدقيق النحوي والأسلوب في Harper في ملف
مثال محرر موناكو.

التكامل الحتمي يعيش في الإطار الحيادي
{@link AttachHarperMonaco} (تمت مشاركته مع ملف write-once
محرر `@mission-platform/components`)؛ يضيف هذا التركيب رد الفعل
الأسلاك: يتم (إعادة) توصيلها عند تغيير `enabled`/`editor` والتخلص منها
قم بإلغاء التحميل وإعادة التحقق عند تغيير `language`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مرجع المحرر | MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |  |
| ممكّن | MaybeRefOrGetter<boolean> |  |
| مرجع اللغة | MaybeRefOrGetter<string> |  |

#### عقد

- **@param:** - ملف `MaybeRefOrGetter` يغلف نسخة محرر Monaco.
- **@param:** - `MaybeRefOrGetter<boolean>` الذي يقوم بتبديل التحقق من التشغيل/الإيقاف.
- **@param:** - `MaybeRefOrGetter<string>` للغة المحرر
(يستخدم عند تسجيل موفر إجراء التعليمات البرمجية).

## `src/types`

### مشكلة هاربر

**النوع:** الواجهة

```typescript
export interface HarperIssue
```

مشكلة نحوية أو نمطية واحدة أبلغ عنها هاربر.

المواضع عبارة عن إزاحات أحرف ذات أساس صفري في النص المحدد، وهي مطابقة
بيانات الامتداد التي يتم إرجاعها بواسطة محرك harper.js WebAssembly.

### طلب هاربر ووركر

**النوع:** الواجهة

```typescript
export interface HarperWorkerRequest
```

تم إرسال الرسالة من الموضوع الرئيسي إلى عامل هاربر.

### استجابة هاربر ووركر

**النوع:** النوع

```typescript
export type HarperWorkerResponse = HarperIssue[];
```

تم إرسال الرسالة من عامل Harper إلى الموضوع الرئيسي.
