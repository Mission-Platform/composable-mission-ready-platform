# הפניה ל-@mission-platform/typography API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/typography/docs/reference/generated/api.md: [packages/typography/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/typography`.

## `src/components/atoms/forge-typography/forge-typography`

### ForgeTypography

פונקציה **סוג:**

```typescript
function ForgeTypography(properties: Readonly<TypographyProperties>): MpElement
```

`ForgeTypography` - הפרימיטיבי בסגנון הטקסט שכתב פעם אחת ב-JSX הנייטרלי
ניב והורכב ישר ל-React או Vue על ידי
`@mission-platform/vite-plugin-forge`.

הוא מעבד את תוכן משבצת ברירת המחדל שלו בתג הסמנטי עבור הנבחר
`variant` (ניתן לעקוף עם `as`), תוך החלת סולם הסוג של הגרסה בתוספת
מתקנים אופציונליים של `weight`, `color`, `horizontalAlign`, `verticalAlign` ו-`truncate`.

קישורים מגיעים גם מכאן, בשתי דרכים: `variant="link"` לטקסט קישור עצמאי,
ו-`href` על **כל** וריאציה, כך שכותרת או כיתוב יכולים להיות קישור ללא
משאיר סולם סוג משלו. כך או כך, האלמנט מוצג כ-`<a>` עם ה-
צבע קישור, טיפול ריחוף/פעיל ו-`:visited`, טבעת פוקוס גלויה
ומצב `underline` הנבחר (`'always'` כברירת מחדל); `target="_blank"` מקבל
`rel="noopener noreferrer"` באופן אוטומטי. זה הבעלים שלו
עיצוב באמצעות מודול ה-CSS המשותף `forge-typography.module.scss`
(נישא לכל מסגרת על ידי המהדר הדו-שלבי, אז הרכיב
שולח `@layer mp.typography` CSS משלו); שמות הכיתות הגובבים הם
מורכב עם העוזר {@link classNames} הניטרלי במסגרת.

ה-`@floating-ui` הקטוע של Vue SFC המקורי משוחזר כאן באמצעות
אביזר `truncatePopup`: כאשר הוא פועל, הטקסט הקטום הופך לעוגן עבור
`role="tooltip"` צף קופץ (נחשף ברחף/מיקוד רק כאשר הטקסט
למעשה עולה על גדותיו), ממוקם עם **מיקום עוגן CSS** במקום
`@floating-ui`, ומונע על ידי הווים הנייטרליים `useRef`/`useState`.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| נכסים | לקריאה בלבד<TypographyProperties> |  |

### טיפוגרפיה צבע

**סוג:** סוג

```typescript
export type TypographyColor = | 'primary' | 'secondary' | 'tertiary' | 'disabled' | 'inverse' | 'inherit' | 'neutral' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

צבע טקסט. האסימונים המבניים (`primary`/`secondary`/`tertiary`/
`disabled`/`inverse`) מפה ל-`--mp-color-text-*`; הטונים הסמנטיים הקנוניים
(`neutral`/`success`/`warning`/`info`/`error`/`critical`) מפה להתאמה
אסימון `--mp-color-<family>-text`; `inherit` לא חל.

### טיפוגרפיהHorizontal Align

**סוג:** סוג

```typescript
export type TypographyHorizontalAlign = 'start' | 'center' | 'end';
```

יישור טקסט אופקי (ממפה ל-CSS `text-align`).

### טיפוגרפיהLineHeight

**סוג:** סוג

```typescript
export type TypographyLineHeight = 'tight' | 'snug' | 'normal' | 'relaxed' | 'loose';
```

עקיפה של קו גובה (מוביל), ממופה לאסימוני עיצוב `--mp-line-height-*`.

### טיפוגרפיה מאפיינים

**סוג:** ממשק

```typescript
export interface TypographyProperties
```

לא סופק תיאור.

### טיפוגרפיהגודל

**סוג:** סוג

```typescript
export type TypographySize = SizeScale;
```

אסימון גודל אופציונלי - קנוני 2xs → 2xl קנה מידה (עוקף את גודל הגופן של הגרסה כשהיא מוגדרת).

### טיפוגרפיה יעד

**סוג:** סוג

```typescript
export type TypographyTarget = '_self' | '_blank' | '_parent' | '_top';
```

בהקשר של גלישה נפתח קישור.

### טיפוגרפיה קו תחתון

**סוג:** סוג

```typescript
export type TypographyUnderline = 'always' | 'hover' | 'none';
```

כאשר קישור מצייר את הקו התחתון שלו.

### טיפוגרפיה Variant

**סוג:** סוג

```typescript
export type TypographyVariant = | 'display' | 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'body-lg' | 'body-md' | 'body-sm' | 'body-xs' | 'label' | 'caption' | 'code' /** Standalone link text: renders an `<a>` at the body scale with link styling. */ | 'link';
```

השלב בקנה מידה סמנטי/חזותי שבו הטקסט מוצג.

### טיפוגרפיהVerticalAlign

**סוג:** סוג

```typescript
export type TypographyVerticalAlign = 'baseline' | 'top' | 'middle' | 'bottom' | 'sub' | 'super' | 'text-top' | 'text-bottom';
```

יישור אנכי של תיבת הטקסט (מוטבעת) (ממפה ל-CSS `vertical-align`).

### טיפוגרפיה משקל

**סוג:** סוג

```typescript
export type TypographyWeight = 'regular' | 'medium' | 'semibold' | 'bold';
```

ביטול משקל גופן הוחל על משקל ברירת המחדל של הגרסה.
