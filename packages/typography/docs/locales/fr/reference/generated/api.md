# Référence API @mission-platform/typography

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/typography/docs/reference/generated/api.md: [packages/typography/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/typography`.

## `src/components/atoms/forge-typography/forge-typography`

### ForgeTypographie

**Genre :** fonction

```typescript
function ForgeTypography(properties: Readonly<TypographyProperties>): MpElement
```

`ForgeTypography` — la primitive de style de texte créée une fois dans le JSX neutre
dialecte et compilé directement en React ou Vue par
`@mission-platform/vite-plugin-forge`.

Il restitue le contenu de son emplacement par défaut dans la balise sémantique du
`variant` (remplaçable avec `as`), en appliquant l'échelle de type de la variante plus le
modificateurs facultatifs `weight`, `color`, `horizontalAlign`, `verticalAlign` et `truncate`.

Les liens proviennent également d'ici, de deux manières : `variant="link"` pour le texte du lien autonome,
et `href` sur **n'importe quelle** variante afin qu'un titre ou une légende puisse être un lien sans
laissant sa propre échelle de types. Quoi qu'il en soit, l'élément s'affiche sous la forme d'un `<a>` avec le
couleur du lien, son traitement hover/actif et `:visited`, une bague de mise au point visible
et le mode `underline` choisi (`'always'` par défaut) ; `target="_blank"` obtient
`rel="noopener noreferrer"` automatiquement. Il possède son
style via le module CSS colocalisé `forge-typography.module.scss`
(transporté sur chaque framework par le compilateur en deux étapes, donc le composant
expédie son propre CSS `@layer mp.typography`); les noms de classe hachés sont
assemblé avec l'assistant {@link classNames} indépendant du framework.

La fenêtre contextuelle tronquée `@floating-ui` originale du SFC Vue est restaurée ici via
la prop `truncatePopup` : lorsqu'elle est activée, le texte tronqué devient l'ancre d'un
popup `role="tooltip"` flottant (révélé au survol/au focus uniquement lorsque le texte
déborde en fait), positionné avec **CSS Anchor Positioning** au lieu de
`@floating-ui`, et piloté par les crochets neutres `useRef`/`useState`.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| propriétés | Lecture seule<TypographyProperties> |  |

### TypographieCouleur

**Genre :** type

```typescript
export type TypographyColor = | 'primary' | 'secondary' | 'tertiary' | 'disabled' | 'inverse' | 'inherit' | 'neutral' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

Couleur du texte. Les jetons structurels (`primary`/`secondary`/`tertiary`/
`disabled`/`inverse`) mappé vers `--mp-color-text-*` ; les tons sémantiques canoniques
(`neutral`/`success`/`warning`/`info`/`error`/`critical`) mappé au correspondant
Jeton `--mp-color-<family>-text` ; `inherit` n'en applique aucun.

### TypographieHorizontalAlign

**Genre :** type

```typescript
export type TypographyHorizontalAlign = 'start' | 'center' | 'end';
```

Alignement horizontal du texte (mappé vers CSS `text-align`).

### TypographieLineHeight

**Genre :** type

```typescript
export type TypographyLineHeight = 'tight' | 'snug' | 'normal' | 'relaxed' | 'loose';
```

Remplacement de la hauteur de ligne (en tête), mappé à un jeton de conception `--mp-line-height-*`.

### TypographiePropriétés

**Genre :** interface

```typescript
export interface TypographyProperties
```

Aucune description fournie.

### TypographieTaille

**Genre :** type

```typescript
export type TypographySize = SizeScale;
```

Jeton de taille facultatif — échelle canonique 2xs → 2xl (remplace la taille de police de la variante lorsqu'elle est définie).

### TypographieTarget

**Genre :** type

```typescript
export type TypographyTarget = '_self' | '_blank' | '_parent' | '_top';
```

Contexte de navigation dans lequel un lien s'ouvre.

### TypographieSouligné

**Genre :** type

```typescript
export type TypographyUnderline = 'always' | 'hover' | 'none';
```

Lorsqu'un lien dessine son soulignement.

### TypographieVariant

**Genre :** type

```typescript
export type TypographyVariant = | 'display' | 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'body-lg' | 'body-md' | 'body-sm' | 'body-xs' | 'label' | 'caption' | 'code' /** Standalone link text: renders an `<a>` at the body scale with link styling. */ | 'link';
```

L'étape d'échelle de type sémantique/visuelle à laquelle le texte est rendu.

### TypographieVerticalAlign

**Genre :** type

```typescript
export type TypographyVerticalAlign = 'baseline' | 'top' | 'middle' | 'bottom' | 'sub' | 'super' | 'text-top' | 'text-bottom';
```

Alignement vertical de la zone de texte (en ligne) (mappé vers CSS `vertical-align`).

### TypographieWeight

**Genre :** type

```typescript
export type TypographyWeight = 'regular' | 'medium' | 'semibold' | 'bold';
```

Remplacement du poids de la police appliqué en plus du poids par défaut de la variante.
