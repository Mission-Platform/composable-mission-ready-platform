# Referencia de API @mission-platform/code-scanner

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/integrations/code-scanner/docs/reference/generated/api.md: [packages/integrations/code-scanner/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/code-scanner`.

## `src/capture`

### blobToImageData

**Tipo:** función

```typescript
function blobToImageData(blob: Blob, roi = 1): Promise<ImageLike>;
```

Decodificar una imagen `Blob`/`File` en un {@link ImageLike} (píxeles RGBA). Utiliza
`createImageBitmap` por lo que funciona fuera del hilo principal donde sea compatible.

#### Parámetros

| Nombre | Tipo | Descripción |
| ------ | ---- | ----------- |
| gota   | gota |             |
| rey    |      |             |

### escanear archivo

**Tipo:** función

```typescript
function scanFile(file: Blob): Promise<ScanResult | null>;
```

Decodifica `file` y escanéalo en busca de un código, ejecutando WebAssembly sincrónicamente
escáner (que se autoinicializa desde su binario incorporado en una compilación de producción).

#### Parámetros

| Nombre  | Tipo | Descripción |
| ------- | ---- | ----------- |
| archivo | gota |             |

### escanearArchivoAsync

**Tipo:** función

```typescript
function scanFileAsync(file: Blob): Promise<ScanResult | null>;
```

Decodifica `file` y escanéalo en busca de un código, inicializando el escáner y los decodificadores.
de forma asincrónica: seguro en cualquier entorno (no se requiere binario insertado).

#### Parámetros

| Nombre  | Tipo | Descripción |
| ------- | ---- | ----------- |
| archivo | gota |             |

### videoFrameToImageData

**Tipo:** función

```typescript
function videoFrameToImageData(video: HTMLVideoElement, roi = DEFAULT_ROI): ImageLike;
```

Capture el fotograma actual de un elemento `<video>` en reproducción como un
{@link ImageLike}, recortando opcionalmente a la fracción central `roi` (ver
{@link DEFAULT_ROI}). El recorte es especialmente valioso aquí: los fotogramas en vivo son
lleno de desorden que los localizadores de cajas delimitadoras de tinta no pueden tolerar, por lo que pasar el
El tamaño de la retícula que la interfaz de usuario muestra al usuario aumenta considerablemente la tasa de aciertos.

#### Parámetros

| Nombre | Tipo                   | Descripción |
| ------ | ---------------------- | ----------- |
| vídeo  | Elemento de vídeo HTML |             |
| rey    |                        |             |

## `src/debug`

### isCodeScannerDebugEnabled

**Tipo:** función

```typescript
function isCodeScannerDebugEnabled(): boolean;
```

Si el registro de diagnóstico del escáner está actualmente habilitado.

### setCodeScannerDebug

**Tipo:** función

```typescript
function setCodeScannerDebug(value: boolean): void;
```

Habilite o deshabilite el registro de diagnóstico del lado JS del escáner en tiempo de ejecución.

#### Parámetros

| Nombre | Tipo     | Descripción |
| ------ | -------- | ----------- |
| valor  | booleano |             |

#### Contrato

- **@param:** - `true` para registrar cada etapa de escaneo en la consola, `false` para silenciarla.

## `src/image`

### contrasteEstiramientoLuma

**Tipo:** función

```typescript
function contrastStretchLuma(
  luma: LumaImage,
  lowPercentile: number = DEFAULT_LOW_PERCENTILE,
  highPercentile: number = DEFAULT_HIGH_PERCENTILE,
): LumaImage;
```

Estire por contraste un {@link LumaImage}, mapeando su `lowPercentile`..`highPercentile`
rango de intensidad en el rango completo de `0..255` (los valores fuera del rango son
sujetado). Devuelve una imagen **nueva**; la entrada se deja intacta.

El binarizador wasm elige un único umbral Otsu global, que separa un
Lo mejor es un histograma limpio y bimodal. Las imágenes cargadas suelen ser; camara en vivo
los marcos a menudo no lo son: el resplandor, la iluminación desigual y el bajo contraste aplastan el
histograma para que los módulos oscuros y el fondo claro se fusionen. Estiramiento
el rango dinámico primero los vuelve a separar sin tocar el umbral
lógica, que es exactamente lo que los localizadores de código de barras 1D y Data Matrix (que
confíe en el cuadro delimitador de tinta) necesario.

Un marco degenerado (plano), donde los dos percentiles colapsan al mismo
valor: se devuelve sin cambios, ya que no hay nada que estirar.

#### Parámetros

| Nombre         | Tipo       | Descripción |
| -------------- | ---------- | ----------- |
| luma           | LumaImagen |             |
| percentil bajo | número     |             |
| percentil alto | número     |             |

### imagenDataToLuma

**Tipo:** función

```typescript
function imageDataToLuma(image: ImageLike): LumaImage;
```

Convierta un RGBA {@link ImageLike} (por ejemplo, un lienzo `ImageData`) en un
{@link LumaImage} de un solo canal usando el Rec. 601 pesas luminosas
(`0.299 R + 0.587 G + 0.114 B`). El canal alfa se ignora.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| imagen | Imagen como |             |

### LumaImagen

**Tipo:** interfaz

```typescript
export interface LumaImage
```

Una imagen en escala de grises de un solo canal: `width * height` luma bytes.

## `src/scanner/index`

### escanear datos de imagen

**Tipo:** función

```typescript
function scanImageData(image: ImageLike, roi?: Roi): ScanResult | null;
```

Localice y decodifique el primer código admitido en `image`, creando una instancia del
gráfico del escáner de forma sincrónica en el primer uso.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| imagen | Imagen como |             |
| rey    | Rey         |             |

#### Contrato

- **@param:** región de interés opcional (píxeles de la imagen) para restringir el escaneo a
  — recortado antes de la binarización, por lo que se ignora el desorden circundante.
- **@returns:** el {@link ScanResult}, o `null` cuando no se encuentra ningún código. cuando un código
  está ubicado pero su carga útil no se puede decodificar, `result.value` es `null`.

### escanearImagenDatosTodos

**Tipo:** función

```typescript
function scanImageDataAll(image: ImageLike): ScanResult[];
```

Localice y decodifique _cada_ código distinto en `image` (no solo el primero),
crear una instancia del gráfico del escáner de forma sincrónica en el primer uso.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| imagen | Imagen como |             |

#### Contrato

- **@returns:** los {@link ScanResult} decodificados en orden de descubrimiento, deduplicados;
  vacío cuando no se decodifica nada.

### escanearImageDataAllAsync

**Tipo:** función

```typescript
function scanImageDataAllAsync(image: ImageLike): Promise<ScanResult[]>;
```

Localice y decodifique _cada_ código distinto en `image`, cargando el gráfico del escáner
de forma asincrónica en el primer uso. Los fallos de inicialización y escaneo son
devueltos como rechazos de promesa.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| imagen | Imagen como |             |

#### Contrato

- **@returns:** los {@link ScanResult} decodificados en orden de descubrimiento, deduplicados.

### escanearImageDataAsync

**Tipo:** función

```typescript
function scanImageDataAsync(image: ImageLike, roi?: Roi): Promise<ScanResult | null>;
```

Localice y decodifique el primer código compatible en `image`, cargando el escáner
gráfico de forma asincrónica en el primer uso. Inicialización y escaneo
los fallos se devuelven como rechazos de promesa.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| imagen | Imagen como |             |
| rey    | Rey         |             |

#### Contrato

- **@param:** región de interés opcional; consulte {@link scanImageData}.
- **@returns:** el {@link ScanResult}, o `null` cuando no se encuentra ningún código.

## `src/types`

### Imagen como

**Tipo:** interfaz

```typescript
export interface ImageLike
```

La forma mínima de un `ImageData` que este paquete necesita: un RGBA sin formato y de fila principal
buffer de píxeles con sus dimensiones. El DOM `ImageData` lo satisface, al igual que
cualquier objeto `{ width, height, data }` construido a mano (útil para pruebas/SSR).

### rey

**Tipo:** interfaz

```typescript
export interface Roi
```

Una región rectangular de interés, en píxeles de la imagen, a la que restringir un escaneo (p. ej.
una retícula que la interfaz de usuario de una cámara en vivo dibuja sobre el marco). El cultivo ocurre en wasm.
_antes_ de la binarización, por lo que el desorden circundante nunca llega a los localizadores. un
se sujeta la región que sobresale de un borde; uno completamente fuera del marco encuentra
nada.

### Formato de escaneo

**Tipo:** tipo

```typescript
export type ScanFormat = 'qr' | 'datamatrix' | 'barcode' | 'aztec' | 'pdf417' | 'databar' | 'maxicode';
```

Las familias de códigos que el escáner puede localizar y decodificar.

### Resultado del escaneo

**Tipo:** interfaz

```typescript
export interface ScanResult
```

El resultado de una _detección_ exitosa.
