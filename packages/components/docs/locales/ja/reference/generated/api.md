# @mission-platform/components API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/components/docs/reference/generated/api.md: [packages/components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/components` のパブリック ソース宣言から生成されます。

## `src/components/atoms/forge-avatar/forge-avatar`

### アバターのプロパティ

**種類:** インターフェース

```typescript
export interface AvatarProperties
```

説明はありません。

### アバターの形状

**種類：**タイプ

```typescript
export type AvatarShape = 'circle' | 'square';
```

アバターの輪郭形状。

### アバターサイズ

**種類：**タイプ

```typescript
export type AvatarSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の 2xs → 2xl サイズ スケールがディスプレイ コンポーネント全体で共有されます。

### アバターステータス

**種類：**タイプ

```typescript
export type AvatarStatus = 'online' | 'offline' | 'away' | 'busy' | undefined;
```

角のドットとしてレンダリングされるオプションのプレゼンス インジケーター。

### フォージアバター

**種類:** 関数

```typescript
function ForgeAvatar(properties: Readonly<AvatarProperties>): MpElement;
```

`ForgeAvatar` — ニュートラルな JSX 方言で一度作成されたユーザー/エンティティのアバター
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`src` イメージ、フォールバック `initials`、または
デフォルトのスロット、正規の `2xs … 2xl` スケールによるサイズ、オプションで
プレゼンス ステータス コーナー ドット。移行されたレイアウト プリミティブと同じように配置されます
それ自体は**インライン スタイル** (デザイン トークンによって駆動される) を備えているため、出荷されません。
独自の CSS モジュール。同じ場所にある `forge-avatar.module.scss` のみのドレス
Storybook のデモ コンテンツ。

#### パラメータ

| 名前       | タイプ                         | 説明 |
| ---------- | ------------------------------ | ---- |
| プロパティ | 読み取り専用<AvatarProperties> |      |

## `src/components/atoms/forge-background-video/forge-background-video`

### 背景ビデオフィット

**種類：**タイプ

```typescript
export type BackgroundVideoFit = 'cover' | 'contain';
```

ビデオがボックスをどのように埋めるか。

### 背景ビデオプロパティ

**種類:** インターフェース

```typescript
export interface BackgroundVideoProperties
```

説明はありません。

### 背景ビデオソース

**種類:** インターフェース

```typescript
export interface BackgroundVideoSource
```

単一のフォーマット固有のビデオ ソース。

### 鍛造背景ビデオ

**種類:** 関数

```typescript
function ForgeBackgroundVideo(properties: Readonly<BackgroundVideoProperties>): MpElement;
```

`ForgeBackgroundVideo` — 一度作成された装飾的なフルブリード背景ビデオ
中立的な JSX 方言で、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

コンテナを覆う自動再生、ミュート、ループ `<video>` をレンダリングします。
オプションの前景コンテンツ (デフォルトのスロット) がその上に重ねられ、
コントラストを維持するためのオプションのスクリム `overlay`。ビデオは次のように扱われます
装飾的 (`aria-hidden`) であり、コントロールは公開されません。

`prefers-reduced-motion`: リアクティブな `matchMedia` クエリ (によって駆動される) を尊重します。
ニュートラルな {@link useState}/{@link useEffect} フック) は再生を一時停止し、
ユーザーがモーションの縮小を要求した場合、自動再生をスキップし、
`poster`の画像。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-background-video.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<BackgroundVideoProperties> |      |

## `src/components/atoms/forge-badge/forge-badge`

### バッジのプロパティ

**種類:** インターフェース

```typescript
export interface BadgeProperties
```

説明はありません。

### バッジのサイズ

**種類：**タイプ

```typescript
export type BadgeSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の 2xs → 2xl サイズ スケールで、共有サイズのトークンに一致します。

### バッジバリアント

**種類：**タイプ

```typescript
export type BadgeVariant =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

バッジの視覚的なトーン。 `@mission-platform/components` `ForgeBadge` をミラーリングします。

### フォージバッジ

**種類:** 関数

```typescript
function ForgeBadge(properties: Readonly<BadgeProperties>): MpElement;
```

`ForgeBadge` — ニュートラル JSX で一度作成された小さなステータス/ラベル チップ
方言。 `toReactComponent(ForgeBadge)` または
`toVueComponent(ForgeBadge)`、または事前に適応されたバリアントを次からインポートします
`@mission-platform/components` — フレームワーク ビルドは
指定子によるものではなく、消費者の `mp:<framework>` エクスポート条件。

`@mission-platform/components` `ForgeBadge` を反映しています: 同じ 9 トーン
バリアント、正規の `2xs → 2xl` サイズ スケール、およびラベルがレンダリングされます
{@link ForgeTypography} 経由 (`caption`、中程度の太さ、継承された色)
タイポグラフィは Vue ライブラリと一致します。

同じ場所に配置された CSS モジュール `forge-badge.module.scss` を通じてそのスタイルを所有します。
(2 段階のコンパイラーによってすべてのフレームワークに引き継がれるため、コンポーネント
独自の `@layer mp.components` CSS が同梱されています)。ハッシュされたモジュールのクラス名は次のとおりです。
フレームワークに依存しない {@link classNames} ヘルパーでアセンブルされ、
プレーン文字列と `{ className: boolean }` オブジェクト形式。

#### パラメータ

| 名前       | タイプ                        | 説明 |
| ---------- | ----------------------------- | ---- |
| プロパティ | 読み取り専用<BadgeProperties> |      |

## `src/components/atoms/forge-button/forge-button`

### ボタンのプロパティ

**種類:** インターフェース

```typescript
export interface ButtonProperties
```

説明はありません。

### ボタンバリアント

**種類：**タイプ

```typescript
export type ButtonVariant =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical' | 'ghost';
```

ボタンの視覚的な処理。 `@mission-platform/components` `ForgeButton` をミラーリングします。

### 鍛造ボタン

**種類:** 関数

```typescript
function ForgeButton(properties: Readonly<ButtonProperties>): MpElement;
```

`ForgeButton` — ニュートラルな JSX 方言で一度作成され、コンパイルされたボタン
`@mission-platform/vite-plugin-forge` によって React または Vue に直接送信されます。

これは、正規版の `@mission-platform/components` `ForgeButton` を反映しています。
カラーバリエーションと透明な `ghost` 処理、標準
`2xs → 2xl` サイズスケール、焦点が見える輪郭、
`aria-busy` を設定し、
アクティブなときにクリックします。オリジナルの Vue SFC は、スピナーのラベルを次の場所から取得しました。
`@mission-platform/i18n`;中立バージョン (i18n はこれに含まれていません)
ライブラリ) のデフォルトは `loadingLabel` から `'Loading…'` です。

同じ場所に配置された CSS モジュール `forge-button.module.scss` を通じてスタイルを所有します。
(2 段階のコンパイラーによってすべてのフレームワークに引き継がれるため、コンポーネント
独自の `@layer mp.components` CSS が付属しています)。ハッシュされたモジュールのクラス名は次のとおりです。
フレームワークに依存しない {@link classNames} ヘルパーを使用してアセンブルされます。
`{ className: boolean }` 条件付き読み込み修飾子のオブジェクト フォーム。

#### パラメータ

| 名前       | タイプ                         | 説明 |
| ---------- | ------------------------------ | ---- |
| プロパティ | 読み取り専用<ButtonProperties> |      |

## `src/components/atoms/forge-icon-button/forge-icon-button`

### 鍛造アイコンボタン

**種類:** 関数

```typescript
function ForgeIconButton(properties: Readonly<IconButtonProperties>): MpElement;
```

`ForgeIconButton` — コンパクトで正方形のアイコンのみのボタンで、
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

アイコンがデフォルトで提供されるネイティブ `<button>` をレンダリングします。
スロット。表示されるテキストがないため、アクセス可能な名前が **必須** です。
`label` (`aria-label` として適用)。 `click` コールバックは抑制されます。
`disabled`。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-icon-button.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<IconButtonProperties> |      |

### アイコンボタンプロパティ

**種類:** インターフェース

```typescript
export interface IconButtonProperties
```

説明はありません。

### アイコンボタンサイズ

**種類：**タイプ

```typescript
export type IconButtonSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正方形のパディングを制御するサイズ トークン — 正規の 2xs → 2xl スケール。

### アイコンボタンバリアント

**種類：**タイプ

```typescript
export type IconButtonVariant =
  'ghost' | 'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

アイコン ボタンの視覚的な処理 — 正規のカラー セットと透明な `ghost`。

## `src/components/atoms/forge-progress-bar/forge-progress-bar`

### ForgeProgressBar

**種類:** 関数

```typescript
function ForgeProgressBar(properties: Readonly<ProgressBarProperties>): MpElement;
```

`ForgeProgressBar` — 作成された確定または不確定の進行状況インジケーター
一度中立的な JSX 方言で変換し、直接 React または Vue にコンパイルします。
`@mission-platform/vite-plugin-forge`。

トーン/サイズ、オプションのラベルを使用して、ネイティブ `<progress>` トラックをレンダリングします。
行 (ラベル + 合成されたニュートラル {@link ForgeTypography} によるパーセンテージ)、および
不定モード。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-progress-bar.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<ProgressBarProperties> |      |

### ProgressBarProperties

**種類:** インターフェース

```typescript
export interface ProgressBarProperties
```

説明はありません。

### 進行サイズ

**種類：**タイプ

```typescript
export type ProgressSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

カノニカルの2xs→2xlサイズスケール（トラックの太さ）。

### プログレスバリアント

**種類：**タイプ

```typescript
export type ProgressVariant =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

プログレスフィルのトーン。

## `src/components/atoms/forge-quote/forge-quote`

### 引用を鍛造する

**種類:** 関数

```typescript
function ForgeQuote(properties: Readonly<QuoteProperties>): MpElement;
```

`ForgeQuote` — ニュートラルで一度作成されたセマンティックなブロッククォート/プルクォート
JSX 方言であり、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

デフォルトのスロットの内容を引用としてレンダリングします（合成された
中立的な {@link ForgeTypography})、オプションの `author`/`source` 属性付き
構成可能な視覚処理。スタイルを確立するのは、
同じ場所に配置された CSS モジュール `forge-quote.module.scss`、
フレームワークに依存しない {@link classNames} ヘルパー。

元の Vue SFC は、スコープ付き `author` スロット (オーバーライド) をさらに公開しました。
`author`/`source` プロパティ）および検出されたスロットの存在。ニュートラルバージョン
そのスコープ付きスロットを削除します — アトリビューションは `author`/`source` プロパティによって駆動されます
— 他の移行されたコンポーネントがスコープ付きスロットを削除した方法と一致しています。

#### パラメータ

| 名前       | タイプ                        | 説明 |
| ---------- | ----------------------------- | ---- |
| プロパティ | 読み取り専用<QuoteProperties> |      |

### 見積プロパティ

**種類:** インターフェース

```typescript
export interface QuoteProperties
```

説明はありません。

### 見積サイズ

**種類：**タイプ

```typescript
export type QuoteSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

引用テキストのスケールを制御するサイズ トークン — 正規の 2xs → 2xl スケール。

### 引用バリアント

**種類：**タイプ

```typescript
export type QuoteVariant = 'default' | 'bordered' | 'plain';
```

引用文の視覚的な処理。

## `src/components/atoms/forge-responsive-image/forge-responsive-image`

### レスポンシブ画像のフォージ

**種類:** 関数

```typescript
function ForgeResponsiveImage(properties: Readonly<ResponsiveImageProperties>): MpElement;
```

`ForgeResponsiveImage` — アート指向のレスポンシブ `<picture>` 要素
ニュートラルな JSX 方言で一度作成され、React に直接コンパイルされるか、
`@mission-platform/vite-plugin-forge` による Vue。

`sources` のエントリごとに 1 つの `<source>` を含む `<picture>` をレンダリングします (アート用)
方向/形式ネゴシエーション）およびフォールバック `<img>`、ネイティブをサポート
`srcset`/`sizes`、遅延ロード、非同期デコード、固定 `aspectRatio` (
レイアウト スペースを確保し、CLS を回避します)、`object-fit` 制御。それはそれを所有しています
同じ場所に配置された CSS モジュール `forge-responsive-image.module.scss` によるスタイリング、
フレームワークに依存しない {@link classNames} ヘルパーを使用してアセンブルされます。

元の Vue SFC は、ネイティブ `load`/`error` イベントをコンポーネントとして再発行しました
放出します。中立バージョンでは、それらを `onLoad`/`onError` コールバックとして公開します
`<img>` に直接バインドされた props (callback-prop と一致)
移行されたコンポーネント全体で使用される規則です)。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<ResponsiveImageProperties> |      |

### レスポンシブイメージフィット

**種類：**タイプ

```typescript
export type ResponsiveImageFit = 'cover' | 'contain' | 'fill' | 'none' | 'scale-down';
```

画像がボックスにどのように収まるか。

### ResponsiveImageProperties

**種類:** インターフェース

```typescript
export interface ResponsiveImageProperties
```

説明はありません。

### レスポンシブイメージソース

**種類:** インターフェース

```typescript
export interface ResponsiveImageSource
```

単一のアート志向/フォーマット固有の画像ソース。

## `src/components/atoms/forge-responsive-video/forge-responsive-video`

### Forgeレスポンシブビデオ

**種類:** 関数

```typescript
function ForgeResponsiveVideo(properties: Readonly<ResponsiveVideoProperties>): MpElement;
```

`ForgeResponsiveVideo` — レスポンシブ `<video>` 要素は、
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

固定のパラメータを維持しながら、コンテナに合わせてスケーリングする `<video>` をレンダリングします。
`aspectRatio` (レイアウトのシフトを回避)、複数の `<source>` エントリをサポート
フォーマットネゴシエーション、ポスター画像、ネイティブコントロール、および通常の
再生フラグ。装飾的でコンテンツのない背景の場合は、
{@link ForgeBackgroundVideo}。同じ場所に配置された CSS を通じてスタイルを所有します。
モジュール `forge-responsive-video.module.scss`、
フレームワークに依存しない {@link classNames} ヘルパー。

元の Vue SFC は、ネイティブ `play`/`pause`/`ended` イベントを次のように再発行しました。
コンポーネントが放出します。中立バージョンでは、それらを次のように公開します。
`onPlay`/`onPause`/`onEnded` コールバック プロパティは `<video>` に直接バインドされています
(移行されたシステム全体で使用される callback-prop 規則と一致します)
コンポーネント）。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<ResponsiveVideoProperties> |      |

### レスポンシブビデオフィット

**種類：**タイプ

```typescript
export type ResponsiveVideoFit = 'cover' | 'contain' | 'fill' | 'none' | 'scale-down';
```

ビデオがボックスをどのように埋めるか。

### ResponsiveVideoProperties

**種類:** インターフェース

```typescript
export interface ResponsiveVideoProperties
```

説明はありません。

### レスポンシブビデオソース

**種類:** インターフェース

```typescript
export interface ResponsiveVideoSource
```

単一のフォーマット固有のビデオ ソース。

## `src/components/atoms/forge-separator/forge-separator`

### フォージセパレーター

**種類:** 関数

```typescript
function ForgeSeparator(properties: Readonly<SeparatorProperties>): MpElement;
```

`ForgeSeparator` — ニュートラルで一度作成された視覚的なセパレーター/ディバイダー
JSX 方言であり、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

グループを区切るために使用される水平または垂直の罫線 (`<hr>`) をレンダリングします。
内容。デフォルトのスロット コンテンツが提供されている場合 (水平のみ)、レンダリングされます。
代わりに 2 行の間にラベルを中央に配置します。デフォルトでは公開します
`role="separator"` と適切な `aria-orientation`。 `decorative` を設定
それを純粋にプレゼンテーションとしてマークします (`role="none"`)。

同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-separator.module.scss` (2 段階のフレームワークによって継承される)
コンパイラに組み込まれているため、コンポーネントには独自の `@layer mp.components` CSS が付属しています)。の
ハッシュされたモジュール クラス名はフレームワークに依存しないでアセンブルされます。
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<SeparatorProperties> |      |

### セパレータの方向

**種類：**タイプ

```typescript
export type SeparatorOrientation = 'horizontal' | 'vertical';
```

セパレータのレイアウト方向。

### セパレータのプロパティ

**種類:** インターフェース

```typescript
export interface SeparatorProperties
```

説明はありません。

### セパレータの間隔

**種類：**タイプ

```typescript
export type SeparatorSpacing = 'none' | 'sm' | 'md' | 'lg' | 'xl';
```

セパレータの周囲のマージンとして適用されるスペース。

### 区切り文字バリアント

**種類：**タイプ

```typescript
export type SeparatorVariant = 'solid' | 'dashed' | 'dotted';
```

線のスタイル。

## `src/components/atoms/forge-skeleton/forge-skeleton`

### 鍛造スケルトン

**種類:** 関数

```typescript
function ForgeSkeleton(properties: Readonly<SkeletonProperties>): MpElement;
```

`ForgeSkeleton` — ニュートラル JSX で一度作成された読み込みプレースホルダー
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

`aria-hidden` ブロックを 3 つの形状 (`line`/`circle`/) のいずれかでレンダリングします。
`block`) オプションのシマー アニメーションを使用し、明示的な `width`/ を受け入れます
`height` はオーバーライドします。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-skeleton.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<SkeletonProperties> |      |

### スケルトンのプロパティ

**種類:** インターフェース

```typescript
export interface SkeletonProperties
```

説明はありません。

### スケルトン形状

**種類：**タイプ

```typescript
export type SkeletonShape = 'line' | 'circle' | 'block';
```

スケルトン プレースホルダーがレンダリングされる形状。

## `src/components/atoms/forge-spinner/forge-spinner`

### フォージスピナー

**種類:** 関数

```typescript
function ForgeSpinner(properties: Readonly<SpinnerProperties>): MpElement;
```

`ForgeSpinner` — ニュートラルで一度作成された不定読み込みスピナー
JSX 方言であり、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

トーン/サイズとアクセス可能なラベルを備えた `role="status"` リングをレンダリングします。
同じ場所に配置された CSS モジュール `forge-spinner.module.scss` を通じてそのスタイルを所有します。
フレームワークに依存しない {@link classNames} ヘルパーを使用してアセンブルされます。

元の Vue SFC は、デフォルトのラベルを `@mission-platform/i18n` から取得しました。
ニュートラル バージョン (i18n はこのライブラリの一部ではありません) では、`label` がデフォルトで
`'Loading…'`。

#### パラメータ

| 名前       | タイプ                          | 説明 |
| ---------- | ------------------------------- | ---- |
| プロパティ | 読み取り専用<SpinnerProperties> |      |

### スピナーのプロパティ

**種類:** インターフェース

```typescript
export interface SpinnerProperties
```

説明はありません。

### スピナーサイズ

**種類：**タイプ

```typescript
export type SpinnerSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の2xs→2xlサイズスケール。

### スピナーバリアント

**種類：**タイプ

```typescript
export type SpinnerVariant =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

スピナーリングの音色。

## `src/components/atoms/forge-stack/forge-stack`

### フォージスタック

**種類:** 関数

```typescript
function ForgeStack(properties: Readonly<StackProperties>): MpElement;
```

`ForgeStack` — 子を配置するフレックスボックス スタック レイアウト プリミティブ
**垂直** (列) または **水平** (行) の単一行、
それらの間には一貫した `gap` があります。ニュートラル JSX で一度作成されています
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

BEM クラス名と計算されたフレックスボックス インライン スタイルを出力します。デモ
スタイリングは同じ場所にある `forge-stack.module.scss` にあります (インポートされたものは
`forge-stack.stories.tsx`)。

#### パラメータ

| 名前       | タイプ                        | 説明 |
| ---------- | ----------------------------- | ---- |
| プロパティ | 読み取り専用<StackProperties> |      |

### スタックアライン

**種類：**タイプ

```typescript
export type StackAlign = 'start' | 'center' | 'end' | 'stretch' | 'baseline';
```

軸間配置キーワード (`align` → `align-items`)。

### スタック方向

**種類：**タイプ

```typescript
export type StackDirection = 'vertical' | 'horizontal';
```

{@link ForgeStack} がその子を配置する軸。

### スタックギャップ

**種類：**タイプ

```typescript
export type StackGap = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

名前付きギャップスケール。各ステップは `--mp-spacing-*` デザイン トークンに解決されます。

### スタックジャスティファイ

**種類：**タイプ

```typescript
export type StackJustify = 'start' | 'center' | 'end' | 'between' | 'around' | 'evenly';
```

主軸分布キーワード (`justify` → `justify-content`)。

### スタックプロパティ

**種類:** インターフェース

```typescript
export interface StackProperties
```

説明はありません。

## `src/components/atoms/forge-status-icon/forge-status-icon`

### 鍛造ステータスアイコン

**種類:** 関数

```typescript
function ForgeStatusIcon(properties: Readonly<StatusIconProperties>): MpElement;
```

説明はありません。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<StatusIconProperties> |      |

### ステータスアイコンレベル

**種類：**タイプ

```typescript
export type StatusIconLevel =
  'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

アイコンによって伝えられるステータス/トーン — 正規のカラーセット。

### ステータスアイコンのプロパティ

**種類:** インターフェース

```typescript
export interface StatusIconProperties
```

説明はありません。

### ステータスアイコンのサイズ

**種類：**タイプ

```typescript
export type StatusIconSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の2xs→2xlサイズスケール。

## `src/components/molecules/forge-accordion/forge-accordion`

### アコーディオンアイテム

**種類:** インターフェース

```typescript
export interface AccordionItem
```

単一の折りたたみ可能な行記述子。

### AccordionItemScope

**種類:** インターフェース

```typescript
export interface AccordionItemScope
```

スコープ指定された `summary`/`content` スロットに渡されるスコープ。

### アコーディオンのプロパティ

**種類:** インターフェース

```typescript
export interface AccordionProperties
```

説明はありません。

### アコーディオンを鍛造する

**種類:** 関数

```typescript
function ForgeAccordion(properties: Readonly<AccordionProperties>): MpElement;
```

`ForgeAccordion` — 作成された折りたたみ可能な行の垂直方向に積み重ねられたコンテナー
一度ニュートラルな JSX 方言で変換し、直接 React または Vue にコンパイルします。
`@mission-platform/vite-plugin-forge`。

元の Vue ペア (`ForgeAccordion` + `ForgeAccordionItem`) 共有オープン
`provide`/`inject` までの状態。ニュートラル バージョンは**平坦化**します。
`items` 配列によって駆動される単一コンポーネントへの合成 - 同じ
移行された {@link ForgeTabs} が採用したアプローチ - オープン状態を所有します
内部的には `useState` 配列を使用します (したがってストアは必要ありません)。 `exclusive` は保持します
単一のアイテムが開いています。一度に複数を許可するには、`false` に設定します。それはそれを所有しています
同じ場所にある CSS モジュール `forge-accordion.module.scss` を介したスタイリング。

元の SFC からの置換: `provide`/`inject` が内部になります
`useState`;山形は追記型 `@mission-platform/icons` です
`ForgeIconChevron` (`direction` プロパティを介して回転され、それ自体は次のようにコンパイルされます)
React/Vue);アイテムごとの `summary`/デフォルト スロットは 2 つのスコープ付きスロットになります
アイテムの `title`/`content` テキストにフォールバックします。

#### パラメータ

| 名前       | タイプ                            | 説明 |
| ---------- | --------------------------------- | ---- |
| プロパティ | 読み取り専用<AccordionProperties> |      |

## `src/components/molecules/forge-breadcrumb/forge-breadcrumb`

### ブレッドクラム項目

**種類:** インターフェース

```typescript
export interface BreadcrumbItem
```

単一のブレッドクラム トレイル エントリ。

### ブレッドクラムのプロパティ

**種類:** インターフェース

```typescript
export interface BreadcrumbProperties
```

説明はありません。

### 鍛造ブレッドクラム

**種類:** 関数

```typescript
function ForgeBreadcrumb(properties: Readonly<BreadcrumbProperties>): MpElement;
```

`ForgeBreadcrumb` — ニュートラルな JSX 方言で一度作成されたブレッドクラム トレイル
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

これは、ラベル付きの `<nav>` 内に順序付きリストをレンダリングします。
`href` が付いている最後のエントリはリンクであり、最後のエントリは現在のページです
(`aria-current="page"`)、および構成可能な `separator` が間に描画されます。
エントリ。すべてのパンくず（リンクまたは現在のパン粉）は、トレイル独自のフォントを継承します。
`<nav>` 上の `forge-size--*` 修飾子、つまり現在のページは次のようになります。
タイポグラフィーは兄弟と一致しており、色のみが異なります。それは所有しています
同じ場所に配置された CSS モジュール `forge-breadcrumb.module.scss` によるスタイリング。

元の Vue SFC は、`RouterLink` 経由で `vue-router` `to` ターゲットをサポートしていました。の
中立バージョンは、プレーン `<a href>` (確立されたルーター) をレンダリングします。
置換)、`href` リンク フォームのみが引き継がれます。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<BreadcrumbProperties> |      |

## `src/components/molecules/forge-button-group/forge-button-group`

### ボタングループギャップ

**種類：**タイプ

```typescript
export type ButtonGroupGap = 'none' | 'xs' | 'sm' | 'md';
```

切り離されたボタン間の間隔。

### ボタングループの方向

**種類：**タイプ

```typescript
export type ButtonGroupOrientation = 'horizontal' | 'vertical';
```

グループ化されたボタンの配置方向。

### ボタングループのプロパティ

**種類:** インターフェース

```typescript
export interface ButtonGroupProperties
```

説明はありません。

### フォージボタングループ

**種類:** 関数

```typescript
function ForgeButtonGroup(properties: Readonly<ButtonGroupProperties>): MpElement;
```

`ForgeButtonGroup` — 関連するボタンを単一の視覚単位にグループ化します。執筆済み
一度はニュートラルな JSX 方言で、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

デフォルトのスロットの子 (通常は `Button` / `IconButton`) をラップします。
インスタンス) フレックスコンテナ内。 `attached` が設定されている場合、子は次のようになります。
内側の境界線の半径とギャップを折りたたむことで視覚的に結合され、グループが読み取れるようになります。
単一のセグメント化されたコントロールとして。 `role="group"` を公開します。 `ariaLabel` をに渡します
グループについて説明します。

同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-button-group.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<ButtonGroupProperties> |      |

## `src/components/molecules/forge-card/forge-card`

### カードパディング

**種類：**タイプ

```typescript
export type CardPadding = 'none' | 'sm' | 'md' | 'lg';
```

カード領域の内側のパディングスケール。

### カードのプロパティ

**種類:** インターフェース

```typescript
export interface CardProperties
```

説明はありません。

### フォージカード

**種類:** 関数

```typescript
function ForgeCard(properties: Readonly<CardProperties>): MpElement;
```

`ForgeCard` — オプションのヘッダー/フッター領域を備えたサーフェス コンテナー (作成済み)
一度はニュートラルな JSX 方言で、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

カードはインライン サイズのコンテナであるため、そのパディングはカードの
自分の幅。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-card.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

本体はデフォルトのスロットです。オプションの `header` および `footer` 名前付きスロット
それぞれは、スロットが埋まっており、次のように検出された場合にのみ、境界領域をレンダリングします。
フレームワークに依存しない {@link hasSlot} ヘルパー (Vue の `$slots` / にコンパイルされる)
React の prop-presence チェック）により、両方で存在検出が同様に機能します。
フレームワーク。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<CardProperties> |      |

## `src/components/molecules/forge-chat-bubble/forge-chat-bubble`

### チャットバブルのプロパティ

**種類:** インターフェース

```typescript
export interface ChatBubbleProperties
```

説明はありません。

### チャットバブルサイド

**種類：**タイプ

```typescript
export type ChatBubbleSide = 'start' | 'end';
```

バブルが会話のどちら側に固定されているか。

### チャットバブルバリアント

**種類：**タイプ

```typescript
export type ChatBubbleVariant = 'default' | 'primary';
```

バブルカラートリートメント。

### フォージチャットバブル

**種類:** 関数

```typescript
function ForgeChatBubble(properties: Readonly<ChatBubbleProperties>): MpElement;
```

`ForgeChatBubble` — ニュートラル JSX で一度作成された単一のメッセージ バブル
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

メッセージ本文を保持する吹き出しの横にオプションのアバターをレイアウトします。
(デフォルトのスロット)。 `side` はバブルを開始 (受信) または終了に固定します
会話の (発信)、`variant` は発信バブルに色を付けます。
オプションの `author`/`timestamp` はメタ ラインをレンダリングします。各バブルはセマンティックです
`<li>`。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-chat-bubble.module.scss` を使用し、中立的な {@link ForgeAvatar} を作成します
および {@link ForgeTypography}。

オリジナルの Vue SFC からの代替: `avatar`/`footer` SFC スロット
`avatarContent`/`footer` 名前付きスロット (`<Slot>`) になり、
フレームワークに依存しない {@link hasSlot} ヘルパーを通じて存在が検出されました。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<ChatBubbleProperties> |      |

## `src/components/molecules/forge-collapse/forge-collapse`

### Collapseプロパティ

**種類:** インターフェース

```typescript
export interface CollapseProperties
```

説明はありません。

### 鍛造崩壊

**種類:** 関数

```typescript
function ForgeCollapse(properties: Readonly<CollapseProperties>): MpElement;
```

`ForgeCollapse` — ネイティブの `<details>` ベースの開示。
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

クリック可能な概要テキストは `summary` プロパティから取得されます。体は
デフォルトのスロット。切り替えると、新しいオープン状態で `onToggle` コールバックが起動されます。
同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-collapse.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

オリジナルの Vue SFC は、`@mission-platform/icons` `ForgeIconChevron` を使用していました。
`toggle` は発行し、`summary` スロットはオーバーライドします。ニュートラルバージョンでは、
ライトワンス `@mission-platform/icons` `ForgeIconChevron` は純粋にローテーションされます。
ネイティブ `[open]` 属性 (JS オープン状態は必要ありません)、クロスフレームワーク
`onToggle` コールバック、およびプレーン `summary` テキスト プロップ (スロット オーバーライドは
ドロップされました。これは、他の移行されたコンポーネントがスロットをドロップした方法と一致しています。
同じ名前のプロップと衝突します)。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<CollapseProperties> |      |

## `src/components/molecules/forge-device-mock/forge-device-mock`

### デバイスモックの向き

**種類：**タイプ

```typescript
export type DeviceMockOrientation = 'portrait' | 'landscape';
```

ハンドヘルド フレームの方向 (`mobile`/`tablet`)。 `desktop`/`browser` では無視されます。

### デバイスモックのプロパティ

**種類:** インターフェース

```typescript
export interface DeviceMockProperties
```

説明はありません。

### デバイスモックサイズ

**種類：**タイプ

```typescript
export type DeviceMockSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

サイズ トークン — 正規の 2xs → 2xl スケール。 `em` の相対寸法を使用してフレーム全体をスケーリングします。

### デバイスモックタイプ

**種類：**タイプ

```typescript
export type DeviceMockType = 'mobile' | 'tablet' | 'desktop' | 'browser';
```

画面コンテンツが内部に表示されるデバイスフレーム。

### フォージデバイスモック

**種類:** 関数

```typescript
function ForgeDeviceMock(properties: Readonly<DeviceMockProperties>): MpElement;
```

`ForgeDeviceMock` — 任意の画面を包み込む装飾的なデバイス フレーム
`mobile`、`tablet`、`desktop`、または `browser` のコンテンツ (デフォルトのスロット)
chrome、ニュートラルな JSX 方言で一度作成されました (`@mission-platform/forge`)
そして、直接ReactまたはVueにコンパイルされます。
`@mission-platform/vite-plugin-forge`。

フレームは完全に CSS デザイン トークンで描画され、`em` 単位でサイズ設定されます。
そのため、共有 `size` トークン (`2xs … 2xl`) によってデバイス全体がスケーリングされます。の
ハンドヘルド フレーム (`mobile`/`tablet`) は `orientation` プロップを尊重します。の
`browser` フレームは、交通信号コントロールと
`url` アドレス バー。デフォルトのスロットがデバイスの画面に投影されます。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<DeviceMockProperties> |      |

## `src/components/molecules/forge-grid/forge-grid`

### フォージグリッド

**種類:** 関数

```typescript
function ForgeGrid(properties: Readonly<GridProperties>): MpElement;
```

`ForgeGrid` — コンテンツを配置する CSS グリッド レイアウト プリミティブ
`rows` (m) と `cols` (n) のグリッド。構成可能な `gap` (ごとにオーバーライド可能)
`rowGap` / `columnGap` 経由の軸）。一度はニュートラルな JSX 方言で作成されました
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

デフォルトのスロットを通じて提供された子は、ソース内のグリッド セルに流れ込みます。
標準の `grid-column` / を使用すると、複数のトラックにまたがる場合があります。
`grid-row` `span` CSS。オリジナルの Vue とは異なり、スコープ付きの
`cell` スロット - 完全に設定されたモードは、Vue のスコープ付きスロットにマップされます。
中立的な方言はモデル化されません。

BEM クラスと計算されたグリッド インライン スタイルを出力します。デモのスタイリング
同じ場所にある `forge-grid.module.scss` に住んでいます (輸入元
`forge-grid.stories.tsx`)。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<GridProperties> |      |

### グリッドアラインメント

**種類：**タイプ

```typescript
export type GridAlignment = 'start' | 'center' | 'end' | 'stretch';
```

インライン (`justify`) 軸とブロック (`align`) 軸の両方の項目配置キーワード。

### グリッドギャップ

**種類：**タイプ

```typescript
export type GridGap = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

名前付きギャップスケール。各ステップは `--mp-spacing-*` デザイン トークンに解決されます。

### グリッドプロパティ

**種類:** インターフェース

```typescript
export interface GridProperties
```

説明はありません。

## `src/components/molecules/forge-in-view/forge-in-view`

### ForgeInView

**種類:** 関数

```typescript
function ForgeInView(properties: Readonly<InViewProperties>): MpElement;
```

`ForgeInView` — コンテンツをラップし、`IntersectionObserver` を使用してそれを表示します。
スクロールして表示されるアニメーションは設定可能です。で一度執筆されました
中立的な JSX 方言。フレームワーク間の状態/ライフサイクルは、
ニュートラル フック (`useState`/`useRef`/`useEffect`)。React フックにコンパイルされます。
または、`@mission-platform/vite-plugin-forge` 経由の Vue フック シム。

注: オリジナルの Vue とは異なり、スコープ付きスロットを公開しません - 状態を公開します
純粋にラッパーのアニメーション スタイルを通じて伝えられます。
`onEnter`/`onLeave` コールバック。両方のフレームワークにきれいにマップされます。

#### パラメータ

| 名前       | タイプ                         | 説明 |
| ---------- | ------------------------------ | ---- |
| プロパティ | 読み取り専用<InViewProperties> |      |

### インビューアニメーション

**種類：**タイプ

```typescript
export type InViewAnimation = 'fade' | 'slide-up' | 'slide-left' | 'slide-right' | 'scale' | 'none';
```

説明はありません。

### InViewプロパティ

**種類:** インターフェース

```typescript
export interface InViewProperties
```

説明はありません。

## `src/components/molecules/forge-list/forge-list`

### フォージリスト

**種類:** 関数

```typescript
function ForgeList(properties: Readonly<ListProperties>): MpElement;
```

`ForgeList` — 順序付けされた/順序付けされていない/説明/単純なリスト。
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

提供された `items` を適切なセマンティック要素 (`ul` /
`ol` / `dl`)、合成されたニュートラルを通じてレンダリングされた各行のテキスト
{@link ForgeTypography};デフォルトスロットの子はすべて、
生成された行。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-list.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

元の Vue SFC は、行ごとにスコープ指定された `item` スロット (
`item`/`index` バインディング) 生成された行をオーバーライドします。ニュートラルバージョン
そのスコープ付きスロットを削除します — 行は `items` によって駆動されます — の方法と一致しています
他の移行されたコンポーネントはスコープ付きスロットを削除しました。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<ListProperties> |      |

### リストアイテム

**種類:** インターフェース

```typescript
export interface ListItem
```

単一のリスト エントリ。 `label` は、順序付き/順序なし/なしの行をドライブします。 `term`/`content` ドライブの説明行。

### リストプロパティ

**種類:** インターフェース

```typescript
export interface ListProperties
```

説明はありません。

### リストサイズ

**種類：**タイプ

```typescript
export type ListSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の2xs→2xlサイズスケール。

### リストバリアント

**種類：**タイプ

```typescript
export type ListVariant = 'unordered' | 'ordered' | 'description' | 'none';
```

意味論的/視覚的なリスト スタイル。

## `src/components/molecules/forge-masonry/forge-masonry`

### フォージメイソンリー

**種類:** 関数

```typescript
function ForgeMasonry(properties: Readonly<MasonryProperties>): MpElement;
```

`ForgeMasonry` — 一度作成された CSS 複数列石積みレイアウト プリミティブ
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

デフォルトスロットの子をバランスのとれた列に流し込みます。
自然な高さを保ち、上から下までしっかりと詰め込みます。固定を提供する
`columns` の数、または同じ数の列に適合するように `minColumnWidth` を設定します。
少なくともその幅が適合します (`minColumnWidth` が勝ちます)。コンテナが露出する
`--mp-masonry-gap` カスタム プロパティとしてのギャップと、オリジナルの Vue と同様に、
すべてのデフォルトスロットの子を自動的にブレークセーフに保ちます。 Vue とは異なります。
オリジナルでは、`items` prop / スコープ付き `item` スロットを公開しません。これはマップされます。
Vue のスコープ付きスロットに接続します。

コンテナボックスと、同じ場所に設置された子供ごとの安全装置を備えています。
CSS モジュール `forge-masonry.module.scss` (すべてのフレームワークに引き継がれます)
2 段階のコンパイラであるため、コンポーネントには独自の `@layer mp.components` が同梱されます。
CSS);動的な複数列プロパティはインラインで適用されます。

#### パラメータ

| 名前       | タイプ                          | 説明 |
| ---------- | ------------------------------- | ---- |
| プロパティ | 読み取り専用<MasonryProperties> |      |

### 石積みギャップ

**種類：**タイプ

```typescript
export type MasonryGap = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

名前付きギャップスケール。各ステップは `--mp-spacing-*` デザイン トークンに解決されます。

### 石積みのプロパティ

**種類:** インターフェース

```typescript
export interface MasonryProperties
```

説明はありません。

## `src/components/molecules/forge-menu-item/forge-menu-item`

### 鍛造メニュー項目

**種類:** 関数

```typescript
function ForgeMenuItem(properties: Readonly<MenuItemProperties>): MpElement;
```

`ForgeMenuItem` — ニュートラルな JSX 方言で一度作成された単一のメニュー エントリ
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`role="menuitem"` 要素を含む `<li role="none">` をレンダリングします。
`href` が設定され、項目が有効な場合はリンク (`<a href>`)、それ以外の場合はリンク (`<a href>`)
クリックまたは Enter/Space で `onClick` を起動する、アクティブ化可能な `<span>`。ラベル
デフォルトのスロットから取得されます（合成されたスロットにフォールバックします）
`label` の {@link ForgeTypography} レンダリング）、先頭にオプションの `icon` を付けます
スロット。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-menu-item.module.scss`。

元の Vue SFC は、`RouterLink` 経由で `vue-router` `to` ターゲットをサポートし、
`click` が発行します。ニュートラル バージョンでは、プレーンな `<a href>` がレンダリングされます (
確立されたルーター置換)、`onClick` コールバック プロパティを公開します。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<MenuItemProperties> |      |

### メニュー項目のプロパティ

**種類:** インターフェース

```typescript
export interface MenuItemProperties
```

説明はありません。

### メニュー項目バリアント

**種類：**タイプ

```typescript
export type MenuItemVariant =
  'default' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'information' | 'error' | 'critical';
```

メニュー項目に適用されるトーン。

## `src/components/molecules/forge-menu/forge-menu`

### フォージメニュー

**種類:** 関数

```typescript
function ForgeMenu(properties: Readonly<MenuProperties>): MpElement;
```

`ForgeMenu` — ニュートラルな JSX 方言で一度作成されたアクセス可能なメニュー
`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

これはアイテムの `role="menubar"` をレンダリングします。 `children` の項目は、
**任意の深さ**にネストできる拡張可能なサブメニュー (`renderItems` ウォーク)
各レベルを再帰します)、`href` を持つリーフはリンクとしてレンダリングされ、
他のリーフは `onClick` を起動します。オリジナルの Vue と同様に、サブメニューは 1 つだけです。
レベルごとに開きます (兄弟を開くと前の兄弟が閉じます)。
チェーンは開いたままです。外側をクリックするか、`Escape` を押すと、開くたびに閉じます
サブメニュー (ニュートラル `useEffect` 経由でアタッチされたドキュメント リスナー)。それはそれを所有しています
同じ場所にある CSS モジュール `forge-menu.module.scss` を介したスタイリング。

オリジナルの Vue SFC は `ForgeMenuList`/`ForgeMenuItemButton`/ で構成されました
`ForgeMenuItemLink`/`ForgeMenuSubmenu` (それ自体に再帰するサブメニュー)。の
中立バージョンは、その任意の深さの再帰を 1 つの関数で再現します。
`renderItems(items, parentPath)` ウォークと単一のパスキー付き `openPath`
`useState` (例: `'1.0.2'`)、同じレベルごとに 1 つオープンするセマンティクスを維持します。それ
ライトワンス `@mission-platform/icons` `ForgeIconChevron` をレンダリングします (経由で回転)
`direction` プロップ）とプレーン `<a href>`（確立された `vue-router`
置換)、それ以外の場合は、Vue マークアップの役割/ARIA と一致します。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<MenuProperties> |      |

### メニューノード

**種類:** インターフェース

```typescript
export interface MenuNode
```

単一のメニューエントリ。 1 つのレベルの `children` (サブメニュー) を含めることができます。

### メニューの方向

**種類：**タイプ

```typescript
export type MenuOrientation = 'vertical' | 'horizontal';
```

メニューリストの向き。

### メニュープロパティ

**種類:** インターフェース

```typescript
export interface MenuProperties
```

説明はありません。

## `src/components/molecules/forge-navbar-item/forge-navbar-item`

### NavbarItem を鍛造する

**種類:** 関数

```typescript
function ForgeNavbarItem(properties: Readonly<NavbarItemProperties>): MpElement;
```

`ForgeNavbarItem` — ニュートラルな JSX 方言で一度作成された navbar エントリ
`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`dropdownItems` がない場合、単一のアイテム、つまりリンク (`<a href>`) がレンダリングされます。
有効であり、`href` が設定されています。それ以外の場合は、アクティブ化可能な `<button>` が起動されます。
`onClick`。 `dropdownItems` を使用すると、ドロップダウン トリガーと
`role="menu"` パネル;クリックする
外側にいるか、`Escape` を押すと閉じます。ラベルはデフォルトのスロットから取得されます
(`label` にフォールバック) オプションの先頭の `icon` スロットを使用します。それはそれを所有しています
同じ場所にある CSS モジュール `forge-navbar-item.module.scss` を介したスタイリング。

元の Vue SFC はフローティング ドロップダウンを使用していました
(テレポート + `@floating-ui` 位置決め)、動的 `<component :is>` タグ、
`vue-router` `RouterLink`、`@mission-platform/icons` `ForgeIconChevron`、および
`click` が発行します。ニュートラル バージョンでは **動的タグ** (子なし) が維持されます。
項目はニュートラル `<Dynamic is={tag}>` プリミティブ (次のようにコンパイルされます) を通じてレンダリングされます。
React の要素タイプ / Vue の `<component :is>`)、`tag` は `'a'` に解決されます
(enabled + `href`) または `'button'` — インラインを置き換えます。
絶対配置ドロップダウン (`useState` で開いた状態、経由で閉じた状態)
`useEffect` ドキュメント リスナー)、プレーン `<a>` を含む `RouterLink` ターゲット、
追記型 `@mission-platform/icons` `ForgeIconChevron` (その
独自の `direction` プロパティ)、および `onClick` コールバック プロパティ。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<NavbarItemProperties> |      |

### NavbarItemChild

**種類:** インターフェース

```typescript
export interface NavbarItemChild
```

単一のドロップダウン子エントリ。

### NavbarItemProperties

**種類:** インターフェース

```typescript
export interface NavbarItemProperties
```

説明はありません。

### ナビゲーションバー項目バリアント

**種類：**タイプ

```typescript
export type NavbarItemVariant =
  'default' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'information' | 'error' | 'critical';
```

ナビゲーションバー項目に適用されるトーン。

## `src/components/molecules/forge-pagination/forge-pagination`

### Forgeページネーション

**種類:** 関数

```typescript
function ForgePagination(properties: Readonly<PaginationProperties>): MpElement;
```

`ForgePagination` — ニュートラル JSX で一度作成されたページ ナビゲーション コントロール
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

オプションの最初/前/次/最後を含むページ ボタンのリストをレンダリングします。
コントロールと、ページ数が多い場合の MUI スタイルの切り捨て省略記号。現在の
ページは `modelValue` によって **制御**されています。ユーザーインタラクションは両方を起動します
`onUpdateModelValue` (`v-model` アップデート) および `onChange`。どちらかを提供してください
`pageCount` を直接指定するか、`total` + `pageSize` を指定すると、ページ数が導出されます。
同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-pagination.module.scss`。

オリジナルの Vue SFC は、`v-model` + `change` の発行を使用していました。ニュートラルバージョン
確立された制御された `modelValue` + callback-prop 規則を使用します。

#### パラメータ

| 名前       | タイプ                             | 説明 |
| ---------- | ---------------------------------- | ---- |
| プロパティ | 読み取り専用<PaginationProperties> |      |

### ページネーション項目

**種類：**タイプ

```typescript
export type PaginationItem = number | 'ellipsis';
```

レンダリングされたページネーション項目: ページ番号または切り捨て省略記号。

### ページネーションプロパティ

**種類:** インターフェース

```typescript
export interface PaginationProperties
```

説明はありません。

### ページネーションサイズ

**種類：**タイプ

```typescript
export type PaginationSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

ボタンの寸法を制御するサイズ トークン — 正規の 2xs → 2xl スケール。

## `src/components/molecules/forge-tabs/forge-tabs`

### タブを鍛造する

**種類:** 関数

```typescript
function ForgeTabs(properties: Readonly<TabsProperties>): MpElement;
```

`ForgeTabs` — で一度作成された、アクセス可能で制御されたタブ コンテナ
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

タブ ボタンの ARIA `tablist` をレンダリングします (ラベルは移行された
{@link ForgeTypography}) ロービング `tabindex` + 矢印/ホーム/エンド キーボード
ナビゲーション、オプションのタブごとのクローズ アフォーダンス、およびオプションの末尾の追加
(`+`) ボタン。アクティブなタブのコンテンツは、スコープ指定された `panel` を通じてレンダリングされます。
**レンダープロップ**。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-tabs.module.scss`。

オリジナルの Vue SFC は `ForgeTabList`/`ForgeTab`/`ForgeTabPanel` で構成されました
サブコンポーネント、追加/閉じるグリフに `@mission-platform/icons` を使用、駆動
タブ ID ごとの名前付きスロットを介してパネルのコンテンツを取得し、`v-model` + を使用して出力します。の
ニュートラル バージョンはタブ バーをインライン化します (`ForgeTable`/ の方法と一致します)
`ForgeVirtualTable` はサブコンポーネントをインライン化し、ライトワンスをレンダリングします
`@mission-platform/icons` `ForgeIconPlus`/`ForgeIconClose` (追加/クローズ用)
アフォーダンス。オリジナルの Vue と同様に、`role="tabpanel"` をレンダリングします。
**すべて**のタブに対して非アクティブなパネルをマウントしたままにしますが、`hidden` (したがってパネル
状態はタブ切り替え後に存続します);中立的な方言では Vue を表現できないため、
ID ごとの動的なスロット名 (`<slot :name="tab.id">`)、各パネルは
単一スコープの `panel` **render-prop** と `{ tab }` (コンシューマのスイッチがオン)
`tab.id`)。 `panel` render-prop は (コマンド経由ではなく) 直接呼び出されます。
ニュートラル `<Slot>`) なので、両方のフレームワークとニュートラルの実際のプロップのままです。
消費者はそれをプレーン属性 (`panel={({ tab }) => …}`) — Vue として渡すことができます。
`<Slot>` でサポートされた名前付きスロットは、からプロップとして渡されるとドロップされます。
コンパイルされたニュートラル親。パネルは空白のままです。確立されたものを使用します
`modelValue` + コールバックプロパティ規約。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<TabsProperties> |      |

### タブ項目

**種類:** インターフェース

```typescript
export interface TabItem
```

単一のタブ記述子。

### タブパネルスコープ

**種類:** インターフェース

```typescript
export interface TabPanelScope
```

`panel` render-prop に渡されるスコープ。

### タブのプロパティ

**種類:** インターフェース

```typescript
export interface TabsProperties
```

説明はありません。

### タブバリアント

**種類：**タイプ

```typescript
export type TabsVariant = 'line' | 'pill';
```

タブリストの視覚的な処理。

## `src/components/molecules/forge-timeline/forge-timeline`

### フォージタイムライン

**種類:** 関数

```typescript
function ForgeTimeline(properties: Readonly<TimelineProperties>): MpElement;
```

`ForgeTimeline` — 一度作成されたイベントの順序付けされた時系列リスト。
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

元の Vue ペア (`ForgeTimeline` + `ForgeTimelineItem`) は、
`provide`/`inject` コンテキストによる方向性/交互リズム。の
ニュートラル バージョンは、その構成を **平坦化**して、単一のコンポーネント主導型にします
`items` 配列による — 移行された {@link ForgeTabs} が採用したのと同じアプローチ —
そのため、レイアウト状態は単純に props から各 `<li>` に流れます (コンテキストはありません)。
必要です）。同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-timeline.module.scss`、元のコンテナとアイテムをマージします
スタイル。

アイテムごとの `marker`/`time`/`title`/デフォルト スロットは 4 つのスコープ付きスロットになります
(`marker`/`time`/`title`/`content`) アイテムのフィールドにフォールバックします。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<TimelineProperties> |      |

### タイムライン整列

**種類：**タイプ

```typescript
export type TimelineAlign = 'start' | 'alternate';
```

垂直タイムライン項目が線に対してどのように配置されるか。

### タイムラインアイテム

**種類:** インターフェース

```typescript
export interface TimelineItem
```

{@link ForgeTimeline} 内の単一のイベント記述子。

### タイムラインアイテムスコープ

**種類:** インターフェース

```typescript
export interface TimelineItemScope
```

スコープ付きアイテム スロットに渡されるスコープ。

### タイムライン項目バリアント

**種類：**タイプ

```typescript
export type TimelineItemVariant =
  'default' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'information' | 'error' | 'critical';
```

マーカー ドットに適用されるカラー ファミリ。

### タイムラインの向き

**種類：**タイプ

```typescript
export type TimelineOrientation = 'vertical' | 'horizontal';
```

タイムラインの接続線の方向。

### タイムラインのプロパティ

**種類:** インターフェース

```typescript
export interface TimelineProperties
```

説明はありません。

## `src/components/molecules/forge-window-popout/forge-window-popout`

### フォージウィンドウポップアウト

**種類:** 関数

```typescript
function ForgeWindowPopout(properties: Readonly<WindowPopoutProperties>): MpElement;
```

`ForgeWindowPopout` — コンテンツを別のブラウザ ウィンドウにポップアウトします。
ニュートラルな JSX 方言で一度作成され、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge` 著。

インラインでは、デフォルトのスロットのコンテンツとトグル ボタンがレンダリングされます。ポップしたとき
実際の 2 番目のウィンドウ (`window.open`) が開き、ページはめ込みが表示されます。
`placeholder`、および `onOpen` / `onClose` コールバックを介して状態をレポートします
props (元の SFC のエミットを置き換えます)。中立的な `useState` / `useRef`
/ `useEffect` フックはウィンドウのライフサイクルを駆動します (オープン、外部クローズのポーリング、
両方のフレームワークでクリーンアップをアンマウントします)。

同じ場所にある `forge-window-popout.module.scss` を通じてそのスタイルを所有しています。
オリジナルの Vue SFC と比較して、中立方言はどちらのモデルもモデル化しません
`<Teleport>`、スコープ付きスロットは、`useI18n`、または `@mission-platform/icons` を発行します。
ポップアップされたウィンドウは、インラインの **静的な HTML スナップショット**を受け取ります。
コンテンツ (ライブ フレームワーク ポータルではない)、スコープ指定された `controls` スロット、および
ポップイン/アウト アイコンは削除され、ページはめ込み `placeholder` は `placeholder` になります。
名前付きスロット (`<Slot>`、フォールバックとしてデフォルト メッセージを使用)、およびラベル
プレーンな小道具です。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<WindowPopoutProperties> |      |

### ウィンドウポップアウトプロパティ

**種類:** インターフェース

```typescript
export interface WindowPopoutProperties
```

説明はありません。

## `src/components/organisms/forge-carousel/forge-carousel`

### カルーセルのプロパティ

**種類:** インターフェース

```typescript
export interface CarouselProperties
```

説明はありません。

### カルーセルスライド

**種類:** インターフェース

```typescript
export interface CarouselSlide
```

単一のスライド記述子。

### カルーセルスライドスコープ

**種類:** インターフェース

```typescript
export interface CarouselSlideScope
```

スコープ指定された `slide` スロットに渡されるスコープ。

### フォージカルーセル

**種類:** 関数

```typescript
function ForgeCarousel(properties: Readonly<CarouselProperties>): MpElement;
```

`ForgeCarousel` — 水平スクロール可能なスライドデッキ。
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

オプションの前/次コントロール、インジケーターを使用して、一度に 1 つのスライドを表示します。
ドット、キーボード (矢印/ホーム/エンド) ナビゲーション、ポインター スワイプ、およびオプション
WCAG 2.2.2 一時停止/再生コントロールによる自動再生。スタイルを確立するのは、
CSS モジュール `forge-carousel.module.scss` が同じ場所にあります。

オリジナルの Vue SFC からの代替: スライドはデフォルトのスロットから取得されました
そして、その数はスロット VNode をイントロスペクトすることによって導出されました。これはニュートラルです。
方言ではそれができないため、スライドは `slides` 配列によって駆動されます (
カスタム コンテンツ用のスコープ付き `slide` スロット）、移行されたものと同じフラット化
{@link ForgeTabs} が使用されます。構成 API `ref`/`watch`/`onBeforeUnmount`
自動再生配管は、`useState` + `useEffect` 間隔になります (
アクティブなインデックスなので、古いクロージャーなしで進みます)。 `useReducedMotion`
コンポーザブルはエフェクト内でインライン `matchMedia` チェックになります。ポインタ
開始座標は `useRef` になります。 `defineExpose` は削除されます。そして
`v-model` + エミットはコールバック プロパティになります。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<CarouselProperties> |      |

## `src/components/organisms/forge-chat-area/forge-chat-area`

### チャットエリアのプロパティ

**種類:** インターフェース

```typescript
export interface ChatAreaProperties
```

説明はありません。

### フォージチャットエリア

**種類:** 関数

```typescript
function ForgeChatArea(properties: Readonly<ChatAreaProperties>): MpElement;
```

`ForgeChatArea` — で一度作成されたスクロール可能な会話サーフェス
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`。

チャットの周囲にクロムを提供します: オプションの `header` スロット、スクロール可能なスロット
`aria-live="polite"` `role="log"` メッセージ リスト (デフォルトのスロット - 通常、
`<li>` としてレンダリングされる {@link ForgeChatBubble} のスタック）、およびオプション
`footer` 作曲家用スロット。 `autoScroll` が有効 (デフォルト) の場合、
ログはマウント時に最新のメッセージに固定され、その内容が増加するにつれて、
ユーザーが履歴を読むために上にスクロールしている間、一時停止します。スタイルを所有するのは、
同じ場所にある CSS モジュール `forge-chat-area.module.scss`。

オリジナルの Vue SFC からの置換:Composition-API `onMounted` +
`watch` + `ResizeObserver` 配管は単一の中性 `useEffect` になります
(コンパイラは各フレームワークのマウント/クリーンアップ ライフサイクルにマップします)、および
変更可能な「下に固定」フラグは `useRef` になります (したがって、更新は行われません)
再レンダリング)。 SFC の `defineExpose({ scrollToBottom })` 命令型ハンドルは
中立的な方言は公開されたインスタンス メソッドをモデル化していないため、削除されました。
自動スクロールは一般的なケースをカバーします。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<ChatAreaProperties> |      |

## `src/components/organisms/forge-drawer/forge-drawer`

### ドロワーブレークポイント

**種類：**タイプ

```typescript
export type DrawerBreakpoint = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
```

`inline` ドロワーが固定オープンでレンダリングされる名前付きビューポート ブレークポイント。

### 引き出しドラッグ可能

**種類：**タイプ

```typescript
export type DrawerDraggable = boolean | DrawerSize | number;
```

引き出しの内側の端をドラッグしてサイズを変更できるかどうかを制御します。
`false` (デフォルト、固定サイズ)、`true` (ビューポート全体までサイズ変更可能)
関連する軸）、{@link DrawerSize}（そのサイズの最大値までサイズ変更可能）、または
`number` (`rem` までサイズ変更可能)。

### 引き出しの配置

**種類：**タイプ

```typescript
export type DrawerPlacement = 'start' | 'end' | 'top' | 'bottom';
```

引き出しが固定されているビューポートの端。

### ドロワーのプロパティ

**種類:** インターフェース

```typescript
export interface DrawerProperties
```

説明はありません。

### 引き出しのサイズ

**種類：**タイプ

```typescript
export type DrawerSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の名前付きサイズ スケール (開始/終了の幅、上部/下部の高さ)。

### 引き出しのバリアント

**種類：**タイプ

```typescript
export type DrawerVariant = 'overlay' | 'inline';
```

表示動作 - フローティング `overlay` (デフォルト) またはレスポンシブ `inline` パネル。

### フォージドロワー

**種類:** 関数

```typescript
function ForgeDrawer(properties: Readonly<DrawerProperties>): MpElement;
```

`ForgeDrawer` — ニュートラルな JSX 方言で一度作成されたスライディング パネル
`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`overlay` ドロワー (デフォルト) は、ドロワーに固定された `position: fixed` パネルです。
ビューポート エッジ (`placement`)、`open` によってゲートされ、クリックして閉じることでサポートされます
スクリム、オリジナルの Vue SFC と同じエンター/リーブ フェード + スライド
ニュートラル `<Transition>` プリミティブ。 `inline` ドロワーは静的としてレンダリングされます。
`inlineBreakpoint` 以降の固定オープン パネル (リアクティブ `matchMedia` クエリ)
そして、その下のオーバーレイ動作にフォールバックします — レイアウト プリミティブによって使用されます
`ForgeVerticalLayout` など。

`draggable` が設定されている場合、内側のエッジにサイズ変更ハンドルが成長します。ハンドルをドラッグします。
(共有 `pointer-drag` ヘルパーを介して `window` で追跡されるポインター ドラッグ)
`MIN_SIZE_REM` と解決された最大値の間でパネルのサイズを変更し、レポートします
`rem` から `onResize` までの新しいサイズ (Vue `resize` が発行)。

クロスフレームワーク `callback-prop` 規約に従って、終了までを報告します。
Vue または `v-model` ではなく `onOpenChange(false)` / `onClose()` が発行されます。それ
は、同じ場所にある `forge-drawer.module.scss` を通じてそのスタイルを所有しています。相対的に
オリジナルの Vue SFC では、中立方言はまだ `<Teleport>` をモデル化できません。
スコープ付きスロット、`useI18n`、`useZIndex`、または `useRouterClose` なので、パネル
`header`/`footer` という名前を使用して、その場でレンダリングします (`<body>` にはテレポートされません)。
スロット (`<Slot>`、フレームワークに依存しない {@link hasSlot} で存在が検出されました)
helper) と、i18n の代わりに `closeLabel` prop を使用します。

#### パラメータ

| 名前       | タイプ                         | 説明 |
| ---------- | ------------------------------ | ---- |
| プロパティ | 読み取り専用<DrawerProperties> |      |

## `src/components/organisms/forge-menubar/forge-menubar`

### フォージメニューバー

**種類:** 関数

```typescript
function ForgeMenubar(properties: Readonly<MenubarProperties>): MpElement;
```

`ForgeMenubar` — ニュートラルで一度作成された水平アプリケーション メニューバー
JSX 方言であり、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

水平方向の `role="menubar"` をレンダリングします。 `children` を持つアイテムは、
**任意の深さ**にネストするドロップダウン サブメニュー (`renderItems` ウォークは再帰的)
各レベルを通じて)、`href` を持つリーフはリンクとしてレンダリングされ、その他の
リーフは `onClick` を起動します。オリジナルの Vue と同様に、1 つにつき 1 つのサブメニューのみが開きます。
祖先チェーンが維持されている間、レベル (兄弟を開くと前の兄弟が閉じます)
開いた。 `items` が省略された場合、代わりにデフォルトのスロットがレンダリングされます。クリックする
外側にいるか、`Escape` を押すと、開いているすべてのドロップダウンが閉じます。独自のスタイルを持っています
同じ場所にある CSS モジュール `forge-menubar.module.scss` を介して。

オリジナルの Vue SFC は `ForgeMenuItemButton`/`ForgeMenuItemLink`/ を再利用しました
`ForgeMenuSubmenu` (それ自体を再帰するサブメニュー)。ニュートラルバージョン
任意の深さの再帰を単一の関数で再現します。
`renderItems(items, parentPath)` ウォークと単一のパスキー付き `openPath`
`useState`、同じレベルごとに 1 つオープンするセマンティクスを維持します。それは、
ライトワンス `@mission-platform/icons` `ForgeIconChevron` (その経由でローテーション)
`direction` プロップ) とプレーン `<a href>` (確立された `vue-router`
置換）。

#### パラメータ

| 名前       | タイプ                          | 説明 |
| ---------- | ------------------------------- | ---- |
| プロパティ | 読み取り専用<MenubarProperties> |      |

### メニューバーのプロパティ

**種類:** インターフェース

```typescript
export interface MenubarProperties
```

説明はありません。

## `src/components/organisms/forge-navbar/forge-navbar`

### フォージナビゲーションバー

**種類:** 関数

```typescript
function ForgeNavbar(properties: Readonly<NavbarProperties>): MpElement;
```

`ForgeNavbar` — ニュートラルで一度作成されたトップアプリケーションナビゲーションバー
JSX 方言であり、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

これは、`brand` スロット、中央のデフォルト スロット (ナビゲーション項目) をレイアウトします。
`end` スロット、モバイルでは折りたたまれます (構成可能な `mobileBreakpoint` の下、
デフォルトでは `'sm'`）を、{@link ForgeDrawer} を切り替えるハンバーガー ボタンに設定します。
デフォルト + `end` スロットをミラーリングします。崩壊は反応性によって引き起こされる
`matchMedia` クエリを実行すると、ドロワーのオープン状態はローカルに保持されます。
ニュートラル `useState` フック。

追記型の {@link ForgeDrawer} と {@link ForgeTypography} を構成し、所有しています
同じ場所にある `forge-navbar.module.scss` を通じてそのスタイルを取得し、そのスタイルを返します。
単一の `display: contents` ホスト下のヘッダー + ドロワー (中立的な方言)
マルチルート フラグメント リターンはありません)。

#### パラメータ

| 名前       | タイプ                         | 説明 |
| ---------- | ------------------------------ | ---- |
| プロパティ | 読み取り専用<NavbarProperties> |      |

### ナビゲーションバーの位置合わせ

**種類：**タイプ

```typescript
export type NavbarAlign = 'start' | 'center' | 'end';
```

デフォルトのスロット (中央) ナビゲーション項目の配置。

### ナビゲーションバーブレークポイント

**種類：**タイプ

```typescript
export type NavbarBreakpoint = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
```

名前付きビューポート ブレークポイント **その下**でナビゲーションバーが折りたたまれます。
ハンバーガー切り替えモバイルドロワー (その上/上に完全な `brand` /
中央/`end` 領域はインラインで表示されます)。

### ナビゲーションバーのプロパティ

**種類:** インターフェース

```typescript
export interface NavbarProperties
```

説明はありません。

## `src/components/organisms/forge-table/forge-table`

### フォージテーブル

**種類:** 関数

```typescript
function ForgeTable(properties: Readonly<TableProperties>): MpElement;
```

`ForgeTable` — ニュートラルな JSX 言語で一度作成された並べ替え可能なデータ テーブル
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`columns`/`rows` を、クリックして並べ替えるセマンティック `<table>` としてレンダリングします。
`onSort` コールバックを起動するヘッダー (asc → desc → unsorted を循環)、
オプションのキャプション、読み込み中のオーバーレイ、および空の状態。セルのテキストがレンダリングされます
構成されたニュートラルな {@link ForgeTypography} を通じて。ソート状態は保持されます
ニュートラルな {@link useState}/{@link useMemo} フック。独自のスタイルを持っています
同じ場所にある CSS モジュール `forge-table.module.scss` を通じて。

元の Vue SFC は行タイプに対して汎用であり、構成されていました。
`ForgeTableHead`/`ForgeTableBody` サブコンポーネント、`sort` 発行が使用され、公開されました
列ごとにスコープ指定された `cell-<key>` スロット。ニュートラルバージョンでは、
`Record<string, unknown>` 行、ヘッド/ボディをインライン化、`onSort` を使用
コールバックを呼び出し、各列のオプションを通じてセルごとのレンダリングを駆動します。
`render` フォーマッタ (スコープ指定されたセル スロットは削除されます) — 方法と一致しています
他の移行されたコンポーネントはスコープ付きスロットを削除しました。

#### パラメータ

| 名前       | タイプ                        | 説明 |
| ---------- | ----------------------------- | ---- |
| プロパティ | 読み取り専用<TableProperties> |      |

### 並べ替え方向

**種類：**タイプ

```typescript
export type SortDirection = 'asc' | 'desc';
```

並べ替え方向。未ソートは `undefined` で表されます。

### テーブル列

**種類:** インターフェース

```typescript
export interface TableColumn
```

単一列の定義。

### テーブルのプロパティ

**種類:** インターフェース

```typescript
export interface TableProperties
```

説明はありません。

## `src/components/organisms/forge-tree-view/forge-tree-view`

### フォージツリービュー

**種類:** 関数

```typescript
function ForgeTreeView(properties: Readonly<TreeViewProperties>): MpElement;
```

`ForgeTreeView` — ニュートラル JSX で一度作成された再帰的でアクセス可能なツリー
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。各 node は展開または折りたたむことができ、
クリックしました。オープン状態はニュートラルな {@link useState} フックで保持されます。
各ラベルは、スコープ指定された `label` スロットを通じてカスタマイズできます。
(`{ node, depth }`) これは node のテキストに戻ります。キーボードのサポート
オリジナルをミラーリングします (Enter/Space 選択、右/左矢印で展開/縮小)。
同じ場所に配置された CSS モジュール `forge-tree-view.module.scss` を通じてそのスタイルを所有します。

オリジナルの Vue SFC (再帰的構成を含む) のミラーリング
`ForgeTreeNode`/`ForgeTreeNodeLabel` サブコンポーネント)、JSX バージョンはレンダリングします
**真のネストされたマークアップ**: 各ルート node は再帰的な {@link ForgeTreeViewItem} です
（独自の兄弟コンポーネントに抽出され）その子をレンダリングします。
表示されているツリーを 1 つに平坦化するのではなく、ネストされた `<ul role="group">`
リスト。単一のルートレベル `openMap` (`node.id` によってキー設定されており、Vue とまったく同様)
ルート状態) はすべての node の展開状態を駆動し、すべてのアイテムと共有されます
`resolveOpen`/`selectNode`/`toggleNode`/`keyDownNode` プロパティを介して。の
スコープ指定された `label` スロットは変更されずに下方に転送され、
`aria-selected`/`aria-expanded` セマンティクスと `onSelect`/`onToggle`
それ以外の場合はコールバック プロパティが一致します。

#### パラメータ

| 名前       | タイプ                           | 説明 |
| ---------- | -------------------------------- | ---- |
| プロパティ | 読み取り専用<TreeViewProperties> |      |

### ツリービューラベルスコープ

**種類:** インターフェース

```typescript
export interface TreeViewLabelScope
```

node ごとに、{@link ForgeTreeView} の `label` (スコープ付き) スロットに渡されるスコープ。

### ツリービューノード

**種類:** インターフェース

```typescript
export interface TreeViewNode
```

{@link ForgeTreeView} によってレンダリングされたツリー内の node。

### ツリービューのプロパティ

**種類:** インターフェース

```typescript
export interface TreeViewProperties
```

説明はありません。

## `src/components/organisms/forge-virtual-list/forge-virtual-list`

### ForgeVirtualList

**種類:** 関数

```typescript
function ForgeVirtualList(properties: Readonly<VirtualListProperties>): MpElement;
```

`ForgeVirtualList` — スクロール コンテナ内に表示される行のみをレンダリングします。
したがって、任意の長さの `items` 配列はレンダリングコストが低くなります。 1 回作成済み
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`;スクロール位置は
中立的な {@link useState}/{@link useRef}/{@link useMemo}/{@link useEffect}
フック。

表示されている各行は、**スコープ指定された `row` スロット** を通じてレンダリングされます。
(`<Slot name="row" item={…} index={…} />`)、コンパイラーは Vue にマップします。
スコープ付きスロット (`slots.row?.({ item, index })`) と React render-prop
(`row?.({ item, index })`) — したがって、コンシューマは同じ方法で行をレンダリングします。
どちらかのフレームワーク。元の Vue SFC は行タイプに対して汎用であり、使用されていました。
スコープ付き **デフォルト** スロット。ニュートラル バージョンでは `unknown` アイテムが使用されます (キャストは次のとおりです)。
呼び出しサイト) および名前付き `row` スロット (クロスフレームワーク ランタイム アダプター)
前方に名前が付けられていますが、デフォルトではありません。スコープ関数としてのスロットです)。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<VirtualListProperties> |      |

### VirtualListItemScope

**種類:** インターフェース

```typescript
export interface VirtualListItemScope
```

レンダリングされた行ごとに {@link ForgeVirtualList} の `row` (スコープ付き) スロットに渡されるスコープ。

### VirtualListプロパティ

**種類:** インターフェース

```typescript
export interface VirtualListProperties
```

説明はありません。

## `src/components/organisms/forge-virtual-log-viewer/forge-virtual-log-viewer`

### ForgeVirtualLogViewer

**種類:** 関数

```typescript
function ForgeVirtualLogViewer(properties: Readonly<VirtualLogViewerProperties>): MpElement;
```

`ForgeVirtualLogViewer` — 高性能の仮想スクロール ログ ビューア。
ニュートラルな JSX 方言で一度作成され、React に直接コンパイルされます。
Vue by `@mission-platform/vite-plugin-forge`;内の行のみをレンダリングします
フルハイトのスペーサーはビューポート全体を表します (オプションで
フィルタリングされた) ログ。スクロール位置はニュートラルフックを使用し、フォローテールが駆動されます
エントリ数をキーとした {@link useEffect} によって、各行のクリックが発生します
`onSelect` コールバック。 `forge-virtual-log-viewer.module.scss` を通じてスタイリングを所有しています。

オリジナルの Vue SFC は `ForgeLogViewerRow`/`ForgeLogViewerToolbar` で構成されました
サブコンポーネント。レベルごとのグリフのアイコン パッケージと `select` を使用しました。
発する。ニュートラル バージョンでは、行とツールバーがインライン化され、色分けされたバーが置き換えられます。
アイコンの `●` グリフ (他の移行されたコンポーネントと一致)、および
`onSelect` コールバック プロパティを使用します。オリジナルの手動スクロールのフォローテール
抑制は解除されます。

#### パラメータ

| 名前       | タイプ                                   | 説明 |
| ---------- | ---------------------------------------- | ---- |
| プロパティ | 読み取り専用<VirtualLogViewerProperties> |      |

### ログエントリ

**種類:** インターフェース

```typescript
export interface LogEntry
```

{@link ForgeVirtualLogViewer} によってレンダリングされた 1 つのログ行。

### ログレベル

**種類：**タイプ

```typescript
export type LogLevel = 'debug' | 'info' | 'warn' | 'error' | 'fatal';
```

{@link LogEntry} の重大度。

### VirtualLogViewerプロパティ

**種類:** インターフェース

```typescript
export interface VirtualLogViewerProperties
```

説明はありません。

## `src/components/organisms/forge-virtual-table/forge-virtual-table`

### ForgeVirtualTable

**種類:** 関数

```typescript
function ForgeVirtualTable(properties: Readonly<VirtualTableProperties>): MpElement;
```

`ForgeVirtualTable` — レンダリングする仮想スクロール、ソート可能なデータ テーブル
ビューポート内の行のみなので、任意の長さの `rows` 配列が残ります。
レンダリングが安価です。ニュートラルな JSX 方言で一度作成され、コンパイルされました
`@mission-platform/vite-plugin-forge` によって React または Vue に直接送信します。種類と
スクロール状態はニュートラルを使用します {@link useState}/{@link useMemo}/{@link useRef}/
{@link useEffect} フック。

スティッキーヘッダーには、クリックして並べ替える列 (asc → desc → の順に循環) が用意されています。
unsorted) `onSort` コールバックを起動します。ボディ行は `onRowClick` を起動し、
`footer` 名前付きスロットは、行数 + ソートの要約にフォールバックします。のように
オリジナルでは `div` + ARIA `role="table|rowgroup|row|columnheader|gridcell"` を使用します
(ネイティブ `<table>` 要素の代わりに) Mobile Safari のテーブルを回避するため
スクリーンリーダーへのアクセスを維持する際のレイアウトのバグ。

元の Vue SFC は行タイプに対して汎用であり、構成されていました。
`ForgeVirtualTableHead`/`Row`/`Footer` サブコンポーネント、アイコン パッケージを使用
ソートグリフの場合、列ごとにスコープ指定された `cell-<key>` スロットが公開され、発行されます。
`sort`/`rowClick`。ニュートラル バージョンは `Record<string, unknown>` 行を使用します。
ヘッド/行/フッターをインライン化し、ライトワンス `@mission-platform/icons` をレンダリングします。
`ForgeIconSort` (アクティブ方向の山形を埋める)、および
`onSort`/`onRowClick` コールバック プロパティを使用します。セルごとのコンテンツは次によって決まります。
各列のオプションの `render` フォーマッタ、単一のスコープ付き `cell` スロット
(`{ column, row, value }`) 元の列ごとの `cell-<key>` を置き換えます
完全にカスタム (インタラクティブ) セル コンテンツ用のスロット。

#### パラメータ

| 名前       | タイプ                               | 説明 |
| ---------- | ------------------------------------ | ---- |
| プロパティ | 読み取り専用<VirtualTableProperties> |      |

### 仮想テーブルセルスコープ

**種類:** インターフェース

```typescript
export interface VirtualTableCellScope
```

スコープ指定された `cell` スロットに渡されるスコープ。

### 仮想テーブル列

**種類:** インターフェース

```typescript
export interface VirtualTableColumn
```

{@link ForgeVirtualTable} の単一列定義。

### 仮想テーブルのプロパティ

**種類:** インターフェース

```typescript
export interface VirtualTableProperties
```

説明はありません。

## `src/components/organisms/forge-virtual-tabs/forge-virtual-tabs`

### ForgeVirtualTabs

**種類:** 関数

```typescript
function ForgeVirtualTabs(properties: Readonly<VirtualTabsProperties>): MpElement;
```

`ForgeVirtualTabs` — **アクティブなタブのみをレンダリングする制御されたタブ コンテナ
タブのパネル** (仮想化)。ニュートラルな JSX 方言で一度作成され、
`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

{@link ForgeTabs} (ARIA `tablist`/`tab`/`tabpanel`、ロービング) のように動作します。
`tabindex`、矢印/ホーム/終了キーボード ナビゲーション、閉じる/追加可能なアフォーダンス、
スコープ付きの `panel` **render-prop**) ですが、非アクティブなパネルをマウントすることはありません -
重いパネルコンテンツに適しています。同じ場所に配置された CSS を通じてスタイルを所有します。
モジュール `forge-virtual-tabs.module.scss`。

元の Vue SFC は共有 `ForgeTabList` サブコンポーネントを再利用し、
`@mission-platform/icons`、タブ ID ごとの名前付きパネル スロットと `v-model` +
発する。ニュートラル バージョンではタブ バーがインライン化され、ライトワンス バージョンがレンダリングされます。
`@mission-platform/icons` `ForgeIconPlus`/`ForgeIconClose` (追加/クローズ用)
アフォーダンス、単一スコープの `panel` を通じてアクティブ パネルをレンダリングします。
**render-prop** (ニュートラル `<Slot>` 経由ではなく、直接呼び出されるため、
両方のフレームワーク上の実際の prop と、コンパイルされたニュートラルな親は、それを として渡すことができます。
プレーン `panel={({ tab }) => …}` 属性）、確立された
`modelValue` + callback-prop 規約。

#### パラメータ

| 名前       | タイプ                              | 説明 |
| ---------- | ----------------------------------- | ---- |
| プロパティ | 読み取り専用<VirtualTabsProperties> |      |

### VirtualTabPanelScope

**種類:** インターフェース

```typescript
export interface VirtualTabPanelScope
```

`panel` render-prop に渡されるスコープ。

### 仮想タブのプロパティ

**種類:** インターフェース

```typescript
export interface VirtualTabsProperties
```

説明はありません。

## `src/components/organisms/forge-virtual-tree-view/forge-virtual-tree-view`

### ForgeVirtualTreeView

**種類:** 関数

```typescript
function ForgeVirtualTreeView(properties: Readonly<VirtualTreeViewProperties>): MpElement;
```

`ForgeVirtualTreeView` — 表示を平坦化する仮想スクロール ツリー
ノードを 1 つのリストにまとめ、ビューポート内の行のみをレンダリングします。
数万のノードを持つツリーは滑らかなままです。で一度執筆されました
中立的な JSX 方言であり、直接 React または Vue にコンパイルされます。
`@mission-platform/vite-plugin-forge`;開いた状態とスクロール位置を使用します
ニュートラルフック。各行は、スコープ指定された `row` スロットを通じてカスタマイズできます。
(`{ node, depth, isOpen, toggle, select }`) 組み込みのラベルに戻ります
展開/折りたたみ山形付き。 `forge-virtual-tree-view.module.scss` を通じてスタイリングを所有しています。

元の Vue SFC は `ForgeTreeNodeLabel` を構成し、スコープ付き **デフォルト** を使用しました
スロットに接続すると、`select`/`toggle` が出力されます。ニュートラル バージョンではラベルがインライン化されます。
ライトワンス `@mission-platform/icons` `ForgeIconChevron` をレンダリングします (経由で回転)
`direction` プロパティ)、名前付き `row` スコープ スロット (ランタイム アダプター) を使用します。
デフォルトではなく、スコープ付きの順方向の名前付きスロット)、および `onSelect`/`onToggle`
コールバックの小道具。

#### パラメータ

| 名前       | タイプ                                  | 説明 |
| ---------- | --------------------------------------- | ---- |
| プロパティ | 読み取り専用<VirtualTreeViewProperties> |      |

### ツリーノード

**種類:** インターフェース

```typescript
export interface TreeNode
```

{@link ForgeVirtualTreeView} によってレンダリングされたツリー内の node。

### ツリー行スコープ

**種類:** インターフェース

```typescript
export interface TreeRowScope
```

表示される node ごとに、{@link ForgeVirtualTreeView} の `row` (スコープ付き) スロットに渡されるスコープ。

### VirtualTreeViewプロパティ

**種類:** インターフェース

```typescript
export interface VirtualTreeViewProperties
```

説明はありません。

## `src/components/templates/forge-hero/forge-hero`

### フォージヒーロー

**種類:** 関数

```typescript
function ForgeHero(properties: Readonly<HeroProperties>): MpElement;
```

`ForgeHero` — ニュートラル JSX で一度作成されたページのヒーロー/バナー セクション
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

オプションの眉毛、タイトル、サブタイトルを含む目立つバナーをレンダリングします。
自由形式の本文コンテンツ (デフォルトのスロット)、および `actions` の行。 `media`
領域では、コンテンツの背後にあるフルブリードの背景をレンダリングできます。
テキストのコントラストを維持するためのオプションのスクリム `overlay` (メディア上の眉毛 /
タイトル/サブタイトルを反転テキスト色に切り替えます)。

眉毛の兄弟ライトワンス {@link ForgeTypography} を構成します。
タイトル、サブタイトル、および同じ場所にある CSS モジュールを通じてそのスタイルを所有します。
`forge-hero.module.scss` (2 段階のフレームワークによって継承される)
コンパイラ）。 `media`/`actions` 領域は名前付きスロットとして作成されます
(`<Slot>`、フレームワークに依存しない {@link hasSlot} で存在が検出されました
helper) と `eyebrow`/`title`/`subtitle` は文字列プロパティです。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| プロパティ | 読み取り専用<HeroProperties> |      |

### ヒーローアライン

**種類：**タイプ

```typescript
export type HeroAlign = 'start' | 'center' | 'end';
```

ヒーロー コンテンツ列の水平方向の配置。

### ヒーローのプロパティ

**種類:** インターフェース

```typescript
export interface HeroProperties
```

説明はありません。

### ヒーローサイズ

**種類：**タイプ

```typescript
export type HeroSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

垂直パディングスケール — 標準的な 2xs → 2xl スケール。

## `src/utils/pointer-drag/pointer-drag`

### beginPointerドラッグ

**種類:** 関数

```typescript
function beginPointerDrag(handlers: PointerDragHandlers): () => void;
```

ウィンドウ追跡ポインタのドラッグを開始します。 `pointermove`/`pointerup`/を添付します
`pointercancel` リスナーを `window` に転送し、`handlers.onMove` に転送します。
そしてリリース時にリスナーを削除し、`handlers.onEnd` を呼び出します。を返します
ドラッグを早期に終了するディスポーザ (リスナーも削除します)。ノーオペオン
サーバー (`window` なし)。

#### パラメータ

| 名前       | タイプ                       | 説明 |
| ---------- | ---------------------------- | ---- |
| ハンドラー | ポインタードラッグハンドラー |      |

### ポインタードラッグハンドラー

**種類:** インターフェース

```typescript
export interface PointerDragHandlers
```

{@link beginPointerDrag} で開始されたポインター ドラッグの存続期間中に呼び出されるコールバック。
