base_alert_banner:
  dismiss_label: 閉じる
base_breadcrumb:
  breadcrumb_label: パンくずリスト
base_calendar:
  select_month_label: 月を選択
  select_year_label: 年を選択
base_carousel:
  carousel_role_description: カルーセル
  next_slide_label: 次のスライド
  previous_slide_label: 前のスライド
  slide_role_description: スライド
base_checkbox:
  required_indicator_label: 必須
base_color_input:
  open_picker_label: カラーピッカーを開く
  required_indicator_label: 必須
base_date_input:
  required_indicator_label: 必須
base_date_range_input:
  required_indicator_label: 必須
base_date_time_range_input:
  required_indicator_label: 必須
base_file_input:
  required_indicator_label: 必須
base_input:
  required_indicator_label: 必須
base_markdown_input:
  preview_tab_label: プレビュー
  required_indicator_label: 必須
  write_tab_label: 入力
base_monaco_editor:
  code_editor_label: コードエディター
base_multiselect:
  required_indicator_label: 必須
base_navbar:
  main_navigation_label: メインナビゲーション
  mobile_navigation_label: モバイルナビゲーション
base_number_stepper:
  decrease_label: 減らす
  increase_label: 増やす
base_radio_group:
  required_indicator_label: 必須
base_scheduler:
  calendar_view_label: カレンダー表示
  more_events_label_zero: 件
  more_events_label_one: 件
  more_events_label_other: 件
base_select:
  required_indicator_label: 必須
base_table:
  loading_label: テーブルデータを読み込み中
  loading_spinner_label: 読み込み中…
base_tabs:
  new_tab_label: 新しいタブ
base_textarea:
  required_indicator_label: 必須
base_time_input:
  done_label: 完了
  required_indicator_label: 必須
base_time_range_input:
  done_label: 完了
  required_indicator_label: 必須
base_virtual_table:
  row_count_zero: '{{formattedCount}} 行'
  row_count_one: '{{formattedCount}} 行'
  row_count_other: '{{formattedCount}} 行'
  sorted_by_label: ソート順
base_virtual_tabs:
  new_tab_label: 新しいタブ
