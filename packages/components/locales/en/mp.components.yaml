base_alert_banner:
  dismiss_label: Dismiss
base_breadcrumb:
  breadcrumb_label: Breadcrumb
base_calendar:
  select_month_label: Select month
  select_year_label: Select year
base_carousel:
  carousel_role_description: carousel
  next_slide_label: Next slide
  previous_slide_label: Previous slide
  slide_role_description: slide
base_checkbox:
  required_indicator_label: required
base_color_input:
  open_picker_label: Open colour picker
  required_indicator_label: required
base_date_input:
  required_indicator_label: required
base_date_range_input:
  required_indicator_label: required
base_date_time_range_input:
  required_indicator_label: required
base_file_input:
  required_indicator_label: required
base_input:
  required_indicator_label: required
base_markdown_input:
  preview_tab_label: Preview
  required_indicator_label: required
  write_tab_label: Write
base_monaco_editor:
  code_editor_label: Code editor
base_multiselect:
  required_indicator_label: required
base_navbar:
  main_navigation_label: Main navigation
  mobile_navigation_label: Mobile navigation
base_number_stepper:
  decrease_label: Decrease
  increase_label: Increase
base_radio_group:
  required_indicator_label: required
base_scheduler:
  calendar_view_label: Calendar view
  more_events_label_zero: more
  more_events_label_one: more
  more_events_label_other: more
base_select:
  required_indicator_label: required
base_table:
  loading_label: Loading table data
  loading_spinner_label: Loading…
base_tabs:
  new_tab_label: New tab
base_textarea:
  required_indicator_label: required
base_time_input:
  done_label: Done
  required_indicator_label: required
base_time_range_input:
  done_label: Done
  required_indicator_label: required
base_virtual_table:
  row_count_zero: '{{formattedCount}} rows'
  row_count_one: '{{formattedCount}} rows'
  row_count_other: '{{formattedCount}} rows'
  sorted_by_label: Sorted by
base_virtual_tabs:
  new_tab_label: New tab
