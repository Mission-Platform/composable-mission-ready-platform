import "./memory.fws" as memory;

export fn collect(heap: u32, root: u32) -> u32 {
  if root == 0 && memory.heap_has_roots(heap) == 1 {
    return 0;
  }
  return memory.heap_collect(heap, root);
}

export fn tracked_objects(heap: u32) -> u32 {
  return memory.heap_object_count(heap);
}