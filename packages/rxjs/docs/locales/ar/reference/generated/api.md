# مرجع @mission-platform/rxjs API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/rxjs/docs/reference/generated/api.md: [packages/rxjs/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/rxjs`.

## `src/composables/use-observable/use-observable`

### useObservable

**النوع:** الوظيفة

```typescript
function useObservable(source: Observable<T>): T | undefined
```

اشترك في `source` وقم بإرجاع أحدث إصدار لها كحالة. المرتجعات
`undefined` حتى وصول الانبعاثات الأولى.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المصدر | يمكن ملاحظتها<T> |  |

## `src/composables/use-subscription/use-subscription`

### غير قابل للاشتراك

**النوع:** الواجهة

```typescript
export interface Unsubscribable
```

أي شيء يمكن إلغاء الاشتراك فيه — يطابق `Unsubscribable` الخاص بـ RxJS.

### استخدم الاشتراك

**النوع:** الوظيفة

```typescript
function useSubscribe(source: Observable<T>, observerOrNext?: Partial<Observer<T>> | ((value: T) => void), dependencies: MpDependencyList = []): void
```

اشترك في `source` باستخدام `observer` المحدد (أو رد الاتصال `next`) لـ
عمر المكون، وإعادة الاشتراك عند `source` (أو أي ملف إضافي
`dependencies`) التغييرات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المصدر | يمكن ملاحظتها<T> |  |
| المراقب أو التالي | جزئي<Observer<T>> \| ((القيمة: T) => باطل) |  |
| التبعيات | قائمة التبعيات |  |

### useSubscription

**النوع:** الوظيفة

```typescript
function useSubscription(subscribe: () => Unsubscribable, dependencies: MpDependencyList = []): void
```

قم بتشغيل `subscribe` بعد التحميل وإلغاء الاشتراك من نتيجته عند إلغاء التحميل (و
كلما تغير `dependencies`). تقوم `subscribe` بإرجاع أي `Unsubscribable`، لذا
يمكنه التفاف مكالمة `source.subscribe(...)` العارية أو خط أنابيب كامل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| اشترك | () => غير قابل للاشتراك |  |
| التبعيات | قائمة التبعيات |  |
