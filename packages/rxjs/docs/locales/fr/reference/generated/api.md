# Référence API @mission-platform/rxjs

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> packages/rxjs/docs/reference/generated/api.md: [packages/rxjs/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/rxjs`.

## `src/composables/use-observable/use-observable`

### utiliserObservable

**Genre :** fonction

```typescript
function useObservable(source: Observable<T>): T | undefined
```

Abonnez-vous à `source` et renvoyez sa dernière émission sous forme d'état. Retours
`undefined` jusqu'à l'arrivée de la première émission.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| source | Observable<T> |  |

## `src/composables/use-subscription/use-subscription`

### Désabonnement

**Genre :** interface

```typescript
export interface Unsubscribable
```

Tout ce qui peut être désabonné correspond au `Unsubscribable` de RxJS.

### utiliserS'abonner

**Genre :** fonction

```typescript
function useSubscribe(source: Observable<T>, observerOrNext?: Partial<Observer<T>> | ((value: T) => void), dependencies: MpDependencyList = []): void
```

Abonnez-vous à `source` avec le rappel `observer` donné (ou `next`) pour le
durée de vie du composant, réabonnement lorsque `source` (ou tout supplément
`dependencies`) change.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| source | Observable<T> |  |
| observateurOuSuivant | Partial<Observer<T>> \| ((valeur : T) => vide) |  |
| dépendances | MpDependencyList |  |

### utiliser l'abonnement

**Genre :** fonction

```typescript
function useSubscription(subscribe: () => Unsubscribable, dependencies: MpDependencyList = []): void
```

Exécutez `subscribe` après le montage et désabonnez-vous de son résultat au démontage (et
chaque fois que `dependencies` change). `subscribe` renvoie n'importe quel `Unsubscribable`, donc
il peut envelopper un simple appel `source.subscribe(...)` ou un pipeline entier.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| abonnez-vous | () => Désabonnement |  |
| dépendances | MpDependencyList |  |
