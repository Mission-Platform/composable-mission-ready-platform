# @mission-platform/rxjs API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/rxjs/docs/reference/generated/api.md: [packages/rxjs/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/rxjs` 中的公共源声明生成。

## `src/composables/use-observable/use-observable`

### 使用Observable

**种类：**功能

```typescript
function useObservable(source: Observable<T>): T | undefined;
```

订阅 `source` 并返回其最新发射作为状态。退货
`undefined` 直到第一次发射到达。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 来源 | Observable<T> |      |

## `src/composables/use-subscription/use-subscription`

### 不可订阅

**种类：**接口

```typescript
export interface Unsubscribable
```

任何可以取消订阅的内容 — 与 RxJS 的 `Unsubscribable` 匹配。

### 使用订阅

**种类：**功能

```typescript
function useSubscribe(
  source: Observable<T>,
  observerOrNext?: Partial<Observer<T>> | ((value: T) => void),
  dependencies: MpDependencyList = [],
): void;
```

使用给定的 `observer`（或 `next` 回调）订阅 `source`
组件的生命周期，当 `source`（或任何额外的
`dependencies`) 更改。

#### 参数

| 名称           | 类型                                     | 描述             |
| -------------- | ---------------------------------------- | ---------------- |
| 来源           | Observable<T>                            |                  |
| 观察者或下一个 | 部分<Observer<T>> \| （（值：T）=>无效） |                  |
| 依赖关系       | MpDependencyList                         | MpDependencyList |     |

### 使用订阅

**种类：**功能

```typescript
function useSubscription(subscribe: () => Unsubscribable, dependencies: MpDependencyList = []): void;
```

挂载后运行 `subscribe` 并在卸载时取消订阅其结果（并且
每当 `dependencies` 更改时）。 `subscribe` 返回任何 `Unsubscribable`，因此
它可以包装一个裸 `source.subscribe(...)` 调用或整个管道。

#### 参数

| 名称     | 类型             | 描述             |
| -------- | ---------------- | ---------------- |
| 订阅     | () => 取消订阅   |                  |
| 依赖关系 | MpDependencyList | MpDependencyList |     |
