# הפניה ל-@mission-platform/rxjs API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/rxjs/docs/reference/generated/api.md: [packages/rxjs/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/rxjs`.

## `src/composables/use-observable/use-observable`

### useObservable

פונקציה **סוג:**

```typescript
function useObservable(source: Observable<T>): T | undefined
```

הירשם ל-`source` והחזר את הפליטה האחרונה שלו כמדינה. מחזיר
`undefined` עד הגעת הפליטה הראשונה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מקור | לצפייה<T> |  |

## `src/composables/use-subscription/use-subscription`

### לא ניתן לרשום

**סוג:** ממשק

```typescript
export interface Unsubscribable
```

כל דבר שניתן לבטל את המנוי - מתאים ל-`Unsubscribable` של RxJS.

### useSubscribe

פונקציה **סוג:**

```typescript
function useSubscribe(source: Observable<T>, observerOrNext?: Partial<Observer<T>> | ((value: T) => void), dependencies: MpDependencyList = []): void
```

הירשם ל-`source` עם ה-`observer` (או `next` התקשרות חוזרת) עבור
משך החיים של הרכיב, הרשמה מחדש כאשר `source` (או כל תוספת
`dependencies`) שינויים.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| מקור | לצפייה<T> |  |
| observerOrNext | חלקי<Observer<T>> \| ((ערך: T) => ריק) |  |
| תלות | MpDependencyList |  |

### השתמש במנוי

פונקציה **סוג:**

```typescript
function useSubscription(subscribe: () => Unsubscribable, dependencies: MpDependencyList = []): void
```

הפעל את `subscribe` לאחר הטעינה ובטל את הרישום מהתוצאה שלו בביטול הטעינה (ו
בכל פעם ש-`dependencies` משתנה). `subscribe` מחזיר כל `Unsubscribable`, אז
זה יכול לעטוף שיחת `source.subscribe(...)` חשופה או צינור שלם.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| הירשם | () => לא ניתן להירשם |  |
| תלות | MpDependencyList |  |
