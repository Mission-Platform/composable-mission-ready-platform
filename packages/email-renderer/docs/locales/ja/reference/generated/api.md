# @mission-platform/email-renderer API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/email-renderer/docs/reference/generated/api.md: [packages/email-renderer/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/email-renderer` のパブリック ソース宣言から生成されます。

## `src/adapters/react`

### renderToEmailReact

**種類:** 関数

```typescript
function renderToEmailReact(node: EmailNode): ReactElement;
```

共有 Forge ツリーをブラウザ プレビュー用の React 要素としてレンダリングします。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

### toEmailReactコンポーネント

**種類:** 関数

```typescript
function toEmailReactComponent(component: MpComponent<P>, name?: string): FunctionComponent<P>;
```

Forge 電子メール コンポーネントを React 関数コンポーネントとしてラップします。

#### パラメータ

| 名前           | タイプ         | 説明 |
| -------------- | -------------- | ---- |
| コンポーネント | MpComponent<P> |      |
| 名前           | 文字列         |      |

## `src/adapters/solid`

### renderToEmailSolid

**種類:** 関数

```typescript
function renderToEmailSolid(node: EmailNode): JSX.Element;
```

共有 Forge ツリーをブラウザー プレビュー用に Solid JSX としてレンダリングします。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

### toEmailSolidComponent

**種類:** 関数

```typescript
function toEmailSolidComponent(component: Component<P>): Component<P>;
```

Forge コンポーネントを Solid コンポーネントとしてラップします。

#### パラメータ

| 名前           | タイプ            | 説明 |
| -------------- | ----------------- | ---- |
| コンポーネント | コンポーネント<P> |      |

## `src/adapters/svelte`

### renderToEmailSvelte

**種類:** 関数

```typescript
function renderToEmailSvelte(node: EmailNode): Snippet;
```

共有 Forge ツリーを `{@render ...}` の Svelte 5 生のスニペットに変換します。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

## `src/adapters/vue`

### renderToEmailVue

**種類:** 関数

```typescript
function renderToEmailVue(node: EmailNode): VNode;
```

共有 Forge ツリーをブラウザ プレビュー用の Vue VNode としてレンダリングします。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

### toEmailVueコンポーネント

**種類:** 関数

```typescript
function toEmailVueComponent(component: MpComponent<P>, name?: string): FunctionalComponent<P>;
```

Forge 電子メール コンポーネントを Vue 機能コンポーネントとしてラップします。

#### パラメータ

| 名前           | タイプ         | 説明 |
| -------------- | -------------- | ---- |
| コンポーネント | MpComponent<P> |      |
| 名前           | 文字列         |      |

## `src/adapters/web-components`

### renderToEmailWebComponent

**種類:** 関数

```typescript
function renderToEmailWebComponent(node: EmailNode): TemplateResult;
```

Web コンポーネント ホストのネイティブ Forge テンプレート結果を作成します。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

## `src/markdown/render-markdown`

### renderMarkdown

**種類:** 関数

```typescript
function renderMarkdown(markdown: string, options: MarkdownRenderOptions = {}): MarkdownDocument;
```

安全な Markdown トークンを、電子メール コンポーネントで使用されるのと同じ Forge ツリーに変換します。

#### パラメータ

| 名前       | タイプ                | 説明 |
| ---------- | --------------------- | ---- |
| 値下げ     | 文字列                |      |
| オプション | MarkdownRenderOptions |      |

## `src/markdown/types`

### マークダウンドキュメント

**種類:** インターフェース

```typescript
export interface MarkdownDocument
```

Markdown を解析して共有 Forge ツリー パイプラインに取り込んだ結果。

### MarkdownRenderOptions

**種類:** インターフェース

```typescript
export interface MarkdownRenderOptions
```

Markdown ツリーの生成を制御するオプション。

## `src/render/attributes`

### シリアル化属性

**種類:** 関数

```typescript
function serializeAttributes(properties: EmailProperties): string;
```

ホワイトリストに登録された Forge プロパティのセットを決定的な順序でシリアル化します。

#### パラメータ

| 名前       | タイプ                 | 説明 |
| ---------- | ---------------------- | ---- |
| プロパティ | 電子メールのプロパティ |      |

## `src/render/escape`

### エスケープHTML

**種類:** 関数

```typescript
function escapeHtml(value: string): string;
```

HTML シリアル化のテキストと属性コンテンツをエスケープします。

#### パラメータ

| 名前 | タイプ | 説明 |
| ---- | ------ | ---- |
| 値   | 文字列 |      |

### 検証URL

**種類:** 関数

```typescript
function validateUrl(value: string, attribute: string): string;
```

コードを実行したり、予期しないリソースをロードしたりする可能性のあるスキームを拒否します。

#### パラメータ

| 名前 | タイプ | 説明 |
| ---- | ------ | ---- |
| 値   | 文字列 |      |
| 属性 | 文字列 |      |

## `src/render/render-email`

### レンダリング電子メール

**種類:** 関数

```typescript
function renderEmail(node: EmailNode, options: RenderEmailOptions = {}): string;
```

Forge 電子メール ツリーを、フレームワークを使用しない完全な HTML ドキュメントにレンダリングします。

#### パラメータ

| 名前       | タイプ                   | 説明 |
| ---------- | ------------------------ | ---- |
| node       | メールノード             |      |
| オプション | レンダーメールオプション |      |

### renderEmailFragment

**種類:** 関数

```typescript
function renderEmailFragment(node: EmailNode): string;
```

Forge ツリーをブラウザ アダプタ ホスト用の安全な HTML フラグメントとしてレンダリングします。

#### パラメータ

| 名前 | タイプ       | 説明 |
| ---- | ------------ | ---- |
| node | メールノード |      |

## `src/render/style`

### シリアル化スタイル

**種類:** 関数

```typescript
function serializeStyle(style: EmailStyle | string): string;
```

CSS 変数を使用せずに、決定論的なプロパティ順序でインライン スタイルをシリアル化します。

#### パラメータ

| 名前     | タイプ                      | 説明 |
| -------- | --------------------------- | ---- |
| スタイル | 電子メールスタイル \|文字列 |      |

## `src/render/types`

### 電子メールコンポーネント

**種類：**タイプ

```typescript
export type EmailComponent<P extends MpPropertyBag = MpPropertyBag> = MpComponent<P>;
```

電子メール ノードの作成に使用できる Forge コンポーネント。

### 電子メールノード

**種類：**タイプ

```typescript
export type EmailNode = MpElement;
```

電子メール シリアライザーによって受け入れられる、フレームワークに依存しない Forge 要素。

### 電子メールのプロパティ

**種類：**タイプ

```typescript
export type EmailProperties = MpPropertyBag &
```

検証後にシリアル化された Forge 要素に渡されるプロパティ。

### メールスタイル

**種類：**タイプ

```typescript
export type EmailStyle = Readonly<Record<string, EmailStyleValue>>;
```

キーが字句順にシリアル化されたスタイル オブジェクト。

### 電子メールスタイル値

**種類：**タイプ

```typescript
export type EmailStyleValue = number | string | EmailStyleValueWithFallback | null | undefined;
```

説明はありません。

### EmailStyleValueWithFallback

**種類:** インターフェース

```typescript
export interface EmailStyleValueWithFallback
```

決定論的なインライン スタイル シリアライザーによって受け入れられる値。

### レンダー電子メールオプション

**種類:** インターフェース

```typescript
export interface RenderEmailOptions
```

完全な電子メール文書をレンダリングするためのオプション。
