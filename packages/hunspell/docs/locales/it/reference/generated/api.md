# Riferimento API @mission-platform/hunspell

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/hunspell/docs/reference/generated/api.md: [packages/hunspell/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/hunspell`.

## `src/composables/use-hunspell-monaco/use-hunspell-monaco`

### utilizzareHunspellMonaco

**Tipo:** funzione

```typescript
function useHunspellMonaco(editorReference: MaybeRefOrGetter<monaco.editor.IStandaloneCodeEditor | undefined>, enabled: MaybeRefOrGetter<boolean>, languageReference: MaybeRefOrGetter<string>): void
```

Vue componibile che integra il controllo ortografico Hunspell in un editor Monaco.

L’integrazione imperativa vive in un quadro agnostico
{@link attachHunspellMonaco} (condiviso con write-once
editor `@mission-platform/components`); questo componibile aggiunge il reattivo
cablaggio: si (ri)collega ogni volta che `enabled`/`editor` cambia e si smaltisce
smonta e ricontrolla quando `language` cambia.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| editorRiferimento | ForseRefOrGetter<monaco.editor.IStandaloneCodeEditor \| undefined> |  |
| abilitato | ForseRefOrGetter<boolean> |  |
| linguaRiferimento | ForseRefOrGetter<string> |  |

## `src/wasm/hunspell.d`

### HunspellChecker

**Tipo:** interfaccia

```typescript
export interface HunspellChecker extends ClassHandle
```

Nessuna descrizione fornita.

### Modulo Hunspell

**Genere:** tipo

```typescript
export type MainModule = WasmModule & typeof RuntimeExports & EmbindModule;
```

Nessuna descrizione fornita.

### StringVector

**Tipo:** interfaccia

```typescript
export interface StringVector extends ClassHandle, Iterable<string>
```

Nessuna descrizione fornita.

### Risultato token

**Genere:** tipo

```typescript
export type TokenResult =
```

Nessuna descrizione fornita.

### TokenResultVector

**Tipo:** interfaccia

```typescript
export interface TokenResultVector extends ClassHandle, Iterable<TokenResult>
```

Nessuna descrizione fornita.
