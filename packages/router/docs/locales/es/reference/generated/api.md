# Referencia de API @mission-platform/router

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/router/docs/reference/generated/api.md: [packages/router/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/router`.

## `src/capabilities`

### crear capacidades de MpRouter

**Tipo:** función

```typescript
function createMpRouterCapabilities(overrides: MpRouterCapabilityOverrides = {}): MpRouterCapabilities;
```

Cree un objeto de capacidad neutral para un adaptador de destino, SSR o una prueba.

#### Parámetros

| Nombre | Tipo                                 | Descripción |
| ------ | ------------------------------------ | ----------- |
| anula  | Anulaciones de capacidad de MpRouter |             |

### isMpRouterCapabilityError

**Tipo:** función

```typescript
function isMpRouterCapabilityError(error: unknown): error is MpRouterCapabilityError;
```

Si un error desconocido es el error determinista de reserva del enrutador neutral.

#### Parámetros

| Nombre | Tipo        | Descripción |
| ------ | ----------- | ----------- |
| error  | desconocido |             |

### MP_ROUTER_COMPILER_MARKER

**Tipo:** constante

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

Metadatos estables consumidos por los complementos del compilador de enrutadores de Forge.

### MpEnlace

**Tipo:** componente

```typescript
function MpLink(properties: MpLinkProps): MpLinkDescriptor;
```

Marcador de enlace neutro. Los destinos nativos reemplazan esta función durante la compilación.

#### Parámetros

| Nombre      | Tipo        | Descripción |
| ----------- | ----------- | ----------- |
| propiedades | MpLinkProps |             |

### MpLinkDescriptor

**Tipo:** componente

```typescript
export interface MpLinkDescriptor
```

Una descripción de enlace neutral en el marco utilizada por SSR y pruebas no compiladas.

### Opciones de MpLink

**Tipo:** componente

```typescript
export interface MpLinkOptions
```

Opciones compartidas por enlaces neutrales y llamadas de navegación nativas.

### MpLinkProps

**Tipo:** componente

```typescript
export interface MpLinkProps extends MpLinkOptions
```

Accesorios aceptados por el marcador del compilador representado por {@link MpLink}.

### Opciones de navegación Mp

**Tipo:** componente

```typescript
export interface MpNavigationOptions extends MpLinkOptions
```

Opciones enviadas a una implementación de navegación nativa.

### Capacidades de MpRouter

**Tipo:** componente

```typescript
export interface MpRouterCapabilities
```

Las capacidades proporcionadas por el enrutador nativo de la aplicación consumidora.

### Capacidad del MpRouter

**Tipo:** componente

```typescript
export type MpRouterCapability = 'link' | 'route' | 'navigate' | 'resolve' | 'view';
```

Capacidades que un enrutador objetivo puede reducir de forma independiente.

### Error de capacidad del enrutador Mp

**Tipo:** componente

```typescript
export class MpRouterCapabilityError extends Error
```

Diagnóstico estable generado cuando el respaldo no puede realizar una operación.

### Anulaciones de capacidad de MpRouter

**Tipo:** componente

```typescript
export type MpRouterCapabilityOverrides = Partial<MpRouterCapabilities>;
```

Una implementación parcial útil para apuntar a adaptadores y arneses de prueba/SSR.

### MpRouterVer

**Tipo:** componente

```typescript
function MpRouterView(properties: MpRouterViewProps = {}): MpRouterViewDescriptor;
```

Salida neutra/marcador de vista. Los destinos nativos reemplazan esta función durante la compilación.

#### Parámetros

| Nombre      | Tipo              | Descripción |
| ----------- | ----------------- | ----------- |
| propiedades | MpRouterViewProps |             |

### MpRouterViewDescriptor

**Tipo:** componente

```typescript
export interface MpRouterViewDescriptor
```

Una descripción de vista neutral del marco utilizada por SSR y pruebas no compiladas.

### MpRouterViewProps

**Tipo:** componente

```typescript
export interface MpRouterViewProps
```

Accesorios aceptados por el marcador del compilador de salida/vista neutral.

### resolverMpLink

**Tipo:** función

```typescript
function resolveMpLink(
  to: MpRouteLocationRaw,
  capabilities: Pick<MpRouterCapabilities, 'resolve'> = useMpRouter(),
): string;
```

Resuelva un objetivo a través del enrutador propiedad de la aplicación. La capacidad opcional
El argumento hace que el ayudante sea determinista en SSR/pruebas y adaptadores de destino.

#### Parámetros

| Nombre      | Tipo                                         | Descripción |
| ----------- | -------------------------------------------- | ----------- |
| a           | MpRouteLocationRaw                           |             |
| capacidades | Seleccionar<MpRouterCapabilities, 'resolve'> |             |

### utilizarMpNavigation

**Tipo:** función

```typescript
function useMpNavigation(): Pick<MpRouterCapabilities, 'navigate' | 'resolve'>;
```

Lea solo la capacidad de navegación imperativa.

### utilizarMpRoute

**Tipo:** función

```typescript
function useMpRoute(): MpResolvedLocation | null;
```

Lea la ruta actual propiedad de la aplicación. El respaldo no compilado es seguro para SSR y
devuelve `null`; un destino del compilador reemplaza esta llamada con el estado de ruta nativo.

### utilizarMpRouter

**Tipo:** función

```typescript
function useMpRouter(): MpRouterCapabilities;
```

Lea el objeto de capacidad neutral proporcionado por la aplicación.

## `src/compatibility-fixtures`

### aplicaciónCompatibilidadAccesorios

**Tipo:** constante

```typescript
export const applicationCompatibilityFixtures;
```

No se proporciona descripción.

### docsCompatibilidadAccesorio

**Tipo:** constante

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Registros de ruta que reflejan la tabla local/búsqueda/catch-all de la aplicación de documentación.

### documentaciónCompatibilidadAccesorio

**Tipo:** constante

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Registros de ruta que reflejan la tabla local/búsqueda/catch-all de la aplicación de documentación.

### Caso de compatibilidad de Forge

**Tipo:** interfaz

```typescript
export interface ForgeCompatibilityCase
```

No se proporciona descripción.

### Accesorio de compatibilidad de Forge

**Tipo:** interfaz

```typescript
export interface ForgeCompatibilityFixture
```

No se proporciona descripción.

### myCareNotesCompatibilidadAccesorio

**Tipo:** constante

```typescript
export const myCareNotesCompatibilityFixture: ForgeCompatibilityFixture;
```

Enrute registros que reflejen el segmento de idioma de My Care Notes y las superposiciones basadas en URL.

### enrutadorCompatibilidadAccesorios

**Tipo:** constante

```typescript
export const routerCompatibilityFixtures: readonly ForgeCompatibilityCase[];
```

Filas de comportamiento compartidas consumidas por el conjunto de pruebas de contrato de cada tiempo de ejecución.

### sitio webCompatibilidadAccesorio

**Tipo:** constante

```typescript
export const websiteCompatibilityFixture: ForgeCompatibilityFixture;
```

Registros de ruta que reflejan la ruta de inicio local prefijada opcional del sitio web.

## `src/contracts`

### ForgeGuardResultado

**Tipo:** tipo

```typescript
export type ForgeGuardOutcome = MpGuardOutcome;
```

No se proporciona descripción.

### ForjaHistoria

**Tipo:** tipo

```typescript
export type ForgeHistory = MpHistory;
```

Una implementación de navegador, hash o historial de memoria proporcionada por un tiempo de ejecución.

### ForgeHistoryEntrada

**Tipo:** tipo

```typescript
export type ForgeHistoryEntry = MpHistoryEntry;
```

No se proporciona descripción.

### ForgeHistoriaEvento

**Tipo:** tipo

```typescript
export type ForgeHistoryEvent = MpHistoryEvent;
```

No se proporciona descripción.

### ForgeHistoryListener

**Tipo:** tipo

```typescript
export type ForgeHistoryListener = MpHistoryListener;
```

No se proporciona descripción.

### Modo Historial Forge

**Tipo:** tipo

```typescript
export type ForgeHistoryMode = MpHistoryMode;
```

No se proporciona descripción.

### ForgeMetadataContexto

**Tipo:** tipo

```typescript
export type ForgeMetadataContext = MpMetadataContext;
```

No se proporciona descripción.

### ForgeMetadatosGancho

**Tipo:** tipo

```typescript
export type ForgeMetadataHook = MpMetadataHook;
```

No se proporciona descripción.

### Objetivo con nombre de forja

**Tipo:** tipo

```typescript
export type ForgeNamedTarget = MpNamedLocation;
```

No se proporciona descripción.

### Contexto de navegación de Forge

**Tipo:** tipo

```typescript
export type ForgeNavigationContext = MpNavigationContext;
```

No se proporciona descripción.

### ForgeNavegaciónFalla

**Tipo:** tipo

```typescript
export type ForgeNavigationFailure = MpNavigationFailure;
```

No se proporciona descripción.

### Tipo de error de navegación de Forge

**Tipo:** tipo

```typescript
export type ForgeNavigationFailureType = MpNavigationFailureType;
```

No se proporciona descripción.

### Redirección de navegación de Forge

**Tipo:** tipo

```typescript
export type ForgeNavigationRedirect = MpNavigationRedirect;
```

No se proporciona descripción.

### Resultado de navegación de Forge

**Tipo:** tipo

```typescript
export type ForgeNavigationResult = MpNavigationResult;
```

No se proporciona descripción.

### ForgeNavegaciónÉxito

**Tipo:** tipo

```typescript
export type ForgeNavigationSuccess = MpNavigationSuccess;
```

No se proporciona descripción.

### Tipo de navegación de Forge

**Tipo:** tipo

```typescript
export type ForgeNavigationType = MpNavigationType;
```

No se proporciona descripción.

### Valor del parámetro de forja

**Tipo:** tipo

```typescript
export type ForgeParameterValue = MpParameterValue;
```

No se proporciona descripción.

### Objetivo de ruta de forja

**Tipo:** tipo

```typescript
export type ForgePathTarget = MpPathLocation;
```

No se proporciona descripción.

### ForjarQuery

**Tipo:** tipo

```typescript
export type ForgeQuery = MpQueryParameters;
```

No se proporciona descripción.

### ForgeQueryEntrada

**Tipo:** tipo

```typescript
export type ForgeQueryInput = MpQueryInput;
```

No se proporciona descripción.

### ForgeSeñal de solo lectura

**Tipo:** tipo

```typescript
export type ForgeReadonlySignal<Value> = MpReadonlySignal<Value>;
```

Estado reactivo estructural utilizado por adaptadores sin prescribir una estructura primitiva.

### ForgeRedirect

**Tipo:** tipo

```typescript
export type ForgeRedirect = MpRedirect;
```

No se proporciona descripción.

### ForjaResueltoRuta

**Tipo:** tipo

```typescript
export type ForgeResolvedRoute = MpResolvedLocation;
```

No se proporciona descripción.

### ForgeRouteChangeEvento

**Tipo:** tipo

```typescript
export type ForgeRouteChangeEvent = MpRouteChangeEvent;
```

No se proporciona descripción.

### ForgeRouteChangeListener

**Tipo:** tipo

```typescript
export type ForgeRouteChangeListener = MpRouteChangeListener;
```

No se proporciona descripción.

### ForjaRutaGuardia

**Tipo:** tipo

```typescript
export type ForgeRouteGuard = MpRouteGuard;
```

No se proporciona descripción.

### ForgeRouteEnlace

**Tipo:** tipo

```typescript
export type ForgeRouteLink = MpRouteLink;
```

No se proporciona descripción.

### ForgeRutaPartido

**Tipo:** tipo

```typescript
export type ForgeRouteMatch = MpRouteMatch;
```

No se proporciona descripción.

### ForjaRutaMeta

**Tipo:** tipo

```typescript
export type ForgeRouteMeta = MpRouteMeta;
```

No se proporciona descripción.

### ForgeRouteMetadatos

**Tipo:** tipo

```typescript
export type ForgeRouteMetadata = MpRouteMetadata;
```

No se proporciona descripción.

### Parámetros de ruta de Forge

**Tipo:** tipo

```typescript
export type ForgeRouteParameters = MpRouteParameters;
```

No se proporciona descripción.

### ForgeRouterAdaptador

**Tipo:** tipo

```typescript
export type ForgeRouterAdapter = MpRouterAdapter;
```

No se proporciona descripción.

### ForgeRouteRecord

**Tipo:** tipo

```typescript
export type ForgeRouteRecord<View = unknown> = MpRoute<View>;
```

No se proporciona descripción.

### ForgeRutaObjetivo

**Tipo:** tipo

```typescript
export type ForgeRouteTarget = MpRouteLocationRaw;
```

No se proporciona descripción.

### ForgeRouteViewAdaptador

**Tipo:** tipo

```typescript
export type ForgeRouteViewAdapter<View = unknown, Outlet = unknown> = MpRouteViewAdapter<View, Outlet>;
```

No se proporciona descripción.

### ForgeRouteViewContext

**Tipo:** tipo

```typescript
export type ForgeRouteViewContext<View = unknown> = MpRouteViewContext<View>;
```

No se proporciona descripción.

### ForgeScrollComportamiento

**Tipo:** tipo

```typescript
export type ForgeScrollBehavior = MpScrollBehavior;
```

No se proporciona descripción.

### ForgeDesplazamientoPosición

**Tipo:** tipo

```typescript
export type ForgeScrollPosition = MpScrollPosition;
```

No se proporciona descripción.

## `src/define-routes`

### crearRouteResolver

**Tipo:** función

```typescript
function createRouteResolver(routes: readonly MpRoute[]): MpRouteResolver;
```

Cree un {@link MpRouteResolver} vinculado a un árbol de rutas, aplanándolo una vez
Las llamadas repetidas `match`/`resolve` reutilizan la misma lista de rutas absoluta.

#### Parámetros

| Nombre | Tipo                      | Descripción |
| ------ | ------------------------- | ----------- |
| rutas  | MpRoute de sólo lectura[] |             |

### definir rutas

**Tipo:** función

```typescript
function defineRoutes(routes: T): T;
```

Asistente de identidad que define un árbol de ruta neutral en el marco con tipo completo
inferencia. Úselo para que los editores verifiquen las rutas `name`s, `meta` y las secundarias.

#### Parámetros

| Nombre | Tipo | Descripción |
| ------ | ---- | ----------- |
| rutas  | T    |             |

#### Contrato

- **@ejemplo:** rutas constantes = definirRutas([
  {ruta: '/', nombre: 'casa', componente: Casa },
  { ruta: '/usuarios/:id', nombre: 'usuario', componente: Usuario },
  ])

### buscar ruta por nombre

**Tipo:** función

```typescript
function findRouteByName(routes: readonly MpRoute[], name: string): MpFlatRoute | undefined;
```

Encuentra la primera ruta aplanada con el nombre de pila.

#### Parámetros

| Nombre | Tipo                      | Descripción |
| ------ | ------------------------- | ----------- |
| rutas  | MpRoute de sólo lectura[] |             |
| nombre | cadena                    |             |

### aplanarRutas

**Tipo:** función

```typescript
function flattenRoutes(routes: readonly MpRoute[]): MpFlatRoute[];
```

Aplanar un árbol de rutas anidadas en una lista de rutas absolutas en profundidad, con
`path` de cada ruta resuelta contra sus padres y `meta` heredado de
sus antepasados.

#### Parámetros

| Nombre | Tipo                      | Descripción |
| ------ | ------------------------- | ----------- |
| rutas  | MpRoute de sólo lectura[] |             |

### partidoRutas

**Tipo:** función

```typescript
function matchRoutes(routes: readonly MpRoute[], pathname: string): MpRouteMatch | undefined;
```

Haga coincidir un nombre de ruta con un árbol de rutas, devolviendo la primera ruta (en
orden de definición en profundidad) cuyo patrón coincida, o `undefined`.

#### Parámetros

| Nombre         | Tipo                      | Descripción |
| -------------- | ------------------------- | ----------- |
| rutas          | MpRoute de sólo lectura[] |             |
| nombre de ruta | cadena                    |             |

### MpPlanaRuta

**Tipo:** componente

```typescript
export interface MpFlatRoute
```

Una ruta reducida a una ruta absoluta, con metadatos y ancestros heredados.

### MpRoutePartido

**Tipo:** componente

```typescript
export interface MpRouteMatch
```

Una coincidencia exitosa de un nombre de ruta con un árbol de rutas.

### MpRouteResolver

**Tipo:** componente

```typescript
export interface MpRouteResolver
```

Un solucionador vinculado a un único árbol de ruta.

### resolverUbicación

**Tipo:** función

```typescript
function resolveLocation(to: MpRouteLocationRaw, routes: readonly MpRoute[] = []): MpResolvedLocation;
```

Resuelva cualquier {@link MpRouteLocationRaw} neutral en el marco de trabajo en un formato completamente formado.
{@link MpResolvedLocation}. Las ubicaciones de las cadenas y las rutas se comparan con las
árbol de rutas (cuando se proporciona) para completar `params`/`name`/`meta`; nombrado
Las ubicaciones construyen su camino a partir de la ruta correspondiente.

#### Parámetros

| Nombre | Tipo                      | Descripción |
| ------ | ------------------------- | ----------- |
| a      | MpRouteLocationRaw        |             |
| rutas  | MpRoute de sólo lectura[] |             |

#### Contrato

- **@ejemplo:** resolveLocation({ nombre: 'usuario', parámetros: { id: 42 } }, rutas)
  // → { ruta: '/usuarios/42', ruta completa: '/usuarios/42', parámetros: { id: '42' },… }

## `src/location`

### MpUbicaciónEntrada

**Tipo:** componente

```typescript
export interface MpLocationInput
```

Una ubicación descrita por sus partes, con consulta/hash opcional.

### MpUbicaciónPartes

**Tipo:** componente

```typescript
export interface MpLocationParts
```

Las partes descompuestas de una URL: nombre de ruta, consulta y hash.

### normalizarHash

**Tipo:** función

```typescript
function normalizeHash(hash: string): string;
```

Normalice un fragmento hash para que incluya un único `#` inicial o esté vacío.

#### Parámetros

| Nombre    | Tipo   | Descripción |
| --------- | ------ | ----------- |
| picadillo | cadena |             |

### analizarUbicación

**Tipo:** función

```typescript
function parseLocation(url: string): MpLocationParts;
```

Divida una URL relativa a una aplicación en sus {@link MpLocationParts}.

#### Parámetros

| Nombre | Tipo   | Descripción |
| ------ | ------ | ----------- |
| URL    | cadena |             |

#### Contrato

- **@ejemplo:** parseLocation('/users/42?tab=info#bio')
  // → { ruta: '/usuarios/42', consulta: { pestaña: 'información' }, hash: '#bio' }

### stringifyUbicación

**Tipo:** función

```typescript
function stringifyLocation(location: MpLocationInput): string;
```

Ensamble una cadena URL relativa a la aplicación a partir de partes de ubicación. El nombre de la ruta es
normalizada, la consulta serializada (con un `?` inicial cuando no está vacía) y la
hash con el prefijo `#` cuando no está vacío.

#### Parámetros

| Nombre    | Tipo              | Descripción |
| --------- | ----------------- | ----------- |
| ubicación | MpLocationEntrada |             |

#### Contrato

- **@ejemplo:** stringifyLocation({ ruta: '/usuarios/42', consulta: { pestaña: 'info' }, hash: 'bio' })
  // → '/usuarios/42?tab=info#bio'

## `src/path`

### ruta de construcción

**Tipo:** función

```typescript
function buildPath(
  pattern: string,
  parameters: Record<string, MpParameterValue | readonly MpParameterValue[]> = {},
): string;
```

Construya un nombre de ruta concreto a partir de un patrón y un conjunto de parámetros, codificando
cada valor. Los segmentos opcionales se eliminan cuando su valor está ausente; un
faltan lanzamientos de parámetros requeridos.

#### Parámetros

| Nombre     | Tipo                                                              | Descripción |
| ---------- | ----------------------------------------------------------------- | ----------- |
| patrón     | cadena                                                            |             |
| parámetros | Registro<string, MpParameterValue \| readonly MpParameterValue[]> |             |

#### Contrato

- **@ejemplo:** buildPath('/usuarios/:id', { id: 42 }) // → '/usuarios/42'
  buildPath('/usuarios/:id?', {}) // → '/usuarios'
  buildPath('/archivos/:rest*', { resto: ['a', 'b'] }) // → '/files/a/b'

### ruta de compilación

**Tipo:** función

```typescript
function compilePath(pattern: string): MpCompiledPath;
```

Compile un patrón de ruta en un {@link MpCompiledPath} con el orden
claves de parámetros y una expresión regular coincidente.

#### Parámetros

| Nombre | Tipo   | Descripción |
| ------ | ------ | ----------- |
| patrón | cadena |             |

#### Contrato

- **@ejemplo:** compilePath('/users/:id').regexp.test('/users/42') // → verdadero

### ruta de coincidencia

**Tipo:** función

```typescript
function matchPath(pattern: string, pathname: string): MpRouteParameters | undefined;
```

Haga coincidir un nombre de ruta con un patrón, devolviendo los parámetros extraídos o
`undefined` si el nombre de la ruta no coincide.

#### Parámetros

| Nombre         | Tipo   | Descripción |
| -------------- | ------ | ----------- |
| patrón         | cadena |             |
| nombre de ruta | cadena |             |

#### Contrato

- **@ejemplo:** matchPath('/usuarios/:id', '/usuarios/42') // → { id: '42' }
  matchPath('/usuarios/:id', '/posts/42') // → indefinido

### MpCompiledPath

**Tipo:** componente

```typescript
export interface MpCompiledPath
```

El resultado de compilar un patrón de ruta en un comparador.

### MpPathParameterKey

**Tipo:** componente

```typescript
export interface MpPathParameterKey
```

Una clave de parámetro único extraída de un patrón de ruta compilado.

### normalizar ruta

**Tipo:** función

```typescript
function normalizePath(path: string): string;
```

Normalizar un patrón de ruta (o nombre de ruta) a una forma canónica de barra diagonal inicial
sin barra diagonal (excepto la raíz, que permanece `/`).

#### Parámetros

| Nombre | Tipo   | Descripción |
| ------ | ------ | ----------- |
| camino | cadena |             |

### WILDCARD_PARAM_KEY

**Tipo:** constante

```typescript
export const WILDCARD_PARAM_KEY;
```

La clave bajo la cual se captura un segmento general `*` independiente.

## `src/query`

### analizarQuery

**Tipo:** función

```typescript
function parseQuery(search: string): MpQueryParameters;
```

Analiza una cadena de consulta en un mapa {@link MpQueryParameters}. Un `?` líder es
opcional. Las claves repetidas se colapsan en una matriz de valores, en orden.

#### Parámetros

| Nombre | Tipo   | Descripción |
| ------ | ------ | ----------- |
| buscar | cadena |             |

#### Contrato

- **@ejemplo:** parseQuery('?tag=a&tag=b&page=2') // → { etiqueta: ['a', 'b'], página: '2' }

### stringifyQuery

**Tipo:** función

```typescript
function stringifyQuery(query: MpQueryInput): string;
```

Serializar un mapa {@link MpQueryInput} en una cadena de consulta con un `?` inicial,
o una cadena vacía cuando no hay nada que serializar. `null`/`undefined`
los valores (y los elementos de la matriz) se eliminan; Las matrices emiten un par `key=value` cada una.

#### Parámetros

| Nombre   | Tipo            | Descripción |
| -------- | --------------- | ----------- |
| consulta | Entrada MpQuery |             |

#### Contrato

- **@ejemplo:** stringifyQuery({ etiqueta: ['a', 'b'], página: 2 }) // → '?tag=a&tag=b&page=2'
  stringifyQuery({}) // → ''

## `src/types`

### MpGuardResultado

**Tipo:** componente

```typescript
export type MpGuardOutcome = void | boolean | MpRouteLocationRaw;
```

El resultado devuelto por un guardia de ruta antes de que se confirme una transición.

### MpHistoria

**Tipo:** componente

```typescript
export interface MpHistory
```

Una implementación de navegador, hash o historial de memoria proporcionada por un tiempo de ejecución.

### MpHistoryEntrada

**Tipo:** componente

```typescript
export interface MpHistoryEntry
```

No se proporciona descripción.

### MpHistoriaEvento

**Tipo:** componente

```typescript
export interface MpHistoryEvent
```

No se proporciona descripción.

### MpHistoryListener

**Tipo:** componente

```typescript
export type MpHistoryListener = (event: MpHistoryEvent) => void;
```

No se proporciona descripción.

### MpHistorialModo

**Tipo:** componente

```typescript
export type MpHistoryMode = 'web' | 'hash' | 'memory';
```

La estrategia histórica que debe utilizar el enrutador de un adaptador.

- `web`: historial HTML5 (URL limpias), el valor predeterminado para las aplicaciones del navegador.
- `hash` — historial basado en hash (`/#/path`), para alojamiento estático sin
  reescritura del servidor.
- `memory`: historial en memoria sin URL, para SSR y pruebas.

### MpMetadataContexto

**Tipo:** componente

```typescript
export interface MpMetadataContext
```

No se proporciona descripción.

### MpMetadataHook

**Tipo:** componente

```typescript
export type MpMetadataHook = (context: MpMetadataContext) => MpRouteMetadata | void | Promise<MpRouteMetadata | void>;
```

No se proporciona descripción.

### MpNombreUbicación

**Tipo:** componente

```typescript
export interface MpNamedLocation
```

Un objetivo de navegación expresado por el nombre de la ruta.

### MpNavigationContext

**Tipo:** componente

```typescript
export interface MpNavigationContext
```

No se proporciona descripción.

### Error de navegación Mp

**Tipo:** componente

```typescript
export interface MpNavigationFailure
```

No se proporciona descripción.

### MpNavigationFailureType

**Tipo:** componente

```typescript
export type MpNavigationFailureType = 'aborted' | 'cancelled' | 'duplicated' | 'error' | 'not-found';
```

No se proporciona descripción.

### MpNavigationRedirect

**Tipo:** componente

```typescript
export interface MpNavigationRedirect
```

No se proporciona descripción.

### MpNavigationResult

**Tipo:** componente

```typescript
export type MpNavigationResult = MpNavigationSuccess | MpNavigationRedirect | MpNavigationFailure;
```

No se proporciona descripción.

### MpNavigationÉxito

**Tipo:** componente

```typescript
export interface MpNavigationSuccess
```

No se proporciona descripción.

### Tipo de navegación Mp

**Tipo:** componente

```typescript
export type MpNavigationType = 'push' | 'replace' | 'pop' | 'go';
```

Una operación de transición iniciada por un enrutador o una implementación histórica.

### ValorParámetroMp

**Tipo:** componente

```typescript
export type MpParameterValue = string | number | boolean | null | undefined;
```

Un valor de entrada de parámetro de consulta/ruta único y neutral al marco.

### MpPathUbicación

**Tipo:** componente

```typescript
export interface MpPathLocation
```

Un objetivo de navegación expresado por ruta.

### MpQueryEntrada

**Tipo:** componente

```typescript
export type MpQueryInput = Record<string, MpParameterValue | readonly MpParameterValue[]>;
```

Entrada de consulta sin formato y neutral al marco. Los valores pueden ser escalares o matrices (repetidas
claves); Los valores `null`/`undefined` se eliminan cuando se serializan.

### Parámetros MpQuery

**Tipo:** componente

```typescript
export type MpQueryParameters = Record<string, string | string[]>;
```

Parámetros de consulta analizados: una cadena escalar o una matriz de cadenas por clave.

### MpReadonlySeñal

**Tipo:** componente

```typescript
export interface MpReadonlySignal<Value>
```

Estado reactivo estructural utilizado por adaptadores sin prescribir una estructura primitiva.

### MpRedireccionamiento

**Tipo:** componente

```typescript
export type MpRedirect =
  MpRouteLocationRaw | ((to: MpResolvedLocation) => MpRouteLocationRaw | Promise<MpRouteLocationRaw>);
```

Un destino de redireccionamiento, calculado opcionalmente a partir de la ruta de destino.

### MpResueltoUbicación

**Tipo:** componente

```typescript
export interface MpResolvedLocation
```

Una ubicación totalmente resuelta, neutral en cuanto al marco.

### MpRuta

**Tipo:** componente

```typescript
export interface MpRoute<View = unknown>
```

Una definición de ruta neutral en el marco. El mismo árbol se puede traducir a
cualquier enrutador de destino (enrutador vue, enrutador react, enrutador TanStack) o basado en archivos
convención (Next.js, Nuxt) mediante un adaptador.

### Evento MpRouteChange

**Tipo:** componente

```typescript
export interface MpRouteChangeEvent
```

No se proporciona descripción.

### MpRouteChangeListener

**Tipo:** componente

```typescript
export type MpRouteChangeListener = (event: MpRouteChangeEvent) => void;
```

No se proporciona descripción.

### MpRouteGuard

**Tipo:** componente

```typescript
export type MpRouteGuard = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
) => MpGuardOutcome | Promise<MpGuardOutcome>;
```

Una protección de ruta evaluada por una máquina de estado de navegación propiedad del tiempo de ejecución.

### MpRouteEnlace

**Tipo:** componente

```typescript
export interface MpRouteLink
```

No se proporciona descripción.

### MpRutaUbicaciónSin procesar

**Tipo:** componente

```typescript
export type MpRouteLocationRaw = string | MpPathLocation | MpNamedLocation;
```

Un objetivo de navegación neutral en el marco: una cadena de URL o un objeto estructurado.

### MpRutaMeta

**Tipo:** componente

```typescript
export type MpRouteMeta = Record<string, unknown>;
```

Metadatos arbitrarios y neutrales adjuntos a una ruta.

### MpRouteMetadatos

**Tipo:** componente

```typescript
export interface MpRouteMetadata extends MpRouteMeta
```

No se proporciona descripción.

### MpRouteParámetros

**Tipo:** componente

```typescript
export type MpRouteParameters = Record<string, string | string[]>;
```

Parámetros de ruta resueltos extraídos de una ruta coincidente. Un valor es una matriz
solo para segmentos repetibles (generales) como `:rest*`.

### Enrutador Mp

**Tipo:** componente

```typescript
export interface MpRouter
```

El contrato de navegación neutral en el marco lo cumple cada adaptador. Los adaptadores pueden
exponer equivalentes reactivos (por ejemplo, el `useMpRoute` del adaptador Vue devuelve un
ref reactiva), pero la superficie imperativa es la misma en todas partes.

### Adaptador MpRouter

**Tipo:** componente

```typescript
export interface MpRouterAdapter
```

Contrato de navegación y estado del enrutador propiedad del tiempo de ejecución.

### Adaptador MpRouteView

**Tipo:** componente

```typescript
export interface MpRouteViewAdapter<View = unknown, Outlet = unknown>
```

No se proporciona descripción.

### MpRouteViewContext

**Tipo:** componente

```typescript
export interface MpRouteViewContext<View = unknown>
```

No se proporciona descripción.

### Comportamiento MpScroll

**Tipo:** componente

```typescript
export type MpScrollBehavior = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
  savedPosition?: MpScrollPosition,
) => MpScrollPosition | false | void | Promise<MpScrollPosition | false | void>;
```

No se proporciona descripción.

### MpScrollPosition

**Tipo:** componente

```typescript
export interface MpScrollPosition
```

No se proporciona descripción.
