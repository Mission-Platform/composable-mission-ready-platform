/**
 * Styling for the write-once `ForgeQrCode`.
 *
 * `ForgeQrCode` owns this co-located CSS Module: it side-effect-imports the
 * stylesheet so its appearance ships in the built package CSS (the two-stage
 * compiler carries the import onto both the React and Vue builds).
 *
 * The rules live in the package-owned `mp.qr-code` cascade layer.
 */
/**
 * Registered inherited custom properties for forge-qr-code overrides.
 *
 * Kept in a dedicated partial so the component module can stay focused on
 * selectors while @property initial values remain token-backed defaults.
 */
@property --forge-qr-code-spacing-3 {
  syntax: "*";
  inherits: true;
  initial-value: var(--mp-spacing-3);
}
@property --forge-qr-code-spacing-2 {
  syntax: "*";
  inherits: true;
  initial-value: var(--mp-spacing-2);
}
@layer mp.qr-code {
  .forge-qr-code {
    display: block;
    max-width: 100%;
    height: auto;
  }
  .forge-qr-code-figure {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: var(--forge-qr-code-spacing-3, var(--mp-spacing-3));
  }
  .forge-qr-code__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--forge-qr-code-spacing-2, var(--mp-spacing-2));
  }
  .forge-qr-code__logo-plate,
  .forge-qr-code__logo {
    pointer-events: none;
  }
}
