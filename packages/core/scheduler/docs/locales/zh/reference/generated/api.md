# @mission-platform/scheduler API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/core/scheduler/docs/reference/generated/api.md: [packages/core/scheduler/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/scheduler` 中的公共源声明生成。

## `src/components/organisms/forge-scheduler/forge-scheduler`

### 锻造调度器

**种类：**功能

```typescript
function ForgeScheduler(properties: Readonly<SchedulerProperties>): MpElement;
```

`ForgeScheduler` — 在中性 JSX 中编写的完整日历/调度程序
方言并直接编译为 React 或 Vue
`@mission-platform/vite-plugin-forge`。

与 Vue 原始版本完全同等：工具栏（今天/上一页-下一页/标题/
新事件/五视图切换器）位于活动视图上方 - **时间网格**
（天/3天/周），具有小时行和列碰撞事件放置，
**月网格**（6×7），或**年网格**（12 个小月）。事件为 RFC 5545
{@link VEvent}；所有繁重的逻辑（递归扩展、视图范围、
碰撞布局、持续时间格式化）来自调度程序包核心，
因此 Vue 和 JSX 调度程序的行为相同（构造奇偶校验）。
时间网格事件可以拖到新的
时间并通过位于同一位置的指针拖动助手调整大小；事件对话框
（迁移的{@link ForgeDialog}）创建/编辑/删除事件。

Vue SFC 的替代：`@mission-platform/icons` V 形变为
`‹`/`›` 字形； `vue-i18n`/labels 是内联字符串； `v-model` +
`event-click` 发出成为 `onUpdateModelValue` / `onEventClick` 回调
道具。

#### 参数

| 名称 | 类型                      | 描述 |
| ---- | ------------------------- | ---- |
| 属性 | 只读<SchedulerProperties> |      |

### 调度程序属性

**种类：**接口

```typescript
export interface SchedulerProperties
```

没有提供描述。

### 调度程序大小

**种类：**类型

```typescript
export type SchedulerSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

尺寸标记 — 规范 2xs → 2xl 比例。

## `src/core/events`

### 应用事件补丁

**种类：**功能

```typescript
function applyEventPatch(event: VEvent, patch: Partial<Omit<VEvent, 'uid'>>): VEvent;
```

将补丁应用于事件，碰撞 `lastModified` 和 `sequence`（标准
RFC 5545 修订语义）。 `uid` 被保留。

#### 参数

| 名称 | 类型                      | 描述 |
| ---- | ------------------------- | ---- |
| 活动 | V活动                     |      |
| 补丁 | 部分<Omit<VEvent, 'uid'>> |      |

### 创建事件

**种类：**功能

```typescript
function createEvent(partial: Omit<VEvent, 'uid' | 'dtstamp'>): VEvent;
```

从部分构建全新的{@link VEvent}（没有 `uid`/`dtstamp`），
生成 UID 和创建/修改时间戳。

#### 参数

| 名称 | 类型                             | 描述 |
| ---- | -------------------------------- | ---- |
| 部分 | 省略<VEvent, 'uid' \| 'dtstamp'> |      |

### 当日活动

**种类：**功能

```typescript
function eventsForDay(events: VEvent[], day: Date): VEvent[];
```

返回给定日历日发生的事件。

#### 参数

| 名称 | 类型    | 描述 |
| ---- | ------- | ---- |
| 活动 | V事件[] |      |
| 日   | 日期    |      |

### 范围事件

**种类：**功能

```typescript
function eventsForRange(events: VEvent[], start: Date, end: Date): VEvent[];
```

返回与给定重叠的事件（包括重复发生的事件）
日期范围[开始、结束)。 排除带有 `status === 'CANCELLED'` 的事件。
重复事件通过其 RRULE / RDATE 进行扩展； EXDATE 排除是
很荣幸。

#### 参数

| 名称 | 类型    | 描述 |
| ---- | ------- | ---- |
| 活动 | V事件[] |      |
| 开始 | 日期    |      |
| 结束 | 日期    |      |

### 格式持续时间

**种类：**功能

```typescript
function formatDuration(event: VEvent): string;
```

返回事件的人类可读的持续时间字符串，例如“1小时30m”。

#### 参数

| 名称 | 类型  | 描述 |
| ---- | ----- | ---- |
| 活动 | V活动 |      |

### 布局日

**种类：**功能

```typescript
function layoutDay(events: VEvent[]): SchedulerEventSlot[];
```

使用简单的基于列的碰撞来布置一天的事件
算法：每个事件都放置在最左边的列中，并且不重叠，
每个占用 `column` 或 `totalColumns` 并行磁道。

#### 参数

| 名称 | 类型    | 描述 |
| ---- | ------- | ---- |
| 活动 | V事件[] |      |

### 移动事件补丁

**种类：**功能

```typescript
function moveEventPatch(event: VEvent, deltaMs: number): Pick<VEvent, 'dtstart' | 'dtend'>;
```

`{ dtstart, dtend }` 补丁将事件移动 `deltaMs` 毫秒，
保留其全天/定时 ISO 表示。

#### 参数

| 名称    | 类型  | 描述 |
| ------- | ----- | ---- |
| 活动    | V活动 |      |
| DeltaMs | 数量  |      |

### 调整大小事件补丁

**种类：**功能

```typescript
function resizeEventPatch(event: VEvent, deltaMs: number): Pick<VEvent, 'dtend'>;
```

`{ dtend }` 补丁通过 `deltaMs` 调整事件结束的大小，夹紧以便
活动持续时间至少 15 分钟（如果是全天活动，则为一天）。

#### 参数

| 名称    | 类型  | 描述 |
| ------- | ----- | ---- |
| 活动    | V活动 |      |
| DeltaMs | 数量  |      |

## `src/core/range`

### 步锚

**种类：**功能

```typescript
function stepAnchor(view: SchedulerView, anchor: Date, direction: -1 | 1): Date;
```

将 `anchor` 步进 `direction`（-1 = 上一个，+1 = 下一个）视图单元。
天/三天/周以天为单位；他们的周期中的月/年步长。

#### 参数

| 名称 | 类型     | 描述 |
| ---- | -------- | ---- |
| 查看 | 调度视图 |      |
| 锚   | 日期     |      |
| 方向 | -1 \| 1  |      |

### 可见范围

**种类：**功能

```typescript
function visibleRangeFor(view: SchedulerView, anchor: Date, weekStartsOn: WeekStart = 0): DateRange;
```

给定 `view` 锚定的可见半开 `[start, end)` 窗口
`anchor`，表彰 `weekStartsOn` 的每周观看次数。

#### 参数

| 名称     | 类型     | 描述 |
| -------- | -------- | ---- |
| 查看     | 调度视图 |      |
| 锚       | 日期     |      |
| 周开始于 | 周开始   |      |

## `src/core/recurrence`

### 展开重复

**种类：**功能

```typescript
function expandRecurrences(event: VEvent, rangeStart: Date, rangeEnd: Date): VEvent[];
```

将带有 RRULE 的单个 VEvent 扩展到所有重叠的事件中
与 [rangeStart, rangeEnd)。 返回虚拟 VEvent 克隆 — 每个
发生 — 调整 dtstart / dtend。

还处理 RDATE（额外日期）和 EXDATE（排除日期）。

安全上限：发生 3 650 次后停止（约 10 年的日常事件）
防止无限规则上的无限循环。

#### 参数

| 名称     | 类型  | 描述 |
| -------- | ----- | ---- |
| 活动     | V活动 |      |
| 范围开始 | 日期  |      |
| 范围结束 | 日期  |      |

## `src/core/types`

### 日期范围

**种类：**接口

```typescript
export interface DateRange
```

半开放日期范围 `[start, end)`。

### 调度程序事件槽

**种类：**接口

```typescript
export interface SchedulerEventSlot
```

位于调度程序的时间网格碰撞布局中的事件。

### 调度视图

**种类：**类型

```typescript
export type SchedulerView = 'day' | 'three-day' | 'week' | 'month' | 'year';
```

ForgeScheduler 中提供日历视图模式。

### 周开始

**种类：**类型

```typescript
export type WeekStart = 0 | 1 | 2 | 3 | 4 | 5 | 6;
```

基于周的视图的第一天开始于（0 = 星期日 ... 6 = 星期六）。
