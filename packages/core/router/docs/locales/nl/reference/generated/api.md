# @mission-platform/router API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> packages/core/router/docs/reference/generated/api.md: [packages/core/router/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/router`.

## `src/capabilities`

### createMpRouterCapabilities

**Soort:** functie

```typescript
function createMpRouterCapabilities(overrides: MpRouterCapabilityOverrides = {}): MpRouterCapabilities;
```

Maak een neutraal capaciteitsobject voor een doeladapter, SSR of een test.

#### Parameters

| Naam         | Typ                                | Beschrijving |
| ------------ | ---------------------------------- | ------------ |
| overschrijft | MpRouterCapabilityOverschrijvingen |              |

### isMpRouterCapabilityError

**Soort:** functie

```typescript
function isMpRouterCapabilityError(error: unknown): error is MpRouterCapabilityError;
```

Of een onbekende fout de deterministische terugvalfout van de neutrale router is.

#### Parameters

| Naam | Typ      | Beschrijving |
| ---- | -------- | ------------ |
| fout | onbekend |              |

### MP_ROUTER_COMPILER_MARKER

**Soort:** constant

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

Stabiele metadata die worden gebruikt door Forge-routercompilerplug-ins.

### MpLink

**Soort:** component

```typescript
function MpLink(properties: MpLinkProps): MpLinkDescriptor;
```

Neutrale linkmarkering. Native doelen vervangen deze functie tijdens compilatie.

#### Parameters

| Naam          | Typ         | Beschrijving |
| ------------- | ----------- | ------------ |
| eigenschappen | MpLinkProps |              |

### MpLinkDescriptor

**Soort:** component

```typescript
export interface MpLinkDescriptor
```

Een raamwerkneutrale linkbeschrijving die wordt gebruikt door SSR en niet-gecompileerde tests.

### MpLinkOpties

**Soort:** component

```typescript
export interface MpLinkOptions
```

Opties gedeeld door neutrale links en native navigatie-oproepen.

### MpLinkProps

**Soort:** component

```typescript
export interface MpLinkProps extends MpLinkOptions
```

Rekwisieten geaccepteerd door de compilermarkering, weergegeven door {@link MpLink}.

### MpNavigatieOpties

**Soort:** component

```typescript
export interface MpNavigationOptions extends MpLinkOptions
```

Opties doorgestuurd naar een native navigatie-implementatie.

### MpRouter-mogelijkheden

**Soort:** component

```typescript
export interface MpRouterCapabilities
```

De mogelijkheden die worden geleverd door de eigen router van de verbruikende applicatie.

### MpRouter-capaciteit

**Soort:** component

```typescript
export type MpRouterCapability = 'link' | 'route' | 'navigate' | 'resolve' | 'view';
```

Mogelijkheden die een routerdoel onafhankelijk kan verlagen.

### MpRouterCapabilityFout

**Soort:** component

```typescript
export class MpRouterCapabilityError extends Error
```

Stabiele diagnose wordt gegenereerd wanneer een bewerking niet kan worden uitgevoerd door de fallback.

### MpRouterCapabilityOverschrijvingen

**Soort:** component

```typescript
export type MpRouterCapabilityOverrides = Partial<MpRouterCapabilities>;
```

Een gedeeltelijke implementatie die nuttig is voor het richten op adapters en SSR/testharnassen.

### MpRouterView

**Soort:** component

```typescript
function MpRouterView(properties: MpRouterViewProps = {}): MpRouterViewDescriptor;
```

Neutrale uitlaat/zichtmarkering. Native doelen vervangen deze functie tijdens compilatie.

#### Parameters

| Naam          | Typ               | Beschrijving |
| ------------- | ----------------- | ------------ |
| eigenschappen | MpRouterViewProps |              |

### MpRouterViewDescriptor

**Soort:** component

```typescript
export interface MpRouterViewDescriptor
```

Een raamwerkneutrale weergavebeschrijving die wordt gebruikt door SSR en niet-gecompileerde tests.

### MpRouterViewProps

**Soort:** component

```typescript
export interface MpRouterViewProps
```

Rekwisieten geaccepteerd door de neutrale outlet/view-compilermarkering.

### oplossingMpLink

**Soort:** functie

```typescript
function resolveMpLink(
  to: MpRouteLocationRaw,
  capabilities: Pick<MpRouterCapabilities, 'resolve'> = useMpRouter(),
): string;
```

Los een doel op via de router van de app. De optionele mogelijkheid
argument maakt de helper deterministisch in SSR/tests en doeladapters.

#### Parameters

| Naam          | Typ                                   | Beschrijving |
| ------------- | ------------------------------------- | ------------ |
| naar          | MpRouteLocationRaw                    |              |
| mogelijkheden | Kies<MpRouterCapabilities, 'resolve'> |              |

### gebruik MpNavigatie

**Soort:** functie

```typescript
function useMpNavigation(): Pick<MpRouterCapabilities, 'navigate' | 'resolve'>;
```

Lees alleen de dwingende navigatiemogelijkheden.

### gebruikMpRoute

**Soort:** functie

```typescript
function useMpRoute(): MpResolvedLocation | null;
```

Lees de huidige route van de app. De niet-gecompileerde fallback is SSR-veilig en
retourneert `null`; een compilerdoel vervangt deze aanroep door de oorspronkelijke routestatus.

### gebruikMpRouter

**Soort:** functie

```typescript
function useMpRouter(): MpRouterCapabilities;
```

Lees het door de app geleverde neutrale capaciteitsobject.

## `src/compatibility-fixtures`

### toepassingCompatibiliteitArmaturen

**Soort:** constant

```typescript
export const applicationCompatibilityFixtures;
```

Geen beschrijving opgegeven.

### docsCompatibiliteitArmatuur

**Soort:** constant

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Routerecords die de tabel locale/search/catch-all van de documentatie-app weerspiegelen.

### documentatieCompatibiliteitArmatuur

**Soort:** constant

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Routerecords die de tabel locale/search/catch-all van de documentatie-app weerspiegelen.

### ForgeCompatibiliteitCase

**Soort:** interface

```typescript
export interface ForgeCompatibilityCase
```

Geen beschrijving opgegeven.

### ForgeCompatibiliteitArmatuur

**Soort:** interface

```typescript
export interface ForgeCompatibilityFixture
```

Geen beschrijving opgegeven.

### myCareNotesCompatibiliteitArmatuur

**Soort:** constant

```typescript
export const myCareNotesCompatibilityFixture: ForgeCompatibilityFixture;
```

Routerecords die het taalsegment en URL-gestuurde overlays van My Care Notes weerspiegelen.

### routerCompatibiliteitArmaturen

**Soort:** constant

```typescript
export const routerCompatibilityFixtures: readonly ForgeCompatibilityCase[];
```

Rijen met gedeeld gedrag die worden gebruikt door de contracttestsuite van elke runtime.

### websiteCompatibiliteitArmatuur

**Soort:** constant

```typescript
export const websiteCompatibilityFixture: ForgeCompatibilityFixture;
```

Routerecords die de optionele thuisroute met vooraf ingestelde landinstelling van de website weerspiegelen.

## `src/contracts`

### ForgeGuardOutcome

**Soort:** type

```typescript
export type ForgeGuardOutcome = MpGuardOutcome;
```

Geen beschrijving opgegeven.

### ForgeGeschiedenis

**Soort:** type

```typescript
export type ForgeHistory = MpHistory;
```

Een browser-, hash- of geheugengeschiedenisimplementatie die door een runtime wordt geleverd.

### ForgeGeschiedenisInvoer

**Soort:** type

```typescript
export type ForgeHistoryEntry = MpHistoryEntry;
```

Geen beschrijving opgegeven.

### ForgeGeschiedenisEvent

**Soort:** type

```typescript
export type ForgeHistoryEvent = MpHistoryEvent;
```

Geen beschrijving opgegeven.

### ForgeGeschiedenisListener

**Soort:** type

```typescript
export type ForgeHistoryListener = MpHistoryListener;
```

Geen beschrijving opgegeven.

### ForgeGeschiedenisModus

**Soort:** type

```typescript
export type ForgeHistoryMode = MpHistoryMode;
```

Geen beschrijving opgegeven.

### ForgeMetadataContext

**Soort:** type

```typescript
export type ForgeMetadataContext = MpMetadataContext;
```

Geen beschrijving opgegeven.

### ForgeMetadataHook

**Soort:** type

```typescript
export type ForgeMetadataHook = MpMetadataHook;
```

Geen beschrijving opgegeven.

### ForgeNamedTarget

**Soort:** type

```typescript
export type ForgeNamedTarget = MpNamedLocation;
```

Geen beschrijving opgegeven.

### ForgeNavigatieContext

**Soort:** type

```typescript
export type ForgeNavigationContext = MpNavigationContext;
```

Geen beschrijving opgegeven.

### ForgeNavigatieFout

**Soort:** type

```typescript
export type ForgeNavigationFailure = MpNavigationFailure;
```

Geen beschrijving opgegeven.

### ForgeNavigationFailureType

**Soort:** type

```typescript
export type ForgeNavigationFailureType = MpNavigationFailureType;
```

Geen beschrijving opgegeven.

### ForgeNavigatieRedirect

**Soort:** type

```typescript
export type ForgeNavigationRedirect = MpNavigationRedirect;
```

Geen beschrijving opgegeven.

### ForgeNavigatieResultaat

**Soort:** type

```typescript
export type ForgeNavigationResult = MpNavigationResult;
```

Geen beschrijving opgegeven.

### ForgeNavigatieSucces

**Soort:** type

```typescript
export type ForgeNavigationSuccess = MpNavigationSuccess;
```

Geen beschrijving opgegeven.

### ForgeNavigatieType

**Soort:** type

```typescript
export type ForgeNavigationType = MpNavigationType;
```

Geen beschrijving opgegeven.

### ForgeParameterwaarde

**Soort:** type

```typescript
export type ForgeParameterValue = MpParameterValue;
```

Geen beschrijving opgegeven.

### ForgePathDoel

**Soort:** type

```typescript
export type ForgePathTarget = MpPathLocation;
```

Geen beschrijving opgegeven.

### ForgeQuery

**Soort:** type

```typescript
export type ForgeQuery = MpQueryParameters;
```

Geen beschrijving opgegeven.

### ForgeQueryInvoer

**Soort:** type

```typescript
export type ForgeQueryInput = MpQueryInput;
```

Geen beschrijving opgegeven.

### ForgeReadonlySignaal

**Soort:** type

```typescript
export type ForgeReadonlySignal<Value> = MpReadonlySignal<Value>;
```

Structurele reactieve toestand die door adapters wordt gebruikt zonder een raamwerkprimitief voor te schrijven.

### ForgeRedirect

**Soort:** type

```typescript
export type ForgeRedirect = MpRedirect;
```

Geen beschrijving opgegeven.

### ForgeResolvedRoute

**Soort:** type

```typescript
export type ForgeResolvedRoute = MpResolvedLocation;
```

Geen beschrijving opgegeven.

### ForgeRouteChangeEvent

**Soort:** type

```typescript
export type ForgeRouteChangeEvent = MpRouteChangeEvent;
```

Geen beschrijving opgegeven.

### ForgeRouteChangeListener

**Soort:** type

```typescript
export type ForgeRouteChangeListener = MpRouteChangeListener;
```

Geen beschrijving opgegeven.

### ForgeRouteGuard

**Soort:** type

```typescript
export type ForgeRouteGuard = MpRouteGuard;
```

Geen beschrijving opgegeven.

### ForgeRouteLink

**Soort:** type

```typescript
export type ForgeRouteLink = MpRouteLink;
```

Geen beschrijving opgegeven.

### ForgeRouteMatch

**Soort:** type

```typescript
export type ForgeRouteMatch = MpRouteMatch;
```

Geen beschrijving opgegeven.

### ForgeRouteMeta

**Soort:** type

```typescript
export type ForgeRouteMeta = MpRouteMeta;
```

Geen beschrijving opgegeven.

### ForgeRouteMetadata

**Soort:** type

```typescript
export type ForgeRouteMetadata = MpRouteMetadata;
```

Geen beschrijving opgegeven.

### ForgeRouteParameters

**Soort:** type

```typescript
export type ForgeRouteParameters = MpRouteParameters;
```

Geen beschrijving opgegeven.

### ForgeRouterAdapter

**Soort:** type

```typescript
export type ForgeRouterAdapter = MpRouterAdapter;
```

Geen beschrijving opgegeven.

### ForgeRouteRecord

**Soort:** type

```typescript
export type ForgeRouteRecord<View = unknown> = MpRoute<View>;
```

Geen beschrijving opgegeven.

### ForgeRouteDoel

**Soort:** type

```typescript
export type ForgeRouteTarget = MpRouteLocationRaw;
```

Geen beschrijving opgegeven.

### ForgeRouteViewAdapter

**Soort:** type

```typescript
export type ForgeRouteViewAdapter<View = unknown, Outlet = unknown> = MpRouteViewAdapter<View, Outlet>;
```

Geen beschrijving opgegeven.

### ForgeRouteViewContext

**Soort:** type

```typescript
export type ForgeRouteViewContext<View = unknown> = MpRouteViewContext<View>;
```

Geen beschrijving opgegeven.

### ForgeScroll-gedrag

**Soort:** type

```typescript
export type ForgeScrollBehavior = MpScrollBehavior;
```

Geen beschrijving opgegeven.

### ForgeScrollPositie

**Soort:** type

```typescript
export type ForgeScrollPosition = MpScrollPosition;
```

Geen beschrijving opgegeven.

## `src/define-routes`

### createRouteResolver

**Soort:** functie

```typescript
function createRouteResolver(routes: readonly MpRoute[]): MpRouteResolver;
```

Maak een {@link MpRouteResolver} die aan een routeboom is gebonden en maak deze één keer plat
herhaalde `match`/`resolve`-oproepen gebruiken dezelfde absolute routelijst.

#### Parameters

| Naam      | Typ                    | Beschrijving |
| --------- | ---------------------- | ------------ |
| trajecten | alleen-lezen MpRoute[] |              |

### definieerRoutes

**Soort:** functie

```typescript
function defineRoutes(routes: T): T;
```

Identiteitshelper die een raamwerk-neutrale routeboom met volledig type definieert
gevolgtrekking. Gebruik het zodat redacteuren de route `name`s, `meta` en onderliggende routes kunnen controleren.

#### Parameters

| Naam      | Typ | Beschrijving |
| --------- | --- | ------------ |
| trajecten | T   |              |

#### Contract

- **@example:** const routes = definiërenRoutes([
  { pad: '/', naam: 'home', component: Home },
  { pad: '/users/:id', naam: 'gebruiker', component: Gebruiker },
  ])

### vindRouteByName

**Soort:** functie

```typescript
function findRouteByName(routes: readonly MpRoute[], name: string): MpFlatRoute | undefined;
```

Zoek de eerste afgevlakte route met de opgegeven naam.

#### Parameters

| Naam      | Typ                    | Beschrijving |
| --------- | ---------------------- | ------------ |
| trajecten | alleen-lezen MpRoute[] |              |
| naam      | tekenreeks             |              |

### Maak routes plat

**Soort:** functie

```typescript
function flattenRoutes(routes: readonly MpRoute[]): MpFlatRoute[];
```

Maak een geneste routeboom plat tot een diepte-eerst-lijst van absolute routes, met
de `path` van elke route is opgelost tegen de ouders en `meta` geërfd van
zijn voorouders.

#### Parameters

| Naam      | Typ                    | Beschrijving |
| --------- | ---------------------- | ------------ |
| trajecten | alleen-lezen MpRoute[] |              |

### matchRoutes

**Soort:** functie

```typescript
function matchRoutes(routes: readonly MpRoute[], pathname: string): MpRouteMatch | undefined;
```

Vergelijk een padnaam met een routeboom en retourneer de eerste route (in
diepte-eerste definitievolgorde) waarvan het patroon overeenkomt, of `undefined`.

#### Parameters

| Naam      | Typ                    | Beschrijving |
| --------- | ---------------------- | ------------ |
| trajecten | alleen-lezen MpRoute[] |              |
| padnaam   | tekenreeks             |              |

### MpFlatRoute

**Soort:** component

```typescript
export interface MpFlatRoute
```

Een route afgevlakt tot een absoluut pad, met overgeërfde metadata en voorouders.

### MpRouteMatch

**Soort:** component

```typescript
export interface MpRouteMatch
```

Een succesvolle match van een padnaam met een routeboom.

### MpRouteResolver

**Soort:** component

```typescript
export interface MpRouteResolver
```

Een oplosser die is gebonden aan een enkele routeboom.

### oplossingLocatie

**Soort:** functie

```typescript
function resolveLocation(to: MpRouteLocationRaw, routes: readonly MpRoute[] = []): MpResolvedLocation;
```

Los elke raamwerkneutrale {@link MpRouteLocationRaw} op in een volledig gevormde
{@link MpResolvedLocation}. Tekenreeks- en padlocaties worden vergeleken met de
routeboom (indien aanwezig) om `params`/`name`/`meta` te vullen; genoemd
locaties bouwen hun pad op basis van de bijpassende route.

#### Parameters

| Naam      | Typ                    | Beschrijving |
| --------- | ---------------------- | ------------ |
| naar      | MpRouteLocationRaw     |              |
| trajecten | alleen-lezen MpRoute[] |              |

#### Contract

- **@example:** solveLocation({ naam: 'gebruiker', params: { id: 42 } }, routes)
  // → { pad: '/users/42', fullPath: '/users/42', params: { id: '42' }, … }

## `src/location`

### MpLocationInvoer

**Soort:** component

```typescript
export interface MpLocationInput
```

Een locatie beschreven in zijn onderdelen, met optionele query/hash.

### MpLocationParts

**Soort:** component

```typescript
export interface MpLocationParts
```

De ontlede delen van een URL: padnaam, query en hash.

### normaliserenHash

**Soort:** functie

```typescript
function normalizeHash(hash: string): string;
```

Normaliseer een hashfragment zodat het één leidende `#` bevat, of wees leeg.

#### Parameters

| Naam | Typ        | Beschrijving |
| ---- | ---------- | ------------ |
| hasj | tekenreeks |              |

### parseLocatie

**Soort:** functie

```typescript
function parseLocation(url: string): MpLocationParts;
```

Splits een app-relatieve URL in de bijbehorende {@link MpLocationParts}.

#### Parameters

| Naam | Typ        | Beschrijving |
| ---- | ---------- | ------------ |
| URL  | tekenreeks |              |

#### Contract

- **@voorbeeld:** parseLocation('/users/42?tab=info#bio')
  // → { pad: '/users/42', query: { tab: 'info' }, hash: '#bio' }

### stringifyLocation

**Soort:** functie

```typescript
function stringifyLocation(location: MpLocationInput): string;
```

Stel een app-relatieve URL-tekenreeks samen uit locatiedelen. De padnaam is
genormaliseerd, de query geserialiseerd (met een leidende `?` als deze niet leeg is), en de
hash voorafgegaan door `#` als deze niet leeg is.

#### Parameters

| Naam    | Typ              | Beschrijving |
| ------- | ---------------- | ------------ |
| locatie | MpLocationInvoer |              |

#### Contract

- **@example:** stringifyLocation({ pad: '/users/42', query: { tab: 'info' }, hash: 'bio' })
  // → '/users/42?tab=info#bio'

## `src/path`

### bouwpad

**Soort:** functie

```typescript
function buildPath(
  pattern: string,
  parameters: Record<string, MpParameterValue | readonly MpParameterValue[]> = {},
): string;
```

Bouw een concrete padnaam op basis van een patroon en een reeks parameters, codering
elke waarde. Optionele segmenten worden verwijderd als hun waarde ontbreekt; een
ontbrekende vereiste parameterworpen.

#### Parameters

| Naam       | Typ                                                             | Beschrijving |
| ---------- | --------------------------------------------------------------- | ------------ |
| patroon    | tekenreeks                                                      |              |
| parameters | Record<string, MpParameterValue \| readonly MpParameterValue[]> |              |

#### Contract

- **@example:** buildPath('/users/:id', { id: 42 }) // → '/users/42'
  buildPath('/users/:id?', {}) // → '/users'
  buildPath('/files/:rest*', {rest: ['a', 'b'] }) // → '/files/a/b'

### compilepad

**Soort:** functie

```typescript
function compilePath(pattern: string): MpCompiledPath;
```

Compileer een padpatroon in een {@link MpCompiledPath} met het orderbestand
parametersleutels en een overeenkomende reguliere expressie.

#### Parameters

| Naam    | Typ        | Beschrijving |
| ------- | ---------- | ------------ |
| patroon | tekenreeks |              |

#### Contract

- **@example:** compilePath('/users/:id').regexp.test('/users/42') // → true

### matchPath

**Soort:** functie

```typescript
function matchPath(pattern: string, pathname: string): MpRouteParameters | undefined;
```

Match een padnaam met een patroon en retourneer de geëxtraheerde parameters of
`undefined` als de padnaam niet overeenkomt.

#### Parameters

| Naam    | Typ        | Beschrijving |
| ------- | ---------- | ------------ |
| patroon | tekenreeks |              |
| padnaam | tekenreeks |              |

#### Contract

- **@example:** matchPath('/users/:id', '/users/42') // → {id: '42' }
  matchPath('/users/:id', '/posts/42') // → ongedefinieerd

### MpCompiledPath

**Soort:** component

```typescript
export interface MpCompiledPath
```

Het resultaat van het compileren van een padpatroon in een matcher.

### MpPathParameterKey

**Soort:** component

```typescript
export interface MpPathParameterKey
```

Eén parametersleutel geëxtraheerd uit een gecompileerd padpatroon.

### normaliserenPath

**Soort:** functie

```typescript
function normalizePath(path: string): string;
```

Normaliseer een padpatroon (of padnaam) naar een canonieke vorm met een leidende schuine streep
zonder afsluitende slash (behalve de root, die `/` blijft).

#### Parameters

| Naam | Typ        | Beschrijving |
| ---- | ---------- | ------------ |
| pad  | tekenreeks |              |

### WILDCARD_PARAM_KEY

**Soort:** constant

```typescript
export const WILDCARD_PARAM_KEY;
```

De sleutel waaronder een zelfstandig `*` catch-all-segment valt.

## `src/query`

### parseQuery

**Soort:** functie

```typescript
function parseQuery(search: string): MpQueryParameters;
```

Parseer een queryreeks in een {@link MpQueryParameters}-kaart. Een toonaangevende `?` is dat wel
optioneel. Herhaalde sleutels worden in volgorde samengevoegd tot een reeks waarden.

#### Parameters

| Naam   | Typ        | Beschrijving |
| ------ | ---------- | ------------ |
| zoeken | tekenreeks |              |

#### Contract

- **@example:** parseQuery('?tag=a&tag=b&page=2') // → { tag: ['a', 'b'], pagina: '2' }

### stringifyQuery

**Soort:** functie

```typescript
function stringifyQuery(query: MpQueryInput): string;
```

Serialiseer een {@link MpQueryInput}-kaart in een queryreeks met een leidende `?`,
of een lege string als er niets te serialiseren is. `null`/`undefined`
waarden (en array-items) worden verwijderd; arrays zenden elk één `key=value`-paar uit.

#### Parameters

| Naam  | Typ          | Beschrijving |
| ----- | ------------ | ------------ |
| vraag | MpQueryInput |              |

#### Contract

- **@example:** stringifyQuery({ tag: ['a', 'b'], pagina: 2 }) // → '?tag=a&tag=b&page=2'
  stringifyQuery({}) // → ''

## `src/types`

### MpGuardOutcome

**Soort:** component

```typescript
export type MpGuardOutcome = void | boolean | MpRouteLocationRaw;
```

Het resultaat dat door een routebewaker wordt geretourneerd voordat een overgang wordt doorgevoerd.

### MpGeschiedenis

**Soort:** component

```typescript
export interface MpHistory
```

Een browser-, hash- of geheugengeschiedenisimplementatie die door een runtime wordt geleverd.

### MpGeschiedenisInvoer

**Soort:** component

```typescript
export interface MpHistoryEntry
```

Geen beschrijving opgegeven.

### MpGeschiedenisEvent

**Soort:** component

```typescript
export interface MpHistoryEvent
```

Geen beschrijving opgegeven.

### MpHistoryListener

**Soort:** component

```typescript
export type MpHistoryListener = (event: MpHistoryEvent) => void;
```

Geen beschrijving opgegeven.

### MpHistory-modus

**Soort:** component

```typescript
export type MpHistoryMode = 'web' | 'hash' | 'memory';
```

De geschiedenisstrategie die de router van een adapter moet gebruiken.

- `web` — HTML5-geschiedenis (schone URL's), de standaard voor browser-apps.
- `hash` — op hash gebaseerde geschiedenis (`/#/path`), voor statische hosting zonder
  server herschrijft.
- `memory` — geschiedenis in het geheugen zonder URL, voor SSR en tests.

### MpMetadataContext

**Soort:** component

```typescript
export interface MpMetadataContext
```

Geen beschrijving opgegeven.

### MpMetadataHook

**Soort:** component

```typescript
export type MpMetadataHook = (context: MpMetadataContext) => MpRouteMetadata | void | Promise<MpRouteMetadata | void>;
```

Geen beschrijving opgegeven.

### MpNamedLocation

**Soort:** component

```typescript
export interface MpNamedLocation
```

Een navigatiedoel uitgedrukt in routenaam.

### MpNavigatieContext

**Soort:** component

```typescript
export interface MpNavigationContext
```

Geen beschrijving opgegeven.

### MpNavigatieFout

**Soort:** component

```typescript
export interface MpNavigationFailure
```

Geen beschrijving opgegeven.

### MpNavigationFailureType

**Soort:** component

```typescript
export type MpNavigationFailureType = 'aborted' | 'cancelled' | 'duplicated' | 'error' | 'not-found';
```

Geen beschrijving opgegeven.

### MpNavigatieRedirect

**Soort:** component

```typescript
export interface MpNavigationRedirect
```

Geen beschrijving opgegeven.

### MpNavigatieResultaat

**Soort:** component

```typescript
export type MpNavigationResult = MpNavigationSuccess | MpNavigationRedirect | MpNavigationFailure;
```

Geen beschrijving opgegeven.

### MpNavigatieSucces

**Soort:** component

```typescript
export interface MpNavigationSuccess
```

Geen beschrijving opgegeven.

### MpNavigatieType

**Soort:** component

```typescript
export type MpNavigationType = 'push' | 'replace' | 'pop' | 'go';
```

Een transitiebewerking geïnitieerd door een router- of geschiedenisimplementatie.

### MpParameterwaarde

**Soort:** component

```typescript
export type MpParameterValue = string | number | boolean | null | undefined;
```

Eén raamwerkneutrale invoerwaarde voor pad/queryparameter.

### MpPathLocatie

**Soort:** component

```typescript
export interface MpPathLocation
```

Een navigatiedoel uitgedrukt in pad.

### MpQueryInvoer

**Soort:** component

```typescript
export type MpQueryInput = Record<string, MpParameterValue | readonly MpParameterValue[]>;
```

Ruwe, raamwerk-neutrale query-invoer. Waarden kunnen scalairen of arrays zijn (herhaald
sleutels); `null`/`undefined`-waarden worden verwijderd wanneer ze worden geserialiseerd.

### MpQueryParameters

**Soort:** component

```typescript
export type MpQueryParameters = Record<string, string | string[]>;
```

Geparseerde queryparameters: een scalaire tekenreeks of een reeks tekenreeksen per sleutel.

### MpReadonlySignaal

**Soort:** component

```typescript
export interface MpReadonlySignal<Value>
```

Structurele reactieve toestand die door adapters wordt gebruikt zonder een raamwerkprimitief voor te schrijven.

### MpRedirect

**Soort:** component

```typescript
export type MpRedirect =
  MpRouteLocationRaw | ((to: MpResolvedLocation) => MpRouteLocationRaw | Promise<MpRouteLocationRaw>);
```

Een omleidingsdoel, optioneel berekend op basis van de bestemmingsroute.

### MpResolvedLocation

**Soort:** component

```typescript
export interface MpResolvedLocation
```

Een volledig opgeloste locatie, kaderneutraal.

### MpRoute

**Soort:** component

```typescript
export interface MpRoute<View = unknown>
```

Een kaderneutrale routedefinitie. Dezelfde boom kan worden vertaald in
elke doelrouter (vue-router, react-router, TanStack Router) of op bestanden gebaseerd
conventie (Next.js, Nuxt) door een adapter.

### MpRouteChangeEvent

**Soort:** component

```typescript
export interface MpRouteChangeEvent
```

Geen beschrijving opgegeven.

### MpRouteChangeListener

**Soort:** component

```typescript
export type MpRouteChangeListener = (event: MpRouteChangeEvent) => void;
```

Geen beschrijving opgegeven.

### MpRouteGuard

**Soort:** component

```typescript
export type MpRouteGuard = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
) => MpGuardOutcome | Promise<MpGuardOutcome>;
```

Een routewachter geëvalueerd door een navigatiestatusmachine die eigendom is van runtime.

### MpRouteLink

**Soort:** component

```typescript
export interface MpRouteLink
```

Geen beschrijving opgegeven.

### MpRouteLocationRaw

**Soort:** component

```typescript
export type MpRouteLocationRaw = string | MpPathLocation | MpNamedLocation;
```

Een raamwerkneutraal navigatiedoel: een URL-tekenreeks of een gestructureerd object.

### MpRouteMeta

**Soort:** component

```typescript
export type MpRouteMeta = Record<string, unknown>;
```

Willekeurige, raamwerkneutrale metadata die aan een route zijn gekoppeld.

### MpRouteMetadata

**Soort:** component

```typescript
export interface MpRouteMetadata extends MpRouteMeta
```

Geen beschrijving opgegeven.

### MpRouteParameters

**Soort:** component

```typescript
export type MpRouteParameters = Record<string, string | string[]>;
```

Opgeloste padparameters geëxtraheerd uit een overeenkomende route. Een waarde is een array
alleen voor herhaalbare (catch-all) segmenten zoals `:rest*`.

### MpRouter

**Soort:** component

```typescript
export interface MpRouter
```

Het raamwerkneutrale navigatiecontract waaraan elke adapter voldoet. Adapters mogen
reactieve equivalenten blootleggen (de Vue-adapter `useMpRoute` retourneert bijvoorbeeld een
reactieve ref), maar het gebiedende oppervlak is overal hetzelfde.

### MpRouterAdapter

**Soort:** component

```typescript
export interface MpRouterAdapter
```

Routerstatus en navigatiecontract dat eigendom is van runtime.

### MpRouteViewAdapter

**Soort:** component

```typescript
export interface MpRouteViewAdapter<View = unknown, Outlet = unknown>
```

Geen beschrijving opgegeven.

### MpRouteViewContext

**Soort:** component

```typescript
export interface MpRouteViewContext<View = unknown>
```

Geen beschrijving opgegeven.

### MpScroll-gedrag

**Soort:** component

```typescript
export type MpScrollBehavior = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
  savedPosition?: MpScrollPosition,
) => MpScrollPosition | false | void | Promise<MpScrollPosition | false | void>;
```

Geen beschrijving opgegeven.

### MpScrollPositie

**Soort:** component

```typescript
export interface MpScrollPosition
```

Geen beschrijving opgegeven.
