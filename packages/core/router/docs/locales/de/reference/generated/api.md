# @mission-platform/router API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/core/router/docs/reference/generated/api.md: [packages/core/router/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/router`.

## `src/capabilities`

### createMpRouterCapabilities

**Art:** Funktion

```typescript
function createMpRouterCapabilities(overrides: MpRouterCapabilityOverrides = {}): MpRouterCapabilities;
```

Erstellen Sie ein neutrales Fähigkeitsobjekt für einen Zieladapter, SSR oder einen Test.

#### Parameter

| Name         | Geben Sie                   | ein Beschreibung |
| ------------ | --------------------------- | ---------------- |
| überschreibt | MpRouterCapabilityOverrides |                  |

### isMpRouterCapabilityError

**Art:** Funktion

```typescript
function isMpRouterCapabilityError(error: unknown): error is MpRouterCapabilityError;
```

Ob es sich bei einem unbekannten Fehler um den deterministischen Neutral-Router-Fallback-Fehler handelt.

#### Parameter

| Name   | Geben Sie | ein Beschreibung |
| ------ | --------- | ---------------- |
| Fehler | unbekannt |                  |

### MP_ROUTER_COMPILER_MARKER

**Art:** konstant

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

Stabile Metadaten, die von Forge-Router-Compiler-Plugins verwendet werden.

### MpLink

**Art:** Komponente

```typescript
function MpLink(properties: MpLinkProps): MpLinkDescriptor;
```

Neutraler Linkmarker. Native Ziele ersetzen diese Funktion während der Kompilierung.

#### Parameter

| Name          | Geben Sie   | ein Beschreibung |
| ------------- | ----------- | ---------------- |
| Eigenschaften | MpLinkProps |                  |

### MpLinkDescriptor

**Art:** Komponente

```typescript
export interface MpLinkDescriptor
```

Eine Framework-neutrale Linkbeschreibung, die von SSR und nicht kompilierten Tests verwendet wird.

### MpLinkOptions

**Art:** Komponente

```typescript
export interface MpLinkOptions
```

Optionen, die von neutralen Links und nativen Navigationsaufrufen geteilt werden.

### MpLinkProps

**Art:** Komponente

```typescript
export interface MpLinkProps extends MpLinkOptions
```

Vom Compiler-Marker akzeptierte Requisiten, dargestellt durch {@link MpLink}.

### MpNavigationOptions

**Art:** Komponente

```typescript
export interface MpNavigationOptions extends MpLinkOptions
```

An eine native Navigationsimplementierung weitergeleitete Optionen.

### MpRouterCapabilities

**Art:** Komponente

```typescript
export interface MpRouterCapabilities
```

Die vom nativen Router der nutzenden Anwendung bereitgestellten Funktionen.

### MpRouterCapability

**Art:** Komponente

```typescript
export type MpRouterCapability = 'link' | 'route' | 'navigate' | 'resolve' | 'view';
```

Fähigkeiten, die ein Router-Ziel unabhängig voneinander verringern kann.

### MpRouterCapabilityError

**Art:** Komponente

```typescript
export class MpRouterCapabilityError extends Error
```

Eine stabile Diagnose wird ausgelöst, wenn ein Vorgang vom Fallback nicht ausgeführt werden kann.

### MpRouterCapabilityOverrides

**Art:** Komponente

```typescript
export type MpRouterCapabilityOverrides = Partial<MpRouterCapabilities>;
```

Eine Teilimplementierung, die für Zieladapter und SSR-/Testkabelbäume nützlich ist.

### MpRouterView

**Art:** Komponente

```typescript
function MpRouterView(properties: MpRouterViewProps = {}): MpRouterViewDescriptor;
```

Neutrale Steckdose/Ansichtsmarkierung. Native Ziele ersetzen diese Funktion während der Kompilierung.

#### Parameter

| Name          | Geben Sie         | ein Beschreibung |
| ------------- | ----------------- | ---------------- |
| Eigenschaften | MpRouterViewProps |                  |

### MpRouterViewDescriptor

**Art:** Komponente

```typescript
export interface MpRouterViewDescriptor
```

Eine Framework-neutrale Ansichtsbeschreibung, die von SSR und nicht kompilierten Tests verwendet wird.

### MpRouterViewProps

**Art:** Komponente

```typescript
export interface MpRouterViewProps
```

Vom neutralen Outlet-/View-Compiler-Marker akzeptierte Requisiten.

### auflösenMpLink

**Art:** Funktion

```typescript
function resolveMpLink(
  to: MpRouteLocationRaw,
  capabilities: Pick<MpRouterCapabilities, 'resolve'> = useMpRouter(),
): string;
```

Lösen Sie ein Ziel über den App-eigenen Router auf. Die optionale Funktion
Das Argument macht den Helfer in SSR/Tests und Zieladaptern deterministisch.

#### Parameter

| Name        | Geben Sie                             | ein Beschreibung |
| ----------- | ------------------------------------- | ---------------- |
| zu          | MpRouteLocationRaw                    |                  |
| Fähigkeiten | Pick<MpRouterCapabilities, 'resolve'> |                  |

### useMpNavigation

**Art:** Funktion

```typescript
function useMpNavigation(): Pick<MpRouterCapabilities, 'navigate' | 'resolve'>;
```

Lesen Sie nur die zwingend erforderliche Navigationsfähigkeit.

### useMpRoute

**Art:** Funktion

```typescript
function useMpRoute(): MpResolvedLocation | null;
```

Lesen Sie die aktuelle App-eigene Route. Der unkompilierte Fallback ist SSR-sicher und
gibt `null` zurück; Ein Compilerziel ersetzt diesen Aufruf durch den nativen Routenstatus.

### useMpRouter

**Art:** Funktion

```typescript
function useMpRouter(): MpRouterCapabilities;
```

Lesen Sie das von der App bereitgestellte neutrale Fähigkeitsobjekt.

## `src/compatibility-fixtures`

### applicationCompatibilityFixtures

**Art:** konstant

```typescript
export const applicationCompatibilityFixtures;
```

Keine Beschreibung angegeben.

### docsCompatibilityFixture

**Art:** konstant

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Leiten Sie Datensätze weiter, die die Locale-/Such-/Catch-All-Tabelle der Dokumentations-App widerspiegeln.

### DokumentationCompatibilityFixture

**Art:** konstant

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

Leiten Sie Datensätze weiter, die die Locale-/Such-/Catch-All-Tabelle der Dokumentations-App widerspiegeln.

### ForgeCompatibilityCase

**Art:** Schnittstelle

```typescript
export interface ForgeCompatibilityCase
```

Keine Beschreibung angegeben.

### ForgeCompatibilityFixture

**Art:** Schnittstelle

```typescript
export interface ForgeCompatibilityFixture
```

Keine Beschreibung angegeben.

### myCareNotesCompatibilityFixture

**Art:** konstant

```typescript
export const myCareNotesCompatibilityFixture: ForgeCompatibilityFixture;
```

Routendatensätze, die das Sprachsegment und die URL-gesteuerten Overlays von My Care Notes widerspiegeln.

### routerCompatibilityFixtures

**Art:** konstant

```typescript
export const routerCompatibilityFixtures: readonly ForgeCompatibilityCase[];
```

Gemeinsam genutzte Verhaltenszeilen, die von der Vertragstestsuite jeder Laufzeit genutzt werden.

### WebsiteCompatibilityFixture

**Art:** konstant

```typescript
export const websiteCompatibilityFixture: ForgeCompatibilityFixture;
```

Routendatensätze, die die optionale Home-Route mit vorangestelltem Gebietsschema der Website widerspiegeln.

## `src/contracts`

### ForgeGuardOutcome

**Art:** Typ

```typescript
export type ForgeGuardOutcome = MpGuardOutcome;
```

Keine Beschreibung angegeben.

### ForgeHistory

**Art:** Typ

```typescript
export type ForgeHistory = MpHistory;
```

Eine von einer Laufzeit bereitgestellte Browser-, Hash- oder Speicherverlaufsimplementierung.

### ForgeHistoryEntry

**Art:** Typ

```typescript
export type ForgeHistoryEntry = MpHistoryEntry;
```

Keine Beschreibung angegeben.

### ForgeHistoryEvent

**Art:** Typ

```typescript
export type ForgeHistoryEvent = MpHistoryEvent;
```

Keine Beschreibung angegeben.

### ForgeHistoryListener

**Art:** Typ

```typescript
export type ForgeHistoryListener = MpHistoryListener;
```

Keine Beschreibung angegeben.

### ForgeHistoryMode

**Art:** Typ

```typescript
export type ForgeHistoryMode = MpHistoryMode;
```

Keine Beschreibung angegeben.

### ForgeMetadataContext

**Art:** Typ

```typescript
export type ForgeMetadataContext = MpMetadataContext;
```

Keine Beschreibung angegeben.

### ForgeMetadataHook

**Art:** Typ

```typescript
export type ForgeMetadataHook = MpMetadataHook;
```

Keine Beschreibung angegeben.

### ForgeNamedTarget

**Art:** Typ

```typescript
export type ForgeNamedTarget = MpNamedLocation;
```

Keine Beschreibung angegeben.

### ForgeNavigationContext

**Art:** Typ

```typescript
export type ForgeNavigationContext = MpNavigationContext;
```

Keine Beschreibung angegeben.

### ForgeNavigationFailure

**Art:** Typ

```typescript
export type ForgeNavigationFailure = MpNavigationFailure;
```

Keine Beschreibung angegeben.

### ForgeNavigationFailureType

**Art:** Typ

```typescript
export type ForgeNavigationFailureType = MpNavigationFailureType;
```

Keine Beschreibung angegeben.

### ForgeNavigationRedirect

**Art:** Typ

```typescript
export type ForgeNavigationRedirect = MpNavigationRedirect;
```

Keine Beschreibung angegeben.

### ForgeNavigationResult

**Art:** Typ

```typescript
export type ForgeNavigationResult = MpNavigationResult;
```

Keine Beschreibung angegeben.

### ForgeNavigationSuccess

**Art:** Typ

```typescript
export type ForgeNavigationSuccess = MpNavigationSuccess;
```

Keine Beschreibung angegeben.

### ForgeNavigationType

**Art:** Typ

```typescript
export type ForgeNavigationType = MpNavigationType;
```

Keine Beschreibung angegeben.

### ForgeParameterValue

**Art:** Typ

```typescript
export type ForgeParameterValue = MpParameterValue;
```

Keine Beschreibung angegeben.

### ForgePathTarget

**Art:** Typ

```typescript
export type ForgePathTarget = MpPathLocation;
```

Keine Beschreibung angegeben.

### ForgeQuery

**Art:** Typ

```typescript
export type ForgeQuery = MpQueryParameters;
```

Keine Beschreibung angegeben.

### ForgeQueryInput

**Art:** Typ

```typescript
export type ForgeQueryInput = MpQueryInput;
```

Keine Beschreibung angegeben.

### ForgeReadonlySignal

**Art:** Typ

```typescript
export type ForgeReadonlySignal<Value> = MpReadonlySignal<Value>;
```

Struktureller reaktiver Zustand, der von Adaptern verwendet wird, ohne ein Framework-Grundelement vorzuschreiben.

### ForgeRedirect

**Art:** Typ

```typescript
export type ForgeRedirect = MpRedirect;
```

Keine Beschreibung angegeben.

### ForgeResolvedRoute

**Art:** Typ

```typescript
export type ForgeResolvedRoute = MpResolvedLocation;
```

Keine Beschreibung angegeben.

### ForgeRouteChangeEvent

**Art:** Typ

```typescript
export type ForgeRouteChangeEvent = MpRouteChangeEvent;
```

Keine Beschreibung angegeben.

### ForgeRouteChangeListener

**Art:** Typ

```typescript
export type ForgeRouteChangeListener = MpRouteChangeListener;
```

Keine Beschreibung angegeben.

### ForgeRouteGuard

**Art:** Typ

```typescript
export type ForgeRouteGuard = MpRouteGuard;
```

Keine Beschreibung angegeben.

### ForgeRouteLink

**Art:** Typ

```typescript
export type ForgeRouteLink = MpRouteLink;
```

Keine Beschreibung angegeben.

### ForgeRouteMatch

**Art:** Typ

```typescript
export type ForgeRouteMatch = MpRouteMatch;
```

Keine Beschreibung angegeben.

### ForgeRouteMeta

**Art:** Typ

```typescript
export type ForgeRouteMeta = MpRouteMeta;
```

Keine Beschreibung angegeben.

### ForgeRouteMetadata

**Art:** Typ

```typescript
export type ForgeRouteMetadata = MpRouteMetadata;
```

Keine Beschreibung angegeben.

### ForgeRouteParameters

**Art:** Typ

```typescript
export type ForgeRouteParameters = MpRouteParameters;
```

Keine Beschreibung angegeben.

### ForgeRouterAdapter

**Art:** Typ

```typescript
export type ForgeRouterAdapter = MpRouterAdapter;
```

Keine Beschreibung angegeben.

### ForgeRouteRecord

**Art:** Typ

```typescript
export type ForgeRouteRecord<View = unknown> = MpRoute<View>;
```

Keine Beschreibung angegeben.

### ForgeRouteTarget

**Art:** Typ

```typescript
export type ForgeRouteTarget = MpRouteLocationRaw;
```

Keine Beschreibung angegeben.

### ForgeRouteViewAdapter

**Art:** Typ

```typescript
export type ForgeRouteViewAdapter<View = unknown, Outlet = unknown> = MpRouteViewAdapter<View, Outlet>;
```

Keine Beschreibung angegeben.

### ForgeRouteViewContext

**Art:** Typ

```typescript
export type ForgeRouteViewContext<View = unknown> = MpRouteViewContext<View>;
```

Keine Beschreibung angegeben.

### ForgeScrollBehavior

**Art:** Typ

```typescript
export type ForgeScrollBehavior = MpScrollBehavior;
```

Keine Beschreibung angegeben.

### ForgeScrollPosition

**Art:** Typ

```typescript
export type ForgeScrollPosition = MpScrollPosition;
```

Keine Beschreibung angegeben.

## `src/define-routes`

### createRouteResolver

**Art:** Funktion

```typescript
function createRouteResolver(routes: readonly MpRoute[]): MpRouteResolver;
```

Erstellen Sie einen {@link MpRouteResolver}, der an einen Routenbaum gebunden ist, und reduzieren Sie ihn einmal
Wiederholte `match`/`resolve`-Aufrufe verwenden dieselbe absolute Routenliste erneut.

#### Parameter

| Name   | Geben Sie                  | ein Beschreibung |
| ------ | -------------------------- | ---------------- |
| Routen | schreibgeschützt MpRoute[] |                  |

### Routen definieren

**Art:** Funktion

```typescript
function defineRoutes(routes: T): T;
```

Identitätshelfer, der einen Framework-neutralen Routenbaum mit vollständigem Typ definiert
Schlussfolgerung. Verwenden Sie es, damit Redakteure die Route `name`s, `meta` und untergeordnete Elemente typüberprüfen.

#### Parameter

| Name   | Geben Sie | ein Beschreibung |
| ------ | --------- | ---------------- |
| Routen | T         |                  |

#### Vertrag

- **@example:** constroutes = defineRoutes([
  { Pfad: '/', Name: 'home', Komponente: Home },
  { Pfad: '/users/:id', Name: 'Benutzer', Komponente: Benutzer },
  ])

### findRouteByName

**Art:** Funktion

```typescript
function findRouteByName(routes: readonly MpRoute[], name: string): MpFlatRoute | undefined;
```

Suchen Sie die erste abgeflachte Route mit dem angegebenen Namen.

#### Parameter

| Name   | Geben Sie                  | ein Beschreibung |
| ------ | -------------------------- | ---------------- |
| Routen | schreibgeschützt MpRoute[] |                  |
| Name   | Zeichenfolge               |                  |

### flattenRoutes

**Art:** Funktion

```typescript
function flattenRoutes(routes: readonly MpRoute[]): MpFlatRoute[];
```

Reduzieren Sie einen verschachtelten Routenbaum in eine Tiefenliste absoluter Routen mit
Der `path` jeder Route wird gegen seine Eltern aufgelöst und `meta` wird von geerbt
seine Vorfahren.

#### Parameter

| Name   | Geben Sie                  | ein Beschreibung |
| ------ | -------------------------- | ---------------- |
| Routen | schreibgeschützt MpRoute[] |                  |

### matchRoutes

**Art:** Funktion

```typescript
function matchRoutes(routes: readonly MpRoute[], pathname: string): MpRouteMatch | undefined;
```

Vergleichen Sie einen Pfadnamen mit einem Routenbaum und geben Sie die erste Route zurück (in
Tiefendefinitionsreihenfolge), deren Muster übereinstimmt, oder `undefined`.

#### Parameter

| Name     | Geben Sie                  | ein Beschreibung |
| -------- | -------------------------- | ---------------- |
| Routen   | schreibgeschützt MpRoute[] |                  |
| Pfadname | Zeichenfolge               |                  |

### MpFlatRoute

**Art:** Komponente

```typescript
export interface MpFlatRoute
```

Eine auf einen absoluten Pfad reduzierte Route mit geerbten Metadaten und Vorfahren.

### MpRouteMatch

**Art:** Komponente

```typescript
export interface MpRouteMatch
```

Ein erfolgreicher Abgleich eines Pfadnamens mit einem Routenbaum.

### MpRouteResolver

**Art:** Komponente

```typescript
export interface MpRouteResolver
```

Ein Resolver, der an einen einzelnen Routenbaum gebunden ist.

### Auflösungsstandort

**Art:** Funktion

```typescript
function resolveLocation(to: MpRouteLocationRaw, routes: readonly MpRoute[] = []): MpResolvedLocation;
```

Lösen Sie jedes Framework-neutrale {@link MpRouteLocationRaw} in ein vollständig geformtes auf
{@link MpResolvedLocation}. Zeichenfolgen- und Pfadpositionen werden mit denen abgeglichen
Routenbaum (sofern bereitgestellt), um `params`/`name`/`meta` zu füllen; benannt
Orte bauen ihren Weg aus der passenden Route auf.

#### Parameter

| Name   | Geben Sie                  | ein Beschreibung |
| ------ | -------------------------- | ---------------- |
| zu     | MpRouteLocationRaw         |                  |
| Routen | schreibgeschützt MpRoute[] |                  |

#### Vertrag

- **@example:** discoverLocation({ name: 'user', params: { id: 42 } }, Routen)
  // → { path: '/users/42', fullPath: '/users/42', params: { id: '42' }, … }

## `src/location`

### MpLocationInput

**Art:** Komponente

```typescript
export interface MpLocationInput
```

Ein durch seine Teile beschriebener Ort, optional mit Abfrage/Hash.

### MpLocationParts

**Art:** Komponente

```typescript
export interface MpLocationParts
```

Die zerlegten Teile einer URL: Pfadname, Abfrage und Hash.

### normalizeHash

**Art:** Funktion

```typescript
function normalizeHash(hash: string): string;
```

Normalisieren Sie ein Hash-Fragment so, dass es ein einzelnes führendes `#` enthält oder leer ist.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Hash | Zeichenfolge |                  |

### parseLocation

**Art:** Funktion

```typescript
function parseLocation(url: string): MpLocationParts;
```

Teilen Sie eine app-relative URL in ihre {@link MpLocationParts} auf.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| URL  | Zeichenfolge |                  |

#### Vertrag

- **@example:** parseLocation('/users/42?tab=info#bio')
  // → { Pfad: '/users/42', Abfrage: { Tab: 'info' }, Hash: '#bio' }

### stringifyLocation

**Art:** Funktion

```typescript
function stringifyLocation(location: MpLocationInput): string;
```

Stellen Sie aus Standortteilen eine app-relative URL-Zeichenfolge zusammen. Der Pfadname ist
normalisiert, die Abfrage serialisiert (mit einem führenden `?`, wenn nicht leer) und die
Hash mit dem Präfix `#`, wenn er nicht leer ist.

#### Parameter

| Name     | Geben Sie       | ein Beschreibung |
| -------- | --------------- | ---------------- |
| Standort | MpLocationInput |                  |

#### Vertrag

- **@example:** stringifyLocation({ path: '/users/42', query: { tab: 'info' }, hash: 'bio' })
  // → '/users/42?tab=info#bio'

## `src/path`

### buildPath

**Art:** Funktion

```typescript
function buildPath(
  pattern: string,
  parameters: Record<string, MpParameterValue | readonly MpParameterValue[]> = {},
): string;
```

Erstellen Sie einen konkreten Pfadnamen aus einem Muster und einer Reihe von Parametern sowie einer Codierung
jeder Wert. Optionale Segmente werden gelöscht, wenn ihr Wert fehlt; a
Es fehlen erforderliche Parameterwürfe.

#### Parameter

| Name      | Geben Sie                                                       | ein Beschreibung |
| --------- | --------------------------------------------------------------- | ---------------- |
| Muster    | Zeichenfolge                                                    |                  |
| Parameter | Record<string, MpParameterValue \| readonly MpParameterValue[]> |                  |

#### Vertrag

- **@example:** buildPath('/users/:id', { id: 42 }) // → '/users/42'
  buildPath('/users/:id?', {}) // → '/users'
  buildPath('/files/:rest*', { rest: ['a', 'b'] }) // → '/files/a/b'

### Kompilierungspfad

**Art:** Funktion

```typescript
function compilePath(pattern: string): MpCompiledPath;
```

Kompilieren Sie ein Pfadmuster in einen {@link MpCompiledPath} mit der Reihenfolge
Parameterschlüssel und einen passenden regulären Ausdruck.

#### Parameter

| Name   | Geben Sie    | ein Beschreibung |
| ------ | ------------ | ---------------- |
| Muster | Zeichenfolge |                  |

#### Vertrag

- **@example:** compilePath('/users/:id').regexp.test('/users/42') // → true

### matchPath

**Art:** Funktion

```typescript
function matchPath(pattern: string, pathname: string): MpRouteParameters | undefined;
```

Vergleichen Sie einen Pfadnamen mit einem Muster und geben Sie die extrahierten Parameter zurück oder
`undefined`, wenn der Pfadname nicht übereinstimmt.

#### Parameter

| Name     | Geben Sie    | ein Beschreibung |
| -------- | ------------ | ---------------- |
| Muster   | Zeichenfolge |                  |
| Pfadname | Zeichenfolge |                  |

#### Vertrag

- **@example:** matchPath('/users/:id', '/users/42') // → { id: '42' }
  matchPath('/users/:id', '/posts/42') // → undefiniert

### MpCompiledPath

**Art:** Komponente

```typescript
export interface MpCompiledPath
```

Das Ergebnis der Kompilierung eines Pfadmusters in einen Matcher.

### MpPathParameterKey

**Art:** Komponente

```typescript
export interface MpPathParameterKey
```

Ein einzelner Parameterschlüssel, der aus einem kompilierten Pfadmuster extrahiert wurde.

### normalizePath

**Art:** Funktion

```typescript
function normalizePath(path: string): string;
```

Normalisieren Sie ein Pfadmuster (oder einen Pfadnamen) in eine kanonische Form mit einem führenden Schrägstrich
ohne abschließenden Schrägstrich (mit Ausnahme des Stammverzeichnisses, das `/` bleibt).

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Pfad | Zeichenfolge |                  |

### WILDCARD_PARAM_KEY

**Art:** konstant

```typescript
export const WILDCARD_PARAM_KEY;
```

Der Schlüssel, unter dem ein eigenständiges `*`-Catch-All-Segment erfasst wird.

## `src/query`

### parseQuery

**Art:** Funktion

```typescript
function parseQuery(search: string): MpQueryParameters;
```

Analysieren Sie eine Abfragezeichenfolge in eine {@link MpQueryParameters}-Map. Ein führendes `?` ist
optional. Wiederholte Schlüssel werden der Reihe nach in ein Array von Werten umgewandelt.

#### Parameter

| Name   | Geben Sie    | ein Beschreibung |
| ------ | ------------ | ---------------- |
| suchen | Zeichenfolge |                  |

#### Vertrag

- **@example:** parseQuery('?tag=a&tag=b&page=2') // → { tag: ['a', 'b'], page: '2' }

### stringifyQuery

**Art:** Funktion

```typescript
function stringifyQuery(query: MpQueryInput): string;
```

Serialisieren Sie eine {@link MpQueryInput}-Map in eine Abfragezeichenfolge mit einem führenden `?`.
oder eine leere Zeichenfolge, wenn nichts zu serialisieren ist. `null`/`undefined`
Werte (und Array-Elemente) werden gelöscht; Arrays geben jeweils ein `key=value`-Paar aus.

#### Parameter

| Name    | Geben Sie    | ein Beschreibung |
| ------- | ------------ | ---------------- |
| Abfrage | MpQueryInput |                  |

#### Vertrag

- **@example:** stringifyQuery({ tag: ['a', 'b'], page: 2 }) // → '?tag=a&tag=b&page=2'
  stringifyQuery({}) // → ''

## `src/types`

### MpGuardOutcome

**Art:** Komponente

```typescript
export type MpGuardOutcome = void | boolean | MpRouteLocationRaw;
```

Das von einem Routenwächter zurückgegebene Ergebnis, bevor ein Übergang festgeschrieben wird.

### MpHistory

**Art:** Komponente

```typescript
export interface MpHistory
```

Eine von einer Laufzeit bereitgestellte Browser-, Hash- oder Speicherverlaufsimplementierung.

### MpHistoryEntry

**Art:** Komponente

```typescript
export interface MpHistoryEntry
```

Keine Beschreibung angegeben.

### MpHistoryEvent

**Art:** Komponente

```typescript
export interface MpHistoryEvent
```

Keine Beschreibung angegeben.

### MpHistoryListener

**Art:** Komponente

```typescript
export type MpHistoryListener = (event: MpHistoryEvent) => void;
```

Keine Beschreibung angegeben.

### MpHistoryMode

**Art:** Komponente

```typescript
export type MpHistoryMode = 'web' | 'hash' | 'memory';
```

Die Verlaufsstrategie, die der Router eines Adapters verwenden sollte.

- `web` – HTML5-Verlauf (saubere URLs), der Standard für Browser-Apps.
- `hash` – Hash-basierter Verlauf (`/#/path`), für statisches Hosting ohne
  Server schreibt neu.
  – `memory` – In-Memory-Verlauf ohne URL, für SSR und Tests.

### MpMetadataContext

**Art:** Komponente

```typescript
export interface MpMetadataContext
```

Keine Beschreibung angegeben.

### MpMetadataHook

**Art:** Komponente

```typescript
export type MpMetadataHook = (context: MpMetadataContext) => MpRouteMetadata | void | Promise<MpRouteMetadata | void>;
```

Keine Beschreibung angegeben.

### MpNamedLocation

**Art:** Komponente

```typescript
export interface MpNamedLocation
```

Ein Navigationsziel, ausgedrückt durch den Routennamen.

### MpNavigationContext

**Art:** Komponente

```typescript
export interface MpNavigationContext
```

Keine Beschreibung angegeben.

### MpNavigationFailure

**Art:** Komponente

```typescript
export interface MpNavigationFailure
```

Keine Beschreibung angegeben.

### MpNavigationFailureType

**Art:** Komponente

```typescript
export type MpNavigationFailureType = 'aborted' | 'cancelled' | 'duplicated' | 'error' | 'not-found';
```

Keine Beschreibung angegeben.

### MpNavigationRedirect

**Art:** Komponente

```typescript
export interface MpNavigationRedirect
```

Keine Beschreibung angegeben.

### MpNavigationResult

**Art:** Komponente

```typescript
export type MpNavigationResult = MpNavigationSuccess | MpNavigationRedirect | MpNavigationFailure;
```

Keine Beschreibung angegeben.

### MpNavigationSuccess

**Art:** Komponente

```typescript
export interface MpNavigationSuccess
```

Keine Beschreibung angegeben.

### MpNavigationType

**Art:** Komponente

```typescript
export type MpNavigationType = 'push' | 'replace' | 'pop' | 'go';
```

Ein Übergangsvorgang, der von einem Router oder einer Verlaufsimplementierung initiiert wird.

### MPParameterValue

**Art:** Komponente

```typescript
export type MpParameterValue = string | number | boolean | null | undefined;
```

Ein einzelner, Framework-neutraler Pfad-/Abfrageparameter-Eingabewert.

### MpPathLocation

**Art:** Komponente

```typescript
export interface MpPathLocation
```

Ein durch den Pfad ausgedrücktes Navigationsziel.

### MpQueryInput

**Art:** Komponente

```typescript
export type MpQueryInput = Record<string, MpParameterValue | readonly MpParameterValue[]>;
```

Rohe, Framework-neutrale Abfrageeingabe. Werte können Skalare oder Arrays (wiederholt) sein
Schlüssel); `null`/`undefined`-Werte werden bei der Serialisierung gelöscht.

### MpQueryParameters

**Art:** Komponente

```typescript
export type MpQueryParameters = Record<string, string | string[]>;
```

Geparste Abfrageparameter: eine skalare Zeichenfolge oder ein Array von Zeichenfolgen pro Schlüssel.

### MpReadonlySignal

**Art:** Komponente

```typescript
export interface MpReadonlySignal<Value>
```

Struktureller reaktiver Zustand, der von Adaptern verwendet wird, ohne ein Framework-Grundelement vorzuschreiben.

### MpRedirect

**Art:** Komponente

```typescript
export type MpRedirect =
  MpRouteLocationRaw | ((to: MpResolvedLocation) => MpRouteLocationRaw | Promise<MpRouteLocationRaw>);
```

Ein Umleitungsziel, optional berechnet aus der Zielroute.

### MpResolvedLocation

**Art:** Komponente

```typescript
export interface MpResolvedLocation
```

Ein vollständig aufgelöster Standort, rahmenneutral.

### MpRoute

**Art:** Komponente

```typescript
export interface MpRoute<View = unknown>
```

Eine Framework-neutrale Routendefinition. Derselbe Baum kann in übersetzt werden
Jeder Zielrouter (vue-Router, react-Router, TanStack Router) oder dateibasiert
Konvention (Next.js, Nuxt) durch einen Adapter.

### MpRouteChangeEvent

**Art:** Komponente

```typescript
export interface MpRouteChangeEvent
```

Keine Beschreibung angegeben.

### MpRouteChangeListener

**Art:** Komponente

```typescript
export type MpRouteChangeListener = (event: MpRouteChangeEvent) => void;
```

Keine Beschreibung angegeben.

### MpRouteGuard

**Art:** Komponente

```typescript
export type MpRouteGuard = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
) => MpGuardOutcome | Promise<MpGuardOutcome>;
```

Ein Routenwächter, der von einer laufzeiteigenen Navigationszustandsmaschine ausgewertet wird.

### MpRouteLink

**Art:** Komponente

```typescript
export interface MpRouteLink
```

Keine Beschreibung angegeben.

### MpRouteLocationRaw

**Art:** Komponente

```typescript
export type MpRouteLocationRaw = string | MpPathLocation | MpNamedLocation;
```

Ein Framework-neutrales Navigationsziel: eine URL-Zeichenfolge oder ein strukturiertes Objekt.

### MpRouteMeta

**Art:** Komponente

```typescript
export type MpRouteMeta = Record<string, unknown>;
```

Beliebige, Framework-neutrale Metadaten, die an eine Route angehängt sind.

### MpRouteMetadata

**Art:** Komponente

```typescript
export interface MpRouteMetadata extends MpRouteMeta
```

Keine Beschreibung angegeben.

### MpRouteParameters

**Art:** Komponente

```typescript
export type MpRouteParameters = Record<string, string | string[]>;
```

Aufgelöste Pfadparameter, die aus einer übereinstimmenden Route extrahiert wurden. Ein Wert ist ein Array
Nur für wiederholbare (Catch-All-)Segmente wie `:rest*`.

### MpRouter

**Art:** Komponente

```typescript
export interface MpRouter
```

Den rahmenneutralen Navigationsvertrag erfüllt jeder Adapter. Adapter evtl
Stellen Sie reaktive Äquivalente bereit (z. B. gibt der `useMpRoute` des Vue-Adapters eine zurück
reaktiver Ref), aber die Imperativfläche ist überall gleich.

### MpRouterAdapter

**Art:** Komponente

```typescript
export interface MpRouterAdapter
```

Laufzeiteigener Routerstatus und Navigationsvertrag.

### MpRouteViewAdapter

**Art:** Komponente

```typescript
export interface MpRouteViewAdapter<View = unknown, Outlet = unknown>
```

Keine Beschreibung angegeben.

### MpRouteViewContext

**Art:** Komponente

```typescript
export interface MpRouteViewContext<View = unknown>
```

Keine Beschreibung angegeben.

### MpScrollBehavior

**Art:** Komponente

```typescript
export type MpScrollBehavior = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
  savedPosition?: MpScrollPosition,
) => MpScrollPosition | false | void | Promise<MpScrollPosition | false | void>;
```

Keine Beschreibung angegeben.

### MpScrollPosition

**Art:** Komponente

```typescript
export interface MpScrollPosition
```

Keine Beschreibung angegeben.
