# @mission-platform/router API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/core/router/docs/reference/generated/api.md: [packages/core/router/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/router` のパブリック ソース宣言から生成されます。

## `src/capabilities`

### createMpRouterCapabilities

**種類:** 関数

```typescript
function createMpRouterCapabilities(overrides: MpRouterCapabilityOverrides = {}): MpRouterCapabilities;
```

ターゲット アダプタ、SSR、またはテスト用のニュートラル機能オブジェクトを作成します。

#### パラメータ

| 名前           | タイプ                      | 説明 |
| -------------- | --------------------------- | ---- |
| オーバーライド | MpRouterCapabilityOverrides |      |

### isMpRouterCapabilityError

**種類:** 関数

```typescript
function isMpRouterCapabilityError(error: unknown): error is MpRouterCapabilityError;
```

不明なエラーが決定的なニュートラル ルーター フォールバック エラーであるかどうか。

#### パラメータ

| 名前   | タイプ | 説明 |
| ------ | ------ | ---- |
| エラー | 不明   |      |

### MP_ROUTER_COMPILER_MARKER

**種類:** 定数

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

Forge ルーター コンパイラー プラグインによって消費される安定したメタデータ。

### エムピーリンク

**種類:** コンポーネント

```typescript
function MpLink(properties: MpLinkProps): MpLinkDescriptor;
```

ニュートラルリンクマーカー。ネイティブ ターゲットは、コンパイル中にこの関数を置き換えます。

#### パラメータ

| 名前       | タイプ      | 説明 |
| ---------- | ----------- | ---- |
| プロパティ | MpLinkProps |      |

### MPLink記述子

**種類:** コンポーネント

```typescript
export interface MpLinkDescriptor
```

SSR および未コンパイルのテストで使用される、フレームワークに依存しないリンクの説明。

### MpLinkオプション

**種類:** コンポーネント

```typescript
export interface MpLinkOptions
```

ニュートラル リンクとネイティブ ナビゲーション コールによって共有されるオプション。

### MpLinkProps

**種類:** コンポーネント

```typescript
export interface MpLinkProps extends MpLinkOptions
```

{@link MpLink} で表されるコンパイラ マーカーによって受け入れられるプロパティ。

### MpNavigationオプション

**種類:** コンポーネント

```typescript
export interface MpNavigationOptions extends MpLinkOptions
```

ネイティブ ナビゲーション実装に転送されるオプション。

### MPRouterCapabilities

**種類:** コンポーネント

```typescript
export interface MpRouterCapabilities
```

使用側アプリケーションのネイティブ ルーターによって提供される機能。

### MPRouterCapability

**種類:** コンポーネント

```typescript
export type MpRouterCapability = 'link' | 'route' | 'navigate' | 'resolve' | 'view';
```

ルーターのターゲットが独自に低下させる可能性がある機能。

### MpRouterCapabilityError

**種類:** コンポーネント

```typescript
export class MpRouterCapabilityError extends Error
```

フォールバックで操作を実行できない場合に、安定した診断が発生します。

### MpRouterCapabilityOverrides

**種類:** コンポーネント

```typescript
export type MpRouterCapabilityOverrides = Partial<MpRouterCapabilities>;
```

ターゲット アダプターおよび SSR/テスト ハーネスに役立つ部分的な実装。

### MpRouterView

**種類:** コンポーネント

```typescript
function MpRouterView(properties: MpRouterViewProps = {}): MpRouterViewDescriptor;
```

ニュートラルアウトレット/ビューマーカー。ネイティブ ターゲットは、コンパイル中にこの関数を置き換えます。

#### パラメータ

| 名前       | タイプ            | 説明 |
| ---------- | ----------------- | ---- |
| プロパティ | MpRouterViewProps |      |

### MpRouterViewDescriptor

**種類:** コンポーネント

```typescript
export interface MpRouterViewDescriptor
```

SSR および未コンパイルのテストで使用される、フレームワークに依存しないビューの記述。

### MpRouterViewProps

**種類:** コンポーネント

```typescript
export interface MpRouterViewProps
```

ニュートラル アウトレット/ビュー コンパイラ マーカーによって受け入れられる小道具。

### 解決MpLink

**種類:** 関数

```typescript
function resolveMpLink(
  to: MpRouteLocationRaw,
  capabilities: Pick<MpRouterCapabilities, 'resolve'> = useMpRouter(),
): string;
```

アプリ所有のルーターを通じてターゲットを解決します。オプションの機能
引数により、SSR/テストおよびターゲット アダプターでヘルパーが決定的になります。

#### パラメータ

| 名前 | タイプ                                | 説明 |
| ---- | ------------------------------------- | ---- |
| へ   | MpRouteLocationRaw                    |      |
| 能力 | Pick<MpRouterCapabilities, 'resolve'> |      |

### useMpNavigation

**種類:** 関数

```typescript
function useMpNavigation(): Pick<MpRouterCapabilities, 'navigate' | 'resolve'>;
```

命令型ナビゲーション機能のみを読み取ります。

### useMpRoute

**種類:** 関数

```typescript
function useMpRoute(): MpResolvedLocation | null;
```

現在のアプリが所有するルートを読み取ります。未コンパイルのフォールバックは SSR セーフであり、
`null` を返します。コンパイラ ターゲットは、この呼び出しをネイティブ ルート状態に置き換えます。

### useMpRouter

**種類:** 関数

```typescript
function useMpRouter(): MpRouterCapabilities;
```

アプリが提供するニュートラル機能オブジェクトを読み取ります。

## `src/compatibility-fixtures`

### アプリケーション互換性治具

**種類:** 定数

```typescript
export const applicationCompatibilityFixtures;
```

説明はありません。

### ドキュメント互換性フィクスチャ

**種類:** 定数

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

ドキュメント アプリのロケール/検索/キャッチオール テーブルをミラーリングするルート レコード。

### ドキュメント互換性フィクスチャ

**種類:** 定数

```typescript
export const documentationCompatibilityFixture: ForgeCompatibilityFixture;
```

ドキュメント アプリのロケール/検索/キャッチオール テーブルをミラーリングするルート レコード。

### Forge互換性ケース

**種類:** インターフェース

```typescript
export interface ForgeCompatibilityCase
```

説明はありません。

### ForgeCompatibilityFixture

**種類:** インターフェース

```typescript
export interface ForgeCompatibilityFixture
```

説明はありません。

### myCareNotesCompatibilityFixture

**種類:** 定数

```typescript
export const myCareNotesCompatibilityFixture: ForgeCompatibilityFixture;
```

My Care Notes の言語セグメントと URL 駆動のオーバーレイをミラーリングするルート レコード。

### ルーター互換性フィクスチャー

**種類:** 定数

```typescript
export const routerCompatibilityFixtures: readonly ForgeCompatibilityCase[];
```

すべてのランタイムのコントラクト テスト スイートによって消費される共有動作行。

### ウェブサイト互換性治具

**種類:** 定数

```typescript
export const websiteCompatibilityFixture: ForgeCompatibilityFixture;
```

Web サイトのオプションのプレフィックス付きロケール ホーム ルートをミラーリングするルート レコード。

## `src/contracts`

### フォージガードの成果

**種類：**タイプ

```typescript
export type ForgeGuardOutcome = MpGuardOutcome;
```

説明はありません。

### フォージヒストリー

**種類：**タイプ

```typescript
export type ForgeHistory = MpHistory;
```

ランタイムによって提供されるブラウザ、ハッシュ、またはメモリ履歴の実装。

### ForgeHistoryEntry

**種類：**タイプ

```typescript
export type ForgeHistoryEntry = MpHistoryEntry;
```

説明はありません。

### Forge歴史イベント

**種類：**タイプ

```typescript
export type ForgeHistoryEvent = MpHistoryEvent;
```

説明はありません。

### ForgeHistoryListener

**種類：**タイプ

```typescript
export type ForgeHistoryListener = MpHistoryListener;
```

説明はありません。

### フォージヒストリーモード

**種類：**タイプ

```typescript
export type ForgeHistoryMode = MpHistoryMode;
```

説明はありません。

### メタデータコンテキストの鍛造

**種類：**タイプ

```typescript
export type ForgeMetadataContext = MpMetadataContext;
```

説明はありません。

### ForgeMetadataHook

**種類：**タイプ

```typescript
export type ForgeMetadataHook = MpMetadataHook;
```

説明はありません。

### ForgeNamedTarget

**種類：**タイプ

```typescript
export type ForgeNamedTarget = MpNamedLocation;
```

説明はありません。

### ForgeNavigationContext

**種類：**タイプ

```typescript
export type ForgeNavigationContext = MpNavigationContext;
```

説明はありません。

### フォージナビゲーション失敗

**種類：**タイプ

```typescript
export type ForgeNavigationFailure = MpNavigationFailure;
```

説明はありません。

### ForgeNavigationFailureType

**種類：**タイプ

```typescript
export type ForgeNavigationFailureType = MpNavigationFailureType;
```

説明はありません。

### ForgeNavigationRedirect

**種類：**タイプ

```typescript
export type ForgeNavigationRedirect = MpNavigationRedirect;
```

説明はありません。

### ForgeNavigationResult

**種類：**タイプ

```typescript
export type ForgeNavigationResult = MpNavigationResult;
```

説明はありません。

### 鍛造ナビゲーション成功

**種類：**タイプ

```typescript
export type ForgeNavigationSuccess = MpNavigationSuccess;
```

説明はありません。

### ForgeNavigationType

**種類：**タイプ

```typescript
export type ForgeNavigationType = MpNavigationType;
```

説明はありません。

### ForgeParameterValue

**種類：**タイプ

```typescript
export type ForgeParameterValue = MpParameterValue;
```

説明はありません。

### フォージパスターゲット

**種類：**タイプ

```typescript
export type ForgePathTarget = MpPathLocation;
```

説明はありません。

### ForgeQuery

**種類：**タイプ

```typescript
export type ForgeQuery = MpQueryParameters;
```

説明はありません。

### ForgeQuery入力

**種類：**タイプ

```typescript
export type ForgeQueryInput = MpQueryInput;
```

説明はありません。

### ForgeReadonlyシグナル

**種類：**タイプ

```typescript
export type ForgeReadonlySignal<Value> = MpReadonlySignal<Value>;
```

フレームワーク プリミティブを規定せずにアダプターによって使用される構造反応状態。

### フォージリダイレクト

**種類：**タイプ

```typescript
export type ForgeRedirect = MpRedirect;
```

説明はありません。

### ForgeResolvedRoute

**種類：**タイプ

```typescript
export type ForgeResolvedRoute = MpResolvedLocation;
```

説明はありません。

### ForgeRouteChangeEvent

**種類：**タイプ

```typescript
export type ForgeRouteChangeEvent = MpRouteChangeEvent;
```

説明はありません。

### ForgeRouteChangeListener

**種類：**タイプ

```typescript
export type ForgeRouteChangeListener = MpRouteChangeListener;
```

説明はありません。

### フォージルートガード

**種類：**タイプ

```typescript
export type ForgeRouteGuard = MpRouteGuard;
```

説明はありません。

### フォージルートリンク

**種類：**タイプ

```typescript
export type ForgeRouteLink = MpRouteLink;
```

説明はありません。

### ForgeRouteMatch

**種類：**タイプ

```typescript
export type ForgeRouteMatch = MpRouteMatch;
```

説明はありません。

### ForgeRouteMeta

**種類：**タイプ

```typescript
export type ForgeRouteMeta = MpRouteMeta;
```

説明はありません。

### ForgeRouteMetadata

**種類：**タイプ

```typescript
export type ForgeRouteMetadata = MpRouteMetadata;
```

説明はありません。

### ForgeRouteパラメータ

**種類：**タイプ

```typescript
export type ForgeRouteParameters = MpRouteParameters;
```

説明はありません。

### Forgeルーターアダプター

**種類：**タイプ

```typescript
export type ForgeRouterAdapter = MpRouterAdapter;
```

説明はありません。

### ルートレコードの作成

**種類：**タイプ

```typescript
export type ForgeRouteRecord<View = unknown> = MpRoute<View>;
```

説明はありません。

### フォージルートターゲット

**種類：**タイプ

```typescript
export type ForgeRouteTarget = MpRouteLocationRaw;
```

説明はありません。

### ForgeRouteViewAdapter

**種類：**タイプ

```typescript
export type ForgeRouteViewAdapter<View = unknown, Outlet = unknown> = MpRouteViewAdapter<View, Outlet>;
```

説明はありません。

### ForgeRouteViewContext

**種類：**タイプ

```typescript
export type ForgeRouteViewContext<View = unknown> = MpRouteViewContext<View>;
```

説明はありません。

### ForgeScrollBehavior

**種類：**タイプ

```typescript
export type ForgeScrollBehavior = MpScrollBehavior;
```

説明はありません。

### スクロール位置を鍛造

**種類：**タイプ

```typescript
export type ForgeScrollPosition = MpScrollPosition;
```

説明はありません。

## `src/define-routes`

### createRouteResolver

**種類:** 関数

```typescript
function createRouteResolver(routes: readonly MpRoute[]): MpRouteResolver;
```

ルート ツリーにバインドされた {@link MpRouteResolver} を作成し、一度フラット化して、
`match`/`resolve` 呼び出しを繰り返すと、同じ絶対ルート リストが再利用されます。

#### パラメータ

| 名前   | タイプ                 | 説明 |
| ------ | ---------------------- | ---- |
| ルート | 読み取り専用 MpRoute[] |      |

### ルートの定義

**種類:** 関数

```typescript
function defineRoutes(routes: T): T;
```

完全なタイプのフレームワーク中立のルート ツリーを定義する ID ヘルパー
推論。これを使用して、編集者がルート `name`、`meta`、および子を型チェックするようにします。

#### パラメータ

| 名前   | タイプ | 説明 |
| ------ | ------ | ---- |
| ルート | た     |      |

#### 契約

- **@example:** const Routes = defineRoutes([
  { パス: '/'、名前: 'ホーム'、コンポーネント: ホーム },
  { パス: '/users/:id'、名前: 'user'、コンポーネント: ユーザー },
  ])

### 名前によるルートの検索

**種類:** 関数

```typescript
function findRouteByName(routes: readonly MpRoute[], name: string): MpFlatRoute | undefined;
```

指定された名前の最初の平坦化されたルートを検索します。

#### パラメータ

| 名前   | タイプ                 | 説明 |
| ------ | ---------------------- | ---- |
| ルート | 読み取り専用 MpRoute[] |      |
| 名前   | 文字列                 |      |

### 平坦化ルート

**種類:** 関数

```typescript
function flattenRoutes(routes: readonly MpRoute[]): MpFlatRoute[];
```

ネストされたルート ツリーを絶対ルートの深さ優先リストに平坦化します。
各ルートの `path` はその親に対して解決され、`meta` はその親から継承されます。
その先祖たち。

#### パラメータ

| 名前   | タイプ                 | 説明 |
| ------ | ---------------------- | ---- |
| ルート | 読み取り専用 MpRoute[] |      |

### ルートの一致

**種類:** 関数

```typescript
function matchRoutes(routes: readonly MpRoute[], pathname: string): MpRouteMatch | undefined;
```

パス名をルート ツリーと照合し、最初のルートを返します (
深さ優先の定義順序)、パターンが一致するもの、または `undefined`。

#### パラメータ

| 名前   | タイプ                 | 説明 |
| ------ | ---------------------- | ---- |
| ルート | 読み取り専用 MpRoute[] |      |
| パス名 | 文字列                 |      |

### MPフラットルート

**種類:** コンポーネント

```typescript
export interface MpFlatRoute
```

継承されたメタデータと先祖を含む、絶対パスにフラット化されたルート。

### MpRouteMatch

**種類:** コンポーネント

```typescript
export interface MpRouteMatch
```

ルート ツリーに対するパス名の一致が成功しました。

### MpRouteResolver

**種類:** コンポーネント

```typescript
export interface MpRouteResolver
```

単一のルート ツリーにバインドされたリゾルバー。

### 解決場所

**種類:** 関数

```typescript
function resolveLocation(to: MpRouteLocationRaw, routes: readonly MpRoute[] = []): MpResolvedLocation;
```

フレームワークに依存しない {@link MpRouteLocationRaw} を完全な形式に解決します。
{@link MpResolvedLocation}。文字列とパスの場所は、
`params`/`name`/`meta` を設定するルート ツリー (指定されている場合)。名前付き
場所は、一致するルートからパスを構築します。

#### パラメータ

| 名前   | タイプ                 | 説明 |
| ------ | ---------------------- | ---- |
| へ     | MpRouteLocationRaw     |      |
| ルート | 読み取り専用 MpRoute[] |      |

#### 契約

- **@example:**solveLocation({ name: 'user', params: { id: 42 } }, Routes)
  // → { パス: '/users/42', fullPath: '/users/42', params: { id: '42' }, … }

## `src/location`

### MpLocation入力

**種類:** コンポーネント

```typescript
export interface MpLocationInput
```

部分によって記述される場所。クエリ/ハッシュはオプションです。

### MpLocationParts

**種類:** コンポーネント

```typescript
export interface MpLocationParts
```

URL の分解された部分 (パス名、クエリ、ハッシュ)。

### 正規化ハッシュ

**種類:** 関数

```typescript
function normalizeHash(hash: string): string;
```

ハッシュ フラグメントを正規化して、先頭に 1 つの `#` を含めるか、空にします。

#### パラメータ

| 名前     | タイプ | 説明 |
| -------- | ------ | ---- |
| ハッシュ | 文字列 |      |

### 解析位置

**種類:** 関数

```typescript
function parseLocation(url: string): MpLocationParts;
```

アプリ相対 URL をその {@link MpLocationParts} に分割します。

#### パラメータ

| 名前 | タイプ | 説明 |
| ---- | ------ | ---- |
| URL  | 文字列 |      |

#### 契約

- **@example:** parseLocation('/users/42?tab=info#bio')
  // → { パス: '/users/42'、クエリ: { タブ: 'info' }、ハッシュ: '#bio' }

### stringifyLocation

**種類:** 関数

```typescript
function stringifyLocation(location: MpLocationInput): string;
```

場所の部分からアプリ相対 URL 文字列を組み立てます。パス名は
正規化され、クエリはシリアル化されます (空でない場合は先頭に `?` が付きます)。
空でない場合、ハッシュには `#` という接頭辞が付けられます。

#### パラメータ

| 名前 | タイプ         | 説明 |
| ---- | -------------- | ---- |
| 場所 | MpLocation入力 |      |

#### 契約

- **@example:** stringifyLocation({ パス: '/users/42'、クエリ: { タブ: 'info' }、ハッシュ: 'bio' })
  // → '/users/42?tab=info#bio'

## `src/path`

### ビルドパス

**種類:** 関数

```typescript
function buildPath(
  pattern: string,
  parameters: Record<string, MpParameterValue | readonly MpParameterValue[]> = {},
): string;
```

パターンとパラメータのセット、エンコーディングから具体的なパス名を構築します。
それぞれの値。オプションのセグメントは、その値が存在しない場合には削除されます。ある
必須パラメータが欠落している場合がスローされます。

#### パラメータ

| 名前       | タイプ                                                            | 説明 |
| ---------- | ----------------------------------------------------------------- | ---- |
| パターン   | 文字列                                                            |      |
| パラメータ | レコード<string, MpParameterValue \| readonly MpParameterValue[]> |      |

#### 契約

- **@example:** buildPath('/users/:id', { id: 42 }) // → '/users/42'
  buildPath('/users/:id?', {}) // → '/users'
  buildPath('/files/:rest*', {rest: ['a', 'b'] }) // → '/files/a/b'

### コンパイルパス

**種類:** 関数

```typescript
function compilePath(pattern: string): MpCompiledPath;
```

順序付けされたパス パターンを {@link MpCompiledPath} にコンパイルします。
パラメータキーと一致する正規表現。

#### パラメータ

| 名前     | タイプ | 説明 |
| -------- | ------ | ---- |
| パターン | 文字列 |      |

#### 契約

- **@example:** applyPath('/users/:id').regexp.test('/users/42') // → true

### マッチパス

**種類:** 関数

```typescript
function matchPath(pattern: string, pathname: string): MpRouteParameters | undefined;
```

パス名をパターンと照合して、抽出されたパラメータを返すか、
パス名が一致しない場合は `undefined`。

#### パラメータ

| 名前     | タイプ | 説明 |
| -------- | ------ | ---- |
| パターン | 文字列 |      |
| パス名   | 文字列 |      |

#### 契約

- **@example:** matchPath('/users/:id', '/users/42') // → { id: '42' }
  matchPath('/users/:id', '/posts/42') // → 未定義

### Mpコンパイルされたパス

**種類:** コンポーネント

```typescript
export interface MpCompiledPath
```

パス パターンをマッチャーにコンパイルした結果。

### MpPathParameterKey

**種類:** コンポーネント

```typescript
export interface MpPathParameterKey
```

コンパイルされたパス パターンから抽出された単一のパラメーター キー。

### 正規化パス

**種類:** 関数

```typescript
function normalizePath(path: string): string;
```

パス パターン (またはパス名) を正規の先頭のスラッシュ形式に正規化します。
末尾にスラッシュはありません (`/` のままのルートを除く)。

#### パラメータ

| 名前 | タイプ | 説明 |
| ---- | ------ | ---- |
| パス | 文字列 |      |

### WILDCARD_PARAM_KEY

**種類:** 定数

```typescript
export const WILDCARD_PARAM_KEY;
```

スタンドアロン `*` キャッチオール セグメントがキャプチャされるキー。

## `src/query`

### 解析クエリ

**種類:** 関数

```typescript
function parseQuery(search: string): MpQueryParameters;
```

クエリ文字列を {@link MpQueryParameters} マップに解析します。先頭の `?` は
オプション。繰り返されるキーは、順番に値の配列に折りたたまれます。

#### パラメータ

| 名前 | タイプ | 説明 |
| ---- | ------ | ---- |
| 検索 | 文字列 |      |

#### 契約

- **@example:** parseQuery('?tag=a&tag=b&page=2') // → { タグ: ['a', 'b'], ページ: '2' }

### stringifyQuery

**種類:** 関数

```typescript
function stringifyQuery(query: MpQueryInput): string;
```

{@link MpQueryInput} マップを先頭に `?` を付けたクエリ文字列にシリアル化します。
シリアル化するものが何もない場合は空の文字列。 `null`/`undefined`
値 (および配列項目) は削除されます。配列はそれぞれ 1 つの `key=value` ペアを出力します。

#### パラメータ

| 名前   | タイプ      | 説明 |
| ------ | ----------- | ---- |
| クエリ | MpQuery入力 |      |

#### 契約

- **@example:** stringifyQuery({ タグ: ['a', 'b'], ページ: 2 }) // → '?tag=a&tag=b&page=2'
  stringifyQuery({}) // → ''

## `src/types`

### MPGuard の結果

**種類:** コンポーネント

```typescript
export type MpGuardOutcome = void | boolean | MpRouteLocationRaw;
```

遷移がコミットされる前にルート ガードによって返される結果。

### 議員履歴

**種類:** コンポーネント

```typescript
export interface MpHistory
```

ランタイムによって提供されるブラウザ、ハッシュ、またはメモリ履歴の実装。

### MpHistoryEntry

**種類:** コンポーネント

```typescript
export interface MpHistoryEntry
```

説明はありません。

### Mp歴史イベント

**種類:** コンポーネント

```typescript
export interface MpHistoryEvent
```

説明はありません。

### MpHistoryListener

**種類:** コンポーネント

```typescript
export type MpHistoryListener = (event: MpHistoryEvent) => void;
```

説明はありません。

### Mp 履歴モード

**種類:** コンポーネント

```typescript
export type MpHistoryMode = 'web' | 'hash' | 'memory';
```

アダプターのルーターが使用する必要がある履歴戦略。

- `web` — HTML5 履歴 (クリーン URL)、ブラウザー アプリのデフォルト。
- `hash` — ハッシュベースの履歴 (`/#/path`)、静的ホスティング用
  server rewrites.
- `memory` — SSR およびテスト用の URL なしのメモリ内履歴。

### MpMetadataContext

**種類:** コンポーネント

```typescript
export interface MpMetadataContext
```

説明はありません。

### MpMetadataHook

**種類:** コンポーネント

```typescript
export type MpMetadataHook = (context: MpMetadataContext) => MpRouteMetadata | void | Promise<MpRouteMetadata | void>;
```

説明はありません。

### mpNamedLocation

**種類:** コンポーネント

```typescript
export interface MpNamedLocation
```

航路名で表現される航行対象。

### MpNavigationContext

**種類:** コンポーネント

```typescript
export interface MpNavigationContext
```

説明はありません。

### MPナビゲーション失敗

**種類:** コンポーネント

```typescript
export interface MpNavigationFailure
```

説明はありません。

### MpNavigationFailureType

**種類:** コンポーネント

```typescript
export type MpNavigationFailureType = 'aborted' | 'cancelled' | 'duplicated' | 'error' | 'not-found';
```

説明はありません。

### MpNavigationRedirect

**種類:** コンポーネント

```typescript
export interface MpNavigationRedirect
```

説明はありません。

### MpNavigationResult

**種類:** コンポーネント

```typescript
export type MpNavigationResult = MpNavigationSuccess | MpNavigationRedirect | MpNavigationFailure;
```

説明はありません。

### MPナビゲーション成功

**種類:** コンポーネント

```typescript
export interface MpNavigationSuccess
```

説明はありません。

### MpNavigationType

**種類:** コンポーネント

```typescript
export type MpNavigationType = 'push' | 'replace' | 'pop' | 'go';
```

ルーターまたは履歴実装によって開始される移行操作。

### MpParameterValue

**種類:** コンポーネント

```typescript
export type MpParameterValue = string | number | boolean | null | undefined;
```

単一の、フレームワークに依存しないパス/クエリ パラメーターの入力値。

### MpPathLocation

**種類:** コンポーネント

```typescript
export interface MpPathLocation
```

パスで表現されるナビゲーションターゲット。

### MpQuery入力

**種類:** コンポーネント

```typescript
export type MpQueryInput = Record<string, MpParameterValue | readonly MpParameterValue[]>;
```

生の、フレームワークに依存しないクエリ入力。値はスカラーまたは配列にすることができます (繰り返し
キー); `null`/`undefined` 値はシリアル化時に削除されます。

### MpQueryパラメータ

**種類:** コンポーネント

```typescript
export type MpQueryParameters = Record<string, string | string[]>;
```

解析されたクエリ パラメータ: スカラー文字列またはキーごとの文字列の配列。

### MpReadonly信号

**種類:** コンポーネント

```typescript
export interface MpReadonlySignal<Value>
```

フレームワーク プリミティブを規定せずにアダプターによって使用される構造反応状態。

### MPリダイレクト

**種類:** コンポーネント

```typescript
export type MpRedirect =
  MpRouteLocationRaw | ((to: MpResolvedLocation) => MpRouteLocationRaw | Promise<MpRouteLocationRaw>);
```

リダイレクト ターゲット。オプションで宛先ルートから計算されます。

### MPResolvedLocation

**種類:** コンポーネント

```typescript
export interface MpResolvedLocation
```

フレームワークに依存しない、完全に解決された場所。

### MPルート

**種類:** コンポーネント

```typescript
export interface MpRoute<View = unknown>
```

フレームワークに依存しないルート定義。同じ木を次のように翻訳できます。
任意のターゲットルーター (vue-router、react-router、TanStack Router) またはファイルベース
アダプターによる規則 (Next.js、Nuxt)。

### MpRouteChangeEvent

**種類:** コンポーネント

```typescript
export interface MpRouteChangeEvent
```

説明はありません。

### MpRouteChangeListener

**種類:** コンポーネント

```typescript
export type MpRouteChangeListener = (event: MpRouteChangeEvent) => void;
```

説明はありません。

### MpRouteGuard

**種類:** コンポーネント

```typescript
export type MpRouteGuard = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
) => MpGuardOutcome | Promise<MpGuardOutcome>;
```

ランタイムが所有するナビゲーション ステート マシンによって評価されるルート ガード。

### MpRouteLink

**種類:** コンポーネント

```typescript
export interface MpRouteLink
```

説明はありません。

### MpRouteLocationRaw

**種類:** コンポーネント

```typescript
export type MpRouteLocationRaw = string | MpPathLocation | MpNamedLocation;
```

フレームワークに依存しないナビゲーション ターゲット: URL 文字列または構造化オブジェクト。

### MpRouteMeta

**種類:** コンポーネント

```typescript
export type MpRouteMeta = Record<string, unknown>;
```

ルートに添付される任意のフレームワーク中立のメタデータ。

### MpRouteメタデータ

**種類:** コンポーネント

```typescript
export interface MpRouteMetadata extends MpRouteMeta
```

説明はありません。

### MpRouteパラメータ

**種類:** コンポーネント

```typescript
export type MpRouteParameters = Record<string, string | string[]>;
```

一致したルートから抽出された解決されたパス パラメータ。値は配列です
`:rest*` などの反復可能な (キャッチオール) セグメントのみ。

### MPルーター

**種類:** コンポーネント

```typescript
export interface MpRouter
```

フレームワークに依存しないナビゲーション契約は、すべてのアダプターで実現されます。アダプターは、
リアクティブな等価物を公開します (例: Vue アダプターの `useMpRoute` は
reactive ref) ですが、命令面はどこでも同じです。

### MPルーターアダプター

**種類:** コンポーネント

```typescript
export interface MpRouterAdapter
```

ランタイムが所有するルーターの状態とナビゲーション コントラクト。

### MpRouteViewAdapter

**種類:** コンポーネント

```typescript
export interface MpRouteViewAdapter<View = unknown, Outlet = unknown>
```

説明はありません。

### MpRouteViewContext

**種類:** コンポーネント

```typescript
export interface MpRouteViewContext<View = unknown>
```

説明はありません。

### MpScrollBehavior

**種類:** コンポーネント

```typescript
export type MpScrollBehavior = (
  to: MpResolvedLocation,
  from: MpResolvedLocation | null,
  savedPosition?: MpScrollPosition,
) => MpScrollPosition | false | void | Promise<MpScrollPosition | false | void>;
```

説明はありません。

### MpScrollPosition

**種類:** コンポーネント

```typescript
export interface MpScrollPosition
```

説明はありません。
