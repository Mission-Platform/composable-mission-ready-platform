# הפניה ל-@mission-platform/vite-plugin-forge API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> packages/tooling/vite/forge/docs/reference/generated/api.md: [packages/tooling/vite/forge/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/vite-plugin-forge`.

## `src/build-integration`

### forgeArtifactPublishPlugin

פונקציה **סוג:**

```typescript
function forgeArtifactPublishPlugin(options: ForgeArtifactPublishOptions): Plugin;
```

פרסם את כל הפלט המקורי דרך אותה עסקת מניפסט כמו Forge
מקורות שנוצרו. חבילות מקוריות וכלי הצהרה עשויים לכתוב בחופשיות
בתוך `attemptDirectory`; ספריה זו לעולם אינה היעד שפורסם.

#### פרמטרים

| שם       | הקלד                        | תיאור |
| -------- | --------------------------- | ----- |
| אפשרויות | ForgeArtifactPublishOptions |       |

### forgeBuildLifecyclePlugin

פונקציה **סוג:**

```typescript
function forgeBuildLifecyclePlugin(options: ForgeBuildLifecycleOptions): Plugin;
```

חבר תוכנית יעד אחת ל-Vite או ל-Rolldown/tsdown. הדור הוא
ממתין בכוונה מהווים של מחזור החיים, לעולם לא בזמן שנוצרת תצורה.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| אפשרויות | ForgeBuildLifecycleOptions |       |

### forgeVirtualEntry

פונקציה **סוג:**

```typescript
function forgeVirtualEntry(targetId: string): string;
```

שימוש בכניסה וירטואלית יציבה עד שהיעד הוכן על ידי buildStart.

#### פרמטרים

| שם       | הקלד   | תיאור |
| -------- | ------ | ----- |
| targetId | מחרוזת |       |

## `src/compiler/artifact-manifest`

### createForgeArtifactManifest

פונקציה **סוג:**

```typescript
function createForgeArtifactManifest(
  targetId: string,
  artifacts: readonly ForgeArtifactRecord[],
  complete = true,
): ForgeArtifactManifest;
```

בנה מניפסט יציב מבלי להכניס חותמות זמן לפלט שנוצר.

#### פרמטרים

| שם       | הקלד                              | תיאור |
| -------- | --------------------------------- | ----- |
| targetId | מחרוזת                            |       |
| חפצים    | לקריאה בלבד ForgeArtifactRecord[] |       |
| להשלים   |                                   |       |

### ForgeArtifactKind

**סוג:** סוג

```typescript
export type ForgeArtifactKind = 'entry' | 'module' | 'declaration' | 'map' | 'asset' | 'style';
```

סוגי קבצים שיעד Forge עשוי לשמור בעץ החפצים שנוצר שלו.

### ForgeArtifactManifest

**סוג:** ממשק

```typescript
export interface ForgeArtifactManifest
```

מלאי מלאי פלט עבור יעד מפורש אחד.

### ForgeArtifactRecord

**סוג:** ממשק

```typescript
export interface ForgeArtifactRecord
```

פלט דטרמיניסטי אחד מתועד על ידי מניפסט חפץ בהיקף מטרה.

## `src/compiler/artifact-path`

### assertForgeArtifactRoot

פונקציה **סוג:**

```typescript
function assertForgeArtifactRoot(root: string): string;
```

אמת שורש פלט ודחה קישור סימלי קיים או לא ספרייה.
רכיבים צאצאים נבדקים על ידי פותר החפצים לפני השימוש.

#### פרמטרים

| שם   | הקלד   | תיאור |
| ---- | ------ | ----- |
| שורש | מחרוזת |       |

### secureForgeArtifactDirectory

פונקציה **סוג:**

```typescript
function ensureForgeArtifactDirectory(root: string, directory: string): string;
```

צור ספריית פלט רכיב אחד בכל פעם ללא קישורים הבאים.

#### פרמטרים

| שם    | הקלד   | תיאור |
| ----- | ------ | ----- |
| שורש  | מחרוזת |       |
| מדריך | מחרוזת |       |

### resolveForgeArtifactPath

פונקציה **סוג:**

```typescript
function resolveForgeArtifactPath(root: string, relativeName: string): string;
```

פתור שם חפץ וודא שרכיבי נתיב קיימים בטוחים.

#### פרמטרים

| שם       | הקלד   | תיאור |
| -------- | ------ | ----- |
| שורש     | מחרוזת |       |
| שם משפחה | מחרוזת |       |

### validateForgeArtifactName

פונקציה **סוג:**

```typescript
function validateForgeArtifactName(relativeName: string): string;
```

אמת שם חפץ מבלי לנרמל מקטעי נתיב לא בטוחים.

#### פרמטרים

| שם       | הקלד   | תיאור |
| -------- | ------ | ----- |
| שם משפחה | מחרוזת |       |

### validateForgeArtifactSegment

פונקציה **סוג:**

```typescript
function validateForgeArtifactSegment(segment: string): string;
```

אמת רכיב נתיב בודד המשמש לבניית שורש חפץ.

#### פרמטרים

| שם  | הקלד   | תיאור |
| --- | ------ | ----- |
| קטע | מחרוזת |       |

## `src/compiler/artifact-writer`

### createForgeArtifactWriter

פונקציה **סוג:**

```typescript
function createForgeArtifactWriter(
  outDir: string,
  targetId: string,
  options: ForgeArtifactWriterOptions = {},
): ForgeArtifactWriter;
```

לא סופק תיאור.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| outDir   | מחרוזת                     |       |
| targetId | מחרוזת                     |       |
| אפשרויות | ForgeArtifactWriterOptions |       |

### forgeArtifactAttemptDirectory

פונקציה **סוג:**

```typescript
function forgeArtifactAttemptDirectory(outDir: string, targetId: string): string;
```

הקצה ספריית אחים בבעלות לניסיון בניית יעד מקורי.

#### פרמטרים

| שם       | הקלד   | תיאור |
| -------- | ------ | ----- |
| outDir   | מחרוזת |       |
| targetId | מחרוזת |       |

### ForgeArtifactWriter

**סוג:** ממשק

```typescript
export interface ForgeArtifactWriter
```

לא סופק תיאור.

## `src/compiler/cache`

### createEmptyForgeCacheStats

פונקציה **סוג:**

```typescript
function createEmptyForgeCacheStats(): ForgeCacheStats;
```

לא סופק תיאור.

### DEFAULT_FORGE_CACHE_LIMITS

**סוג:** קבוע

```typescript
export const DEFAULT_FORGE_CACHE_LIMITS: Required<ForgeCacheLimits>;
```

לא סופק תיאור.

### ForgeCacheLimits

**סוג:** ממשק

```typescript
export interface ForgeCacheLimits
```

מגבלות שהוחלו על מטמון המהדר בתהליך.

### ForgeCacheStats

**סוג:** ממשק

```typescript
export interface ForgeCacheStats
```

מוני מטמון ניתנים לצפייה במשך חיי שירות מהדר אחד.

## `src/compiler/compile`

### compileComponentModule

פונקציה **סוג:**

```typescript
function compileComponentModule(source: string, options: CompileOptions): CompiledModule;
```

הרכיב מודול רכיב נייטרלי (או מוגבל למסגרת) אחד לכל מסגרת שלו
מקור (שלב 1).

הוראת `"use <framework>";` מובילה נמחקת לפני הפליטה
כך שהמרקר לעולם אינו דולף לתוך הפלט; שער מודול מתוך
בניית מסגרת שאינה תואמת מטופלת במעלה הזרם על ידי שלב הגילוי
(ראה {@link moduleTargetsFramework}).

#### פרמטרים

| שם       | הקלד           | תיאור |
| -------- | -------------- | ----- |
| מקור     | מחרוזת         |       |
| אפשרויות | CompileOptions |       |

### CompiledModule

**סוג:** ממשק

```typescript
export interface CompiledModule
```

תוצאת שלב 1: מקור שנפלט והסיומת שהוא צריך להיכתב תחתיו.

### compileHookModule

פונקציה **סוג:**

```typescript
function compileHookModule(source: string, options: CompileHookOptions): CompiledModule;
```

קומפלו **מודול הוק** ניטרלי אחד (ניתן לכתיבה של פעם שנכתב נגד
הווים בסגנון React של `@mission-platform/forge-jsx`, _לא_ רכיב ממשק משתמש)
מקור לכל מסגרת (שלב 1).

#### פרמטרים

| שם       | הקלד               | תיאור |
| -------- | ------------------ | ----- |
| מקור     | מחרוזת             |       |
| אפשרויות | CompileHookOptions |       |

### CompileHookOptions

**סוג:** ממשק

```typescript
export interface CompileHookOptions
```

אפשרויות עבור {@link compileHookModule}.

### compileModule

פונקציה **סוג:**

```typescript
function compileModule(source: string, options: CompileModuleOptions): CompiledModule;
```

הידור מודול ניטרלי אחד באמצעות תוסף פלט שסופק על ידי המתקשר.

שלא כמו עטיפות הנוחות ההיסטוריות של JSX, נקודת הכניסה הזו משמרת
שפת הפלט הפתוחה של התוסף ושפות מודול העזר. זה ה
תפר משותף בשימוש על ידי חבילות יעד עצמאיות כגון Astro.

#### פרמטרים

| שם       | הקלד                 | תיאור |
| -------- | -------------------- | ----- |
| מקור     | מחרוזת               |       |
| אפשרויות | CompileModuleOptions |       |

### CompileModuleOptions

**סוג:** ממשק

```typescript
export interface CompileModuleOptions
```

אפשרויות להידור של מודול עם תוסף פלט שסופק חיצונית.

### CompileOptions

**סוג:** ממשק

```typescript
export interface CompileOptions
```

אפשרויות עבור {@link compileComponentModule}.

## `src/compiler/components`

### findComponentFunction

פונקציה **סוג:**

```typescript
function findComponentFunction(sourceFile: OxcParsedModule, name: string): OxcNode | undefined;
```

מצא הצהרת פונקציית Oxc מיוצאת עבור רכיב ניטרלי לפי שם.

#### פרמטרים

| שם         | הקלד            | תיאור |
| ---------- | --------------- | ----- |
| sourceFile | OxcParsedModule |       |
| שם         | מחרוזת          |       |

### isSlotElement

פונקציה **סוג:**

```typescript
function isSlotElement(node: OxcNode): boolean;
```

האם Oxc node הוא אלמנט נייטרלי בשם חריץ.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |

### readSlotName

פונקציה **סוג:**

```typescript
function readSlotName(node: OxcNode): string | undefined;
```

קרא את ה-`name="…"` הסטטי של אלמנט Oxc `<Slot>`.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |

## `src/compiler/constants`

### LOCAL_JSX_TYPES_FILE

**סוג:** קבוע

```typescript
export const LOCAL_JSX_TYPES_FILE;
```

שם הקובץ המשמש למודול סוגי JSX הספציפי למסגרת שנוצר.

### LOCAL_JSX_TYPES_MODULE

**סוג:** קבוע

```typescript
export const LOCAL_JSX_TYPES_MODULE;
```

המפרט היחסי למודול סוגי JSX הספציפיים למסגרת שנוצר.

### localJsxTypesModuleSource

פונקציה **סוג:**

```typescript
function localJsxTypesModuleSource(framework: JsxFramework): string;
```

צור הצהרות מסוג JSX הממוקמות במשותף ספציפית למסגרת.

#### פרמטרים

| שם    | הקלד         | תיאור |
| ----- | ------------ | ----- |
| מסגרת | JsxFramework |       |

### NEUTRAL_MODULE

**סוג:** קבוע

```typescript
export const NEUTRAL_MODULE;
```

החבילה הנייטרלית שממנה הרכיבים מייבאים את הפרימיטיבים שלהם.

## `src/compiler/directives`

### moduleTargetsFramework

פונקציה **סוג:**

```typescript
function moduleTargetsFramework(fileName: string, source: string, framework: string): boolean;
```

האם מודול ניטרלי או ספציפי למסגרת שייך לבניית יעד.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |
| מסגרת   | מחרוזת |       |

### readFrameworkDirective

פונקציה **סוג:**

```typescript
function readFrameworkDirective(fileName: string, source: string): JsxFramework | undefined;
```

קרא את ההנחיה המובילה `"use <framework>"` ממודול Oxc.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

## `src/compiler/discover`

### DiscoverComponents

פונקציה **סוג:**

```typescript
function discoverComponents(barrelSource: string, stripPrefix = 'Forge'): DiscoveredComponent[];
```

גלה את הרכיבים שחבית מייצאת וקבל את צורתם הציבורית. כל אחד
ייצוא ערך מותאם לממשק האביזרים המיוצא מחדש מאותו
הצהרה (על פי ועידת `<PublicName>Properties`) והתיקיה שבה היא חיה
in (שם בסיס המודול של הייצוא מחדש).

#### פרמטרים

| שם           | הקלד   | תיאור |
| ------------ | ------ | ----- |
| barrelSource | מחרוזת |       |
| stripPrefix  |        |       |

### DiscoverComponentsFromGraph

פונקציה **סוג:**

```typescript
function discoverComponentsFromGraph(
  graph: ForgeFileGraph,
  stripPrefix = 'Forge',
  diagnostics?: CompilerDiagnostic[],
): DiscoveredComponent[];
```

פרויקט ייצוא של רכיבים ציבוריים מהגרף הקנוני תוך שמירה על צורת התוצאה מדור קודם.

#### פרמטרים

| שם          | הקלד                 | תיאור |
| ----------- | -------------------- | ----- |
| גרף         | ForgeFileGraph       |       |
| stripPrefix |                      |       |
| אבחון       | CompilerDiagnostic[] |       |

### DiscoveredComponent

**סוג:** ממשק

```typescript
export interface DiscoveredComponent
```

רכיב ניטרלי שהתגלה בחבית, בתוספת צורתו הציבורית הנגזרת.

### DiscoveredHelperExport

**סוג:** ממשק

```typescript
export interface DiscoveredHelperExport
```

**מודול עוזר** שאינו מורכב ממרכיבים שיוצא מחדש מהחבית (למשל
`toast-store`), כך שה-API הציבורי שלו רוכב דרך `./react` / שנוצר
ערך `./vue` לצד הרכיבים.

### discoverHelperExports

פונקציה **סוג:**

```typescript
function discoverHelperExports(barrelSource: string, componentFolders: ReadonlySet<string>): DiscoveredHelperExport[];
```

גלה את **מודולי העזר** שהחבית מייצאת מחדש - כל `ייצוא { … }
מהמשפט './…'` ששם בסיס המודול שלו הוא **לא** אחד מהשמות שהתגלו
תיקיות רכיבים. משמש להעברה של ממשקי API משותפים למסגרת אגנוסטית (כגון
עוזרי `useToast`/`showToast`/... ההכרחיים של `toast-store`, המקבילה
של `useToast` composable של החבילה Vue) דרך הערך שנוצר, אז
צרכנים נוהגים בדיוק באותו יחיד לפי מסגרת שבו משתמשים הרכיבים.

#### פרמטרים

| שם               | הקלד                | תיאור |
| ---------------- | ------------------- | ----- |
| barrelSource     | מחרוזת              |       |
| componentFolders | ReadonlySet<string> |       |

### discoverHelperExportsFromGraph

פונקציה **סוג:**

```typescript
function discoverHelperExportsFromGraph(
  graph: ForgeFileGraph,
  componentFolders: ReadonlySet<string>,
  discoveredComponents?: readonly DiscoveredComponent[],
): DiscoveredHelperExport[];
```

פרויקט יצוא ציבורי שאינו מרכיב מהגרף הקנוני.

#### פרמטרים

| שם                   | הקלד                           | תיאור |
| -------------------- | ------------------------------ | ----- |
| גרף                  | ForgeFileGraph                 |       |
| componentFolders     | ReadonlySet<string>            |       |
| discoveredComponents | Readonly DiscoveredComponent[] |       |

## `src/compiler/facts`

### ForgeExportFact

**סוג:** ממשק

```typescript
export interface ForgeExportFact
```

הצהרה או ייצוא מחדש שהתגלו במודול מקור Forge.

### ForgeImportFact

**סוג:** ממשק

```typescript
export interface ForgeImportFact
```

ייבוא ​​סטטי שהתגלה במודול מקור Forge.

### ForgeModuleFacts

**סוג:** ממשק

```typescript
export interface ForgeModuleFacts
```

כל עובדות המודול הסטטי הדרושות לבניית גרף קובץ Forge הקנוני.

### ForgeSourceSpan

**סוג:** ממשק

```typescript
export interface ForgeSourceSpan
```

מיקום מקור המשמש לאבחון גרפים ועובדות ייבוא/ייצוא.

### inspectForgeModule

פונקציה **סוג:**

```typescript
function inspectForgeModule(fileName: string, source: string): ForgeModuleFacts;
```

חילוץ יבוא סטטי, ייצוא, קצוות מסוגים בלבד ועובדות מסגרת ממודול מנותח.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

## `src/compiler/frontends`

### parseForgeSource

פונקציה **סוג:**

```typescript
function parseForgeSource(fileName: string, source: string): OxcParsedModule;
```

נתח את המקור של Forge דרך Oxc למודול הניתן לסידרה הנייטרלי
חזית קצה, אופטימיזציה והסקת מסקנות כולם צורכים. אין קובץ מקור TypeScript
נוצר; המודול המוחזר הוא הייצוג היחיד המגובה על מנתח.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

### parseFrontendModule

פונקציה **סוג:**

```typescript
function parseFrontendModule(
  fileName: string,
  source: string,
  moduleKind: 'component' | 'composable',
  componentName?: string,
): FrontendModule;
```

לא סופק תיאור.

#### פרמטרים

| שם         | הקלד                    | תיאור |
| ---------- | ----------------------- | ----- |
| שם קובץ    | מחרוזת                  |       |
| מקור       | מחרוזת                  |       |
| moduleKind | 'רכיב' \| 'ניתן להרכיב' |       |
| שם רכיב    | מחרוזת                  |       |

## `src/compiler/generation-context`

### createForgeGenerationContext

פונקציה **סוג:**

```typescript
function createForgeGenerationContext(options: ForgeGenerationContextOptions): ForgeGenerationContext;
```

לא סופק תיאור.

#### פרמטרים

| שם       | הקלד                          | תיאור |
| -------- | ----------------------------- | ----- |
| אפשרויות | ForgeGenerationContextOptions |       |

### ForgeGenerationContext

**סוג:** ממשק

```typescript
export interface ForgeGenerationContext
```

לא סופק תיאור.

### ForgeGenerationContextOptions

**סוג:** ממשק

```typescript
export interface ForgeGenerationContextOptions
```

לא סופק תיאור.

## `src/compiler/graph`

### buildForgeFileGraph

פונקציה **סוג:**

```typescript
function buildForgeFileGraph(options: ForgeFileGraphOptions): ForgeFileGraph;
```

בנה את גרף המקור הקנוני ממודול כניסה מוגדר אחד.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| אפשרויות | ForgeFileGraphOptions |       |

### ForgeFileEdge

**סוג:** ממשק

```typescript
export interface ForgeFileEdge
```

לא סופק תיאור.

### ForgeFileGraph

**סוג:** ממשק

```typescript
export interface ForgeFileGraph
```

לא סופק תיאור.

### ForgeFileGraphOptions

**סוג:** ממשק

```typescript
export interface ForgeFileGraphOptions
```

לא סופק תיאור.

### ForgeFileKind

**סוג:** סוג

```typescript
export type ForgeFileKind =
  'entry' | 'component' | 'composable' | 'code' | 'style' | 'folder' | 'asset' | 'declaration';
```

לא סופק תיאור.

### ForgeFileNode

**סוג:** ממשק

```typescript
export interface ForgeFileNode
```

לא סופק תיאור.

### ForgeGraphDiagnostic

**סוג:** ממשק

```typescript
export interface ForgeGraphDiagnostic
```

לא סופק תיאור.

### ForgeGraphDiagnosticCode

**סוג:** סוג

```typescript
export type ForgeGraphDiagnosticCode =
  | 'missing-entry'
  | 'missing-file'
  | 'unsupported-extension'
  | 'ambiguous-export'
  | 'unsupported-authoring-form'
  | 'cycle';
```

לא סופק תיאור.

## `src/compiler/hoist-static`

### hoistStaticJsx

פונקציה **סוג:**

```typescript
function hoistStaticJsx(
  module: OxcParsedModule,
  moduleKind: 'component' | 'composable' = 'component',
  componentName?: string,
): GenericHoistResult;
```

סימן סטטי ואז להניף את עץ העיבוד של מודול Oxc-מנותח, ולהחזיר את
מודול גנרי מונף בתוספת הערכים המורמות. שווה ערך להפעלת ה
מעבר שלב 1 לסימון סטטי ואחריו הנפה גנרית.

#### פרמטרים

| שם         | הקלד                    | תיאור |
| ---------- | ----------------------- | ----- |
| מודול      | OxcParsedModule         |       |
| moduleKind | 'רכיב' \| 'ניתן להרכיב' |       |
| שם רכיב    | מחרוזת                  |       |

## `src/compiler/imports`

### יבוא ניטרלי

**סוג:** ממשק

```typescript
export interface NeutralImports
```

השמות שמודול מייבא מהחבילה הנייטרלית, מפוצלים לפי סוג מחייב.

### קראייבוא ​​חיצוני

פונקציה **סוג:**

```typescript
function readExternalImports(fileName: string, source: string): string[];
```

אסוף יבוא חבילות חשוף המבוצע מילה במילה אל מקורות מסגרת שנוצרו.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

### readNeutralImports

פונקציה **סוג:**

```typescript
function readNeutralImports(fileName: string, source: string): NeutralImports;
```

בדוק את ייבוא ​​החבילות הנייטרליות של מודול.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

### readStyleImports

פונקציה **סוג:**

```typescript
function readStyleImports(fileName: string, source: string, sourceRoot?: string): StyleImport[];
```

אסוף גיליונות סגנונות יחסיים ממודול ניטרלי.

#### פרמטרים

| שם         | הקלד   | תיאור |
| ---------- | ------ | ----- |
| שם קובץ    | מחרוזת |       |
| מקור       | מחרוזת |       |
| sourceRoot | מחרוזת |       |

### resolveWorkspaceLocalImport

פונקציה **סוג:**

```typescript
function resolveWorkspaceLocalImport(
  specifier: string,
  sourceFileName: string,
  sourceRoot: string | undefined,
): string | undefined;
```

פתור ייבוא ​​`@/` מקומי של סביבת עבודה ביחס לקובץ המקור שלו.

#### פרמטרים

| שם             | הקלד               | תיאור |
| -------------- | ------------------ | ----- |
| מפרט           | מחרוזת             |       |
| sourceFileName | מחרוזת             |       |
| sourceRoot     | מחרוזת \| לא מוגדר |       |

### StyleIport

**סוג:** ממשק

```typescript
export interface StyleImport
```

ייבוא ​​גיליון סגנונות בוצע לתוך העץ השטוח שנוצר.

### usesI18nextT

פונקציה **סוג:**

```typescript
function usesI18nextT(node: OxcNode): boolean;
```

בין אם מודול Oxc או node קורא `i18next.t(...)`.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |

## `src/compiler/optimize`

### optimizeForgeModule

פונקציה **סוג:**

```typescript
function optimizeForgeModule(module: OxcParsedModule, options: OptimizeOptions = {}): OxcParsedModule;
```

הפעל את מעברי האופטימיזציה של שלב 1 ברמת המקור (גיזום ענפים מתים ו
מסקנות מפתח יציב) על מודול מנותח Oxc, מחזיר מודול מנותח מחדש
שהמקור שלו משקף את העריכות שהוחלו. סימון סטטי-node הוא טהור
טרנספורמציה ברמת שיא המטופלת על ידי {@link optimizeGenericModule}.

ברירת המחדל של כל מעבר מופעל; להעביר `{ deadBranchPruning: false, … }` כדי להשבית.

#### פרמטרים

| שם       | הקלד                 | תיאור |
| -------- | -------------------- | ----- |
| מודול    | OxcParsedModule      |       |
| אפשרויות | אופטימיזציה אפשרויות |       |

### optimizeSourceFile

**סוג:** קבוע

```typescript
export const optimizeSourceFile;
```

כינוי תאימות נשמר עבור אינטגרציות מהדר באמצעות השם הישן.

## `src/compiler/oxc`

### buildOxcParentMap

פונקציה **סוג:**

```typescript
function buildOxcParentMap(root: OxcNode): Map<OxcNode, OxcNode>;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| שורש | OxcNode |       |

### isOxcJsxRoot

פונקציה **סוג:**

```typescript
function isOxcJsxRoot(node: OxcNode): boolean;
```

נכון כאשר node הוא אלמנט JSX, אלמנט סגירה עצמית או fragment.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |

### isOxcNode

פונקציה **סוג:**

```typescript
function isOxcNode(value: unknown): value is OxcNode;
```

לא סופק תיאור.

#### פרמטרים

| שם  | הקלד    | תיאור |
| --- | ------- | ----- |
| ערך | לא ידוע |       |

### oxcArray

פונקציה **סוג:**

```typescript
function oxcArray(node: OxcNode, key: string): OxcNode[];
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |
| מפתח | מחרוזת  |       |

### oxcילדים

פונקציה **סוג:**

```typescript
function oxcChildren(node: OxcNode): OxcNode[];
```

ילד עומק ראשון הליכה על אינספור נכסים בשווי node.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |

### OxcComment

**סוג:** ממשק

```typescript
export interface OxcComment
```

לא סופק תיאור.

### oxcIdentifierName

פונקציה **סוג:**

```typescript
function oxcIdentifierName(node: OxcNode | undefined): string | undefined;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                | תיאור |
| ---- | ------------------- | ----- |
| node | OxcNode \| לא מוגדר |       |

### oxcLiteralValue

פונקציה **סוג:**

```typescript
function oxcLiteralValue(node: OxcNode | undefined): unknown;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                | תיאור |
| ---- | ------------------- | ----- |
| node | OxcNode \| לא מוגדר |       |

### OxcNode

**סוג:** ממשק

```typescript
export interface OxcNode
```

קבוצת המשנה הניתנת לסידרה של Oxc node המשמשת את החזית הנייטרלית.

### oxcNodeText

פונקציה **סוג:**

```typescript
function oxcNodeText(source: string, node: OxcNode | undefined): string;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                | תיאור |
| ---- | ------------------- | ----- |
| מקור | מחרוזת              |       |
| node | OxcNode \| לא מוגדר |       |

### oxcObject

פונקציה **סוג:**

```typescript
function oxcObject(node: OxcNode, key: string): OxcNode | undefined;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| node | OxcNode |       |
| מפתח | מחרוזת  |       |

### OxcParentMap

**סוג:** סוג

```typescript
export type OxcParentMap = ReadonlyMap<OxcNode, OxcNode>;
```

לא סופק תיאור.

### OxcParsedModule

**סוג:** ממשק

```typescript
export interface OxcParsedModule
```

לא סופק תיאור.

### OxcParseError

**סוג:** ממשק

```typescript
export interface OxcParseError
```

לא סופק תיאור.

### oxcProgramBody

פונקציה **סוג:**

```typescript
function oxcProgramBody(program: OxcNode): OxcNode[];
```

לא סופק תיאור.

#### פרמטרים

| שם     | הקלד    | תיאור |
| ------ | ------- | ----- |
| תוכנית | OxcNode |       |

### oxcSourceExpression

פונקציה **סוג:**

```typescript
function oxcSourceExpression(
  source: string,
  node: OxcNode,
  syntax: SourceBackedExpression['syntax'] = 'expression',
): SourceBackedExpression;
```

לא סופק תיאור.

#### פרמטרים

| שם    | הקלד                            | תיאור |
| ----- | ------------------------------- | ----- |
| מקור  | מחרוזת                          |       |
| node  | OxcNode                         |       |
| תחביר | SourceBackedExpression['תחביר'] |       |

### oxcSourceSpan

פונקציה **סוג:**

```typescript
function oxcSourceSpan(source: string, node: OxcNode): SourceSpan;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד    | תיאור |
| ---- | ------- | ----- |
| מקור | מחרוזת  |       |
| node | OxcNode |       |

### oxcString

פונקציה **סוג:**

```typescript
function oxcString(node: OxcNode | undefined, key: 'name' | 'value' | 'directive' = 'name'): string | undefined;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                     | תיאור |
| ---- | ------------------------ | ----- |
| node | OxcNode \| לא מוגדר      |       |
| מפתח | 'שם' \| 'ערך' \| 'הנחיה' |       |

### oxcTypeNode

פונקציה **סוג:**

```typescript
function oxcTypeNode(node: OxcNode | undefined): OxcNode | undefined;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                | תיאור |
| ---- | ------------------- | ----- |
| node | OxcNode \| לא מוגדר |       |

### oxcUnwrapModuleStatement

פונקציה **סוג:**

```typescript
function oxcUnwrapModuleStatement(statement: OxcNode): {
  readonly node: OxcNode;
  readonly exported: boolean;
  readonly exportStatement: OxcNode | undefined;
};
```

פתח את עטיפות `export …` כדי שהמתקשרים יראו את ההצהרה הבסיסית.

#### פרמטרים

| שם    | הקלד    | תיאור |
| ----- | ------- | ----- |
| הצהרה | OxcNode |       |

### parseOxcModule

פונקציה **סוג:**

```typescript
function parseOxcModule(fileName: string, source: string): OxcParsedModule;
```

לא סופק תיאור.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| שם קובץ | מחרוזת |       |
| מקור    | מחרוזת |       |

### stripOxcFrameworkDirective

פונקציה **סוג:**

```typescript
function stripOxcFrameworkDirective(module: OxcParsedModule): OxcParsedModule;
```

שחרר את הנחיות הפרולוג `"use <framework>"` מובילות ממודול מנותח.

#### פרמטרים

| שם    | הקלד            | תיאור |
| ----- | --------------- | ----- |
| מודול | OxcParsedModule |       |

### visitOxc

פונקציה **סוג:**

```typescript
function visitOxc(node: OxcNode, visitor: (node: OxcNode) => void | boolean): void;
```

לא סופק תיאור.

#### פרמטרים

| שם   | הקלד                              | תיאור |
| ---- | --------------------------------- | ----- |
| node | OxcNode                           |       |
| מבקר | (node: OxcNode) => ריק \| בוליאני |       |

## `src/compiler/pipeline`

### analyzeForgeModule

פונקציה **סוג:**

```typescript
function analyzeForgeModule(
  input: CompilerInput,
  service: ForgeCompilerService = defaultCompilerService,
): SemanticModule;
```

נתח, נרמל והסיק את המודול הסמנטי נייטרלי היעד.

זהו התפר הנייטרלי של כל צרכן שאינו מסגרת (בעיקר ה-CMS
מנהל ההקרנה) משתמש כדי להשיג את ה-IR מבלי לבחור תוסף יעד.
התוצאות משותפות דרך אותו מטמון שבו משתמש צינור המסגרת, אז א
רכיב המנותח עבור מספר יעדים בבנייה אחת מוסק רק פעם אחת.

#### פרמטרים

| שם    | הקלד                 | תיאור |
| ----- | -------------------- | ----- |
| קלט   | קלט מהדר             |       |
| שירות | ForgeCompilerService |       |

### קלט מהדר

**סוג:** ממשק

```typescript
export interface CompilerInput
```

קלט מקור ניטרלי מקובל על ידי צינור המהדר.

### CompilerPipeline

**סוג:** ממשק

```typescript
export interface CompilerPipeline
```

חוזה צינור משותף לכל נקודות הכניסה של המהדר.

### createCompilerPipeline

פונקציה **סוג:**

```typescript
function createCompilerPipeline(service: ForgeCompilerService = createForgeCompilerService()): CompilerPipeline;
```

צור את משדר השלב המשמש את נקודות הכניסה של המהדר.

#### פרמטרים

| שם    | הקלד                 | תיאור |
| ----- | -------------------- | ----- |
| שירות | ForgeCompilerService |       |

## `src/compiler/report`

### ForgeCompilationReport

**סוג:** ממשק

```typescript
export interface ForgeCompilationReport
```

אבחון ומדדים שנאספו במהלך חיי שירות אחד.

## `src/compiler/router`

### לנתח את יכולות הנתב

פונקציה **סוג:**

```typescript
function analyzeRouterCapabilities(
  input: Pick<RouterCompilerInput, 'source' | 'fileName' | 'moduleKind'>,
): RouterCapabilityModule;
```

נתח יבוא ושימושים ניטרליים של נתב מבלי לייבא שום נתב מקורי.

#### פרמטרים

| שם  | הקלד                                                              | תיאור |
| --- | ----------------------------------------------------------------- | ----- |
| קלט | Pick<RouterCompilerInput, 'source' \| 'fileName' \| 'moduleKind'> |       |

### compileRouterModule

פונקציה **סוג:**

```typescript
function compileRouterModule(input: RouterCompilerInput): RouterCompilationResult;
```

הידור שימוש נייטרלי בנתב באמצעות מתאם יעד מקורי נבחר.

#### פרמטרים

| שם  | הקלד                | תיאור |
| --- | ------------------- | ----- |
| קלט | RouterCompilerInput |       |

### createRouterCompilerPipeline

פונקציה **סוג:**

```typescript
function createRouterCompilerPipeline();
```

טופס ה-Dispatcher בשימוש על ידי המהדר Forge ועל ידי גופי יעד עצמאיים.

## `src/compiler/service`

### CompiledArtifact

**סוג:** ממשק

```typescript
export interface CompiledArtifact
```

פלט יעד בתוספת מטא נתונים של שירות, מבלי לשנות את GeneratedModule עצמו.

### createForgeCompilerService

פונקציה **סוג:**

```typescript
function createForgeCompilerService(limits: ForgeCacheLimits = {}): ForgeCompilerService;
```

לא סופק תיאור.

#### פרמטרים

| שם     | הקלד             | תיאור |
| ------ | ---------------- | ----- |
| גבולות | ForgeCacheLimits |       |

### ForgeCompileRequest

**סוג:** ממשק

```typescript
export interface ForgeCompileRequest
```

בקשה להידור יעד מפורש אחד.

### ForgeCompilerService

**סוג:** ממשק

```typescript
export interface ForgeCompilerService
```

לא סופק תיאור.

### ForgeInvalidationResult

**סוג:** ממשק

```typescript
export interface ForgeInvalidationResult
```

לא סופק תיאור.

### ForgeProjectInput

**סוג:** ממשק

```typescript
export interface ForgeProjectInput
```

קלט ברמת הפרויקט המשפיע על הכנת המקור וזהות המטמון.

### ForgeProjectSnapshot

**סוג:** ממשק

```typescript
export interface ForgeProjectSnapshot extends ForgeProjectInput
```

תמונת מצב בלתי ניתנת לשינוי הקשורה לפרויקט מהדר מוכן.

### PersistentForgeCompilerService

**סוג:** כיתה

```typescript
export class PersistentForgeCompilerService implements ForgeCompilerService
```

מצב מהדר סינכרוני ארוך טווח עבור הפעלה אחת של תהליך/בנייה.

## `src/compiler/session`

### createForgeBuildSession

פונקציה **סוג:**

```typescript
function createForgeBuildSession(options: CreateForgeBuildSessionOptions = {}): ForgeBuildSession;
```

צור את הבעלים המפורש של מחזור החיים המשמש את מתאמי Vite ומתאמי tsdown.
בנייה רושמת רק בעלות על שירות; גילוי הגרפים מתחיל ב
`prepare`, שאליו מתאמים קוראים מהוק למחזור חיים של צרור.

#### פרמטרים

| שם       | הקלד                           | תיאור |
| -------- | ------------------------------ | ----- |
| אפשרויות | CreateForgeBuildSessionOptions |       |

### CreateForgeBuildSessionOptions

**סוג:** ממשק

```typescript
export interface CreateForgeBuildSessionOptions
```

לא סופק תיאור.

### ForgeBuildKind

**סוג:** סוג

```typescript
export type ForgeBuildKind = 'component' | 'hook' | 'neutral' | 'router' | 'cms-island';
```

בניית מדיניות הנתמכת על ידי מחזור החיים המשותף של Forge.

### ForgeBuildPlan

**סוג:** ממשק

```typescript
export interface ForgeBuildPlan
```

הפרויקט הנייטרלי ויעדים נבחרים מתואמים לפי מחזור חיים אחד.

### ForgeBuildSession

**סוג:** ממשק

```typescript
export interface ForgeBuildSession
```

לא סופק תיאור.

### ForgeTargetGenerationContext

**סוג:** ממשק

```typescript
export interface ForgeTargetGenerationContext
```

ההקשר הועבר להתקשרות חוזרת של דור יעד לאחר הכנת הפרויקט.

### ForgeTargetGenerationResult

**סוג:** ממשק

```typescript
export interface ForgeTargetGenerationResult
```

התוצאה הוחזרה על ידי שלב יצירת מקור עצל של יעד.

### ForgeTargetPlan

**סוג:** ממשק

```typescript
export interface ForgeTargetPlan
```

יעד אחד בבעלות מתקשר בסשן בניית Forge.

### ForgeTargetResult

**סוג:** ממשק

```typescript
export interface ForgeTargetResult
```

לא סופק תיאור.

## `src/config`

### defineJsxHookLibraryConfig

פונקציה **סוג:**

```typescript
function defineJsxHookLibraryConfig(options: JsxHookLibraryConfigOptions): UserConfig;
```

לא סופק תיאור.

#### פרמטרים

| שם       | הקלד                        | תיאור |
| -------- | --------------------------- | ----- |
| אפשרויות | JsxHookLibraryConfigOptions |       |

### defineJsxLibraryConfig

פונקציה **סוג:**

```typescript
function defineJsxLibraryConfig(options: JsxLibraryConfigOptions): UserConfig;
```

לא סופק תיאור.

#### פרמטרים

| שם       | הקלד                    | תיאור |
| -------- | ----------------------- | ----- |
| אפשרויות | JsxLibraryConfigOptions |       |

### JsxHookLibraryConfigOptions

**סוג:** ממשק

```typescript
export interface JsxHookLibraryConfigOptions
```

לא סופק תיאור.

### JsxLibraryConfigOptions

**סוג:** ממשק

```typescript
export interface JsxLibraryConfigOptions
```

לא סופק תיאור.

### reactJsxPlugin

פונקציה **סוג:**

```typescript
function reactJsxPlugin(): Plugin;
```

לא סופק תיאור.

### solidJsxPlugin

פונקציה **סוג:**

```typescript
function solidJsxPlugin(): Plugin[];
```

התוסף `vite-plugin-solid` שמרכיב את Solid `.tsx` שנוצר
מקורות מקוריים (Babel `babel-preset-solid`), מה שהופך את ה-JSX הנייטרלי ל
פעולות DOM עדינות של SolidJS. ייצוא מחדש כך מגולגל ביד
`vite.config.ts` - כזה שמרכיב את `stagePlugins` משלו במקום
קורא ל-{@link defineJsxLibraryConfig} - יכול לחבר את ה-Solid להפוך את
באותה דרך {@link reactJsxPlugin}/{@link sveltePlugin} תן לזה לחבר את React/Svelte,
מבלי למשוך תלות ישירה חדשה. **נדרש** עבור `solid`
יעד: בלעדיו, טרנספורמציה Oxc JSX המוגדרת כברירת מחדל של Vite מרכיבה את Solid
מקורות נגד `react/jsx-runtime`, המייצרים פלט לא פונקציונלי.

**Vite בלבד** - תחת tsdown/Rolldown השתמש ב-{@link solidJsxTsdownPlugin} במקום זאת.

### SolidJsxTsdownPlugin

פונקציה **סוג:**

```typescript
function solidJsxTsdownPlugin(): Plugin;
```

תוסף Solid JSX תואם Rolldown/tsdown. נציגים ל-`vite-plugin-solid`'s
הוק `transform` בלבד (דילוג על הגדרת Vite בלבד `config`/`configResolved`) ו
מגדיר Rolldown `transform.jsx = 'preserve'` באמצעות הוק `options` כך ש-Oxc עושה זאת
לא לשכתב את Solid JSX כ-React.

### stagePluginsForTsdown

פונקציה **סוג:**

```typescript
function stagePluginsForTsdown(plugin: FrameworkOutputPlugin): Plugin[];
```

תוספים של שלב 2 ל-Forge framework שנבנה תחת **tsdown** (Rolldown).
העדיפו את זה על פני {@link reactJsxPlugin}/{@link solidJsxPlugin}/{@link sveltePlugin}
כאשר המצרף אינו Vite - התוספים Vite בלבד Svelte/Solid קורסים בגלל
הם דורשים את התצורה שנפתרה של Vite.

#### פרמטרים

| שם   | הקלד                  | תיאור |
| ---- | --------------------- | ----- |
| תוסף | FrameworkOutputPlugin |       |

### sveltePlugin

פונקציה **סוג:**

```typescript
function sveltePlugin(): Plugin[];
```

תוסף Vite להידור רכיבי Svelte שנוצרו.

### פלאגין svelteTsdown

פונקציה **סוג:**

```typescript
function svelteTsdownPlugin(): Plugin;
```

תוסף מהדר Svelte תואם Rolldown/tsdown. מרכיב `.svelte` SFCs באמצעות
`svelte/compiler` בהוק רגיל של `transform` - ללא ממשקי API של Vite עם תצורה פתורה.
העדיפו את זה (או את {@link stagePluginsForTsdown}) כשאתם בונים עם tsdown.

## `src/generate-hooks`

### generHookLibrarySources

פונקציה **סוג:**

```typescript
function generateHookLibrarySources(options: GenerateHookLibrarySourcesOptions): string;
```

הרכיב ספריית הוק ניטראלית לעץ המקור שלה לכל מסגרת (שלב 1),
החזרת נתיב מודול הכניסה שנוצר.

#### פרמטרים

| שם       | הקלד                              | תיאור |
| -------- | --------------------------------- | ----- |
| אפשרויות | GenerateHookLibrarySourcesOptions |       |

### GenerateHookLibrarySourcesOptions

**סוג:** ממשק

```typescript
export interface GenerateHookLibrarySourcesOptions
```

אפשרויות עבור {@link generateHookLibrarySources}.

## `src/generate`

### createFrameworkSourceTarget

פונקציה **סוג:**

```typescript
function createFrameworkSourceTarget(plugin: FrameworkOutputPlugin): FrameworkSourceTarget;
```

צור מתאר עץ מקור מתוך תוסף פלט מפורש.

#### פרמטרים

| שם   | הקלד                  | תיאור |
| ---- | --------------------- | ----- |
| תוסף | FrameworkOutputPlugin |       |

### FrameworkSourceTarget

**סוג:** ממשק

```typescript
export interface FrameworkSourceTarget
```

מוסכמות עץ מקור ספציפיות למטרות המשמשות את מנהל ההתקן של החבילה הגנרית.

### generFrameworkSources

פונקציה **סוג:**

```typescript
function generateFrameworkSources(options: GenerateFrameworkSourcesOptions): string;
```

הרכיב חבילת רכיבים ניטרליים לעץ המקור שלה לכל מסגרת (שלב 1),
החזרת נתיב מודול הכניסה שנוצר.

#### פרמטרים

| שם       | הקלד                            | תיאור |
| -------- | ------------------------------- | ----- |
| אפשרויות | GenerateFrameworkSourcesOptions |       |

### GenerateFrameworkSourcesOptions

**סוג:** ממשק

```typescript
export interface GenerateFrameworkSourcesOptions
```

אפשרויות עבור {@link generateFrameworkSources}.

## `src/tsdown`

### CanonicalChunkCandidate

**סוג:** סוג

```typescript
export type CanonicalChunkCandidate = | string |
```

לא סופק תיאור.

### defineTsdownForgeComponentsAll

פונקציה **סוג:**

```typescript
function defineTsdownForgeComponentsAll(options: TsdownForgeComponentPluginsOptions): UserConfig[];
```

בנה תצורות tsdown עצמאיות עבור כל מסגרת מבוקשת של רכיב Forge.

#### פרמטרים

| שם       | הקלד                               | תיאור |
| -------- | ---------------------------------- | ----- |
| אפשרויות | TsdownForgeComponentPluginsOptions |       |

### defineTsdownForgeEmailComponents

פונקציה **סוג:**

```typescript
function defineTsdownForgeEmailComponents(options: TsdownForgeEmailComponentsOptions): UserConfig;
```

בנה את רכיב האימייל של Forge הנייטרלי לשרת בלבד.

זה משמר את עץ הפורג כך
`@mission-platform/email-renderer` יכול לעשות אותו בסידרה בשרת.

#### פרמטרים

| שם       | הקלד                              | תיאור |
| -------- | --------------------------------- | ----- |
| אפשרויות | TsdownForgeEmailComponentsOptions |       |

### defineTsdownForgeHooksAll

פונקציה **סוג:**

```typescript
function defineTsdownForgeHooksAll(options: TsdownForgeHooksAllOptions): UserConfig[];
```

בנה מערך של תצורות tsdown עבור כל מסגרת מבוקשת של Forge Hooks
(בתוספת ערך השורש הנייטרלי כברירת מחדל). פחית `tsdown.config.ts` של חבילה
`export default defineTsdownForgeHooksAll(...)`.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| אפשרויות | TsdownForgeHooksAllOptions |       |

### resolveCanonicalChunkName

פונקציה **סוג:**

```typescript
function resolveCanonicalChunkName(chunkInfo: CanonicalChunkCandidate): string;
```

קבע את שם קובץ הנתח הנפלט הקנוני עבור נתח.
משמר נתיבים קנוניים `[name].js` ללא התנגשויות ומפות
Vue מודולי סקריפט וירטואליים ל-`${component}.script.js`.

#### פרמטרים

| שם        | הקלד                    | תיאור |
| --------- | ----------------------- | ----- |
| chunkInfo | CanonicalChunkCandidate |       |

### resolveCanonicalEntryName

פונקציה **סוג:**

```typescript
function resolveCanonicalEntryName(chunkInfo: CanonicalChunkCandidate): string;
```

קבע את שם קובץ הערך הנפלט הקנוני עבור נתח.
ערכי זיוף וירטואליים ממופים ל-`index.js`, בעודם נשמרים
מודולים מאצילים לרזולוציית שם נתח.

#### פרמטרים

| שם        | הקלד                    | תיאור |
| --------- | ----------------------- | ----- |
| chunkInfo | CanonicalChunkCandidate |       |

### tsdownForgeComponentPlugins

פונקציה **סוג:**

```typescript
function tsdownForgeComponentPlugins(options: TsdownForgeComponentPluginsOptions): TsdownPlugin[];
```

שחזר מסגרת אחת של Archetype-C **component** שנבנתה תחת tsdown:
שלב 1 (`generateFrameworkSources`) + פלאגין שלב 2 + css-import + פלאגין dts,
פולט לתוך `dist/<framework>/`.

#### פרמטרים

| שם       | הקלד                               | תיאור |
| -------- | ---------------------------------- | ----- |
| אפשרויות | TsdownForgeComponentPluginsOptions |       |

### TsdownForgeComponentPluginsOptions

**סוג:** ממשק

```typescript
export interface TsdownForgeComponentPluginsOptions
```

לא סופק תיאור.

### TsdownForgeEmailComponentsOptions

**סוג:** ממשק

```typescript
export interface TsdownForgeEmailComponentsOptions
```

לא סופק תיאור.

### tsdownForgeHookPlugins

פונקציה **סוג:**

```typescript
function tsdownForgeHookPlugins(options: TsdownForgeHooksAllOptions): TsdownPlugin[];
```

צורת tsdown-plugin מקורית של מתאם הוו. התוספים שהוחזרו מזריקים
תצורת היעד שלהם מ-`tsdownConfig`, המאפשרת ליצור בניית Hook
עם תצורת `defineTsdownLibrary` בבעלות מתקשר אחד.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| אפשרויות | TsdownForgeHooksAllOptions |       |

### TsdownForgeHooksAllOptions

**סוג:** ממשק

```typescript
export interface TsdownForgeHooksAllOptions
```

לא סופק תיאור.
