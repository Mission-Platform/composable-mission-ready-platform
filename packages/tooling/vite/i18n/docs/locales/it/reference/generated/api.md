# Riferimento API @mission-platform/vite-plugin-i18n

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/tooling/vite/i18n/docs/reference/generated/api.md: [packages/tooling/vite/i18n/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/vite-plugin-i18n`.

## `src/index`

### predefinito

**Tipo:** funzione

```typescript
function i18nPlugin(options: I18nPluginOptions = {}): Plugin;
```

Nessuna descrizione fornita.

#### Parametri

| Nome    | Digitare          | Descrizione |
| ------- | ----------------- | ----------- |
| opzioni | I18nPluginOptions |             |

### i18nPlugin

**Tipo:** funzione

```typescript
function i18nPlugin(options: I18nPluginOptions = {}): Plugin;
```

Nessuna descrizione fornita.

#### Parametri

| Nome    | Digitare          | Descrizione |
| ------- | ----------------- | ----------- |
| opzioni | I18nPluginOptions |             |

### I18nPluginOptions

**Tipo:** interfaccia

```typescript
export interface I18nPluginOptions
```

Nessuna descrizione fornita.

### readSupportedLocales

**Tipo:** funzione

```typescript
function readSupportedLocales(
  options: ReadSupportedLocalesOptions = {},
): string[];
```

Scopri le versioni locali supportate al **periodo di compilazione/configurazione**, utilizzando esattamente lo stesso
logica come il modulo `virtual:i18n-locales`. I file di configurazione Vite non possono importare a
modulo virtuale (esiste solo all'interno dell'app bundle), quindi questo consente a config
ricavare il suo elenco di impostazioni locali (ad esempio per percorsi SSG/mappe dei siti) dalla stessa fonte
della verità invece di mantenere manualmente un elenco parallelo.

#### Parametri

| Nome    | Digitare                     | Descrizione |
| ------- | ---------------------------- | ----------- |
| opzioni | LeggiSupportedLocalesOptions |             |

### LeggiSupportedLocalesOptions

**Tipo:** interfaccia

```typescript
export interface ReadSupportedLocalesOptions
```

Opzioni per l'helper in fase di compilazione {@link readSupportedLocales}.
