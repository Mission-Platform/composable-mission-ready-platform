# @mission-platform/vite-plugin-seo API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> packages/tooling/vite/seo/docs/reference/generated/api.md: [packages/tooling/vite/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/vite-plugin-seo` 中的公共源声明生成。

## `src/index`

### 默认

**种类：**功能

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

创建一个 Vite 插件，将 `robots.txt` 和 `sitemap.xml` 写入
项目开头的 `publicDir` （或 {@link SeoPluginOptions.outDir}）
每个构建和开发服务器都运行。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 选项 | SeoPlugin选项 |      |

#### 合同

- **@例子：** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### seo插件

**种类：**功能

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

创建一个 Vite 插件，将 `robots.txt` 和 `sitemap.xml` 写入
项目开头的 `publicDir` （或 {@link SeoPluginOptions.outDir}）
每个构建和开发服务器都运行。

#### 参数

| 名称 | 类型          | 描述 |
| ---- | ------------- | ---- |
| 选项 | SeoPlugin选项 |      |

#### 合同

- **@例子：** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### Seo插件选项

**种类：**接口

```typescript
export interface SeoPluginOptions
````

{@link seoPlugin} 的选项。
