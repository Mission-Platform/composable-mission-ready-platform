# @mission-platform/vite-plugin-seo API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/tooling/vite/seo/docs/reference/generated/api.md: [packages/tooling/vite/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/vite-plugin-seo` のパブリック ソース宣言から生成されます。

## `src/index`

### デフォルト

**種類:** 関数

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

を作成します Vite 書き込むプラグイン `robots.txt` そして `sitemap.xml` に
プロジェクトの `publicDir` (または {@link SeoPluginOptions.outDir}) の開始時に
すべてのビルドと開発サーバーの実行。

#### パラメータ

| 名前       | タイプ              | 説明 |
| ---------- | ------------------- | ---- |
| オプション | SeoPluginオプション |      |

#### 契約

- **@例：** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### seoプラグイン

**種類:** 関数

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

を作成します Vite 書き込むプラグイン `robots.txt` そして `sitemap.xml` に
プロジェクトの `publicDir` (または {@link SeoPluginOptions.outDir}) の開始時に
すべてのビルドと開発サーバーの実行。

#### パラメータ

| 名前       | タイプ              | 説明 |
| ---------- | ------------------- | ---- |
| オプション | SeoPluginオプション |      |

#### 契約

- **@例：** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### SeoPluginオプション

**種類:** インターフェース

```typescript
export interface SeoPluginOptions
````

{@link seoPlugin} のオプション。
