# مرجع @mission-platform/vite-plugin-seo API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/tooling/vite/seo/docs/reference/generated/api.md: [packages/tooling/vite/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/vite-plugin-seo`.

## `src/index`

### تقصير

**النوع:** الوظيفة

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

قم بإنشاء مكون إضافي Vite يكتب `robots.txt` و`sitemap.xml` في الملف
`publicDir` للمشروع (أو {@link SeoPluginOptions.outDir}) في بداية
كل بناء وتشغيل خادم التطوير.

#### حدود

| الاسم  | اكتب             | الوصف |
| ------ | ---------------- | ----- |
| خيارات | خيارات سيوبلوجين |       |

#### عقد

- **@مثال:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### com.seoPlugin

**النوع:** الوظيفة

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

قم بإنشاء مكون إضافي Vite يكتب `robots.txt` و`sitemap.xml` في الملف
`publicDir` للمشروع (أو {@link SeoPluginOptions.outDir}) في بداية
كل بناء وتشغيل خادم التطوير.

#### حدود

| الاسم  | اكتب             | الوصف |
| ------ | ---------------- | ----- |
| خيارات | خيارات سيوبلوجين |       |

#### عقد

- **@مثال:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### خيارات SeoPlugin

**النوع:** الواجهة

```typescript
export interface SeoPluginOptions
````

خيارات {@link seoPlugin}.
