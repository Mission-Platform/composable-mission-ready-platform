# Riferimento API @mission-platform/vite-plugin-flint

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/tooling/vite/flint/docs/reference/generated/api.md: [packages/tooling/vite/flint/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/vite-plugin-flint`.

## `src/compile`

### compileFlintFile

**Tipo:** funzione

```typescript
function compileFlintFile(
  fileName: string,
  options: FlintPluginOptions,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): FlintCompiledModule;
```

Compila un file FWS e restituisci il suo artefatto più i metadati della mappa sorgente.

#### Parametri

| Nome     | Digitare             | Descrizione |
| -------- | -------------------- | ----------- |
| nomefile | stringa              |             |
| opzioni  | FlintPluginOptions   |             |
| servizio | FlintCompilerService |             |

### compileFlintGraph

**Tipo:** funzione

```typescript
function compileFlintGraph(
  fileName: string,
  options: FlintPluginOptions,
  resolver: FlintModuleResolver,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): Promise<FlintCompiledModule>;
```

Risolvi, collega e compila un grafico del modulo FWS importato.

#### Parametri

| Nome       | Digitare             | Descrizione |
| ---------- | -------------------- | ----------- |
| nomefile   | stringa              |             |
| opzioni    | FlintPluginOptions   |             |
| risolutore | FlintModuleResolver  |             |
| servizio   | FlintCompilerService |             |

### FlintCompiledModule

**Tipo:** interfaccia

```typescript
export interface FlintCompiledModule
```

Sorgente compilata, metadati degli artefatti, mappa sorgente e report grafici opzionali.

### FlintPluginOptions

**Tipo:** interfaccia

```typescript
export interface FlintPluginOptions
```

Nessuna descrizione fornita.

### risolvereFlintPath

**Tipo:** funzione

```typescript
function resolveFlintPath(root: string, value: string): string;
```

Risolvi il percorso di un plugin relativo a root a meno che il valore non sia già assoluto.

#### Parametri

| Nome   | Digitare | Descrizione |
| ------ | -------- | ----------- |
| radice | stringa  |             |
| valore | stringa  |             |

## `src/generate`

### createFlintDeclarationsSource

**Tipo:** funzione

```typescript
function createFlintDeclarationsSource(compiled: FlintCompiledModule): string;
```

Genera un modulo contenente dichiarazioni, metadati di collegamento e facoltativi
Metadati del compilatore self-hosted per i consumatori di attrezzature e strumenti.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### createFlintManifestSource

**Tipo:** funzione

```typescript
function createFlintManifestSource(compiled: FlintCompiledModule): string;
```

Genera un modulo contenente il manifest ABI serializzabile JSON.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### createFlintModuleSource

**Tipo:** funzione

```typescript
function createFlintModuleSource(compiled: FlintCompiledModule): string;
```

Generare il normale modulo consumatore. Il modulo generato riesporta il file
funzioni compilate ed espone lo stesso manifest ABI tramite `abiManifest`.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### createFlintSourceMapSource

**Tipo:** funzione

```typescript
function createFlintSourceMapSource(compiled: FlintCompiledModule): string;
```

Genera un modulo la cui esportazione predefinita è l'oggetto della mappa sorgente generato.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### createFlintWasmSource

**Tipo:** funzione

```typescript
function createFlintWasmSource(compiled: FlintCompiledModule): string;
```

Genera un modulo la cui esportazione predefinita è l'array di byte Wasm compilato.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### createFlintWatSource

**Tipo:** funzione

```typescript
function createFlintWatSource(compiled: FlintCompiledModule): string;
```

Genera un modulo la cui esportazione predefinita è il testo WAT compilato. Questo è
destinato all'ispezione e alle dichiarazioni di conformità, non all'esecuzione Wasm.

#### Parametri

| Nome      | Digitare            | Descrizione |
| --------- | ------------------- | ----------- |
| compilato | FlintCompiledModule |             |

### FORGE_WEB_SCRIPT_DECLARATION_QUERY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_DECLARATIONS_QUERY;
```

Query che restituisce dichiarazioni collegate e metadati del grafico come modulo ES.

### FORGE_WEB_SCRIPT_MANIFEST_QUERY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_MANIFEST_QUERY;
```

Query che restituisce il manifest ABI compilato come modulo ES.

### FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY;
```

Query che restituisce la mappa di origine generata come esportazione predefinita del modulo.

### FORGE_WEB_SCRIPT_WASM_QUERY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_WASM_QUERY;
```

Query che restituisce i byte Wasm compilati come esportazione predefinita del modulo.

### FORGE_WEB_SCRIPT_WAT_QUERY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_WAT_QUERY;
```

Query che restituisce il WAT generato, incluso l'abbassamento della tabella dei rami, come stringa.

## `src/index`

### predefinito

**Tipo:** funzione

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installare la compilazione di Forge Web Script e le query sugli artefatti virtuali in Vite.
Il plugin preserva l'ABI scalare dell'esportazione Wasm mentre espone manifest,
dichiarazioni, Wasm, WAT e query sulla mappa sorgente per utensili e dispositivi.

#### Parametri

| Nome    | Digitare           | Descrizione |
| ------- | ------------------ | ----------- |
| opzioni | FlintPluginOptions |             |

### flintPlugin

**Tipo:** funzione

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installare la compilazione di Forge Web Script e le query sugli artefatti virtuali in Vite.
Il plugin preserva l'ABI scalare dell'esportazione Wasm mentre espone manifest,
dichiarazioni, Wasm, WAT e query sulla mappa sorgente per utensili e dispositivi.

#### Parametri

| Nome    | Digitare           | Descrizione |
| ------- | ------------------ | ----------- |
| opzioni | FlintPluginOptions |             |

### FlintViteError

**Gentile:** lezione

```typescript
export class FlintViteError extends Error
```

Diagnostica rivolta a Vite che preserva il codice del compilatore, il percorso di origine e la fase.
