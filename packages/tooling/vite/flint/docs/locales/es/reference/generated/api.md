# Referencia de API @mission-platform/vite-plugin-flint

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/tooling/vite/flint/docs/reference/generated/api.md: [packages/tooling/vite/flint/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/vite-plugin-flint`.

## `src/compile`

### compilarFlintFile

**Tipo:** función

```typescript
function compileFlintFile(
  fileName: string,
  options: FlintPluginOptions,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): FlintCompiledModule;
```

Compile un archivo FWS y devuelva su artefacto más los metadatos del mapa fuente.

#### Parámetros

| Nombre            | Tipo                           | Descripción |
| ----------------- | ------------------------------ | ----------- |
| nombre de archivo | cadena                         |             |
| opciones          | Opciones del complemento Flint |             |
| servicio          | Servicio FlintCompiler         |             |

### compilarFlintGraph

**Tipo:** función

```typescript
function compileFlintGraph(
  fileName: string,
  options: FlintPluginOptions,
  resolver: FlintModuleResolver,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): Promise<FlintCompiledModule>;
```

Resuelva, vincule y compile un gráfico de módulo FWS importado.

#### Parámetros

| Nombre            | Tipo                           | Descripción |
| ----------------- | ------------------------------ | ----------- |
| nombre de archivo | cadena                         |             |
| opciones          | Opciones del complemento Flint |             |
| resolver          | FlintModuleResolver            |             |
| servicio          | Servicio FlintCompiler         |             |

### FlintMódulo compilado

**Tipo:** interfaz

```typescript
export interface FlintCompiledModule
```

Fuente compilada, metadatos de artefactos, mapa fuente e informes gráficos opcionales.

### Opciones del complemento Flint

**Tipo:** interfaz

```typescript
export interface FlintPluginOptions
```

No se proporciona descripción.

### resolverFlintPath

**Tipo:** función

```typescript
function resolveFlintPath(root: string, value: string): string;
```

Resuelva la ruta de un complemento relativa a la raíz a menos que el valor ya sea absoluto.

#### Parámetros

| Nombre | Tipo   | Descripción |
| ------ | ------ | ----------- |
| raíz   | cadena |             |
| valor  | cadena |             |

## `src/generate`

### createFlintDeclarationsSource

**Tipo:** función

```typescript
function createFlintDeclarationsSource(compiled: FlintCompiledModule): string;
```

Genere un módulo que contenga declaraciones, metadatos de enlaces y opciones
Metadatos del compilador autohospedado para consumidores de accesorios y herramientas.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### crearFlintManifestSource

**Tipo:** función

```typescript
function createFlintManifestSource(compiled: FlintCompiledModule): string;
```

Genere un módulo que contenga el manifiesto ABI serializable en JSON.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### crearFlintModuleSource

**Tipo:** función

```typescript
function createFlintModuleSource(compiled: FlintCompiledModule): string;
```

Genere el módulo de consumidor normal. El módulo generado reexporta el
funciones compiladas y expone el mismo manifiesto ABI a través de `abiManifest`.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### crearFlintSourceMapSource

**Tipo:** función

```typescript
function createFlintSourceMapSource(compiled: FlintCompiledModule): string;
```

Genere un módulo cuya exportación predeterminada sea el objeto de mapa fuente generado.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### crearFlintWasmSource

**Tipo:** función

```typescript
function createFlintWasmSource(compiled: FlintCompiledModule): string;
```

Genere un módulo cuya exportación predeterminada sea la matriz de bytes Wasm compilada.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### crearFlintWatSource

**Tipo:** función

```typescript
function createFlintWatSource(compiled: FlintCompiledModule): string;
```

Genere un módulo cuya exportación predeterminada sea el texto WAT compilado. esto es
destinado a la inspección y afirmaciones de conformidad, no a la ejecución de Wasm.

#### Parámetros

| Nombre    | Tipo                      | Descripción |
| --------- | ------------------------- | ----------- |
| compilado | Módulo compilado de Flint |             |

### FORGE_WEB_SCRIPT_DECLARACIONES_QUERY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_DECLARATIONS_QUERY;
```

Consulta que devuelve declaraciones vinculadas y metadatos de gráficos como un módulo ES.

### FORGE_WEB_SCRIPT_MANIFEST_QUERY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_MANIFEST_QUERY;
```

Consulta que devuelve el manifiesto ABI compilado como un módulo ES.

### FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY;
```

Consulta que devuelve el mapa fuente generado como exportación predeterminada del módulo.

### FORGE_WEB_SCRIPT_WASM_QUERY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_WASM_QUERY;
```

Consulta que devuelve los bytes Wasm compilados como exportación predeterminada del módulo.

### FORGE_WEB_SCRIPT_WAT_QUERY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_WAT_QUERY;
```

Consulta que devuelve el WAT generado, incluida la reducción de la tabla de ramas, como una cadena.

## `src/index`

### por defecto

**Tipo:** función

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Instale la compilación de Forge Web Script y las consultas de artefactos virtuales en Vite.
El complemento conserva la ABI de exportación escalar de Wasm mientras expone el manifiesto,
declaraciones, Wasm, WAT y consultas de mapas de origen para herramientas y accesorios.

#### Parámetros

| Nombre   | Tipo                           | Descripción |
| -------- | ------------------------------ | ----------- |
| opciones | Opciones del complemento Flint |             |

### flintPlugin

**Tipo:** función

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Instale la compilación de Forge Web Script y las consultas de artefactos virtuales en Vite.
El complemento conserva la ABI de exportación escalar de Wasm mientras expone el manifiesto,
declaraciones, Wasm, WAT y consultas de mapas de origen para herramientas y accesorios.

#### Parámetros

| Nombre   | Tipo                           | Descripción |
| -------- | ------------------------------ | ----------- |
| opciones | Opciones del complemento Flint |             |

### FlintViteError

**Tipo:** clase

```typescript
export class FlintViteError extends Error
```

Diagnóstico orientado a Vite que conserva el código del compilador, la ubicación de origen y la fase.
