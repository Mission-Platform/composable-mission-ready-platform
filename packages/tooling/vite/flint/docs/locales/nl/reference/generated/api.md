# @mission-platform/vite-plugin-flint API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> packages/tooling/vite/flint/docs/reference/generated/api.md: [packages/tooling/vite/flint/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/vite-plugin-flint`.

## `src/compile`

### compileFlintFile

**Soort:** functie

```typescript
function compileFlintFile(
  fileName: string,
  options: FlintPluginOptions,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): FlintCompiledModule;
```

Compileer één FLINT-bestand en retourneer het bijbehorende artefact plus metagegevens van de bronkaart.

#### Parameters

| Naam         | Typ                  | Beschrijving |
| ------------ | -------------------- | ------------ |
| bestandsnaam | tekenreeks           |              |
| opties       | FlintPluginOptions   |              |
| dienst       | FlintCompilerService |              |

### compileFlintGraph

**Soort:** functie

```typescript
function compileFlintGraph(
  fileName: string,
  options: FlintPluginOptions,
  resolver: FlintModuleResolver,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): Promise<FlintCompiledModule>;
```

Een geïmporteerde FLINT-modulegrafiek oplossen, koppelen en compileren.

#### Parameters

| Naam         | Typ                  | Beschrijving |
| ------------ | -------------------- | ------------ |
| bestandsnaam | tekenreeks           |              |
| opties       | FlintPluginOptions   |              |
| oplosser     | FlintModuleResolver  |              |
| dienst       | FlintCompilerService |              |

### FlintCompiledModule

**Soort:** interface

```typescript
export interface FlintCompiledModule
```

Gecompileerde bron, metadata van artefacten, bronkaart en optionele grafiekrapporten.

### FlintPluginOptions

**Soort:** interface

```typescript
export interface FlintPluginOptions
```

Geen beschrijving opgegeven.

### solveFlintPath

**Soort:** functie

```typescript
function resolveFlintPath(root: string, value: string): string;
```

Los het pad van een plug-in op ten opzichte van de root, tenzij de waarde al absoluut is.

#### Parameters

| Naam   | Typ        | Beschrijving |
| ------ | ---------- | ------------ |
| wortel | tekenreeks |              |
| waarde | tekenreeks |              |

## `src/generate`

### createFlintDeclarationsSource

**Soort:** functie

```typescript
function createFlintDeclarationsSource(compiled: FlintCompiledModule): string;
```

Genereer een module met declaraties, link-metagegevens en optioneel
zelfgehoste metadata van de compiler voor consumenten van armatuur en tooling.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### createFlintManifestSource

**Soort:** functie

```typescript
function createFlintManifestSource(compiled: FlintCompiledModule): string;
```

Genereer een module met het JSON-serialiseerbare ABI-manifest.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### createFlintModuleSource

**Soort:** functie

```typescript
function createFlintModuleSource(compiled: FlintCompiledModule): string;
```

Genereer de normale consumentenmodule. De gegenereerde module exporteert het
gecompileerde functies en geeft hetzelfde ABI-manifest weer via `abiManifest`.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### createFlintSourceMapSource

**Soort:** functie

```typescript
function createFlintSourceMapSource(compiled: FlintCompiledModule): string;
```

Genereer een module waarvan de standaardexport het gegenereerde bronkaartobject is.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### createFlintWasmSource

**Soort:** functie

```typescript
function createFlintWasmSource(compiled: FlintCompiledModule): string;
```

Genereer een module waarvan de standaardexport de gecompileerde Wasm-bytearray is.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### createFlintWatSource

**Soort:** functie

```typescript
function createFlintWatSource(compiled: FlintCompiledModule): string;
```

Genereer een module waarvan de standaardexport de gecompileerde WAT-tekst is. Dit is
bedoeld voor inspectie en conformiteitsverklaringen, niet voor Wasm-uitvoering.

#### Parameters

| Naam         | Typ                 | Beschrijving |
| ------------ | ------------------- | ------------ |
| samengesteld | FlintCompiledModule |              |

### FORGE_WEB_SCRIPT_DECLARATIONS_QUERY

**Soort:** constant

```typescript
export const FORGE_WEB_SCRIPT_DECLARATIONS_QUERY;
```

Query die gekoppelde declaraties en grafiekmetagegevens retourneert als een ES-module.

### FORGE_WEB_SCRIPT_MANIFEST_QUERY

**Soort:** constant

```typescript
export const FORGE_WEB_SCRIPT_MANIFEST_QUERY;
```

Query die het gecompileerde ABI-manifest retourneert als een ES-module.

### FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY

**Soort:** constant

```typescript
export const FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY;
```

Query die de gegenereerde bronkaart retourneert als de standaardexport van de module.

### FORGE_WEB_SCRIPT_WASM_QUERY

**Soort:** constant

```typescript
export const FORGE_WEB_SCRIPT_WASM_QUERY;
```

Query die de gecompileerde Wasm-bytes retourneert als de standaardexport van de module.

### FORGE_WEB_SCRIPT_WAT_QUERY

**Soort:** constant

```typescript
export const FORGE_WEB_SCRIPT_WAT_QUERY;
```

Query die de gegenereerde WAT retourneert, inclusief het verlagen van de vertakkingstabel, als een tekenreeks.

## `src/index`

### standaard

**Soort:** functie

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installeer Forge Web Script-compilatie en virtuele artefactquery's in Vite.
De plug-in behoudt de scalaire Wasm-export-ABI terwijl het manifest zichtbaar wordt,
declaraties, Wasm-, WAT- en bronkaartquery's voor gereedschappen en armaturen.

#### Parameters

| Naam   | Typ                | Beschrijving |
| ------ | ------------------ | ------------ |
| opties | FlintPluginOptions |              |

### flintPlugin

**Soort:** functie

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installeer Forge Web Script-compilatie en virtuele artefactquery's in Vite.
De plug-in behoudt de scalaire Wasm-export-ABI terwijl het manifest zichtbaar wordt,
declaraties, Wasm-, WAT- en bronkaartquery's voor gereedschappen en armaturen.

#### Parameters

| Naam   | Typ                | Beschrijving |
| ------ | ------------------ | ------------ |
| opties | FlintPluginOptions |              |

### FlintViteError

**Soort:** klasse

```typescript
export class FlintViteError extends Error
```

Vite-gerichte diagnose die de compilercode, bronlocatie en fase behoudt.
