# @mission-platform/vite-plugin-flint API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> packages/tooling/vite/flint/docs/reference/generated/api.md: [packages/tooling/vite/flint/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/vite-plugin-flint`.

## `src/compile`

### compileFlintFile

**Art:** Funktion

```typescript
function compileFlintFile(
  fileName: string,
  options: FlintPluginOptions,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): FlintCompiledModule;
```

Kompilieren Sie eine FLINT-Datei und geben Sie deren Artefakt sowie Quellzuordnungsmetadaten zurück.

#### Parameter

| Name      | Geben Sie            | ein Beschreibung |
| --------- | -------------------- | ---------------- |
| Dateiname | Zeichenfolge         |                  |
| Optionen  | FlintPluginOptions   |                  |
| Dienst    | FlintCompilerService |                  |

### compileFlintGraph

**Art:** Funktion

```typescript
function compileFlintGraph(
  fileName: string,
  options: FlintPluginOptions,
  resolver: FlintModuleResolver,
  service: FlintCompilerService = options.compilerService ??
    createFlintCompilerService({
      selfHostedRunner: runFlintSelfHostedLexStage,
      selfHostedVmMode: options.selfHostedVmMode,
    }),
): Promise<FlintCompiledModule>;
```

Lösen, verknüpfen und kompilieren Sie ein importiertes FLINT-Moduldiagramm.

#### Parameter

| Name      | Geben Sie            | ein Beschreibung |
| --------- | -------------------- | ---------------- |
| Dateiname | Zeichenfolge         |                  |
| Optionen  | FlintPluginOptions   |                  |
| Resolver  | FlintModuleResolver  |                  |
| Dienst    | FlintCompilerService |                  |

### FlintCompiledModule

**Art:** Schnittstelle

```typescript
export interface FlintCompiledModule
```

Kompilierte Quelle, Artefaktmetadaten, Quellkarte und optionale Diagrammberichte.

### FlintPluginOptions

**Art:** Schnittstelle

```typescript
export interface FlintPluginOptions
```

Keine Beschreibung angegeben.

### „resolveFlintPath“.

**Art:** Funktion

```typescript
function resolveFlintPath(root: string, value: string): string;
```

Lösen Sie einen Plugin-Pfad relativ zum Stammverzeichnis auf, es sei denn, der Wert ist bereits absolut.

#### Parameter

| Name   | Geben Sie    | ein Beschreibung |
| ------ | ------------ | ---------------- |
| Wurzel | Zeichenfolge |                  |
| Wert   | Zeichenfolge |                  |

## `src/generate`

### createFlintDeclarationsSource

**Art:** Funktion

```typescript
function createFlintDeclarationsSource(compiled: FlintCompiledModule): string;
```

Generieren Sie ein Modul, das Deklarationen, Linkmetadaten und optionale Elemente enthält
Selbstgehostete Compiler-Metadaten für Verbraucher von Vorrichtungen und Werkzeugen.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### createFlintManifestSource

**Art:** Funktion

```typescript
function createFlintManifestSource(compiled: FlintCompiledModule): string;
```

Generieren Sie ein Modul, das das JSON-serialisierbare ABI-Manifest enthält.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### createFlintModuleSource

**Art:** Funktion

```typescript
function createFlintModuleSource(compiled: FlintCompiledModule): string;
```

Generieren Sie das normale Verbrauchermodul. Das generierte Modul exportiert die erneut
kompilierte Funktionen und stellt das gleiche ABI-Manifest über `abiManifest` bereit.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### createFlintSourceMapSource

**Art:** Funktion

```typescript
function createFlintSourceMapSource(compiled: FlintCompiledModule): string;
```

Generieren Sie ein Modul, dessen Standardexport das generierte Quellzuordnungsobjekt ist.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### createFlintWasmSource

**Art:** Funktion

```typescript
function createFlintWasmSource(compiled: FlintCompiledModule): string;
```

Generieren Sie ein Modul, dessen Standardexport das kompilierte Wasm-Byte-Array ist.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### createFlintWatSource

**Art:** Funktion

```typescript
function createFlintWatSource(compiled: FlintCompiledModule): string;
```

Generieren Sie ein Modul, dessen Standardexport der kompilierte WAT-Text ist. Das ist
ist für Inspektions- und Konformitätsbehauptungen gedacht, nicht für die Wasm-Ausführung.

#### Parameter

| Name             | Geben Sie           | ein Beschreibung |
| ---------------- | ------------------- | ---------------- |
| zusammengestellt | FlintCompiledModule |                  |

### FORGE_WEB_SCRIPT_DECLARATIONS_QUERY

**Art:** konstant

```typescript
export const FORGE_WEB_SCRIPT_DECLARATIONS_QUERY;
```

Abfrage, die verknüpfte Deklarationen und Diagrammmetadaten als ES-Modul zurückgibt.

### FORGE_WEB_SCRIPT_MANIFEST_QUERY

**Art:** konstant

```typescript
export const FORGE_WEB_SCRIPT_MANIFEST_QUERY;
```

Abfrage, die das kompilierte ABI-Manifest als ES-Modul zurückgibt.

### FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY

**Art:** konstant

```typescript
export const FORGE_WEB_SCRIPT_SOURCE_MAP_QUERY;
```

Abfrage, die die generierte Quellkarte als Standardexport des Moduls zurückgibt.

### FORGE_WEB_SCRIPT_WASM_QUERY

**Art:** konstant

```typescript
export const FORGE_WEB_SCRIPT_WASM_QUERY;
```

Abfrage, die die kompilierten Wasm-Bytes als Standardexport des Moduls zurückgibt.

### FORGE_WEB_SCRIPT_WAT_QUERY

**Art:** konstant

```typescript
export const FORGE_WEB_SCRIPT_WAT_QUERY;
```

Abfrage, die den generierten WAT, einschließlich der Verzweigungstabellenabsenkung, als Zeichenfolge zurückgibt.

## `src/index`

### Standard

**Art:** Funktion

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installieren Sie die Kompilierung von Forge-Webskripten und Abfragen virtueller Artefakte in Vite.
Das Plugin behält die skalare Wasm-Export-ABI bei, während das Manifest verfügbar gemacht wird.
Deklarationen, Wasm, WAT und Quellkartenabfragen für Werkzeuge und Vorrichtungen.

#### Parameter

| Name     | Geben Sie          | ein Beschreibung |
| -------- | ------------------ | ---------------- |
| Optionen | FlintPluginOptions |                  |

### flintPlugin

**Art:** Funktion

```typescript
function flintPlugin(options: FlintPluginOptions = {}): Plugin;
```

Installieren Sie die Kompilierung von Forge-Webskripten und Abfragen virtueller Artefakte in Vite.
Das Plugin behält die skalare Wasm-Export-ABI bei, während das Manifest verfügbar gemacht wird.
Deklarationen, Wasm, WAT und Quellkartenabfragen für Werkzeuge und Vorrichtungen.

#### Parameter

| Name     | Geben Sie          | ein Beschreibung |
| -------- | ------------------ | ---------------- |
| Optionen | FlintPluginOptions |                  |

### FlintViteError

**Art:** Klasse

```typescript
export class FlintViteError extends Error
```

Vite-bezogene Diagnose, die den Compilercode, den Quellspeicherort und die Phase beibehält.
