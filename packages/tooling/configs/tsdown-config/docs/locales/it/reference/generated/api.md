# Riferimento API @mission-platform/tsdown-config

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/tooling/configs/tsdown-config/docs/reference/generated/api.md: [packages/tooling/configs/tsdown-config/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/tsdown-config`.

## `src/index`

### createExternalMatcher

**Tipo:** funzione

```typescript
function createExternalMatcher(names: readonly string[]): (id: string) => boolean;
```

Crea un predicato Rolldown `external` / `deps.neverBundle` che tratti ogni
nome in `names` (e qualsiasi importazione di sottopercorsi, ad esempio `pkg/sub`) come esterno.

#### Parametri

| Nome | Digitare                  | Descrizione |
| ---- | ------------------------- | ----------- |
| nomi | stringa di sola lettura[] |             |

### DEFAULT_LIBRARY_EXTERNALS

**Genere:** costante

```typescript
export const DEFAULT_LIBRARY_EXTERNALS: readonly string[];
```

Nomi di pacchetto predefiniti che ogni libreria condivisa dovrebbe considerare come forniti da peer.
Esegue il mirroring di {@link DEFAULT_LIBRARY_EXTERNALS} di `@mission-platform/vite-config`.

### defineTsdownLibrary

**Tipo:** funzione

```typescript
function defineTsdownLibrary(options: TsdownLibraryOptions): UserConfig;
```

Crea una configurazione tsdown per una semplice libreria TypeScript (o bundle singolo) —
Archetipo A/B. Esegue il mirroring della semantica di esternalizzazione `defineLibraryConfig` da
`@mission-platform/vite-config`.

#### Parametri

| Nome    | Digitare             | Descrizione |
| ------- | -------------------- | ----------- |
| opzioni | TsdownLibraryOptions |             |

### defineTsdownVueLibrary

**Tipo:** funzione

```typescript
function defineTsdownVueLibrary(options: TsdownLibraryOptions): UserConfig;
```

Come {@link defineTsdownLibrary}, ma collega `unplugin-vue` e dts compatibili con Vue
per i pacchetti che spediscono SFC `.vue` (`i18n`, `router`, …).

#### Parametri

| Nome    | Digitare             | Descrizione |
| ------- | -------------------- | ----------- |
| opzioni | TsdownLibraryOptions |             |

### leggerePackageDependencyNames

**Tipo:** funzione

```typescript
function readPackageDependencyNames(rootDirectory: string): string[];
```

Leggi `dependencies` e `peerDependencies` dichiarati nel package.json
situato in `rootDirectory`. Utilizzato per mantenere lontane le dipendenze di runtime di una libreria
del proprio pacchetto in modo che i consumatori possano deduplicarli e scuoterli.

#### Parametri

| Nome          | Digitare | Descrizione |
| ------------- | -------- | ----------- |
| rootDirectory | stringa  |             |

### risolvereTsdownOutputDirectory

**Tipo:** funzione

```typescript
function resolveTsdownOutputDirectory(rootDirectory: string, outputDirectory: string, outputRoot?: string): string;
```

Esegui il mirroring del percorso di output del pacchetto finale in una radice di build isolata. Mantenere il
il suffisso relativo al pacchetto rende gli output neutri, framework, email e CMS
promozionabile in modo indipendente senza consentire a tsdown di pulire un albero fratello.

#### Parametri

| Nome            | Digitare | Descrizione |
| --------------- | -------- | ----------- |
| rootDirectory   | stringa  |             |
| outputDirectory | stringa  |             |
| outputRoot      | stringa  |             |

### TsdownLibraryOptions

**Tipo:** interfaccia

```typescript
export interface TsdownLibraryOptions
```

Nessuna descrizione fornita.
