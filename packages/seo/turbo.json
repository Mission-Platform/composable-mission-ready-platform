{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["build:check", "build:bundle"]
    },
    "build:bundle": {
      "dependsOn": ["^build", "build:check"]
    }
  }
}
