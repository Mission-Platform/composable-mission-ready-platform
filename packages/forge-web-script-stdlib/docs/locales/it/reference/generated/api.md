# Riferimento API @mission-platform/forge-web-script-stdlib

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> packages/forge-web-script-stdlib/docs/reference/generated/api.md: [packages/forge-web-script-stdlib/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/forge-web-script-stdlib`.

## `fws/option`

### è_qualche

**Tipo:** fws-export

```typescript
fn is_some(value: Option<T>) -> bool
```

Restituisce vero quando l'opzione contiene un valore.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| valore | Opzione<T> | Valore dell'opzione da controllare. |

#### Contrarre

- **@param:** valore Valore dell'opzione da controllare.
- **@restituisce:** `true` per `Some`, altrimenti `false`.

### Opzione

**Tipo:** fws-export

```typescript
enum Option
```

Contenitore di valori facoltativo utilizzato nei pacchetti Forge Web Script.

`None` rappresenta l'assenza di un valore. `Some(T)` trasporta un valore attuale di tipo `T`. Preferire `match` rispetto ai controlli booleani quando entrambi i rami necessitano del payload.

## `fws/result`

### va_ok

**Tipo:** fws-export

```typescript
fn is_ok(value: Result<T, E>) -> bool
```

Restituisce vero quando il risultato ha esito positivo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| valore | Risultato<T, E> | Valore del risultato da controllare. |

#### Contrarre

- **@param:** valore Valore del risultato da controllare.
- **@restituisce:** `true` per `Ok`, altrimenti `false`.

### Risultato

**Tipo:** fws-export

```typescript
enum Result
```

Risultato di un'operazione fallibile.

`Ok(T)` porta un valore corretto. `Error(E)` presenta un errore reversibile. Utilizzare `match` per gestire entrambe le varianti senza eccezioni.

## `src/index`

### FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE
```

Percorso relativo al modulo di dichiarazione asincrona utilizzato dai consumatori del compilatore.

### FORGE_WEB_SCRIPT_STDLIB_IDENTITY

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_IDENTITY
```

Contratto stabile di identità e rappresentanza per la libreria standard.
I valori aggregati utilizzano layout monomorfizzati ove possibile e descrittore
limiti per i valori che attraversano l'ABI host/Wasm.

### FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT

**Genere:** costante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT
```

Directory contenente i moduli di dichiarazione Forge Web Script forniti.
