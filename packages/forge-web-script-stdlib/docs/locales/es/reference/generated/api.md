# Referencia de API @mission-platform/forge-web-script-stdlib

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> packages/forge-web-script-stdlib/docs/reference/generated/api.md: [packages/forge-web-script-stdlib/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/forge-web-script-stdlib`.

## `fws/option`

### es_algunos

**Tipo:** fws-export

```typescript
fn is_some(value: Option<T>) -> bool
```

Devuelve verdadero cuando la opción tiene un valor.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| valor | Opción<T> | Valor de opción a inspeccionar. |

#### Contrato

- **@param:** valor Valor de la opción a inspeccionar.
- **@returns:** `true` para `Some`, en caso contrario `false`.

### Opción

**Tipo:** fws-export

```typescript
enum Option
```

Contenedor de valores opcional utilizado en todos los paquetes de Forge Web Script.

`None` representa la ausencia de un valor. `Some(T)` lleva un valor presente de tipo `T`. Prefiera `match` a las comprobaciones booleanas cuando ambas ramas necesiten la carga útil.

## `fws/result`

### esta_bien

**Tipo:** fws-export

```typescript
fn is_ok(value: Result<T, E>) -> bool
```

Devuelve verdadero cuando el resultado es exitoso.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| valor | Resultado<T, E> | Valor del resultado a inspeccionar. |

#### Contrato

- **@param:** valor Valor del resultado a inspeccionar.
- **@returns:** `true` para `Ok`, en caso contrario `false`.

### Resultado

**Tipo:** fws-export

```typescript
enum Result
```

Resultado de una operación falible.

`Ok(T)` lleva un valor exitoso. `Error(E)` conlleva un error recuperable. Utilice `match` para manejar ambas variantes sin excepciones.

## `src/index`

### FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE
```

Ruta relativa al módulo de declaración asíncrona utilizado por los consumidores del compilador.

### FORGE_WEB_SCRIPT_STDLIB_IDENTITY

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_IDENTITY
```

Contrato estable de identidad y representación de la biblioteca estándar.
Los valores agregados utilizan diseños monomorfizados siempre que sea posible y descriptores.
límites para los valores que cruzan la ABI del host/Wasm.

### FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT

**Tipo:** constante

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT
```

Directorio que contiene los módulos de declaración de Forge Web Script enviados.
