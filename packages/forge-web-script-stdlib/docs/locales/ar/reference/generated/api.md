# مرجع @mission-platform/forge-web-script-stdlib API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> packages/forge-web-script-stdlib/docs/reference/generated/api.md: [packages/forge-web-script-stdlib/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-web-script-stdlib`.

## `fws/option`

### is_some

**النوع:** fws-export

```typescript
fn is_some(value: Option<T>) -> bool
```

يعود صحيحًا عندما يحمل الخيار قيمة.

#### حدود

| الاسم  | اكتب      | الوصف              |
| ------ | --------- | ------------------ |
| القيمة | الخيار<T> | قيمة الخيار للفحص. |

#### عقد

- **@param:** القيمة قيمة الخيار المراد فحصها.
- **@returns:** `true` لـ `Some`، وإلا `false`.

### خيار

**النوع:** fws-export

```typescript
enum Option
```

حاوية قيمة اختيارية تُستخدم عبر حزم Forge Web Script.

يمثل `None` غياب القيمة. يحمل `Some(T)` قيمة حالية من النوع `T`. تفضل `match` على عمليات التحقق المنطقية عندما يحتاج كلا الفرعين إلى الحمولة.

## `fws/result`

### is_ok

**النوع:** fws-export

```typescript
fn is_ok(value: Result<T, E>) -> bool
```

يُرجع صحيحًا عندما تكون النتيجة ناجحة.

#### حدود

| الاسم  | اكتب          | الوصف               |
| ------ | ------------- | ------------------- |
| القيمة | النتيجة<T, E> | قيمة النتيجة للفحص. |

#### عقد

- **@param:** القيمة قيمة النتيجة المطلوب فحصها.
- **@returns:** `true` لـ `Ok`، وإلا `false`.

### نتيجة

**النوع:** fws-export

```typescript
enum Result
```

نتيجة عملية غير معصومة.

يحمل `Ok(T)` قيمة ناجحة. يحمل `Error(E)` فشلاً قابلاً للاسترداد. استخدم `match` للتعامل مع كلا المتغيرين بدون استثناءات.

## `src/index`

### FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_ASYNC_SOURCE;
```

المسار النسبي لوحدة الإعلان غير المتزامن التي يستخدمها مستهلكو المترجم.

### FORGE_WEB_SCRIPT_STDLIB_IDENTITY

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_IDENTITY;
```

عقد هوية وتمثيل ثابت للمكتبة القياسية.
تستخدم القيم المجمعة تخطيطات أحادية الشكل حيثما أمكن ذلك وموصفًا
حدود القيم التي تعبر المضيف/Wasm ABI.

### FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT

**النوع:** ثابت

```typescript
export const FORGE_WEB_SCRIPT_STDLIB_SOURCE_ROOT;
```

الدليل الذي يحتوي على وحدات إعلان Forge Web Script التي تم شحنها.
