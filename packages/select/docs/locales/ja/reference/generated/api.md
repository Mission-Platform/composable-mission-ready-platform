# @mission-platform/select API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/select/docs/reference/generated/api.md: [packages/select/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/select` のパブリック ソース宣言から生成されます。

## `src/components/atoms/forge-tag/forge-tag`

### フォージタグ

**種類:** 関数

```typescript
function ForgeTag(properties: Readonly<TagProperties>): MpElement
```

`ForgeTag` — ニュートラルな JSX 方言で一度作成された、コンパクトで丸みを帯びたラベル
そして、`@mission-platform/vite-plugin-forge` によって React または Vue に直接コンパイルされます。

`label` を (合成されたニュートラル {@link ForgeTypography} 経由) でレンダリングします。
トーン/サイズ、および — `disabled` ではなく `removable` の場合 — 削除ボタン
`onRemove` コールバックを起動します。同じ場所に配置された CSS を通じてスタイルを所有します。
モジュール `forge-tag.module.scss`、フレームワーク中立でアセンブル
{@link classNames} ヘルパー。

削除ボタンは、追記型 `@mission-platform/icons` をレンダリングします。
`ForgeIconClose` (React/Vue にコンパイルされます) およびクロスフレームワークを起動します
callback-prop `onRemove` (Vue `remove` は代替を発行します)。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プロパティ |読み取り専用<TagProperties> |  |

### タグのプロパティ

**種類:** インターフェース

```typescript
export interface TagProperties
```

説明はありません。

### タグサイズ

**種類：**タイプ

```typescript
export type TagSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規の2xs→2xlサイズスケール。

### タグバリアント

**種類：**タイプ

```typescript
export type TagVariant = 'neutral' | 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'info' | 'error' | 'critical';
```

タグのトーン。

## `src/components/molecules/forge-language-switcher/forge-language-switcher`

### Forge言語スイッチャー

**種類:** 関数

```typescript
function ForgeLanguageSwitcher(properties: Readonly<ForgeLanguageSwitcherProperties>): MpElement
```

説明はありません。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プロパティ |読み取り専用<ForgeLanguageSwitcherProperties> |  |

### ForgeLanguageSwitcherオプション

**種類:** インターフェース

```typescript
export interface ForgeLanguageSwitcherOption
```

説明はありません。

### ForgeLanguageSwitcherプロパティ

**種類:** インターフェース

```typescript
export interface ForgeLanguageSwitcherProperties
```

説明はありません。

## `src/components/molecules/forge-multiselect/forge-multiselect`

### ForgeMultiselect

**種類:** 関数

```typescript
function ForgeMultiselect(properties: Readonly<MultiselectProperties>): MpElement
```

`ForgeMultiselect` — 検索フィールドを備えたタグベースの複数選択コンボボックス、
ニュートラルな JSX 方言で一度作成され、React に直接コンパイルされるか、
`@mission-platform/vite-plugin-forge` による Vue。

選択した値は、取り外し可能な {@link ForgeTag} チップとしてレンダリングされます。インライン検索
input は、リストボックスに表示される残りのオプションをフィルターします。選択は
確立された `modelValue` (`(string | number)[]`) で制御 +
`onUpdateModelValue`/`onChange` コールバックプロップ規約、および視覚的な
非表示のネイティブ `<select multiple>` により、自動入力/ネイティブ送信が機能し続けます。
同じ場所に配置された CSS モジュールを通じてスタイルを所有します。
`forge-multiselect.module.scss`。

フィルタリングされたリストボックスは、ライトワンス **`ForgeDropdown`** を通じてレンダリングされます。
(それ自体は React/Vue にコンパイルされます)、ニュートラルな名前付きスロット **passing** 経由で構成されます
構文: タグ/検索コンボボックスはドロップダウンの `trigger` スロットに渡されます。
(`slot="trigger"`)、`<ul role="listbox">` がデフォルトのスロットになります。
パネルは `<Teleport>` を通じてポータル化され、CSS アンカーで固定されます
インプレースリストの代わりに配置します。内部 `useState` オープン フラグは次のとおりです。
`onUpdateOpen` コールバックを介してドロップダウンとの同期が維持されます。

オリジナルの Vue SFC からのその他の置換: 検索クエリは
内部 `useState` 文字列。 `useId` コンポーザブルは、
フレームワークネイティブの `useId` フック。山形は追記型 `@mission-platform/icons` です
`ForgeIconChevron` (`direction` プロパティを介して回転され、それ自体は次のようにコンパイルされます)
React/Vue); `useI18n` 文字列はプレーン テキストになります。そして `v-model` + は
コールバック小道具になります。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プロパティ |読み取り専用<MultiselectProperties> |  |

### 複数選択オプション

**種類:** インターフェース

```typescript
export interface MultiselectOption
```

単一の選択可能なオプション。

### 複数選択プロパティ

**種類:** インターフェース

```typescript
export interface MultiselectProperties
```

説明はありません。

### 複数選択サイズ

**種類：**タイプ

```typescript
export type MultiselectSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規のコントロール サイズ スケール。

## `src/components/molecules/forge-select/forge-select`

### フォージセレクト

**種類:** 関数

```typescript
function ForgeSelect(properties: Readonly<SelectProperties>): MpElement
```

`ForgeSelect` — ニュートラル JSX で一度作成されたカスタム コンボボックス/選択
方言を使用し、React または Vue に直接コンパイルされます。
`@mission-platform/vite-plugin-forge`。

デフォルト (`searchable`、デフォルト) では、トリガーはフィルタリングするテキストフィールドです。
ユーザーが入力したオプション — {@link ForgeMultiselect} をミラーリング — したがって、値
スクロールするのではなく、検索することで見つけることができます。 `searchable={false}` を a に設定します
プレーンなボタントリガー。いずれにしても、リストボックスは視覚的に隠されたものによって裏付けられています。
ネイティブ `<select>` なので、ブラウザーの自動入力とネイティブ フォームの送信は機能し続けます。
選択は確立された `modelValue` + によって制御されます
`onUpdateModelValue`/`onChange` コールバックプロパティ規則。独自のスタイルを持っています
同じ場所にある CSS モジュール `forge-select.module.scss` を通じて。

フローティング リストボックスは、追記型 **`ForgeDropdown`** によってレンダリングされます。
(それ自体は React/Vue にコンパイルされます)、ニュートラルな名前付きスロット **passing** 経由で構成されます
構文: コンボボックス ラッパーはドロップダウンの `trigger` スロットに渡されます。
(`slot="trigger"`)、`<ul role="listbox">` がデフォルトのスロットになるため、
パネルは `<Teleport>` を通じてポータル化され、CSS アンカーで固定されます
インプレース絶対配置リストの代わりに配置します。内部
`useState` オープン フラグは、そのドロップダウンを介してドロップダウンと同期されます。
`onUpdateOpen` コールバック。

オリジナルの Vue SFC からのその他の置換: `useId` コンポーザブル マップは、
フレームワークネイティブの `useId` フック。シェブロンはライトワンスです
`@mission-platform/icons` `ForgeIconChevron` (`direction` プロップを介して回転、
それ自体は React/Vue にコンパイルされます)。 `useI18n` 文字列はプレーン テキストになります。そして
`v-model` + はコールバック プロパティになります。 `start`/`end` 名前付きスロットは次のとおりです。
中立的な名前付きスロットとして保存されます。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プロパティ |読み取り専用<SelectProperties> |  |

### オプションの選択

**種類:** インターフェース

```typescript
export interface SelectOption
```

単一の選択可能なオプション。

### プロパティの選択

**種類:** インターフェース

```typescript
export interface SelectProperties
```

説明はありません。

### サイズの選択

**種類：**タイプ

```typescript
export type SelectSize = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';
```

正規のコントロール サイズ スケール。
