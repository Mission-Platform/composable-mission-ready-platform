# Forge Web スクリプト v1

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> packages/forge-web-script/docs/reference/language.md: [packages/forge-web-script/docs/reference/language.md](../../../reference/language.md)
> 言語: 日本語 (ja)

Forge Web Script (`.fws`) は、WebAssembly 用の小型の汎用言語です。
作業量。 Web ファーストであり、機能ベースであり、意図的に独立しています。
Vue、React、DOM、および Forge コンポーネント コンパイラ。この文書は、
権威ある v1 言語とモジュール契約。 `@mission-platform/forge-web-script`
解析、型チェック、グラフ/リンクのためのブラウザーセーフな互換性ファサードです
Vite アダプターによって使用される解像度、マニフェスト データ、およびコンパイラー サービス API
そしてLSP。 `@mission-platform/forge-web-script-wasm` は決定論的なバックエンドです
これにより、チェックされた IR が検証された WebAssembly と WAT に下がります。 Node のみ
`@mission-platform/forge-web-script-cli` パッケージは `forge-web-script` を提供します
ファイルまたはソース グラフをチェックしてコンパイルするためのコマンド。 TypeScript
パッケージには、実行可能な適合フィクスチャも含まれています。

## ステータスとバージョン管理

現在の契約は **言語バージョン `1.0`** および **論理 ABI バージョンです
`1.2`**。言語バージョンはソースとセマンティクスを説明します。 ABI バージョン
WebAssembly 境界とホスト プロトコルについて説明します。バージョン管理されています
独立して。コンパイラは、生成されたすべてのモジュールに両方のバージョンを書き込む必要があります。
マニフェストを作成し、ローダーはインスタンス化の前に両方を検証する必要があります。 ABI `1.2` は、
メモリ契約の破壊的リビジョン: `memory` マニフェストで宣言する必要があります
`allocatorExport: "fws_alloc"`、`deallocatorExport: "fws_dealloc"`、および
`reallocatorExport: "fws_realloc"`、`fws_reset` は
モジュールエクスポートセット。ローダーは古いまたは不完全なマニフェストとモジュールを拒否します
不足しているリアロケータを黙って仮定するのではなく、

ソース形式は、`.fws` 拡張子が付いた UTF-8 テキストです。ソースファイルとは、
ファイル定義モジュール。その ID は正規化された Vite ファイル ID から導出されます。
(またはワークスペースの相対パス)。コンパイラ入力は言語バージョンを識別しますが、
生成されたマニフェストは、ローダーによって消費される永続化されたバージョン マーカーです。未来
リビジョンではソース プラグマが追加される場合がありますが、v1 ではソース プラグマは必要ありません。 v1 コンパイラ
理解できないソース構成を推測するのではなく拒否する必要があります。
バージョン。

## ソース分析とリリースポリシー

コア パッケージは、コンパイラ、言語用の 1 つの分析コントラクトを公開します。
サービス、CLI、および MCP の統合。 `analyzeForgeWebScript` はチェックされたものを受け入れます
フロントエンドの結果とオプションの登録済みルールから、事実、調査結果、および
コンパイラの残りの部分で使用されるのと同じ安定した診断。分析コンテキスト
ソース ファイル、オプションのソース マップ エントリ、生の最適化された IR、
ABI マニフェスト、グラフ/リンク メタデータ、ターゲット プロファイル、および正規化されたポリシー。

分析結果には安定した `FWS-ANALYSIS-*` コードが使用されており、次のカテゴリが含まれています。
重大度、UTF-16 互換のソース スパン、証拠、修復ヒント、および
オプションの OWASP/CWE リファレンス。彼らの診断には `phase: "analysis"` と
既存の `FWS-LEX-*`、`FWS-PARSE-*`、を変更せずにセキュリティ メタデータを作成する
`FWS-TYPE-*`、または `FWS-ABI-*` 診断。

コンパイルではデフォルトで厳密なプロファイルが使用されます。厳密モードでは、エラー重大度
検出結果 (または明示的に `blocking` とマークされた検出結果) は、Wasm および ESM の出力を妨げます。
完全なレポートは、返されたアーティファクトで引き続き利用できます。開発
プロファイルは編集者および調査ワークフローを対象としており、結果を報告します。
ただし、リリースゲートとしては使用しません。ポリシーには明示的な機能が含まれています
検出結果、呼び出しの深さ、ループ、割り当て、非同期の許可リストと制限
タスク、および正規表現入力。

コンパイラ サービスのキャッシュ キーには、正規化された分析ポリシーが含まれており、登録されています。
ルール識別子、およびソースマップ入力。これらの分析入力のいずれかを変更する
したがって、別のポリシーに基づいて生成されたアーティファクトを再利用することはできません。

## 例外のない結果と構造化された制御フロー

Forge Web Script は、標準ライブラリを使用して回復可能な結果を表現します
`Option<T>` および `Result<T, E>` 列挙型。すべてのバリアントを処理するには、`match` を使用します。
ソースレベルの `throw`、`try`、および `catch` は実行可能な構造ではありません。の
構造化された `for`、`while`、および `do while` フォームは、実行可能な v1 制御フローです。
これらは例外や反復子の構成要素ではありません。 `Result` には、まさに
バリアント `Ok(T)` および `Error(E)`。

イテレータ関数は `iter fn` を使用し、`Iterator<T>` を返し、`yield` で一時停止します。

```fws
export iter fn forward(source: Iterator<i32>) -> Iterator<i32> {
  loop value = source.next() { yield value; }
}
```

コンパイラは、JavaScript 互換のメソッドを通じてイテレータのエクスポートを公開します。
`next()`アダプター。各呼び出しでは値として `{ value, done: false }` が返され、
完了時は `{ value: undefined, done: true }`。後続の通話は残ります
完了しました。 `Iterator<T>.next()` は `Option<T>` として型付けされるため、連鎖イテレータ
要素タイプと所有権契約を保持する必要があります。

## 最適化とターゲットプロファイル

リリースの最適化では、実績のあるイテレータのアンローリング、純粋な呼び出しのインライン化、
末尾呼び出し分析、および安全な条件付き折りたたみ。 `noinline` ディレクティブを使用する
関数の境界を表示しておかなければならない場合。機能のインポートとロギング
これらは観察可能な副作用であり、並べ替えられることはありません。対象の機能はオプトインです
入力をコンパイルし、ABI マニフェストとキャッシュ キーに記録されます。

```ts
const artifact = compileForgeWebScript({
  source,
  fileName: 'runtime.fws',
  compilerVersion: '1.0.0',
  optimization: 'release',
  targetFeatures: { simd: true, tailCall: true, memory64: true },
  compilerHints: { iteratorUnrollLimit: 4 },
});
```

共有メモリのアトミック出力に対しては、`threads` と `atomics` の両方を有効にする必要があります。
サポートされていない組み合わせでは診断が行われます。 Memory64 マニフェストは `u64` を使用します
アドレスとポインター長 u64 の値。デバッグ モードでは、設定されたキャッシュが
永続的決定論的 `<key>.optimized.wat`、`<key>.unoptimized.wat`、
`<key>.optimized.wasm` および `<key>.unoptimized.wasm` アーティファクト。キャッシュ書き込み
追加的で、キャッシュが利用できないか、キャッシュに障害が発生しても、コンパイルは失敗しません。

## クロスプロジェクトリンクプロファイル

FWS は、プロジェクト間の依存関係管理のために 2 つのプライマリ リンク プロファイルをサポートしています。

- `linkProfile: "static"`: プロジェクト間のモジュールが単一にフラット化されます
  スキャナー グラフのアーティファクト。これにより、積極的な静的最適化が可能になります
  (`static-aggressive` プロファイル) でのランタイム モジュール ルックアップを排除します。
  アーティファクトのサイズのコスト。
- `linkProfile: "dynamic"`: 明示的なソース モジュール境界は保持されます。
  `ForgeWebScriptDynamicLinkCache` は、実行時にデコーダ モジュールを解決するために使用されます。
  アーティファクトとマニフェスト ID をキーとしたキャッシュされた関数アドレスを使用します。これ
  は、より安全な `dynamic-conservative` 最適化プロファイルを使用します。
  モジュール式ディストリビューション。

## 語彙参照

正規のチェックイン文法は次のとおりです。
[`src/grammar/forge-web-script.ebnf`](../../../../src/grammar/forge-web-script.ebnf)。
以下の語彙とパーサーの概要は、パブリック v1 コントラクトについて説明しています。の
EBNF アーティファクトは、実装の詳細があいまいな場合に信頼できます。

空白は文字列内を除いて重要ではありません。 `//` はコメントを開始します。
列の最後尾まで走ります。 `/*` は次のブロック コメントで終了するブロック コメントを開始します
`*/`;ブロックコメントは複数行にまたがる場合があります。コメントは雑学なので入力しないでください
文法。識別子は `A-Z`、`a-z`、または `_` で始まり、
それらの文字または 10 進数を続けてください。識別子は
大文字と小文字が区別されます。整数リテラルは、負でない 10 進数のシーケンスです。 v1 は行います
16 進数、8 進数、または浮動小数点リテラル構文は受け入れられません。
ブートストラップのサブセット。文字列では二重引用符と JSON 互換のエスケープのみを使用します。
`\\`、`\"`、`\/`、`\b`、`\f`、`\n`、`\r`、`\t`、および `\uXXXX` を正確に指定します。
4 桁の 16 進数。生の行末文字と無効なエスケープは字句的です
エラー。代わりに `\n` または `\r` を使用してください。文字列値は UTF-8 値です。

予約語は、`as`、`capability`、`case`、`catch`、`class`、
`constructor`、`default`、`do`、`else`、`enum`、`extends`、`export`、`for`、
`fn`、`if`、`impl`、`import`、`inline`、`interface`、`iter`、`let`、`likely`、
`loop`、`match`、`module`、`new`、`noinline`、`return`、`struct`、`switch`、
`throw`、`trait`、`try`、`unlikely`、`while`、および `yield`。 `true` および `false`
ブール値リテラルです。句読点は
`{ } ( ) [ ] : ; , | .`;演算子は
`! % * + - / < <= == != > >= && || = -> => ::`。

すべての診断スパンは、ハーフオープンソースオフセット範囲 `[start, end)` です。
元の UTF-16 TypeScript 文字列 (オフセットは UTF-16 コード単位をカウントします)、
1 ベースの行フィールドと列フィールド。の
ブートストラップ実装は、オフセットと行/列データを一緒にレポートします。
Vite アダプターは、再解析せずにソース マップされた診断を生成できます。

スキャナーはコメントを `comment` トークンとして保持するため、ドキュメントのコメントを
パーサーの決定はすべてのトリビアをスキップしますが、関数に関連付けられます。オペレーター
共有プレフィックスを持つものは最長一致によって選択されます。不正な入力の場合、
スキャナーは境界領域を消費し、安定した `FWS-LEX-*` 診断を発行し、
単一の EOF トークンに続きます。この回復動作は文法の一部です
契約。 TypeScript フロントエンドは、すべてのオフセットを UTF-16 コード単位で測定します。
自己ホスト型バイトステージは、
共有トークンコントラクト。

### 関数ドキュメントのコメント

開始区切り文字が `/**` であるブロック コメントはドキュメント コメントです。
これは、次の最上位の `fn` または `export fn` 宣言に添付されます。
空白と通常のコメントは、コメントと宣言の間に出現します。

```fws
/**
 * Adds one to a value.
 *
 * @param value The value to increment.
 * @return The incremented value.
 * @deprecated Use `increment` in new code.
 */
export fn add(value: i32) -> i32 {
  return value + 1;
}
```

機能のインポート、ソースのインポート、構造体の前のドキュメントのコメント
enum、インターフェイス、またはその他の非関数宣言は破棄されます。彼らはそうします
後の機能には持ち越されません。複数のドキュメントコメントが発生した場合
1 つの宣言の前では、最も近い (最後の) ドキュメント コメントが使用されます。
通常の `//` および `/* ... */` コメントはこれを置き換えません。ドキュメントは
トップレベルでのみ認識されます。関数本体内のコメントはそうではありません
関数メタデータ。終了していないブロック コメントは安定した字句を生成します。
診断 `FWS-LEX-003` とパーサーの回復は残りの部分で引き続き利用可能です
ソース。

正規化された AST メタデータは次の形式になります。

```ts
interface ForgeWebScriptDocumentation {
  readonly description: string;
  readonly tags: readonly ForgeWebScriptDocumentationTag[];
}

interface ForgeWebScriptDocumentationTag {
  readonly name: string;
  readonly subject?: string;
  readonly text: string;
}
```

ノーマライザーは、`/**` および `*/` 区切り文字、先頭の空白、
オプションで、各行の先頭の `*` 装飾と周囲の空白。走る
空白の部分が 1 つのスペースに折りたたまれます。最初のタグの前の説明行
段落にグループ化されています。空白行は段落区切りのままです。タグが始まります
`@` で始まる行にあり、空ではない次の行に続きます。
前のタグ。タグの順序と重複したタグは保持されます。

一般的に使用されるタグ形式は次のとおりです。

| タグフォーム                                                 | 構造化フィールド                        |
| ------------------------------------------------------------ | --------------------------------------- |
| `@param name text`、`@arg`、`@argument`、または `@parameter` | `name` は `subject` です。残りは `text` |
| `@typeparam name text`                                       | `name` は `subject` です。残りは `text` |
| `@throws type text` または `@exception type text`            | `type` は `subject` です。残りは `text` |
| `@return text` または `@returns text`                        | `text` のみ                             |
| `@deprecated text`                                           | `text` のみ                             |

他の `@name` フォームは、順序付きタグとして受け入れられ、保持されます。
診断として報告されます。彼らには推測された主語がありません。残りのテキスト
保存されています。タグ名では大文字と小文字が区別されます。

エディターのコンシューマーの場合、同じメタデータが決定論的にレンダリングされます。
説明の後にソース順に各タグが続き、間に空白行が入ります。
部品。件名は、タグ名とそのテキストの間に出力されます。次に例を示します。

```text
Adds one to a value.

@param value The value to increment.

@return The incremented value.

@deprecated Use `increment` in new code.
```

ドキュメントは分析メタデータであり、実行可能な言語のセマンティクスではありません。かもしれない
言語サービス利用者向けに AST および IR に保存されますが、保存されません。
宣言の解析、型チェック、下降、または実行時の動作に影響します。
ドキュメントは、生成される ABI 署名およびマニフェストから除外されます。
宣言とローダーアーティファクト、Wasm/WAT、実行可能コンテンツのハッシュ、および
能力要件。したがって、ドキュメントのコメントのみを変更すると、
モジュールの ABI または生成された実行可能コントラクトを変更しないでください。

## ソース文法

上にリンクされているチェックインされた EBNF アーティファクトは、完全な語彙を記述しています。
ブートストラップ、拡張集約、リカバリ コントラクト。以下の抜粋
完全なファイルを必要としない読者向けの v1 ブートストラップ サーフェスについて説明します。
文法では、通常の EBNF の意味で `*` と `?` が使用されます。

```ebnf
module       = { import | function } ;
import       = "import", "capability", string, "as", identifier,
               "(", [ parameters ], ")", "->", type, ";" ;
sourceImport = "import", string, "as", identifier, ";" ;
function     = [ "export" ], "fn", identifier, "(", [ parameters ], ")",
               "->", type, block ;
parameters   = parameter, { ",", parameter } ;
parameter    = identifier, ":", type ;
block        = "{", { statement }, "}" ;
statement    = "let", identifier, ":", type, "=", expression, ";"
             | "return", [ expression ], ";"
             | "if", expression, block, [ "else", block ]
             | "while", expression, block
             | "for", "(", [ for-clause ], ";", expression, ";",
               [ for-clause ], ")", block
             | "do", block, "while", expression, ";"
             | identifier, "=", expression, ";"
             | expression, ";" ;
for-clause   = "let", identifier, ":", type, "=", expression
             | identifier, "=", expression
             | expression ;
type         = "bool" | "bytes" | "f32" | "f64" | "i32" | "i64"
             | "string" | "u32" | "u64" | "unit" ;
expression   = literal | identifier | call | unary | binary ;
call         = identifier, "(", [ expression, { ",", expression } ], ")" ;
unary        = ( "!" | "-" ), expression ;
literal      = integer | string | "true" | "false" ;
```

二項演算子は、最も強いものから最も弱いものへと、次の優先順位に従います。
`* / %`、`+ -`、順序付き比較、等価、`&&`、および `||`。演算子は
左連想。括弧で囲まれた式は次のブートストラップ用に予約されています
改訂;コンパイラはサイレントではなく解析診断を発行する必要があります
今日は彼らを受け入れます。

この抜粋は **ブートストラップ** 文法です。ファイル定義モジュールをカバーします。
機能/ソースのインポート、プリミティブ署名、呼び出し、ローカル値、
式、構造化 `if`/`else`、`while`、C スタイル `for`、`do while`、および
`return`。ループ形式は、実行可能なブートストラップ コントラクトの一部です。だけ
予約例外ワード `throw`、`try`、および `catch` は拒否されます。
実行可能な構造。以下の集計宣言と値は、
**拡張** 契約であり、代替スペルとして扱ってはなりません
ブートストラップ文法。

### 拡張集約文法

拡張コントラクトは、不変の構造体、タグ付き列挙型、ジェネリック型、
インターフェイス、関数値、コレクション リテラル、インデックス付け、`match`。
その中心となるソース形式は次のとおりです。

```ebnf
aggregate    = struct | enum | interface ;
struct       = "struct", identifier, [ generic_parameters ], "{",
               { identifier, ":", type, ";" }, "}" ;
enum         = [ "export" ], "enum", identifier, [ generic_parameters ], "{",
               variant, { ",", variant }, [ "," ], "}" ;
variant      = identifier, [ "(", [ parameters ], ")" ] ;
generic_parameters = "<", generic_parameter, { ",", generic_parameter }, ">" ;
generic_parameter  = identifier, [ ":", identifier ] ;
type         = primitive | identifier, [ "<", type, { ",", type }, ">" ]
             | "[", type, ";", integer, "]"
             | "Fn", "<", type, ",", type, ">" ;
constructor  = identifier, "::", identifier, "(", [ expression ], ")" ;
match        = "match", expression, "{", match_arm, { ",", match_arm }, "}" ;
match_arm    = pattern, "=>", expression ;
pattern      = "_" | identifier, [ "(", [ identifier, { ",", identifier } ], ")" ] ;
```

`Result::Ok(value)` や
`Result::Error(message)` は集計に対して解決し、バリアントを検証します
アリティとフィールドタイプ。標準の `Result<T, E>` バリアントは正確に次のとおりです。
`Ok(T)` および `Error(E)`。 `Option<T>` は `Some(T)` および `None` のままです。関数
value は、`fn name` および宣言された `Fn<parameter, result>` タイプを使用します。たとえば、
`let callback: Fn<i32, i32> = fn increment;`。関数の値は次のようにチェックされます。
参照される関数のシグネチャであり、アリティが一致する場合にのみ呼び出し可能です。
そして引数の型。

一致バインディングはアームにローカルです: `Result::Ok(item) => item` バインド
`item` その式のみをチェックします。バインディング名は、その中で一意である必要があります。
arm とそのカウントは、選択したバリアント フィールドと一致する必要があります。漏れません
兄弟アームまたは周囲の機能に。

## 型とセマンティクス

V1 には、プリミティブ タイプ `bool`、署名付き `i32`/`i64`、署名なし `u32`/`u64`、
`f32`/`f64`、`string`、`bytes`、および `unit`。暗黙的な数値はありません
変換。算術オペランドは同じ数値型でなければなりません。比較
`bool` を生成します。論理演算子には `bool` が必要です。平等には平等が必要だ
タイプ。関数には 1 つの宣言された結果型があり、`unit` 関数は戻り値を返します。
価値がない。

### コンパイラが所有する正規表現

Forge Web Script は、決定論的な正規表現の標準ライブラリを提供します。
`regex_full_match(pattern, value) -> bool` を呼び出します。
`regex_prefix_match(pattern, value) -> bool`、および
`regex_search(pattern, value, start: i32) -> bool` 整数値を実行します。
それぞれ、位置ゼロのプレフィックスと左端の検索マッチングです。キャプチャ境界
対応する `regex_*_capture_start` および
`regex_*_capture_end` が呼び出します。グループインデックスを取得し、UTF-16 文字列を返します。
offset、または一致しない場合、またはグループが設定されていない場合は `-1`。検索キャプチャ
呼び出しではさらに、グループ インデックスの前に開始オフセットが取られます。

正規表現呼び出しは、コンパイラが所有する標準ライブラリ関数です。それらは次によって入力されます
フロントエンドは IR で注釈が付けられており、機能のインポートではありません。を使用するモジュール
したがって、正規表現呼び出しのみに空の `imports` 配列と空の配列が含まれます。
`requiredCapabilities` 配列。バックエンドの低下とモジュール内 VM は、
個別の実装フェーズ。コンパイラはこれらの呼び出しを
ブラウザ `RegExp`、Node API、または暗黙的なホスト インポート。

サポートされる構文は、リテラル、`.`、文字に意図的に制限されています。
クラスと範囲 (`^` の否定を含む)、`\d`、`\D`、`\w`、`\W`、`\s`、
`\S`、エスケープリテラル、キャプチャグループと非キャプチャグループ、代替、
`*`、`+`、`?`、有界 `{n}`、`{n,}`、`{n,m}` 量指定子、遅延量指定子、
および `^`/`Forge Web Script は、決定論的な正規表現の標準ライブラリを提供します。`regex_full_match(pattern, value) -> bool`を呼び出します。`regex_prefix_match(pattern, value) -> bool`、および
`regex_search(pattern, value, start: i32) -> bool`整数値を実行します。
それぞれ、位置ゼロのプレフィックスと左端の検索マッチングです。キャプチャ境界
対応する`regex__*capture_start`および`regex*__capture_end`が呼び出します。グループインデックスを取得し、UTF-16 文字列を返します。
offset、または一致しない場合、またはグループが設定されていない場合は`-1`。検索キャプチャ
呼び出しではさらに、グループ インデックスの前に開始オフセットが取られます。

正規表現呼び出しは、コンパイラが所有する標準ライブラリ関数です。それらは次によって入力されます
フロントエンドは IR で注釈が付けられており、機能のインポートではありません。を使用するモジュール
したがって、正規表現呼び出しのみに空の `imports` 配列と空の配列が含まれます。
`requiredCapabilities` 配列。バックエンドの低下とモジュール内 VM は、
個別の実装フェーズ。コンパイラはこれらの呼び出しを
ブラウザ `RegExp`、Node API、または暗黙的なホスト インポート。

サポートされる構文は、リテラル、`.`、文字に意図的に制限されています。
クラスと範囲 (`^` の否定を含む)、`\d`、`\D`、`\w`、`\W`、`\s`、
`\S`、エスケープリテラル、キャプチャグループと非キャプチャグループ、代替、
`*`、`+`、`?`、有界 `{n}`、`{n,}`、`{n,m}` 量指定子、遅延量指定子、
および `^`/ アンカー。後方参照、ルックアラウンド、名前付きグループ、フラグ、
他のホスト エンジン拡張機能は拒否されます。サポートされていない構文には安定した構文があります
`FWS-REGEX-001` 診断;不正なパターンでは `FWS-REGEX-002` が使用され、
内部コンパイラの不変エラーは `FWS-REGEX-003` を使用します。

共有パッケージ `@mission-platform/forge-web-script-regex` は安定版を所有しています。 `$`
バイトコード (`FORGE_REGEX_BYTECODE_VERSION`) とビルド時コンパイラ。その明示的な
`/reference` エントリ ポイントは、TypeScript VM を適合オラクルとしてのみ公開します
ネイティブ エンジンとバックエンドの差分テスト用。パッケージのルートはそうではありません
その VM を公開します。電話固有のメタデータは電話番号パッケージ内に残ります。
実稼働正規表現の実行は、Forge Web Script バックエンドと
生成された WASM モジュールは、TypeScript ランタイム層やホスト機能には決して影響されません。

`string` および `bytes` は、v1 の集計値です。文字列は不変です
ABI 境界で UTF-8 として表される Unicode スカラー値のシーケンス。
バイトは不変のオクテットのシーケンスであり、次のいずれかの値を含めることができます。
`0x00` ～ `0xff`。ソースレベルの操作は意図的に小さくなっています
ブートストラップサブセット内。ホスト呼び出しとそれ以降の標準ライブラリ モジュールが提供する
アンビエントブラウザを追加せずにエンコード、スライス、コレクション操作を実行
言語への API。

### コレクションの署名

拡張収集契約は構造的であり、受信者ベースです。それはあります
任意のオブジェクト メソッドを追加しないでください。固定配列は `[T; N]` および
ベクトルは `Vector<T>` として指定されます。サポートされている署名は次のとおりです。

| 受信機      | 方法                | 署名                    |
| ----------- | ------------------- | ----------------------- |
| `Array<T>`  | `length`            | `() -> u32`             |
| `Array<T>`  | `get`               | `(u32) -> Option<T>`    |
| `Array<T>`  | `set`               | `(u32, T) -> Array<T>`  |
| `Array<T>`  | `iter`              | `() -> Iterator<T>`     |
| `Vector<T>` | `length`            | `() -> u32`             |
| `Vector<T>` | `get`               | `(u32) -> Option<T>`    |
| `Vector<T>` | `set`               | `(u32, T) -> Vector<T>` |
| `Vector<T>` | `push` または `add` | `(T) -> Vector<T>`      |
| `Vector<T>` | `pop`               | `() -> Option<T>`       |
| `Vector<T>` | `iter`              | `() -> Iterator<T>`     |

`add` のスペルは、意図的にベクターの互換性エイリアスです。
`push`;それは配列メソッドではありません。インデックスは `u32` であり、要素の引数は次のとおりです。
`T` と一致し、戻り値は上記の署名と一致する必要があります。間違ったアリティ、
引数の型、受信者の種類、および不明なメソッドは型チェック エラーです。
空のリテラルにはコンテキスト要素タイプが必要ですが、空ではない配列/ベクトル
リテラルは要素の型を再帰的に推論し、混合要素を拒否します。あ
固定配列リテラルには、`N` 要素が正確に含まれている必要があります。

ローカルは関数スコープであり、1 回だけ初期化され、事前に読み取ることはできません。
彼らの宣言。ローカル宣言は既存の名前をシャドウしません: 重複しています
名前はエラーです。関数と機能のエイリアスは 1 つのモジュール名前空間を共有します
そして一意である必要があります。呼び出しでは、宣言された関数またはインポートされた関数を指定する必要があります
機能、そのアリティと引数の型が正確に一致する必要があります。

v1 制御フロー サーフェスは、`if`/`else`、`while`、C スタイル `for` で構造化されています。
`do while`、および初期の `return`。 `for` 句は明示的なステートメントであり、
ループの外側にクラス、レシーバー、または暗黙的な変更を導入しないでください。
ローカルな価値環境。暗黙的なフォールスルー結果はありません。
`unit` 以外の関数の到達可能なパスは、宣言された型を返す必要があります。の
ブートストラップ チェッカーは戻り値の型エラーを報告します。到達可能性分析は、
コンパイラが完全に v1 に準拠していると宣言する前に、フォローアップが必要です。

FWS は意図的にクラスフリーです。 `class`、`constructor`、`extends`、`impl`、
`new` および `trait` は予約されており、安定した診断で拒否されます。
`FWS-PARSE-052`;不変の構造体、タグ付き列挙型、インターフェイス、および関数
値は、サポートされている値指向の代替案です。段階的なセルフホスティング
コントラクトは、FWS コンパイラの間、チェックインされた TypeScript コンパイラをシードとして保持します。
実行時コントラクトは段階的にブートストラップされます。

## ファイル定義モジュール、ソースのインポート、およびエクスポート

ネストされた `module` 宣言はありません。すべての `.fws` ファイルはモジュールであり、その
安定した名前は、正規化されたファイル ID から派生します。たとえば、
プロジェクト `/workspace/app` の `src/time.fws` には、モジュール ID `src/time` があります。入れ子になった
`module name { ... }` 構文は移行診断で拒否されます。

ソース モジュールのインポートは、ホスト機能のインポートとは異なります。

```fws
import "./math.fws" as math;
import capability "clock.now" as now() -> i64;
```

Vite アダプターは、モジュール グラフを通じてソース インポートを解決します。依存関係
1 つのプロジェクト内では、デフォルトで静的にリンクされます。プロジェクト間のエッジのデフォルト
動的ロードに対応し、明示的に `static` または `dynamic` として構成できます。
プロジェクトとルートのリンク構成。不足しているモジュール、サポートされていないサイクル
選択されたリンク モードとアイデンティティの衝突はグラフ診断です。

静的リンクは、到達可能なゲストのエクスポートを 1 つの成果物に平坦化します。エクスポートの衝突
決定的に拒否されます (重複署名の場合は `FWS-LINK-003`、
互換性のない署名の場合は `FWS-LINK-004`)。リンカはサイレントに実行しません
名前空間を使用するか、ゲスト関数を上書きします。ダイナミックリンクは別個のモジュールのまま
境界を定義し、ABI マニフェストにソース モジュールのインポートとして記録されることはありません。
アンビエントホスト機能として。

`export` が前に付く宣言のみが公開されます。エクスポート名は安定しています。
大文字と小文字が区別される文字列であり、生成されたファイル内で辞書順に並べ替えられます。
マニフェストします。プライベート関数はエクスポートされた関数で使用できますが、使用できません。
ホストに表示されます。ワイルドカード エクスポートやアンビエント インポートはありません。

機能のインポートには、引用符で囲まれたホスト所有の名前とゲストローカルの別名があります。

```fws
import capability "clock.now" as now() -> i64;

export fn current_time() -> i64 {
  return now();
}
```

引用符で囲まれた機能名、エイリアス、パラメーター名/タイプ、および結果のタイプは次のとおりです。
すべてマニフェストに含まれています。インポートは決定的です: 重複したエイリアスまたは
機能宣言は拒否され、必要な機能名は
重複が排除され、並べ替えられました。ホストは機能名ごとに実装を提供します。
ゲストは、ゲストに存在しない機能を検出したり呼び出したりすることはできません。
マニフェストします。

## 論理機能 ABI

Forge Web Script は、完全な境界を主張するのではなく、WASI にインスピレーションを得た論理境界を使用します。
WASI互換性。機能は、次のような限定された明示的なホスト機能です。
`clock.now`、`random.bytes`、または `storage.read`。機能名の所有者は
プラットフォームに対応しており、それぞれの名前には個別にバージョン管理された署名があります。 DOM オブジェクト、
`window`、`document`、Node 組み込み、ネットワーク クライアント、およびその他のブラウザー グローバル
アンビエントゲスト依存関係ではありません。

ローダーはインスタンス化の前に次のチェックを実行します。

1. マニフェスト形式、言語バージョン、および ABI バージョンがサポートされています。
2. 必要なすべての機能がホスト レジストリに存在します。
3. 提供されたすべての機能には、正確に宣言された署名があり、未宣言の署名はありません。
   ゲストインポートは受け入れられます。
4. メモリ、アロケータ、エクスポート、インポートの宣言は内部的に行われます。
   一貫した。

機能の検出は、明示的なホスト操作です。ホストは、
機能インベントリをアプリケーション コードに追加しますが、ゲストは
そのモジュールによって宣言されたインポート。欠落している機能または拒否された機能は、次のエラーで失敗します。
ロード時間 `CapabilityDenied` トラップ; `undefined` または
サイレントノーオペ。

## 価値観、線形記憶、所有権

このモジュールは、64 KiB ページとリトル エンディアンを持つ 1 つの WebAssembly リニア メモリを使用します。
スカラー値。スカラー値は次のようにマッピングされます。

| Web スクリプトを作成する | WebAssembly 表現                    |
| ------------------------ | ----------------------------------- |
| `bool`                   | `i32`、`0` は false、`1` は true    |
| `i32`、`u32`             | `i32`                               |
| `i64`、`u64`             | `i64`                               |
| `f32`、`f64`             | 一致する WebAssembly フロート       |
| `unit`                   | 結果値がありません                  |
| `string`、`bytes`        | 2 つの `u32` 値: ポインタ、バイト長 |

マニフェストは、`valueRepresentations` で同じマッピングを宣言します。あ
ポインタと長さのペアは、読み取り前に常に符号なし範囲としてチェックされます。
書き込み: `pointer <= memory.byteLength` および `length <= byteLength - pointer`。
長さ 0 は有効であり、末尾を含む任意のインバウンド ポインターを使用できます。
記憶。失敗したチェックは `MemoryOutOfBounds` でトラップされ、決して公開されません。
部分的にデコードされた値。

生成されたモジュールは `fws_alloc(size: u32) -> u32` をエクスポートします。
`fws_dealloc(pointer: u32, size: u32) -> unit`、および
所有権として `fws_realloc(pointer: u32, oldSize: u32, newSize: u32) -> u32`
バッファの境界。署名の省略表現では、操作は次のようになります。
`fws_realloc(pointer, oldSize, newSize) -> pointer`。バッファを割り当てる呼び出し元はそれを所有しており、バッファを割り当てる必要があります。
同じモジュールと正確な現在のサイズを使用して、割り当てを解除または再割り当てします。
再割り当て者は、現在の高水位割り当てのサイズを適切に変更することを好みます。
リニアメモリが拡張できる場合の縮小と拡張を含みます。それ以外の場合は
置換を割り当て、`min(oldSize, newSize)` バイトを正確にコピーし、
置換ポインタを返す前に古い割り当てを解放します。あ
サイズ 0 の結果は有効であり、等しいサイズのリクエストは元の結果を返します。
ポインタ。ホスト実装はゲスト呼び出しの前に入力バイトをコピーする必要があります
マニフェストが将来の借用バッファを明示的に導入しない限り、戻ります
契約。ゲスト コードは、ホスト呼び出し後にホスト所有のポインターを保持してはなりません。
割り当てまたは拡張の失敗トラップ `MemoryExhausted`;無効なポインタまたは
サイズ範囲のトラップ `MemoryOutOfBounds`;古いポインター、間違っています
`oldSize`、二重解放、または無効な解放トラップ `InvalidOwnership`。これら
チェックは突然変異の前に行われ、再割り当てが失敗すると元のものが残ります。
割り当てとバイトは変更されません。

ホスト例外は次のように変換されます。 `HostError` 機能名と
不透明なホスト エラー コード。ゲスト トラップが通常のリターンに変換されることはありません
価値観。ホストはトラップの詳細をログに記録できますが、秘密や生の情報を公開してはなりません。
信頼できないゲスト コードに対するブラウザの例外。

### ゲスト所有のチェック済みメモリ操作

ステートフル ゲスト ヒープを実装する FWS ソース モジュールは、コンパイラが所有する
操作 `memory_alloc(size: u32) -> u32`、
`memory_dealloc(pointer: u32, size: u32) -> unit`、
`memory_realloc(pointer: u32, oldSize: u32, newSize: u32) -> u32`、
`memory_load_u32(address: u32) -> u32`、および
`memory_store_u32(address: u32, value: u32) -> unit`。これらの操作は、
モジュール アロケータに直接降格されるか、WebAssembly メモリがチェックされる
指示。これらはホストのインポートではなく、ゲストの状態を公開しません。
TypeScript。

アロケータは、`fws_alloc` と同じ所有権およびトラップ コントラクトを使用し、
`fws_realloc`。ロードまたはストアには、範囲内の完全な 4 バイト範囲が必要です。
現在の線形メモリ。無効な範囲は、前に `MemoryOutOfBounds` でトラップされます。
操作は部分的に実行できます。 `memory_realloc` は最初の
`min(oldSize, newSize)` バイトを取得し、ゲスト所有のポインタを返しますが、呼び出し元は
返されたポインタとその正確な現在のサイズを後の操作で使用する必要があります。
ステートフル メモリ フィクスチャ
`packages/forge-web-script/src/fixtures/stateful-memory.fws` が適合です
これらの署名のフィクスチャ、アロケータの再利用、再帰、リセット、および境界
罠。

コンパイラ所有のバイト リーダーは、ゲスト用の符号なしインデックスのバリアントも提供します
ソース オフセットをハンドルとして表すフロント エンド: `bytes_length_u32(value:
バイト) -> u32` and `bytes_byte_at_u32(値: バイト、インデックス: u32) -> u32`。彼らは
署名付き `bytes_length` と同じポインター長境界チェックを使用し、
`bytes_byte_at` 操作はホスト インポートではありません。 WebLua フロントエンドは次を使用します。
これらの操作は、レクサー オフセットとゲスト メモリ アドレスを 1 つに保つためのものです。
`u32` ドメインを確認しました。

### 未加工の WASM ABI と生成された ESM コントラクト

上記の表現は、安定した生の WASM ABI です。それは意図的にです
低レベルであり、生成された JavaScript ファサードがより高度になっても変化しません。
人間工学に基づいた:

```text
raw string value: (pointer: u32, length: u32)
raw bytes value:  (pointer: u32, length: u32)
```

コンパイラによって生成された ESM アーティファクトは、その ABI を JavaScript API に投影します。

```ts
type ForgeWebScriptBytes = readonly [pointer: number, length: number];

interface ForgeWebScriptExports {
  readonly memory: WebAssembly.Memory;
  readonly fws_alloc: (size: number) => number;
  readonly fws_dealloc: (pointer: number, size: number) => void;
  readonly fws_realloc: (pointer: number, oldSize: number, newSize: number) => number;
  readonly fws_reset: () => void;
  readonly echo: (value: string) => string;
  readonly processBytes: (value: ForgeWebScriptBytes) => ForgeWebScriptBytes;
}
```

機能のインポートや動的リンクを含む、生成されたすべての宣言
エクスポートでは、FWS `string` 値に `string` を使用します。生成された `load` と
`loadSync` ラッパーは JavaScript 文字列を UTF-8 としてエンコードし、ポインター長を渡します
変更されていない WASM ABI とペアリングし、返された文字列をデコードして JavaScript に戻します。
文字列。デコードには致命的な UTF-8 デコーダが使用されます。不正なゲスト バイトは危険です。
置換文字ではなく、明示的な境界エラー。

1 つの呼び出しの文字列引数が最初にエンコードされ、1 つの連続した引数にパックされます。
ゲストの割り当て。これにより、1 つのゲストを回避しながら生の ABI が変更されなくなります。
引数ごとの割り当てと JavaScript から WASM へのコピー。スカラー引数は保持されます
彼らの直接の高速パス。 `bytes` は意図的に `Uint8Array` に変換されません。
呼び出し元は引き続き `ForgeWebScriptBytes` の送受信を行い、`memory` は
呼び出し元がモジュールのメモリを使用して生のバイト範囲を読み書きできるように公開される
そして所有権のルール。

生成されたアダプターは、文字列引数用に作成された一時バッファーを所有し、
文字列の結果。結果をリリースする前にデコードしてから、それぞれをリリースします。
一時範囲は、成功時、ゲスト トラップ、ホスト時に `finally` パスに 1 回だけ指定されます。
例外とデコードの失敗。文字列値を含むホスト機能は、
JavaScript 文字列。JavaScript 文字列を返す場合もあります。ラッパーが実行するのは、
ゲスト割り当てとその戻り値の UTF-8 コピー。ホストコードは引き続きコピーする必要があります
将来のマニフェストで明示的に宣言しない限り、戻る前の生の `bytes` 入力
借りバッファ契約。 `load` と `loadSync` は、生成された同じものを公開します
契約。モジュールの初期化スケジュールのみが異なります。

この JavaScript プロジェクションを変更しても、`valueRepresentations` は変更されません。
raw ポインター長 ABI、ABI バージョン、または raw WASM コンテンツ ハッシュ。
生成されたアーティファクトは、遅延デコードされた 1 つの組み込み WASM 表現を保持します。
`load` と `loadSync` は、個別のペイロードを実体化するのではなく、それを共有します。
コピー。したがって、非同期ローダー チェックと同期ローダー チェックでは動作を比較する必要があります。
および宣言、決定論的なコンテンツ ハッシュ チェックでは生のデータをハッシュする必要があります。
生成された ESM ソース サイズまたはローダー実装に依存しない WASM バイト
詳細。

## マニフェスト形式

生成された各モジュールには、安定した JSON 互換の ABI マニフェストが含まれます。
WASM アーティファクトと型指定された ESM ローダー:

```json
{
  "format": "forge-web-script-module",
  "languageVersion": "1.0",
  "abiVersion": "1.2",
  "moduleName": "src/clocked",
  "exports": [{ "name": "current_time", "parameters": [], "result": "i64" }],
  "imports": [
    {
      "capability": "clock.now",
      "alias": "now",
      "function": { "name": "now", "parameters": [], "result": "i64" }
    }
  ],
  "sourceImports": [],
  "requiredCapabilities": ["clock.now"],
  "memory": {
    "pageSize": 65536,
    "addressType": "u32",
    "ownership": "caller-owned",
    "stringEncoding": "utf8",
    "byteArrayRepresentation": "pointer-length",
    "allocatorExport": "fws_alloc",
    "deallocatorExport": "fws_dealloc",
    "reallocatorExport": "fws_realloc"
  },
  "valueRepresentations": { "i64": "i64", "string": "pointer-length-u32" },
  "trapModel": "explicit-trap",
  "standardLibrary": { "regexBytecodeVersion": "bytecode-1" }
}
```

実際のマニフェストには、すべてのプリミティブ表現エントリが含まれています。
例で使用されているもの。エクスポート、インポート、機能の JSON キーは次のとおりです。
ビルドを繰り返しても安定しています。ソース マップとコンテンツ ハッシュは次によって生成されます。
コンパイラ アダプタであり、ABI シグネチャ マッチングの一部ではありません。

`standardLibrary` マニフェスト フィールドには、コンパイラが所有するライブラリ ID が記録されます。
正規表現の場合、`regexBytecodeVersion` とオプションの `regexCorpusHash` がキャッシュされます。
そしてアーティファクト入力。正規化されたソース、コンパイラのバージョン、最適化
モード、モジュール グラフ、リンク構成、標準ライブラリ ID、およびメタデータ
コーパス ハッシュは、キャッシュ ルックアップの前に安定した順序でシリアル化する必要があります。同一
入力により、同一のバイトコード テーブル、マニフェスト、宣言、WAT、および
コンテンツのハッシュ。 ID 入力を変更するとキャッシュミスになります。コーパスハッシュは
コーパスを提供するパッケージが所有しており、ホストから推測してはなりません
実行時の状態。

## コンパイラと CLI の境界

パブリック TypeScript ファサードにより、フロントエンド コントラクトとオーケストレーションが分離されます。
排出から。ソース ファイルまたは解決されたグラフを受け入れ、構造化されたグラフを生成します
診断と型付き IR、WebAssembly/WAT 生成を委任します。
`@mission-platform/forge-web-script-wasm`。バックエンドは前にバイトを検証します。
それらを返却する。エラーが発生すると、実行可能出力が抑制されます。 Vite アダプターと LSP は使用します
Node CLI に依存する必要はありません。

ファイルシステムのワークフローの場合は、`@mission-platform/forge-web-script-cli` をインストールし、
スタンドアロンの `forge-web-script` バイナリを使用します。

```text
forge-web-script check <entry.fws> [--root <directory>] [--project-root <directory>]
forge-web-script compile <entry.fws> --out-dir <directory>
  [--root <directory>] [--project-root <directory>]
  [--link-mode static|dynamic] [--capability <name>] [--optimization debug|release]
```

`check` は、ファイルを書き込まずにソースとグラフの入力を検証します。成功した
`compile` は、`<entry>.wasm`、`<entry>.wat`、`<entry>.abi.json`、を正確に書き込みます。
`<entry>.d.ts`、`<entry>.js`、および `<entry>.map` を選択した出力ディレクトリにコピーします。
CLI は、診断がクリアされた後にのみ完全なセットをステージングして名前を変更します。
不正なソース、未解決のグラフ エッジ、拒否された機能、および ABI エラー
実行可能なアーティファクトを残さず、ゼロ以外のステータスを返します。出力の順序付け、
マニフェスト JSON、WAT、宣言、ローダー データ、ソース マップ、およびコンテンツ ハッシュ
同一の入力に対しては決定的です。

## Vitest および Vite テストの統合

Vitest スイートが必要な場合は、`@mission-platform/forge-web-script-vitest` を使用してください。
Assert コンパイラ アーティファクト、構造化診断、Wasm 動作、グラフ リンク、
または生成された Vite モジュール コントラクト。そのダイレクトハーネスメソッド (`compile`、
`compileSource`、`compileGraph`、`inspect`、`load`、`loadSync`、および
`checkVmParity`) パブリック コンパイラ/ランタイム コントラクトに委任します。その
`defineForgeWebScriptVitestConfig` ヘルパーは本番環境をインストールします
`forgeWebScriptPlugin` は消費者向けの Vite プラグインと設定を保持します。
詳細については、[ミッション プラットフォームでのテスト](../../../../../../docs/locales/ja/testing.md#forge-web-script-tests) を参照してください。
構成と治具の例。

ハーネスは、キー付きの明示的な機能マップを介してのみホスト機能を受け入れます。
マニフェスト機能名によって、たとえば次のようになります。

```ts
const exports = await harness.load<{ current: () => bigint }>('capabilities/clock-now.fws', {
  'clock.now': { now: () => 123n },
});
```

宣言されたインポートが欠落していたり​​、宣言されていない提供されたインポートは失敗となります。テスト
`.fws` またはその仮想アーティファクト クエリをインポートするプロジェクトでは、
型のみの宣言のサブパス
`@mission-platform/forge-web-script-vitest/forge-web-script` からの
TypeScript `types` リストまたは参照されたテスト タイプ エントリポイント。

下の共有ハーネス器具
`packages/forge-web-script-vitest/fixtures/` は、以下のクロスパッケージ コーパスです。
有効なモジュール、診断、機能、グラフ、セルフホスト パリティ。
パッケージローカルのフィクスチャはコンパイラ、ランタイム、プラグインに適切なまま
個人情報を扱うテスト。

`checkVmParity` は、有界セルフホスト型 lex ステージ パリティ コントラクトをレポートします。
`interpret`、`jit`、または `aot` モード。パリティ、フィンガープリント、ステップ数をアサートします。
および AOT 再現性メタデータを含みますが、このレポートを恣意的なものとして扱わないでください。
コンパイル済みの FWS VM の実行。 Wasm ロードはランタイム動作チェックのままです。

## 診断

診断は、`code`、`severity`、`phase`、`message`、
`fileName`、およびソース `span`。実用的なレコードには `hint` が含まれる場合もあります。
フェーズは、`lex`、`parse`、`type-check`、または `abi` のいずれかです。安定した v1 コード
家族には次のものが含まれます。

| コードファミリー | 意味                                                                                 |
| ---------------- | ------------------------------------------------------------------------------------ |
| `FWS-LEX-*`      | 無効な文字/エスケープ、生の文字列の行終端記号、または終了していない文字列/コメント。 |
| `FWS-PARSE-*`    | 無効なモジュール、宣言、ステートメント、または式の構文です。                         |
| `FWS-TYPE-*`     | 無効なプリミティブ型、名前、演算子、引数、または戻り値                               |
| `FWS-ABI-*`      | 重複した名前、拒否された機能、エクスポートまたはインポート                           |
| `FWS-REGEX-*`    | サポートされていない、または不正な形式のコンパイラ所有の正規表現パターン             |

エラーによりアーティファクトの生成が妨げられます。警告と情報診断は、
セマンティクスを変更しないでください。診断の順序付けはソース順であり、その後にフェーズが続きます。
同じスパンに接続された診断の順序。 Vite アダプターは保存する必要があります
エラーを Vite に転送するときの安定したコードとスパン。

## ブートストラップ適合契約

v1 コンパイラのターゲットは、言語と ABI サーフェスに意図的に制限されています
ここに文書化されています。プログラムがブートストラップ サブセットを使用する場合、そのプログラムはブートストラップ サブセットに含まれます。
モジュール、上記の字句規則、プリミティブ型、`string`/`bytes` 値、
明示的にエクスポートされた関数、機能のインポート、ローカル宣言、呼び出し、
式、`if`/`else`、`while`、C スタイル `for`、`do while`、および `return`。
拡張された集約契約は個別に適合性テストが行われ、追加のものが追加されます。
構造体、列挙型、ジェネリック型、コレクション値、関数値、および
`match`;暗黙的なブラウザまたは Node グローバルに依存してはなりません。

`packages/forge-web-script/src/fixtures/bootstrap.ts` は実行可能ファイルです
適合コーパス。受け入れられたフィクスチャは、エラー診断なしで検証される必要があります。
拒否されたフィクスチャは、リストされた安定した診断コードと有効な診断コードを報告する必要があります。
ソーススパン。他の言語での実装でも同じフィクスチャを使用できる
正規化された AST、診断、およびマニフェスト JSON を形成して比較します。治具
スイートは適合ターゲットであり、実装固有のスナップショットではありません。

の共有ソース コーパス
`packages/forge-web-script-vitest/fixtures` は同じ境界をカバーします。
`valid/collections.fws` 演習コレクション リテラル、インデックス作成、コンテキスト
空のベクトル、`length()`、および有効なエスケープ文字列。
`valid/aggregates.fws` は関数値を実行し、修飾された `Result::Ok` と
`Result::Error` コンストラクター、およびアームローカルの一致バインディング。そして
`diagnostics/collections.fws` は無効なコレクション呼び出しと集計を実行します
コンストラクター/バインディング診断。コレクションフィクスチャもコンパイルされています
共有 Wasm ハーネスを介して。集計構文はフロントエンドとして保持されます。
そのハーネスに対して合計 Wasm の低下が有効になるまで、適合ソースを維持します。

## 互換性ポリシー

デフォルトでは、言語と ABI メジャー バージョンに互換性がありません。ローダーは受け入れることができます
プロデューサーがマークした場合にのみ、より高いマイナー バージョンを持つ同じメジャー ABI
新しいフィールドはオプションであり、コンシューマは未知のフィールドを安全に無視します。を削除する
エクスポート、タイプの変更、所有権の変更、または機能の変更
署名には破壊的な ABI リビジョンが必要であり、次のようなローダーによって拒否される必要があります。
実装しないでください。 ABI `1.2` は、保持されているにもかかわらず、非常に破壊的なリビジョンです。
`1.x` 番号付け: 必須の `fws_realloc` メモリ エクスポートはオプションではありません。
ABI `1.1` マニフェストはサイレント アップグレードされません。決して機能を追加しない
既存のモジュールをサイレントに変更します。新しいマニフェスト宣言が必要です。
ホストの承認。

コンパイラのバージョンは ABI のバージョンではありません。コンパイラーはそのバージョンを以下に含める必要があります
コンパイル入力とアーティファクト ハッシュ、ただしローダーは言語と ABI を比較します
バージョンとマニフェスト署名。互換性チェックに失敗すると、
実行時のフォールバックではなく、読み込み時の診断。 Rust および AssemblyScript モジュール
共存中は既存のラッパーと ABI コントラクトを引き続き使用します
期間。 Forge Web Script は、それらを再解釈したり置き換えたりしません。

正規表現の標準ライブラリ互換性はホスト正規表現とは意図的に分離されています
互換性。 Forge バイトコード コントラクトとコンパイラは、受け入れられるバイトコード コントラクトとコンパイラを定義します。
構文と安定した診断。参照 VM は、
左端/バックトラッキング動作、UTF-16 キャプチャ オフセット、`-1` センチネルの設定解除
バックエンド VM が使用可能になるまで。ブラウザまたは Node 正規表現の動作
これは単なる差分オラクルであり、TypeScript 参照 VM でも、
ホスト正規表現 API は実稼働標準ライブラリ呼び出しを実行する場合があります。
オペコード番号付け、キャプチャ スロット レイアウト、サポートされる構文、診断の変更
コード、または一致するセマンティクスには新しい正規表現バイトコード バージョンと新しいバージョンが必要です
アーティファクトのアイデンティティ。バックエンド/ランタイムの適合と電話番号の移行まで
証拠は完全ですが、AssemblyScript 電話の実装はまだ残っています。
明示的なレガシー回帰オラクルであり、Forge アーティファクトと混合されることはありません。

## 共存と移行

Forge Web Script は、中立的なユーザー向けの実稼働ターゲットです。
`@mission-platform/code-scanner` アーティファクト。スキャナ グラフは静的にリンクします
QR、マトリックス、バーコード デコーダ ソースを 1 つの自己完結型 WebAssembly に統合
アーティファクト;動的プロファイルはソースとモジュールの境界を明示的に保ち、
解決されたエクスポートをキャッシュします。 Rust `code-scan` クレートは引き続き
ネイティブ/参照実装であり、パッケージの実行時依存関係ではありません。
パブリック QR、マトリックス、およびバーコード パッケージは、独自の型付きラッパーを保持します。
これらの API は、スキャナ グラフを通じてサイレントにリダイレクトされません。

`codecMigrationFixture` の
`packages/forge-web-script/src/fixtures/codec-migration.ts` が最初です
コーデック アダプターのような形状の適合フィクスチャ。宣言します
`codec.barcode.encode(payload: string) -> bytes`、`encode_payload` をエクスポート、検証します。
ポインター長 ABI を使用し、注入可能なホストを使用して呼び出し元所有の出力を書き込みます。
意図的に狭い ABI フィクスチャのままです。ホストは決定論的な ABI フィクスチャを使用できます。
フィクスチャは Forge Web Script を証明する一方で、適合性テストの偽装
境界線。製品版コーデック パリティには依然として一致するベクトルが必要であり、
関数名の一致だけでなく、パフォーマンスの測定も可能です。

対応する従来のラッパーは `encode(symbology, data)` をエクスポートし、返します。
`Uint8Array | undefined`;フィクスチャは `encode_payload(payload)` をエクスポートし、
ABI が所有する `bytes` ペアを返します。この意図的な違いにより、
機能境界が明示的: 移行アダプターはレガシーをマッピングする可能性があります
シンボル/データは宣言された機能に呼び出されますが、フィクスチャはそれを行いません
2 つのエクスポートはまだ動作的に交換可能であると仮定します。

### 実装の選択

| ワークロードまたは要件                                                | 選択                                                                 | 理由                                                                                                                                                |
| --------------------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 既存の QR またはマトリックス パッケージの動作                         | `@mission-platform/qr-code` / `@mission-platform/matrix-code`        | パッケージ固有の型付き ESM ラッパーは、これらのパブリック API で引き続き使用できます。                                                              |
| ニュートラルな画像とカメラ スキャナーの動作                           | `@mission-platform/code-scanner`                                     | デフォルトで静的にリンクされた FWS グラフを使用するか、キャッシュされたディスパッチを備えた明示的な動的ソース モジュール プロファイルを使用します。 |
| 既存のバーコードの動作                                                | `@mission-platform/barcode`                                          | パッケージローカルの Forge Web Script グラフは、入力されたバーコードのファサードを提供します。                                                      |
| 明示的なホスト効果を備えた新しい汎用ブラウザセーフ コンピューティング | Forge Web Script と `@mission-platform/vite-plugin-forge-web-script` | バージョン管理された `.fws` ソース、マニフェスト、型付きローダー、およびデフォルト拒否機能。                                                        |
| 既存の AssemblyScript ソースまたは AssemblyScript 固有の移行          | `@mission-platform/vite-plugin-assemblyscript`                       | `.ts` AssemblyScript エントリをコンパイルし、生成された raw エクスポート コントラクトを保存します。                                                 |
| フレームワークに依存しない UI/コンポーネントのコンパイル              | Forge コンポーネント コンパイラ                                      | Forge Web Script は、`FrameworkOutputPlugin` またはコンポーネント ターゲットに代わるものではありません。                                            |

Forge Web Script Vite プラグインは、`.fws` エントリに対してのみ使用してください。を使用します。
既存の AssemblyScript エントリ用の AssemblyScript プラグイン。移行中、
アプリケーションは両方の種類のモジュールをバンドルできます。各ローダーは独自のモジュールを所有します。
初期化、メモリ、ABI 検証、および機能のインポートは、
Forge Web Script モジュールに明示的に提供されます。

### 証拠と非推奨ゲート

移行作業では、候補ごとに 4 つの独立した比較を記録する必要があります。

1. 共有ゴールデン ベクトルに対するエクスポートされた動作（無効な入力や無効な入力など）
   境界の場合。
2. ABI の安全性（マニフェスト/バージョン チェック、インポート拒否、境界チェックなど）
   トラップ変換とバッファ所有権。
3. 生成されたアーティファクトの安定性（再現可能なハッシュ、宣言、
   ソース マップ、およびブラウザ/Node の読み込み。そして
4. コンパイルを含む代表的なリリース - ビルドのパフォーマンス測定
   時間、アーティファクトのサイズ、初期化、および定常状態の呼び出し。

現在、移行フィクスチャは、このコンポーネントの ABI とアーティファクト部分を提供しています。
証拠。既存のバーコード ラッパーおよびデコーダー パッケージのテストは引き続き使用されます。
動作とレガシー回帰オラクル。むしろ器具の横に置いてください
フィクスチャを交換ベンチマークとして扱うよりも、ウェブを作成する
ワークロードが通過するまで、スクリプトは Rust または AssemblyScript パスを非推奨にしてはなりません
サポートされている 2 つのホスト環境での 4 つの比較すべてについて、文書化されています。
ロールバック パスであり、未解決の ABI またはセキュリティの検出結果はありません。廃止予定
発表された互換性ウィンドウとアダプターまたは移行ガイドが必要です。
削除するには後続のメジャー リリースが必要です。

## クラスフリーの集約および実行契約

拡張されたクラスフリー コントラクトにより、`enum` のタグが付いた不変の `struct` 値が追加されます。
値、構造的なコンパイル時の `interface` 宣言、汎用パラメーター
インターフェイス境界、関数値、コレクション リテラル/メソッド、および
`match` 式/ステートメント。修飾された enum コンストラクターは `Type::Variant` を使用します
一致バインディングはアームローカルです。たとえば、
`Result::Ok(item) => item` は、そのアーム内でのみ `item` に結合します。標準
`Result<T, E>` コントラクトは、`Err(E)` ではなく、`Ok(T)` と `Error(E)` を使用します。
構造体の更新は純粋な値の変換です。構造体でもインターフェースでもない
コンストラクター、アイデンティティ、継承、レシーバー、またはランタイムディスパッチがあります。どれでも
クラス/オブジェクト指向の構成要素 (`class`、
`constructor`、`extends`、`impl`、`new`、および `trait`) は安定版で拒否されます
診断 `FWS-PARSE-052`。

集約レイアウトは、正規の名前順でマニフェストに記録されます。構造体
フィールドは順序付けされた 4 バイトで整列された値です。 enum レイアウトは 4 バイトで始まります
差別的な。フィールドの所有権が明示的 (`owned`、`borrowed`、または `shared`) であり、
デフォルトは所有された不変ストレージです。汎用値はコンクリートごとに特殊化されます
タイプ;記述子ベースの表現は、明示的な反復子または
インターフェイスの境界は特殊化レコードによって表されます。

VM バイトコード コントラクトはバックエンドに依存しません。 `ForgeWebScriptVmModule`
型付き関数、定数、集約レイアウト、特殊化、
機能インポート、ソース スパン、および 64 KiB リニア メモリ
`fws_alloc`/`fws_dealloc`/`fws_realloc` 境界。 `interpret`、`jit`、`aot` は実行中です
同じ命令/値/トラップのセマンティクス上のモード。 JIT キャッシュ キーと AOT
アーティファクトにはコンパイラとソースのハッシュが含まれます。機能は呼び出し可能のみです
モジュールマニフェストに存在する場合。

リアクティブなランタイム状態はデータです。エンティティ インデックスは生成カウンターを使用します。
コンポーネント ストアとワールドは不変のスナップショットであり、システムはワールドを返します。
トランジション。シグナル、サブスクリプション、クエリ要件、決定的な順序、
制限されたスケジューラのステップは明示的な値です。 ECS ホスト統合には必要なものがあります
他の FWS インポートと同じ宣言された機能境界。

## 範囲の境界

v1 実装は、TypeScript フロントエンドと決定論的な WebAssembly です。
バックエンド。互換性ファサードとスタンドアロン Node CLI を通じて公開されます。
適合フィクスチャと生成されたアーティファクトは互換性のターゲットです。

セルフホスト型コンパイル (FWS プログラムとしてコンパイラを実行) は明示的に行われます。
この v1 コントラクトのクラスフリー サーフェスと VM バイトコード実行によってサポートされています
モデルですが、v1 ABI と言語の正確性のためには必要ありません
境界線。より豊富な言語機能、既存の Rust または
AssemblyScript ワークロード、およびその他の非 v1 コンパイラーの進化はこの範囲外です。
契約。

## ツールのカットオーバーとブートストラップの境界

CLI、Vite プラグイン、言語サービス、および LSP はすべてパブリック コンパイラを使用します。
サービス契約。レクサーの移行は意図的に LSP ファーストで行われます。
EBNF 文法は、TypeScript トークン コントラクト、言語サービス、および
エディター アダプターは最初の許容境界であり、コンパイラー/フロントエンドまたは
自己ホスト型の所有権は、トークンの種類、診断、シンボル、
補完、ホバー、および UTF-16 範囲に準拠します。現在の境界のある FWS で作成された
lex/token ステージは互換性パリティ パスのままですが、TypeScript レクサーは
および言語サービス ゲートは移行中です。それは文法の権威ではありません。

LSP ゲートが緑色になった後、同じ文法が FWS/VM レクサーに移植されます。
そして、境界のあるパーサーモジュール段階に進みます。残りのフロントエンド、リンカー、
オプティマイザー、マニフェスト、および Wasm エミッション ステージは、依然としてシードバックされています。
リリース;この境界は意図的なものであり、次のように露出されています。
`ForgeWebScriptSelfHostedStageReport` が完全なものとして表示されるのではなく
セルフホスティング。

CLI は、`--vm-mode interpret|jit|aot` を使用して VM モードを選択します。 Vite プラグイン
および言語サービス ワークスペース オプションは、対応する `selfHostedVmMode` を使用します
値。 3 つのモードはすべて同じバイトコードを実行し、lex フィンガープリントを比較します。
独立したシード参照を使用します。不一致または VM トラップが安定した状態になる
`FWS-BOOTSTRAP-001` 診断により、無効な Wasm アーティファクトが生成されるのを防ぎます。
発せられた。 `interpret` は迅速なチェックを目的としており、`jit` と `aot` は
適合/開発モード。コンパイルされた Wasm は通常のプロダクションのままです
アーティファクトとランタイム パス。

グラフのリンク、宣言、ソース マップ、ABI マニフェスト、決定論的ハッシュ、
線形メモリの所有権、機能の拒否、コレクション/ECS 値、および明示的
非同期スケジューラ機能は、引き続き既存の公的契約によって管理されます。
ツール アダプターは、アンビエント ホスト API や暗黙的なオブジェクト ディスパッチを追加しません。
マイクロタスクと Web ワーカーは、宣言されたスケジューラを通じてのみ利用可能です
機能とその順序は明示的かつ決定的なままです。消費者
今後のリリースまで、VM レポートをパリティ/適合性シグナルとして扱う必要があります。
追加のコンパイラ ステージを同じ FWS 境界の後ろに移動します。
