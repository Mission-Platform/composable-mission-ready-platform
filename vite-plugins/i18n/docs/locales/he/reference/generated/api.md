# הפניה ל-@mission-platform/vite-plugin-i18n API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> vite-plugins/i18n/docs/reference/generated/api.md: [vite-plugins/i18n/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/vite-plugin-i18n`.

## `src/index`

### בְּרִירַת מֶחדָל

פונקציה **סוג:**

```typescript
function i18nPlugin(options: I18nPluginOptions = {}): Plugin
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| אפשרויות | I18nPluginOptions |  |

### i18nPlugin

פונקציה **סוג:**

```typescript
function i18nPlugin(options: I18nPluginOptions = {}): Plugin
```

לא סופק תיאור.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| אפשרויות | I18nPluginOptions |  |

### I18nPluginOptions

**סוג:** ממשק

```typescript
export interface I18nPluginOptions
```

לא סופק תיאור.

### readSupportedLocales

פונקציה **סוג:**

```typescript
function readSupportedLocales(options: ReadSupportedLocalesOptions = {}): string[]
```

גלה את המקומות הנתמכים ב**זמן בנייה/קונפיגורציה**, תוך שימוש זהה בדיוק
לוגיקה כמודול `virtual:i18n-locales`. קבצי תצורה Vite אינם יכולים לייבא א
מודול וירטואלי (הוא קיים רק בתוך חבילת האפליקציות), אז זה מאפשר תצורה
גוזר את רשימת המקומות שלו (למשל עבור מסלולי SSG / מפות אתר) מאותו מקור
של אמת במקום שמירה על רשימה מקבילה.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| אפשרויות | ReadSupportedLocalesOptions |  |

### ReadSupportedLocalesOptions

**סוג:** ממשק

```typescript
export interface ReadSupportedLocalesOptions
```

אפשרויות עבור מסייע הבנייה {@link readSupportedLocales}.
