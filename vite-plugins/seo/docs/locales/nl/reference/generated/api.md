# @mission-platform/vite-plugin-seo API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/vite-plugin-seo`.

## `src/index`

### standaard

**Soort:** functie

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

Maak een Vite-plug-in die `robots.txt` en `sitemap.xml` naar de
`publicDir` van het project (of {@link SeoPluginOptions.outDir}) aan het begin van
elke build- en dev-server-run.

#### Parameters

| Naam   | Typ              | Beschrijving |
| ------ | ---------------- | ------------ |
| opties | SeoPlugin-opties |              |

#### Contract

- **@voorbeeld:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### seoPlugin

**Soort:** functie

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

Maak een Vite-plug-in die `robots.txt` en `sitemap.xml` naar de
`publicDir` van het project (of {@link SeoPluginOptions.outDir}) aan het begin van
elke build- en dev-server-run.

#### Parameters

| Naam   | Typ              | Beschrijving |
| ------ | ---------------- | ------------ |
| opties | SeoPlugin-opties |              |

#### Contract

- **@voorbeeld:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### SeoPlugin-opties

**Soort:** interface

```typescript
export interface SeoPluginOptions
````

Opties voor {@link seoPlugin}.
