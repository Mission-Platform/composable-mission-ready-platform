# Referencia de API @mission-platform/vite-plugin-seo

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/vite-plugin-seo`.

## `src/index`

### por defecto

**Tipo:** función

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

Cree un complemento Vite que escriba `robots.txt` y `sitemap.xml` en el
`publicDir` del proyecto (o {@link SeoPluginOptions.outDir}) al inicio de
cada ejecución del servidor de desarrollo y compilación.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| opciones | Opciones de SeoPlugin |             |

#### Contrato

- **@ejemplo:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### seoPlugin

**Tipo:** función

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

Cree un complemento Vite que escriba `robots.txt` y `sitemap.xml` en el
`publicDir` del proyecto (o {@link SeoPluginOptions.outDir}) al inicio de
cada ejecución del servidor de desarrollo y compilación.

#### Parámetros

| Nombre   | Tipo                  | Descripción |
| -------- | --------------------- | ----------- |
| opciones | Opciones de SeoPlugin |             |

#### Contrato

- **@ejemplo:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### Opciones de SeoPlugin

**Tipo:** interfaz

```typescript
export interface SeoPluginOptions
````

Opciones para {@link seoPlugin}.
