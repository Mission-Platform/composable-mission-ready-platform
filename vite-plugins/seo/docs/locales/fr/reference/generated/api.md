# Référence API @mission-platform/vite-plugin-seo

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/vite-plugin-seo`.

## `src/index`

### défaut

**Genre :** fonction

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

Créez un plugin Vite qui écrit `robots.txt` et `sitemap.xml` dans le
`publicDir` du projet (ou {@link SeoPluginOptions.outDir}) au début de
chaque exécution de build et de serveur de développement.

#### Paramètres

| Nom     | Tapez                 | Descriptif |
| ------- | --------------------- | ---------- |
| options | Options du plugin SEO |            |

#### Contracter

- **@exemple:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### SEOPlugin

**Genre :** fonction

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

Créez un plugin Vite qui écrit `robots.txt` et `sitemap.xml` dans le
`publicDir` du projet (ou {@link SeoPluginOptions.outDir}) au début de
chaque exécution de build et de serveur de développement.

#### Paramètres

| Nom     | Tapez                 | Descriptif |
| ------- | --------------------- | ---------- |
| options | Options du plugin SEO |            |

#### Contracter

- **@exemple:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### Options du plugin SEO

**Genre :** interface

```typescript
export interface SeoPluginOptions
````

Options pour {@link seoPlugin}.
