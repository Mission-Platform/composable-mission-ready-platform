# הפניה ל-@mission-platform/vite-plugin-seo API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב-`@mission-platform/vite-plugin-seo`.

## `src/index`

### בְּרִירַת מֶחדָל

פונקציה **סוג:**

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
```

צור תוסף Vite שכותב `robots.txt` ו-`sitemap.xml` לתוך
`publicDir` של הפרויקט (או {@link SeoPluginOptions.outDir}) בתחילת
כל הפעלה ושרת פיתוח.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| אפשרויות | SeoPluginOptions |  |

#### חוֹזֶה

- **@example:** ```ts
import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
  plugins: [
    seoPlugin({
      sitemap: { urls: [{ loc: 'https://example.com/' }] },
      robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
    }),
  ],
});
```

### seoPlugin

פונקציה **סוג:**

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
```

צור תוסף Vite שכותב `robots.txt` ו-`sitemap.xml` לתוך
`publicDir` של הפרויקט (או {@link SeoPluginOptions.outDir}) בתחילת
כל הפעלה ושרת פיתוח.

#### פרמטרים

| שם | הקלד | תיאור |
| --- | --- | --- |
| אפשרויות | SeoPluginOptions |  |

#### חוֹזֶה

- **@example:** ```ts
import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
  plugins: [
    seoPlugin({
      sitemap: { urls: [{ loc: 'https://example.com/' }] },
      robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
    }),
  ],
});
```

### SeoPluginOptions

**סוג:** ממשק

```typescript
export interface SeoPluginOptions
```

אפשרויות עבור {@link seoPlugin}.
