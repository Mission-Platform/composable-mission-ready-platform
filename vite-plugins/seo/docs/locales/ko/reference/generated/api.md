# @mission-platform/vite-plugin-seo API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/vite-plugin-seo`의 공개 소스 선언에서 생성됩니다.

## `src/index`

### 기본

**종류:** 기능

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

`robots.txt` 및 `sitemap.xml`을 작성하는 Vite 플러그인을 생성합니다.
프로젝트의 `publicDir`(또는 {@link SeoPluginOptions.outDir}) 시작 시
모든 빌드 및 개발 서버가 실행됩니다.

#### 매개변수

| 이름 | 유형            | 설명 |
| ---- | --------------- | ---- |
| 옵션 | Seo플러그인옵션 |      |

#### 계약

- **@예:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### seo플러그인

**종류:** 기능

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

`robots.txt` 및 `sitemap.xml`을 작성하는 Vite 플러그인을 생성합니다.
프로젝트의 `publicDir`(또는 {@link SeoPluginOptions.outDir}) 시작 시
모든 빌드 및 개발 서버가 실행됩니다.

#### 매개변수

| 이름 | 유형            | 설명 |
| ---- | --------------- | ---- |
| 옵션 | Seo플러그인옵션 |      |

#### 계약

- **@예:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
plugins: [
seoPlugin({
sitemap: { urls: [{ loc: 'https://example.com/' }] },
robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
}),
],
});

````

### Seo플러그인옵션

**종류:** 인터페이스

```typescript
export interface SeoPluginOptions
````

{@link seoPlugin} 옵션.
