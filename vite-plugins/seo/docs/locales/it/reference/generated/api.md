# Riferimento API @mission-platform/vite-plugin-seo

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> vite-plugins/seo/docs/reference/generated/api.md: [vite-plugins/seo/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/vite-plugin-seo`.

## `src/index`

### predefinito

**Tipo:** funzione

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
```

Crea un plugin Vite che scrive `robots.txt` e `sitemap.xml` nel file
`publicDir` del progetto (o {@link SeoPluginOptions.outDir}) all'inizio di
ogni build e ogni esecuzione del server di sviluppo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| opzioni | OpzioniSeoPlugin |  |

#### Contrarre

- **@esempio:** ```ts
import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
  plugins: [
    seoPlugin({
      sitemap: { urls: [{ loc: 'https://example.com/' }] },
      robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
    }),
  ],
});
```

### seoPlugin

**Tipo:** funzione

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
```

Crea un plugin Vite che scrive `robots.txt` e `sitemap.xml` nel file
`publicDir` del progetto (o {@link SeoPluginOptions.outDir}) all'inizio di
ogni build e ogni esecuzione del server di sviluppo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| opzioni | OpzioniSeoPlugin |  |

#### Contrarre

- **@esempio:** ```ts
import { seoPlugin } from '@mission-platform/vite-plugin-seo';

export default defineConfig({
  plugins: [
    seoPlugin({
      sitemap: { urls: [{ loc: 'https://example.com/' }] },
      robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
    }),
  ],
});
```

### Opzioni SeoPlugin

**Tipo:** interfaccia

```typescript
export interface SeoPluginOptions
```

Opzioni per {@link seoPlugin}.
