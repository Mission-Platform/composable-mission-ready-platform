# @mission-platform/vite-plugin-seo API reference

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generated from public source declarations in `@mission-platform/vite-plugin-seo`.

## `src/index`

### default

**Kind:** function

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin;
```

Create a Vite plugin that writes `robots.txt` and `sitemap.xml` into the
project's `publicDir` (or {@link SeoPluginOptions.outDir}) at the start of
every build and dev-server run.

#### Parameters

| Name    | Type             | Description |
| ------- | ---------------- | ----------- |
| options | SeoPluginOptions |             |

#### Contract

- **@example:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';
  export default defineConfig({
  plugins: [
  seoPlugin({
  sitemap: { urls: [{ loc: 'https://example.com/' }] },
  robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
  }),
  ],
  });

````

### seoPlugin

**Kind:** function

```typescript
function seoPlugin(options: SeoPluginOptions): Plugin
````

Create a Vite plugin that writes `robots.txt` and `sitemap.xml` into the
project's `publicDir` (or {@link SeoPluginOptions.outDir}) at the start of
every build and dev-server run.

#### Parameters

| Name    | Type             | Description |
| ------- | ---------------- | ----------- |
| options | SeoPluginOptions |             |

#### Contract

- **@example:** ```ts
  import { seoPlugin } from '@mission-platform/vite-plugin-seo';
  export default defineConfig({
  plugins: [
  seoPlugin({
  sitemap: { urls: [{ loc: 'https://example.com/' }] },
  robots: { groups: [{ userAgent: '*', allow: ['/'] }] },
  }),
  ],
  });

````

### SeoPluginOptions

**Kind:** interface

```typescript
export interface SeoPluginOptions
````

Options for {@link seoPlugin}.
