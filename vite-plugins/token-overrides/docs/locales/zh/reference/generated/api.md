# @mission-platform/vite-plugin-token-overrides API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> vite-plugins/token-overrides/docs/reference/generated/api.md: [vite-plugins/token-overrides/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/vite-plugin-token-overrides` 中的公共源声明生成。

## `src/index`

### 默认

**种类：**功能

```typescript
function tokenOverridesPlugin(options: TokenOverridesPluginOptions): Plugin
```

Vite 插件，可自动生成应用程序的设计令牌*覆盖*样式表。

它读取 DTCG 样式的覆盖文档 (`options.source`)，并将其转换为
{@link buildTokenOverrideScss}，并写入生成的 `:root { --<prefix>-*: … }`
SCSS 偏向 `options.outFile`。从您的导入生成的文件
样式表 *after* `@mission-platform/tokens` 因此覆盖赢得了级联。

生成在汇总 `buildStart` 挂钩中运行（因此它涵盖了 `vite build`，
`vite build --watch`，和开发服务器启动类似）并在源启动时重新运行
文档在开发服务器运行时发生变化。生成的文件是一个构建文件
artefact — 将其添加到 `.gitignore`/`.prettierignore` 而不是提交它。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 | TokenOverridesPluginOptions | TokenOverridesPluginOptions |  |

#### 合同

- **@例子：** ```ts
// vite.config.ts
import { tokenOverridesPlugin } from '@mission-platform/vite-plugin-token-overrides';

export default defineConfig({
  plugins: [tokenOverridesPlugin({ source: 'design-tokens/overrides.tokens.json' })],
});
```
```scss
// styles.css / styles.scss
- **@import:** '@mission-platform/tokens';
- **@import:** '../design-tokens/overrides.generated.scss';
```

### tokenOverrides插件

**种类：**功能

```typescript
function tokenOverridesPlugin(options: TokenOverridesPluginOptions): Plugin
```

Vite 插件，可自动生成应用程序的设计令牌*覆盖*样式表。

它读取 DTCG 样式的覆盖文档 (`options.source`)，并将其转换为
{@link buildTokenOverrideScss}，并写入生成的 `:root { --<prefix>-*: … }`
SCSS 偏向 `options.outFile`。从您的导入生成的文件
样式表 *after* `@mission-platform/tokens` 因此覆盖赢得了级联。

生成在汇总 `buildStart` 挂钩中运行（因此它涵盖了 `vite build`，
`vite build --watch`，和开发服务器启动类似）并在源启动时重新运行
文档在开发服务器运行时发生变化。生成的文件是一个构建文件
artefact — 将其添加到 `.gitignore`/`.prettierignore` 而不是提交它。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 | TokenOverridesPluginOptions | TokenOverridesPluginOptions |  |

#### 合同

- **@例子：** ```ts
// vite.config.ts
import { tokenOverridesPlugin } from '@mission-platform/vite-plugin-token-overrides';

export default defineConfig({
  plugins: [tokenOverridesPlugin({ source: 'design-tokens/overrides.tokens.json' })],
});
```
```scss
// styles.css / styles.scss
- **@import:** '@mission-platform/tokens';
- **@import:** '../design-tokens/overrides.generated.scss';
```

### TokenOverrides插件选项

**种类：**接口

```typescript
export interface TokenOverridesPluginOptions
```

{@link tokenOverridesPlugin} 的选项。

## `src/transform`

### buildTokenOverrideScss

**种类：**功能

```typescript
function buildTokenOverrideScss(document_: OverrideGroup, options: TokenOverrideScssOptions = {}): string
```

构建 SCSS/CSS 部分覆盖：单个 `:root { … }` 块
`--<prefix>-*` 自定义属性是从 `document_` 扁平化的。 *之后*导入它
基本 `@mission-platform/tokens` 因此声明赢得了级联。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|文件_ |覆盖组 |  |
|选项 | TokenOverrideScssOptions | TokenOverrideScssOptions |  |

### 平面覆盖

**种类：**接口

```typescript
export interface FlatOverride
```

扁平化覆盖准备作为单个自定义属性声明发出。

### 展平覆盖

**种类：**功能

```typescript
function flattenOverrides(document_: OverrideGroup, prefix = 'mp'): FlatOverride[]
```

递归地将覆盖文档展平为 {@link FlatOverride}。按键
以 `递归地将覆盖文档展平为 {@link FlatOverride}。按键
以 （DTCG 元数据）开头的被跳过；每片剩下的叶子都变成了
`--<prefix>-<path-joined-by-dashes>` 自定义属性。组件覆盖
路径保留其 `component.*` DTCG 包装器，但生成的 CSS 忽略了这一点
包装器（`component.button.*` 变为 `--<prefix>-button-*`）。
 `$`
#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|文件_ |覆盖组 |  |
|前缀 |  |  |

### 明暗值

**种类：**接口

```typescript
export interface LightDarkValue
```

浅色和深色配色方案之间不同的颜色（或任何值）。

### 覆盖组

**种类：**类型

```typescript
export type OverrideGroup = Record<string, unknown>;
```

DTCG 样式覆盖组 node（嵌套组/令牌加上可选的 `DTCG 样式覆盖组 node（嵌套组/令牌加上可选的  元数据）。
 `$`
### 覆盖令牌

**种类：**接口

```typescript
export interface OverrideToken
```

DTCG 样式覆盖叶令牌。

### 覆盖值

**种类：**类型

```typescript
export type OverrideValue = string | number | LightDarkValue;
```

单个覆盖 `$value` — 标量或方案感知 `{ light, dark }` 对。

### TokenOverrideScssOptions

**种类：**接口

```typescript
export interface TokenOverrideScssOptions
```

{@link buildTokenOverrideScss} 的选项。
