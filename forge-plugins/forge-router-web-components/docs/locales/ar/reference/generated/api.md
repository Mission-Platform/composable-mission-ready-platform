# مرجع @mission-platform/forge-router-web-components API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-router-web-components/docs/reference/generated/api.md: [forge-plugins/forge-router-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-router-web-components`.

## `src/runtime/elements`

### MpRouterLinkElement

**النوع:** مكون

```typescript
export class MpRouterLinkElement extends HTMLElement
```

يتم تقديم رابط جهاز توجيه محايد كعنصر ربط عادي يمكن الوصول إليه.

### MpRouterOutletElement

**النوع:** مكون

```typescript
export class MpRouterOutletElement extends HTMLElement
```

منفذ جهاز التوجيه الذي يقوم بتثبيت عرض DOM المحايد للمسار. قد تمر التطبيقات أ
`viewAdapter` إلى جهاز التوجيه عندما تحتاج العروض إلى دورة حياة مخصصة؛ وإلا أ
يتم عرض Node أو مصنع DOM في `route.component` مباشرة.

### import org.registerRouterElements

**النوع:** الوظيفة

```typescript
function registerRouterElements(options: RegisterRouterElementsOptions = {}): void;
```

قم بتسجيل أساسيات جهاز التوجيه مرة واحدة؛ آمن للاستدعاء من الترطيب والاختبارات.

#### حدود

| الاسم  | اكتب                        | الوصف |
| ------ | --------------------------- | ----- |
| خيارات | تسجيل خيارات RouterElements |       |

## `src/runtime/history`

### createBrowserHistory

**النوع:** الوظيفة

```typescript
function createBrowserHistory(win?: Window): MpBrowserHistory;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب  | الوصف |
| ----- | ----- | ----- |
| فوز   | نافذة |       |

### createMemoryHistory

**النوع:** الوظيفة

```typescript
function createMemoryHistory(initialUrl = '/'): MpMemoryHistory;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم            | اكتب | الوصف |
| ---------------- | ---- | ----- |
| عنوان URL الأولي |      |       |

### createWebHistory

**النوع:** الوظيفة

```typescript
function createWebHistory(): MpHistory;
```

لم يتم تقديم أي وصف.

### MpBrowserHistory

**النوع:** مكون

```typescript
export class MpBrowserHistory implements MpHistory
```

تاريخ متصفح HTML5. يتم إنشاؤه فقط في سياق المتصفح.

### MpMemoryHistory

**النوع:** مكون

```typescript
export class MpMemoryHistory implements MpHistory
```

تنفيذ التاريخ الحتمي للاختبارات، SSR، والعرض المسبق.

## `src/runtime/index`

### com.ForgeRouterLink

**النوع:** ثابت

```typescript
export const ForgeRouterLink;
```

لم يتم تقديم أي وصف.

#### عقد

- **@مهمل:** تفضل {@link MpLink}; تم الاحتفاظ بها للمستهلكين الحاليين في وقت التشغيل.

### ForgeRouterOutlet

**النوع:** ثابت

```typescript
export const ForgeRouterOutlet;
```

لم يتم تقديم أي وصف.

#### عقد

- **@deprecated:** تفضل {@link MpRouterView}; تم الاحتفاظ بها للمستهلكين الحاليين في وقت التشغيل.

### مبلينك

**النوع:** مكون

```typescript
export MpLink = 'forge-router-link'
```

علامة العنصر المخصص المستخدمة للارتباطات المحايدة المجمعة.

### MpRouterView

**النوع:** مكون

```typescript
export MpRouterView = 'forge-router-outlet'
```

علامة العنصر المخصص المستخدمة للمنافذ المحايدة المجمعة.

### حل فورجي لينك

**النوع:** ثابت

```typescript
export const resolveForgeLink;
```

لم يتم تقديم أي وصف.

#### عقد

- **@مهمل:** تفضل {@link ResolveMpLink}.

### ResolveMpLink

**النوع:** الوظيفة

```typescript
function resolveMpLink(to: MpRouteLocationRaw): string;
```

قم بحل هدف محايد من خلال جهاز توجيه Web Components المرتبط.

#### حدود

| الاسم | اكتب             | الوصف |
| ----- | ---------------- | ----- |
| إلى   | مبرووتلوكيشن راو |       |

### setForgeRouter

**النوع:** الوظيفة

```typescript
function setForgeRouter(router: MpWebComponentsRouter<View>): void;
```

قم بربط جهاز التوجيه المملوك للتطبيق للحصول على إمكانات الحزمة المترجمة الخالية من إطار العمل.

#### حدود

| الاسم | اكتب                        | الوصف |
| ----- | --------------------------- | ----- |
| راوتر | MpWebComponentsRouter<View> |       |

### useForgeNavigation

**النوع:** ثابت

```typescript
export const useForgeNavigation;
```

لم يتم تقديم أي وصف.

#### عقد

- **@مهمل:** تفضل {@link useMpNavigation}.

### useForgeRoute

**النوع:** ثابت

```typescript
export const useForgeRoute;
```

لم يتم تقديم أي وصف.

#### عقد

- **@مهمل:** تفضل {@link useMpRoute}.

### useForgeRouter

**النوع:** ثابت

```typescript
export const useForgeRouter;
```

لم يتم تقديم أي وصف.

#### عقد

- **@مهمل:** تفضل {@link useMpRouter}.

### useMpNavigation

**النوع:** الوظيفة

```typescript
function useMpNavigation(): Pick<MpRouterCapabilities, 'navigate' | 'resolve'>;
```

اقرأ فقط قدرات التنقل/الحل الضرورية.

### useMpRoute

**النوع:** الوظيفة

```typescript
function useMpRoute(): MpWebComponentsRouter<unknown>['current']['value'];
```

اقرأ المسار الحالي المملوك للتطبيق كموقع محايد.

### useMpRouter

**النوع:** الوظيفة

```typescript
function useMpRouter(): MpRouterCapabilities;
```

اقرأ كائن القدرة المحايدة المدعوم بوقت تشغيل Web Components.

## `src/runtime/router`

### createWebComponentsRouter

**النوع:** الوظيفة

```typescript
function createWebComponentsRouter(options: MpWebComponentsRouterOptions<View>): MpWebComponentsRouter<View>;
```

قم بإنشاء وقت تشغيل كامل لجهاز التوجيه بدون إطار عمل. مطابقة المسار وعنوان URL
يظل القرار مفوضًا إلى `@mission-platform/router`؛ تمتلك هذه الفئة
السجل والحراس وعمليات إعادة التوجيه والاشتراكات وسلوك تمرير المتصفح.

#### حدود

| الاسم  | اكتب                               | الوصف |
| ------ | ---------------------------------- | ----- |
| خيارات | MpWebComponentsRouterOptions<View> |       |

### MpRouterLoadingFallback

**النوع:** مكون

```typescript
export type MpRouterLoadingFallback = Node | string | (() => Node | string);
```

المحتوى الذي يتم عرضه بواسطة منفذ جهاز التوجيه أثناء تحميل طريقة عرض الوجهة.

### MpWebComponentsRouter

**النوع:** مكون

```typescript
export interface MpWebComponentsRouter<View = unknown> extends MpRouterAdapter
```

لم يتم تقديم أي وصف.

### MpWebComponentsRouterOptions

**النوع:** مكون

```typescript
export interface MpWebComponentsRouterOptions<View = unknown>
```

لم يتم تقديم أي وصف.

### RouteHref

**النوع:** الوظيفة

```typescript
function routeHref(router: MpRouterAdapter, to: MpRouteLocationRaw): string;
```

لم يتم تقديم أي وصف.

#### حدود

| الاسم | اكتب             | الوصف |
| ----- | ---------------- | ----- |
| راوتر | م راوتر أدابتر   |       |
| إلى   | مبرووتلوكيشن راو |       |

## `src/utils/forge-router-web-components`

### com.forgeRouterWebComponents

**النوع:** ثابت

```typescript
export const forgeRouterWebComponents;
```

صياغة هدف جهاز التوجيه مدعومًا بوقت تشغيل/منفذ خالٍ من الإطار.
