# @mission-platform/forge-router-plugin-api API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> forge-plugins/forge-router-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-router-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

공개 소스 선언에서 생성됨 `@mission-platform/forge-router-plugin-api`.

## `src/router`

### createRouter진단

**종류:** 기능

```typescript
function createRouterDiagnostic(
  diagnostic: Omit<CompilerDiagnostic, "phase" | "fileName"> & {
    readonly fileName?: string;
  },
): CompilerDiagnostic;
```

라우터 컴파일러 오류에 대한 소스-로컬 진단을 만듭니다.

#### 매개변수

| 이름 | 유형                                                                                | 설명 |
| ---- | ----------------------------------------------------------------------------------- | ---- |
| 진단 | 생략<CompilerDiagnostic, 'phase' \| 'fileName'> & { 읽기 전용 파일 이름?: 문자열; } |      |

### 정의ForgeRouter플러그인

**종류:** 기능

```typescript
function defineForgeRouterPlugin(plugin: T): T;
```

라우터 플러그인 메타데이터가 컴파일러 파이프라인에 들어가기 전에 유효성을 검사합니다.

#### 매개변수

| 이름     | 유형 | 설명 |
| -------- | ---- | ---- |
| 플러그인 | 티   |      |

### DefineForgeRouterTarget

**종류:** 기능

```typescript
function defineForgeRouterTarget(
  options: ForgeRouterTargetOptions,
): RouterOutputPlugin;
```

중립 라우터 가져오기를 다시 작성하는 결정적 대상을 만듭니다.

{@link ForgeRouterTargetOptions.runtimeModule}을 선호합니다. 호출 사이트를 유지합니다.
(`useMpRouter().navigate`, `useMpRoute().query`, ...) 형태 호환 가능
다른 기본 후크 대신 대상 런타임 도우미 가져오기
서명. 대상은 여전히 사용자 정의 하위/생성 단계를 제공할 수 있습니다.
파일 기반 또는 서버 전용 라우터.

#### 매개변수

| 이름 | 유형                  | 설명 |
| ---- | --------------------- | ---- |
| 옵션 | ForgeRouterTarget옵션 |      |

### FORGE_ROUTER_RUNTIME_EXPORTS

**종류:** 상수

```typescript
export const FORGE_ROUTER_RUNTIME_EXPORTS;
```

패키지 작성자 계약을 유지하는 중립 런타임 내보내기입니다.

네이티브 후크의 이름을 1:1로 바꿀 수 없는 대상(예: React's
`useNavigate` 대 중립 `useMpRouter().navigate`) 이 이름을 공개해야 합니다
대상 런타임 모듈에서 {@link ForgeRouterTargetOptions.runtimeModule}을 가리킵니다.
그 모듈에서.

### forgeRouterExtension계약

**종류:** 상수

```typescript
export const forgeRouterExtensionContracts: readonly RouterTargetExtensionContract[];
```

자사 어댑터가 이 마일스톤의 일부가 아닌 라우터에 대해 예약된 대상 계약입니다.

### ForgeRouterTarget옵션

**종류:** 인터페이스

```typescript
export interface ForgeRouterTargetOptions
```

표준 소스 재작성 라우터 대상 팩토리에 대한 구성입니다.

### 생성된Router선언

**종류:** 인터페이스

```typescript
export interface GeneratedRouterDeclaration
```

생성된 라우터 모듈 옆에 내보내는 선언입니다.

### 생성된 라우터 모듈

**종류:** 인터페이스

```typescript
export interface GeneratedRouterModule
```

라우터 대상에서 반환된 생성된 소스 모듈입니다.

### MP_ROUTER_COMPILER_MARKER

**종류:** 상수

```typescript
export const MP_ROUTER_COMPILER_MARKER;
```

중립 라우터 패키지와 공유되는 안정적인 마커입니다.

### MP_ROUTER_MODULE

**종류:** 상수

```typescript
export const MP_ROUTER_MODULE;
```

라우터 컴파일러 패스에서 가져오기를 이해하는 패키지입니다.

### 라우터빌드어댑터

**종류:** 인터페이스

```typescript
export interface RouterBuildAdapters
```

라우터 대상이 소유한 후크를 구축합니다. 코어에 의해 라우터 종속성이 로드되지 않습니다.

### 라우터 기능

**종류:** 유형

```typescript
export type RouterCapability =
  "link" | "route" | "navigate" | "resolve" | "view";
```

중립 라우터 계약에 의해 노출되는 독립적으로 낮은 작업이 가능합니다.

### 라우터기능가져오기

**종류:** 인터페이스

```typescript
export interface RouterCapabilityImport
```

선택한 라우터 대상에 의해 다시 작성될 수 있는 중립 가져오기입니다.

### RouterCapability모듈

**종류:** 인터페이스

```typescript
export interface RouterCapabilityModule
```

중립 IR이 라우터 대상 플러그인에 전달되었습니다. 소비에 대해서만 설명합니다.

### 라우터기능사용

**종류:** 인터페이스

```typescript
export interface RouterCapabilityUse
```

진단을 위한 소스 좌표를 유지하는 중립 라우터 마커를 한 번 사용합니다.

### 라우터능력사용종류

**종류:** 유형

```typescript
export type RouterCapabilityUseKind = "call" | "jsx" | "reference";
```

소스 모듈에서 중립 라우터 바인딩이 사용되는 방법.

### 라우터네이티브가져오기

**종류:** 인터페이스

```typescript
export interface RouterNativeImport
```

하나의 중립 라우터 마커를 대체하는 데 사용되는 기본 가져오기입니다.

### 라우터최적화옵션

**종류:** 인터페이스

```typescript
export interface RouterOptimizeOptions
```

라우터 최적화 프로그램이 공유하는 중립 옵션입니다.

### 라우터출력플러그인

**종류:** 인터페이스

```typescript
export interface RouterOutputPlugin
```

기본 라우터 대상을 위한 Forge 스타일 컴파일러 플러그인입니다.

### 라우터플러그인선택

**종류:** 유형

```typescript
export type RouterPluginSelection = RouterOutputPlugin | string;
```

라우터 플러그인은 직접 제공되거나 안정적인 ID로 선택될 수 있습니다.

### RouterTargetContext

**종류:** 인터페이스

```typescript
export interface RouterTargetContext
```

라우터 대상과 독립적으로 선택된 앱/프레임워크 컨텍스트입니다.

### RouterTargetExtension계약

**종류:** 인터페이스

```typescript
export interface RouterTargetExtensionContract
```

어댑터를 배송하지 않고 미래의 라우터 대상이 광고하는 계약입니다.

### 라우터대상 계획

**종류:** 인터페이스

```typescript
export interface RouterTargetPlan
```

하강과 생성 사이에 생산되는 대상별 계획.

### selectForgeRouter플러그인

**종류:** 기능

```typescript
function selectForgeRouterPlugin(
  selection: RouterPluginSelection | undefined,
  plugins: readonly RouterOutputPlugin[] = [],
): RouterOutputPlugin | undefined;
```

UI 플러그인에 연결하지 않고 직접 또는 ID가 선택한 라우터 대상을 찾습니다.

#### 매개변수

| 이름     | 유형                                  | 설명 |
| -------- | ------------------------------------- | ---- |
| 선택     | 라우터 플러그인 선택 \| 정의되지 않음 |      |
| 플러그인 | 읽기 전용 RouterOutputPlugin[]        |      |

### 지원되지 않는 라우터 기능

**종류:** 기능

```typescript
function unsupportedRouterCapabilities(
  ir: RouterCapabilityModule,
  plugin: RouterOutputPlugin | undefined,
): readonly CompilerDiagnostic[];
```

대상이 자동으로 모듈을 잘못 컴파일하기 전에 기능 격차를 보고합니다.

#### 매개변수

| 이름     | 유형                                | 설명 |
| -------- | ----------------------------------- | ---- |
| IR       | RouterCapability모듈                |      |
| 플러그인 | RouterOutputPlugin \| 정의되지 않음 |      |
