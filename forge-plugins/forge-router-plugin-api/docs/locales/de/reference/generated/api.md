# @mission-platform/forge-router-plugin-api API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> forge-plugins/forge-router-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-router-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-router-plugin-api`.

## `src/router`

### createRouterDiagnostic

**Art:** Funktion

```typescript
function createRouterDiagnostic(diagnostic: Omit<CompilerDiagnostic, 'phase' | 'fileName'> & {
    readonly fileName?: string;
  }): CompilerDiagnostic
```

Erstellen Sie eine quelllokale Diagnose für einen Router-Compiler-Fehler.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Diagnose | Lassen Sie <CompilerDiagnostic, 'phase' \| 'fileName'> & { readonly fileName?: string;   } |  |

### defineForgeRouterPlugin

**Art:** Funktion

```typescript
function defineForgeRouterPlugin(plugin: T): T
```

Validieren Sie die Metadaten des Router-Plugins, bevor sie in eine Compiler-Pipeline gelangen.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Plugin | T |  |

### defineForgeRouterTarget

**Art:** Funktion

```typescript
function defineForgeRouterTarget(options: ForgeRouterTargetOptions): RouterOutputPlugin
```

Erstellen Sie ein deterministisches Ziel, das neutrale Routerimporte neu schreibt.

Bevorzugen Sie {@link ForgeRouterTargetOptions.runtimeModule}: Es behält Anrufseiten bei
(`useMpRouter().navigate`, `useMpRoute().query`, …) formkompatibel von
Importieren von Ziel-Laufzeithilfsmitteln anstelle bloßer nativer Hooks mit unterschiedlichen Zielen
Unterschriften. Ziele können weiterhin benutzerdefinierte niedrigere/generierte Phasen bereitstellen
dateibasierte oder reine Server-Router.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Optionen | ForgeRouterTargetOptions |  |

### FORGE_ROUTER_RUNTIME_EXPORTS

**Art:** konstant

```typescript
export const FORGE_ROUTER_RUNTIME_EXPORTS
```

Neutrale Laufzeitexporte, die den Paketautorenvertrag beibehalten.

Ziele, die native Hooks nicht 1:1 umbenennen können (z. B. React).
`useNavigate` vs. neutrales `useMpRouter().navigate`) müssen diese Namen offenlegen
von einem Ziellaufzeitmodul und Punkt {@link ForgeRouterTargetOptions.runtimeModule}
in diesem Modul.

### forgeRouterExtensionContracts

**Art:** konstant

```typescript
export const forgeRouterExtensionContracts: readonly RouterTargetExtensionContract[]
```

Reservierte Zielverträge für Router, deren Erstanbieteradapter nicht Teil dieses Meilensteins sind.

### ForgeRouterTargetOptions

**Art:** Schnittstelle

```typescript
export interface ForgeRouterTargetOptions
```

Konfiguration für die Standard-Source-Rewriting-Router-Zielfabrik.

### GenerierteRouterDeclaration

**Art:** Schnittstelle

```typescript
export interface GeneratedRouterDeclaration
```

Eine Deklaration, die neben einem generierten Routermodul ausgegeben wird.

### GeneriertesRouterModule

**Art:** Schnittstelle

```typescript
export interface GeneratedRouterModule
```

Ein generiertes Quellmodul, das von einem Router-Ziel zurückgegeben wird.

### MP_ROUTER_COMPILER_MARKER

**Art:** konstant

```typescript
export const MP_ROUTER_COMPILER_MARKER
```

Stabiler Marker, der mit dem neutralen Router-Paket geteilt wird.

### MP_ROUTER_MODULE

**Art:** konstant

```typescript
export const MP_ROUTER_MODULE
```

Das Paket, dessen Importe vom Router-Compiler-Durchlauf verstanden werden.

### RouterBuildAdapters

**Art:** Schnittstelle

```typescript
export interface RouterBuildAdapters
```

Erstellen Sie Hooks, die einem Router-Ziel gehören. Es wird keine Router-Abhängigkeit vom Kern geladen.

### RouterCapability

**Art:** Typ

```typescript
export type RouterCapability = 'link' | 'route' | 'navigate' | 'resolve' | 'view';
```

Unabhängig absenkbare Vorgänge, die durch den neutralen Router-Vertrag offengelegt werden.

### RouterCapabilityImport

**Art:** Schnittstelle

```typescript
export interface RouterCapabilityImport
```

Ein neutraler Import, der von einem ausgewählten Router-Ziel neu geschrieben werden kann.

### RouterCapabilityModule

**Art:** Schnittstelle

```typescript
export interface RouterCapabilityModule
```

Neutrale IR wird an Router-Ziel-Plugins weitergeleitet. Es beschreibt nur den Verbrauch.

### RouterCapabilityUse

**Art:** Schnittstelle

```typescript
export interface RouterCapabilityUse
```

Eine Verwendung eines neutralen Router-Markers, wobei die Quellkoordinaten für die Diagnose beibehalten werden.

### RouterCapabilityUseKind

**Art:** Typ

```typescript
export type RouterCapabilityUseKind = 'call' | 'jsx' | 'reference';
```

Wie eine neutrale Routerbindung in einem Quellmodul verwendet wird.

### RouterNativeImport

**Art:** Schnittstelle

```typescript
export interface RouterNativeImport
```

Ein nativer Import, der zum Ersetzen eines neutralen Router-Markers verwendet wird.

### RouterOptimizeOptions

**Art:** Schnittstelle

```typescript
export interface RouterOptimizeOptions
```

Neutrale Optionen, die von Router-Optimierern geteilt werden.

### RouterOutputPlugin

**Art:** Schnittstelle

```typescript
export interface RouterOutputPlugin
```

Compiler-Plugin im Forge-Stil für ein natives Router-Ziel.

### RouterPluginSelection

**Art:** Typ

```typescript
export type RouterPluginSelection = RouterOutputPlugin | string;
```

Ein Router-Plugin kann direkt bereitgestellt oder über seine stabile ID ausgewählt werden.

### RouterTargetContext

**Art:** Schnittstelle

```typescript
export interface RouterTargetContext
```

Der App-/Framework-Kontext, der unabhängig vom Router-Ziel ausgewählt wird.

### RouterTargetExtensionContract

**Art:** Schnittstelle

```typescript
export interface RouterTargetExtensionContract
```

Ein Vertrag, der von einem zukünftigen Router-Ziel ohne Versand seines Adapters angekündigt wird.

### RouterTargetPlan

**Art:** Schnittstelle

```typescript
export interface RouterTargetPlan
```

Zielgenauer Plan zwischen Absenken und Generieren erstellt.

### Wählen SieForgeRouterPlugin

**Art:** Funktion

```typescript
function selectForgeRouterPlugin(selection: RouterPluginSelection | undefined, plugins: readonly RouterOutputPlugin[] = []): RouterOutputPlugin | undefined
```

Finden Sie ein direktes oder per ID ausgewähltes Routerziel, ohne es an UI-Plugins zu koppeln.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Auswahl | RouterPluginSelection \| undefiniert |  |
| Plugins | schreibgeschützt RouterOutputPlugin[] |  |

### nicht unterstützte RouterCapabilities

**Art:** Funktion

```typescript
function unsupportedRouterCapabilities(ir: RouterCapabilityModule, plugin: RouterOutputPlugin | undefined): readonly CompilerDiagnostic[]
```

Melden Sie Funktionslücken, bevor ein Ziel die Möglichkeit hat, ein Modul stillschweigend falsch zu kompilieren.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| ir | RouterCapabilityModule |  |
| Plugin | RouterOutputPlugin \| undefiniert |  |
