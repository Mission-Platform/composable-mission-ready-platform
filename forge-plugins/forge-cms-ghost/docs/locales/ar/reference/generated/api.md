# @mission-platform/forge-cms-ghost مرجع واجهة برمجة التطبيقات

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-cms-ghost/docs/reference/generated/api.md: [forge-plugins/forge-cms-ghost/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-cms-ghost`.

## `src/diagnostics`

### FORGE_GHOST_FIELD_UNSUPPORTED

**النوع:** ثابت

```typescript
export const FORGE_GHOST_FIELD_UNSUPPORTED
```

لا يستطيع الشبح من النوع الميداني التعبير أصلاً؛ فإنه يتحلل إلى النص المقدم.

### FORGE_GHOST_SETTING_LIMIT

**النوع:** ثابت

```typescript
export const FORGE_GHOST_SETTING_LIMIT
```

أكثر `@cmsSetting` تم توقع الحقول من حقول Ghost `config.custom` يسمح.

### ghostWarning

**النوع:** الوظيفة

```typescript
function ghostWarning(code: string, message: string, fileName: string): CompilerDiagnostic
```

إنشاء تحذير لمرحلة الجيل؛ الهدف الشبح لا يصدر أخطاء أبدًا.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| كود | سلسلة |  |
| رسالة | سلسلة |  |
| اسم الملف | سلسلة |  |

## `src/ghost`

### com.forgeGhostCms

**النوع:** الوظيفة

```typescript
function forgeGhostCms(options: ForgeGhostCmsOptions): CmsOutputPlugin
```

ربط عرض سمة Ghost بمكوّن إضافي لإخراج إطار عمل مملوك للمتصل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | فورججوستكمسوبتيونس |  |

### ForgeGhostCmsOptions

**النوع:** الواجهة

```typescript
export interface ForgeGhostCmsOptions
```

خيارات {@link forgeGhostCms}.

## `src/manifest`

### buildGhostThemeConfig

**النوع:** الوظيفة

```typescript
function buildGhostThemeConfig(components: readonly ContentComponent[], themeName: string, diagnostics: CompilerDiagnostic[]): GhostThemeConfig
```

بناء `config.custom` قطعة من كل `@cmsSetting`-الحقل الموسوم.

الإعدادات موجودة على مستوى الموقع، لذا يتم مفتاحها بواسطة الخاصية Snake_cased الخاصة بالحقل
الاسم وليس حسب المكون: يتم وصف مكونين يضعان علامات على نفس الدعامة
نفس المقبض، ولدى Ghost مساحة اسم واحدة فقط لهم.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مكونات | للقراءة فقط ContentComponent[] |  |
| اسم الموضوع | سلسلة |  |
| التشخيص | مترجم التشخيص[] |  |

### DEFAULT_GHOST_THEME_NAME

**النوع:** ثابت

```typescript
export const DEFAULT_GHOST_THEME_NAME
```

اسم السمة الافتراضي المستخدم عندما لا يقدم المتصل اسمًا.

### emitGhostComponentsManifest

**النوع:** الوظيفة

```typescript
function emitGhostComponentsManifest(components: readonly ContentComponent[]): string
```

قم بإصدار عقد المعلمة الجزئية كـ JSON مطبوع بشكل جميل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مكونات | للقراءة فقط ContentComponent[] |  |

### emitGhostThemeConfig

**النوع:** الوظيفة

```typescript
function emitGhostThemeConfig(components: readonly ContentComponent[], themeName: string, diagnostics: CompilerDiagnostic[]): string
```

تنبعث منها `config.custom` جزء مثل JSON مطبوع بشكل جميل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مكونات | للقراءة فقط ContentComponent[] |  |
| اسم الموضوع | سلسلة |  |
| التشخيص | مترجم التشخيص[] |  |

### GHOST_COMPONENTS_MANIFEST

**النوع:** ثابت

```typescript
export const GHOST_COMPONENTS_MANIFEST
```

الملف الذي تم كتابة عقد المعلمة الجزئية إليه.

### GHOST_SETTING_LIMIT

**النوع:** ثابت

```typescript
export const GHOST_SETTING_LIMIT
```

الحد الأقصى لعدد الإدخالات التي يقبلها Ghost `config.custom`.

يرفض Ghost السمة التي تتجاوزها في وقت التحميل، والتي من شأنها أن تتحول إلى
تتحول لعبة Forge الناجحة إلى فشل لا يكتشفه المؤلف إلا في Ghost
المشرف - ومن هنا الاقتطاع والتحذير هنا.

### GHOST_THEME_CONFIG_MANIFEST

**النوع:** ثابت

```typescript
export const GHOST_THEME_CONFIG_MANIFEST
```

الملف `config.custom` تتم كتابة جزء ل.

### GhostComponentEntry

**النوع:** الواجهة

```typescript
export interface GhostComponentEntry
```

دخول مكون واحد `forge-components.json`.

### GhostComponentsManifest

**النوع:** الواجهة

```typescript
export interface GhostComponentsManifest
```

كامل العقد الجزئي المعلمة.

### GhostParameter

**النوع:** الواجهة

```typescript
export interface GhostParameter
```

معلمة تجزئة واحدة لجزئي تم إنشاؤه.

### إعداد الشبح

**النوع:** الواجهة

```typescript
export interface GhostSetting
```

إدخال واحد للموضوع `config.custom` حاجز.

### GhostSettingType

**النوع:** الوظيفة

```typescript
function ghostSettingType(field: ContentField): GhostSettingType
```

نوع إعداد Ghost هو نوع حقل محايد يضيق عليه.

`color` جزء من مفردات Ghost ولكن لا يوجد نوع محايد لها: أ
اللون هو سلسلة عادية في TypeScript والتخمين من اسم الدعامة
من شأنه أن يجعل الإسقاط غير حتمية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المجال | حقل المحتوى |  |

### GhostSettingType

**النوع:** النوع

```typescript
export type GhostSettingType = "select" | "boolean" | "color" | "image" | "text";
```

الإعدادات الخمسة هي Ghost's `config.custom` تقبل الكتلة.

### GhostSlot

**النوع:** الواجهة

```typescript
export interface GhostSlot
```

فتحة واحدة للجزئي الذي تم إنشاؤه.

### GhostThemeConfig

**النوع:** الواجهة

```typescript
export interface GhostThemeConfig
```

ال `config.custom` تجزئة الموضوع `package.json` يندمج في.

### toGhostComponentEntry

**النوع:** الوظيفة

```typescript
function toGhostComponentEntry(component: ContentComponent): GhostComponentEntry
```

قم بإسقاط مكون واحد عليه `forge-components.json` دخول.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

## `src/template`

### emitGhostPartial

**النوع:** الوظيفة

```typescript
function emitGhostPartial(component: ContentComponent): string
```

قم بإصدار المقاود جزئيًا لمكون واحد.

يكون الإخراج حتميًا — تحتفظ الحقول بترتيب الإعلان وفتحاته
قم بتعقبهم - لذا فإن السمة المُعاد إنشاؤها تنتج فرقًا فارغًا عندما لا يكون هناك شيء
تغير المكون.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

### GhostPartialFileName

**النوع:** الوظيفة

```typescript
function ghostPartialFileName(component: ContentComponent): string
```

المسار النسبي للموضوع الذي تمت كتابة الجزء الجزئي إليه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

### GhostPartialName

**النوع:** الوظيفة

```typescript
function ghostPartialName(component: ContentComponent): string
```

الاسم الجزئي الذي يتم استدعاء المكون تحته (`{{> forge/badge}}`).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

### GhostTemplateDiagnostics

**النوع:** الوظيفة

```typescript
function ghostTemplateDiagnostics(component: ContentComponent, fileName: string): readonly CompilerDiagnostic[]
```

التشخيص الذي ينبعث منه إنتاج جزئي لمكون واحد.

فقط `number` تم الإبلاغ عن: Ghost ليس له نوع رقمي في أي مكان في موضوعه
واجهة برمجة التطبيقات (API)، بحيث تبقى القيمة كنص معروض وأي عملية حسابية يريدها الموضوع
يجب أن يحدث ذلك قبل استدعاء الجزء الجزئي.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |
| اسم الملف | سلسلة |  |
