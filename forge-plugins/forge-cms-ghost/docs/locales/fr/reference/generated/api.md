# @mission-platform/forge-cms-ghost Référence API

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> forge-plugins/forge-cms-ghost/docs/reference/generated/api.md: [forge-plugins/forge-cms-ghost/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir de déclarations de source publique dans `@mission-platform/forge-cms-ghost`.

## `src/diagnostics`

### FORGE_GHOST_FIELD_UNSUPPORTED

**Genre :** constante

```typescript
export const FORGE_GHOST_FIELD_UNSUPPORTED
```

Un fantôme de type champ ne peut pas s'exprimer de manière native ; il se dégrade en texte rendu.

### FORGE_GHOST_SETTING_LIMIT

**Genre :** constante

```typescript
export const FORGE_GHOST_SETTING_LIMIT
```

Plus `@cmsSetting` les champs étaient projetés que ceux de Ghost `config.custom` permet.

### fantômeAvertissement

**Genre :** fonction

```typescript
function ghostWarning(code: string, message: string, fileName: string): CompilerDiagnostic
```

Créer un avertissement de phase de génération ; la cible Ghost n'émet jamais d'erreurs.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| codes | chaîne |  |
| messages | chaîne |  |
| Nom du fichier | chaîne |  |

## `src/ghost`

### forgeGhostCms

**Genre :** fonction

```typescript
function forgeGhostCms(options: ForgeGhostCmsOptions): CmsOutputPlugin
```

Liez la projection du thème Ghost à un plugin de sortie de framework appartenant à l'appelant.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| options | ForgeGhostCmsOptions |  |

### ForgeGhostCmsOptions

**Genre :** interface

```typescript
export interface ForgeGhostCmsOptions
```

Options pour {@link forgeGhostCms}.

## `src/manifest`

### buildGhostThemeConfig

**Genre :** fonction

```typescript
function buildGhostThemeConfig(components: readonly ContentComponent[], themeName: string, diagnostics: CompilerDiagnostic[]): GhostThemeConfig
```

Construire le `config.custom` fragment de chaque `@cmsSetting`-champ balisé.

Les paramètres s'appliquent à l'ensemble du site, ils sont donc saisis par l'accessoire Snake_cased du champ.
nom plutôt que par composant : deux composants balisant le même accessoire décrivent
le même bouton, et Ghost n'a qu'un seul espace de noms pour eux.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composants | lecture seule ContentComponent[] |  |
| nom du thème | chaîne |  |
| diagnostic | CompilateurDiagnostic[] |  |

### DEFAULT_GHOST_THEME_NAME

**Genre :** constante

```typescript
export const DEFAULT_GHOST_THEME_NAME
```

Le nom du thème par défaut utilisé lorsque l’appelant n’en fournit pas.

### émettreGhostComponentsManifest

**Genre :** fonction

```typescript
function emitGhostComponentsManifest(components: readonly ContentComponent[]): string
```

Émettez le contrat à paramètres partiels sous forme de JSON joliment imprimé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composants | lecture seule ContentComponent[] |  |

### émettreGhostThemeConfig

**Genre :** fonction

```typescript
function emitGhostThemeConfig(components: readonly ContentComponent[], themeName: string, diagnostics: CompilerDiagnostic[]): string
```

Émettre le `config.custom` fragment sous forme de JSON joliment imprimé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composants | lecture seule ContentComponent[] |  |
| nom du thème | chaîne |  |
| diagnostic | CompilateurDiagnostic[] |  |

### GHOST_COMPONENTS_MANIFEST

**Genre :** constante

```typescript
export const GHOST_COMPONENTS_MANIFEST
```

Le fichier dans lequel le contrat à paramètres partiels est écrit.

### GHOST_SETTING_LIMIT

**Genre :** constante

```typescript
export const GHOST_SETTING_LIMIT
```

Le nombre maximum d'entrées que Ghost accepte dans `config.custom`.

Ghost rejette un thème qui le dépasse au moment du téléchargement, ce qui
le succès de Forge s'est transformé en un échec que l'auteur ne découvre que dans Ghost
Admin — d'où la troncature et l'avertissement ici.

### GHOST_THEME_CONFIG_MANIFEST

**Genre :** constante

```typescript
export const GHOST_THEME_CONFIG_MANIFEST
```

Le fichier le `config.custom` le fragment est écrit.

### Entrée de composant fantôme

**Genre :** interface

```typescript
export interface GhostComponentEntry
```

L'entrée d'un composant dans `forge-components.json`.

### Manifeste des composants fantômes

**Genre :** interface

```typescript
export interface GhostComponentsManifest
```

L'ensemble du contrat à paramètres partiels.

### Paramètre fantôme

**Genre :** interface

```typescript
export interface GhostParameter
```

Un paramètre de hachage d'un partiel généré.

### Paramètres fantômes

**Genre :** interface

```typescript
export interface GhostSetting
```

Une entrée d'un thème `config.custom` bloc.

### ghostSettingType

**Genre :** fonction

```typescript
function ghostSettingType(field: ContentField): GhostSettingType
```

Le type de paramètre Ghost est un type de champ neutre qui se réduit à.

`color` fait partie du vocabulaire de Ghost mais aucun type neutre ne s'y rapporte : un
la couleur est une chaîne ordinaire dans TypeScript et deviner à partir du nom d'un accessoire
rendrait la projection non déterministe.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| champ | Champ de contenu |  |

### Type de paramètre fantôme

**Genre :** type

```typescript
export type GhostSettingType = "select" | "boolean" | "color" | "image" | "text";
```

Les cinq types de paramètres de Ghost `config.custom` le bloc accepte.

### Emplacement Fantôme

**Genre :** interface

```typescript
export interface GhostSlot
```

Un emplacement d'un partiel généré.

### GhostThemeConfig

**Genre :** interface

```typescript
export interface GhostThemeConfig
```

Le `config.custom` fragmenter un thème `package.json` fusionne.

### versGhostComponentEntry

**Genre :** fonction

```typescript
function toGhostComponentEntry(component: ContentComponent): GhostComponentEntry
```

Projeter un composant sur son `forge-components.json` entrée.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composant | Composant de contenu |  |

## `src/template`

### émettreGhostPartial

**Genre :** fonction

```typescript
function emitGhostPartial(component: ContentComponent): string
```

Émettez le partiel Guidon pour un composant.

La sortie est déterministe : les champs conservent leur ordre de déclaration et leurs emplacements.
suivez-les - donc un thème régénéré produit un diff vide quand rien ne se passe
le composant a changé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composant | Composant de contenu |  |

### ghostPartialFileName

**Genre :** fonction

```typescript
function ghostPartialFileName(component: ContentComponent): string
```

Le chemin relatif au thème dans lequel le partiel est écrit.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composant | Composant de contenu |  |

### ghostPartialName

**Genre :** fonction

```typescript
function ghostPartialName(component: ContentComponent): string
```

Le nom partiel sous lequel un composant est invoqué (`{{> forge/badge}}`).

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composant | Composant de contenu |  |

### ghostTemplateDiagnostics

**Genre :** fonction

```typescript
function ghostTemplateDiagnostics(component: ContentComponent, fileName: string): readonly CompilerDiagnostic[]
```

Les diagnostics émettant les produits partiels d'un composant.

Seulement `number` est signalé : Ghost n'a aucun type numérique dans son thème
API, de sorte que la valeur survit sous forme de texte rendu et de toute arithmétique souhaitée par un thème
cela doit se produire avant que le partiel ne soit invoqué.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| composant | Composant de contenu |  |
| Nom du fichier | chaîne |  |
