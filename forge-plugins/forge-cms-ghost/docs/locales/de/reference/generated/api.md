# @mission-platform/forge-cms-ghost API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> forge-plugins/forge-cms-ghost/docs/reference/generated/api.md: [forge-plugins/forge-cms-ghost/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-cms-ghost`.

## `src/diagnostics`

### FORGE_GHOST_FIELD_UNSUPPORTED

**Art:** konstant

```typescript
export const FORGE_GHOST_FIELD_UNSUPPORTED;
```

Ein Feldgeist kann sich nicht nativ ausdrücken; es wird zu gerendertem Text degradiert.

### FORGE_GHOST_SETTING_LIMIT

**Art:** konstant

```typescript
export const FORGE_GHOST_SETTING_LIMIT;
```

Mehr `@cmsSetting` Felder wurden projiziert als die von Ghost `config.custom` erlaubt.

### ghostWarning

**Art:** Funktion

```typescript
function ghostWarning(
  code: string,
  message: string,
  fileName: string,
): CompilerDiagnostic;
```

Erstellen Sie eine Warnung für die Generierungsphase. Das Ghost-Ziel gibt niemals Fehler aus.

#### Parameter

| Name      | Geben Sie    | ein Beschreibung |
| --------- | ------------ | ---------------- |
| Code      | Zeichenfolge |                  |
| Nachricht | Zeichenfolge |                  |
| Dateiname | Zeichenfolge |                  |

## `src/ghost`

### forgeGhostCms

**Art:** Funktion

```typescript
function forgeGhostCms(options: ForgeGhostCmsOptions): CmsOutputPlugin;
```

Binden Sie die Ghost-Designprojektion an ein Framework-Ausgabe-Plugin im Besitz des Aufrufers.

#### Parameter

| Name     | Geben Sie            | ein Beschreibung |
| -------- | -------------------- | ---------------- |
| Optionen | ForgeGhostCmsOptions |                  |

### ForgeGhostCmsOptions

**Art:** Schnittstelle

```typescript
export interface ForgeGhostCmsOptions
```

Optionen für {@link forgeGhostCms}.

## `src/manifest`

### buildGhostThemeConfig

**Art:** Funktion

```typescript
function buildGhostThemeConfig(
  components: readonly ContentComponent[],
  themeName: string,
  diagnostics: CompilerDiagnostic[],
): GhostThemeConfig;
```

Bauen Sie die `config.custom` Fragment von jedem `@cmsSetting`-getaggtes Feld.

Die Einstellungen gelten für die gesamte Website und werden daher durch die Eigenschaft „snake_cased“ des Felds festgelegt
Name statt nach Komponente: Zwei Komponenten markieren dieselbe Requisite beschreiben
den gleichen Knopf, und Ghost hat nur einen Namensraum für sie.

#### Parameter

| Name        | Geben Sie                   | ein Beschreibung |
| ----------- | --------------------------- | ---------------- |
| Komponenten | readonly ContentComponent[] |                  |
| Themenname  | Zeichenfolge                |                  |
| Diagnose    | CompilerDiagnostic[]        |                  |

### DEFAULT_GHOST_THEME_NAME

**Art:** konstant

```typescript
export const DEFAULT_GHOST_THEME_NAME;
```

Der standardmäßige Designname, der verwendet wird, wenn der Aufrufer keinen angibt.

### emitGhostComponentsManifest

**Art:** Funktion

```typescript
function emitGhostComponentsManifest(
  components: readonly ContentComponent[],
): string;
```

Geben Sie den Teilparametervertrag als schön gedrucktes JSON aus.

#### Parameter

| Name        | Geben Sie                   | ein Beschreibung |
| ----------- | --------------------------- | ---------------- |
| Komponenten | readonly ContentComponent[] |                  |

### emitGhostThemeConfig

**Art:** Funktion

```typescript
function emitGhostThemeConfig(
  components: readonly ContentComponent[],
  themeName: string,
  diagnostics: CompilerDiagnostic[],
): string;
```

Sende das aus `config.custom` Fragment als hübsch gedrucktes JSON.

#### Parameter

| Name        | Geben Sie                   | ein Beschreibung |
| ----------- | --------------------------- | ---------------- |
| Komponenten | readonly ContentComponent[] |                  |
| Themenname  | Zeichenfolge                |                  |
| Diagnose    | CompilerDiagnostic[]        |                  |

### GHOST_COMPONENTS_MANIFEST

**Art:** konstant

```typescript
export const GHOST_COMPONENTS_MANIFEST;
```

Die Datei, in die der Teilparametervertrag geschrieben wird.

### GHOST_SETTING_LIMIT

**Art:** konstant

```typescript
export const GHOST_SETTING_LIMIT;
```

Die maximale Anzahl an Einträgen, die Ghost akzeptiert `config.custom`.

Ghost lehnt ein Thema ab, das zum Zeitpunkt des Hochladens darüber hinausgeht, was zu einem Problem führen würde
Der erfolgreiche Aufbau von Forge wird zu einem Misserfolg, den der Autor erst in Ghost entdeckt
Admin – daher die Kürzung und die Warnung hier.

### GHOST_THEME_CONFIG_MANIFEST

**Art:** konstant

```typescript
export const GHOST_THEME_CONFIG_MANIFEST;
```

Die Datei `config.custom` Fragment, in das geschrieben wird.

### GhostComponentEntry

**Art:** Schnittstelle

```typescript
export interface GhostComponentEntry
```

Der Eintrag einer Komponente in `forge-components.json`.

### GhostComponentsManifest

**Art:** Schnittstelle

```typescript
export interface GhostComponentsManifest
```

Der gesamte Teilparametervertrag.

### GhostParameter

**Art:** Schnittstelle

```typescript
export interface GhostParameter
```

Ein Hash-Parameter eines generierten Teils.

### GhostSetting

**Art:** Schnittstelle

```typescript
export interface GhostSetting
```

Ein Eintrag zu einem Thema `config.custom` Block.

### ghostSettingType

**Art:** Funktion

```typescript
function ghostSettingType(field: ContentField): GhostSettingType;
```

Der Ghost-Einstellungstyp, auf den sich ein neutraler Feldtyp beschränkt.

`color` ist Teil des Ghost-Vokabulars, aber keine neutrale Art ist darauf abgebildet: a
color ist eine gewöhnliche Zeichenfolge in TypeScript und anhand des Namens einer Requisite zu raten
würde die Projektion nichtdeterministisch machen.

#### Parameter

| Name | Geben Sie    | ein Beschreibung |
| ---- | ------------ | ---------------- |
| Feld | ContentField |                  |

### GhostSettingType

**Art:** Typ

```typescript
export type GhostSettingType =
  "select" | "boolean" | "color" | "image" | "text";
```

Die fünf Einstellungstypen Ghost's `config.custom` Block akzeptiert.

### GhostSlot

**Art:** Schnittstelle

```typescript
export interface GhostSlot
```

Ein Slot eines generierten Teils.

### GhostThemeConfig

**Art:** Schnittstelle

```typescript
export interface GhostThemeConfig
```

Der `config.custom` ein Thema fragmentieren `package.json` verschmilzt mit.

### toGhostComponentEntry

**Art:** Funktion

```typescript
function toGhostComponentEntry(
  component: ContentComponent,
): GhostComponentEntry;
```

Projizieren Sie eine Komponente darauf `forge-components.json` Eintrag.

#### Parameter

| Name       | Geben Sie        | ein Beschreibung |
| ---------- | ---------------- | ---------------- |
| Komponente | ContentComponent |                  |

## `src/template`

### emitGhostPartial

**Art:** Funktion

```typescript
function emitGhostPartial(component: ContentComponent): string;
```

Geben Sie die Lenker teilweise für eine Komponente aus.

Die Ausgabe ist deterministisch – Felder behalten ihre Deklarationsreihenfolge und Slots
Verfolgen Sie sie – ein neu generiertes Thema erzeugt also ein leeres Diff, wenn nichts darüber passiert
Die Komponente hat sich geändert.

#### Parameter

| Name       | Geben Sie        | ein Beschreibung |
| ---------- | ---------------- | ---------------- |
| Komponente | ContentComponent |                  |

### ghostPartialFileName

**Art:** Funktion

```typescript
function ghostPartialFileName(component: ContentComponent): string;
```

Der themenbezogene Pfad, in den der Teil geschrieben wird.

#### Parameter

| Name       | Geben Sie        | ein Beschreibung |
| ---------- | ---------------- | ---------------- |
| Komponente | ContentComponent |                  |

### ghostPartialName

**Art:** Funktion

```typescript
function ghostPartialName(component: ContentComponent): string;
```

Der Teilname, unter dem eine Komponente aufgerufen wird (`{{> forge/badge}}`).

#### Parameter

| Name       | Geben Sie        | ein Beschreibung |
| ---------- | ---------------- | ---------------- |
| Komponente | ContentComponent |                  |

### ghostTemplateDiagnostics

**Art:** Funktion

```typescript
function ghostTemplateDiagnostics(
  component: ContentComponent,
  fileName: string,
): readonly CompilerDiagnostic[];
```

Die Diagnose gibt den Teil einer Komponente aus.

Nur `number` wird berichtet: Ghost hat nirgendwo in seinem Thema einen numerischen Typ
API, sodass der Wert als gerenderter Text und jede von einem Thema gewünschte Arithmetik erhalten bleibt
damit zu tun hat, muss geschehen, bevor der Teil aufgerufen wird.

#### Parameter

| Name       | Geben Sie        | ein Beschreibung |
| ---------- | ---------------- | ---------------- |
| Komponente | ContentComponent |                  |
| Dateiname  | Zeichenfolge     |                  |
