# @mission-platform/forge-cms-plugin-api API参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据公共来源声明生成 `@mission-platform/forge-cms-plugin-api`.

## `src/__fixtures__/components`

### 徽章

**种类：**常数

```typescript
export const BADGE
```

文字联合选项、JSDoc 描述以及 `?? literal` 默认值。

### 徽章名称

**种类：**常数

```typescript
export const badgeNames: ContentComponentNamesInput
```

没有提供描述。

### 按钮

**种类：**常数

```typescript
export const BUTTON
```

联合体降级为文本加上删除的回调道具。

### 按钮名称

**种类：**常数

```typescript
export const buttonNames: ContentComponentNamesInput
```

没有提供描述。

### 柜台

**种类：**常数

```typescript
export const COUNTER
```

交互式组件：状态和事件处理程序强制形成一个水合岛。

### 计数器名称

**种类：**常数

```typescript
export const counterNames: ContentComponentNamesInput
```

没有提供描述。

### 空的

**种类：**常数

```typescript
export const EMPTY
```

一个根本没有 props 的组件。

### 空名

**种类：**常数

```typescript
export const emptyNames: ContentComponentNamesInput
```

没有提供描述。

### 网格

**种类：**常数

```typescript
export const GRID
```

具有数字默认值的数字属性。

### 网格名称

**种类：**常数

```typescript
export const gridNames: ContentComponentNamesInput
```

没有提供描述。

### 布局

**种类：**常数

```typescript
export const LAYOUT
```

默认插槽旁边的命名插槽。

### 布局名称

**种类：**常数

```typescript
export const layoutNames: ContentComponentNamesInput
```

没有提供描述。

### 必需的

**种类：**常数

```typescript
export const REQUIRED
```

一个必需的（非可选的）道具和一个可选的道具。

### 必填名称

**种类：**常数

```typescript
export const requiredNames: ContentComponentNamesInput
```

没有提供描述。

### 站点_标题

**种类：**常数

```typescript
export const SITE_HEADER
```

具有站点范围设置道具的组件。

### 站点标题名称

**种类：**常数

```typescript
export const siteHeaderNames: ContentComponentNamesInput
```

没有提供描述。

## `src/__fixtures__/framework`

### 存根框架

**种类：**功能

```typescript
function stubFramework(id = "react"): FrameworkOutputPlugin
```

创建一个具有可重写 id 的存根框架插件。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|编号 |  |  |

## `src/analyze`

### 分析内容组件

**种类：**功能

```typescript
function analyzeContentComponent(sourceFile: ts.SourceFile, names: ContentComponentNamesInput, semantic?: SemanticModule): ContentComponent
```

分析中立组件模块，返回其平台中立内容
model：有序的可创作字段、它呈现的槽以及是否需要
水润的运行时间。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|源文件 | ts.SourceFile |  |
|名称 |内容组件名称输入 |  |
|语义|语义模块 |  |

### CMS_COLOR_TAG

**种类：**常数

```typescript
export const CMS_COLOR_TAG
```

没有提供描述。

### CMS_COLOUR_TAG

**种类：**常数

```typescript
export const CMS_COLOUR_TAG
```

源注释接受英式拼写。

### CMS_ICON_TAG

**种类：**常数

```typescript
export const CMS_ICON_TAG
```

用于可选组件级编辑器元数据的 JSDoc 标记。

### CMS_SETTING_TAG

**种类：**常数

```typescript
export const CMS_SETTING_TAG
```

将 prop 提升为站点范围 CMS 设置的 JSDoc 标记。

### CMS_TAB_TAG

**种类：**常数

```typescript
export const CMS_TAB_TAG
```

将 prop 分配给编辑器选项卡的 JSDoc 标记。

### 默认插槽字段

**种类：**常数

```typescript
export const DEFAULT_SLOT_FIELD
```

该字段键默认插槽（组件的 `children`) 暴露在下。

### 派生内容组件名称

**种类：**功能

```typescript
function deriveContentComponentNames(names: ContentComponentNamesInput): ContentComponentNames
```

从调用者提供的子集中派生完整名称集。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|名称 |内容组件名称输入 |  |

### 是交互模块

**种类：**功能

```typescript
function isInteractiveModule(semantic: SemanticModule | undefined): boolean
```

当中性 IR 携带只有真正的运行时才能提供的行为时为真。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|语义|语义模块\|未定义 |  |

## `src/classify`

### 资产类型参考

**种类：**常数

```typescript
export const ASSET_TYPE_REFERENCES: ReadonlySet<string>
```

键入将道具标记为媒体资产的引用。

### 分类字段类型

**种类：**类型

```typescript
export type ClassifiedFieldKind = ContentFieldKind | undefined;
```

对 prop 进行分类的结果 (`undefined` → 放下道具）。

### 分类类型

**种类：**功能

```typescript
function classifyType(node: ts.TypeNode, aliases: Map<string, ts.TypeNode>, seen: Set<string> = new Set()): ClassifiedFieldKind
```

对道具类型进行分类 node 转换为中性内容类型，解析本地别名/联合。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
| node | ts.TypeNode |  |
|别名 |地图<string, ts.TypeNode> |  |
|见过|放<string> |  |

### 收集类型别名

**种类：**功能

```typescript
function collectTypeAliases(sourceFile: ts.SourceFile): Map<string, ts.TypeNode>
```

映射每个顶层 `type X = …` 其类型的别名 node，用于工会决议。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|源文件 | ts.SourceFile |  |

### LINK_TYPE_REFERENCES

**种类：**常数

```typescript
export const LINK_TYPE_REFERENCES: ReadonlySet<string>
```

输入将 prop 标记为链接/URL 的引用。

### RITCHTEXT_TYPE_REFERENCES

**种类：**常数

```typescript
export const RICHTEXT_TYPE_REFERENCES: ReadonlySet<string>
```

将 prop 标记为格式化富文本而不是纯文本的类型引用。

### SLOT_TYPE_REFERENCES

**种类：**常数

```typescript
export const SLOT_TYPE_REFERENCES: ReadonlySet<string>
```

其 props 被视为嵌套内容的类型引用 (`children`).

## `src/cms`

### CMS神器

**种类：**接口

```typescript
export interface CmsArtifact
```

目标要求驱动程序写入的单个文件。

### CmsArtifactKind

**种类：**类型

```typescript
export type CmsArtifactKind = "schema" | "template" | "manifest" | "entry" | "declaration" | "module";
```

发出的工件在目标的输出树中扮演的角色。

### Cms孤岛策略

**种类：**类型

```typescript
export type CmsIslandStrategy = "none" | "framework";
```

目标如何获取组件的交互式运行时。

### Cms输出插件

**种类：**接口

```typescript
export interface CmsOutputPlugin
```

一个可组合的 CMS 输出插件。

### CmsTargetContext

**种类：**接口

```typescript
export interface CmsTargetContext
```

发射器可能需要的关于其运行的构建的一切。

### cms目标目录

**种类：**功能

```typescript
function cmsTargetDirectory(plugin: CmsOutputPlugin): string
```

目标的每框架输出写入的目录段。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|插件 | CmsOutput插件 |  |

### CMS目标ID

**种类：**类型

```typescript
export type CmsTargetId = "storyblok" | "astro" | "ghost" | "jekyll" | "webflow" | (string &
```

内置CMS目标id，开放给任何附加字符串id。

### 定义ForgeCms插件

**种类：**功能

```typescript
function defineForgeCmsPlugin(plugin: T): T
```

验证并返回 CMS 输出插件。

在配置时抛出失败——错误声明的目标绝不能
到达驾驶员处并默默地发出一棵部分树。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|插件 | T |  |

## `src/config`

### 定义ViteForgeCmsLibrary

**种类：**功能

```typescript
function defineViteForgeCmsLibrary(options: ViteForgeCmsLibraryOptions): UserConfig
```

创建一个 Vite 一个 CMS 目标的库配置。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 | ViteForgeCmsLibrary选项 |  |

### ViteForgeCmsLibrary选项

**种类：**接口

```typescript
export interface ViteForgeCmsLibraryOptions
```

{@link DefineViteForgeCmsLibrary} 的选项。

## `src/content-model`

### 内容组件

**种类：**接口

```typescript
export interface ContentComponent
```

投影到中性内容模型上的组件。

### 内容组件元数据

**种类：**接口

```typescript
export interface ContentComponentMetadata
```

组件上注释的可选平台中立元数据。

### 内容组件名称

**种类：**接口

```typescript
export interface ContentComponentNames
```

内容平台可能要求的每个名称都源自一个组件。

### 内容组件名称输入

**种类：**类型

```typescript
export type ContentComponentNamesInput = Pick< ContentComponentNames, "neutralName" | "publicName" > & Partial< Pick<ContentComponentNames, "folder" | "propertiesType" | "sourceDir"> >;
```

调用者提供的 {@link ContentComponentNames} 子集；其余的都是派生出来的。

### 内容默认值

**种类：**类型

```typescript
export type ContentDefaultValue = string | number | boolean;
```

prop 可以携带的字面默认值。

### 内容字段

**种类：**接口

```typescript
export interface ContentField
```

从组件道具投影的单个可创作字段。

### 内容字段类型

**种类：**类型

```typescript
export type ContentFieldKind = |
```

内容类型道具 TypeScript 键入映射到。

`undefined` 这里从来没有建模过——一个不能投影到
内容字段（一个回调，一个不透明的对象）被简单地删除
分类器，返回 `undefined` 而不是一种。

### 内容字段种类名称

**种类：**类型

```typescript
export type ContentFieldKindName = ContentFieldKind["kind"];
```

{@link ContentFieldKind} 的鉴别器。

### 内容字段

**种类：**功能

```typescript
function contentFields(component: ContentComponent): readonly ContentField[]
```

组件的字段不是槽（标量、可创作内容）。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|组件|内容组件 |  |

### 插槽字段

**种类：**功能

```typescript
function slotFields(component: ContentComponent): readonly ContentField[]
```

携带嵌套内容的组件的字段。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|组件|内容组件 |  |

## `src/driver`

### 生成CmsArtifacts

**种类：**功能

```typescript
function generateCmsArtifacts(options: GenerateCmsArtifactsOptions): GeneratedCmsTree
```

为一个 CMS 运行完整的发现 → IR → 内容模型 → 发出 → 写入循环
target，返回入口模块路径和写入的所有内容。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 |生成 CmsArtifacts 选项 |  |

### 生成CmsArtifacts选项

**种类：**接口

```typescript
export interface GenerateCmsArtifactsOptions
```

{@linkgenerateCmsArtifacts} 的选项。

### 生成的Cms树

**种类：**接口

```typescript
export interface GeneratedCmsTree
```

一次目标运行所产生的一切。

## `src/island`

### 生成岛

**种类：**接口

```typescript
export interface GeneratedIsland
```

生成的岛屿树，或者 `undefined` 当目标不使用岛屿时。

### 生成岛

**种类：**功能

```typescript
function generateIsland(options: GenerateIslandOptions): GeneratedIsland | undefined
```

共同生成目标的框架岛树，返回说明符
发出的模板应该通过以下方式导入它。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 |生成岛屿选项 |  |

### 生成岛屿选项

**种类：**接口

```typescript
export interface GenerateIslandOptions
```

{@linkgenerateIsland} 的选项。

### ISLAND_DIRECTORY

**种类：**常数

```typescript
export const ISLAND_DIRECTORY
```

相对于目标输出目录的目录名称，岛将生成到其中。

## `src/names`

### 显示名称

**种类：**功能

```typescript
function toDisplayName(publicName: string): string
```

将公共名称转换为其显示名称 (`InView` → `In View`).

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|公共名称 |字符串|  |

### 烤肉串名称

**种类：**功能

```typescript
function toKebabName(publicName: string): string
```

将公共名称转换为短横线命名（`InView` → `in-view`).

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|公共名称 |字符串|  |

### 至技术名称

**种类：**功能

```typescript
function toTechnicalName(publicName: string): string
```

将公共名称转换为其技术名称 (`InView` → `in_view`).

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|公共名称 |字符串|  |

## `src/tsdown`

### cms缓存目录

**种类：**功能

```typescript
function cmsCacheDirectory(rootDir: string, target: CmsOutputPlugin): string
```

目标生成的树被写入的缓存目录。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|根目录 |字符串|  |
|目标| CmsOutput插件 |  |

### cms输出目录

**种类：**功能

```typescript
function cmsOutputDirectory(rootDir: string, target: CmsOutputPlugin): string
```

目标的每个框架模块被发送到的分发目录。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|根目录 |字符串|  |
|目标| CmsOutput插件 |  |

### 定义TsdownForgeCms

**种类：**功能

```typescript
function defineTsdownForgeCms(options: TsdownForgeCmsOptions): UserConfig
```

为一个 CMS 目标创建 tsdown 配置。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 | TsdownForgeCmsOptions | TsdownForgeCmsOptions |  |

### 定义TsdownForgeCmsAll

**种类：**功能

```typescript
function defineTsdownForgeCmsAll(options: TsdownForgeCmsAllOptions): UserConfig[]
```

为每个请求的 CMS 目标创建 tsdown 配置。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|选项 | TsdownForgeCmsAllOptions | TsdownForgeCmsAllOptions |  |

### 解析组件模块

**种类：**功能

```typescript
function resolveComponentsModule(rootDir: string, explicit?: string): string
```

找到包装中的中性元件桶。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|根目录 |字符串|  |
|明确 |字符串|  |

### TsdownForgeCmsAllOptions

**种类：**接口

```typescript
export interface TsdownForgeCmsAllOptions
```

{@link DefineTsdownForgeCmsAll} 的选项。

### TsdownForgeCms选项

**种类：**接口

```typescript
export interface TsdownForgeCmsOptions
```

{@link DefineTsdownForgeCms} 的选项。
