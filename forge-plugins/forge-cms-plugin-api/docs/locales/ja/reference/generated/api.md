# @mission-platform/forge-cms-plugin-api APIリファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

のパブリック ソース宣言から生成 `@mission-platform/forge-cms-plugin-api`.

## `src/__fixtures__/components`

### バッジ

**種類:** 定数

```typescript
export const BADGE
```

リテラル共用体のオプション、JSDoc の説明、および `?? literal` デフォルト。

### バッジ名

**種類:** 定数

```typescript
export const badgeNames: ContentComponentNamesInput
```

説明はありません。

### ボタン

**種類:** 定数

```typescript
export const BUTTON
```

テキストに劣化するユニオンと、ドロップされたコールバック プロップ。

### ボタン名

**種類:** 定数

```typescript
export const buttonNames: ContentComponentNamesInput
```

説明はありません。

### カウンタ

**種類:** 定数

```typescript
export const COUNTER
```

インタラクティブなコンポーネント: 状態とイベント ハンドラーにより、強制的に水和島が形成されます。

### カウンタ名

**種類:** 定数

```typescript
export const counterNames: ContentComponentNamesInput
```

説明はありません。

### 空の

**種類:** 定数

```typescript
export const EMPTY
```

小道具がまったくないコンポーネント。

### 空の名前

**種類:** 定数

```typescript
export const emptyNames: ContentComponentNamesInput
```

説明はありません。

### グリッド

**種類:** 定数

```typescript
export const GRID
```

数値のデフォルトを持つ数値プロパティ。

### グリッド名

**種類:** 定数

```typescript
export const gridNames: ContentComponentNamesInput
```

説明はありません。

### レイアウト

**種類:** 定数

```typescript
export const LAYOUT
```

デフォルトのスロットと並ぶ名前付きスロット。

### レイアウト名

**種類:** 定数

```typescript
export const layoutNames: ContentComponentNamesInput
```

説明はありません。

### 必須

**種類:** 定数

```typescript
export const REQUIRED
```

必須 (オプションではない) プロップとオプションのプロップ。

### 必須名前

**種類:** 定数

```typescript
export const requiredNames: ContentComponentNamesInput
```

説明はありません。

### SITE_HEADER

**種類:** 定数

```typescript
export const SITE_HEADER
```

サイト全体の設定プロパティを持つコンポーネント。

### サイトヘッダー名

**種類:** 定数

```typescript
export const siteHeaderNames: ContentComponentNamesInput
```

説明はありません。

## `src/__fixtures__/framework`

### スタブフレームワーク

**種類:** 関数

```typescript
function stubFramework(id = "react"): FrameworkOutputPlugin
```

オーバーライド可能な ID を使用してスタブ フレームワーク プラグインを作成します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
| ID |  |  |

## `src/analyze`

### 分析コンテンツコンポーネント

**種類:** 関数

```typescript
function analyzeContentComponent(sourceFile: ts.SourceFile, names: ContentComponentNamesInput, semantic?: SemanticModule): ContentComponent
```

中立的なコンポーネント モジュールを分析し、プラットフォームに中立的なコンテンツを返します。
モデル: 順序付けされた作成可能なフィールド、レンダリングされるスロット、および必要かどうか
水分補給されたランタイム。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|ソースファイル | ts.ソースファイル |  |
|名前 |コンテンツコンポーネント名入力 |  |
|セマンティック |セマンティックモジュール |  |

### CMS_COLOR_TAG

**種類:** 定数

```typescript
export const CMS_COLOR_TAG
```

説明はありません。

### CMS_COLOUR_TAG

**種類:** 定数

```typescript
export const CMS_COLOUR_TAG
```

出典注釈として英国のスペルが受け入れられます。

### CMS_ICON_TAG

**種類:** 定数

```typescript
export const CMS_ICON_TAG
```

オプションのコンポーネントレベルのエディターのメタデータの JSDoc タグ。

### CMS_SETTING_TAG

**種類:** 定数

```typescript
export const CMS_SETTING_TAG
```

プロップをサイト全体の CMS 設定に昇格させる JSDoc タグ。

### CMS_TAB_TAG

**種類:** 定数

```typescript
export const CMS_TAB_TAG
```

プロパティをエディター タブに割り当てる JSDoc タグ。

### DEFAULT_SLOT_FIELD

**種類:** 定数

```typescript
export const DEFAULT_SLOT_FIELD
```

フィールドキーはデフォルトのスロット（コンポーネントの `children`) 下に露出しています。

### 派生ContentComponentNames

**種類:** 関数

```typescript
function deriveContentComponentNames(names: ContentComponentNamesInput): ContentComponentNames
```

呼び出し元が指定したサブセットから完全な名前セットを導出します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|名前 |コンテンツコンポーネント名入力 |  |

### isInteractiveModule

**種類:** 関数

```typescript
function isInteractiveModule(semantic: SemanticModule | undefined): boolean
```

ニュートラル IR が実際のランタイムのみが提供できる動作を実行する場合は True。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|セマンティック |セマンティックモジュール \|未定義 |  |

## `src/classify`

### ASSET_TYPE_REFERENCES

**種類:** 定数

```typescript
export const ASSET_TYPE_REFERENCES: ReadonlySet<string>
```

プロップをメディア アセットとしてマークするタイプ参照。

### 分類されたフィールドの種類

**種類：**タイプ

```typescript
export type ClassifiedFieldKind = ContentFieldKind | undefined;
```

プロップを分類した結果 (`undefined` →支柱を落とします）。

### 分類タイプ

**種類:** 関数

```typescript
function classifyType(node: ts.TypeNode, aliases: Map<string, ts.TypeNode>, seen: Set<string> = new Set()): ClassifiedFieldKind
```

プロップのタイプを分類する node 中立的なコンテンツの種類に変換し、ローカルのエイリアス/共用体を解決します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
| node | ts.TypeNode |  |
|別名 |地図<string, ts.TypeNode> |  |
|見た |セット<string> |  |

### collectTypeAliases

**種類:** 関数

```typescript
function collectTypeAliases(sourceFile: ts.SourceFile): Map<string, ts.TypeNode>
```

すべてのトップレベルをマップする `type X = …` その型のエイリアス node、組合決議のため。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|ソースファイル | ts.ソースファイル |  |

### LINK_TYPE_REFERENCES

**種類:** 定数

```typescript
export const LINK_TYPE_REFERENCES: ReadonlySet<string>
```

プロップをリンク/URL としてマークする参照を入力します。

### RICHTEXT_TYPE_REFERENCES

**種類:** 定数

```typescript
export const RICHTEXT_TYPE_REFERENCES: ReadonlySet<string>
```

プロップをプレーン テキストではなくフォーマットされたリッチ テキストとしてマークする型参照。

### SLOT_TYPE_REFERENCES

**種類:** 定数

```typescript
export const SLOT_TYPE_REFERENCES: ReadonlySet<string>
```

プロパティがネストされたコンテンツとして扱われる型参照 (`children`).

## `src/cms`

### Cmsアーティファクト

**種類:** インターフェース

```typescript
export interface CmsArtifact
```

ターゲットがドライバーに書き込みを要求する単一のファイル。

### Cmsアーティファクト種類

**種類：**タイプ

```typescript
export type CmsArtifactKind = "schema" | "template" | "manifest" | "entry" | "declaration" | "module";
```

放出されたアーティファクトがターゲットの出力ツリーで果たす役割。

### CmsIsland戦略

**種類：**タイプ

```typescript
export type CmsIslandStrategy = "none" | "framework";
```

ターゲットがコンポーネントの対話型ランタイムを取得する方法。

### Cms出力プラグイン

**種類:** インターフェース

```typescript
export interface CmsOutputPlugin
```

コンポーザブルな CMS 出力プラグイン。

### CmsTargetContext

**種類:** インターフェース

```typescript
export interface CmsTargetContext
```

エミッターが実行するビルドに関して必要となる可能性のあるすべて。

### cmsターゲットディレクトリ

**種類:** 関数

```typescript
function cmsTargetDirectory(plugin: CmsOutputPlugin): string
```

ターゲットのフレームワークごとの出力が書き込まれるディレクトリ セグメント。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プラグイン | Cms出力プラグイン |  |

### CmsTargetId

**種類：**タイプ

```typescript
export type CmsTargetId = "storyblok" | "astro" | "ghost" | "jekyll" | "webflow" | (string &
```

組み込みの CMS ターゲット ID。追加の文字列 ID を受け入れます。

### 定義ForgeCmsプラグイン

**種類:** 関数

```typescript
function defineForgeCmsPlugin(plugin: T): T
```

CMS 出力プラグインを検証して返します。

失敗は構成時にスローされます。ターゲットを誤って宣言してはなりません。
ドライバーに到達し、静かに木の部分を放射します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|プラグイン |た |  |

## `src/config`

### 定義ViteForgeCmsライブラリ

**種類:** 関数

```typescript
function defineViteForgeCmsLibrary(options: ViteForgeCmsLibraryOptions): UserConfig
```

を作成します Vite 1 つの CMS ターゲットのライブラリ構成。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|オプション | ViteForgeCmsライブラリオプション |  |

### ViteForgeCmsライブラリオプション

**種類:** インターフェース

```typescript
export interface ViteForgeCmsLibraryOptions
```

{@link defineViteForgeCmsLibrary} のオプション。

## `src/content-model`

### コンテンツコンポーネント

**種類:** インターフェース

```typescript
export interface ContentComponent
```

ニュートラルコンテンツモデルに投影されたコンポーネント。

### コンテンツコンポーネントメタデータ

**種類:** インターフェース

```typescript
export interface ContentComponentMetadata
```

コンポーネントに注釈が付けられたオプションのプラットフォーム中立のメタデータ。

### コンテンツコンポーネント名

**種類:** インターフェース

```typescript
export interface ContentComponentNames
```

コンテンツ プラットフォームが要求するすべての名前は、1 つのコンポーネントから派生しています。

### コンテンツコンポーネント名入力

**種類：**タイプ

```typescript
export type ContentComponentNamesInput = Pick< ContentComponentNames, "neutralName" | "publicName" > & Partial< Pick<ContentComponentNames, "folder" | "propertiesType" | "sourceDir"> >;
```

呼び出し元が指定する {@link ContentComponentNames} のサブセット。残りは導出されます。

### コンテンツデフォルト値

**種類：**タイプ

```typescript
export type ContentDefaultValue = string | number | boolean;
```

プロップが保持できるリテラルのデフォルト値。

### コンテンツフィールド

**種類:** インターフェース

```typescript
export interface ContentField
```

コンポーネント prop から射影された単一の作成可能なフィールド。

### コンテンツフィールドの種類

**種類：**タイプ

```typescript
export type ContentFieldKind = |
```

コンテンツの種類 TypeScript タイプはにマップされます。

`undefined` ここでは決してモデル化されません。プロップは投影することができません。
content フィールド (コールバック、不透明なオブジェクト) は単に
分類子、戻り値 `undefined` 種類の代わりに。

### コンテンツフィールドの種類名

**種類：**タイプ

```typescript
export type ContentFieldKindName = ContentFieldKind["kind"];
```

{@link ContentFieldKind} の識別子。

### コンテンツフィールド

**種類:** 関数

```typescript
function contentFields(component: ContentComponent): readonly ContentField[]
```

スロットではないコンポーネントのフィールド (スカラー、作成可能なコンテンツ)。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|コンポーネント |コンテンツコンポーネント |  |

### スロットフィールド

**種類:** 関数

```typescript
function slotFields(component: ContentComponent): readonly ContentField[]
```

ネストされたコンテンツを保持するコンポーネントのフィールド。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|コンポーネント |コンテンツコンポーネント |  |

## `src/driver`

### GenerateCmsArtifacts

**種類:** 関数

```typescript
function generateCmsArtifacts(options: GenerateCmsArtifactsOptions): GeneratedCmsTree
```

1 つの CMS に対して、完全な検出 → IR → コンテンツ モデル → エミット → 書き込みループを実行します。
ターゲットを返し、エントリモジュールのパスと書き込まれたすべての内容を返します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|オプション | CmsArtifacts オプションを生成する |  |

### GenerateCmsArtifactsオプション

**種類:** インターフェース

```typescript
export interface GenerateCmsArtifactsOptions
```

{@link generatedCmsArtifacts} のオプション。

### 生成されたCmsツリー

**種類:** インターフェース

```typescript
export interface GeneratedCmsTree
```

1 回のターゲット実行で生成されたすべて。

## `src/island`

### 生成された島

**種類:** インターフェース

```typescript
export interface GeneratedIsland
```

生成された島ツリー、または `undefined` ターゲットが島を使用していない場合。

### 生成島

**種類:** 関数

```typescript
function generateIsland(options: GenerateIslandOptions): GeneratedIsland | undefined
```

ターゲットのフレームワーク アイランド ツリーを同時生成し、指定子を返します。
発行されたテンプレートは、それをインポートする必要があります。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|オプション | GenerateIslandOptions |  |

### アイランドオプションの生成

**種類:** インターフェース

```typescript
export interface GenerateIslandOptions
```

{@linkgenerateIsland} のオプション。

### ISLAND_DIRECTORY

**種類:** 定数

```typescript
export const ISLAND_DIRECTORY
```

ターゲット出力ディレクトリを基準としたディレクトリ名。アイランドが生成されます。

## `src/names`

### to表示名

**種類:** 関数

```typescript
function toDisplayName(publicName: string): string
```

パブリック名を表示名に変換します (`InView` → `In View`).

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|公開名 |文字列 |  |

### toケバブ名

**種類:** 関数

```typescript
function toKebabName(publicName: string): string
```

パブリック名をケバブケースの名前に変換します (`InView` → `in-view`).

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|公開名 |文字列 |  |

### to技術名

**種類:** 関数

```typescript
function toTechnicalName(publicName: string): string
```

パブリック名を技術名に変換します (`InView` → `in_view`).

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|公開名 |文字列 |  |

## `src/tsdown`

### cmsキャッシュディレクトリ

**種類:** 関数

```typescript
function cmsCacheDirectory(rootDir: string, target: CmsOutputPlugin): string
```

ターゲットの生成されたツリーが書き込まれるキャッシュ ディレクトリ。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|ルートディレクトリ |文字列 |  |
|ターゲット | Cms出力プラグイン |  |

### cms出力ディレクトリ

**種類:** 関数

```typescript
function cmsOutputDirectory(rootDir: string, target: CmsOutputPlugin): string
```

ターゲットのフレームワークごとのモジュールが出力される配布ディレクトリ。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|ルートディレクトリ |文字列 |  |
|ターゲット | Cms出力プラグイン |  |

### 定義TsdownForgeCms

**種類:** 関数

```typescript
function defineTsdownForgeCms(options: TsdownForgeCmsOptions): UserConfig
```

1 つの CMS ターゲットの tsdown 構成を作成します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|オプション | TsdownForgeCmsオプション |  |

### 定義TsdownForgeCmsAll

**種類:** 関数

```typescript
function defineTsdownForgeCmsAll(options: TsdownForgeCmsAllOptions): UserConfig[]
```

要求された CMS ターゲットごとに tsdown 構成を作成します。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|オプション | TsdownForgeCmsAllOptions |  |

### コンポーネントモジュールの解決

**種類:** 関数

```typescript
function resolveComponentsModule(rootDir: string, explicit?: string): string
```

パッケージの中性コンポーネントのバレルを見つけます。

#### パラメータ

|名前 |タイプ |説明 |
| --- | --- | --- |
|ルートディレクトリ |文字列 |  |
|明示的な |文字列 |  |

### TsdownForgeCmsAllOptions

**種類:** インターフェース

```typescript
export interface TsdownForgeCmsAllOptions
```

{@link defineTsdownForgeCmsAll} のオプション。

### TsdownForgeCmsOptions

**種類:** インターフェース

```typescript
export interface TsdownForgeCmsOptions
```

{@link defineTsdownForgeCms} のオプション。
