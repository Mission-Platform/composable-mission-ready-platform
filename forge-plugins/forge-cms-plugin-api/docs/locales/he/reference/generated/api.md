# @mission-platform/forge-cms-plugin-api הפניה ל-API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב `@mission-platform/forge-cms-plugin-api`.

## `src/__fixtures__/components`

### תָג

**סוג:** קבוע

```typescript
export const BADGE;
```

אפשרויות איחוד מילולי, תיאורי JSDoc ו `?? literal` ברירות מחדל.

### תג שמות

**סוג:** קבוע

```typescript
export const badgeNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### לַחְצָן

**סוג:** קבוע

```typescript
export const BUTTON;
```

איגוד משפיל לטקסט בתוספת אביזר התקשרות חזרה.

### שמות לחצנים

**סוג:** קבוע

```typescript
export const buttonNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### דֶלְפֵּק

**סוג:** קבוע

```typescript
export const COUNTER;
```

מרכיב אינטראקטיבי: מדינה ומטפל באירועים מכריחים אי לחות.

### counterNames

**סוג:** קבוע

```typescript
export const counterNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### רֵיק

**סוג:** קבוע

```typescript
export const EMPTY;
```

רכיב ללא אביזרים כלל.

### שמות ריקים

**סוג:** קבוע

```typescript
export const emptyNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### רֶשֶׁת

**סוג:** קבוע

```typescript
export const GRID;
```

אבזר מספרי עם ברירת מחדל מספרית.

### שמות רשת

**סוג:** קבוע

```typescript
export const gridNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### מַעֲרָך

**סוג:** קבוע

```typescript
export const LAYOUT;
```

משבצת עם שם לצד משבצת ברירת המחדל.

### layoutNames

**סוג:** קבוע

```typescript
export const layoutNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### דָרוּשׁ

**סוג:** קבוע

```typescript
export const REQUIRED;
```

אביזר נדרש (לא אופציונלי) לצד אופציונלי.

### שמות נדרשים

**סוג:** קבוע

```typescript
export const requiredNames: ContentComponentNamesInput;
```

לא סופק תיאור.

### SITE_HEADER

**סוג:** קבוע

```typescript
export const SITE_HEADER;
```

רכיב עם אבזר הגדרה לכל האתר.

### siteHeaderNames

**סוג:** קבוע

```typescript
export const siteHeaderNames: ContentComponentNamesInput;
```

לא סופק תיאור.

## `src/__fixtures__/framework`

### stubFramework

פונקציה **סוג:**

```typescript
function stubFramework(id = "react"): FrameworkOutputPlugin;
```

צור תוסף של מסגרת סטאב עם מזהה שניתן לעקוף.

#### פרמטרים

| שם   | הקלד | תיאור |
| ---- | ---- | ----- |
| מזהה |      |       |

## `src/analyze`

### analysContentComponent

פונקציה **סוג:**

```typescript
function analyzeContentComponent(
  sourceFile: ts.SourceFile,
  names: ContentComponentNamesInput,
  semantic?: SemanticModule,
): ContentComponent;
```

נתח מודול רכיב ניטרלי, והחזר את התוכן הנייטרלי שלו לפלטפורמה
דגם: שדות הניתנים לכתיבה מסודרים, החריצים שהוא מציג והאם הוא צריך
זמן ריצה עם לחות.

#### פרמטרים

| שם         | הקלד                       | תיאור |
| ---------- | -------------------------- | ----- |
| sourceFile | ts.SourceFile              |       |
| שמות       | ContentComponentNamesInput |       |
| סמנטי      | SemanticModule             |       |

### CMS_COLOR_TAG

**סוג:** קבוע

```typescript
export const CMS_COLOR_TAG;
```

לא סופק תיאור.

### CMS_COLOUR_TAG

**סוג:** קבוע

```typescript
export const CMS_COLOUR_TAG;
```

איות בריטי מקובל להערות מקור.

### CMS_ICON_TAG

**סוג:** קבוע

```typescript
export const CMS_ICON_TAG;
```

תגיות JSDoc עבור מטא נתונים אופציונליים של עורך ברמת הרכיב.

### CMS_SETTING_TAG

**סוג:** קבוע

```typescript
export const CMS_SETTING_TAG;
```

תג JSDoc שמקדם אביזר להגדרת CMS כלל-אתר.

### CMS_TAB_TAG

**סוג:** קבוע

```typescript
export const CMS_TAB_TAG;
```

תג JSDoc שמקצה אביזר ללשונית עורך.

### DEFAULT_SLOT_FIELD

**סוג:** קבוע

```typescript
export const DEFAULT_SLOT_FIELD;
```

מקש השדה חריץ ברירת המחדל (של רכיב `children`) נחשף מתחת.

### deriveContentComponentNames

פונקציה **סוג:**

```typescript
function deriveContentComponentNames(
  names: ContentComponentNamesInput,
): ContentComponentNames;
```

הפק את ערכת השם המלא מתת-הקבוצה שסופקה על ידי המתקשר.

#### פרמטרים

| שם   | הקלד                       | תיאור |
| ---- | -------------------------- | ----- |
| שמות | ContentComponentNamesInput |       |

### isInteractiveModule

פונקציה **סוג:**

```typescript
function isInteractiveModule(semantic: SemanticModule | undefined): boolean;
```

נכון כאשר ה-IR הנייטרלי נושא התנהגות שרק זמן ריצה אמיתי יכול לספק.

#### פרמטרים

| שם    | הקלד                    | תיאור |
| ----- | ----------------------- | ----- |
| סמנטי | מודול סמנטי \| לא מוגדר |       |

## `src/classify`

### ASSET_TYPE_REFERENCES

**סוג:** קבוע

```typescript
export const ASSET_TYPE_REFERENCES: ReadonlySet<string>;
```

הקלד הפניות המסמנות אבזר כנכס מדיה.

### ClassifiedFieldKind

**סוג:** סוג

```typescript
export type ClassifiedFieldKind = ContentFieldKind | undefined;
```

התוצאה של סיווג אביזר (`undefined` ← שחרר את האביזר).

### classifyType

פונקציה **סוג:**

```typescript
function classifyType(
  node: ts.TypeNode,
  aliases: Map<string, ts.TypeNode>,
  seen: Set<string> = new Set(),
): ClassifiedFieldKind;
```

סיווג סוג אביזר node לסוג תוכן ניטרלי, תוך פתרון כינויים/איגודים מקומיים.

#### פרמטרים

| שם      | הקלד                        | תיאור |
| ------- | --------------------------- | ----- |
| node    | ts.TypeNode                 |       |
| כינויים | מַפָּה<string, ts.TypeNode> |       |
| ראה     | מַעֲרֶכֶת<string>           |       |

### collectTypeAliases

פונקציה **סוג:**

```typescript
function collectTypeAliases(
  sourceFile: ts.SourceFile,
): Map<string, ts.TypeNode>;
```

מפה כל רמה עליונה `type X = …` כינוי לסוגו node, להחלטת האיגוד.

#### פרמטרים

| שם         | הקלד          | תיאור |
| ---------- | ------------- | ----- |
| sourceFile | ts.SourceFile |       |

### LINK_TYPE_REFERENCES

**סוג:** קבוע

```typescript
export const LINK_TYPE_REFERENCES: ReadonlySet<string>;
```

הקלד הפניות שמסמנות אביזר כקישור/כתובת אתר.

### RICHTEXT_TYPE_REFERENCES

**סוג:** קבוע

```typescript
export const RICHTEXT_TYPE_REFERENCES: ReadonlySet<string>;
```

הקלד הפניות המסמנות אבזר כטקסט עשיר מעוצב במקום טקסט רגיל.

### SLOT_TYPE_REFERENCES

**סוג:** קבוע

```typescript
export const SLOT_TYPE_REFERENCES: ReadonlySet<string>;
```

הקלד הפניות שהאביזרים שלהן מטופלים כתוכן מקונן (`children`).

## `src/cms`

### CmsArtifact

**סוג:** ממשק

```typescript
export interface CmsArtifact
```

קובץ בודד של יעד מבקש מהנהג לכתוב.

### CmsArtifactKind

**סוג:** סוג

```typescript
export type CmsArtifactKind =
  "schema" | "template" | "manifest" | "entry" | "declaration" | "module";
```

התפקיד שממלא חפץ שנפלט בעץ הפלט של המטרה.

### CmsIsland Strategy

**סוג:** סוג

```typescript
export type CmsIslandStrategy = "none" | "framework";
```

כיצד יעד משיג זמן ריצה אינטראקטיבי עבור רכיב.

### CmsOutputPlugin

**סוג:** ממשק

```typescript
export interface CmsOutputPlugin
```

תוסף פלט CMS הניתן לחיבור.

### CmsTargetContext

**סוג:** ממשק

```typescript
export interface CmsTargetContext
```

כל מה שפולט עשוי להזדקק למבנה שבו הוא פועל.

### cmsTargetDirectory

פונקציה **סוג:**

```typescript
function cmsTargetDirectory(plugin: CmsOutputPlugin): string;
```

פלט הספרייה תחתיו כתוב פלט לכל מסגרת של יעד.

#### פרמטרים

| שם   | הקלד            | תיאור |
| ---- | --------------- | ----- |
| תוסף | CmsOutputPlugin |       |

### CmsTargetId

**סוג:** סוג

```typescript
export type CmsTargetId = "storyblok" | "astro" | "ghost" | "jekyll" | "webflow" | (string &
```

מזהי היעד המובנים של CMS, פתוחים לכל מזהה מחרוזת נוסף.

### defineForgeCmsPlugin

פונקציה **סוג:**

```typescript
function defineForgeCmsPlugin(plugin: T): T;
```

אמת והחזר תוסף פלט CMS.

כשלים נזרקים בזמן קביעת התצורה - לעולם אסור למטרה שהוכרזה שגויה
להגיע לנהג ולפלוט בשקט עץ חלקי.

#### פרמטרים

| שם   | הקלד | תיאור |
| ---- | ---- | ----- |
| תוסף | ט    |       |

## `src/config`

### defineViteForgeCmsLibrary

פונקציה **סוג:**

```typescript
function defineViteForgeCmsLibrary(
  options: ViteForgeCmsLibraryOptions,
): UserConfig;
```

צור א Vite תצורת ספרייה עבור יעד CMS אחד.

#### פרמטרים

| שם       | הקלד                       | תיאור |
| -------- | -------------------------- | ----- |
| אפשרויות | ViteForgeCmsLibraryOptions |       |

### ViteForgeCmsLibraryOptions

**סוג:** ממשק

```typescript
export interface ViteForgeCmsLibraryOptions
```

אפשרויות עבור {@link defineViteForgeCmsLibrary}.

## `src/content-model`

### ContentComponent

**סוג:** ממשק

```typescript
export interface ContentComponent
```

רכיב המוקרן על מודל התוכן הנייטרלי.

### ContentComponentMetadata

**סוג:** ממשק

```typescript
export interface ContentComponentMetadata
```

מטא נתונים אופציונליים נייטרליים לפלטפורמה עם הערות על רכיב.

### ContentComponentNames

**סוג:** ממשק

```typescript
export interface ContentComponentNames
```

כל שם שפלטפורמת תוכן עשויה לבקש, נגזר ממרכיב אחד.

### ContentComponentNamesInput

**סוג:** סוג

```typescript
export type ContentComponentNamesInput = Pick<
  ContentComponentNames,
  "neutralName" | "publicName"
> &
  Partial<
    Pick<ContentComponentNames, "folder" | "propertiesType" | "sourceDir">
  >;
```

קבוצת המשנה של {@link ContentComponentNames} המתקשר מספקת; השאר נגזר.

### ContentDefaultValue

**סוג:** סוג

```typescript
export type ContentDefaultValue = string | number | boolean;
```

ערך ברירת מחדל מילולי שאביזר יכול לשאת.

### ContentField

**סוג:** ממשק

```typescript
export interface ContentField
```

שדה יחיד שניתן ליצירה המוקרן מאביזר רכיב.

### ContentFieldKind

**סוג:** סוג

```typescript
export type ContentFieldKind = |
```

התוכן סוג של אביזר TypeScript הקלד מפות ל.

`undefined` אף פעם לא מעוצב כאן - אביזר שלא ניתן להקרין על א
שדה התוכן (התקשרות חוזרת, אובייקט אטום) פשוט נשמט על ידי ה-
מסווג, שמחזיר `undefined` במקום סוג.

### ContentFieldKindName

**סוג:** סוג

```typescript
export type ContentFieldKindName = ContentFieldKind["kind"];
```

המפלה של {@link ContentFieldKind}.

### תוכן שדות

פונקציה **סוג:**

```typescript
function contentFields(component: ContentComponent): readonly ContentField[];
```

השדות של רכיב שאינם משבצות (סקלארי, תוכן ניתן ליצירה).

#### פרמטרים

| שם   | הקלד             | תיאור |
| ---- | ---------------- | ----- |
| רכיב | ContentComponent |       |

### slotFields

פונקציה **סוג:**

```typescript
function slotFields(component: ContentComponent): readonly ContentField[];
```

השדות של רכיב הנושאים תוכן מקונן.

#### פרמטרים

| שם   | הקלד             | תיאור |
| ---- | ---------------- | ----- |
| רכיב | ContentComponent |       |

## `src/driver`

### generCmsArtifacts

פונקציה **סוג:**

```typescript
function generateCmsArtifacts(
  options: GenerateCmsArtifactsOptions,
): GeneratedCmsTree;
```

הפעל את הגילוי המלא → IR → מודל תוכן → emit → לולאת כתיבה עבור CMS אחד
target, מחזיר את נתיב מודול הכניסה וכל מה שנכתב.

#### פרמטרים

| שם       | הקלד                        | תיאור |
| -------- | --------------------------- | ----- |
| אפשרויות | GenerateCmsArtifactsOptions |       |

### GenerateCmsArtifactsOptions

**סוג:** ממשק

```typescript
export interface GenerateCmsArtifactsOptions
```

אפשרויות עבור {@link generateCmsArtifacts}.

### GeneratedCmsTree

**סוג:** ממשק

```typescript
export interface GeneratedCmsTree
```

כל מה שיצר ריצת מטרה אחת.

## `src/island`

### GeneratedIsland

**סוג:** ממשק

```typescript
export interface GeneratedIsland
```

עץ האי שנוצר, או `undefined` כאשר המטרה אינה משתמשת באיים.

### generIsland

פונקציה **סוג:**

```typescript
function generateIsland(
  options: GenerateIslandOptions,
): GeneratedIsland | undefined;
```

יצירה משותפת של עץ האי המסגרת עבור יעד, והחזרת המפרט
תבנית שנפלטה צריכה לייבא אותה על ידי.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| אפשרויות | GenerateIslandOptions |       |

### GenerateIslandOptions

**סוג:** ממשק

```typescript
export interface GenerateIslandOptions
```

אפשרויות עבור {@link generateIsland}.

### ISLAND_DIRECTORY

**סוג:** קבוע

```typescript
export const ISLAND_DIRECTORY;
```

שם הספרייה, ביחס לספריית הפלט היעד, נוצרים לתוך האיים.

## `src/names`

### toDisplayName

פונקציה **סוג:**

```typescript
function toDisplayName(publicName: string): string;
```

המר שם ציבורי לשם התצוגה שלו (`InView` → `In View`).

#### פרמטרים

| שם         | הקלד   | תיאור |
| ---------- | ------ | ----- |
| publicName | מחרוזת |       |

### toKebabName

פונקציה **סוג:**

```typescript
function toKebabName(publicName: string): string;
```

המרת שם ציבורי לשם בעל ציר קבב (`InView` → `in-view`).

#### פרמטרים

| שם         | הקלד   | תיאור |
| ---------- | ------ | ----- |
| publicName | מחרוזת |       |

### toTechnicalName

פונקציה **סוג:**

```typescript
function toTechnicalName(publicName: string): string;
```

המר שם ציבורי לשמו הטכני (`InView` → `in_view`).

#### פרמטרים

| שם         | הקלד   | תיאור |
| ---------- | ------ | ----- |
| publicName | מחרוזת |       |

## `src/tsdown`

### cmsCacheDirectory

פונקציה **סוג:**

```typescript
function cmsCacheDirectory(rootDir: string, target: CmsOutputPlugin): string;
```

ספריית המטמון שאליו נכתב העץ שנוצר של היעד.

#### פרמטרים

| שם      | הקלד            | תיאור |
| ------- | --------------- | ----- |
| rootDir | מחרוזת          |       |
| יעד     | CmsOutputPlugin |       |

### cmsOutputDirectory

פונקציה **סוג:**

```typescript
function cmsOutputDirectory(rootDir: string, target: CmsOutputPlugin): string;
```

ספריית ההפצה אליה נפלטים המודולים לכל מסגרת של יעד.

#### פרמטרים

| שם      | הקלד            | תיאור |
| ------- | --------------- | ----- |
| rootDir | מחרוזת          |       |
| יעד     | CmsOutputPlugin |       |

### defineTsdownForgeCms

פונקציה **סוג:**

```typescript
function defineTsdownForgeCms(options: TsdownForgeCmsOptions): UserConfig;
```

צור תצורת tsdown עבור יעד CMS אחד.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| אפשרויות | TsdownForgeCmsOptions |       |

### defineTsdownForgeCmsAll

פונקציה **סוג:**

```typescript
function defineTsdownForgeCmsAll(
  options: TsdownForgeCmsAllOptions,
): UserConfig[];
```

צור תצורות tsdown עבור כל יעד CMS מבוקש.

#### פרמטרים

| שם       | הקלד                     | תיאור |
| -------- | ------------------------ | ----- |
| אפשרויות | TsdownForgeCmsAllOptions |       |

### resolveComponentsModule

פונקציה **סוג:**

```typescript
function resolveComponentsModule(rootDir: string, explicit?: string): string;
```

אתר את חבית הרכיבים הניטרליים של החבילה.

#### פרמטרים

| שם      | הקלד   | תיאור |
| ------- | ------ | ----- |
| rootDir | מחרוזת |       |
| מפורש   | מחרוזת |       |

### TsdownForgeCmsAllOptions

**סוג:** ממשק

```typescript
export interface TsdownForgeCmsAllOptions
```

אפשרויות עבור {@link defineTsdownForgeCmsAll}.

### TsdownForgeCmsOptions

**סוג:** ממשק

```typescript
export interface TsdownForgeCmsOptions
```

אפשרויות עבור {@link defineTsdownForgeCms}.
