# @mission-platform/forge-cms-plugin-api مرجع واجهة برمجة التطبيقات

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-cms-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-cms-plugin-api`.

## `src/__fixtures__/components`

### شارة

**النوع:** ثابت

```typescript
export const BADGE
```

خيارات الاتحاد الحرفي، وأوصاف JSDoc، و `?? literal` الإعدادات الافتراضية.

### أسماء الشارات

**النوع:** ثابت

```typescript
export const badgeNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### زر

**النوع:** ثابت

```typescript
export const BUTTON
```

اتحاد مهين للنص بالإضافة إلى دعم رد الاتصال المسقط.

### أسماء الأزرار

**النوع:** ثابت

```typescript
export const buttonNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### عداد

**النوع:** ثابت

```typescript
export const COUNTER
```

مكون تفاعلي: الحالة ومعالج الحدث يفرضان جزيرة رطبة.

### أسماء العدادات

**النوع:** ثابت

```typescript
export const counterNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### فارغ

**النوع:** ثابت

```typescript
export const EMPTY
```

مكون بدون دعائم على الإطلاق.

### أسماء فارغة

**النوع:** ثابت

```typescript
export const emptyNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### شبكة

**النوع:** ثابت

```typescript
export const GRID
```

دعامة رقمية ذات قيمة رقمية افتراضية.

### أسماء الشبكة

**النوع:** ثابت

```typescript
export const gridNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### تَخطِيط

**النوع:** ثابت

```typescript
export const LAYOUT
```

فتحة مسماة بجانب الفتحة الافتراضية.

### أسماء التخطيط

**النوع:** ثابت

```typescript
export const layoutNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### مطلوب

**النوع:** ثابت

```typescript
export const REQUIRED
```

دعامة مطلوبة (غير اختيارية) إلى جانب دعامة اختيارية.

### الأسماء المطلوبة

**النوع:** ثابت

```typescript
export const requiredNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

### SITE_HEADER

**النوع:** ثابت

```typescript
export const SITE_HEADER
```

مكون ذو دعامة إعداد على مستوى الموقع.

### siteHeaderNames

**النوع:** ثابت

```typescript
export const siteHeaderNames: ContentComponentNamesInput
```

لم يتم تقديم أي وصف.

## `src/__fixtures__/framework`

### stubFramework

**النوع:** الوظيفة

```typescript
function stubFramework(id = "react"): FrameworkOutputPlugin
```

قم بإنشاء مكون إضافي لإطار عمل كعب الروتين بمعرف يمكن تجاوزه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| معرف |  |  |

## `src/analyze`

### AnalyticsContentComponent

**النوع:** الوظيفة

```typescript
function analyzeContentComponent(sourceFile: ts.SourceFile, names: ContentComponentNamesInput, semantic?: SemanticModule): ContentComponent
```

تحليل وحدة مكون محايد، وإرجاع محتواها المحايد للنظام الأساسي
النموذج: الحقول المطلوبة القابلة للتأليف، والفتحات التي يعرضها، وما إذا كان يحتاج إليها
وقت تشغيل رطب.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| ملف المصدر | ts.SourceFile |  |
| أسماء | ContentComponentNamesInput |  |
| الدلالية | الوحدة الدلالية |  |

### CMS_COLOR_TAG

**النوع:** ثابت

```typescript
export const CMS_COLOR_TAG
```

لم يتم تقديم أي وصف.

### CMS_COLOUR_TAG

**النوع:** ثابت

```typescript
export const CMS_COLOUR_TAG
```

تم قبول التهجئة البريطانية للتعليقات التوضيحية للمصدر.

### CMS_ICON_TAG

**النوع:** ثابت

```typescript
export const CMS_ICON_TAG
```

علامات JSDoc لبيانات تعريف المحرر الاختيارية على مستوى المكون.

### CMS_SETTING_TAG

**النوع:** ثابت

```typescript
export const CMS_SETTING_TAG
```

علامة JSDoc التي تروج للدعامة لإعداد CMS على مستوى الموقع.

### CMS_TAB_TAG

**النوع:** ثابت

```typescript
export const CMS_TAB_TAG
```

علامة JSDoc التي تقوم بتعيين خاصية لعلامة تبويب المحرر.

### DEFAULT_SLOT_FIELD

**النوع:** ثابت

```typescript
export const DEFAULT_SLOT_FIELD
```

مفتاح الحقل هو الفتحة الافتراضية (مكون `children`) يتعرض تحت.

### deriveContentComponentNames

**النوع:** الوظيفة

```typescript
function deriveContentComponentNames(names: ContentComponentNamesInput): ContentComponentNames
```

اشتق مجموعة الاسم الكامل من المجموعة الفرعية التي يوفرها المتصل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| أسماء | ContentComponentNamesInput |  |

### isInteractiveModule

**النوع:** الوظيفة

```typescript
function isInteractiveModule(semantic: SemanticModule | undefined): boolean
```

صحيح عندما يحمل IR المحايد سلوكًا لا يمكن أن يوفره إلا وقت التشغيل الحقيقي.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الدلالية | الوحدة الدلالية \| غير محدد |  |

## `src/classify`

### ASSET_TYPE_REFERENCES

**النوع:** ثابت

```typescript
export const ASSET_TYPE_REFERENCES: ReadonlySet<string>
```

اكتب المراجع التي تحدد الدعامة كأصل وسائط.

### ClassifiedFieldKind

**النوع:** النوع

```typescript
export type ClassifiedFieldKind = ContentFieldKind | undefined;
```

نتيجة تصنيف الدعامة (`undefined` → إسقاط الدعامة).

### classifyType

**النوع:** الوظيفة

```typescript
function classifyType(node: ts.TypeNode, aliases: Map<string, ts.TypeNode>, seen: Set<string> = new Set()): ClassifiedFieldKind
```

تصنيف نوع الدعامة node إلى نوع محتوى محايد، وحل الأسماء المستعارة/النقابات المحلية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | ts.TypeNode |  |
| الأسماء المستعارة | رسم خريطة<string, ts.TypeNode> |  |
| رأيت | تعيين<string> |  |

### CollectTypeAliases

**النوع:** الوظيفة

```typescript
function collectTypeAliases(sourceFile: ts.SourceFile): Map<string, ts.TypeNode>
```

خريطة كل المستوى الأعلى `type X = …` الاسم المستعار لنوعه node، من أجل حل الاتحاد.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| ملف المصدر | ts.SourceFile |  |

### LINK_TYPE_REFERENCES

**النوع:** ثابت

```typescript
export const LINK_TYPE_REFERENCES: ReadonlySet<string>
```

اكتب المراجع التي تحدد الدعامة كرابط/عنوان URL.

### RICHTEXT_TYPE_REFERENCES

**النوع:** ثابت

```typescript
export const RICHTEXT_TYPE_REFERENCES: ReadonlySet<string>
```

اكتب المراجع التي تحدد الدعامة كنص منسق منسق بدلاً من نص عادي.

### SLOT_TYPE_REFERENCES

**النوع:** ثابت

```typescript
export const SLOT_TYPE_REFERENCES: ReadonlySet<string>
```

اكتب المراجع التي يتم التعامل مع عناصرها كمحتوى متداخل (`children`).

## `src/cms`

### CmsArtifact

**النوع:** الواجهة

```typescript
export interface CmsArtifact
```

ملف واحد يطلب الهدف من السائق كتابته.

### CmsArtifactKind

**النوع:** النوع

```typescript
export type CmsArtifactKind = "schema" | "template" | "manifest" | "entry" | "declaration" | "module";
```

الدور الذي تلعبه القطعة الأثرية المنبعثة في شجرة مخرجات الهدف.

### استراتيجية CmsIsland

**النوع:** النوع

```typescript
export type CmsIslandStrategy = "none" | "framework";
```

كيف يحصل الهدف على وقت تشغيل تفاعلي لأحد المكونات.

### CmsOutputPlugin

**النوع:** الواجهة

```typescript
export interface CmsOutputPlugin
```

ملحق إخراج CMS قابل للتركيب.

### CmsTargetContext

**النوع:** الواجهة

```typescript
export interface CmsTargetContext
```

كل ما قد يحتاجه الباعث بشأن البناء الذي يعمل فيه.

### cmsTargetDirectory

**النوع:** الوظيفة

```typescript
function cmsTargetDirectory(plugin: CmsOutputPlugin): string
```

مقطع الدليل الذي تتم كتابة مخرجات الهدف لكل إطار تحته.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| البرنامج المساعد | كمسوتبوتبلوجين |  |

### CmsTargetId

**النوع:** النوع

```typescript
export type CmsTargetId = "storyblok" | "astro" | "ghost" | "jekyll" | "webflow" | (string &
```

معرفات أهداف CMS المضمنة، مفتوحة لأي معرف سلسلة إضافي.

### تعريفForgeCmsPlugin

**النوع:** الوظيفة

```typescript
function defineForgeCmsPlugin(plugin: T): T
```

التحقق من صحة وإرجاع البرنامج المساعد لإخراج CMS.

يتم طرح حالات الفشل في وقت التكوين - يجب ألا يتم الإعلان عن هدف خاطئ أبدًا
الوصول إلى السائق وتنبعث بصمت شجرة جزئية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| البرنامج المساعد | ت |  |

## `src/config`

### تعريفViteForgeCmsLibrary

**النوع:** الوظيفة

```typescript
function defineViteForgeCmsLibrary(options: ViteForgeCmsLibraryOptions): UserConfig
```

إنشاء أ Vite تكوين المكتبة لهدف CMS واحد.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | ViteForgeCmsLibraryOptions |  |

### ViteForgeCmsLibraryOptions

**النوع:** الواجهة

```typescript
export interface ViteForgeCmsLibraryOptions
```

خيارات {@link DefineViteForgeCmsLibrary}.

## `src/content-model`

### مكون المحتوى

**النوع:** الواجهة

```typescript
export interface ContentComponent
```

أحد المكونات المسقطة على نموذج المحتوى المحايد.

### ContentComponentMetadata

**النوع:** الواجهة

```typescript
export interface ContentComponentMetadata
```

بيانات تعريف اختيارية محايدة للنظام الأساسي مشروحة على أحد المكونات.

### أسماء مكونات المحتوى

**النوع:** الواجهة

```typescript
export interface ContentComponentNames
```

كل اسم قد تطلبه منصة المحتوى، مشتق من مكون واحد.

### ContentComponentNamesInput

**النوع:** النوع

```typescript
export type ContentComponentNamesInput = Pick< ContentComponentNames, "neutralName" | "publicName" > & Partial< Pick<ContentComponentNames, "folder" | "propertiesType" | "sourceDir"> >;
```

المجموعة الفرعية من {@link ContentComponentNames} التي يقدمها المتصل؛ والباقي مشتق.

### ContentDefaultValue

**النوع:** النوع

```typescript
export type ContentDefaultValue = string | number | boolean;
```

قيمة افتراضية حرفية يمكن أن تحملها الدعامة.

### حقل المحتوى

**النوع:** الواجهة

```typescript
export interface ContentField
```

حقل واحد قابل للتأليف يتم عرضه من عنصر مكون.

### ContentFieldKind

**النوع:** النوع

```typescript
export type ContentFieldKind = |
```

المحتوى يشكل دعامة TypeScript اكتب الخرائط ل.

`undefined` لم يتم تصميمه هنا مطلقًا - دعامة لا يمكن عرضها على ملف
يتم ببساطة إسقاط حقل المحتوى (رد اتصال، كائن غير شفاف) بواسطة ملف
المصنف الذي يعود `undefined` بدلا من نوع.

### ContentFieldKindName

**النوع:** النوع

```typescript
export type ContentFieldKindName = ContentFieldKind["kind"];
```

تمييز {@link ContentFieldKind}.

### حقول المحتوى

**النوع:** الوظيفة

```typescript
function contentFields(component: ContentComponent): readonly ContentField[]
```

حقول المكون التي ليست فتحات (محتوى عددي وقابل للتأليف).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

### SlotFields

**النوع:** الوظيفة

```typescript
function slotFields(component: ContentComponent): readonly ContentField[]
```

حقول المكون الذي يحمل المحتوى المتداخل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المكون | مكون المحتوى |  |

## `src/driver`

### createCmsArtifacts

**النوع:** الوظيفة

```typescript
function generateCmsArtifacts(options: GenerateCmsArtifactsOptions): GeneratedCmsTree
```

قم بتشغيل الاكتشاف الكامل → IR → نموذج المحتوى → إرسال → حلقة الكتابة لنظام إدارة محتوى واحد
الهدف، وإرجاع مسار وحدة الإدخال وكل ما تمت كتابته.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | إنشاء CmsArtifactsOptions |  |

### إنشاء CmsArtifactsOptions

**النوع:** الواجهة

```typescript
export interface GenerateCmsArtifactsOptions
```

خيارات {@link generatorCmsArtifacts}.

### تم إنشاء CmsTree

**النوع:** الواجهة

```typescript
export interface GeneratedCmsTree
```

تم إنتاج كل شيء من خلال هدف واحد.

## `src/island`

### GeneratedIsland

**النوع:** الواجهة

```typescript
export interface GeneratedIsland
```

شجرة الجزيرة المولدة، أو `undefined` عندما لا يستخدم الهدف الجزر.

### createIsland

**النوع:** الوظيفة

```typescript
function generateIsland(options: GenerateIslandOptions): GeneratedIsland | undefined
```

شارك في إنشاء شجرة جزيرة إطار العمل لهدف ما، مع إرجاع المحدد
يجب أن يستورد القالب المنبعث بواسطة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | إنشاء خيارات الجزيرة |  |

### إنشاء خيارات الجزيرة

**النوع:** الواجهة

```typescript
export interface GenerateIslandOptions
```

خيارات {@link generatorIsland}.

### ISLAND_DIRECTORY

**النوع:** ثابت

```typescript
export const ISLAND_DIRECTORY
```

اسم الدليل، بالنسبة إلى دليل الإخراج الهدف، يتم إنشاء الجزر فيه.

## `src/names`

### toDisplayName

**النوع:** الوظيفة

```typescript
function toDisplayName(publicName: string): string
```

تحويل اسم عام إلى اسم العرض الخاص به (`InView` → `In View`).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الاسم العام | سلسلة |  |

### toKebabName

**النوع:** الوظيفة

```typescript
function toKebabName(publicName: string): string
```

تحويل اسم عام إلى اسم مغلف بالكباب (`InView` → `in-view`).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الاسم العام | سلسلة |  |

### إلىالاسم الفني

**النوع:** الوظيفة

```typescript
function toTechnicalName(publicName: string): string
```

تحويل اسم عام إلى اسمه الفني (`InView` → `in_view`).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الاسم العام | سلسلة |  |

## `src/tsdown`

### cmsCacheDirectory

**النوع:** الوظيفة

```typescript
function cmsCacheDirectory(rootDir: string, target: CmsOutputPlugin): string
```

دليل ذاكرة التخزين المؤقت الذي تتم كتابة الشجرة التي تم إنشاؤها للهدف إليه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الجذر | سلسلة |  |
| الهدف | كمسوتبوتبلوجين |  |

### cmsOutputDirectory

**النوع:** الوظيفة

```typescript
function cmsOutputDirectory(rootDir: string, target: CmsOutputPlugin): string
```

دليل التوزيع الذي يتم إرسال الوحدات النمطية لكل إطار عمل إليه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الجذر | سلسلة |  |
| الهدف | كمسوتبوتبلوجين |  |

### تعريفTsdownForgeCms

**النوع:** الوظيفة

```typescript
function defineTsdownForgeCms(options: TsdownForgeCmsOptions): UserConfig
```

قم بإنشاء تكوين tsdown لهدف CMS واحد.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | تسداونفورجكمسوبتيونس |  |

### تعريفTsdownForgeCmsAll

**النوع:** الوظيفة

```typescript
function defineTsdownForgeCmsAll(options: TsdownForgeCmsAllOptions): UserConfig[]
```

قم بإنشاء تكوينات tsdown لكل هدف CMS مطلوب.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خيارات | تسداونفورجكمسالوبتيونس |  |

### ResolveComponentsModule

**النوع:** الوظيفة

```typescript
function resolveComponentsModule(rootDir: string, explicit?: string): string
```

حدد موقع برميل المكونات المحايدة للحزمة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الجذر | سلسلة |  |
| صريح | سلسلة |  |

### TsdownForgeCmsAllOptions

**النوع:** الواجهة

```typescript
export interface TsdownForgeCmsAllOptions
```

خيارات {@link DefineTsdownForgeCmsAll}.

### TsdownForgeCmsOptions

**النوع:** الواجهة

```typescript
export interface TsdownForgeCmsOptions
```

خيارات {@link DefineTsdownForgeCms}.
