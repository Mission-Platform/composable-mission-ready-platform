# @mission-platform/forge-plugin-vue API-referentie

Machineondersteunde vertaling van de canonieke Engelse bron. Handmatig nalezen indien nodig. Pakketnamen, opdrachten, paden en technische identificatoren blijven ongewijzigd.

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Taal: Nederlands (nl)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Gegenereerd op basis van openbare bronverklaringen in `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### UitgezondenExtraModule

**Soort:** interface

```typescript
export interface EmittedExtraModule
```

Een extra SFC die de Vue-zender genereert naast de primaire module.

### UitgezondenVueModule

**Soort:** interface

```typescript
export interface EmittedVueModule
```

Het resultaat van de Vue-zender: de primaire SFC plus eventuele aanvullende SFC's die deze heeft gegenereerd.

### emitVueModule

**Soort:** functie

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

Transformeer een neutrale componentmodule in een Vue SFC (plus eventuele hulp-SFC's).

#### Parameters

| Naam | Typ | Beschrijving |
| --- | --- | --- |
| module | SemantischeModule |  |
| componentnaam | tekenreeks |  |
| componentMappen | Alleen-lezenSet<string> |  |
| plannen | VueLoweredModule |  |

## `src/emitters/hook-module`

### emitVueHookModule

**Soort:** functie

```typescript
function emitVueHookModule(module: SemanticModule): string
```

Compileer een neutrale haakmodule met de Vue samenstelbare bron (`.ts`).

#### Parameters

| Naam | Typ | Beschrijving |
| --- | --- | --- |
| module | SemantischeModule |  |

## `src/index`

### forgeVueFramework

**Soort:** functie

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

Maak de Vue-uitvoerplug-in en de bijbehorende Vite/Rolldown-compileradapters.

## `src/lower`

### isVueVerlaagd

**Soort:** functie

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

Beperk een doelplan tot het Vue-plan zonder te casten.

#### Parameters

| Naam | Typ | Beschrijving |
| --- | --- | --- |
| verlaagd | TargetLoweredModule \| ongedefinieerd |  |

### lagereVueModule

**Soort:** functie

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

Laat een neutrale module in het Vue-doelplan zakken. De neutrale module beweegt
onaangeroerd zodat latere fasen volledige toegang tot de IR behouden.

#### Parameters

| Naam | Typ | Beschrijving |
| --- | --- | --- |
| ir | SemantischeModule |  |
| context | DoelContext |  |

### VueComputedPlan

**Soort:** interface

```typescript
export interface VueComputedPlan
```

Een `useMemo` verlaagd naar een Vue `computed`.

### VueDynamicNodePlan

**Soort:** interface

```typescript
export interface VueDynamicNodePlan
```

Een `<component :is>`-weergavedoel.

### VueEventPlan

**Soort:** interface

```typescript
export interface VueEventPlan
```

Een gebeurtenis die de component uitzendt.

### VueListKeyPlan

**Soort:** interface

```typescript
export interface VueListKeyPlan
```

Een belangrijke beslissing van `v-for`.

### VueLoweredModule

**Soort:** interface

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

Het Vue-doelplan geproduceerd door {@link lowerVueModule}.

### VuePropPlan

**Soort:** interface

```typescript
export interface VuePropPlan
```

Eén lid van het verlaagde `defineProps`-contract.

### VuePropsContract

**Soort:** interface

```typescript
export interface VuePropsContract
```

De verlaagde `defineProps`-vorm.

### VueRefPlan

**Soort:** interface

```typescript
export interface VueRefPlan
```

Een `useState` verlaagd naar een Vue `ref`-declaratie.

### VueRuntimeImports

**Soort:** interface

```typescript
export interface VueRuntimeImports
```

De `vue` bindt een plan dat nodig is.

### VueSlotPlan

**Soort:** interface

```typescript
export interface VueSlotPlan
```

Een slot dat door de component wordt weergegeven.

### VueStaticSubtreePlan

**Soort:** interface

```typescript
export interface VueStaticSubtreePlan
```

Een statisch bekende deelboom die kan worden gehesen.

### VueTemplateRefPlan

**Soort:** interface

```typescript
export interface VueTemplateRefPlan
```

Een `useRef` verlaagd naar een Vue-sjabloonreferentie (of een gewone `shallowRef`).

### VueWatcherPlan

**Soort:** interface

```typescript
export interface VueWatcherPlan
```

Een `useEffect` verlaagd tot een Vue-watcher.

## `src/optimize`

### DEDUPE_COMPUTED

**Soort:** constant

```typescript
export const DEDUPE_COMPUTED
```

`computed`-declaraties samenvouwen die een fabriek delen.

### DROP_UNUSED_IMPORTS

**Soort:** constant

```typescript
export const DROP_UNUSED_IMPORTS
```

Prune `vue` importeert het verfijnde plan dat niet langer nodig is.

### HOIST_STATIC_SUBTRIES

**Soort:** constant

```typescript
export const HOIST_STATIC_SUBTREES
```

Hijs volledig statische subbomen (moduleconstanten / `v-once`).

### INLINE_SINGLE_USE_REFS

**Soort:** constant

```typescript
export const INLINE_SINGLE_USE_REFS
```

Zend een nooit opnieuw toegewezen status uit als een gewone `const` in plaats van een `ref`.

### optimizeVueModule

**Soort:** functie

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Verfijn een verlaagd Vue-abonnement. Intenties met een buitenlands (of ontbrekend) plan zijn dat wel
onaangeroerd terug, zodat de optimizer veilig in elke pijplijn kan worden gebruikt.

#### Parameters

| Naam | Typ | Beschrijving |
| --- | --- | --- |
| bedoelingen | DoelIntenties |  |
| opties | TargetOptimizeOptions |  |

### STABLE_LIST_KEYS

**Soort:** constant

```typescript
export const STABLE_LIST_KEYS
```

Bewaar alleen lijstsleutels waarvan bewezen is dat ze stabiel zijn, en registreer de lijsten die niet zijn ingetoetst.

### VUE_OPTIMIZATIES

**Soort:** constant

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

Elke optimalisatie-ID die het Vue-doel kan registreren, in toepassingsvolgorde.
