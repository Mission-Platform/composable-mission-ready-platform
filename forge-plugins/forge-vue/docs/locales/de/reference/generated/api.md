# @mission-platform/forge-plugin-vue API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### EmittedExtraModule

**Art:** Schnittstelle

```typescript
export interface EmittedExtraModule
```

Der Vue-Emitter generiert neben dem Primärmodul einen Hilfs-SFC.

### EmittedVueModule

**Art:** Schnittstelle

```typescript
export interface EmittedVueModule
```

Das Ergebnis des Vue-Emitters: der primäre SFC plus alle von ihm generierten Hilfs-SFCs.

### emitVueModule

**Art:** Funktion

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

Verwandeln Sie ein neutrales Komponentenmodul in einen Vue SFC (plus alle Hilfs-SFCs).

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Modul | SemanticModule |  |
| Komponentenname | Zeichenfolge |  |
| Komponentenordner | ReadonlySet<string> |  |
| planen | VueLoweredModule |  |

## `src/emitters/hook-module`

### emitVueHookModule

**Art:** Funktion

```typescript
function emitVueHookModule(module: SemanticModule): string
```

Kompilieren Sie ein neutrales Hook-Modul mit seiner zusammensetzbaren Vue-Quelle (`.ts`).

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Modul | SemanticModule |  |

## `src/index`

### forgeVueFramework

**Art:** Funktion

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

Erstellen Sie das Vue-Ausgabe-Plugin und seine Vite/Rolldown-Compiler-Adapter.

## `src/lower`

### isVueLowered

**Art:** Funktion

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

Grenzen Sie einen Zielplan ohne Umwandlung auf den Vue-Plan ein.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| abgesenkt | TargetLoweredModule \| undefiniert |  |

### LowerVueModule

**Art:** Funktion

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

Senken Sie ein neutrales Modul in den Vue-Zielplan ab. Das Neutralmodul fährt
bleibt unberührt, sodass spätere Phasen weiterhin vollen Zugriff auf die IR haben.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| ir | SemanticModule |  |
| Kontext | Zielkontext |  |

### VueComputedPlan

**Art:** Schnittstelle

```typescript
export interface VueComputedPlan
```

Ein `useMemo` wurde auf einen Vue `computed` gesenkt.

### VueDynamicNodePlan

**Art:** Schnittstelle

```typescript
export interface VueDynamicNodePlan
```

Ein `<component :is>`-Renderziel.

### VueEventPlan

**Art:** Schnittstelle

```typescript
export interface VueEventPlan
```

Ein Ereignis, das die Komponente ausgibt.

### VueListKeyPlan

**Art:** Schnittstelle

```typescript
export interface VueListKeyPlan
```

Eine `v-for`-Schlüsselentscheidung.

### VueLoweredModule

**Art:** Schnittstelle

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

Der Vue-Zielplan, erstellt von {@link LowerVueModule}.

### VuePropPlan

**Art:** Schnittstelle

```typescript
export interface VuePropPlan
```

Ein Mitglied des herabgesetzten `defineProps`-Vertrags.

### VuePropsContract

**Art:** Schnittstelle

```typescript
export interface VuePropsContract
```

Die abgesenkte `defineProps`-Form.

### VueRefPlan

**Art:** Schnittstelle

```typescript
export interface VueRefPlan
```

Eine `useState`-Deklaration wurde auf eine Vue-`ref`-Deklaration reduziert.

### VueRuntimeImports

**Art:** Schnittstelle

```typescript
export interface VueRuntimeImports
```

Die `vue`-Bindungen, die ein Plan benötigt.

### VueSlotPlan

**Art:** Schnittstelle

```typescript
export interface VueSlotPlan
```

Ein Slot, den die Komponente rendert.

### VueStaticSubtreePlan

**Art:** Schnittstelle

```typescript
export interface VueStaticSubtreePlan
```

Ein statisch bekannter Teilbaum, der angehoben werden kann.

### VueTemplateRefPlan

**Art:** Schnittstelle

```typescript
export interface VueTemplateRefPlan
```

Ein `useRef`, reduziert auf eine Vue-Vorlagenreferenz (oder ein einfaches `shallowRef`).

### VueWatcherPlan

**Art:** Schnittstelle

```typescript
export interface VueWatcherPlan
```

Ein `useEffect` wurde auf einen Vue-Beobachter gesenkt.

## `src/optimize`

### DEDUPE_COMPUTED

**Art:** konstant

```typescript
export const DEDUPE_COMPUTED
```

Reduzieren Sie `computed`-Deklarationen, die eine Factory gemeinsam nutzen.

### DROP_UNUSED_IMPORTS

**Art:** konstant

```typescript
export const DROP_UNUSED_IMPORTS
```

Prune `vue` importiert den verfeinerten Plan, der nicht mehr benötigt wird.

### HOIST_STATIC_SUBTREES

**Art:** konstant

```typescript
export const HOIST_STATIC_SUBTREES
```

Heben Sie vollständig statische Teilbäume an (Modulkonstanten / `v-once`).

### INLINE_SINGLE_USE_REFS

**Art:** konstant

```typescript
export const INLINE_SINGLE_USE_REFS
```

Gibt den nie neu zugewiesenen Status als einfaches `const` statt als `ref` aus.

### optimierenVueModule

**Art:** Funktion

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Verfeinern Sie einen abgesenkten Vue-Plan. Absichten, die einen ausländischen (oder fehlenden) Plan beinhalten, sind
wird unverändert zurückgegeben, sodass der Optimierer sicher in jeder Pipeline ausgeführt werden kann.

#### Parameter

| Name | Geben Sie | ein Beschreibung |
| --- | --- | --- |
| Absichten | Zielabsichten |  |
| Optionen | TargetOptimizeOptions |  |

### STABLE_LIST_KEYS

**Art:** konstant

```typescript
export const STABLE_LIST_KEYS
```

Behalten Sie nur Listenschlüssel bei, die sich als stabil erwiesen haben, und zeichnen Sie die Listen auf, die nicht verschlüsselt sind.

### VUE_OPTIMIZATIONS

**Art:** konstant

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

Jede Optimierungskennung, die das Vue-Ziel aufzeichnen kann, in der Reihenfolge der Anwendung.
