# @mission-platform/forge-plugin-vue API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/forge-plugin-vue` 中的公共源声明生成。

## `src/emitters/component`

### 发射额外模块

**种类：**接口

```typescript
export interface EmittedExtraModule
```

Vue 发射器与主模块一起生成辅助 SFC。

### EmissedVue模块

**种类：**接口

```typescript
export interface EmittedVueModule
```

Vue 发射器的结果：主 SFC 加上它生成的任何辅助 SFC。

### 发出Vue模块

**种类：**功能

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

将中性组件模块转换为 Vue SFC（以及任何辅助 SFC）。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|模块|语义模块 |  |
|组件名称 |字符串|  |
|组件文件夹 |只读设置<string> |  |
|计划| VueLowered 模块 |  |

## `src/emitters/hook-module`

### 发射VueHook模块

**种类：**功能

```typescript
function emitVueHookModule(module: SemanticModule): string
```

将中性挂钩模块编译为其 Vue 可组合源 (`.ts`)。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|模块|语义模块 |  |

## `src/index`

### forgeVue框架

**种类：**功能

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

创建 Vue 输出插件及其 Vite/Rolldown 编译器适配器。

## `src/lower`

### isVueLowered

**种类：**功能

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

将目标计划缩小为 Vue 计划，无需强制转换。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|降低| TargetLoweredModule \|未定义 |  |

### 下层Vue模块

**种类：**功能

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

将中性模块降低到 Vue 目标计划中。中性模块行程
保持不变，以便后续阶段保持对 IR 的完全访问。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|红外|语义模块 |  |
|背景 |目标上下文 |  |

### Vue计算计划

**种类：**接口

```typescript
export interface VueComputedPlan
```

`useMemo` 降低为 Vue `computed`。

### Vue动态节点计划

**种类：**接口

```typescript
export interface VueDynamicNodePlan
```

`<component :is>` 渲染目标。

### Vue事件计划

**种类：**接口

```typescript
export interface VueEventPlan
```

组件发出的事件。

### VueListKeyPlan

**种类：**接口

```typescript
export interface VueListKeyPlan
```

`v-for` 关键决策。

### VueLoweredModule

**种类：**接口

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

由 {@link lowerVueModule} 生成的 Vue 目标计划。

### VueProp计划

**种类：**接口

```typescript
export interface VuePropPlan
```

下调后的 `defineProps` 合约的成员之一。

### VueProps合约

**种类：**接口

```typescript
export interface VuePropsContract
```

降低的 `defineProps` 形状。

### VueRefPlan

**种类：**接口

```typescript
export interface VueRefPlan
```

`useState` 降低为 Vue `ref` 声明。

### Vue运行时导入

**种类：**接口

```typescript
export interface VueRuntimeImports
```

`vue` 绑定计划需求。

### VueSlotPlan

**种类：**接口

```typescript
export interface VueSlotPlan
```

组件渲染的槽。

### Vue静态子树计划

**种类：**接口

```typescript
export interface VueStaticSubtreePlan
```

可以提升的静态已知子树。

### VueTemplateRefPlan

**种类：**接口

```typescript
export interface VueTemplateRefPlan
```

`useRef` 降低为 Vue 模板引用（或普通 `shallowRef`）。

### VueWatcher计划

**种类：**接口

```typescript
export interface VueWatcherPlan
```

`useEffect` 降级为 Vue 观察者。

## `src/optimize`

### 重复数据删除_计算

**种类：**常数

```typescript
export const DEDUPE_COMPUTED
```

折叠共享工厂的 `computed` 声明。

### DROP_UNUSED_IMPORTS

**种类：**常数

```typescript
export const DROP_UNUSED_IMPORTS
```

Prune `vue` 不再需要输入精炼计划。

### HOIST_STATIC_SUBTREES

**种类：**常数

```typescript
export const HOIST_STATIC_SUBTREES
```

提升完全静态子树（模块常量/`v-once`）。

### INLINE_SINGLE_USE_REFS

**种类：**常数

```typescript
export const INLINE_SINGLE_USE_REFS
```

将永不重新分配的状态作为普通 `const` 而不是 `ref` 发出。

### 优化Vue模块

**种类：**功能

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

细化降低的 Vue 计划。携带外国（或缺失）计划的意图是
返回原样，因此优化器可以安全地在任何管道中运行。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|意向 |目标意图|  |
|选项 |目标优化选项 |  |

### STABLE_LIST_KEYS

**种类：**常数

```typescript
export const STABLE_LIST_KEYS
```

仅保留已证明稳定的列表键，记录未键控的列表。

### VUE_优化

**种类：**常数

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

Vue 目标可以按应用程序顺序记录的每个优化标识符。
