# @mission-platform/forge-plugin-vue API リファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/forge-plugin-vue` のパブリック ソース宣言から生成されます。

## `src/emitters/component`

### 放出された追加モジュール

**種類:** インターフェース

```typescript
export interface EmittedExtraModule
```

Vue エミッタがプライマリ モジュールと一緒に生成する補助 SFC。

### EmitedVueモジュール

**種類:** インターフェース

```typescript
export interface EmittedVueModule
```

Vue エミッタの結果: プライマリ SFC とそれが生成した補助 SFC。

### EmitVueモジュール

**種類:** 関数

```typescript
function emitVueModule(
  module: SemanticModule,
  componentName: string,
  componentFolders?: ReadonlySet<string>,
  plan?: VueLoweredModule,
): EmittedVueModule;
```

ニュートラル コンポーネント モジュールを Vue SFC (および補助 SFC) に変換します。

#### パラメータ

| 名前                     | タイプ                   | 説明 |
| ------------------------ | ------------------------ | ---- |
| モジュール               | セマンティックモジュール |      |
| コンポーネント名         | 文字列                   |      |
| コンポーネントフォルダー | ReadonlySet<string>      |      |
| 計画                     | VueLoweredモジュール     |      |

## `src/emitters/hook-module`

### EmitVueHookモジュール

**種類:** 関数

```typescript
function emitVueHookModule(module: SemanticModule): string;
```

ニュートラル フック モジュールを Vue コンポーザブル ソース (`.ts`) にコンパイルします。

#### パラメータ

| 名前       | タイプ                   | 説明 |
| ---------- | ------------------------ | ---- |
| モジュール | セマンティックモジュール |      |

## `src/index`

### forgeVueフレームワーク

**種類:** 関数

```typescript
function forgeVueFramework(): FrameworkOutputPlugin;
```

Vue 出力プラグインとその Vite/Rolldown コンパイラ アダプタを作成します。

## `src/lower`

### isVueLowered

**種類:** 関数

```typescript
function isVueLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is VueLoweredModule;
```

キャストせずにターゲット プランを Vue プランに絞り込みます。

#### パラメータ

| 名前   | タイプ                       | 説明 |
| ------ | ---------------------------- | ---- |
| 下げた | TargetLoweredModule \|未定義 |      |

### 下位Vueモジュール

**種類:** 関数

```typescript
function lowerVueModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

ニュートラル モジュールを Vue ターゲット プランに下げます。ニュートラルモジュールが移動します
そのままなので、後のフェーズでは IR への完全なアクセスが維持されます。

#### パラメータ

| 名前         | タイプ                   | 説明 |
| ------------ | ------------------------ | ---- |
| 赤外線       | セマンティックモジュール |      |
| コンテキスト | ターゲットコンテキスト   |      |

### VueComputedPlan

**種類:** インターフェース

```typescript
export interface VueComputedPlan
```

`useMemo` は Vue `computed` に引き下げられました。

### VueDynamicNodePlan

**種類:** インターフェース

```typescript
export interface VueDynamicNodePlan
```

`<component :is>` レンダー ターゲット。

### Vueイベントプラン

**種類:** インターフェース

```typescript
export interface VueEventPlan
```

コンポーネントが発行するイベント。

### VueListKeyPlan

**種類:** インターフェース

```typescript
export interface VueListKeyPlan
```

`v-for` の重要な決定。

### VueLoweredモジュール

**種類:** インターフェース

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

{@link lowerVueModule} によって作成された Vue ターゲット プラン。

### Vueプロッププラン

**種類:** インターフェース

```typescript
export interface VuePropPlan
```

引き下げられた `defineProps` 契約のメンバーの 1 人。

### VuePropsContract

**種類:** インターフェース

```typescript
export interface VuePropsContract
```

`defineProps`をローダウンした形状。

### VueRefPlan

**種類:** インターフェース

```typescript
export interface VueRefPlan
```

`useState` が Vue `ref` 宣言に引き下げられました。

### Vueランタイムインポート

**種類:** インターフェース

```typescript
export interface VueRuntimeImports
```

プランに必要な `vue` バインディング。

### Vueスロットプラン

**種類:** インターフェース

```typescript
export interface VueSlotPlan
```

コンポーネントがレンダリングするスロット。

### Vue静的サブツリープラン

**種類:** インターフェース

```typescript
export interface VueStaticSubtreePlan
```

ホイストできる静的に既知のサブツリー。

### VueTemplateRefPlan

**種類:** インターフェース

```typescript
export interface VueTemplateRefPlan
```

`useRef` は、Vue テンプレート参照 (またはプレーンな `shallowRef`) に引き下げられました。

### VueWatcherPlan

**種類:** インターフェース

```typescript
export interface VueWatcherPlan
```

`useEffect` は Vue ウォッチャーに格下げされました。

## `src/optimize`

### DEDUPE_COMPUTED

**種類:** 定数

```typescript
export const DEDUPE_COMPUTED;
```

ファクトリを共有する `computed` 宣言を折りたたみます。

### DROP_UNUSED_IMPORTS

**種類:** 定数

```typescript
export const DROP_UNUSED_IMPORTS;
```

Prune `vue` は、不要になった洗練されたプランをインポートします。

### HOIST_STATIC_SUBTREES

**種類:** 定数

```typescript
export const HOIST_STATIC_SUBTREES;
```

完全に静的なサブツリーをホイストします (モジュール定数 / `v-once`)。

### INLINE_SINGLE_USE_REFS

**種類:** 定数

```typescript
export const INLINE_SINGLE_USE_REFS;
```

再割り当てされない状態を `ref` ではなくプレーンの `const` として出力します。

### 最適化Vueモジュール

**種類:** 関数

```typescript
function optimizeVueModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

引き下げられた Vue プランを改良します。外国の（または欠落した）計画を伴う意図は次のとおりです。
そのまま返されるため、オプティマイザはどのパイプラインでも安全に実行できます。

#### パラメータ

| 名前       | タイプ                     | 説明 |
| ---------- | -------------------------- | ---- |
| 意図       | ターゲットインテンション   |      |
| オプション | ターゲット最適化オプション |      |

### STABLE_LIST_KEYS

**種類:** 定数

```typescript
export const STABLE_LIST_KEYS;
```

安定していることが証明されたリストキーのみを保持し、キーなしのままのリストを記録します。

### VUE_最適化

**種類:** 定数

```typescript
export const VUE_OPTIMIZATIONS: readonly string[];
```

Vue ターゲットがアプリケーション順に記録できるすべての最適化識別子。
