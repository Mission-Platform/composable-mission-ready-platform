# @mission-platform/forge-plugin-vue API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/forge-plugin-vue`의 공개 소스 선언에서 생성됩니다.

## `src/emitters/component`

### 방출된추가모듈

**종류:** 인터페이스

```typescript
export interface EmittedExtraModule
```

Vue 이미터는 기본 모듈과 함께 보조 SFC를 생성합니다.

### 방출된Vue모듈

**종류:** 인터페이스

```typescript
export interface EmittedVueModule
```

Vue 이미터의 결과: 기본 SFC와 생성된 보조 SFC.

### EmitVue모듈

**종류:** 기능

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

중립 구성 요소 모듈을 Vue SFC(보조 SFC 포함)로 변환합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 모듈 | 의미 모듈 |  |
| 구성 요소 이름 | 문자열 |  |
| 구성요소폴더 | 읽기전용Set<string> |  |
| 계획 | VueLowered모듈 |  |

## `src/emitters/hook-module`

### EmitVueHook모듈

**종류:** 기능

```typescript
function emitVueHookModule(module: SemanticModule): string
```

중립 후크 모듈을 Vue 구성 가능한 소스(`.ts`)로 컴파일합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 모듈 | 의미 모듈 |  |

## `src/index`

### forgeVue프레임워크

**종류:** 기능

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

Vue 출력 플러그인과 해당 Vite/Rolldown 컴파일러 어댑터를 만듭니다.

## `src/lower`

### isVue낮음

**종류:** 기능

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

캐스팅하지 않고 대상 계획을 Vue 계획으로 좁힙니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 낮아진 | TargetLowered모듈 \| 정의되지 않음 |  |

### lowerVue모듈

**종류:** 기능

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

중립 모듈을 Vue 대상 계획으로 낮추십시오. 중립 모듈이 이동합니다.
이후 단계에서 IR에 대한 전체 액세스를 유지하도록 그대로 유지합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| IR | 의미 모듈 |  |
| 맥락 | 타겟컨텍스트 |  |

### Vue계산 계획

**종류:** 인터페이스

```typescript
export interface VueComputedPlan
```

`useMemo`이 Vue `computed`로 낮아졌습니다.

### VueDynamicNodePlan

**종류:** 인터페이스

```typescript
export interface VueDynamicNodePlan
```

`<component :is>` 렌더 타겟.

### Vue이벤트 계획

**종류:** 인터페이스

```typescript
export interface VueEventPlan
```

구성 요소가 내보내는 이벤트입니다.

### VueListKeyPlan

**종류:** 인터페이스

```typescript
export interface VueListKeyPlan
```

`v-for` 주요 결정.

### VueLowered모듈

**종류:** 인터페이스

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

{@link lowerVueModule}에서 생성된 Vue 대상 계획입니다.

### VuePropPlan

**종류:** 인터페이스

```typescript
export interface VuePropPlan
```

하향된 `defineProps` 계약의 한 구성원입니다.

### VueProps계약

**종류:** 인터페이스

```typescript
export interface VuePropsContract
```

`defineProps` 모양이 낮아졌습니다.

### VueRefPlan

**종류:** 인터페이스

```typescript
export interface VueRefPlan
```

`useState`이 Vue `ref` 선언으로 낮아졌습니다.

### Vue런타임가져오기

**종류:** 인터페이스

```typescript
export interface VueRuntimeImports
```

`vue` 바인딩은 계획에 필요합니다.

### VueSlotPlan

**종류:** 인터페이스

```typescript
export interface VueSlotPlan
```

구성 요소가 렌더링하는 슬롯입니다.

### VueStaticSubtreePlan

**종류:** 인터페이스

```typescript
export interface VueStaticSubtreePlan
```

끌어올릴 수 있는 정적으로 알려진 하위 트리입니다.

### VueTemplateRefPlan

**종류:** 인터페이스

```typescript
export interface VueTemplateRefPlan
```

`useRef`이 Vue 템플릿 참조(또는 일반 `shallowRef`)로 낮아졌습니다.

### VueWatcherPlan

**종류:** 인터페이스

```typescript
export interface VueWatcherPlan
```

`useEffect`이 Vue 감시자로 낮아졌습니다.

## `src/optimize`

### DEDUPE_COMPUTED

**종류:** 상수

```typescript
export const DEDUPE_COMPUTED
```

팩토리를 공유하는 `computed` 선언을 축소합니다.

### DROP_UNUSED_IMPORTS

**종류:** 상수

```typescript
export const DROP_UNUSED_IMPORTS
```

Prune `vue`은 더 이상 필요하지 않은 세련된 계획을 가져옵니다.

### HOIST_STATIC_SUBTREES

**종류:** 상수

```typescript
export const HOIST_STATIC_SUBTREES
```

완전 정적 하위 트리를 호이스트합니다(모듈 상수 / `v-once`).

### INLINE_SINGLE_USE_REFS

**종류:** 상수

```typescript
export const INLINE_SINGLE_USE_REFS
```

`ref` 대신 일반 `const`으로 재할당되지 않은 상태를 내보냅니다.

### 최적화Vue모듈

**종류:** 기능

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

낮아진 Vue 계획을 구체화합니다. 외국(또는 누락) 계획을 담고 있는 의도는 다음과 같습니다.
그대로 반환되므로 최적화 프로그램은 모든 파이프라인에서 안전하게 실행될 수 있습니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 의도 | 타겟의도 |  |
| 옵션 | TargetOptimizeOptions |  |

### STABLE_LIST_KEYS

**종류:** 상수

```typescript
export const STABLE_LIST_KEYS
```

안정성이 입증된 목록 키만 유지하고 키가 없는 상태로 남겨진 목록을 기록합니다.

### VUE_OPTIMIZATIONS

**종류:** 상수

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

Vue 대상이 애플리케이션 순서대로 기록할 수 있는 모든 최적화 식별자입니다.
