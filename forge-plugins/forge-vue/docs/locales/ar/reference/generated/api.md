# مرجع @mission-platform/forge-plugin-vue API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### EmitExtraModule

**النوع:** الواجهة

```typescript
export interface EmittedExtraModule
```

يقوم باعث Vue بإنشاء SFC إضافي بجانب الوحدة الأساسية.

### EmitVueModule

**النوع:** الواجهة

```typescript
export interface EmittedVueModule
```

نتيجة باعث Vue: SFC الأساسي بالإضافة إلى أي SFCs مساعدة قام بإنشائها.

### emitVueModule

**النوع:** الوظيفة

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

قم بتحويل وحدة مكون محايد إلى Vue SFC (بالإضافة إلى أي SFCs مساعدة).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |
| اسم المكون | سلسلة |  |
| مجلدات المكونات | ReadonlySet<string> |  |
| خطة | VueLoweredModule |  |

## `src/emitters/hook-module`

### emitVueHookModule

**النوع:** الوظيفة

```typescript
function emitVueHookModule(module: SemanticModule): string
```

قم بتجميع وحدة ربط محايدة إلى مصدرها القابل للتركيب Vue (`.ts`).

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |

## `src/index`

### com.forgeVueFramework

**النوع:** الوظيفة

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

قم بإنشاء ملحق الإخراج Vue ومحولات برنامج التحويل البرمجي Vite/Rolldown الخاصة به.

## `src/lower`

### isVueLowered

**النوع:** الوظيفة

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

تضييق نطاق الخطة المستهدفة إلى خطة Vue دون الإرسال.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خفضت | TargetLoweredModule \| غير محدد |  |

### LowerVueModule

**النوع:** الوظيفة

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

قم بتضمين وحدة محايدة في الخطة المستهدفة Vue. تنتقل الوحدة المحايدة
على حالها، لذا تحافظ المراحل اللاحقة على الوصول الكامل إلى IR.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الأشعة تحت الحمراء | الوحدة الدلالية |  |
| السياق | سياق الهدف |  |

### VueComputedPlan

**النوع:** الواجهة

```typescript
export interface VueComputedPlan
```

تم تخفيض `useMemo` إلى Vue `computed`.

### VueDynamicNodePlan

**النوع:** الواجهة

```typescript
export interface VueDynamicNodePlan
```

هدف عرض `<component :is>`.

### VueEventPlan

**النوع:** الواجهة

```typescript
export interface VueEventPlan
```

حدث يصدره المكون.

### VueListKeyPlan

**النوع:** الواجهة

```typescript
export interface VueListKeyPlan
```

قرار رئيسي `v-for`.

### VueLoweredModule

**النوع:** الواجهة

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

الخطة المستهدفة Vue التي تم إنتاجها بواسطة {@link LowerVueModule}.

### VuePropPlan

**النوع:** الواجهة

```typescript
export interface VuePropPlan
```

عضو واحد في عقد `defineProps` الذي تم تخفيضه.

### VuePropsContract

**النوع:** الواجهة

```typescript
export interface VuePropsContract
```

الشكل المنخفض `defineProps`.

### VueRefPlan

**النوع:** الواجهة

```typescript
export interface VueRefPlan
```

تم تخفيض `useState` إلى إعلان Vue `ref`.

### VueRuntimeImports

**النوع:** الواجهة

```typescript
export interface VueRuntimeImports
```

يرتبط `vue` باحتياجات الخطة.

### VueSlotPlan

**النوع:** الواجهة

```typescript
export interface VueSlotPlan
```

فتحة يعرضها المكون.

### VueStaticSubtreePlan

**النوع:** الواجهة

```typescript
export interface VueStaticSubtreePlan
```

شجرة فرعية معروفة بشكل ثابت ويمكن رفعها.

### VueTemplateRefPlan

**النوع:** الواجهة

```typescript
export interface VueTemplateRefPlan
```

تم تخفيض `useRef` إلى مرجع قالب Vue (أو `shallowRef` عادي).

### VueWatcherPlan

**النوع:** الواجهة

```typescript
export interface VueWatcherPlan
```

تم تخفيض `useEffect` إلى مراقب Vue.

## `src/optimize`

### DEDUPE_COMPUTED

**النوع:** ثابت

```typescript
export const DEDUPE_COMPUTED
```

طي إعلانات `computed` التي تشترك في المصنع.

### DROP_UNUSED_IMPORTS

**النوع:** ثابت

```typescript
export const DROP_UNUSED_IMPORTS
```

يستورد Prune `vue` الخطة المكررة التي لم تعد بحاجة إليها.

### HOIST_STATIC_SUBTREES

**النوع:** ثابت

```typescript
export const HOIST_STATIC_SUBTREES
```

رفع الأشجار الفرعية الثابتة بالكامل (ثوابت الوحدة / `v-once`).

### INLINE_SINGLE_USE_REFS

**النوع:** ثابت

```typescript
export const INLINE_SINGLE_USE_REFS
```

قم بإصدار حالة لم يتم إعادة تعيينها أبدًا كحالة `const` عادية بدلاً من `ref`.

### optimVueModule

**النوع:** الوظيفة

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

قم بتحسين خطة Vue المنخفضة. النوايا التي تحمل خطة أجنبية (أو مفقودة) هي
تم إرجاعه دون تغيير، لذلك يكون المُحسِّن آمنًا للتشغيل في أي مسار.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نوايا | النوايا المستهدفة |  |
| خيارات | تارجتوبتيميزيوبتيونس |  |

### STABLE_LIST_KEYS

**النوع:** ثابت

```typescript
export const STABLE_LIST_KEYS
```

احتفظ فقط بمفاتيح القائمة التي أثبتت ثباتها، وتسجيل القوائم التي تركت بدون مفاتيح.

### VUE_OPTIMIZATIONS

**النوع:** ثابت

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

كل معرف تحسين يمكن لهدف Vue تسجيله، بترتيب التطبيق.
