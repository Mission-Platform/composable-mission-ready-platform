# Riferimento API @mission-platform/forge-plugin-vue

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> forge-plugins/forge-vue/docs/reference/generated/api.md: [forge-plugins/forge-vue/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/forge-plugin-vue`.

## `src/emitters/component`

### Modulo Extra Emesso

**Tipo:** interfaccia

```typescript
export interface EmittedExtraModule
```

Un SFC ausiliario generato dall'emettitore Vue insieme al modulo primario.

### ModuloVueemesso

**Tipo:** interfaccia

```typescript
export interface EmittedVueModule
```

Il risultato dell'emettitore Vue: l'SFC primario più eventuali SFC ausiliari generati.

### emitVueModule

**Tipo:** funzione

```typescript
function emitVueModule(module: SemanticModule, componentName: string, componentFolders?: ReadonlySet<string>, plan?: VueLoweredModule): EmittedVueModule
```

Trasformare un modulo componente neutro in un SFC Vue (più eventuali SFC ausiliari).

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |
| nomecomponente | stringa |  |
| componenteCartelle | Sola letturaSet<string> |  |
| piano | VueLoweredModule |  |

## `src/emitters/hook-module`

### emitVueHookModule

**Tipo:** funzione

```typescript
function emitVueHookModule(module: SemanticModule): string
```

Compilare un modulo hook neutro sulla relativa origine componibile Vue (`.ts`).

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |

## `src/index`

### forgeVueFramework

**Tipo:** funzione

```typescript
function forgeVueFramework(): FrameworkOutputPlugin
```

Creare il plug-in di output Vue e i relativi adattatori del compilatore Vite/Rolldown.

## `src/lower`

### isVueLowered

**Tipo:** funzione

```typescript
function isVueLowered(lowered: TargetLoweredModule | undefined): lowered is VueLoweredModule
```

Restringi un piano di destinazione al piano Vue senza eseguire il cast.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| abbassato | TargetLoweredModule \| indefinito |  |

### lowerVueModule

**Tipo:** funzione

```typescript
function lowerVueModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

Abbassare un modulo neutro nel piano target Vue. Il modulo neutro viaggia
intatto, quindi le fasi successive mantengono l'accesso completo all'IR.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| io | Modulo Semantico |  |
| contesto | Contesto di destinazione |  |

### VueComputedPlan

**Tipo:** interfaccia

```typescript
export interface VueComputedPlan
```

A `useMemo` abbassato a Vue `computed`.

### VueDynamicNodePlan

**Tipo:** interfaccia

```typescript
export interface VueDynamicNodePlan
```

Una destinazione di rendering `<component :is>`.

### VueEventPlan

**Tipo:** interfaccia

```typescript
export interface VueEventPlan
```

Un evento emesso dal componente.

### VueListKeyPlan

**Tipo:** interfaccia

```typescript
export interface VueListKeyPlan
```

Una decisione chiave `v-for`.

### VueLoweredModule

**Tipo:** interfaccia

```typescript
export interface VueLoweredModule extends TargetLoweredModule
```

Il piano target Vue prodotto da {@link lowerVueModule}.

### VuePropPlan

**Tipo:** interfaccia

```typescript
export interface VuePropPlan
```

Un membro del contratto `defineProps` ridotto.

### VuePropsContract

**Tipo:** interfaccia

```typescript
export interface VuePropsContract
```

La forma `defineProps` ribassata.

### VueRefPlan

**Tipo:** interfaccia

```typescript
export interface VueRefPlan
```

Una dichiarazione `useState` ridotta a una dichiarazione Vue `ref`.

### VueRuntimeImports

**Tipo:** interfaccia

```typescript
export interface VueRuntimeImports
```

Le associazioni `vue` necessarie per un piano.

### VueSlotPlan

**Tipo:** interfaccia

```typescript
export interface VueSlotPlan
```

Uno slot renderizzato dal componente.

### VueStaticSubtreePlan

**Tipo:** interfaccia

```typescript
export interface VueStaticSubtreePlan
```

Un sottoalbero staticamente noto che può essere sollevato.

### VueTemplateRefPlan

**Tipo:** interfaccia

```typescript
export interface VueTemplateRefPlan
```

Un `useRef` ridotto a un modello di riferimento Vue (o un semplice `shallowRef`).

### VueWatcherPlan

**Tipo:** interfaccia

```typescript
export interface VueWatcherPlan
```

Un `useEffect` abbassato a watcher Vue.

## `src/optimize`

### DEDUPE_COMPUTED

**Genere:** costante

```typescript
export const DEDUPE_COMPUTED
```

Comprimi le dichiarazioni `computed` che condividono una factory.

### DROP_UNUSED_IMPORTS

**Genere:** costante

```typescript
export const DROP_UNUSED_IMPORTS
```

Sfoltire le importazioni `vue` di cui il piano perfezionato non ha più bisogno.

### HOIST_STATIC_SUBTREES

**Genere:** costante

```typescript
export const HOIST_STATIC_SUBTREES
```

Sollevare sottoalberi completamente statici (costanti del modulo / `v-once`).

### INLINE_SINGLE_USE_REFS

**Genere:** costante

```typescript
export const INLINE_SINGLE_USE_REFS
```

Emetti lo stato mai riassegnato come un semplice `const` anziché un `ref`.

### ottimizzareVueModule

**Tipo:** funzione

```typescript
function optimizeVueModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Perfezionare un piano Vue ridotto. Le intenzioni che portano con sé un piano straniero (o mancante) lo sono
restituito intatto, quindi l'ottimizzatore può essere eseguito in sicurezza in qualsiasi pipeline.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| intenzioni | Intenzioni target |  |
| opzioni | TargetOptimizeOptions |  |

### STABLE_LIST_KEYS

**Genere:** costante

```typescript
export const STABLE_LIST_KEYS
```

Mantieni solo le chiavi degli elenchi dimostrate stabili, registrando gli elenchi lasciati senza chiave.

### VUE_OTTIMIZZAZIONI

**Genere:** costante

```typescript
export const VUE_OPTIMIZATIONS: readonly string[]
```

Ogni identificatore di ottimizzazione the Vue il target può registrare, nell'ordine dell'applicazione.
