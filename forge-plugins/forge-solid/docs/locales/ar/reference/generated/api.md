# مرجع @mission-platform/forge-plugin-solid API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-solid/docs/reference/generated/api.md: [forge-plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-plugin-solid`.

## `src/emitters/hook-module`

### emitSolidHookModule

**النوع:** الوظيفة

```typescript
function emitSolidHookModule(module: SemanticModule): string;
```

قم بتحويل وحدة ربط محايدة إلى مصدر SolidJS الخاص بها.

#### حدود

| الاسم          | اكتب            | الوصف |
| -------------- | --------------- | ----- |
| الوحدة النمطية | الوحدة الدلالية |       |

## `src/emitters/module`

### emitSolidModule

**النوع:** الوظيفة

```typescript
function emitSolidModule(
  module: SemanticModule,
  options: SolidEmitOptions = {},
): GeneratedSolidModule;
```

تحويل وحدة مكون محايد إلى مصدر SolidJS الخاص بها.

#### حدود

| الاسم          | اكتب               | الوصف |
| -------------- | ------------------ | ----- |
| الوحدة النمطية | الوحدة الدلالية    |       |
| خيارات         | خيارات سوليد إيميت |       |

### GeneratedSolidModule

**النوع:** الواجهة

```typescript
export interface GeneratedSolidModule
```

تم إنتاج مصدر SolidJS لوحدة محايدة واحدة.

### SolidEmitOptions

**النوع:** الواجهة

```typescript
export interface SolidEmitOptions
```

خيارات التحكم في كيفية انبعاث الوحدة النمطية.

## `src/index`

### forgeSolidFramework

**النوع:** الوظيفة

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin;
```

قم بإنشاء ملحق الإخراج Solid ومحولات Vite/Rolldown JSX الخاصة به.

## `src/lower`

### isSolidLowered

**النوع:** الوظيفة

```typescript
function isSolidLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is SolidLoweredModule;
```

تضييق نطاق الخطة المستهدفة إلى الخطة Solid؛ `false` لأي خطة إطار عمل أخرى.

#### حدود

| الاسم | اكتب                            | الوصف |
| ----- | ------------------------------- | ----- |
| خفضت  | TargetLoweredModule \| غير محدد |       |

### LowerSolidModule

**النوع:** الوظيفة

```typescript
function lowerSolidModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

قم بخفض الأشعة تحت الحمراء المحايدة إلى غلاف الهدف المستهدف الخاص بـ Solid، حاملًا الخطة.

#### حدود

| الاسم              | اكتب            | الوصف |
| ------------------ | --------------- | ----- |
| الأشعة تحت الحمراء | الوحدة الدلالية |       |
| السياق             | سياق الهدف      |       |

### PlanSolidImports

**النوع:** الوظيفة

```typescript
function planSolidImports(
  plan: SolidLoweringPlan,
  conditional: boolean,
): string[];
```

أسماء قيمة `solid-js` الدقيقة التي تتطلبها الخطة، مرتبة أبجديًا على النحو التالي:
إعادة التشغيل تنتج قائمة متطابقة. إعادة حساب هذا هو ما يجعل
`solid:drop-unused-imports` وظيفة خالصة للخطة.

#### حدود

| الاسم | اكتب           | الوصف |
| ----- | -------------- | ----- |
| خطة   | خطة خفض الصلبة |       |
| مشروط | منطقية         |       |

### PlanSolidModule

**النوع:** الوظيفة

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan;
```

قم ببناء خطة Solid للحقائق الدلالية للوحدة النمطية.

#### حدود

| الاسم              | اكتب            | الوصف |
| ------------------ | --------------- | ----- |
| الأشعة تحت الحمراء | الوحدة الدلالية |       |

### SOLID_FRAMEWORK

**النوع:** ثابت

```typescript
export const SOLID_FRAMEWORK;
```

معرف إطار العمل الذي يتم التمييز عليه في كل خطة Solid.

### SolidDynamicPlan

**النوع:** الواجهة

```typescript
export interface SolidDynamicPlan
```

تم تخفيض `<Dynamic is={…}>` node إلى النص التشعبي الخاص بـ Solid.

### SolidEffectPlan

**النوع:** الواجهة

```typescript
export interface SolidEffectPlan
```

تم تخفيض `useEffect` إلى `createEffect`/`onMount`، مع `onCleanup` الاختياري.

### SolidListKeyPlan

**النوع:** الواجهة

```typescript
export interface SolidListKeyPlan
```

إسقاط القائمة والمفتاح الذي يحتفظ به Solid.

### SolidLoweredModule

**النوع:** الواجهة

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

تمديد Solid لعقد الخطة المستهدفة المشتركة.

### SolidLoweringPlan

**النوع:** الواجهة

```typescript
export interface SolidLoweringPlan
```

القرارات على شكل Solid المتخذة لوحدة واحدة.

### SolidMemoizedExpression

**النوع:** الواجهة

```typescript
export interface SolidMemoizedExpression
```

تعبير طفل ديناميكي متكرر تمت ترقيته إلى مذكرة خاصة به.

### SolidMemoPlan

**النوع:** الواجهة

```typescript
export interface SolidMemoPlan
```

ربط `useMemo` تم تخفيضه إلى `createMemo`، أو تم طيه إلى ثابت.

### SolidPropPlan

**النوع:** الواجهة

```typescript
export interface SolidPropPlan
```

أحد الدعامات المعلنة وكيف يقرأها Solid.

### SolidRefPlan

**النوع:** الواجهة

```typescript
export interface SolidRefPlan
```

حاوية `useRef` ونوع العنصر الذي تحتوي عليه.

### SolidSignalPlan

**النوع:** الواجهة

```typescript
export interface SolidSignalPlan
```

تم خفض خلية `useState` إلى إشارة Solid.

### SolidSlotPlan

**النوع:** الواجهة

```typescript
export interface SolidSlotPlan
```

فتحة قراءة، يتم التعبير عنها عندما يقرأها عضو الدعائم Solid منها.

## `src/optimize`

### COLLAPSE_SINGLE_CHILD_FRAGMENTS

**النوع:** ثابت

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS;
```

قم بإسقاط جزء يلتف حول عنصر واحد بالضبط.

### DROP_UNUSED_IMPORTS

**النوع:** ثابت

```typescript
export const DROP_UNUSED_IMPORTS;
```

أعد حساب واردات `solid-js` من الخطة المكررة.

### HOIST_STATIC_SUBTREES

**النوع:** ثابت

```typescript
export const HOIST_STATIC_SUBTREES;
```

ترقية الأشجار الفرعية `__mpStatic` إلى ثوابت مستوى الوحدة النمطية.

### MEMOIZE_DYNAMIC_EXPRESSIONS

**النوع:** ثابت

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS;
```

قم بترويج تعبير فرعي ديناميكي متكرر في مذكرة واحدة.

### MP_MEMO_PREFIX

**النوع:** ثابت

```typescript
export const MP_MEMO_PREFIX;
```

بادئة لربط المذكرات يقدمها `solid:memoize-dynamic-expressions`.

### optimSolidModule

**النوع:** الوظيفة

```typescript
function optimizeSolidModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

قم بتحسين خطة Solid المنخفضة. برنامج تضمين الهدف الذي لا يحمل خطة Solid
(مكالمة مباشرة، أو هدف آخر) يتم إرجاعها دون تغيير.

#### حدود

| الاسم  | اكتب                 | الوصف |
| ------ | -------------------- | ----- |
| نوايا  | النوايا المستهدفة    |       |
| خيارات | تارجتوبتيميزيوبتيونس |       |

### STABLE_LIST_KEYS

**النوع:** ثابت

```typescript
export const STABLE_LIST_KEYS;
```

احتفظ فقط بمفاتيح القائمة التي أثبتت الواجهة الأمامية استقرارها.
