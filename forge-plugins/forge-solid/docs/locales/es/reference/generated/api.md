# Referencia de API @mission-platform/forge-plugin-solid

Traducción asistida por máquina a partir de la fuente canónica en inglés. Revisar manualmente cuando sea necesario. Los nombres de paquetes, comandos, rutas e identificadores técnicos no se modifican.

> forge-plugins/forge-solid/docs/reference/generated/api.md: [forge-plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Idioma: Español (es)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generado a partir de declaraciones de fuente pública en `@mission-platform/forge-plugin-solid`.

## `src/emitters/hook-module`

### emitirSolidHookModule

**Tipo:** función

```typescript
function emitSolidHookModule(module: SemanticModule): string
```

Transforme un módulo de enlace neutral en su fuente SolidJS.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| módulo | Módulo Semántico |  |

## `src/emitters/module`

### emitirSolidModule

**Tipo:** función

```typescript
function emitSolidModule(module: SemanticModule, options: SolidEmitOptions = {}): GeneratedSolidModule
```

Transforme un módulo de componente neutro en su fuente SolidJS.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| módulo | Módulo Semántico |  |
| opciones | Opciones de emisión sólida |  |

### Módulo sólido generado

**Tipo:** interfaz

```typescript
export interface GeneratedSolidModule
```

La fuente SolidJS producida para un módulo neutral.

### Opciones de emisión sólida

**Tipo:** interfaz

```typescript
export interface SolidEmitOptions
```

Opciones que controlan cómo se emite un módulo.

## `src/index`

### forjarSolidFramework

**Tipo:** función

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin
```

Cree el complemento de salida Solid y sus adaptadores Vite/Rolldown JSX.

## `src/lower`

### es sólidorebajado

**Tipo:** función

```typescript
function isSolidLowered(lowered: TargetLoweredModule | undefined): lowered is SolidLoweredModule
```

Limitar un plan objetivo al Solid; `false` para el plan de cualquier otro marco.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| bajado | TargetLoweredModule\| indefinido |  |

### módulo sólido inferior

**Tipo:** función

```typescript
function lowerSolidModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

Baje el IR neutral al envoltorio de intención de objetivo de Solid, llevando el plan.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| ir | Módulo Semántico |  |
| contexto | Contexto objetivo |  |

### planSolidImports

**Tipo:** función

```typescript
function planSolidImports(plan: SolidLoweringPlan, conditional: boolean): string[]
```

Los nombres exactos del valor `solid-js` que requiere un plan, ordenados alfabéticamente para que
volver a ejecutar produce una lista idéntica. Recalcular esto es lo que hace
`solid:drop-unused-imports` una función pura del plan.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| plano | Plan de descenso sólido |  |
| condicional | booleano |  |

### planSolidModule

**Tipo:** función

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan
```

Cree el plan Solid para los hechos semánticos de un módulo.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| ir | Módulo Semántico |  |

### SOLID_FRAMEWORK

**Tipo:** constante

```typescript
export const SOLID_FRAMEWORK
```

El identificador de marco por el que se discrimina cada plan Solid.

### Plan Dinámico Sólido

**Tipo:** interfaz

```typescript
export interface SolidDynamicPlan
```

Un `<Dynamic is={…}>` node reducido al hiperíndice de Solid.

### Plan de efecto sólido

**Tipo:** interfaz

```typescript
export interface SolidEffectPlan
```

Un `useEffect` rebajado a `createEffect`/`onMount`, con su opcional `onCleanup`.

### PlanClaveListaSólida

**Tipo:** interfaz

```typescript
export interface SolidListKeyPlan
```

Se guarda una proyección de lista y la clave Solid.

### Módulo SólidoBajado

**Tipo:** interfaz

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

La extensión Solid del contrato del plan de destino compartido.

### Plan de descenso sólido

**Tipo:** interfaz

```typescript
export interface SolidLoweringPlan
```

Las decisiones en forma de Solid se toman para un módulo.

### SolidMemoizedExpresión

**Tipo:** interfaz

```typescript
export interface SolidMemoizedExpression
```

Una expresión infantil dinámica repetida promovida a su propia nota.

### PlanMemoSólido

**Tipo:** interfaz

```typescript
export interface SolidMemoPlan
```

Un enlace `useMemo` reducido a `createMemo` o plegado a una constante.

### PlanPropSólido

**Tipo:** interfaz

```typescript
export interface SolidPropPlan
```

Un accesorio declarado y cómo lo lee Solid.

### PlanRefSólido

**Tipo:** interfaz

```typescript
export interface SolidRefPlan
```

Un contenedor `useRef` y el tipo de elemento que contiene.

### Plan de señal sólida

**Tipo:** interfaz

```typescript
export interface SolidSignalPlan
```

Una celda `useState` reducida a una señal Solid.

### Plan de ranura sólida

**Tipo:** interfaz

```typescript
export interface SolidSlotPlan
```

Una lectura de ranura, expresada como la lee el miembro de accesorios Solid.

## `src/optimize`

### COLAPSE_SINGLE_CHILD_FRAGMENTS

**Tipo:** constante

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS
```

Suelta un fragmento que envuelva exactamente un elemento.

### DROP_UNUSED_IMPORTS

**Tipo:** constante

```typescript
export const DROP_UNUSED_IMPORTS
```

Vuelva a calcular las importaciones `solid-js` del plan refinado.

### HOIST_STATIC_SUBTREES

**Tipo:** constante

```typescript
export const HOIST_STATIC_SUBTREES
```

Promocione los subárboles `__mpStatic` a constantes a nivel de módulo.

### MEMOIZE_DYNAMIC_EXPRESSIONS

**Tipo:** constante

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS
```

Promueva una expresión secundaria dinámica repetida a una sola nota.

### MP_MEMO_PREFIX

**Tipo:** constante

```typescript
export const MP_MEMO_PREFIX
```

Prefijo para las vinculaciones de notas que introduce `solid:memoize-dynamic-expressions`.

### optimizarSolidModule

**Tipo:** función

```typescript
function optimizeSolidModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Refinar un plan Solid reducido. Un contenedor de intención que no incluye ningún plan Solid
(una llamada directa o la de otro objetivo) se devuelve intacta.

#### Parámetros

| Nombre | Tipo | Descripción |
| --- | --- | --- |
| intenciones | Intenciones objetivo |  |
| opciones | Opciones de optimización de destino |  |

### STABLE_LIST_KEYS

**Tipo:** constante

```typescript
export const STABLE_LIST_KEYS
```

Mantenga solo las claves de la lista, la interfaz resultó estable.
