# Riferimento API @mission-platform/forge-plugin-solid

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> forge-plugins/forge-solid/docs/reference/generated/api.md: [forge-plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/forge-plugin-solid`.

## `src/emitters/hook-module`

### emettereSolidHookModule

**Tipo:** funzione

```typescript
function emitSolidHookModule(module: SemanticModule): string
```

Trasforma un modulo hook neutro nella sua sorgente SolidJS.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |

## `src/emitters/module`

### emettereSolidModule

**Tipo:** funzione

```typescript
function emitSolidModule(module: SemanticModule, options: SolidEmitOptions = {}): GeneratedSolidModule
```

Trasforma un modulo componente neutro nella sua sorgente SolidJS.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |
| opzioni | Opzioni SolidEmit |  |

### Modulo solido generato

**Tipo:** interfaccia

```typescript
export interface GeneratedSolidModule
```

Il sorgente SolidJS prodotto per un modulo neutro.

### Opzioni SolidEmit

**Tipo:** interfaccia

```typescript
export interface SolidEmitOptions
```

Opzioni che controllano come viene emesso un modulo.

## `src/index`

### forgeSolidFramework

**Tipo:** funzione

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin
```

Creare il plug-in di output Solid e i relativi adattatori JSX Vite/Rolldown.

## `src/lower`

### è SolidLowered

**Tipo:** funzione

```typescript
function isSolidLowered(lowered: TargetLoweredModule | undefined): lowered is SolidLoweredModule
```

Restringere un piano target a quello Solid; `false` per qualsiasi altro piano di framework.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| abbassato | TargetLoweredModule \| indefinito |  |

### lowerSolidModule

**Tipo:** funzione

```typescript
function lowerSolidModule(ir: SemanticModule, context: TargetContext): TargetIntentions
```

Abbassa l'IR neutro nell'involucro dell'intenzione target di Solid, trasportando il piano.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| io | Modulo Semantico |  |
| contesto | Contesto di destinazione |  |

### planSolidImports

**Tipo:** funzione

```typescript
function planSolidImports(plan: SolidLoweringPlan, conditional: boolean): string[]
```

Il valore esatto `solid-js` nomina i requisiti di un piano, in ordine alfabetico in modo da a
la riesecuzione produce un elenco identico. Ricalcolare questo è ciò che rende
`solid:drop-unused-imports` una pura funzione del piano.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| piano | Piano di abbassamento solido |  |
| condizionale | booleano |  |

### planSolidModule

**Tipo:** funzione

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan
```

Costruisci il piano Solid per i fatti semantici di un modulo.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| io | Modulo Semantico |  |

### SOLID_QUADRO

**Genere:** costante

```typescript
export const SOLID_FRAMEWORK
```

L'identificatore del framework in base al quale viene discriminato ogni piano Solid.

### SolidDynamicPlan

**Tipo:** interfaccia

```typescript
export interface SolidDynamicPlan
```

A `<Dynamic is={…}>` node ridotto all'iperscritto di Solid.

### SolidEffectPlan

**Tipo:** interfaccia

```typescript
export interface SolidEffectPlan
```

Un `useEffect` ridotto a `createEffect`/`onMount`, con il suo `onCleanup` opzionale.

### SolidListKeyPlan

**Tipo:** interfaccia

```typescript
export interface SolidListKeyPlan
```

Per questo viene conservata una proiezione di elenco e la chiave Solid.

### Modulo SolidLowered

**Tipo:** interfaccia

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

L'estensione Solid del contratto del piano con obiettivi condivisi.

### Piano di abbassamento solido

**Tipo:** interfaccia

```typescript
export interface SolidLoweringPlan
```

Le decisioni in formato Solid prese per un modulo.

### SolidMemoizedExpression

**Tipo:** interfaccia

```typescript
export interface SolidMemoizedExpression
```

Un'espressione infantile ripetuta e dinamica promossa al proprio promemoria.

### SolidMemoPlan

**Tipo:** interfaccia

```typescript
export interface SolidMemoPlan
```

Un'associazione `useMemo` ridotta a `createMemo` o ridotta a una costante.

### SolidPropPlan

**Tipo:** interfaccia

```typescript
export interface SolidPropPlan
```

Una prop dichiarata e come la legge Solid.

### SolidRefPlan

**Tipo:** interfaccia

```typescript
export interface SolidRefPlan
```

Un contenitore `useRef` e il tipo di elemento che contiene.

### SolidSignalPlan

**Tipo:** interfaccia

```typescript
export interface SolidSignalPlan
```

Una cella `useState` abbassata a un segnale Solid.

### SolidSlotPlan

**Tipo:** interfaccia

```typescript
export interface SolidSlotPlan
```

Uno slot letto, espresso come il membro props Solid lo legge.

## `src/optimize`

### COLLAPSE_SINGLE_CHILD_FRAGments

**Genere:** costante

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS
```

Rilascia un frammento che racchiude esattamente un elemento.

### DROP_UNUSED_IMPORTS

**Genere:** costante

```typescript
export const DROP_UNUSED_IMPORTS
```

Ricalcolare le importazioni `solid-js` dal piano perfezionato.

### HOIST_STATIC_SUBTREES

**Genere:** costante

```typescript
export const HOIST_STATIC_SUBTREES
```

Promuove le sottostrutture `__mpStatic` a costanti a livello di modulo.

### MEMOIZE_DYNAMIC_EXPRESSIONS

**Genere:** costante

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS
```

Promuovi un'espressione infantile ripetuta e dinamica in un singolo promemoria.

### MP_MEMO_PREFIX

**Genere:** costante

```typescript
export const MP_MEMO_PREFIX
```

Prefisso per le associazioni di promemoria introdotte da `solid:memoize-dynamic-expressions`.

### ottimizzareSolidModule

**Tipo:** funzione

```typescript
function optimizeSolidModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Perfezionare un piano Solid ridotto. Un wrapper di intenzione che non contiene alcun piano Solid
(una chiamata diretta o quella di un altro target) viene restituita intatta.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| intenzioni | Intenzioni target |  |
| opzioni | TargetOptimizeOptions |  |

### STABLE_LIST_KEYS

**Genere:** costante

```typescript
export const STABLE_LIST_KEYS
```

Mantieni solo le chiavi dell'elenco che il frontend si è dimostrato stabile.
