# @mission-platform/forge-plugin-solid API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> forge-plugins/forge-solid/docs/reference/generated/api.md: [forge-plugins/forge-solid/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

`@mission-platform/forge-plugin-solid`의 공개 소스 선언에서 생성됩니다.

## `src/emitters/hook-module`

### EmitSolidHook모듈

**종류:** 기능

```typescript
function emitSolidHookModule(module: SemanticModule): string;
```

중립 후크 모듈을 SolidJS 소스로 변환합니다.

#### 매개변수

| 이름 | 유형      | 설명 |
| ---- | --------- | ---- |
| 모듈 | 의미 모듈 |      |

## `src/emitters/module`

### EmitSolidModule

**종류:** 기능

```typescript
function emitSolidModule(
  module: SemanticModule,
  options: SolidEmitOptions = {},
): GeneratedSolidModule;
```

중립 구성 요소 모듈을 SolidJS 소스로 변환합니다.

#### 매개변수

| 이름 | 유형          | 설명 |
| ---- | ------------- | ---- |
| 모듈 | 의미 모듈     |      |
| 옵션 | SolidEmit옵션 |      |

### 생성된Solid모듈

**종류:** 인터페이스

```typescript
export interface GeneratedSolidModule
```

하나의 중립 모듈을 위해 생성된 SolidJS 소스입니다.

### SolidEmit옵션

**종류:** 인터페이스

```typescript
export interface SolidEmitOptions
```

모듈이 방출되는 방식을 제어하는 ​​옵션입니다.

## `src/index`

### 단조SolidFramework

**종류:** 기능

```typescript
function forgeSolidFramework(): FrameworkOutputPlugin;
```

Solid 출력 플러그인과 해당 Vite/Rolldown JSX 어댑터를 만듭니다.

## `src/lower`

### isSolidLowered

**종류:** 기능

```typescript
function isSolidLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is SolidLoweredModule;
```

목표 계획을 Solid로 좁힙니다. 다른 프레임워크 계획의 경우 `false`입니다.

#### 매개변수

| 이름   | 유형                               | 설명 |
| ------ | ---------------------------------- | ---- |
| 낮아진 | TargetLowered모듈 \| 정의되지 않음 |      |

### lowerSolid모듈

**종류:** 기능

```typescript
function lowerSolidModule(
  ir: SemanticModule,
  context: TargetContext,
): TargetIntentions;
```

중립 IR을 Solid의 목표 의도 래퍼로 낮추어 계획을 전달합니다.

#### 매개변수

| 이름 | 유형         | 설명 |
| ---- | ------------ | ---- |
| IR   | 의미 모듈    |      |
| 맥락 | 타겟컨텍스트 |      |

### planSolidImports

**종류:** 기능

```typescript
function planSolidImports(
  plan: SolidLoweringPlan,
  conditional: boolean,
): string[];
```

계획에 필요한 정확한 `solid-js` 값 이름은 알파벳순으로 정렬되어 있습니다.
다시 실행하면 동일한 목록이 생성됩니다. 다시 계산하면 이렇게 됩니다.
`solid:drop-unused-imports`은 계획의 순수 기능입니다.

#### 매개변수

| 이름   | 유형           | 설명 |
| ------ | -------------- | ---- |
| 계획   | 솔리드하강계획 |      |
| 조건부 | 부울           |      |

### 계획SolidModule

**종류:** 기능

```typescript
function planSolidModule(ir: SemanticModule): SolidLoweringPlan;
```

모듈의 의미론적 사실에 대한 Solid 계획을 구축하십시오.

#### 매개변수

| 이름 | 유형      | 설명 |
| ---- | --------- | ---- |
| IR   | 의미 모듈 |      |

### SOLID_FRAMEWORK

**종류:** 상수

```typescript
export const SOLID_FRAMEWORK;
```

모든 Solid 계획은 프레임워크 식별자로 구별됩니다.

### SolidDynamicPlan

**종류:** 인터페이스

```typescript
export interface SolidDynamicPlan
```

`<Dynamic is={…}>` node이 Solid의 하이퍼스크립트로 낮아졌습니다.

### SolidEffectPlan

**종류:** 인터페이스

```typescript
export interface SolidEffectPlan
```

`useEffect`은 옵션인 `onCleanup`을 사용하여 `createEffect`/`onMount`로 낮아졌습니다.

### SolidListKeyPlan

**종류:** 인터페이스

```typescript
export interface SolidListKeyPlan
```

목록 투영과 Solid 키가 이를 유지합니다.

### SolidLowered모듈

**종류:** 인터페이스

```typescript
export interface SolidLoweredModule extends TargetLoweredModule
```

공유 대상 계획 계약의 Solid 확장입니다.

### 솔리드하강계획

**종류:** 인터페이스

```typescript
export interface SolidLoweringPlan
```

하나의 모듈에 대해 Solid 형태의 결정이 내려졌습니다.

### SolidMemoized표현

**종류:** 인터페이스

```typescript
export interface SolidMemoizedExpression
```

반복되는 동적 자식 표현이 자체 메모로 승격됩니다.

### 솔리드메모플랜

**종류:** 인터페이스

```typescript
export interface SolidMemoPlan
```

`useMemo` 바인딩이 `createMemo`로 낮아지거나 상수로 접혀집니다.

### SolidPropPlan

**종류:** 인터페이스

```typescript
export interface SolidPropPlan
```

하나의 선언된 prop과 Solid이 이를 읽는 방법.

### SolidRefPlan

**종류:** 인터페이스

```typescript
export interface SolidRefPlan
```

`useRef` 컨테이너와 여기에 포함된 요소 유형입니다.

### SolidSignalPlan

**종류:** 인터페이스

```typescript
export interface SolidSignalPlan
```

`useState` 셀이 Solid 신호로 낮아졌습니다.

### SolidSlotPlan

**종류:** 인터페이스

```typescript
export interface SolidSlotPlan
```

Solid이 읽는 소품 멤버로 표현되는 슬롯 읽기입니다.

## `src/optimize`

### COLLAPSE_SINGLE_CHILD_FRAGMENTS

**종류:** 상수

```typescript
export const COLLAPSE_SINGLE_CHILD_FRAGMENTS;
```

정확히 하나의 요소를 래핑하는 조각을 삭제합니다.

### DROP_UNUSED_IMPORTS

**종류:** 상수

```typescript
export const DROP_UNUSED_IMPORTS;
```

개선된 계획에서 `solid-js` 가져오기를 다시 계산합니다.

### HOIST_STATIC_SUBTREES

**종류:** 상수

```typescript
export const HOIST_STATIC_SUBTREES;
```

`__mpStatic` 하위 트리를 모듈 수준 상수로 승격합니다.

### MEMOIZE_DYNAMIC_EXPRESSIONS

**종류:** 상수

```typescript
export const MEMOIZE_DYNAMIC_EXPRESSIONS;
```

반복되는 동적인 자식 표현을 하나의 메모로 승격시킵니다.

### MP_MEMO_PREFIX

**종류:** 상수

```typescript
export const MP_MEMO_PREFIX;
```

`solid:memoize-dynamic-expressions`이 도입한 메모 바인딩의 접두사입니다.

### 최적화SolidModule

**종류:** 기능

```typescript
function optimizeSolidModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

낮아진 Solid 계획을 구체화합니다. Solid 계획이 없는 의도 래퍼
(직접 호출 또는 다른 대상의 호출)은 그대로 반환됩니다.

#### 매개변수

| 이름 | 유형                  | 설명 |
| ---- | --------------------- | ---- |
| 의도 | 타겟의도              |      |
| 옵션 | TargetOptimizeOptions |      |

### STABLE_LIST_KEYS

**종류:** 상수

```typescript
export const STABLE_LIST_KEYS;
```

프런트엔드가 안정적이라고 입증된 목록 키만 유지하세요.
