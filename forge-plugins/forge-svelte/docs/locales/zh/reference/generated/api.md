# @mission-platform/forge-plugin-svelte API 参考

由规范英文源进行的机器辅助翻译。必要时请人工审校。包名、命令、路径与技术标识符保持不变。

> forge-plugins/forge-svelte/docs/reference/generated/api.md: [forge-plugins/forge-svelte/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 语言: 简体中文 (zh)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

根据 `@mission-platform/forge-plugin-svelte` 中的公共源声明生成。

## `src/emitters/component`

### 发出Svelte模块

**种类：**功能

```typescript
function emitSvelteModule(module: SemanticModule, componentName: string = "Component", componentFolders: ReadonlySet<string> = new Set(), plan?: SvelteLoweredModule): SvelteModuleOutput
```

将中性组件模块打印为 Svelte 5 SFC。已经运行的调用者
目标阶段通过细化的`plan`；直接调用者得到一个新鲜的
降低了 1，因此发射器始终由相同的合约驱动。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|模块|语义模块 |  |
|组件名称 |字符串|  |
|组件文件夹 |只读设置<string> |  |
|计划| SvelteLowered 模块 |  |

### Svelte模块输出

**种类：**接口

```typescript
export interface SvelteModuleOutput
```

生成的 `.svelte` 模块以及写在其旁边的任何辅助模块。

## `src/emitters/hook`

### 发出 SvelteHook 模块

**种类：**功能

```typescript
function emitSvelteHookModule(module: SemanticModule): string
```

打印中性挂钩/可组合模块作为其 Svelte 目标 `.ts` 源。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|模块|语义模块 |  |

## `src/index`

### forgeSvelte框架

**种类：**功能

```typescript
function forgeSvelteFramework(): FrameworkOutputPlugin
```

创建 Svelte 输出插件及其 Vite/Rolldown 编译器适配器。

## `src/lower`

### 是SvelteLowered

**种类：**功能

```typescript
function isSvelteLowered(lowered: TargetLoweredModule | undefined): lowered is SvelteLoweredModule
```

目标计划是否是 Svelte 计划——每个消费者都会缩小范围的鉴别因素。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|降低| TargetLoweredModule \|未定义 |  |

### 下层Svelte模块

**种类：**功能

```typescript
function lowerSvelteModule(ir: SemanticModule, context: TargetContext): SvelteTargetIntentions
```

为中立模块构建 Svelte 计划。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|红外|语义模块 |  |
|背景 |目标上下文 |  |

### 精简绑定计划

**种类：**接口

```typescript
export interface SvelteBindingPlan
```

元素通过 `bind:this` 绑定到 `$state` 单元。

### 精简派生计划

**种类：**接口

```typescript
export interface SvelteDerivedPlan
```

`$derived` 绑定。

### 精简动态计划

**种类：**接口

```typescript
export interface SvelteDynamicPlan
```

动态 node 及其实例化的 Svelte 主机元素。

### 精益计划

**种类：**接口

```typescript
export interface SvelteEffectPlan
```

`$effect(…)` 或 `onMount(…)` 生命周期。

### 精简活动计划

**种类：**接口

```typescript
export interface SvelteEventPlan
```

事件绑定降低为 Svelte 5 小写事件属性。

### 精简进口计划

**种类：**接口

```typescript
export interface SvelteImportPlan
```

计划在生成的 `<script>` 中需要导入行。

### 精简初始化计划

**种类：**类型

```typescript
export type SvelteInitializationPlan = |
```

组件脚本初始值设定项按照其在中性源中出现的顺序排列。

### SvelteListKeyPlan

**种类：**接口

```typescript
export interface SvelteListKeyPlan
```

列表和为其推断的 `{#each … (key)}` 键表达式。

### Svelte低位模块

**种类：**接口

```typescript
export interface SvelteLoweredModule extends TargetLoweredModule
```

Svelte 拥有的降低计划，对 `framework` 进行了歧视。

### SvelteProp计划

**种类：**接口

```typescript
export interface SveltePropPlan
```

`$props()` 解构条目中性属性降低到。

### SvelteRenderHelper计划

**种类：**接口

```typescript
export interface SvelteRenderHelperPlan
```

JSX 返回的本地帮助程序 (`const renderX = (a, b) => (<div/>)`) 降低为
Svelte `{#snippet}`。

中性渲染助手是一个参数化的模板值函数，它是
正是 Svelte 5 片段所表达的内容 - 包括安全递归，因此
自引用助手（递归到字段集的字段渲染器）可以工作
没有内联。除了单个返回表达式之外，终端提前返回
守卫和终端 `switch` 表示为有序片段分支。

### SvelteScript计划

**种类：**接口

```typescript
export interface SvelteScriptPlan
```

打印机无法仅从记录中重新发现脚本形成的事实。

### 精简插槽计划

**种类：**接口

```typescript
export interface SvelteSlotPlan
```

一个插槽降低到一个片段道具。

### 精简国家计划

**种类：**接口

```typescript
export interface SvelteStatePlan
```

`$state` 单元格及其中立设置器的分配。

### 精简静态计划

**种类：**类型

```typescript
export type SvelteStaticPlan = HoistedStaticEntry;
```

一个完全静态的标记子树，其命名是为了可以将其提升到片段中。

### SvelteTarget意图

**种类：**接口

```typescript
export interface SvelteTargetIntentions extends TargetIntentions
```

带有完整类型的 Svelte 计划的目标意图。

## `src/optimize`

### 优化Svelte模块

**种类：**功能

```typescript
function optimizeSvelteModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

细化降低的 Svelte 计划。不携带 Svelte 计划的意向（国外
目标的或跳过 `lower` 的调用者）将原封不动地返回。

#### 参数

|名称 |类型 |描述 |
| --- | --- | --- |
|意向 |目标意图|  |
|选项 |目标优化选项 |  |

### SVELTE_优化

**种类：**常数

```typescript
export const SVELTE_OPTIMIZATIONS: readonly string[]
```

此目标可以按应用程序顺序应用的每个优化。
