# @mission-platform/forge-plugin-svelte API-Referenz

Maschinenunterstützte Übersetzung aus der kanonischen englischen Quelle. Bei Bedarf manuell nachprüfen. Paketnamen, Befehle, Pfade und technische Bezeichner bleiben unverändert.

> forge-plugins/forge-svelte/docs/reference/generated/api.md: [forge-plugins/forge-svelte/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Sprache: Deutsch (de)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generiert aus öffentlichen Quelldeklarationen in `@mission-platform/forge-plugin-svelte`.

## `src/emitters/component`

### emitSvelteModule

**Art:** Funktion

```typescript
function emitSvelteModule(
  module: SemanticModule,
  componentName: string = "Component",
  componentFolders: ReadonlySet<string> = new Set(),
  plan?: SvelteLoweredModule,
): SvelteModuleOutput;
```

Drucken Sie ein neutrales Komponentenmodul als Svelte 5 SFC. Ein Anrufer, der bereits ausgeführt wurde
die Zielphasen bestehen den verfeinerten `plan`; Ein direkter Anrufer bekommt einen Fresh
gesenkt, so dass der Emittent immer von demselben Vertrag gesteuert wird.

#### Parameter

| Name              | Geben Sie           | ein Beschreibung |
| ----------------- | ------------------- | ---------------- |
| Modul             | SemanticModule      |                  |
| Komponentenname   | Zeichenfolge        |                  |
| Komponentenordner | ReadonlySet<string> |                  |
| planen            | SvelteLoweredModule |                  |

### SvelteModuleOutput

**Art:** Schnittstelle

```typescript
export interface SvelteModuleOutput
```

Ein generiertes `.svelte`-Modul und alle daneben geschriebenen Hilfsmodule.

## `src/emitters/hook`

### emitSvelteHookModule

**Art:** Funktion

```typescript
function emitSvelteHookModule(module: SemanticModule): string;
```

Drucken Sie ein neutrales Hook-/zusammensetzbares Modul als Svelte-Ziel-`.ts`-Quelle.

#### Parameter

| Name  | Geben Sie      | ein Beschreibung |
| ----- | -------------- | ---------------- |
| Modul | SemanticModule |                  |

## `src/index`

### forgeSvelteFramework

**Art:** Funktion

```typescript
function forgeSvelteFramework(): FrameworkOutputPlugin;
```

Erstellen Sie das Svelte-Ausgabe-Plugin und seine Vite/Rolldown-Compiler-Adapter.

## `src/lower`

### isSvelteLowered

**Art:** Funktion

```typescript
function isSvelteLowered(
  lowered: TargetLoweredModule | undefined,
): lowered is SvelteLoweredModule;
```

Ob es sich bei einem Zielplan um den Svelte-Plan handelt – der Unterscheidungsfaktor, auf den sich jeder Verbraucher einschränkt.

#### Parameter

| Name      | Geben Sie                          | ein Beschreibung |
| --------- | ---------------------------------- | ---------------- |
| abgesenkt | TargetLoweredModule \| undefiniert |                  |

### LowerSvelteModule

**Art:** Funktion

```typescript
function lowerSvelteModule(
  ir: SemanticModule,
  context: TargetContext,
): SvelteTargetIntentions;
```

Erstellen Sie den Svelte-Plan für ein neutrales Modul.

#### Parameter

| Name    | Geben Sie      | ein Beschreibung |
| ------- | -------------- | ---------------- |
| ir      | SemanticModule |                  |
| Kontext | Zielkontext    |                  |

### SvelteBindingPlan

**Art:** Schnittstelle

```typescript
export interface SvelteBindingPlan
```

Eine `$state`-Zelle, in die ein Element über `bind:this` eingebunden ist.

### SvelteDerivedPlan

**Art:** Schnittstelle

```typescript
export interface SvelteDerivedPlan
```

Eine `$derived`-Bindung.

### SvelteDynamicPlan

**Art:** Schnittstelle

```typescript
export interface SvelteDynamicPlan
```

Ein dynamisches node und das Svelte-Hostelement, über das es instanziiert wird.

### SvelteEffectPlan

**Art:** Schnittstelle

```typescript
export interface SvelteEffectPlan
```

Ein `$effect(…)`- oder `onMount(…)`-Lebenszyklus.

### SvelteEventPlan

**Art:** Schnittstelle

```typescript
export interface SvelteEventPlan
```

Eine Ereignisbindung, die auf ein Svelte 5-Ereignisattribut in Kleinbuchstaben reduziert wurde.

### SvelteImportPlan

**Art:** Schnittstelle

```typescript
export interface SvelteImportPlan
```

Eine Importzeile, die der Plan im generierten `<script>` benötigt.

### SvelteInitializationPlan

**Art:** Typ

```typescript
export type SvelteInitializationPlan = |
```

Ein Komponentenskript-Initialisierer in der Reihenfolge, in der er in der neutralen Quelle erschien.

### SvelteListKeyPlan

**Art:** Schnittstelle

```typescript
export interface SvelteListKeyPlan
```

Eine Liste und der dafür abgeleitete Schlüsselausdruck `{#each … (key)}`.

### SvelteLoweredModule

**Art:** Schnittstelle

```typescript
export interface SvelteLoweredModule extends TargetLoweredModule
```

Der Svelte-eigene Senkungsplan unterscheidet sich von `framework`.

### SveltePropPlan

**Art:** Schnittstelle

```typescript
export interface SveltePropPlan
```

Der `$props()`-Destrukturierungseintritt, auf den eine neutrale Stütze abgesenkt wird.

### SvelteRenderHelperPlan

**Art:** Schnittstelle

```typescript
export interface SvelteRenderHelperPlan
```

Ein JSX-zurückgebender lokaler Helfer (`const renderX = (a, b) => (<div/>)`) wurde gesenkt auf
a Svelte `{#snippet}`.

Ein neutraler Render-Helfer ist eine parametrisierte Funktion mit Vorlagenwert
genau das, was ein Svelte 5-Snippet ausdrückt – einschließlich sicherer Rekursion, also a
Der selbstreferenzielle Helfer (ein Feldrenderer, der in Feldsätze rekursiv arbeitet) funktioniert
ohne Inlining. Neben einem einzelnen zurückgegebenen Ausdruck gibt es auch eine vorzeitige Rückgabe des Terminals
Guards und ein Terminal `switch` werden als geordnete Snippet-Zweige dargestellt.

### SvelteScriptPlan

**Art:** Schnittstelle

```typescript
export interface SvelteScriptPlan
```

Die schriftbildenden Fakten kann der Drucker aus den Aufzeichnungen allein nicht wiederentdecken.

### SvelteSlotPlan

**Art:** Schnittstelle

```typescript
export interface SvelteSlotPlan
```

Ein Schlitz, der zu einer Schnipsel-Requisite abgesenkt wurde.

### SvelteStatePlan

**Art:** Schnittstelle

```typescript
export interface SvelteStatePlan
```

Eine `$state`-Zelle und die Zuweisung, zu der ihr neutraler Setzer wird.

### SvelteStaticPlan

**Art:** Typ

```typescript
export type SvelteStaticPlan = HoistedStaticEntry;
```

Ein vollständig statischer Markup-Unterbaum, dessen Name so benannt ist, dass er in ein Snippet gehoben werden kann.

### SvelteTargetIntentions

**Art:** Schnittstelle

```typescript
export interface SvelteTargetIntentions extends TargetIntentions
```

Zielabsichten mit einem vollständig typisierten Svelte-Plan.

## `src/optimize`

### optimierenSvelteModule

**Art:** Funktion

```typescript
function optimizeSvelteModule(
  intentions: TargetIntentions,
  options: TargetOptimizeOptions,
): TargetIntentions;
```

Verfeinern Sie einen abgesenkten Svelte-Plan. Absichten, die keinen Svelte-Plan (einen ausländischen Plan) enthalten
Ziel oder ein Aufrufer, der `lower` übersprungen hat) werden unverändert zurückgegeben.

#### Parameter

| Name      | Geben Sie             | ein Beschreibung |
| --------- | --------------------- | ---------------- |
| Absichten | Zielabsichten         |                  |
| Optionen  | TargetOptimizeOptions |                  |

### SVELTE_OPTIMIZATIONS

**Art:** konstant

```typescript
export const SVELTE_OPTIMIZATIONS: readonly string[];
```

Jede Optimierung, die dieses Ziel anwenden kann, in der Reihenfolge der Anwendung.
