# @mission-platform/forge-plugin-api مرجع واجهة برمجة التطبيقات

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-plugin-api/docs/reference/generated/api.md: [forge-plugins/forge-plugin-api/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-plugin-api`.

## `src/compiler/ast`

### ApplySourceEdits

**النوع:** الوظيفة

```typescript
function applySourceEdits(source: string, edits: readonly SourceEdit[]): string
```

قم بتطبيق تعديلات المصدر غير المتداخلة دون الكشف عن عقد المحلل اللغوي.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| المصدر | سلسلة |  |
| التعديلات | للقراءة فقط تحرير المصدر[] |  |

### CLASS_NAME_ATTRIBUTE

**النوع:** ثابت

```typescript
export const CLASS_NAME_ATTRIBUTE
```

لم يتم تقديم أي وصف.

### COMPONENTS_JSX_MODULES

**النوع:** ثابت

```typescript
export const COMPONENTS_JSX_MODULES
```

لم يتم تقديم أي وصف.

### EventNameForProperty

**النوع:** الوظيفة

```typescript
function eventNameForProperty(propertyName: string): string
```

اشتق اسم الحدث الذي يمثله أ Vue-أسلوب `on<Event>` دعم.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| اسم العقار | سلسلة |  |

### ICONS_JSX_MODULE

**النوع:** ثابت

```typescript
export const ICONS_JSX_MODULE
```

لم يتم تقديم أي وصف.

### JSX_ATTRIBUTE_RENAMES

**النوع:** ثابت

```typescript
export const JSX_ATTRIBUTE_RENAMES: ReadonlyMap<string, string>
```

لم يتم تقديم أي وصف.

### LOCAL_EFFECT_FILE

**النوع:** ثابت

```typescript
export const LOCAL_EFFECT_FILE
```

لم يتم تقديم أي وصف.

### LOCAL_EFFECT_MODULE

**النوع:** ثابت

```typescript
export const LOCAL_EFFECT_MODULE
```

لم يتم تقديم أي وصف.

### LOCAL_JSX_TYPE_NAMES

**النوع:** ثابت

```typescript
export const LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### LOCAL_JSX_TYPES_FILE

**النوع:** ثابت

```typescript
export const LOCAL_JSX_TYPES_FILE
```

لم يتم تقديم أي وصف.

### LOCAL_JSX_TYPES_MODULE

**النوع:** ثابت

```typescript
export const LOCAL_JSX_TYPES_MODULE
```

لم يتم تقديم أي وصف.

### localEffectModuleSource

**النوع:** الوظيفة

```typescript
function localEffectModuleSource(framework: JsxFramework): string
```

تنبعث المشتركة Vue مساعد التأثير؛ الأهداف الأخرى لا تحتاج إلى هذا الملف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الإطار | JsxFramework |  |

### localJsxTypesModuleSource

**النوع:** الوظيفة

```typescript
function localJsxTypesModuleSource(framework: JsxFramework): string
```

قم بإصدار إعلانات JSX البدائية لإطار العمل المحلي دون تبعيات المحلل اللغوي.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الإطار | JsxFramework |  |

### NEUTRAL_COMPILE_TIME_MARKERS

**النوع:** ثابت

```typescript
export const NEUTRAL_COMPILE_TIME_MARKERS: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### NEUTRAL_CONTEXT_VALUES

**النوع:** ثابت

```typescript
export const NEUTRAL_CONTEXT_VALUES: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### NEUTRAL_FRAMEWORK_COMPONENTS

**النوع:** ثابت

```typescript
export const NEUTRAL_FRAMEWORK_COMPONENTS: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### NEUTRAL_MODULE

**النوع:** ثابت

```typescript
export const NEUTRAL_MODULE
```

لم يتم تقديم أي وصف.

### NEUTRAL_RUNTIME_VALUES

**النوع:** ثابت

```typescript
export const NEUTRAL_RUNTIME_VALUES: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### NEUTRAL_VUE_RUNTIME_HOOKS

**النوع:** ثابت

```typescript
export const NEUTRAL_VUE_RUNTIME_HOOKS: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### REACT_ADAPTER_MODULE

**النوع:** ثابت

```typescript
export const REACT_ADAPTER_MODULE
```

لم يتم تقديم أي وصف.

### REACT_TYPE_ALIASES

**النوع:** ثابت

```typescript
export const REACT_TYPE_ALIASES: Readonly<Record<string, string>>
```

لم يتم تقديم أي وصف.

### تحرير المصدر

**النوع:** الواجهة

```typescript
export interface SourceEdit
```

تحرير مصدر ثابت، مرتب حسب الإزاحات في المصدر الأصلي.

### StyleImport

**النوع:** الواجهة

```typescript
export interface StyleImport
```

تم استيراد ورقة الأنماط إلى شجرة مسطحة تم إنشاؤها.

### VUE_ADAPTER_MODULE

**النوع:** ثابت

```typescript
export const VUE_ADAPTER_MODULE
```

لم يتم تقديم أي وصف.

### VUE_BUILTIN_COMPONENTS

**النوع:** ثابت

```typescript
export const VUE_BUILTIN_COMPONENTS: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

### VUE_LOCAL_JSX_TYPE_NAMES

**النوع:** ثابت

```typescript
export const VUE_LOCAL_JSX_TYPE_NAMES: ReadonlySet<string>
```

لم يتم تقديم أي وصف.

## `src/compiler/hoist-static`

### GenericHoistResult

**النوع:** الواجهة

```typescript
export interface GenericHoistResult
```

لم يتم تقديم أي وصف.

### هويستاتيكتري

**النوع:** الواجهة

```typescript
export interface HoistedStaticEntry
```

لم يتم تقديم أي وصف.

### hoistStaticRenderNodes

**النوع:** الوظيفة

```typescript
function hoistStaticRenderNodes(module: GenericModuleAst): GenericHoistResult
```

ارفع سجلات العرض المميزة وأرجع الإدخالات للمولد المستهدف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | GenericModuleAst |  |

### MP_HOIST_PREFIX

**النوع:** ثابت

```typescript
export const MP_HOIST_PREFIX
```

لم يتم تقديم أي وصف.

## `src/compiler/optimize`

### ApplyOptimizationEdits

**النوع:** الوظيفة

```typescript
function applyOptimizationEdits(module: GenericModuleAst, edits: readonly SourceEdit[]): GenericModuleAst
```

قم بتطبيق تعديلات المصدر على مصدر الوحدة النمطية دون الكشف عن نوع تنفيذ AST.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | GenericModuleAst |  |
| التعديلات | للقراءة فقط تحرير المصدر[] |  |

### ConstantBoolean

**النوع:** الوظيفة

```typescript
function constantBoolean(expression: ExpressionLike): boolean | undefined
```

حل قيمة منطقية حرفية دون تقييم التعليمات البرمجية المؤلفة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| تعبير | التعبير مثل |  |

### نتيجة التحسين العامة

**النوع:** الواجهة

```typescript
export interface GenericOptimizationResult
```

لم يتم تقديم أي وصف.

### hasJsxKey

**النوع:** الوظيفة

```typescript
function hasJsxKey(attributes: readonly GenericAttribute[]): boolean
```

ما إذا كانت قائمة سمات JSX العامة تحتوي على مفتاح أم لا.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الصفات | للقراءة فقط GenericAttribute[] |  |

### hasMpStaticMarker

**النوع:** الوظيفة

```typescript
function hasMpStaticMarker(node: GenericRenderNode): boolean
```

سواء كانت JSX عامة node لديه علامة ثابتة خاصة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |

### isCompileTimeConstant

**النوع:** الوظيفة

```typescript
function isCompileTimeConstant(expression: ExpressionLike): boolean
```

ما إذا كان النص المصدر عبارة عن تعبير حرفي خالص في وقت الترجمة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| تعبير | التعبير مثل |  |

### MP_STATIC_ATTR

**النوع:** ثابت

```typescript
export const MP_STATIC_ATTR
```

لم يتم تقديم أي وصف.

### optimgenericModule

**النوع:** الوظيفة

```typescript
function optimizeGenericModule(module: GenericModuleAst, _options: OptimizeOptions = {}): GenericOptimizationResult
```

قم بتطبيق التحسين على مستوى السجل أثناء إرجاع أي تعديلات للمصدر بشكل منفصل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | GenericModuleAst |  |
| _خيارات | خيارات التحسين |  |

### خيارات التحسين

**النوع:** الواجهة

```typescript
export interface OptimizeOptions
```

لم يتم تقديم أي وصف.

### stripMpStaticAttributes

**النوع:** الوظيفة

```typescript
function stripMpStaticAttributes(attributes: readonly GenericAttribute[]): readonly GenericAttribute[]
```

قم بإرجاع قائمة سمات عامة خالية من العلامات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الصفات | للقراءة فقط GenericAttribute[] |  |

### stripMpStaticMarker

**النوع:** الوظيفة

```typescript
function stripMpStaticMarker(node: GenericRenderNode): GenericRenderNode
```

قم بإرجاع عرض عام خالٍ من العلامات node، الحفاظ بشكل متكرر على امتدادات المصدر.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |

## `src/diagnostics`

### تشخيص المترجم

**النوع:** الواجهة

```typescript
export interface CompilerDiagnostic
```

تشخيص يحتفظ بسياق المصدر الكافي لإعداد التقارير على مستوى المرحلة.

### خطأ في تشخيص المترجم

**النوع:** فئة

```typescript
export class CompilerDiagnosticError extends Error
```

حدث خطأ عندما لا تتمكن مرحلة المترجم من إنتاج وحدة صوتية مستهدفة.

### مترجم تشخيص الخطورة

**النوع:** النوع

```typescript
export type CompilerDiagnosticSeverity = "error" | "warning" | "info";
```

خطورة تشخيص المترجم.

### CompilerPhase

**النوع:** النوع

```typescript
export type CompilerPhase = | "frontend" | "ir" | "inference" | "target-lowering" | "optimization" | "generation" | "build";
```

مراحل خط الأنابيب التي يمكنها الإبلاغ عن تشخيص المترجم.

### createCompilerDiagnostic

**النوع:** الوظيفة

```typescript
function createCompilerDiagnostic(diagnostic: Omit<CompilerDiagnostic, "fileName"> & { fileName?: string }): CompilerDiagnostic
```

إنشاء تشخيص دون اقتران عقود المرحلة ل TypeScript العقد.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| التشخيص | حذف<CompilerDiagnostic, "fileName"> & { اسم الملف؟: سلسلة } |  |

### formatCompilerDiagnostic

**النوع:** الوظيفة

```typescript
function formatCompilerDiagnostic(diagnostic: CompilerDiagnostic): string
```

قم بتنسيق تشخيص لخطأ في برنامج التحويل البرمجي دون فقدان موقع المصدر الخاص به.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| التشخيص | تشخيص المترجم |  |

### throwOnCompilerErrors

**النوع:** الوظيفة

```typescript
function throwOnCompilerErrors(diagnostics: readonly CompilerDiagnostic[] | undefined): void
```

قم بإحباط خط أنابيب برنامج التحويل البرمجي عندما تقوم إحدى المراحل بالإبلاغ عن خطأ واحد أو أكثر.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| التشخيص | للقراءة فقط CompilerDiagnostic[] \| غير محدد |  |

## `src/framework`

### FrameworkBuildAdapters

**النوع:** الواجهة

```typescript
export interface FrameworkBuildAdapters
```

إطار مكتوب بشكل مستقل بناء التكامل.

### FrameworkOutputPlugin

**النوع:** الواجهة

```typescript
export interface FrameworkOutputPlugin
```

مكون إضافي قابل للتركيب لإطار عمل ما بعد الأشعة تحت الحمراء.

### FrameworkOutputPluginSelection

**النوع:** النوع

```typescript
export type FrameworkOutputPluginSelection = readonly FrameworkOutputPlugin[];
```

تحديد الهدف المملوكة للمتصل؛ التحقق من الصحة يرفض المعرفات الفارغة والمكررة.

### FrameworkSourceMetadata

**النوع:** الواجهة

```typescript
export interface FrameworkSourceMetadata
```

اصطلاحات شجرة المصدر المطلوبة من قبل برنامج تشغيل البناء المحايد للإطار.

### GeneratedExtraModule

**النوع:** الواجهة

```typescript
export interface GeneratedExtraModule
```

وحدة مساعدة تم إنشاؤها مكتوبة بجوار الوحدة الأساسية التي تم إنشاؤها.

### GeneratedModule

**النوع:** الواجهة

```typescript
export interface GeneratedModule
```

تم إرجاع قطعة أثرية المصدر بواسطة البرنامج المساعد لإخراج الإطار.

### GeneratorContext

**النوع:** النوع

```typescript
export type GeneratorContext = TargetContext;
```

تم تمرير السياق إلى المولدات المصدرية بعد خفض الهدف وتحسينه.

### JsxFramework

**النوع:** النوع

```typescript
export type JsxFramework = "react" | "vue" | "svelte" | "solid" | "web-components";
```

معرفات الإطار المضمنة الحالية؛ قد تستخدم المكونات الإضافية أي معرف سلسلة إضافي.

### خيارات محايدة

**النوع:** الواجهة

```typescript
export interface NeutralOptimizeOptions
```

خيارات التحسين المحايدة المشتركة بين المترجم والمكونات الإضافية المستهدفة.

### لغة الإخراج

**النوع:** النوع

```typescript
export type OutputLanguage = "tsx" | "jsx" | "ts" | "vue" | "svelte" | "astro" | (string &
```

لغات الإخراج المعروفة لدى Forge، مع نقطة امتداد سلسلة مفتوحة للأهداف الجديدة.

### TargetComponentHost

**النوع:** الواجهة

```typescript
export interface TargetComponentHost
```

البيانات التعريفية لاستدعاء المضيف المحايدة للهدف والتي تتم مشاركتها بواسطة مراجع المكونات التي تم إنشاؤها.

### TargetContext

**النوع:** الواجهة

```typescript
export interface TargetContext
```

السياق المشترك عن طريق خفض الهدف والتحسين.

### نوايا الهدف

**النوع:** الواجهة

```typescript
export interface TargetIntentions
```

غلاف النية الخاص بالهدف؛ تظل الحقائق المحايدة متاحة للتمريرات اللاحقة.

### TargetLoweredModule

**النوع:** الواجهة

```typescript
export interface TargetLoweredModule
```

الخطة المملوكة للهدف التي تنتجها المكونات الإضافية `lower` المرحلة والمكررة بها
`optimize` مرحلة. يعلن كل هدف عن تمديده لهذا العقد و
يميزها على {@link TargetLoweredModule.framework}، لذلك يمكن أن تكون الخطة
ضاقت دون الإرسال بينما لا يزال كل مكون إضافي يرضي نفسه
شكل {@link TargetIntentions}.

### خيارات الهدف

**النوع:** الواجهة

```typescript
export interface TargetOptimizeOptions
```

تم تمرير الخيارات إلى مُحسِّن الهدف دون تغيير الخيارات العامة المحايدة.

### TsdownBuildContext

**النوع:** الواجهة

```typescript
export interface TsdownBuildContext
```

سياق مشترك لحزم المكونات الإضافية tsdown/Rolldown المملوكة للإطار.

### ViteBuildContext

**النوع:** الواجهة

```typescript
export interface ViteBuildContext
```

السياق المشترك للإطار المملوك Vite حزم البرنامج المساعد.

## `src/index`

### تعريفForgeFramework

**النوع:** ثابت

```typescript
export const defineForgeFramework
```

الاسم المستعار يؤكد على أن حزم إطار العمل تسجل عمليات التنفيذ المستهدفة.

### تعريفForgeOutputPlugin

**النوع:** الوظيفة

```typescript
function defineForgeOutputPlugin(plugin: T): T
```

التحقق من صحة وإرجاع البرنامج المساعد لإخراج الإطار للتسجيل من قبل المستهلكين.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| البرنامج المساعد | ت |  |

### معرف البرنامج المساعد

**النوع:** النوع

```typescript
export type ForgePluginId = FrameworkOutputPlugin["id"];
```

لم يتم تقديم أي وصف.

### validateForgeOutputPlugin

**النوع:** الوظيفة

```typescript
function validateForgeOutputPlugin(plugin: unknown): asserts plugin is FrameworkOutputPlugin
```

التحقق من صحة البيانات التعريفية المستهدفة بشكل مستقل عن أي سجل إطاري.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| البرنامج المساعد | غير معروف |  |

### validateForgeOutputPluginSelection

**النوع:** الوظيفة

```typescript
function validateForgeOutputPluginSelection(plugins: readonly T[]): readonly T[]
```

التحقق من صحة تحديد الهدف المملوك للمتصل، بما في ذلك المعرفات الفارغة والمكررة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الإضافات | للقراءة فقط T[] |  |

## `src/ir`

### attributeExpressionText

**النوع:** الوظيفة

```typescript
function attributeExpressionText(node: GenericRenderNode, name: string): string | undefined
```

قراءة نص تعبير السمة المسماة، عندما تحمل واحدة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |
| الاسم | سلسلة |  |

### attributeStringValue

**النوع:** الوظيفة

```typescript
function attributeStringValue(node: GenericRenderNode, name: string): string | undefined
```

قراءة قيمة السلسلة الثابتة للسمة المسماة، عندما تحتوي على واحدة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |
| الاسم | سلسلة |  |

### DynamicNodeIntention

**النوع:** الواجهة

```typescript
export interface DynamicNodeIntention
```

عنصر ديناميكي أو اختيار العنصر.

### تأثير النية

**النوع:** الواجهة

```typescript
export interface EffectIntention
```

دورة حياة التأثير وتعبير التنظيف الاختياري الخاص بها.

### EMPTY_SEMANTIC_INTENTIONS

**النوع:** ثابت

```typescript
export const EMPTY_SEMANTIC_INTENTIONS: SemanticIntentions
```

حقائق نية فارغة تستخدمها التركيبات والوحدات النمطية دون أي شيء يمكن استنتاجه.

### EMPTY_SPAN

**النوع:** ثابت

```typescript
export const EMPTY_SPAN: SourceSpan
```

نطاق ذو عرض صفري تستخدمه العقد العامة المركبة.

### EventIntention

**النوع:** الواجهة

```typescript
export interface EventIntention
```

ربط حدث يمكن تمثيله من خلال صيغ مختلفة للسمة المستهدفة.

### findAttribute

**النوع:** الوظيفة

```typescript
function findAttribute(node: GenericRenderNode, name: string): GenericJsxAttribute | undefined
```

ابحث عن السمة المسماة في العرض node.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |
| الاسم | سلسلة |  |

### GenericAstNode

**النوع:** النوع

```typescript
export type GenericAstNode = GenericImport | GenericStatement | GenericRenderNode;
```

لم يتم تقديم أي وصف.

### سمة عامة

**النوع:** النوع

```typescript
export type GenericAttribute = GenericJsxAttribute | GenericJsxSpreadAttribute;
```

لم يتم تقديم أي وصف.

### GenericAttributeValue

**النوع:** النوع

```typescript
export type GenericAttributeValue = |
```

القيمة التي تحملها خاصية JSX.

### GenericBindingKind

**النوع:** النوع

```typescript
export type GenericBindingKind = "identifier" | "object-pattern" | "array-pattern";
```

نموذج الربط لمعلمة الدعائم الخاصة بالمكون.

### مكون عام

**النوع:** الواجهة

```typescript
export interface GenericComponent
```

وظيفة المكون التي تم التعرف عليها في الواجهة الأمامية في الوحدة النمطية.

### GenericExpressionNode

**النوع:** الواجهة

```typescript
export interface GenericExpressionNode
```

A `{ … }` الاستيفاء في موضع الطفل JSX.

### استيراد عام

**النوع:** الواجهة

```typescript
export interface GenericImport
```

حقيقة استيراد محايدة للإطار.

### GenericJsxAttribute

**النوع:** الواجهة

```typescript
export interface GenericJsxAttribute
```

سمة JSX المسماة (`name`, `name="v"`, `name={expr}`).

### GenericJsxSpreadAttribute

**النوع:** الواجهة

```typescript
export interface GenericJsxSpreadAttribute
```

سمة انتشار JSX (`{...props}`).

### GenericModuleAst

**النوع:** الواجهة

```typescript
export interface GenericModuleAst
```

تمثيل المصدر القابل للتسلسل مشترك بين جميع مراحل المترجم.

### معلمة عامة

**النوع:** الواجهة

```typescript
export interface GenericParameter
```

معلمة المكون ونموذج الربط الخاص به ونوعه المُعلن.

### GenericRenderChild

**النوع:** النوع

```typescript
export type GenericRenderChild = GenericRenderNode | GenericTextNode | GenericExpressionNode;
```

لم يتم تقديم أي وصف.

### GenericRenderNode

**النوع:** الواجهة

```typescript
export interface GenericRenderNode
```

عرض محايد للإطار node; قد تقوم عارضات الهدف بخفض تعبير مصدرها أو الحفاظ عليه.

### بيان عام

**النوع:** الواجهة

```typescript
export interface GenericStatement
```

بيان محايد للإطار تحتفظ به الواجهة الأمامية.

### GenericStatementKind

**النوع:** النوع

```typescript
export type GenericStatementKind = | "import" | "interface" | "type-alias" | "enum" | "variable" | "function" | "class" | "export" | "expression" | "return" | "other";
```

تصنيف تقريبي لبيان مستوى الوحدة المحتفظ به.

### GenericTagKind

**النوع:** النوع

```typescript
export type GenericTagKind = "element" | "component" | "fragment" | "dynamic";
```

كيف تقديم nodeيتم حل العلامة في المفردات المستهدفة.

### GenericTextNode

**النوع:** الواجهة

```typescript
export interface GenericTextNode
```

النص الحرفي بين علامات JSX.

### isExpressionNode

**النوع:** الوظيفة

```typescript
function isExpressionNode(child: GenericRenderChild): child is GenericExpressionNode
```

ما إذا كان الطفل المقدم هو a `{ … }` الاستيفاء.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| طفل | GenericRenderChild |  |

### isRenderNode

**النوع:** الوظيفة

```typescript
function isRenderNode(child: GenericRenderChild): child is GenericRenderNode
```

ما إذا كان الطفل الذي يتم تقديمه عبارة عن عنصر/جزء متداخل node.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| طفل | GenericRenderChild |  |

### isTextNode

**النوع:** الوظيفة

```typescript
function isTextNode(child: GenericRenderChild): child is GenericTextNode
```

ما إذا كان الطفل الذي يتم عرضه هو نص JSX حرفيًا.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| طفل | GenericRenderChild |  |

### ListKeyIntention

**النوع:** الواجهة

```typescript
export interface ListKeyIntention
```

مرشح رئيسي مستقر لعارض القائمة المستهدفة.

### مذكرة نية

**النوع:** الواجهة

```typescript
export interface MemoIntention
```

قيمة محفوظة أو مشتقة.

### PropIntention

**النوع:** الواجهة

```typescript
export interface PropIntention
```

إعلان الخاصية المحايدة يُستنتج من توقيع المكون أو نوع الخاصيات.

### RefIntention

**النوع:** الواجهة

```typescript
export interface RefIntention
```

نية مرجعية مستقلة عن تمثيل مرجع الهدف.

### renderNodeTagName

**النوع:** الوظيفة

```typescript
function renderNodeTagName(node: GenericRenderNode): string | undefined
```

اسم العلامة العادي للعرض node، أو `undefined` للعلامة المحسوبة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| node | GenericRenderNode |  |

### النوايا الدلالية

**النوع:** الواجهة

```typescript
export interface SemanticIntentions
```

الحقائق الدلالية المشتركة تم استنتاجها مرة واحدة قبل خفض الهدف.

### SemanticModule

**النوع:** الواجهة

```typescript
export interface SemanticModule
```

الأشعة تحت الحمراء الدلالية المكتوبة التي يستهلكها كل مكون إضافي لإخراج الإطار.

### SlotIntention

**النوع:** الواجهة

```typescript
export interface SlotIntention
```

فتحة قراءة أو فتحة تم تمريرها إلى مكون فرعي.

### sourceBacked

**النوع:** الوظيفة

```typescript
function sourceBacked(text: string, syntax: SourceBackedExpression["syntax"] = "expression", span: SourceSpan = EMPTY_SPAN): SourceBackedExpression
```

أنشئ تعبيرًا مدعومًا بالمصدر من نص مطبوع بالفعل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص | سلسلة |  |
| بناء الجملة | SourceBackedExpression["بناء الجملة"] |  |
| فترة | سورس سبان |  |

### SourceBackedExpression

**النوع:** الواجهة

```typescript
export interface SourceBackedExpression
```

تعبير أو عبارة قد يحتفظ بها الهدف حرفيًا عندما لا يكون له قاعدة تخفيض.

### SourceSpan

**النوع:** الواجهة

```typescript
export interface SourceSpan
```

نطاق مصدر تتم مشاركته بواسطة AST العام والتشخيصات واللقطات.

### نية الدولة

**النوع:** الواجهة

```typescript
export interface StateIntention
```

خلية حالة وأداة الضبط المرتبطة بها، مستقلة عن عناصر التفاعل الأولية المستهدفة.

### walkRenderNodes

**النوع:** الوظيفة

```typescript
function walkRenderNodes(nodes: readonly GenericRenderNode[], visit: (node: GenericRenderNode) => void): void
```

قم بالسير على عمق شجرة العرض أولاً، بما في ذلك علامات التعبير المتداخلة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| العقد | للقراءة فقط GenericRenderNode[] |  |
| زيارة | (node: GenericRenderNode) => باطل |  |
