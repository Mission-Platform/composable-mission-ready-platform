# Riferimento API @mission-platform/forge-plugin-web-components

Traduzione assistita da macchina dalla fonte inglese canonica. Da rivedere manualmente se necessario. Nomi di pacchetti, comandi, percorsi e identificatori tecnici restano invariati.

> forge-plugins/forge-web-components/docs/reference/generated/api.md: [forge-plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Lingua: Italiano (it)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Generato da dichiarazioni di fonte pubblica in `@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### sintetizzaElementClass

**Tipo:** funzione

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string
```

Stampa la sottoclasse `ForgeElement` e la sua registrazione per un piano ribassato.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| piano | WebComponentsLoweredModule |  |

## `src/emitters/module`

### emettereWebComponentModule

**Tipo:** funzione

```typescript
function emitWebComponentModule(module: SemanticModule, componentName: string = "CustomElement", componentFolders: ReadonlySet<string> = new Set(), lowered?: WebComponentsLoweredModule): EmittedWebComponentModule
```

Trasforma l'intero modulo nell'origine di destinazione dei componenti Web nativi.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |
| nomecomponente | stringa |  |
| componenteCartelle | Sola letturaSet<string> |  |
| abbassato | WebComponentsLoweredModule |  |

## `src/index`

### forgeWebComponentsFramework

**Tipo:** funzione

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin
```

Creare il plug-in di output dei componenti Web solo per TypeScript.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**Genere:** costante

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy
```

I componenti interni sono vincolati dalla capacità del runtime; l'associazione del modulo rimane attiva.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**Genere:** costante

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy
```

Impostazioni predefinite di compatibilità conservate per i componenti generati.

### dedurreWebComponentsHost

**Tipo:** funzione

```typescript
function inferWebComponentsHost(returnNode: GenericRenderNode | undefined): WebComponentsHostPlan
```

Dedurre un host integrato personalizzato solo da una singola radice intrinseca statica.

La lista consentita è intenzionalmente conservativa: un tag intrinseco è valido
HTML non garantisce che si tratti di un'integrazione personalizzata sicura o utile
base attraverso le implementazioni DOM supportate.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| returnNode | GenericRenderNode \| indefinito |  |

### isWebComponentsLowered

**Tipo:** funzione

```typescript
function isWebComponentsLowered(lowered: TargetLoweredModule | undefined): lowered is WebComponentsLoweredModule
```

Restringi un piano target al piano Componenti Web senza trasmettere.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| abbassato | TargetLoweredModule \| indefinito |  |

### lowerWebComponentsModule

**Tipo:** funzione

```typescript
function lowerWebComponentsModule(module: SemanticModule, context: TargetContext): TargetIntentions
```

Abbassare l'IR neutro nelle intenzioni target dei componenti Web.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |
| contesto | Contesto di destinazione |  |

### lowerWebComponentsPlan

**Tipo:** funzione

```typescript
function lowerWebComponentsPlan(module: SemanticModule, context: TargetContext): WebComponentsLoweredModule
```

Costruisci il piano target dei componenti Web per un modulo neutro.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |
| contesto | Contesto di destinazione |  |

### TIPO_SCONOSCIUTO

**Genere:** costante

```typescript
export const UNKNOWN_TYPE
```

Il tipo emesso quando non è disponibile né un tipo dichiarato né uno dedotto.

### WEB_COMPONENTS_FRAMEWORK

**Genere:** costante

```typescript
export const WEB_COMPONENTS_FRAMEWORK
```

L'ID del framework del plugin e il discriminatore del suo piano ribassato.

### WEBCOMPONENTS_NATIVE_HOSTS

**Genere:** costante

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS
```

La tabella di compatibilità per root che può essere tranquillamente personalizzata integrata.

### Campo WebComponentsCleanup

**Tipo:** interfaccia

```typescript
export interface WebComponentsCleanupField
```

Un campo privato che mantiene la funzione di pulizia di un effetto tra i callback del ciclo di vita.

### WebComponentsDerivedBody

**Genere:** tipo

```typescript
export type WebComponentsDerivedBody = |
```

Il corpo di un getter derivato, già riscritto nell'ambito dell'istanza dell'elemento:
una fabbrica di promemoria concisa restituisce una singola espressione, una a corpo blocco porta
le proprie istruzioni (che il getter deve eseguire anziché restituire).

### WebComponentsDerivedValue

**Tipo:** interfaccia

```typescript
export interface WebComponentsDerivedValue
```

Un valore memorizzato, ridotto a un getter ricalcolato in lettura.

### WebComponentsElementRef

**Tipo:** interfaccia

```typescript
export interface WebComponentsElementRef
```

Un legame `useRef`, ridotto a una cella `{ current }` detenuta dall'elemento.

### ID generato componenti Web

**Tipo:** interfaccia

```typescript
export interface WebComponentsGeneratedId
```

Un'associazione `useId()` neutra, ridotta a un campo di istanza seminato una volta per elemento.

### WebComponentsHostFallbackReason

**Genere:** tipo

```typescript
export type WebComponentsHostFallbackReason = | "missing-root" | "fragment-root" | "dynamic-root" | "component-root" | "ambiguous-root" | "invalid-root" | "unsupported-root";
```

Motivi stabili per cui un componente non può utilizzare un host integrato personalizzato.

### WebComponentsHostKind

**Genere:** tipo

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

Il modulo host utilizzato da un elemento personalizzato generato.

### WebComponentsHostPlan

**Tipo:** interfaccia

```typescript
export interface WebComponentsHostPlan
```

Selezione dell'host e metadati di chiamata per un componente generato.

### WebComponentsInternalsPolicy

**Tipo:** interfaccia

```typescript
export interface WebComponentsInternalsPolicy
```

Funzionalità ElementInternals richieste da un componente generato.

### WebComponentsLifecycleCallback

**Genere:** tipo

```typescript
export type WebComponentsLifecycleCallback = "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

I callback del ciclo di vita degli elementi personalizzati che un piano può generare.

### WebComponentsLifecycleHook

**Tipo:** interfaccia

```typescript
export interface WebComponentsLifecycleHook
```

Un callback del ciclo di vita generato e le istruzioni che esegue.

### WebComponentsListKey

**Tipo:** interfaccia

```typescript
export interface WebComponentsListKey
```

Un candidato chiave per il rendering dell'elenco conservato per l'output dell'elenco della destinazione.

### Componenti WebLoweredModule

**Tipo:** interfaccia

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

Il piano target dei componenti Web prodotto da `lower` e perfezionato da `optimize`.

### WebComponentsPromotedLocal

**Tipo:** interfaccia

```typescript
export interface WebComponentsPromotedLocal
```

Una costante render-head **promossa** a membro dell'elemento.

Vengono emessi un memo getter, una richiamata del ciclo di vita e un inizializzatore di campo
*esterno* `render()`, quindi un locale per il quale la testa di rendering dichiara non esiste
loro. Promuovere la dichiarazione a un membro è ciò che rende tale lettura
risolvibile — tramite `this.<name>` — invece di penzolare.

Viene promossa solo una dichiarazione dimostrabilmente priva di effetti (vedi
{@link promossoHeadLocals}); tutto il resto rimane in `render()`.

### WebComponentsPropertyDeclaration

**Tipo:** interfaccia

```typescript
export interface WebComponentsPropertyDeclaration
```

Un descrittore della proprietà reattiva `ForgeElement`, che rispecchia il contratto del runtime nativo.

### WebComponentsReactiveProperty

**Tipo:** interfaccia

```typescript
export interface WebComponentsReactiveProperty
```

Una **proprietà** reattiva: un input impostabile esternamente rispecchiato da un attributo.

### Opzioni di registrazione dei componenti Web

**Tipo:** interfaccia

```typescript
export interface WebComponentsRegistrationOptions
```

Le opzioni `customElements.define` emesse per un built-in personalizzato.

### WebComponentsRuntimeImports

**Tipo:** interfaccia

```typescript
export interface WebComponentsRuntimeImports
```

Le importazioni di cui ha bisogno un modulo di elemento generato.

### Fase di installazione dei componenti Web

**Tipo:** interfaccia

```typescript
export interface WebComponentsSetupPhase
```

La fase di **configurazione** una tantum dell'elemento.

Un seme come `useState(parseTime(modelValue))` legge una proprietà reattiva e
una proprietà mantiene un valore solo una volta che gli attributi dell'host sono stati adottati —
che avviene al momento della connessione, molto tempo dopo l'esecuzione del costruttore. Peggio ancora, il
Il valore viene solitamente raggiunto tramite una costante render-head (`const partial =
parseTime(modelValue);`) che non è stato possibile dimostrare privo di effetti e quindi
rimane in `render()`, quindi un seed del costruttore farebbe riferimento a un nome che lo fa
non esistere affatto lì.

Tale seed viene rinviato a `ForgeElement.setup()`, richiamato dal runtime
dopo l'adozione dell'attributo e prima del primo rendering, esattamente una volta per elemento
— una riconnessione non deve eseguire nuovamente il seeding ed eliminare ciò che l'utente ha modificato da allora.
Le dichiarazioni principali di cui il seme ha bisogno vengono riprodotte per prime, in ordine di testa e
transitivamente chiusi, e **solo** quelli.

La riproduzione anziché la memorizzazione nella cache è intenzionale. Nella fonte neutra la testa
*è* il corpo del componente, quindi già `const initial = parseTime(modelValue);`
funziona su ogni rendering; valutarlo ancora una volta durante l'installazione non esegue alcuna chiamata
il componente creato non viene già eseguito a ogni passaggio. Memorizzandolo nella cache in a
il campo *ridurrebbe* invece il numero di valutazioni e darebbe il valore an
identità che la fonte non ha mai promesso. Solo un semplice `const` (o una funzione)
la dichiarazione viene mai riprodotta: vedi {@link headReplay}.

### WebComponentsShadowPolicy

**Tipo:** interfaccia

```typescript
export interface WebComponentsShadowPolicy
```

Politica shadow-root digitata mantenuta dal piano di destinazione.

### WebComponentsStateField

**Tipo:** interfaccia

```typescript
export interface WebComponentsStateField
```

Un campo **stato** reattivo: una cella interna il cui setter ri-renderizza l'elemento.

### WebComponentsStaticTemplatePart

**Tipo:** interfaccia

```typescript
export interface WebComponentsStaticTemplatePart
```

Un blocco di modello completamente statico elevato a una costante a livello di modulo.

### WebComponentsTemplatePlan

**Tipo:** interfaccia

```typescript
export interface WebComponentsTemplatePlan
```

Il piano `render()` abbassato: le sue istruzioni head e il suo modello lit-html.

## `src/optimize`

### ottimizzareWebComponentsModule

**Tipo:** funzione

```typescript
function optimizeWebComponentsModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Ottimizzare le intenzioni target dei componenti Web; passano gli altri bersagli.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| intenzioni | Intenzioni target |  |
| opzioni | TargetOptimizeOptions |  |

### ottimizzareWebComponentsPlan

**Tipo:** funzione

```typescript
function optimizeWebComponentsPlan(plan: WebComponentsLoweredModule, options: TargetOptimizeOptions): WebComponentsLoweredModule
```

Perfeziona un piano di componenti Web ridotto, registrando ogni passaggio eseguito.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| piano | WebComponentsLoweredModule |  |
| opzioni | TargetOptimizeOptions |  |

### WEB_COMPONENTI_OTTIMIZZAZIONI

**Genere:** costante

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS
```

Identificatori registrati in {@link WebComponentsLoweredModule.appliedOptimizations}.

### Ottimizzazione dei componenti Web

**Genere:** tipo

```typescript
export type WebComponentsOptimization = (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

Un identificatore di ottimizzazione dei componenti Web registrato.

## `src/runtime/hook-module`

### emitWebComponentHookModule

**Tipo:** funzione

```typescript
function emitWebComponentHookModule(module: SemanticModule): string
```

Trasforma un modulo hook neutro nella sua sorgente Web-Components.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modulo | Modulo Semantico |  |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**Genere:** costante

```typescript
export const HAS_SLOT_RUNTIME
```

L'helper di runtime nativo a cui si abbassa il marcatore, chiamato con l'elemento host.

### èFunzioneEspressioneTesto

**Tipo:** funzione

```typescript
function isFunctionExpressionText(text: string): boolean
```

Se il testo è un'espressione **con valori di funzione**: una freccia o un
Espressione `function`.

La creazione di una chiusura non ha alcun effetto di per sé, quindi una costante di render-head
può essere promosso a membro dell'elemento (vedere `../lower`) indipendentemente dal suo corpo impuro
è: il corpo continua a funzionare esattamente quando viene chiamato.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| testo | stringa |  |

### èPureExpressionText

**Tipo:** funzione

```typescript
function isPureExpressionText(text: string): boolean
```

Se un'espressione è **dimostrabilmente** priva di effetti: no call, no `new`, no
`await`, nessuna assegnazione e nessun incremento, a qualsiasi profondità.

Utilizzato per decidere se una costante render-head può essere promossa a getter.
Il test è deliberatamente sintattico e pessimistico: una chiamata a un puro aiutante
è indistinguibile da una chiamata a un mutatore, quindi qualsiasi chiamata squalifica il
istruzione e rimane in `render()`.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| testo | stringa |  |

## `src/transformers/props-binding`

### modellooggetto principale

**Tipo:** funzione

```typescript
function leadingObjectPattern(text: string): string | undefined
```

Il modello di oggetto che *inizia* `text`, scartando tutto ciò che segue.

Il testo sorgente di un parametro porta la sua annotazione (`{ a, b }: Readonly<P>`),
quindi il modello deve essere ritagliato prima di poter essere scomposto.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| testo | stringa |  |

### parsePropsBinding

**Tipo:** funzione

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined
```

Decomporre un modello di oggetti di scena.

Restituisce `undefined` quando il testo non è un modello di oggetto o contiene un modello
voce che questo modulo non modella (una chiave calcolata, diciamo) - il chiamante quindi la mantiene
il suo comportamento precedente invece di agire su una lettura parziale.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| modelloTesto | stringa |  |

### PropsBinding

**Tipo:** interfaccia

```typescript
export interface PropsBinding
```

Un pattern di oggetti di scena, scomposto nei membri che legge e nelle entità locali che associa.

### PropsBindingEntry

**Tipo:** interfaccia

```typescript
export interface PropsBindingEntry
```

Una voce di un modello di oggetti di scena.

### propsBindingStatement

**Tipo:** funzione

```typescript
function propsBindingStatement(binding: PropsBinding, isRead: (name: string) => boolean): string | undefined
```

Riproduci un modello di oggetti di scena come `const { … } = this;`, limitato alle voci
i cui locali vengono effettivamente letti dall'ambito di destinazione.

Limitarlo mantiene un ambito sollevato libero da vincoli che non utilizza mai (che a
La build `noUnusedLocals` verrebbe rifiutata) lasciando ogni voce mantenuta - e
quindi è predefinito: identico in byte a quello creato.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| vincolante | Oggetti di scenaRilegatura |  |
| èLeggi | (nome: stringa) => booleano |  |

## `src/transformers/props-type`

### IndexedAccessType

**Tipo:** funzione

```typescript
function indexedAccessType(reference: PropsTypeReference, member: string): string
```

L'annotazione di accesso indicizzato come viene emesso un membro del tipo props.

L'opzionalità del membro viene mantenuta esattamente: un membro obbligatorio *non*
ampliato con `| undefined`. Il campo dell'elemento viene popolato solo una volta
il runtime adotta il suo attributo, quindi è veramente non impostato nel costruttore —
ma ciò viene espresso emettendo il campo come `declare` (il runtime possiede il file
accessor), che lo esenta da `strictPropertyInitialization` senza effettuare
ogni lettura di una prop richiesta possibilmente-`undefined`.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| riferimento | PropsTypeReference |  |
| membro | stringa |  |

### PropsTypeReference

**Tipo:** interfaccia

```typescript
export interface PropsTypeReference
```

È possibile annotare i tipi di proprietà sui campi di un componente.

### risolverePropsTypeReference

**Tipo:** funzione

```typescript
function resolvePropsTypeReference(annotation: string | undefined, declarations: readonly GenericStatement[]): PropsTypeReference | undefined
```

Risolvi l'annotazione props rispetto alle dichiarazioni conservate del modulo.

Restituisce `undefined` quando l'annotazione non è un nome semplice o quando no
l'alias di interfaccia/tipo con quel nome viene mantenuto accanto alla classe dell'elemento —
in entrambi i casi il riferimento non verrebbe risolto nel modulo generato.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| annotazione | stringa \| indefinito |  |
| dichiarazioni | sola lettura GenericStatement[] |  |

### typeMembers

**Tipo:** funzione

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>
```

I membri dichiarati da un corpo di interfaccia/tipo-alias mantenuto, mappati su if
ognuno è facoltativo (`name?: T`).

Vengono raccolti solo i membri al livello più alto del primo corpo `{…}`, e solo
dove un membro può legittimamente iniziare (subito dopo la graffa di apertura, un `;`
o un `,`). I membri forniti da una base `extends`/`&` pertanto *non*
segnalato: il chiamante lo tratta come sconosciuto e ricade, che è il
direzione sicura.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| dichiarazioneTesto | stringa |  |

### unwrapPropsTypeName

**Tipo:** funzione

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined
```

Riduci un'annotazione props al nome semplice a cui fa riferimento, scartando il file
parentesi e wrapper generici che preservano i membri che un componente può scrivere.

#### Parametri

| Nome | Digitare | Descrizione |
| --- | --- | --- |
| testo | stringa \| indefinito |  |
