# Référence API @mission-platform/forge-plugin-web-components

Traduction assistée par machine à partir de la source anglaise canonique. À relire manuellement si besoin. Les noms de paquets, commandes, chemins et identifiants techniques restent inchangés.

> forge-plugins/forge-web-components/docs/reference/generated/api.md: [forge-plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> Langue: Français (fr)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

Généré à partir des déclarations de source publique dans `@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### syntheseElementClass

**Genre :** fonction

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string
```

Imprimez la sous-classe `ForgeElement` et son enregistrement pour un plan abaissé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| planifier | WebComponentsLoweredModule |  |

## `src/emitters/module`

### émettreWebComponentModule

**Genre :** fonction

```typescript
function emitWebComponentModule(module: SemanticModule, componentName: string = "CustomElement", componentFolders: ReadonlySet<string> = new Set(), lowered?: WebComponentsLoweredModule): EmittedWebComponentModule
```

Transformez l'ensemble du module en source cible native Web-Components.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| modules | Module Sémantique |  |
| Nom du composant | chaîne |  |
| dossiers de composants | ReadonlySet<string> |  |
| abaissé | WebComponentsLoweredModule |  |

## `src/index`

### forgeWebComponentsFramework

**Genre :** fonction

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin
```

Créez le plug-in de sortie des composants Web TypeScript uniquement.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**Genre :** constante

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy
```

Les éléments internes sont limités par les capacités du moteur d'exécution ; l'association sous forme reste opt-in.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**Genre :** constante

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy
```

Paramètres de compatibilité par défaut conservés pour les composants générés.

### inférerWebComponentsHost

**Genre :** fonction

```typescript
function inferWebComponentsHost(returnNode: GenericRenderNode | undefined): WebComponentsHostPlan
```

Déduisez un hôte intégré personnalisé uniquement à partir d’une seule racine intrinsèque statique.

La liste blanche est volontairement conservatrice : une balise intrinsèque étant valide
HTML ne garantit pas qu'il s'agit d'un élément intégré personnalisé sûr ou utile.
base à travers les implémentations DOM prises en charge.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| noeud de retour | GénériqueRenderNode \| indéfini |  |

### isWebComponentsLowered

**Genre :** fonction

```typescript
function isWebComponentsLowered(lowered: TargetLoweredModule | undefined): lowered is WebComponentsLoweredModule
```

Affinez un plan cible au plan Web-Components sans diffusion.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| abaissé | CibleLoweredModule \| indéfini |  |

### moduleWebComponents inférieur

**Genre :** fonction

```typescript
function lowerWebComponentsModule(module: SemanticModule, context: TargetContext): TargetIntentions
```

Abaissez l'IR neutre dans les intentions cibles des composants Web.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| modules | Module Sémantique |  |
| contexte | Contexte cible |  |

### LowerWebComponentsPlan

**Genre :** fonction

```typescript
function lowerWebComponentsPlan(module: SemanticModule, context: TargetContext): WebComponentsLoweredModule
```

Construisez le plan cible Web-Components pour un module neutre.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| modules | Module Sémantique |  |
| contexte | Contexte cible |  |

### UNKNOWN_TYPE

**Genre :** constante

```typescript
export const UNKNOWN_TYPE
```

Le type émis lorsque ni un type déclaré ni un type déduit n'est disponible.

### WEB_COMPONENTS_FRAMEWORK

**Genre :** constante

```typescript
export const WEB_COMPONENTS_FRAMEWORK
```

L'ID du framework du plugin, et le discriminateur de son plan abaissé.

### WEBCOMPONENTS_NATIVE_HOSTS

**Genre :** constante

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS
```

Le tableau de compatibilité pour les racines qui peuvent être personnalisées en toute sécurité.

### Champ WebComponentsCleanup

**Genre :** interface

```typescript
export interface WebComponentsCleanupField
```

Un champ privé conservant la fonction de nettoyage d'un effet entre les rappels du cycle de vie.

### WebComponentsDerivedBody

**Genre :** type

```typescript
export type WebComponentsDerivedBody = |
```

Le corps d'un getter dérivé, déjà réécrit dans la portée élément-instance :
une fabrique de mémos concise renvoie une seule expression, une fabrique de blocs apporte
ses propres instructions (que le getter doit exécuter plutôt que retourner).

### WebComponentsDerivedValue

**Genre :** interface

```typescript
export interface WebComponentsDerivedValue
```

Une valeur mémorisée, abaissée à un getter recalculé à la lecture.

### WebComponentsElementRef

**Genre :** interface

```typescript
export interface WebComponentsElementRef
```

Une liaison `useRef`, abaissée à une cellule `{ current }` détenue par l'élément.

### WebComponentsGeneratedId

**Genre :** interface

```typescript
export interface WebComponentsGeneratedId
```

Une liaison `useId()` neutre, réduite à un champ d'instance généré une fois par élément.

### WebComponentsHostFallbackRaison

**Genre :** type

```typescript
export type WebComponentsHostFallbackReason = | "missing-root" | "fragment-root" | "dynamic-root" | "component-root" | "ambiguous-root" | "invalid-root" | "unsupported-root";
```

Raisons stables pour lesquelles un composant ne peut pas utiliser un hôte intégré personnalisé.

### WebComponentsHostKind

**Genre :** type

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

Le formulaire hôte utilisé par un élément personnalisé généré.

### WebComponentsHostPlan

**Genre :** interface

```typescript
export interface WebComponentsHostPlan
```

Métadonnées de sélection d’hôte et d’appel pour un composant généré.

### WebComponentsInternalsPolicy

**Genre :** interface

```typescript
export interface WebComponentsInternalsPolicy
```

Capacités ElementInternals demandées par un composant généré.

### WebComponentsLifecycleCallback

**Genre :** type

```typescript
export type WebComponentsLifecycleCallback = "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

Rappels du cycle de vie des éléments personnalisés qu'un plan peut générer.

### WebComponentsLifecycleHook

**Genre :** interface

```typescript
export interface WebComponentsLifecycleHook
```

Un rappel de cycle de vie généré et les instructions qu'il exécute.

### Clé de liste de composants Web

**Genre :** interface

```typescript
export interface WebComponentsListKey
```

Clé candidate de rendu de liste retenue pour la sortie de liste de la cible.

### WebComponentsLoweredModule

**Genre :** interface

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

Le plan cible Web-Components produit par `lower` et affiné par `optimize`.

### WebComponentsPromotedLocal

**Genre :** interface

```typescript
export interface WebComponentsPromotedLocal
```

Une constante de tête de rendu **promu** au rang de membre d'élément.

Un getter de mémo, un rappel de cycle de vie et un initialiseur de champ sont tous émis
*à l'extérieur* `render()`, donc un local déclaré par la tête de rendu n'existe pas pour
eux. Promouvoir la déclaration auprès d'un membre est ce qui fait une telle lecture
résoluble - via `this.<name>` - au lieu de pendre.

Seule une déclaration dont l’absence d’effet est prouvée est encouragée (voir
{@link promuHeadLocals} ); tout le reste reste dans `render()`.

### WebComponentsPropertyDeclaration

**Genre :** interface

```typescript
export interface WebComponentsPropertyDeclaration
```

Un descripteur de propriété réactive `ForgeElement`, reflétant le contrat du runtime natif.

### WebComponentsReactiveProperty

**Genre :** interface

```typescript
export interface WebComponentsReactiveProperty
```

Une **propriété** réactive : une entrée réglable en externe reflétée à partir d'un attribut.

### Options d'enregistrement des composants Web

**Genre :** interface

```typescript
export interface WebComponentsRegistrationOptions
```

Les options `customElements.define` émises pour un intégré personnalisé.

### WebComponentsRuntimeImports

**Genre :** interface

```typescript
export interface WebComponentsRuntimeImports
```

Les importations dont un module d'élément généré a besoin.

### Phase de configuration des composants Web

**Genre :** interface

```typescript
export interface WebComponentsSetupPhase
```

Phase de **configuration** unique de l'élément.

Une graine telle que `useState(parseTime(modelValue))` lit une propriété réactive et
une propriété ne détient une valeur qu'une fois les attributs de l'hôte adoptés —
ce qui se produit lors de la connexion, longtemps après l'exécution du constructeur. Pire, le
la valeur est généralement atteinte via une constante de tête de rendu (`const initial =
parseTime(modelValue);`) qui n'a pas pu être prouvé sans effet et donc
reste dans `render()`, donc une graine de constructeur ferait référence à un nom qui fait
n'existe pas du tout là-bas.

Une telle graine est reportée à `ForgeElement.setup()`, que le runtime appelle
après l'adoption de l'attribut et avant le premier rendu, exactement une fois par élément
— une reconnexion ne doit pas réamorcer et ignorer ce que l'utilisateur a modifié depuis.
Les déclarations principales dont la graine a besoin y sont relues en premier, dans l'ordre principal et
transitivement fermés, et **uniquement** ceux-là.

La relecture plutôt que la mise en cache est délibérée. Dans la source neutre, la tête
*est* le corps du composant, donc `const initial = parseTime(modelValue);` déjà
s'exécute à chaque rendu ; l'évaluer une fois de plus pendant l'installation n'effectue aucun appel
le composant créé ne fonctionne pas déjà à chaque passe. Le mettre en cache dans un
ce champ *réduirait* à la place le nombre d'évaluations et donnerait à la valeur un
identité que la source n’a jamais promise. Seulement un simple `const` (ou une fonction)
la déclaration est toujours relue — voir {@link headReplay}.

### WebComponentsShadowPolicy

**Genre :** interface

```typescript
export interface WebComponentsShadowPolicy
```

Stratégie de racine fantôme typée conservée par le plan cible.

### WebComponentsStateField

**Genre :** interface

```typescript
export interface WebComponentsStateField
```

Un champ **state** réactif : une cellule interne dont le setter restitue l'élément.

### WebComponentsStaticTemplatePart

**Genre :** interface

```typescript
export interface WebComponentsStaticTemplatePart
```

Un morceau de modèle entièrement statique hissé à une constante au niveau du module.

### WebComponentsTemplatePlan

**Genre :** interface

```typescript
export interface WebComponentsTemplatePlan
```

Le plan `render()` abaissé : ses déclarations principales et son modèle lit-html.

## `src/optimize`

### optimiserWebComponentsModule

**Genre :** fonction

```typescript
function optimizeWebComponentsModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

Optimiser les intentions cibles des Web-Components ; d'autres cibles passent à travers.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| intentions | Intentions cibles |  |
| options | Options d'optimisation cible |  |

### optimiserWebComponentsPlan

**Genre :** fonction

```typescript
function optimizeWebComponentsPlan(plan: WebComponentsLoweredModule, options: TargetOptimizeOptions): WebComponentsLoweredModule
```

Affinez un plan Web-Components réduit, en enregistrant chaque passe exécutée.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| planifier | WebComponentsLoweredModule |  |
| options | Options d'optimisation cible |  |

### WEB_COMPONENTS_OPTIMIZATIONS

**Genre :** constante

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS
```

Identifiants enregistrés dans {@link WebComponentsLoweredModule.appliedOptimizations}.

### Optimisation des composants Web

**Genre :** type

```typescript
export type WebComponentsOptimization = (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

Un identifiant d'optimisation de composants Web enregistré.

## `src/runtime/hook-module`

### émettreWebComponentHookModule

**Genre :** fonction

```typescript
function emitWebComponentHookModule(module: SemanticModule): string
```

Transformez un module hook neutre en sa source Web-Components.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| modules | Module Sémantique |  |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**Genre :** constante

```typescript
export const HAS_SLOT_RUNTIME
```

L'assistant d'exécution natif vers lequel le marqueur descend, appelé avec l'élément hôte.

### isFunctionExpressionText

**Genre :** fonction

```typescript
function isFunctionExpressionText(text: string): boolean
```

Que le texte soit une expression **à valeur fonctionnelle** : une flèche ou un
Expression `function`.

La création d'une fermeture n'a aucun effet en soi, donc une telle constante de tête de rendu
peut être promu membre d'un élément (voir `../lower`), même si son corps est impur
c'est : le corps court toujours exactement quand on l'appelle.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| texte | chaîne |  |

### estPureExpressionText

**Genre :** fonction

```typescript
function isPureExpressionText(text: string): boolean
```

Si une expression est **prouvable** sans effets : pas d'appel, pas de `new`, non
`await`, aucune affectation et aucun incrément, quelle que soit la profondeur.

Utilisé pour décider si une constante de tête de rendu peut être promue en getter.
Le test est délibérément syntaxique et pessimiste – un appel à un pur assistant
est impossible à distinguer d'un appel à un mutateur, donc tout appel disqualifie le
déclaration et elle reste dans `render()`.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| texte | chaîne |  |

## `src/transformers/props-binding`

### modèle d'objet principal

**Genre :** fonction

```typescript
function leadingObjectPattern(text: string): string | undefined
```

Le modèle d'objet qui *démarre* `text`, en ignorant tout ce qui suit.

Le texte source d'un paramètre porte son annotation (`{ a, b }: Readonly<P>`),
le motif doit donc être découpé avant de pouvoir être décomposé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| texte | chaîne |  |

### parsePropsBinding

**Genre :** fonction

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined
```

Décomposez un modèle d'objet d'accessoires.

Renvoie `undefined` lorsque le texte n'est pas un modèle d'objet ou contient un
entrée que ce module ne modélise pas (une clé calculée, par exemple) - l'appelant conserve alors
son comportement antérieur au lieu d'agir sur une lecture partielle.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| motifTexte | chaîne |  |

### AccessoiresReliure

**Genre :** interface

```typescript
export interface PropsBinding
```

Un modèle d'objet props, décomposé en membres qu'il lit et en éléments locaux qu'il lie.

### PropsReliureEntrée

**Genre :** interface

```typescript
export interface PropsBindingEntry
```

Une entrée d'un modèle d'objet d'accessoires.

### propsBindingStatement

**Genre :** fonction

```typescript
function propsBindingStatement(binding: PropsBinding, isRead: (name: string) => boolean): string | undefined
```

Rejouer un modèle d'accessoires en tant que `const { … } = this;`, limité aux entrées
dont les sections locales sont réellement lues par la portée cible.

Le restreindre permet de garder une portée levée libre de liaisons qu'elle n'utilise jamais (ce qu'un
`noUnusedLocals` serait rejeté) tout en laissant chaque entrée conservée - et
donc c'est par défaut - octet identique à celui créé.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| reliure | AccessoiresReliure |  |
| estLire | (nom : chaîne) => booléen |  |

## `src/transformers/props-type`

### Type d'accès indexé

**Genre :** fonction

```typescript
function indexedAccessType(reference: PropsTypeReference, member: string): string
```

L'annotation à accès indexé sous laquelle un membre du type props est émis.

La propre optionnalité du membre est conservée exactement : un membre requis n'est *pas*
élargi avec `| undefined`. Le champ de l'élément n'est renseigné qu'une fois le
le runtime adopte son attribut, il n'est donc véritablement pas défini dans le constructeur -
mais cela s'exprime en émettant le champ sous la forme `declare` (le runtime possède le
accesseur), ce qui l'exempte de `strictPropertyInitialization` sans faire
chaque lecture d'un accessoire requis éventuellement-`undefined`.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| référence | PropsTypeRéférence |  |
| membre | chaîne |  |

### PropsTypeRéférence

**Genre :** interface

```typescript
export interface PropsTypeReference
```

Le type d'accessoires par rapport auquel les champs d'un composant peuvent être annotés.

### solvePropsTypeReference

**Genre :** fonction

```typescript
function resolvePropsTypeReference(annotation: string | undefined, declarations: readonly GenericStatement[]): PropsTypeReference | undefined
```

Résolvez l'annotation props par rapport aux déclarations conservées du module.

Renvoie `undefined` lorsque l'annotation n'est pas un nom simple ou lorsqu'aucun
l'alias d'interface/type de ce nom est conservé à côté de la classe d'élément —
dans les deux cas, la référence ne serait pas résolue dans le module généré.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| annotation | chaîne \| indéfini |  |
| déclarations | lecture seule GenericStatement[] |  |

### typeMembres

**Genre :** fonction

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>
```

Les membres déclarés par un corps d'interface/alias de type conservé, mappés selon que
chacun est facultatif (`name?: T`).

Seuls les membres du niveau supérieur du premier corps `{…}` sont collectés, et seuls
où un membre peut légitimement commencer (juste après l'accolade d'ouverture, un `;`
ou un `,`). Les membres apportés par une base `extends`/`&` ne sont donc *pas*
signalé - l'appelant le traite comme un inconnu et se replie, ce qui est le
direction sûre.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| déclarationTexte | chaîne |  |

### unwrapPropsTypeName

**Genre :** fonction

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined
```

Réduisez une annotation props au nom brut auquel elle fait référence, en déballant le
les parenthèses et les wrappers génériques préservant les membres qu'un composant peut écrire.

#### Paramètres

| Nom | Tapez | Descriptif |
| --- | --- | --- |
| texte | chaîne \| indéfini |  |
