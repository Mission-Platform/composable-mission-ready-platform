# مرجع @mission-platform/forge-plugin-web-components API

ترجمة آلية مساعدة من المصدر الإنجليزي الأساسي. تُراجع يدويًا عند الحاجة. تبقى أسماء الحزم والأوامر والمسارات والمعرّفات التقنية دون تغيير.

> forge-plugins/forge-web-components/docs/reference/generated/api.md: [forge-plugins/forge-web-components/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> اللغة: العربية (ar)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

تم إنشاؤها من إعلانات المصدر العام في `@mission-platform/forge-plugin-web-components`.

## `src/emitters/element`

### syntheseElementClass

**النوع:** الوظيفة

```typescript
function synthesiseElementClass(plan: WebComponentsLoweredModule): string
```

اطبع الفئة الفرعية `ForgeElement` وتسجيلها للخطة المخفضة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خطة | WebComponentsLoweredModule |  |

## `src/emitters/module`

### emitWebComponentModule

**النوع:** الوظيفة

```typescript
function emitWebComponentModule(module: SemanticModule, componentName: string = "CustomElement", componentFolders: ReadonlySet<string> = new Set(), lowered?: WebComponentsLoweredModule): EmittedWebComponentModule
```

تحويل الوحدة بأكملها إلى المصدر الهدف الأصلي لمكونات الويب.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |
| اسم المكون | سلسلة |  |
| مجلدات المكونات | ReadonlySet<string> |  |
| خفضت | WebComponentsLoweredModule |  |

## `src/index`

### forgeWebComponentsFramework

**النوع:** الوظيفة

```typescript
function forgeWebComponentsFramework(): FrameworkOutputPlugin
```

قم بإنشاء البرنامج الإضافي لإخراج Web Components TypeScript فقط.

## `src/lower`

### DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY

**النوع:** ثابت

```typescript
export const DEFAULT_WEBCOMPONENTS_INTERNALS_POLICY: WebComponentsInternalsPolicy
```

يتم تحديد القدرات الداخلية حسب وقت التشغيل؛ يبقى اقتران النموذج اختياريًا.

### DEFAULT_WEBCOMPONENTS_SHADOW_POLICY

**النوع:** ثابت

```typescript
export const DEFAULT_WEBCOMPONENTS_SHADOW_POLICY: WebComponentsShadowPolicy
```

تم الاحتفاظ بافتراضيات التوافق للمكونات التي تم إنشاؤها.

### inferWebComponentsHost

**النوع:** الوظيفة

```typescript
function inferWebComponentsHost(returnNode: GenericRenderNode | undefined): WebComponentsHostPlan
```

استنتج مضيفًا مدمجًا مخصصًا فقط من جذر جوهري ثابت واحد.

القائمة المسموح بها متحفظة عمدًا: حيث تكون العلامة الجوهرية صالحة
لا تضمن لغة HTML أنها آمنة أو مفيدة ومدمجة ومخصصة
قاعدة عبر تطبيقات DOM المدعومة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| عقدة الإرجاع | GenericRenderNode \| غير محدد |  |

### isWebComponentsLowered

**النوع:** الوظيفة

```typescript
function isWebComponentsLowered(lowered: TargetLoweredModule | undefined): lowered is WebComponentsLoweredModule
```

تضييق نطاق الخطة المستهدفة لخطة مكونات الويب دون الإرسال.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خفضت | TargetLoweredModule \| غير محدد |  |

### LowerWebComponentsModule

**النوع:** الوظيفة

```typescript
function lowerWebComponentsModule(module: SemanticModule, context: TargetContext): TargetIntentions
```

خفض الأشعة تحت الحمراء المحايدة في النوايا المستهدفة لمكونات الويب.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |
| السياق | سياق الهدف |  |

### lowerWebComponentsPlan

**النوع:** الوظيفة

```typescript
function lowerWebComponentsPlan(module: SemanticModule, context: TargetContext): WebComponentsLoweredModule
```

قم ببناء الخطة المستهدفة لمكونات الويب لوحدة محايدة.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |
| السياق | سياق الهدف |  |

### UNKNOWN_TYPE

**النوع:** ثابت

```typescript
export const UNKNOWN_TYPE
```

النوع المنبعث عند عدم توفر نوع معلن أو مستنتج.

### WEB_COMPONENTS_FRAMEWORK

**النوع:** ثابت

```typescript
export const WEB_COMPONENTS_FRAMEWORK
```

معرف إطار عمل المكون الإضافي ومميز خطته المنخفضة.

### WEBCOMPONENTS_NATIVE_HOSTS

**النوع:** ثابت

```typescript
export const WEBCOMPONENTS_NATIVE_HOSTS
```

جدول التوافق للجذور التي يمكن تخصيصها بشكل آمن.

### WebComponentsCleanupField

**النوع:** الواجهة

```typescript
export interface WebComponentsCleanupField
```

حقل خاص يحتفظ بوظيفة تنظيف التأثير بين عمليات الاسترجاعات الخاصة بدورة الحياة.

### WebComponentsDerivedBody

**النوع:** النوع

```typescript
export type WebComponentsDerivedBody = |
```

نص getter المشتق، تمت إعادة كتابته بالفعل في نطاق مثيل العنصر:
يُرجع مصنع المذكرات الموجزة تعبيرًا واحدًا، بينما يجلب التعبير المتماسك
البيانات الخاصة به (والتي يجب على المُحصل تشغيلها بدلاً من إعادتها).

### WebComponentsDerivedValue

**النوع:** الواجهة

```typescript
export interface WebComponentsDerivedValue
```

قيمة محفوظة، تم تخفيضها إلى قيمة يُعاد حسابها عند القراءة.

### WebComponentsElementRef

**النوع:** الواجهة

```typescript
export interface WebComponentsElementRef
```

ربط `useRef`، تم تخفيضه إلى خلية `{ current }` التي يحتفظ بها العنصر.

### WebComponentsGeneratedId

**النوع:** الواجهة

```typescript
export interface WebComponentsGeneratedId
```

ربط `useId()` محايد، تم تخفيضه إلى حقل مثيل تم تصنيفه مرة واحدة لكل عنصر.

### WebComponentsHostFallbackReason

**النوع:** النوع

```typescript
export type WebComponentsHostFallbackReason = | "missing-root" | "fragment-root" | "dynamic-root" | "component-root" | "ambiguous-root" | "invalid-root" | "unsupported-root";
```

الأسباب الثابتة لعدم قدرة أحد المكونات على استخدام مضيف مدمج مخصص.

### WebComponentsHostKind

**النوع:** النوع

```typescript
export type WebComponentsHostKind = "customized-built-in" | "autonomous";
```

النموذج المضيف المستخدم بواسطة عنصر مخصص تم إنشاؤه.

### WebComponentsHostPlan

**النوع:** الواجهة

```typescript
export interface WebComponentsHostPlan
```

اختيار المضيف والبيانات التعريفية للاستدعاء للمكون الذي تم إنشاؤه.

### WebComponentsInternalsPolicy

**النوع:** الواجهة

```typescript
export interface WebComponentsInternalsPolicy
```

قدرات ElementInternals المطلوبة بواسطة المكون الذي تم إنشاؤه.

### WebComponentsLifecycleCallback

**النوع:** النوع

```typescript
export type WebComponentsLifecycleCallback = "connectedCallback" | "disconnectedCallback" | "updatedCallback";
```

عمليات الاسترجاعات الخاصة بدورة حياة العنصر المخصص التي يمكن للخطة إنشاؤها.

### WebComponentsLifecycleHook

**النوع:** الواجهة

```typescript
export interface WebComponentsLifecycleHook
```

تم إنشاء رد اتصال لدورة الحياة والبيانات التي يتم تشغيلها.

### WebComponentsListKey

**النوع:** الواجهة

```typescript
export interface WebComponentsListKey
```

يتم الاحتفاظ بالمرشح الرئيسي لعرض القائمة لمخرجات قائمة الهدف.

### WebComponentsLoweredModule

**النوع:** الواجهة

```typescript
export interface WebComponentsLoweredModule extends TargetLoweredModule
```

الخطة المستهدفة لمكونات الويب التي تم إنتاجها بواسطة `lower` وتم تنقيحها بواسطة `optimize`.

### WebComponentsPromotedLocal

**النوع:** الواجهة

```typescript
export interface WebComponentsPromotedLocal
```

ثابت تقديم الرأس **ترقيته** إلى عضو عنصر.

يتم إصدار كل من مُحضر المذكرة ورد اتصال دورة الحياة ومُهيئ الحقل
*خارج* `render()`، لذا فإن المنطقة المحلية التي يعلن عنها رأس التجسيد غير موجودة
لهم. الترويج للإعلان للعضو هو ما يجعل مثل هذه القراءة
قابلة للحل — من خلال `this.<name>` — بدلاً من التعلق.

يتم الترويج فقط للإعلان الخالي من التأثير (انظر
{@link PromotionHeadLocals}); أي شيء آخر يبقى في `render()`.

### WebComponentsPropertyDeclaration

**النوع:** الواجهة

```typescript
export interface WebComponentsPropertyDeclaration
```

واصف خاصية تفاعلية `ForgeElement`، يعكس عقد وقت التشغيل الأصلي.

### WebComponentsReactiveProperty

**النوع:** الواجهة

```typescript
export interface WebComponentsReactiveProperty
```

**خاصية** تفاعلية: مدخل قابل للضبط خارجيًا معكوس من إحدى السمات.

### خيارات التسجيل لمكونات الويب

**النوع:** الواجهة

```typescript
export interface WebComponentsRegistrationOptions
```

تم إصدار خيارات `customElements.define` لملف مدمج مخصص.

### WebComponentsRuntimeImports

**النوع:** الواجهة

```typescript
export interface WebComponentsRuntimeImports
```

الواردات التي تحتاجها وحدة العناصر التي تم إنشاؤها.

### WebComponentsSetupPhase

**النوع:** الواجهة

```typescript
export interface WebComponentsSetupPhase
```

مرحلة **الإعداد** للعنصر لمرة واحدة.

تقرأ بذرة مثل `useState(parseTime(modelValue))` خاصية تفاعلية، و
لا تحتفظ الخاصية بقيمة إلا بعد اعتماد سمات المضيف —
والذي يحدث عند الاتصال، بعد فترة طويلة من تشغيل المنشئ. والأسوأ من ذلك،
عادةً ما يتم الوصول إلى القيمة من خلال ثابت رأس العرض (`const الأولي =
parseTime(modelValue);`) التي لا يمكن إثبات أنها خالية من التأثير وبالتالي
يبقى في `render()`، لذا فإن بذرة المُنشئ ستشير إلى الاسم الذي يفعل ذلك
غير موجودة هناك على الإطلاق.

يتم تأجيل هذه البذرة إلى `ForgeElement.setup()`، الذي يستدعيه وقت التشغيل
بعد اعتماد السمة وقبل العرض الأول، مرة واحدة بالضبط لكل عنصر
- يجب ألا يتم إعادة زرع البذور في عملية إعادة الاتصال وتجاهل ما قام المستخدم بتغييره منذ ذلك الحين.
عبارات الرأس يتم إعادة عرض احتياجات البذور هناك أولاً، بترتيب الرأس و
مغلقة بشكل متعد، و **فقط** تلك.

إعادة التشغيل بدلاً من التخزين المؤقت أمر متعمد. في المصدر المحايد الرأس
*هو* نص المكون، لذا فإن `const initial = parseTime(modelValue);` بالفعل
يعمل على كل تقديم؛ تقييمه مرة أخرى أثناء الإعداد لا يؤدي إلى أي اتصال
لا يتم تنفيذ المكون المؤلف بالفعل في كل تمريرة. التخزين المؤقت في أ
سيؤدي الحقل بدلاً من ذلك إلى *تقليل* عدد التقييمات وإعطاء القيمة
هوية المصدر لم يعد أبدا. فقط `const` عادي (أو وظيفة)
يتم إعادة تشغيل الإعلان دائمًا - راجع {@link headReplay}.

### WebComponentsShadowPolicy

**النوع:** الواجهة

```typescript
export interface WebComponentsShadowPolicy
```

سياسة جذر الظل المكتوبة التي تحتفظ بها الخطة المستهدفة.

### WebComponentsStateField

**النوع:** الواجهة

```typescript
export interface WebComponentsStateField
```

حقل **الحالة** التفاعلي: خلية داخلية يقوم محددها بإعادة عرض العنصر.

### WebComponentsStaticTemplatePart

**النوع:** الواجهة

```typescript
export interface WebComponentsStaticTemplatePart
```

قطعة قالب ثابتة بالكامل مرفوعة إلى ثابت على مستوى الوحدة.

### WebComponentsTemplatePlan

**النوع:** الواجهة

```typescript
export interface WebComponentsTemplatePlan
```

خطة `render()` المنخفضة: بياناتها الرئيسية ونموذج HTML المضاء الخاص بها.

## `src/optimize`

### الأمثلWebComponentsModule

**النوع:** الوظيفة

```typescript
function optimizeWebComponentsModule(intentions: TargetIntentions, options: TargetOptimizeOptions): TargetIntentions
```

تحسين النوايا المستهدفة لمكونات الويب؛ أهداف أخرى تمر عبرها.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نوايا | النوايا المستهدفة |  |
| خيارات | تارجتوبتيميزيوبتيونس |  |

### optimwebComponentsPlan

**النوع:** الوظيفة

```typescript
function optimizeWebComponentsPlan(plan: WebComponentsLoweredModule, options: TargetOptimizeOptions): WebComponentsLoweredModule
```

قم بتحسين خطة مكونات الويب المنخفضة، وتسجيل كل تمريرة تم تشغيلها.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| خطة | WebComponentsLoweredModule |  |
| خيارات | تارجتوبتيميزيوبتيونس |  |

### WEB_COMPONENTS_OPTIMIZATIONS

**النوع:** ثابت

```typescript
export const WEB_COMPONENTS_OPTIMIZATIONS
```

المعرفات المسجلة في {@link WebComponentsLoweredModule.appliedOptimizations}.

### تحسين مكونات الويب

**النوع:** النوع

```typescript
export type WebComponentsOptimization = (typeof WEB_COMPONENTS_OPTIMIZATIONS)[keyof typeof WEB_COMPONENTS_OPTIMIZATIONS];
```

معرف تحسين مكونات الويب المسجل.

## `src/runtime/hook-module`

### emitWebComponentHookModule

**النوع:** الوظيفة

```typescript
function emitWebComponentHookModule(module: SemanticModule): string
```

تحويل وحدة ربط محايدة إلى مصدر مكونات الويب الخاصة بها.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| الوحدة النمطية | الوحدة الدلالية |  |

## `src/transformers/expression`

### HAS_SLOT_RUNTIME

**النوع:** ثابت

```typescript
export const HAS_SLOT_RUNTIME
```

مساعد وقت التشغيل الأصلي الذي تنخفض إليه العلامة، ويتم استدعاؤه باستخدام العنصر المضيف.

### isFunctionExpressionText

**النوع:** الوظيفة

```typescript
function isFunctionExpressionText(text: string): boolean
```

ما إذا كان النص تعبيرًا **ذو قيمة دالة** — سهمًا أو أ
تعبير `function`.

إن إنشاء إغلاق ليس له أي تأثير في حد ذاته، لذا فإن مثل هذا التجسيد ثابت
يمكن ترقيته إلى عضو عنصري (انظر `../lower`) مهما كان جسمه غير نقي
هو: الجسم لا يزال يعمل بالضبط عندما يتم استدعاؤه.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص | سلسلة |  |

### isPureExpressionText

**النوع:** الوظيفة

```typescript
function isPureExpressionText(text: string): boolean
```

ما إذا كان التعبير **يمكن إثباته** خاليًا من التأثيرات: لا يوجد اتصال، لا `new`، لا
`await`، لا تخصيص ولا زيادة، في أي عمق.

يُستخدم لتحديد ما إذا كان من الممكن ترقية ثابت رأس العرض إلى getter.
الاختبار نحوي ومتشائم بشكل متعمد - دعوة إلى مساعد خالص
لا يمكن تمييزه عن استدعاء المتحول، لذا فإن أي استدعاء يؤدي إلى عدم أهلية
البيان ويبقى في `render()`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص | سلسلة |  |

## `src/transformers/props-binding`

### leadingObjectPattern

**النوع:** الوظيفة

```typescript
function leadingObjectPattern(text: string): string | undefined
```

نمط الكائن الذي *يبدأ* `text`، مع تجاهل ما يلي.

يحمل النص المصدر للمعلمة تعليقًا توضيحيًا (`{ a, b }: Readonly<P>`)،
لذلك يجب قطع النمط قبل أن يتحلل.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص | سلسلة |  |

### parsePropsBinding

**النوع:** الوظيفة

```typescript
function parsePropsBinding(patternText: string): PropsBinding | undefined
```

قم بتفكيك نمط كائن الدعائم.

تقوم بإرجاع `undefined` عندما لا يكون النص نمط كائن، أو يحتوي على
إدخال هذه الوحدة لا نموذج (مفتاح محسوب، على سبيل المثال) - ثم يحتفظ المتصل
سلوكه السابق بدلاً من التصرف بناءً على قراءة جزئية.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص النمط | سلسلة |  |

### PropsBinding

**النوع:** الواجهة

```typescript
export interface PropsBinding
```

نمط كائن دعائم، متحلل إلى الأعضاء التي يقرأها والأعضاء المحليين الذين يربطهم.

### PropsBindingEntry

**النوع:** الواجهة

```typescript
export interface PropsBindingEntry
```

إدخال واحد لنمط كائن الدعائم.

### propsBindingStatement

**النوع:** الوظيفة

```typescript
function propsBindingStatement(binding: PropsBinding, isRead: (name: string) => boolean): string | undefined
```

أعد تشغيل نمط الدعائم كـ `const { … } = this;`، مقيدًا بالإدخالات
الذي يقرأ النطاق المستهدف بالفعل سكانه المحليين.

يؤدي تقييده إلى إبقاء النطاق المرفوع خاليًا من الارتباطات التي لا يستخدمها أبدًا (والتي أ
سيتم رفض الإصدار `noUnusedLocals`) مع ترك كل إدخال محتفظ به - و
لذلك فإن البايت الافتراضي مطابق للبايت المؤلف.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| ملزمة | بروبسبيندينج |  |
| إقرأ | (الاسم: سلسلة) => منطقي |  |

## `src/transformers/props-type`

### indexedAccessType

**النوع:** الوظيفة

```typescript
function indexedAccessType(reference: PropsTypeReference, member: string): string
```

يتم إصدار التعليق التوضيحي للوصول المفهرس كعضو من نوع الدعائم.

يتم الاحتفاظ باختيارية العضو تمامًا: العضو المطلوب *ليس*
اتسعت مع `| undefined`. يتم ملء حقل العنصر مرة واحدة فقط
يتبنى وقت التشغيل سمته، لذلك لا يتم ضبطه بشكل حقيقي في المُنشئ —
ولكن يتم التعبير عن ذلك عن طريق إرسال الحقل كـ `declare` (يمتلك وقت التشغيل ملف
Accessor)، والذي يعفيه من `strictPropertyInitialization` دون إجراء
كل قراءة للدعامة المطلوبة ربما هي `undefined`.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| مرجع | PropsTypeReference |  |
| عضو | سلسلة |  |

### PropsTypeReference

**النوع:** الواجهة

```typescript
export interface PropsTypeReference
```

يمكن إضافة تعليقات توضيحية إلى حقول المكون مقابل نوع الدعائم.

### ResolvePropsTypeReference

**النوع:** الوظيفة

```typescript
function resolvePropsTypeReference(annotation: string | undefined, declarations: readonly GenericStatement[]): PropsTypeReference | undefined
```

قم بحل التعليقات التوضيحية للدعائم مقابل الإعلانات المحتجزة للوحدة.

تُرجع `undefined` عندما لا يكون التعليق التوضيحي اسمًا عاديًا، أو عندما لا
يتم الاحتفاظ بالاسم المستعار للواجهة/النوع لهذا الاسم بجوار فئة العنصر -
وفي كلتا الحالتين لن يتم حل المرجع في الوحدة النمطية التي تم إنشاؤها.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| شرح | سلسلة \| غير محدد |  |
| تصريحات | للقراءة فقط GenericStatement[] |  |

### typeMembers

**النوع:** الوظيفة

```typescript
function typeMembers(declarationText: string): ReadonlyMap<string, boolean>
```

يتم الإعلان عن الأعضاء الذين يعلنهم نص الواجهة/الاسم المستعار للنوع، ويتم تعيينهم إلى ما إذا كان
كل واحد اختياري (`name?: T`).

يتم جمع الأعضاء في المستوى الأعلى من نص `{…}` الأول فقط
حيث يمكن للعضو أن يبدأ بشكل شرعي (مباشرة بعد قوس الفتح، `;`
أو `,`). وبالتالي فإن الأعضاء الذين ساهموا بقاعدة `extends`/`&` هم *ليسوا*
مُبلغ - يعاملهم المتصل على أنهم مجهولون ويتراجع، وهو
الاتجاه الآمن.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص الإعلان | سلسلة |  |

### unwrapPropsTypeName

**النوع:** الوظيفة

```typescript
function unwrapPropsTypeName(text: string | undefined): string | undefined
```

قم بتقليل التعليق التوضيحي للدعائم إلى الاسم العادي الذي تشير إليه، وإلغاء تغليف ملف
الأقواس والأغلفة العامة التي تحافظ على الأعضاء قد يكتبها أحد المكونات.

#### حدود

| الاسم | اكتب | الوصف |
| --- | --- | --- |
| نص | سلسلة \| غير محدد |  |
