# @mission-platform/forge-cms-jekyll הפניה ל-API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> forge-plugins/forge-cms-jekyll/docs/reference/generated/api.md: [forge-plugins/forge-cms-jekyll/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב `@mission-platform/forge-cms-jekyll`.

## `src/jekyll`

### COMPONENTS_DATA_FILE

**סוג:** קבוע

```typescript
export const COMPONENTS_DATA_FILE;
```

הסכימה המצטברת שכל אתר שנוצר קורא ממנה `site.data`.

### CONFIG_FILE

**סוג:** קבוע

```typescript
export const CONFIG_FILE;
```

קטע התצורה של אתר מתמזג לשלו `_config.yml`.

### forgeJekyllCms

פונקציה **סוג:**

```typescript
function forgeJekyllCms(options: ForgeJekyllCmsOptions): CmsOutputPlugin;
```

קשר את הקרנת Jekyll (נוזל) לתוסף פלט מסגרת בבעלות מתקשר.

#### פרמטרים

| שם       | הקלד                  | תיאור |
| -------- | --------------------- | ----- |
| אפשרויות | ForgeJekyllCmsOptions |       |

### ForgeJekyllCmsOptions

**סוג:** ממשק

```typescript
export interface ForgeJekyllCmsOptions
```

אפשרויות עבור {@link forgeJekyllCms}.

## `src/manifest`

### emitComponentsData

פונקציה **סוג:**

```typescript
function emitComponentsData(
  components: readonly ContentComponent[],
  namespace: string = DEFAULT_INCLUDE_NAMESPACE,
): string;
```

לִפְלוֹט `_data/forge-components.yml`: הסכימה של כל הנפלט כוללת.

נעשה שימוש ברצף ברמה העליונה ולא במיפוי `site.data` איטרציה
שומר על סדר הרכיבים של המהדר, שהוא הסדר של גלריה או א
דף התיעוד שנוצר צריך לרשום אותם.

#### פרמטרים

| שם        | הקלד                           | תיאור |
| --------- | ------------------------------ | ----- |
| רכיבים    | ContentComponent לקריאה בלבד[] |       |
| מרחב שמות | מחרוזת                         |       |

### emitJekyllConfig

פונקציה **סוג:**

```typescript
function emitJekyllConfig(
  components: readonly ContentComponent[],
  namespace: string = DEFAULT_INCLUDE_NAMESPACE,
): string;
```

פולט את `_config.yml` קטע הרושם את שנוצר כולל.

Jekyll ממזגת את תצורת האתר לתוך `site.*`, אז פרסום מרחב השמות
וברירת המחדל לכל רכיב מאפשרת לפריסה לבנות נתיב כולל או
מלא ארגומנט חסר גם ללא קידוד קשיח. ברירות המחדל משוכפלות
מתוך כולל בכוונה: אתר שמציג רכיב מנתונים
(ולא מתוך מילולית `{% include %}`) אף פעם לא מפעיל את `default:`
מסננים, ובדרך אחרת יראו ערכים ריקים.

#### פרמטרים

| שם        | הקלד                           | תיאור |
| --------- | ------------------------------ | ----- |
| רכיבים    | ContentComponent לקריאה בלבד[] |       |
| מרחב שמות | מחרוזת                         |       |

### fieldToJekyllType

פונקציה **סוג:**

```typescript
function fieldToJekyllType(field: ContentField): JekyllFieldType;
```

סוג הנתונים של Jekyll שדה ניטרלי מתפרסם בתור.

#### פרמטרים

| שם  | הקלד         | תיאור |
| --- | ------------ | ----- |
| שדה | ContentField |       |

### JekyllFieldType

**סוג:** סוג

```typescript
export type JekyllFieldType = "string" | "number" | "boolean" | "enum" | "slot";
```

אוצר המילים של סוגי השדות הניטרליים מתפרסמים תחת `_data`.

### yamlScalar

פונקציה **סוג:**

```typescript
function yamlScalar(value: ContentDefaultValue): string;
```

Scalar של YAML עבור ערך ברירת מחדל ניטרלי.

#### פרמטרים

| שם  | הקלד                | תיאור |
| --- | ------------------- | ----- |
| ערך | ContentDefaultValue |       |

## `src/template`

### DEFAULT_INCLUDE_NAMESPACE

**סוג:** קבוע

```typescript
export const DEFAULT_INCLUDE_NAMESPACE;
```

מרחב השמות המשמש כאשר המתקשר לא בוחר אחד.

### emitLiquidInclude

פונקציה **סוג:**

```typescript
function emitLiquidInclude(
  component: ContentComponent,
  namespace: string = DEFAULT_INCLUDE_NAMESPACE,
): string;
```

פלט את Liquid include עבור רכיב אחד.

#### פרמטרים

| שם        | הקלד             | תיאור |
| --------- | ---------------- | ----- |
| רכיב      | ContentComponent |       |
| מרחב שמות | מחרוזת           |       |

### includeFileName

פונקציה **סוג:**

```typescript
function includeFileName(
  component: ContentComponent,
  namespace: string = DEFAULT_INCLUDE_NAMESPACE,
): string;
```

שם קובץ החפצים של רכיב כולל.

#### פרמטרים

| שם        | הקלד             | תיאור |
| --------- | ---------------- | ----- |
| רכיב      | ContentComponent |       |
| מרחב שמות | מחרוזת           |       |

### includePath

פונקציה **סוג:**

```typescript
function includePath(
  component: ContentComponent,
  namespace: string = DEFAULT_INCLUDE_NAMESPACE,
): string;
```

נתיב הכלול שרכיב מטופל בו, ביחס אליו `_includes`.

#### פרמטרים

| שם        | הקלד             | תיאור |
| --------- | ---------------- | ----- |
| רכיב      | ContentComponent |       |
| מרחב שמות | מחרוזת           |       |

### jekyllDiagnostics

פונקציה **סוג:**

```typescript
function jekyllDiagnostics(
  ir: SemanticModule,
  component: ContentComponent,
): readonly CompilerDiagnostic[];
```

אסוף את האבחון שמדווח על הורדת נוזלים עבור רכיב.

נוזל יכול לשאת כל סוג שדה ניטרלי - כל מה שמגיע לכלול הוא
מחרוזת בסופו של דבר - אז אין מה להזהיר מלבד אי בהירות
הנוזל עצמו אינו יכול לפתור: `include` הוא מרחב שמות שטוח אחד, כך ששם
משבצת שיתוף שם עם אבזר שאינו משבצת יקבל את הערך המחובר של המשבצת
מעובד במקום בו היה צפוי הסימון שלו.

#### פרמטרים

| שם   | הקלד             | תיאור |
| ---- | ---------------- | ----- |
| ir   | SemanticModule   |       |
| רכיב | ContentComponent |       |

### נוזלי ליטרל

פונקציה **סוג:**

```typescript
function liquidLiteral(value: ContentDefaultValue): string;
```

Liquid literal עבור ערך ברירת מחדל ניטרלי.

המחרוזות הן בציטוטים בודדים (המוסכמה של Liquid עצמה עבור ה `default:` מסנן
טיעון); המספרים והבוליאנים נשארים חשופים אז Liquid שומר על הסוג שלהם
מאשר לכפות את ברירת המחדל למחרוזת.

#### פרמטרים

| שם  | הקלד                | תיאור |
| --- | ------------------- | ----- |
| ערך | ContentDefaultValue |       |
