---
'@mission-platform/components': minor
---

add `removable` prop to `BaseTag` (default `false`) to make the inline remove (×) button opt-in, and add an `align` prop (`'start' | 'center' | 'end'`, default `'start'`) to `BaseNavbar` to control the alignment of the default-slot navigation items
