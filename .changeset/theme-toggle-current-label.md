---
'@mission-platform/components': patch
---

fix `base-theme-toggle` default label so it reflects the current theme (`Light mode` / `Dark mode` / `Auto mode`) instead of the next state in the cycle, matching the icon
