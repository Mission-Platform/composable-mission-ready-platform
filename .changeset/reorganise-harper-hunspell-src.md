---
'@mission-platform/harper': patch
'@mission-platform/hunspell': patch
---

reorganise package src layout into logical folders (`monaco/`, `worker/`, and — for hunspell — `wasm/`); the public package entry points and built output filenames are unchanged
