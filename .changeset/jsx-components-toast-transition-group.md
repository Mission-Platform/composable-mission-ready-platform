---
'@mission-platform/components': minor
---

animate `BaseToastContainer` with the neutral `<TransitionGroup name="base-toast">` primitive (matching the Vue SFC's `<TransitionGroup>`), adding the shared `base-toast-*` enter/leave/move transition classes
