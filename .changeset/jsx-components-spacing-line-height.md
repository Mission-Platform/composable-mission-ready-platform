---
'@mission-platform/components': minor
---

add token-driven `padding`/`margin` spacing props (named `2xs … 2xl` scale) to the layout primitives plus `BaseButton`/`BaseSeparator` (and outer `margin` to `BaseCard`), a responsive `minColumnWidth` auto-fit mode to `BaseGrid`, and a `lineHeight` prop to `BaseTypography`
