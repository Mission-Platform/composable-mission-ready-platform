---
'@mission-platform/components': minor
---

add `BaseCarousel` component — a horizontally-scrollable slide container with optional previous/next controls, indicator dots, looping behaviour, `v-model` support for the active slide index, keyboard navigation (ArrowLeft/ArrowRight/Home/End), pointer-based touch swipe with a configurable threshold, and pauseable `autoplay` (with `interval` and `pauseOnHover` options)
