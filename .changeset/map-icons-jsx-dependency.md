---
'@mission-platform/map': patch
---

consume `@mission-platform/icons` in the stories instead of the removed Vue `@mission-platform/icons` package (`IconRotateCCW`/`IconRotateCW` are now `IconRotateCcw`/`IconRotateCw`)
