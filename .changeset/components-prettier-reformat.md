---
'@mission-platform/components': patch
---

reformat stories and specs to match prettier-aligned eslint config; refactor `base-in-view` spec to avoid `unicorn/no-this-assignment` and switch `base-theme-toggle` spec to the `dataset` DOM API
