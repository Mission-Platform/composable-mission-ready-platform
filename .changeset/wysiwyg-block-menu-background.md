---
'@mission-platform/wysiwyg': patch
---

Give the `BaseWysiwygBlockMenu` popup list its own opaque surface background (with matching rounded corners) so the block-style dropdown always reads as a solid popup instead of appearing transparent over the content behind it.
