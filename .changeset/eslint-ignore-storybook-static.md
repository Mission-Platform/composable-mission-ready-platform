---
'@mission-platform/eslint-config': patch
---

Ignore the top-level `storybook-static/` build output (in addition to `.storybook/storybook-static/`) so lint runs don't traverse generated Storybook artifacts.
