---
'@mission-platform/page-meta': minor
---

add new `@mission-platform/page-meta` package providing standard page metadata generation (`<title>`, `<meta>`, `<link rel="canonical">`, `<link rel="alternate" hreflang>`, `<html lang>`) with a Vue 3 `usePageMeta` composable for reactive injection into `document.head`
