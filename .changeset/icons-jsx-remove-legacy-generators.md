---
'@mission-platform/icons': patch
---

remove the legacy `generate-icons`/`generate-stories` scripts (and the `stories:generate` package script) — they derived this package from the now-removed Vue `@mission-platform/icons` source, which no longer exists
