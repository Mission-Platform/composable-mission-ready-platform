---
'@mission-platform/hunspell': patch
---

correct `types` entry points in `package.json` to match the actual emitted declaration files (`./dist/index.d.ts` for the main entry and `./dist/worker/hunspell.worker.d.ts` for the `./worker` subpath)
