---
'@mission-platform/matrix-code': major
---

remove the embedded matrix decoder API

BREAKING CHANGE: remove the matrix decoder APIs and decoder FWS artifacts; use `@mission-platform/code-scanner` for decoding.