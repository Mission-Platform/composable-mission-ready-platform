---
'@mission-platform/components': patch
---

use a `<section>` element as the BaseCarousel root and drop the leading template comment so keyboard, hover, and tabindex behaviour reaches the wrapper element
