---
'@mission-platform/forge': minor
---

add a SolidJS adapter exposing the neutral framework primitives

The new `@mission-platform/forge/solid` subpath exports `Teleport`, `Transition`, and `TransitionGroup` built for SolidJS, so write-once components compiled to the Solid target can resolve their framework-component imports.
