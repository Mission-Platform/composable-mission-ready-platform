---
'@mission-platform/forms-core': minor
---

add the framework-agnostic forms core (JSON Schema → field derivation + Ajv validation, conditional-visibility evaluation, and the form-builder field ⇄ schema model) shared by the Vue `@mission-platform/components` and the write-once `@mission-platform/components` SchemaForm/FormBuilder
