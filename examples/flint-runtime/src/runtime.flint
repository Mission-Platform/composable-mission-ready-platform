import capability "clock.now" as now() -> i64;
import "./project-helper.flint" as project;
import "../shared-project/shared-value.flint" as shared;

export fn currentTime() -> i64 {
    return now();
}
