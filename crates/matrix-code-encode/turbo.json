{
  "$schema": "https://turborepo.com/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "command": [
        "sh",
        "-c",
        "wasm-pack build . --target bundler --release --weak-refs --reference-types --no-pack --out-dir ../../packages/matrix-code-encode-wasm/src/wasm --out-name matrix-code-encode"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!**/.turbo/**",
        "$TURBO_ROOT$/Cargo.toml",
        "$TURBO_ROOT$/Cargo.lock",
        "$TURBO_ROOT$/.cargo/config.toml",
        "$TURBO_ROOT$/crates/matrix-code-encode/**",
        "!$TURBO_ROOT$/crates/matrix-code-encode/.turbo/**",
        "$TURBO_ROOT$/crates/matrix-code-common/**",
        "!$TURBO_ROOT$/crates/matrix-code-common/.turbo/**"
      ],
      "outputs": [
        "$TURBO_ROOT$/packages/matrix-code-encode-wasm/src/wasm/**"
      ]
    },
    "test": {
      "dependsOn": [
        "^build"
      ],
      "command": [
        "wasm-pack",
        "test",
        "--node",
        "--release",
        "."
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!**/.turbo/**",
        "$TURBO_ROOT$/Cargo.toml",
        "$TURBO_ROOT$/Cargo.lock",
        "$TURBO_ROOT$/.cargo/config.toml",
        "$TURBO_ROOT$/crates/matrix-code-encode/**",
        "!$TURBO_ROOT$/crates/matrix-code-encode/.turbo/**",
        "$TURBO_ROOT$/crates/matrix-code-common/**",
        "!$TURBO_ROOT$/crates/matrix-code-common/.turbo/**"
      ],
      "outputs": []
    }
  }
}
