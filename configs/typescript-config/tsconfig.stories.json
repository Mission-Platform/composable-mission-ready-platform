{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Mission Platform — Stories (Storybook)",
  "extends": ["@vue/tsconfig/tsconfig.dom.json", "./tsconfig.base.json"],
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["vite/client"]
  }
}
