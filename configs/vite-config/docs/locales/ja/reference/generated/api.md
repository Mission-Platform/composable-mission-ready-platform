# @mission-platform/vite-config APIリファレンス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> configs/vite-config/docs/reference/generated/api.md: [configs/vite-config/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 言語: 日本語 (ja)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

のパブリック ソース宣言から生成 `@mission-platform/vite-config`.

## `src/index`

### AppConfigオプション

**種類:** インターフェース

```typescript
export interface AppConfigOptions
```

説明はありません。

### create外部マッチャー

**種類:** 関数

```typescript
function createExternalMatcher(names: readonly string[]): (id: string) => boolean;
```

ロールアップ/ロールダウンを作成する `external` すべての名前を扱う述語
`names` (およびそのサブパスインポートのいずれか。 `pkg/sub`) 外部として。

#### パラメータ

| 名前 | タイプ               | 説明 |
| ---- | -------------------- | ---- |
| 名前 | 読み取り専用文字列[] |      |

### DEFAULT_LIBRARY_EXTERNALS

**種類:** 定数

```typescript
export const DEFAULT_LIBRARY_EXTERNALS: readonly string[];
```

デフォルトのロールアップ外部は、すべての共有ライブラリがピア提供のものとして扱う必要があります。
ライブラリを使用するアプリは、これらを独自に提供することが期待されます。

### DEFAULT_LIBRARY_GLOBALS

**種類:** 定数

```typescript
export const DEFAULT_LIBRARY_GLOBALS: Readonly<Record<string, string>>;
```

UMD/IIFE コンシューマのデフォルトのロールアップ出力グローバル。 ESMのみを対象としていますが、
外部が宣言されている場合、ロールアップはこのマップなしでも警告を出します。

### アプリ構成の定義

**種類:** 関数

```typescript
function defineAppConfig(options: AppConfigOptions = {}): UserConfig;
```

を構築する Vite ミッションプラットフォームの設定 Vue 3 つのアプリ: Vue プラグインと
共有PostCSSパイプライン。アプリは独自のルーティング、PWA、ワーカー、および
{@link AppConfigOptions.overrides} によるバンドル調整。

#### パラメータ

| 名前       | タイプ               | 説明 |
| ---------- | -------------------- | ---- |
| オプション | アプリ構成オプション |      |

### フレームワークアプリ構成の定義

**種類:** 関数

```typescript
function defineFrameworkAppConfig(options: FrameworkAppConfigOptions): UserConfig;
```

{@link defineAppConfig} を次のコードでラップします。 `resolve.conditions` 裸にするために必要な
`@mission-platform/<pkg>` インポートは、選択された単一のフレームワーク ビルドに解決されます。
これは、コンシューマーが設定するアプリレベルのスイッチの 1 つです。そうでない外部プロジェクト
このヘルパーを使用すると同じように設定できます `resolve.conditions` 直接。

#### パラメータ

| 名前       | タイプ                    | 説明 |
| ---------- | ------------------------- | ---- |
| オプション | FrameworkAppConfigOptions |      |

### ライブラリ構成の定義

**種類:** 関数

```typescript
function defineLibraryConfig(options: LibraryConfigOptions): UserConfig;
```

を構築する Vite ミッションプラットフォームに合わせた構成 Vue ライブラリパッケージ:
の Vue プラグイン、共有 PostCSS パイプライン、ESM 専用ライブラリ出力、単一 CSS
バンドル、および賢明なピア依存関係の外部。

デフォルトでは、ビルドではソース モジュール グラフが保存されます。
({@link LibraryConfigOptions.preserveModules})、モジュールごとに 1 つのファイルを生成します
そのため、ダウンストリーム アプリはツリー シェイキングとコード分割の恩恵を受けます。
ボックス。単一のバンドルされたアーティファクトを必要とするパッケージは、呼び出しごとにオプトアウトできます。

#### パラメータ

| 名前       | タイプ                   | 説明 |
| ---------- | ------------------------ | ---- |
| オプション | ライブラリ構成オプション |      |

### 定義WebComponentAppConfig

**種類:** 関数

```typescript
function defineWebComponentAppConfig(options: AppConfigOptions = {}): UserConfig;
```

を構築する Vite フレームワークフリーの Web コンポーネント アプリケーションのアプリ構成。

{@link defineFrameworkAppConfig} とは異なり、このヘルパーは意図的に
をインストールします Vue プラグイン (または他のフレームワーク プラグイン)。の `mp:web-component`
条件は引き続きクライアントと SSR/プリレンダーの解決の両方に適用されるため、
裸の Forge パッケージのインポートでは、どこでもカスタム要素のアーティファクトが選択されます。

#### パラメータ

| 名前       | タイプ               | 説明 |
| ---------- | -------------------- | ---- |
| オプション | アプリ構成オプション |      |

### FrameworkAppConfigOptions

**種類:** インターフェース

```typescript
export interface FrameworkAppConfigOptions extends AppConfigOptions
```

説明はありません。

### フレームワークの状態

**種類:** 関数

```typescript
function frameworkCondition(framework: MissionPlatformFramework): string;
```

{@link MissionPlatformFramework} をカスタム エクスポート条件にマッピングします。
`@mission-platform/*` パッケージはその中で宣言します `exports` 地図。を維持する
`mp:` プレフィックスは条件に名前空間を与え、標準と衝突しないようにします。
Node/Vite 条件（`import`, `default`, `browser`, ...).

#### パラメータ

| 名前           | タイプ                                   | 説明 |
| -------------- | ---------------------------------------- | ---- |
| フレームワーク | ミッションプラットフォームフレームワーク |      |

### フレームワーク解決条件

**種類:** 関数

```typescript
function frameworkResolveConditions(framework: MissionPlatformFramework): string[];
```

注文したものを組み立てる `resolve.conditions` アプリが選択するために使用するリスト
フレームワークの構築。フレームワーク固有の条件が最初に配置されるため、それが優先されます。
ジェネリックを超えて `import`/`default` フォールバック、および標準ブラウザ/ESM
条件が追加されるため、他のすべては正常に解決されます。

結果を渡す Vite's `resolve.conditions` ({@link defineFrameworkAppConfig} を参照)。

#### パラメータ

| 名前           | タイプ                                   | 説明 |
| -------------- | ---------------------------------------- | ---- |
| フレームワーク | ミッションプラットフォームフレームワーク |      |

### ignoreVueI18nBlocksプラグイン

**種類:** 関数

```typescript
function ignoreVueI18nBlocksPlugin(): Plugin;
```

Vite 回転するプラグイン Vue SFC `<i18n>` カスタム ブロックを no-op モジュールに変換します。

プラットフォームの `<i18n>` ブロックには、によって消費される英語のソース文字列が保持されます。
の `i18next-cli` ツールのみ - 実行時の変換は
生成されたものからロードされる `src/locales/*.yaml` i18next経由でバンドルします。以来、
プロジェクトはもう使用しません vue-i18n カスタム ブロック コンパイラ、 `@vitejs/plugin-vue`
そうしないと、生の YAML ブロックが JS モジュールとして出力され、解析に失敗します。
このプラグインは、不活性なデフォルトのエクスポート (シェイプ
`@vitejs/plugin-vue` 期待しているので呼び出すことができます `block(component)` 無害です)。

### ライブラリ構成オプション

**種類:** インターフェース

```typescript
export interface LibraryConfigOptions
```

説明はありません。

### ミッションプラットフォームフレームワーク

**種類：**タイプ

```typescript
export type MissionPlatformFramework = 'vue' | 'react' | 'solid' | 'svelte' | 'web-component';
```

Mission Platform アプリがターゲットにできるフレームワークのセット。 1 つのドライブを選択する
両方 Vite バンドル（経由） `resolve.conditions`) そして — マッチングを通じて
`customConditions` tsconfig プリセット — TypeScript 言語サービスがあまりにも貧弱
`@mission-platform/<pkg>` インポートは、そのフレームワークのビルドされた出力に解決されます。

### readPackageDependencyNames

**種類:** 関数

```typescript
function readPackageDependencyNames(rootDirectory: string): string[];
```

読んでください `dependencies` そして `peerDependencies` package.jsonで宣言
にあります `rootDirectory`。ライブラリのランタイム依存関係を避けるために使用されます。
独自のバンドルなので、消費者はそれらを重複排除してツリーシェイクできます。

#### パラメータ

| 名前               | タイプ | 説明 |
| ------------------ | ------ | ---- |
| ルートディレクトリ | 文字列 |      |

## `src/vitest`

### 定義VitestConfig

**種類:** 関数

```typescript
function defineVitestConfig(options: VitestConfigOptions = {}): ViteUserConfig;
```

を構築する Vitest Mission Platform パッケージとアプリの構成。を提供します。
標準 Vue プラグイン、jsdom 環境、および v8 カバレッジプロバイダー
事前設定済み `src/**\/*.vue` コンポーネント。

#### パラメータ

| 名前       | タイプ              | 説明 |
| ---------- | ------------------- | ---- |
| オプション | VitestConfigOptions |      |

### VitestConfigOptions

**種類:** インターフェース

```typescript
export interface VitestConfigOptions
```

説明はありません。
