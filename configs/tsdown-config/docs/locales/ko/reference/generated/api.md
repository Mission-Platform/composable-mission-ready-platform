# @mission-platform/tsdown-config API 참조

정식 영어 원문을 기계 지원으로 번역한 문서입니다. 필요 시 사람이 검수하세요. 패키지 이름, 명령, 경로, 기술 식별자는 그대로 둡니다.

> configs/tsdown-config/docs/reference/generated/api.md: [configs/tsdown-config/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> 언어: 한국어 (ko)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

공개 소스 선언에서 생성됨 `@mission-platform/tsdown-config`.

## `src/index`

### createExternalMatcher

**종류:** 기능

```typescript
function createExternalMatcher(names: readonly string[]): (id: string) => boolean
```

롤다운 구축 `external` / `deps.neverBundle` 모든 것을 처리하는 술어
이름을 입력하다 `names` (그리고 하위 경로 가져오기 중 하나, 예: `pkg/sub`) 외부로.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 이름 | 읽기 전용 문자열[] |  |

### DEFAULT_LIBRARY_EXTERNALS

**종류:** 상수

```typescript
export const DEFAULT_LIBRARY_EXTERNALS: readonly string[]
```

모든 공유 라이브러리는 피어 제공으로 처리되어야 하는 기본 패키지 이름입니다.
거울 `@mission-platform/vite-config`의 {@link DEFAULT_LIBRARY_EXTERNALS}입니다.

### 정의Tsdown라이브러리

**종류:** 기능

```typescript
function defineTsdownLibrary(options: TsdownLibraryOptions): UserConfig
```

일반에 대한 tsdown 구성 구축 TypeScript (또는 단일 번들) 라이브러리 —
원형 A/B. 거울 `defineLibraryConfig` 외부화 의미론
`@mission-platform/vite-config`.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 옵션 | TsdownLibrary옵션 |  |

### 정의TsdownVue라이브러리

**종류:** 기능

```typescript
function defineTsdownVueLibrary(options: TsdownLibraryOptions): UserConfig
```

{@link DefineTsdownLibrary}와 비슷하지만 와이어 `unplugin-vue` 그리고 Vue-인식 dts
배송되는 패키지의 경우 `.vue` SFC(`i18n`, `router`, …).

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 옵션 | TsdownLibrary옵션 |  |

### readPackageDependencyNames

**종류:** 기능

```typescript
function readPackageDependencyNames(rootDirectory: string): string[]
```

읽기 `dependencies` 그리고 `peerDependencies` package.json에 선언됨
에 위치 `rootDirectory`. 라이브러리의 런타임 종속성을 유지하는 데 사용됩니다.
소비자가 중복을 제거하고 트리쉐이크할 수 있도록 자체 번들로 구성됩니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 루트디렉토리 | 문자열 |  |

### 해결TsdownOutput디렉터리

**종류:** 기능

```typescript
function resolveTsdownOutputDirectory(rootDirectory: string, outputDirectory: string, outputRoot?: string): string
```

최종 패키지 출력 경로를 격리된 빌드 루트로 미러링합니다. 유지
패키지 관련 접미사는 중립, 프레임워크, 이메일 및 CMS 출력을 만듭니다.
tsdown이 형제 트리를 정리하는 것을 허용하지 않고 독립적으로 승격 가능합니다.

#### 매개변수

| 이름 | 유형 | 설명 |
| --- | --- | --- |
| 루트디렉토리 | 문자열 |  |
| 출력디렉토리 | 문자열 |  |
| 출력루트 | 문자열 |  |

### TsdownLibrary옵션

**종류:** 인터페이스

```typescript
export interface TsdownLibraryOptions
```

설명이 제공되지 않았습니다.
