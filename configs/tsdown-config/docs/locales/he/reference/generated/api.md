# @mission-platform/tsdown-config הפניה ל-API

תרגום בסיוע מכונה מהמקור האנגלי הקנוני. יש לבדוק ידנית בעת הצורך. שמות חבילות, פקודות, נתיבים ומזהים טכניים נשארים ללא שינוי.

> configs/tsdown-config/docs/reference/generated/api.md: [configs/tsdown-config/docs/reference/generated/api.md](../../../../reference/generated/api.md)
> שפה: עברית (he)

<!-- Generated by scripts/extract-package-docs.ts. Do not edit. -->

נוצר מהצהרות מקור ציבורי ב `@mission-platform/tsdown-config`.

## `src/index`

### createExternalMatcher

פונקציה **סוג:**

```typescript
function createExternalMatcher(names: readonly string[]): (id: string) => boolean;
```

בנה Rolldown `external` / `deps.neverBundle` פרדיקט המטפל בכל
שם ב `names` (וכל מיובאת נתיב המשנה שלהם, למשל. `pkg/sub`) כמו חיצוני.

#### פרמטרים

| שם   | הקלד                 | תיאור |
| ---- | -------------------- | ----- |
| שמות | מחרוזת לקריאה בלבד[] |       |

### DEFAULT_LIBRARY_EXTERNALS

**סוג:** קבוע

```typescript
export const DEFAULT_LIBRARY_EXTERNALS: readonly string[];
```

שמות חבילות ברירת מחדל צריכים להתייחס לכל ספרייה משותפת כאל מתן עמיתים.
מראות `@mission-platform/vite-config`של {@link DEFAULT_LIBRARY_EXTERNALS}.

### defineTsdownLibrary

פונקציה **סוג:**

```typescript
function defineTsdownLibrary(options: TsdownLibraryOptions): UserConfig;
```

בנה תצורת tsdown עבור מישור TypeScript (או חבילה בודדת) ספרייה -
ארכיטיפ A/B. מראות `defineLibraryConfig` סמנטיקה החצנה מ
`@mission-platform/vite-config`.

#### פרמטרים

| שם       | הקלד                 | תיאור |
| -------- | -------------------- | ----- |
| אפשרויות | TsdownLibraryOptions |       |

### defineTsdownVueLibrary

פונקציה **סוג:**

```typescript
function defineTsdownVueLibrary(options: TsdownLibraryOptions): UserConfig;
```

כמו {@link defineTsdownLibrary}, אבל חוטים `unplugin-vue` ו Vue-מודע dts
עבור חבילות שנשלחות `.vue` SFCs (`i18n`, `router`, …).

#### פרמטרים

| שם       | הקלד                 | תיאור |
| -------- | -------------------- | ----- |
| אפשרויות | TsdownLibraryOptions |       |

### readPackageDependencyNames

פונקציה **סוג:**

```typescript
function readPackageDependencyNames(rootDirectory: string): string[];
```

קרא את `dependencies` ו `peerDependencies` הוצהר ב-package.json
ממוקם ב `rootDirectory`. משמש למניעת תלות זמן ריצה של ספרייה
של צרור משלו, כך שהצרכנים יוכלו לרמות אותם ולנער אותם.

#### פרמטרים

| שם            | הקלד   | תיאור |
| ------------- | ------ | ----- |
| rootDirectory | מחרוזת |       |

### resolveTsdownOutputDirectory

פונקציה **סוג:**

```typescript
function resolveTsdownOutputDirectory(rootDirectory: string, outputDirectory: string, outputRoot?: string): string;
```

שיקוף נתיב פלט סופי של חבילה לשורש בנייה מבודד. שמירה על
סיומת יחסית לחבילה הופכת פלטי נייטרלים, מסגרת, דואר אלקטרוני ו-CMS
ניתן לקידום עצמאי מבלי לאפשר ל-tsdown לנקות עץ אחים.

#### פרמטרים

| שם              | הקלד   | תיאור |
| --------------- | ------ | ----- |
| rootDirectory   | מחרוזת |       |
| outputDirectory | מחרוזת |       |
| outputRoot      | מחרוזת |       |

### TsdownLibraryOptions

**סוג:** ממשק

```typescript
export interface TsdownLibraryOptions
```

לא סופק תיאור.
