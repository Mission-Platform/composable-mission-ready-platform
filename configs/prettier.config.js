/* prettier Documentation */
/**
 * @mission-platform/prettier-config
 *
 * Mission Platform's Prettier configuration, based on:
 * - Standard JavaScript/TypeScript formatting
 * - Vue 3 component formatting
 * - Consistent code style across the monorepo
 *
 * Configuration:
 * - Use single quotes for strings
 * - Print width: 100 characters
 * - Tab width: 2 spaces
 * - End of line: lf
 * - Insert final newline
 */
import { plugin } from '@mission-platform/prettier-config';

export default plugin({}
  // Additional configuration can be added here
);