{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test:e2e": {
      "env": ["PLAYWRIGHT_BASE_URL"]
    },
    "build": {
      "dependsOn": ["^build:vue", "^build:forge"],
      "env": ["VITE_*"],
      "outputs": ["dist/**", ".output/**", ".vite/**", "public/sitemap.xml", "public/robots.txt"]
    },
    "build:spa": {
      "dependsOn": ["^build:vue", "^build:forge"],
      "env": ["VITE_*"],
      "outputs": ["dist/**", ".output/**", "public/sitemap.xml", "public/robots.txt"]
    },
    "dev": {
      "dependsOn": ["^build:vue", "^build:forge"],
      "cache": false,
      "persistent": true
    },
    "deploy": {
      "dependsOn": ["build", "@mission-platform/forge-spa#build"],
      "cache": false
    },
    "deploy:staging": {
      "dependsOn": ["build", "@mission-platform/forge-spa#build"],
      "cache": false
    },
    "version": {
      "dependsOn": ["build", "@mission-platform/forge-spa#build"],
      "cache": false
    },
    "version:staging": {
      "dependsOn": ["build", "@mission-platform/forge-spa#build"],
      "cache": false
    }
  }
}
