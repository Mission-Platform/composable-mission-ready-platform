{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "type-check", "build:ui", "build:main"],
      "env": ["VITE_*"],
      "outputs": ["dist/**", ".vite/**"]
    },
    "build:ui": {
      "dependsOn": ["type-check"],
      "env": ["VITE_*"]
    },
    "build:main": {
      "dependsOn": ["build:ui"],
      "env": ["VITE_*"]
    }
  }
}
