# WebLua Lua 5.5.1 互換性マトリックス

正規の英語ソースからの機械支援翻訳です。必要に応じて人手で確認してください。パッケージ名、コマンド、パス、技術識別子は変更しません。

> docs/web-lua-compatibility.md: [docs/web-lua-compatibility.md](../../web-lua-compatibility.md)
> 言語: 日本語 (ja)

このレポートは意図的に保守的です。 `matched` は、その動作がゲストレベルのフィクスチャによってカバーされており、決定的な期待される結果があることを意味します。 `capability-gated` は、ホスト効果には明示的なポリシーが必要であることを意味します。 `unresolved` は、動作は追跡されていますが、通過したものとして扱ってはいけないことを意味します。

|エリア |行動 |ステータス |証拠 |メモ |
| ----------------------- | ----------------------------------------------------------------------------- | ---------------- | -------------------------------------------- | -------------------------------------------------------------------------------------- |
|字句構文 |空白、コメント、キーワード、整数リテラル、演算子 |一致しました | `packages/web-lua/src/differential.spec.ts` |実装されたスカラー サブセットのみが要求されます。                                         |
|スカラー式 |整数算術、単項マイナス、グループ化、優先順位、および比較 |一致しました | `packages/web-lua/src/differential.spec.ts` |結果には現在のゲスト スカラー ABI が使用されます。                                              |
|ローカルと制御フロー |ローカル割り当て、再割り当て、`if`/`else`、`while`、および戻り値 |一致しました | `packages/web-lua/src/differential.spec.ts` |ゲストのローカル容量とスタック容量は明示的な制限のままです。                               |
|名前付き関数 |名前付き定義、パラメータ、呼び出し、およびスカラー戻り値 |一致しました | `packages/web-lua/src/differential.spec.ts` |クロージャ、上位値、可変引数、末尾呼び出し、および複数の戻り値は、この行の外側に残ります。 |
|エラーと読み込み |構文、実行時、除算、および不正な形式のバイナリ プレフィックス ステータス |一致しました | `packages/web-lua/src/utils/web-lua.spec.ts` |ステータスはホスト側の Lua 解釈なしで比較されます。                            |
|ホスト側ライブラリ | I/O、クロック、ランダム性、OS、パッケージのロード、およびデバッグ効果 |機能ゲート型 | `packages/web-lua/src/utils/web-lua.spec.ts` |機能はデフォルトで拒否されます。ライブラリの実装が不完全です。              |
|値とテーブル |文字列、浮動小数点数、テーブル、ユーザーデータ、ID、反復、メタメソッド |未解決 | `packages/web-lua/src/utils/web-lua.spec.ts` |現在の境界は、スカラー値と 1 エントリのテーブル基盤を公開します。           |
|クロージャとコルーチン |上位値、`yield`/`resume`、保護された呼び出し、およびネストされたコルーチン エラー |未解決 | `packages/web-lua/src/utils/web-lua.spec.ts` | `resume` は現在プロトタイプを再実行しており、コルーチン セマンティクスとして主張されていません。  |
|標準ライブラリ |ベース、コルーチン、テーブル、文字列、UTF-8、数学、I/O、OS、デバッグ、およびパッケージ/ロード |未解決 |標準ライブラリの差動フィクスチャなし |ライブラリの動作は暗黙的に通過として扱われることはありません。                                    |

このレポートの生成されたソースは、`packages/web-lua/src/compatibility.ts` の型付き行列です。そのテストでは、すべての行に明示的な分類と証拠の入力が必要です。
