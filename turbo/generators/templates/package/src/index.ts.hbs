/**
 * @mission-platform/{{ dashCase name }}
 *
 * {{ description }}
 */

export interface {{ pascalCase name }}Options {
  readonly enabled?: boolean;
}

export function create{{ pascalCase name }}(options: {{ pascalCase name }}Options = {}): string {
  const isEnabled = options.enabled ?? true;
  return `{{ dashCase name }}:${isEnabled ? 'enabled' : 'disabled'}`;
}
